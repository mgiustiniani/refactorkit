# v0.7.0 Java JDT change-signature acceptance

Status: bounded JDT parameter rename/type-change/add/remove/reorder are qualified
through packaged CLI/daemon/MCP execution on all four native platforms.

## Qualified behavior

The planner resolves an exact JDT method binding, including a signed overload
selector, and publishes one parameter binding with exact declaration/use ranges.
Only those ranges are edited. Same-name fields, other overloads, comments and
literals remain unchanged.

Staged JDT analysis must re-establish method signature, parameter index, new name
and usage count. Introduced error diagnostics refuse while unchanged baseline
errors remain tolerated. Preview remains read-only; apply and rollback use the
normal `PatchEngine` and WAL transaction path.

## Local evidence

- 709 JVM tests, zero failures/errors/skips;
- exact parameter declaration plus two bound-use extraction;
- signed overload selection and same-name field/overload exclusion;
- malformed, unsigned-overload and missing-parameter refusal coverage;
- packaged CLI, daemon and MCP preview, apply and byte-identical rollback using
  the embedded JDK;
- full existing golden suite remains green.

## Native evidence

GitHub Actions run `29607477633` passed packaged parameter rename on Linux
x86-64, Windows x86-64, macOS x86-64 and macOS arm64. Run `29614333900`
qualified packaged CLI/daemon/MCP rename/add/remove/reorder with exact rollback on
the same matrix. Run `29618448454` qualified parameter type change on all four
platforms. Run `29621466959` qualified hierarchy-family add across packaged
CLI/daemon/MCP on the same matrix.

## Expanded local rows

JDT now publishes exact method parameter-list and bound invocation argument-list
ranges. Add/remove/reorder update only the selected signed overload and its JDT
call sites, then recompile a disposable staged source overlay. Public/protected,
interface, override, method-reference and incomplete shapes refuse.

## Parameter type-change local row

One exact regular parameter type token can change when the method is
package/private and outside hierarchies. Staged JDT must retain every original
call-site location and parameter body-use count. Incompatible callers/bodies,
overload drift, annotations, arrays and varargs refuse. CLI/daemon/MCP apply and
rollback restore exact bytes locally.

## Override-family local row

`addParameter` with `includeHierarchy=true` and explicit external-consumer-risk
acceptance updates every JDT-connected source interface/override declaration and
all calls bound to the family. Disposable staged JDT must retain a connected
family and per-file call counts. Missing external declarations refuse. Local
packaged CLI/daemon/MCP apply and exact rollback pass.

Hierarchy remove-parameter passes across declarations with different parameter
names on all four native platforms (run `29624918322`): the selected index must be unused in every body, and the same
argument index is removed from all family calls. Packaged CLI/daemon/MCP exact rollback passes.

Hierarchy reorder passes library and packaged CLI/daemon/MCP acceptance on all
four native platforms (run `29626304459`). The selected declaration's
requested names become an index permutation applied to every family declaration
and bound call, so implementation parameter names may differ.

Hierarchy parameter-type change passes library and packaged CLI/daemon/MCP
acceptance on all four native platforms (run `29628955986`). It changes the
same selected parameter index in every source-complete declaration while staged
JDT preserves family connectivity, call-site identity, and each body-use count.

A bounded private-constructor row passes library and packaged CLI/daemon/MCP
acceptance on all four native platforms (run `29634807550`). JDT now
publishes exact class-creation, `this(...)`, and `super(...)` invocation argument
ranges, enabling rename/add/remove/reorder/type-change staging for `<init>` while
constructor references and public constructors remain refused.

Library and packaged CLI/daemon/MCP acceptance on all four native platforms
(run `29640122922`) also covers private generic rename/remove/reorder,
single-explicit-argument varargs rename/remove/reorder, and annotation-preserving
parameter reorder. Broader forms continue to refuse.

Public ordinary constructors pass library and packaged CLI/daemon/MCP explicit-risk
acceptance on all four native platforms (run `29642091983`) and update bound
creation calls; omission of risk acceptance refuses. Record constructor mutation
remains fail-closed pending a record-component-coordinated operation.

Library and packaged CLI/daemon/MCP matrix coverage on all four native platforms
(run `29643185758`) proves exact overload isolation, bound calls
inside lambda bodies, pre-plan method/constructor-reference refusal, and
zero-partial-edit refusal for multi-expanded varargs calls.

Library and packaged CLI refusal acceptance on all four native platforms
(run `29646244081`) proves JDT-only public-constructor
and hierarchy operations refuse mixed Java/Kotlin workspaces before planning, even with explicit risk acceptance; this prevents
silent Kotlin caller breakage until shared K2/JDT caller evidence exists.

Packaged CLI acceptance on all four native platforms (run `29647225014`) also
verifies byte-identical pre-plan refusal for method
references and record canonical constructors; Windows additionally runs the
method-reference refusal through PowerShell packaging.

Bounded static-method and constructor references now pass library and packaged
CLI/daemon/MCP migration on all four native platforms (run `29651568518`):
`addParameter` rewrites them to explicit lambdas only with
`migrateFunctionalReferences=true`, then staged JDT proves the generated calls.

The initial shared Java/Kotlin `addParameter` row passes library and packaged
CLI/daemon/MCP apply and rollback. ECJ→K2 proves Kotlin external callable uses, JDT proves Java declaration
and calls, and staged K2/JDT diagnostics plus callable identity/count gate the
combined patch.

## Pending promotion gates

- four-platform native shared-caller qualification.
