# v0.7.0 Kotlin organize-imports acceptance

Status: bounded non-aliased row complete across all acceptance layers.
FIR-proven external/source type aliases have local and packaged CLI/daemon/MCP
acceptance; four-platform qualification is pending.

## Qualified local row

`KotlinOrganizeImportsPlanner` accepts one authoritative non-generated saved
`.kt` file with an uncommented contiguous block of explicit non-aliased type
imports. Complete K2 usage evidence proves each import target. A directive is
removed only when its sole resolved target usage is its own import token.
Retained directives are sorted by complete trimmed directive text.

Preview emits one text edit, preserves LF/CRLF and all non-import bytes, then
recompiles the staged overlay. Apply uses `PatchEngine`; rollback restores the
original bytes.

## Executable evidence

```text
681 JVM tests
0 failures
0 errors
0 skipped
23 golden cases
Native library run 29566422517: all four supported platforms passed
Packaged transport run 29571167892: all four supported platforms passed
```

Packaged acceptance includes read-only CLI preview, MCP preview/apply/rollback and
daemon preview/apply/rollback with CRLF byte restoration.

Alias coverage includes FIR alias-to-external-type identity, retained alias uses,
unused alias removal, deterministic sorting and CRLF rollback.

Coverage includes:

- external type import evidence without fabricated non-import usage;
- CRLF removal and sorting with staged K2 diagnostics;
- apply and byte-exact rollback;
- refusal when a comment is attached to the import block.

## Remaining gates

- star/callable imports and project-style-aware formatting.
