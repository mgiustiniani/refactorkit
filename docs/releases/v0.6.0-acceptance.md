# RefactorKit v0.6.0 Acceptance Record

Status: source acceptance complete; immutable tag publication failed and is not a published release. Superseded by publication patch `v0.6.1`.

Date: 2026-07-13.

## Release scope

`v0.6.0` publishes the first deep non-Java adapter slice: explicit-toolchain
TypeScript/JavaScript semantic reads and managed identifier rename. Advanced T5
operations are explicitly deferred to `v0.7.x`. API remains `0.2`.

## Pre-tag evidence

| Gate | Evidence | Result |
|---|---|---|
| Version | source and packaged runtime report implementation `0.6.0`; API `0.2` | PASS |
| Build | clean `./gradlew build` | PASS |
| Golden | `goldenTest` | PASS, 23/23 |
| JVM suites | Kotlin/JVM unit and integration tests | PASS, 548 tests before final stamp |
| Runtime matrix | Linux x86-64, Windows x86-64, macOS x86-64, macOS arm64 CI | PASS |
| Toolchain | Node 22.18.0, TLS 5.1.3, TypeScript 5.9.3, lockfile and scripts disabled | PASS |
| Semantic reads | stable IDs, search, definition, references, exact compiler diagnostics | PASS |
| Managed rename | path alias plus re-export, three-file exact proposal/apply | PASS |
| Transactions | explicit approval, WAL, rollback and source hash restoration | PASS |
| Semantic crash | real language-server kill and bounded provenance-preserving restart | PASS |
| Write crash | 111-file rename, kill at durable `APPLYING`, startup compensation to `ROLLED_BACK` | PASS |
| Hostile inputs | package identity, version, path, symlink, metadata, stream/frame/document/result limits | PASS |
| Integrations | CLI, daemon, MCP managed routing; LSP client-managed ownership | PASS |
| Distribution | archive structure, architecture, modules, permissions, checksum and launcher verification | PASS |
| Supply chain | package lock integrity, Apache-2.0 inputs, no package scripts, SBOM/attestation workflow | PASS |
| Diff hygiene | `git diff --check` | PASS |

Latest pre-release native CI evidence before version stamping:

- managed semantic and transaction qualification: run `29285373205`;
- prior exact diagnostics/restart qualification: run `29282349420`.

The final version-stamped commit reruns every gate. Tag jobs rebuild all assets
from the immutable commit, run real-toolchain and kill-during-write acceptance on
each platform, generate checksums/SBOMs/attestations, and independently verify
archives before publication.

## Publication outcome

Immutable tag `v0.6.0` points to release commit
`fb236f5b181f9a7146cfb051d11e0f729b7f9107`.

- [x] branch release-candidate CI `29288652510` passed all native targets;
- [x] tag workflow `29289311268` preserved successful Linux and both macOS jobs;
- [ ] Windows release job failed in the combined clean/build/package step before
  platform assets were uploaded;
- [ ] publish job was correctly skipped, so no partial GitHub release exists.

The tag remains immutable and is not moved or recreated. The workflow is corrected
by separating clean build/test from package/native smoke, and the same accepted
feature scope is published as patch version `v0.6.1`. See
[`v0.6.1-acceptance.md`](v0.6.1-acceptance.md).
