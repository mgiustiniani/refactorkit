# RefactorKit v0.5.0 Acceptance Record

Status: complete. Immutable tag, four-platform release workflow, independent
archive verification, checksums, SBOMs, and attestations passed.

Date: 2026-07-13.

## Pre-tag evidence

| Gate | Evidence | Result |
|---|---|---|
| Version | Packaged CLI reports implementation `0.5.0` and API `0.2` | PASS |
| Build | `./gradlew build` | PASS |
| Product | `goldenTest` | PASS, 23/23 |
| Tests | Kotlin/JVM unit and integration suites | PASS, 491 tests before final stamp |
| Runtime matrix | Linux x86_64, Windows x86_64, macOS x86_64, macOS arm64 native CI | PASS |
| Transactions | Native preview/apply/WAL/recovery/rollback and collision acceptance | PASS |
| Build model | Maven effective reactor, Gradle declarative, typed/redacted bounded DTOs | PASS |
| Java reactor | Java 21 multi-module scan, diagnostics, references and `moveSourceRoot` | PASS |
| Reference workspace | Magrathea five-module Java 21 reactor, read-only | PASS, zero diagnostics |
| Distribution trust | Hostile archive fixtures and native extracted launcher verification | PASS |
| Multi-language registry | deterministic routing, ambiguity and evidence-authority tests | PASS |
| External lifecycle | limits, environment, provenance, timeout, cancellation, tree kill | PASS |
| External edits | source overlay, strict LSP schema and core normalization/refusal | PASS |
| Structural parser | packaged native TypeScript/JavaScript grammars and CLI smoke | PASS |
| Capability schema | CLI, daemon, LSP and MCP schema v1 contract tests | PASS |
| Diff hygiene | `git diff --check` | PASS |

The final version-stamped commit reruns all gates before tagging. Native tag jobs
then rebuild every asset from the immutable commit and independently verify the
downloaded release inputs before publication.

## Publication evidence

Immutable tag `v0.5.0` points to release commit
`175a5a42cce9f2f8d75685d599ff2eabdcd92847`.

- [x] branch release-candidate CI `29218871939` passed on all native targets;
- [x] tag release workflow `29219153941` passed all four native builders and the
  independent publish job;
- [x] final checksums verified;
- [x] per-platform SPDX JSON SBOMs published;
- [x] build and SBOM attestations published;
- [x] independently downloaded archives passed safe-layout, module, architecture,
  permissions, checksum, and launcher verification;
- [x] GitHub release records unsigned macOS and Windows status explicitly;
- [x] independently re-downloaded Linux asset passed the hostile archive verifier
  and launcher execution with SHA-256
  `3554a312103e3e5c59ad73e89f4ade03d2e5e762018ceb83927dc88795de13e2`.

A failed tag workflow does not permit moving or recreating the tag. Corrective
work receives a new patch version.
