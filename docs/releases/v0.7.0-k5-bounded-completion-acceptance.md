# v0.7.0 K5 bounded completion acceptance

## Current local disposition — r007

The [final local report](v0.7.0-final-local-acceptance.md) records fresh exact24
completion and25 move-next source cases, three actual packaged smokes, both existing
finalizers PASSED, and independent post-native `PASS_FOR_PROMOTION` for Linux
x86-64. Integration005 and approved011–013 pins remain unchanged. The test-only
successor retains explicit source/runtime joins, not invented reruns. Other hosts
remain waived/not verified; no global Kotlin or published-release claim follows.
[ARC42](../arc42/appendix-requirements.adoc) owns integrated status; earlier receipts
below remain historical.

Status: local candidate; current immutable-candidate receipt and independent post-native review pending. The repeated four-platform matrix is `WAIVED_BY_USER` for 0.7.0 only.

## Refreshed packaged evidence — r003

After the independently reviewed I1 staging-ownership correction, the exact
**24 source cases/five suites** pass again, with no missing, duplicate, unexpected,
skipped or failed cases. Completion, legacy compatibility and separately scoped
move-next smokes all pass on the newly built archive, SHA-256
`a614fcc247078c23b75f36168bb5cd1b0225d2c38e0ca485009017ae7715e9a4`.
Existing compiler artifacts are borrowed, not copied. Archive verification and
packaged kill recovery pass; the new core passes125 tests and testkit33 goldens.
The [r003 record](../../.refactorkit/runs/v070-native-closure-r003/summary.md)
retains exact source, archive, commands and logs.

Unlike the earlier T5-only archive refresh, this archive changes the core JAR.
Earlier byte-equivalence and smoke receipts are therefore not substituted for
these new executions. Integration005 and its `PASS_FOR_RESEAL` disposition remain
unchanged. These local passes do not supply a clean-candidate receipt or final
independent post-native K5 promotion. ARC42 owns the integrated status.

## Earlier local integration — 2026-09-09

[Integration change 005](../requirements/kotlin-k5-bounded-completion-approved-change-005.md)
reconciles the stale parameter-rename pin with already-approved supersessions
011–013 and corrects attribution of the four REQ-001 cases to 012 alone. The
independent follow-up is **PASS_FOR_RESEAL**, not PASS_FOR_PROMOTION. All nine
finalizer contract tests pass; the original failed check and first rejected
integration review are preserved.

Exactly **24 source cases in five suites** pass without missing, duplicate,
unexpected, skipped or failed cases. Both required packaged completion and legacy
compatibility smokes pass, as does the separately scoped move-next packaged smoke.
No compiler artifacts were copied; existing pinned inputs were borrowed. These
are local uncommitted-source receipts, not a new sealed native qualification.

A later T5 refusal-metadata correction changes only
`TypeScriptSourceFileRelocationPlanner.class` in the refreshed runtime archive.
All seven K5 subject JARs and the embedded runtime remain identical; original
packaged smoke logs retain their original package identity and are not relabeled
as new executions. A clean immutable candidate and independent post-native
acceptance are still required. The [host waiver](../requirements/v0.7.0-local-host-acceptance-approved-change-001.md)
supersedes repeated four-host clauses below, never local safety or candidate
integrity. See [current evidence](../../.refactorkit/runs/v070-direct-closure-r002/summary.md)
and the integrated [ARC42 narrative](../arc42/appendix-requirements.adoc).

## Earlier candidate record

## Scope

This gate covers only the five non-move K5 rows bound by
`docs/requirements/kotlin-k5-bounded-completion.md` (sealed SHA-256
`f09b59f1d4861076e7d826c8c51c59e5048b3a04676a062822fdbc19ca6e1bce`):

1. callable import organization and bounded project import layout;
2. parameter-name-only Kotlin/JVM change signature;
3. bounded integer-expression extract and inline;
4. read-only advanced Kotlin/JVM declaration-shape facts; and
5. separate fail-closed platform/plugin/generated/framework matrices.

The separately sealed move-next gate remains independent. This candidate does not
close or broaden it.

## Local executable evidence

The exact 24-case source oracle passed on OpenJDK 21 with zero failures, errors or
skips. It covers nine import cases, FIR override/named-argument/external-boundary
facts, capture-risk and external-risk refusal, mixed K2/JDT Java-caller retention,
exact managed diagnostics selection, compiler-PSI extract/inline apply and
rollback, imported-helper and generated-source refusal, advanced shape facts,
delegated-property refusal and Android/Multiplatform/plugin matrices.

`python3 scripts/test-finalize-native-k5-completion.py` passes nine finalizer
contract tests. The managed-diagnostics BDD passes 13 exact route cases,
including Kotlin-only affected files selecting the lazy mixed K2/JDT
parameter-rename provider. Both Java CLI catalogue Cucumber oracles pass on
their explicitly qualified local-Linux boundary; their exact K5 capability/help
resources and glue are subject-hashed in native receipts but are not relabeled as
cross-platform catalogue qualification. `scripts/smoke-packaged-k5-completion.py` passes against
`packageCliRuntime` locally and ends with its exact marker after:

- four read-only packaged CLI previews;
- daemon organize-imports, parameter-rename and extract preview/apply/rollback;
- MCP inline preview/apply/rollback;
- exact byte restoration;
- packaged advanced-shape catalogue execution; and
- no-write enum/property/typealias, generated-root and Maven plugin refusals.

The pre-existing `scripts/smoke-packaged-kotlin.py` also passes, proving that
operation-less shared add-parameter arguments still select the qualified legacy
mode while the new old/new-name arguments select bounded rename.

These local results are not native or promotion evidence.

## Superseded candidates and non-weakening corrections

Candidate `285da48f368a3f220bfe4c8e1e15af2487ed4982` produced four passed
completion receipts in run `31421721819`, but an independent pre-native
adversarial audit found cleanly compiling Java-static-field import rebound,
parameter capture, imported-helper capture and generated-source mutation. The
audit and executable probes are hash-bound under `docs/requirements/evidence/`.
No roadmap row was promoted and those receipts are historical only.

Approved change 001 expands the oracle from 18 to 22 cases. External Java fields
without exact modeled field identity now make usage evidence incomplete;
parameter rename refuses a pre-existing proposed-name token in an affected
source; extract proves the inserted call targets the new helper; and extract and
inline require authoritative non-generated ownership. The dedicated native job
also hash-binds the legacy packaged Kotlin smoke on every host.

A second independent audit of local candidate
`ce65175a4aed6c12920792dd283a6a4480272c86` found external enum-entry and
aliased-property rebound, a custom Maven build-helper generated root, and an
unprojected Maven all-open compiler plugin. No native run or promotion followed.
Approved change 002 adds typed incomplete-evidence refusals for enum/property
alias usage, marks effective Maven roots below bounded output directories as
generated, projects Maven compiler-plugin/kapt/KSP declarations into the
existing refusal matrix, expands the oracle to 23 cases, and subject-hashes the
pinned K5 CLI compatibility resources and their local-Linux glue. The corrected
exact 23-case GREEN is
`docs/requirements/evidence/v0.7.0-k5-completion-second-audit-23-case-green-07516d18391337648be2f27e0ef85a25c265b2d4ef1708489c1ed4d1394ea926.log`.

A third independent audit rejected candidate
`a8822c28070956f56936bdc7b82888fc4a54ed06` because user typealias expansion
could disappear from import evidence and Kotlin Maven `-Xplugin` arguments were
not projected into the plugin refusal. The audit is preserved at
`docs/requirements/evidence/v0.7.0-k5-pre-native-audit-a8822c2-fail-d1dee7d777b99817192d8f039c7b2a0e5dbeef092861f8f471232286acb43d2e.md`.
No native run or promotion followed. Approved change 003 adds typed user-typealias
and stable `external-xplugin` refusals and expands the oracle to 24 cases. The
corrected exact GREEN is
`docs/requirements/evidence/v0.7.0-k5-completion-typealias-xplugin-24-case-green-33f640f4015c6b0555febeea3a9be0c135e0e9c0887f8447c7f5cfa182f92c8e.log`.

A fourth independent audit rejected candidate
`e0ffc264d851d6538b665d9be7dba99c3e8dee53`: only the outer resolved FIR type
was checked, so user and stdlib aliases nested inside generic arguments could
still disappear and cleanly change JVM generic signatures. The audit is
preserved at
`docs/requirements/evidence/v0.7.0-k5-pre-native-audit-e0ffc26-fail-956bcf46f9d0292500df189b681c128beecb2051d6cf3f2653178744cac723f6.md`.
No native run or promotion followed. Approved change 004 recursively bounds type
arguments, strengthens the existing 24-case oracle in place, and adds a separate
typed depth-limit refusal. The corrected exact GREEN is
`docs/requirements/evidence/v0.7.0-k5-completion-nested-typealias-24-case-green-1a14d85b2fc7c2c14cdcd2332b18896a2dac41c48cbf23ffd46968e12f6e5459.log`.

## Safety boundary

All writes remain `PatchPlan` → explicit approval → operation-owned diagnostics
→ `PatchEngine`/WAL. Incomplete, stale, generated, plugin-dependent, ambiguous or
unsupported evidence refuses without edits. Public parameter-name changes require
explicit external-consumer-risk acceptance. Java positional calls are proven
against staged Kotlin output and retain the exact JVM owner/name/descriptor.
A proposed parameter name already present in an affected source refuses rather
than attempting incomplete capture analysis. Unmodeled external JVM fields,
enum entries, property aliases, and compiler-proven typealiases at any bounded
type-argument depth make the complete usage catalogue unavailable rather than
appearing unused. Effective Maven
generated roots and declared compiler plugins, including `-Xplugin` arguments,
remain distinct mutation refusals.

Extract/inline is intentionally much narrower than general data/control-flow
analysis: only a compiler-PSI-proven, top-level, zero-input integer expression is
accepted. Advanced shapes carry read-only facts and no mutation authority.
Delegated properties, Android, Multiplatform/`expect`/`actual`, generated/plugin
and framework-aware semantics remain typed refusals.

## Native and promotion gate

Workflow job: `k5-bounded-completion-native` in `.github/workflows/ci.yml`.
Required hosts are Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64,
all at exact Temurin/OpenJDK `21.0.11+10` for host and embedded runtime.

`scripts/finalize-native-k5-completion.py` requires one clean non-merge exact
revision, immutable requirement hashes, exactly five focused JUnit reports and
24 exact cases, terminal K5-completion and legacy-Kotlin packaged markers, seven
exact subject JARs and all subject files. Receipts do not imply a green whole repository, installed assets,
SBOM/signing or release publication.

No listed roadmap row may change until all four receipts pass and independent
post-native review returns `PASS_FOR_PROMOTION`. Any defect requires a new
immutable candidate and rerun; no subset of hosts or operations is promoted.
