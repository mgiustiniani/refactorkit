# v0.7.0 K5 bounded completion acceptance

Status: local candidate; four-platform receipts and independent post-native review pending.

## Scope

This gate covers only the five non-move K5 rows bound by
`docs/requirements/kotlin-k5-bounded-completion.md` (sealed SHA-256
`c7563bbfa0737d27d69aa9cda8ae6249f6886b298b81d6a1330e981fd788ed4a`):

1. callable import organization and bounded project import layout;
2. parameter-name-only Kotlin/JVM change signature;
3. bounded integer-expression extract and inline;
4. read-only advanced Kotlin/JVM declaration-shape facts; and
5. separate fail-closed platform/plugin/generated/framework matrices.

The separately sealed move-next gate remains independent. This candidate does not
close or broaden it.

## Local executable evidence

The exact 22-case source oracle passed on OpenJDK 21 with zero failures, errors or
skips. It covers seven import cases, FIR override/named-argument/external-boundary
facts, capture-risk and external-risk refusal, mixed K2/JDT Java-caller retention,
exact managed diagnostics selection, compiler-PSI extract/inline apply and
rollback, imported-helper and generated-source refusal, advanced shape facts,
delegated-property refusal and Android/Multiplatform/plugin matrices.

`python3 scripts/test-finalize-native-k5-completion.py` passes nine finalizer
contract tests. The managed-diagnostics BDD passes 13 exact route cases,
including Kotlin-only affected files selecting the lazy mixed K2/JDT
parameter-rename provider. `scripts/smoke-packaged-k5-completion.py` passes against
`packageCliRuntime` locally and ends with its exact marker after:

- four read-only packaged CLI previews;
- daemon organize-imports, parameter-rename and extract preview/apply/rollback;
- MCP inline preview/apply/rollback;
- exact byte restoration; and
- packaged advanced-shape catalogue execution.

The pre-existing `scripts/smoke-packaged-kotlin.py` also passes, proving that
operation-less shared add-parameter arguments still select the qualified legacy
mode while the new old/new-name arguments select bounded rename.

These local results are not native or promotion evidence.

## Superseded candidate and non-weakening correction

Candidate `285da48f368a3f220bfe4c8e1e15af2487ed4982` produced four passed
completion receipts in run `31421721819`, but an independent pre-native
adversarial audit found cleanly compiling Java-static-field import rebound,
parameter capture, imported-helper capture and generated-source mutation. The
audit and executable probes are hash-bound under `docs/requirements/evidence/`.
No roadmap row was promoted and those receipts are historical only.

Approved change 001 expands the oracle from 18 to 22 cases. External Java fields
without exact modeled field identity now make usage evidence incomplete;
parameter rename refuses a pre-existing proposed-name token in an affected
source; extract proves the inserted call targets the new helper; and extract and
inline require authoritative non-generated ownership. The dedicated native job
also hash-binds the legacy packaged Kotlin smoke on every host.

## Safety boundary

All writes remain `PatchPlan` → explicit approval → operation-owned diagnostics
→ `PatchEngine`/WAL. Incomplete, stale, generated, plugin-dependent, ambiguous or
unsupported evidence refuses without edits. Public parameter-name changes require
explicit external-consumer-risk acceptance. Java positional calls are proven
against staged Kotlin output and retain the exact JVM owner/name/descriptor.
A proposed parameter name already present in an affected source refuses rather
than attempting incomplete capture analysis. Unmodeled external JVM fields make
the complete usage catalogue unavailable rather than appearing unused.

Extract/inline is intentionally much narrower than general data/control-flow
analysis: only a compiler-PSI-proven, top-level, zero-input integer expression is
accepted. Advanced shapes carry read-only facts and no mutation authority.
Delegated properties, Android, Multiplatform/`expect`/`actual`, generated/plugin
and framework-aware semantics remain typed refusals.

## Native and promotion gate

Workflow job: `k5-bounded-completion-native` in `.github/workflows/ci.yml`.
Required hosts are Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64,
all at exact Temurin/OpenJDK `21.0.11+10` for host and embedded runtime.

`scripts/finalize-native-k5-completion.py` requires one clean non-merge exact
revision, immutable requirement hashes, exactly five focused JUnit reports and
22 exact cases, terminal K5-completion and legacy-Kotlin packaged markers, seven
exact subject JARs and all subject files. Receipts do not imply a green whole repository, installed assets,
SBOM/signing or release publication.

No listed roadmap row may change until all four receipts pass and independent
post-native review returns `PASS_FOR_PROMOTION`. Any defect requires a new
immutable candidate and rerun; no subset of hosts or operations is promoted.
