# 0.7.0 candidate corrections and class inspection — r006

Status: **bounded source corrections reviewed; not final native/release acceptance**.
[ARC42](../arc42/appendix-requirements.adoc) owns integrated status; this report
records the correction/evidence boundary, not new requirements or waivers.
The user authorized r005's corrective plan and asked to inspect the loose Java
class files before archival. Base candidate: `41f7f42032cee59b6c654da1d04b1f844a983759`.
Exact successor commits, input hashes and receipts are retained in
[the local run](../../.refactorkit/runs/v070-acceptance-corrections-r006/summary.md).

## The two class files are redundant dependency copies

JDK21 `javap` disassembled both without executing them. They are Java8 class files,
byte-identical to entries in the locally resolved `io.cucumber:messages:24.1.0` JAR:

| File | Role | Bytes | SHA-256 |
|---|---|---:|---|
|`io/cucumber/messages/types/Envelope.class`|Immutable wrapper for17 Cucumber message/event variants|12181|`43d595cc355f966e71f86859c978c789514ecd0f5831b31deffc21fd3b8b8e57`|
|`io/cucumber/messages/types/ParseError.class`|SourceReference/message value object|1893|`1117bf0bc962ed9f620c1c47c6c1bfc320b43f3e1aca72bdbdf784f2266073c1`|

The resolved JVM/CLI default Test task classpaths use the dependency JAR and do
not include the repository root. CLI production runtimeClasspath contains neither.
No tracked module/build/script directly references the loose copies. The classes
are useful to Cucumber; these copies contain no custom code to recover. Their hash
identity does not prove who extracted them or constitute an upstream signature.

The authorized move to `.refactorkit/runs/v070-acceptance-corrections-r006/preserved-io/`
preserves all14074 bytes, original paths, inode/device, permissions and timestamps.
Original directories and dependency/cache artifacts are untouched. No deletion,
ignore change, stash or extra worktree occurred. `class-archival.json` records the
move; `archive-classes.py --restore` refuses overwrite/drift and reverses it.
This resolves the two known untracked entries, not the acceptance defects.

## J1 — exact, independently authored oracles

Nine single-scenario slices (11 expanded cases/105 steps) strengthen:

- exact import-block range, replacement and complete image;
- full generated-source message using explicit authored fixture paths;
- lexical type/field edits and images, independent of the production lexer;
- exact override-family declaration/caller edits and image;
- four filesystem baselines captured before the first refused preview, not afterward.

Existing runners/glue are reused. C-DELETE changes only stale intake prose; no
feature tag, Java production, earlier accepted positive or normative baseline
is changed. Generated-message path clarification is not a production RED/waiver.
An initial test-oracle `Set<Path> + Path` mistake split the Path into components;
its failed attempt is retained, corrected with `setOf(destination)`, and not
called a product RED. Full catalogue regression: **113 cases/793 steps**, no
failures/errors/skips. Independent supplied-source review accepts the r005 gaps.
Numerical coverage stays unmeasured, with no invented threshold or N/A claim.

## T5 — genuine eligibility RED/GREEN

One real-compiler outline, six cases and one new step within eight files. All six
returned PREVIEW with compiler leases before correction: non-source targets under
two configurations, source-language conversion, explicit `files` configuration
rewriting, an excluded destination and an excluded original source. The broader
rootDir fixture differs from r005's narrow-include probe; results are not conflated.

The existing planner reuses advertised language extensions and admits only the
one exact rename plus recognized-source modifications. Unproved config edits
refuse. The existing diagnostics bridge checks old/staged target membership via
actual compiler programs; empty diagnostics or matching root prefixes alone are
not authority. The required-source list is bounded, snapshot-owned and echoed;
clients without this optional capability fail closed. Other diagnostics callers
retain empty defaults. No new registry/store/framework, Kotlin glob implementation,
production dependency or managed-write surface is introduced.

Six cases/30 steps now pass with empty refusal edits/affected files, no lease or
approval, no WAL/metadata and unchanged source bytes. Independent review passes
this bounded correction. Source regression: **128 tests**, including **79 Cucumber
cases/351 steps**, no failures/errors/skips. Existing source surface regression:
**27 cases/98 steps**, plus five daemon and one MCP integration tests. CLI XML
contains16 filtered other cases. The27 include the existing client-managed LSP
case;26/95 is only the CLI/daemon/MCP native-comparable subset. An incorrect
collector expectation of26 was reconciled from archived reports, not rerun or
presented as a behavioral failure.

No separately shipped JavaScript recipe or new typed-code requirement was added
for findings withdrawn in r005. Recipes retain their unchanged child authority.

## What remains open

Source execution and supplied-source reviews do not grant native/milestone status.
The r003 runtime and r004/r005 source subjects retain their original identities;
none is silently repaired by this correction. Candidate-bound native acceptance,
K5 reseal/post-native review, wider I1 applicability, and version/tag/runtime/SPDX/
attestation/independently downloaded release assets remain gated. No source suite
is represented as newly executed merely because its bytes were committed.

Repeated0.7.0 multi-host qualification remains **WAIVED_BY_USER**, not PASS. Local
safety, diagnostics, WAL/rollback and asset integrity remain mandatory. No tag,
push, publication, installation, version bump or feature/release promotion occurs.
