# 0.7.0 CI source-parent visibility — r010

Status: **authorized b85fde24 tag update executed; CI qualification stopped on
missing parent history; reviewed CI-only correction ready, not yet tagged**.
[ARC42](../arc42/appendix-requirements.adoc) owns integrated status; this is an
evidence satellite, not a new runtime or semantic requirement.

The user's `si` authorized exactly89426f70→b85fde24. The remote update used an
exact old-value lease and the local tag update used compare-and-swap. Documentation
HEAD69600ec8 was not substituted for the qualified code subject. No branch push,
checkout/reset or global installation occurred; prior failed subjects remain.

## Actual b85fde24 run

[GitHub34479880234](https://github.com/mgiustiniani/refactorkit/actions/runs/34479880234)
ran only Linuxx86-64 against exact source
`b85fde2407299cc0cfaac863c541e54087f61d0c`:

| Gate | Actual result |
|---|---|
|Clean build and golden|PASS:1617 reported,1242 passed,375 skipped; zero failures/errors|
|Package; Java/TypeScript/Kotlin/kill-recovery smokes|PASS|
|J1 native CLI/daemon/MCP and finalizer|3 cases/138 steps PASS; nativeQualification=true|
|K1/K2 native finalizer|PASS:200 tests/14 suites; restored LSP JAR present|
|K5 completion source and packaged smoke|24 tests and smoke PASS|
|K5 completion finalizer|FAIL: required source-parent proof unavailable|
|K5 move-next, T5/I1 later native gates|NOT REACHED|
|SPDX, attestations, release/draft/publication|NOT REACHED; no release created|

The native J1 manifest binds75882034 bytes and ZIP SHA-256
`97caf4e743a1b8a2529e944f280ca3b563531566f4d20a092ca08209300a69a8`.
The failed job did not upload that ZIP as a release input. This identity is not
an independently downloaded accepted runtime. Raw logs, XML/Cucumber and actual
receipts are retained in r010.

## Cause and minimal correction

Checkout logs explicitly show `fetch-depth: 1` and `git fetch --depth=1`.
K5 requires `git rev-list --parents -n 1 HEAD` to expose the exact source commit
and its one parent. At a shallow boundary Git hides parent traversal. The real
b85fde24 object is non-merge and has parent89426f70; this is incomplete checkout
history, not a Kotlin behavior or runtime defect.

Both existing dedicated K5 CI jobs already use `fetch-depth: 2`. Their finalizers
and historical shallow-checkout refusal remain unchanged.

Commit **`b9a31dc144b46e0721e2b8261f0ba2b085816463`** changes only release CI and
its existing Python policy contract:

- onlyv0.7.0 build checkout fetches two commits; other tags retain depth1;
- before the expensive build, an explicit preflight checks actual HEAD equals
  GITHUB_SHA and exposes exactly its one source parent;
- shallow roots, wrong identity and merge commits fail immediately;
- optimized Python remains refused; all finalizers and publication gates remain;
- no full history download, dependency, Gradle/runtime/code/glue/Gherkin change.

The contract executes the actual AST-extracted preflight function against two
synthetic bare Git object repositories: depth1 refuses, depth2 accepts, wrong
identity refuses, and a depth2 merge refuses. There is no project checkout/worktree
or fabricated GitHub-native identity. The initial missing-preflight/configuration
RED is retained as infrastructure evidence, not a product semantic RED. All15
release-policy tests pass. Independent review returns
**PASS_FOR_CHECKOUT_HISTORY_CORRECTION**, not CI or publication PASS.

The corrected runtime's earlier b85fde24 local K1/K2, K5, T5/I1 and J1 executions
retain their exact identities; CI-only changes do not manufacture reruns. Runtime,
Gradle and scenario bytes are unchanged. No additional unaffected source/native
suite rerun is presented as necessary local work.

## Remaining boundary

The public tag still points to b85fde24. A new update to reviewed b9a31dc1 needs
explicit authorization; no further tag movement has occurred. Later documentation
commits do not change that proposed qualification subject.

Final tagged CI, SPDX/checksum/provenance/attestation joins and independently
verified downloaded candidate/release assets remain mandatory. No release/draft
exists and no global installation was replaced. Other hosts remain
WAIVED_BY_USER / not verified; static findings are not clean and numerical coverage
is unmeasured. All earlier bounded race/filesystem/power-loss/LSP exclusions remain.

Exact evidence: [r010](../../.refactorkit/runs/v070-final-tagged-release-r010/summary.md).
