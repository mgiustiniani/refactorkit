# 0.7.0 release preparation — r008

Status: **corrected CI-fixture successor prepared; publication blocked pending the
unreleased-tag decision and a successful tagged build**.
[ARC42](../arc42/appendix-requirements.adoc) owns integrated status. This report is
an evidence satellite, not a new capability or acceptance baseline.

## What progressed

After [r007 bounded local acceptance](v0.7.0-final-local-acceptance.md),
[V070-RELEASE-VERSION-TRANSITION-001](../requirements/v0.7.0-release-version-transition-001.md)
changed only implementation/build metadata to0.7.0, retaining API0.2 and all
semantic, schema, approval, diagnostics and rollback boundaries.

| Subject | Identity/result |
|---|---|
|Version metadata/source-oracle commit|`ac32b605d264cce5e9d22a127bf212cb122dae1f`|
|Reviewed release automation|`cbcf528c0eee7aa96173e29ba587e9840ce686d7`|
|Release notes / original tag subject|`14220182bccaafac8361bfcb4bc44cfdf9fd4602`|
|Reviewed CI-fixture correction|`0a9ddfb0a4edf8e89c31840ea366c2082d610c9b`|
|Local versioned runtime|75213330 bytes; SHA-256 `c53ecc070d94ea93eebd1fd3dc01eb71d65eccaddf71ca9a759394e92dfee9a9`|

Three existing, separately admitted source Stories pass22+31+30 steps with the new
version. Frozen help/capability/reactor fixtures remain unchanged. RESULT001 keeps
its original status-neutral feature digest, reverse-projecting only two exact
single-occurrence version metadata spans; its initial guard refusal remains
recorded as metadata setup, not a product defect RED.

The local versioned package passes the existing three CLI/daemon/MCP Maven
module-rename cases and actual finalizer. The immutable oracle retains its snapshot
version; a separate literal0.7.0 governs actual MCP and WAL implementation versions.
This is a new local archive, not a relabelled6ae7d451 snapshot or a released asset.

Existing K1/K2/K5 finalizers use exact0.7.0 JAR names without changing requirement
pins or case sets. Independent infrastructure assertions first exposed the old
snapshot names and then passed. No wildcard version or receipt framework was added.

Release automation keeps only Linuxx86-64 forv0.7.0, pins JDK21.0.11+10, preserves
other releases' existing matrix, runs existing source/native/finalizer machinery,
and holds0.7.0 as draft until independent review/download joins. Explicit receipt
schema/revision/platform/source/JAR/runtime/archive joins and fresh custom-report /
closed-transport checks corrected pre-tag review findings. SPDX and build/SBOM
attestations remain mandatory. Earlier timeout/FAIL responses are retained, not
superseded into fictional executions.

## Actual GitHub failure and exact correction

Tag **v0.7.0 currently remains at14220182**.
[Run34442715747](https://github.com/mgiustiniani/refactorkit/actions/runs/34442715747)
ran one Linux build only. JDK/toolchain setup and version preflight passed. The
job failed after13m4s; its standard Gradle build lasted12m36s and stopped in CLI.
Of460 reported CLI tests, three failed
and355 were skipped/filtered. Other stages, packaging, signing/attestation and
publication did not execute. No release or draft assets were created.

The catalogue v1/v2 fixtures require an existing installation directory for their
non-selection tripwires; result001 also requires its regular launcher. The clean
runner had not been given this boundary fixture. A same-source isolated-home
counterexample reproduces the catalogue's exact missing-root exception and line639.
Provisioning an authored inert executable inside a workspace-owned fixture HOME
makes all three unchanged Stories pass83 steps. The complete CLI regression then
passes **460 reported =105 passed +355 skipped**, zero failures/errors,333.46s.
These are setup/regression results, not a product RED, a new CI run, or whole-tree
and native acceptance. Production, glue and Gherkin were not changed for this fix.

The correction provisions only
`.refactorkit/runs/v070-release-ci/boundary-home/.local/share/refactorkit/bin/refactorkit`
and overrides `user.home` only in the CLI test JVM, not global HOME, the installed
application, parent Gradle or compiler caches. The executable is an inert forbidden
selection fixture, never a source of product acceptance or installed-runtime parity.
Original lookup/invocation/mutation guards and literal oracles all remain active.

A separate verifier reconstructs its fixed path and literal bytes independently;
it checks the mutable record against those constants, all path components for
symlinks, exact entries/kinds, bytes and mode. Explicit failure conditions and
non-optimized-Python guards prevent assertion-elision. The infrastructure contract
executes pristine success, altered bytes plus a matching altered record refusal,
and optimized preparation/verification refusals. Independent follow-up returns
**PASS_FOR_CI_FIXTURE_SUCCESSOR**, not CI/native/publication PASS. Earlier mutable-
record and optimization findings remain retained with their corrections.

The failed job retained its complete log but not the raw standard XML/Cucumber
reports, because copying occurred after the failed build. The successor always
uploads those original reports too. It does not invent missing original reports.

## Remaining decision and gates

The failed commit, tag binding and run remain immutable evidence. The already-public
unreleased tag is **not silently moved or deleted**. Explicit confirmation is needed
to rebindv0.7.0 to the prepared corrected successor and rerun the Linux-only build.
No existing asset is overwritten; none was produced.

After that decision, the exact tagged source still needs successful CI/native gates,
SPDX/checksums/provenance/attestations, independent post-native review, draft-asset
verification and published-download verification before final publication claims.
No source version label substitutes for those gates. Global installation remains
untouched. Other hosts remain **WAIVED_BY_USER / not verified**, not PASS.
The r007 filesystem/race/power-loss/LSP and static-analysis limits remain unchanged.

Exact inputs, reviews, logs, failed-run download, commits and local checks are under
[r008](../../.refactorkit/runs/v070-release-preparation-r008/summary.md).
