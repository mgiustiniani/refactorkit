# RefactorKit 0.7.0 source-candidate consolidation — r004

Status: **LOCAL_SOURCE_CONSOLIDATED — NOT_RELEASE_ACCEPTANCE**.
Authority boundary: this is a Git-consolidation/evidence satellite.
[ARC42](../arc42/appendix-requirements.adoc) owns integrated status; the frozen
requirements and existing approved changes remain normative. The user's explicit
`si` answers the request to consolidate the candidate in separate commits. It is
not a new behavioral exception or authorization to publish during this step.

## Separate coherent units

Base: `08e7203807623c29575b6b6d82b27195f7698bf0`. Navigation `21550da0`, CI
correction `c0968a40` and the host-waiver ancestry are retained, not squashed or
reclassified as T5 work.

| Unit | Commit | Boundary |
|---|---|---|
| Shared Kotlin test toolchain | `f9e81d073c75f8330ba7285a36a0fb29898d7e0b` | Borrow pinned artifacts; preserve shared-cache ownership and isolated tamper input |
| J1 exact oracles | `d242feeac35d2a1909cc7684bc25dcc502c722ee` | Five characterization oracles/status corrections; no Java production change |
| Complete T5 implementation | `26c038f000f2c0f966ae02e84b2ab3c2a2a631be` | Inherited planners, compiler/model/process authority, public routes, recipes, tests and packaged transport together |
| K5 input integration | `1d52d95c8a283000e43bed4e370c802e567815b4` | Approved-change005, leaf attribution, sidecar/finalizer pins and bounded report; no semantic waiver |
| I1 recovery | `5318331489daad28f5a41e9d6bc947e206171221` | Successful-creation ownership receipt, daemon-crash/conflict Stories and documentation |
| Integrated status | This documentation commit | Complete the cross-cutting ARC42/release ledger and record consolidation without promotion |

The final commit/tree identity is recorded outside its own Git tree in
[the local run](../../.refactorkit/runs/v070-candidate-consolidation-r004/summary.md),
with explicit path manifests, before/after hashes and staged-patch/commit receipts.
This avoids a self-referential candidate hash. Those self-produced integrity
receipts are not independent acceptance verdicts.

All 93 initially owned dirty paths are assigned. The two cross-cutting ledger
files in the historical 55-file T5 inventory are completed in the integrated
commit; no inherited implementation file is omitted. The common testing document
and CLI runner use exact known intermediate index images, never working-tree
rewrites. The final tree contains their complete reviewed r003 images, apart from
the explicitly new integrated documentation prose and this report.

## Preserved inputs and execution boundaries

The initial 1,159 Git-visible input hashes matched r003 exactly. No implementation,
fixture, feature, build, frozen-requirement or existing verification-script bytes
are changed during consolidation. Only the ARC42/release-plan pointers above and
this new record add documentation. No blanket staging, reset, reverse-patch
application, branch rewrite, compiler copy, cache cleanup or installation occurs.

The unrelated untracked files remain byte-identical and outside the candidate:

- `io/cucumber/messages/types/Envelope.class`
- `io/cucumber/messages/types/ParseError.class`

A committed tree is immutable; that does **not** make this entire checkout clean.
Do not bypass an existing clean-checkout qualification gate or conceal these files
with ignore rules. The historical detached J1 candidate is unchanged.

Existing test/review evidence retains its original subject and chronology:

- J1 exact-oracle regression: 76 cases/512 steps; bounded source review PASS.
- r003 packaged T5: 26 cases/95 steps; separate I1: eight cases/31 steps.
- Final helper audit/teardown changes: separate focused help/crash reruns, not
  additional unique cases or a retroactive broad-run source identity.
- r003 core125, golden33, exact K5 24 cases, three Kotlin packaged smokes,
  archive checks and kill/WAL recovery remain their original executions.
- Failed/partial/timeout reviews and genuine behavioral REDs remain preserved;
  narrow source PASS and `PASS_FOR_RESEAL` do not become promotion verdicts.

No JVM/Node behavior suite is rerun merely to create commits. Source hash equality
and Git/script-contract checks establish consolidation integrity, not new native
qualification. The existing runtime archive remains the r003 dirty-source build:
SHA-256 `a614fcc247078c23b75f36168bb5cd1b0225d2c38e0ca485009017ae7715e9a4`.
It is not relabeled as a runtime produced from this new commit chain. Development
version remains `0.7.0-SNAPSHOT`, API `0.2`; no tag, push or publication occurs.

## Remaining dispositions

| Status | Work |
|---|---|
| actionable | Candidate-bound J1 effective-glue/coverage/status reconciliation and final acceptance |
| actionable | T5 nine-row success/refusal applicability and candidate-bound acceptance; no operation/surface Cartesian claim |
| actionable | K5 candidate reseal and independent post-native acceptance |
| actionable | Wider I1 fixture, timeout, hostile/concurrent-writer and filesystem applicability; live validation/deletion replacement races and unqualified stable identities remain excluded |
| blocked | Final release/version/tag/artifact/SPDX/attestation and independently downloaded published-asset joins, pending acceptance and publication stages |
| WAIVED_BY_USER | Repeated multi-host qualification for 0.7.0 only; not local safety or artifact integrity |
| out-of-scope | UI/cloud/IDE work and additional language milestones |

No Gherkin status, roadmap checkbox, task lifecycle or release state is promoted.
[The release plan](v0.7.0-plan.md) and [prior r003 evidence](../../.refactorkit/runs/v070-native-closure-r003/final-evidence-index.json)
remain separately bounded. Git history permits an explicitly reviewed, ordered
revert of these coherent commits; no revert or product rollback is executed here.
