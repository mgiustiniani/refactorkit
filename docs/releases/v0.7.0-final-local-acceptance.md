# 0.7.0 final local acceptance — r007

Status: **J1/T5/K5 and bounded I1 prepublication gates accepted locally**.
This is not a published release. [ARC42](../arc42/appendix-requirements.adoc)
owns integrated status; this report records evidence and limits, not new scope.

## Subjects and independent verdicts

- Native source candidate: `43105037d784382678dcd9ea71ef8404a065f962`.
- Reviewed test-only successor: `9acd759f3bf7cd29e0c58b9e6a2df7b33942fa30`, tree
  `026c2c57dbae20a76b271df5654ac54104ad9da5`.
- Runtime archive: **75213575 bytes**, SHA-256
  `6ae7d4513169a51faa5bfcbe8f1f51e5e31ce1dda2f797b5586b7efab4ab696b`.
- Exact source/runtime/input joins and the supplied-evidence independent review:
  [r007](../../.refactorkit/runs/v070-final-acceptance-r007/summary.md).

Independent follow-up returns **PASS** for J1 local catalogue/package, T5 local
catalogue/package and bounded I1 prepublication integration/regression, and
**PASS_FOR_PROMOTION** for K5 bounded completion/move-next through the unchanged
runtime join. No remaining blocker was identified within those local gates.
The reviewers did not execute tests or recompute filesystem/archive hashes.
Original REVIEW_REQUIRED/PARTIAL reports are retained with their focused follow-up.

| Gate | Evidence and limits |
|---|---|
|J1|Finite19-row catalogue; fresh113 cases/793 steps on the test-only successor. Three actual packaged CLI/daemon/MCP module-rename cases on the native source candidate, exact changes/approval/WAL/rollback. No spoofed GitHub environment or native-CI claim.|
|T5|Nine bounded release rows accepted, including truthful unsupported change-signature/inline-function outcomes.128 source tests/79 cases351 steps retained through an exhaustive input join;26 actual packaged cases/95 steps on CLI/daemon/MCP,26 closed transports. The corrected target refusal retains its separate diagnostic probe identity.|
|K5|Fresh24-case/five-suite completion and25-case/three-suite move-next oracles, three actual packaged smokes, both existing finalizers PASSED on Linux with host/embedded JDK21.0.11+10. Integration005 pins and011–013 supersessions remain exact.|
|I1|Selected126 unit/property/scanner/golden/agent/hostile/cooperative/crash cases; packaged517-edit module-crash/conflict8 cases31 steps; real kill/restart smoke and bounded timeout evidence. Mixed JVM and operation-family evidence joins are explicit.|
|Standard final regression|`check goldenTest`:1616 discovered =1241 passed +375 skipped/filtered, zero failures/errors,100 tasks (41 executed/59 up-to-date),27m54s. TypeScript's unchanged test task was reused, not re-executed.|

The375 skipped/filtered case identities are retained in the run; they are not
PASS. Static tools emitted findings under the existing nonblocking configuration;
SpotBugs exit1 lines remain in the successful Gradle log. Static cleanliness and
numerical coverage are not claimed. No new numerical threshold was invented.

## Full-check failures and narrow reconciliation

The initial full check failed in CLI on four integration assumptions: two catalogue
fixtures required the checkout to have no `.refactorkit` directory; a journal
oracle lacked the approved staging marker; an MCP synthetic payload expected three
constructor fields instead of four. Its901 visible test count contains old reports
from tasks not reached and is not a qualified whole-tree result.

Four single-scenario slices reused the installed analyzer, runners and steps.
Only four CLI glue files changed. Existing metadata presence is captured and
compared, while the exact mutable run-evidence subtree is excluded from content
hashing but retained as a directory entry and still guarded against subject writes
and deletes. All other repository/installation checks remain. The frozen help
fixture remains untouched; expected lines apply only the separately approved T5
help delta. Schema8, PREPARED history/marker, images and approvals stay literal and
exact. The synthetic Kotlin MCP payload verifies the optional fourth gate type
and passes null; lifecycle assertions are unchanged.

All four cases pass (88 scenario steps plus five Background steps); independent
review passes the exact test-only patch. A first isolated catalogue attempt exposed
the stale help expectation and a wrong collector report path; those failures stay
recorded, not promoted to product REDs. No production, frozen feature/fixture,
protocol, dependency or version change was needed.

The successful full check binds the test-only successor. Earlier native executions
remain attached to43105037; complete source/build and extracted-member equality
join them to9acd759f without pretending they were rerun. Documentation-only updates
after acceptance likewise do not re-execute or relabel the accepted tests.

## Authorized class deletion

After inspection, the user explicitly authorized removal. Exactly the two archived
`io/cucumber/messages/types/{Envelope,ParseError}.class` copies were deleted after
hash/type/device/inode/mode verification, totaling14074 bytes. Their existing
Cucumber dependency JAR was not deleted. Disassembly/provenance and all other
r006 evidence remain. The two raw files are explicit authorized retention exceptions
to the historical r006 seal; its archive restore command is no longer applicable.
No other cleanup, ignore change or extra worktree was used.

## Exclusions and remaining publication work

- Only Linux x86-64 is locally qualified. Other final-candidate hosts are
  **WAIVED_BY_USER / not verified**, not PASS.
- Live uncooperative replacement between validation and deletion, unqualified
  filesystem identity and power-loss behavior remain unsupported/unqualified.
  Independent review treats them as explicit scope limits, not silently waived
  safety or guarantees inferred from cooperative lock/replay tests.
- Timeout cases use a hostile protocol peer in an actual bounded process; they
  are operational timeout proof, not compiler semantic-success evidence.
- LSP edit proposals remain client-managed where specified, not managed WAL/rollback.
- Final version/tag, source/artifact release manifest, SPDX SBOM, provenance and
  attestations, publication and independent downloaded-asset verification remain
  applicable and open. The current runtime is still0.7.0-SNAPSHOT/API0.2.
- Existing release automation defaults to a multi-platform build and snapshot-bound
  qualification fixtures still exist. Release preparation must reconcile the
  explicit0.7.0 host exception and version transition before triggering publication;
  no unmodified four-host workflow or mismatched snapshot asset is published here.

No tag, push, publication or global installation replacement occurred in r007.
