# RefactorKit v0.7.0 acceptance

Status: **published and independently verified within the approved bounded scope**.
Date: 2026-09-10. API: **0.2**.

Release: <https://github.com/mgiustiniani/refactorkit/releases/tag/v0.7.0>

[ARC42](../arc42/appendix-requirements.adoc) owns the integrated architecture and
status narrative. This satellite records the exact execution, artifact and
publication evidence; it does not expand semantic or platform support.

## Immutable release identity

- Tag: `v0.7.0`.
- Source: `b9a31dc144b46e0721e2b8261f0ba2b085816463`.
- Tree: `95aa410e75a547a40a579bece3655e10611af8eb`.
- [Tagged CI34505984557](https://github.com/mgiustiniani/refactorkit/actions/runs/34505984557), attempt1: both jobs **SUCCESS**.
- Public publication timestamp: **2026-09-10T19:26:13Z**.
- Platform: **Linux x86-64 only**; embedded Temurin **21.0.11+10**.
- Runtime: `refactorkit-runtime-0.7.0-linux-x86_64.zip`, **75,882,034 bytes**.
- Runtime SHA-256: `97caf4e743a1b8a2529e944f280ca3b563531566f4d20a092ca08209300a69a8`.
- Release-manifest SHA-256: `33edc24fbbe24841c95b6c4a5162c3eab7b858c0951db8c121a30a635222b1a4`.
- SPDX SHA-256: `b129c7bbd2f69bc74237dcf83a26e730312db0ef447505407f13ae4ed4c85cbb`.

The archive, adjacent checksum, SPDX JSON and `.release.json` are the four published
assets. The Linux-only release is deliberately not marked as GitHub's cross-platform
Latest release. Windows/macOS retain separately verified0.6.2 assets; never use
an archive built for a different OS/architecture.

## Actual final tagged gates

| Gate | Actual result |
|---|---|
|Clean build and complete golden regression|1617 reported: **1242 passed,375 skipped**, zero failures/errors|
|Package and Java/TypeScript/Kotlin/kill-recovery smokes|PASS|
|J1 native CLI/daemon/MCP|3 cases /138 steps; nativeQualification=true; actual finalizer PASS|
|K1/K2 shared foundations|200 tests /14 suites; native finalizer PASS|
|K5 bounded completion|24 tests /5 suites; packaged smoke and native finalizer PASS|
|K5 move-next|25 tests /3 suites; packaged smoke and native finalizer PASS|
|Packaged T5|26 cases /95 steps PASS|
|Bounded packaged I1|8 cases /31 steps PASS|
|Transport teardown|34 closed transports; all recorded children exited|
|Independent post-native/draft review|PASS_FOR_PUBLICATION|
|Public-download follow-up|PASS_RELEASE_ACCEPTANCE|

K1/K2 reports are an unchanged exact selection from the full run, not a second
focused source execution. K5 focused cases were actually rerun. The whole-run
module/classname/testcase/status multiset matches the preceding unchanged-source
r010 regression; colliding suite display names are not used as unique identities.
The previous failed GitHub jobs remain failed, not retroactively promoted.

## Source, runtime and supply-chain joins

Independent source reconstruction joins1164 exact Git blobs with the declared
checkout line endings to the native source manifest. `gradlew.bat` in the older
local checkout was physicallyLF; the fresh CI checkout obeyed explicitCRLF.
This representation distinction is not a source change.

All222 downloaded runtime file members match the actual final native image.
All bundled library JARs are byte-identical to the previously qualified local
r009 runtime; native JDK distribution files retain their distinct platform-build
identity. J1's material source/package/case bindings are independently joined;
its canonical `bindingSha256` is not claimed independently recomputed.

Cryptographic `gh attestation verify` passes for:

1. SLSAv1 build provenance covering the ZIP and release manifest;
2. SPDX2.3 attestation covering the ZIP.

Verification pins the repository, signing workflow, source and signer digest,
`refs/tags/v0.7.0`, and GitHub-hosted execution. Certificate claims and verified
timestamps bind run34505984557/attempt1—not merely workflow-controlled predicate
text. The signed SPDX predicate equals the downloaded JSON; the attested release
manifest additionally binds its exact raw bytes.

The build-time `.release.json` review/download fields remain **PENDING** and
unchanged, as they correctly describe that earlier phase. Later acceptance is
recorded separately here and in the public release notes; attested bytes are not
rewritten to invent a build-time independent review.

## Independent downloads and execution

The draft assets and uploaded CI release inputs were independently retrieved and
matched byte-for-byte. Whole-stream CI artifact downloads timed out or reset;
73 bounded byte-range responses subsequently reconstructed the complete outer
artifact. Every range/length and the GitHub artifact digest matched, followed by
ZIP CRC and exact four-entry checks. Partial downloads remain failed evidence.

After publication, all four literal public HTTPS release URLs returned HTTP200
without authentication. Their sizes and hashes exactly match the approved draft,
CI inputs and attested subjects. The public ZIP passed the existing archive
verifier and real MCP initialize/tools/EOF lifecycle. Additional execution observed
exact `RefactorKit 0.7.0 (API 0.2)` and embedded Temurin21.0.11+10 with JAVA_HOME
unset and PATH prefixed by an authored failing global-java tripwire. Runtime and
asset bytes remained unchanged; temporary extracted images were removed.

Independent reviews use exact supplied sources/evidence in isolated tool-disabled
processes. They do not claim reviewer-side execution, downloads or filesystem/hash
recomputation. The executing verifier's actual operations are recorded separately.

## Preserved limits and history

- The approved J1/T5/K5/I1 finite scope is closed for0.7.0; no universal Kotlin,
  Java/Maven, recipe or operation/surface Cartesian support is claimed.
- Other hosts are **WAIVED_BY_USER / not verified** under
  [V070-LOCAL-HOST-ACCEPTANCE-001](../requirements/v0.7.0-local-host-acceptance-approved-change-001.md), only for0.7.0.
- Skips are not passes. Static findings remain configured nonblocking, **not clean**.
  Numerical coverage remains **UNMEASURED**.
- Live uncooperative replacement races, unqualified filesystem identity and
  power-loss behavior remain unqualified. LSP edits remain client-managed where
  specified, not RefactorKit-managed WAL/rollback.
- No global installation was replaced. WIP backups and branches were left intact;
  their integration is not part of this release. No branch was pushed.
- Earlier fixture/LSP/history failures and r007–r011 evidence remain distinct and
  retained. The authorized final retag used an exact old-value lease and local
  compare-and-swap. After publication, this tag and its assets are immutable;
  a later defect requires a new patch version.

Detailed local evidence:
[`.refactorkit/runs/v070-release-publication-r012/`](../../.refactorkit/runs/v070-release-publication-r012/summary.md).
Published notes expose the final acceptance facts; source-tagged documentation
retains its historical checkpoint identity.
