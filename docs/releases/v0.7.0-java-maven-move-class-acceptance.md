# RefactorKit 0.7.0 Java/Maven move-class authority acceptance

Status: the reported Magrathea CLI row is qualified locally on Linux/JDK 21.
The bounded source-built/in-process lexical-review boundary is also validated
across CLI, recipe, daemon-session, managed-LSP-session, and MCP-session paths.
A second packaged-process Cucumber mode for existing
`REQ-JAVA-MAVEN-MOVE-AUTH-001` is implementation-informed from one official
final local OpenJDK 21.0.11/Linux `amd64` run of the mutable tree. Native CI
wiring and `if: always()` report uploads are implemented but unexecuted. Its
four post-commit native CI rows remain **CONFIGURED_UNOBSERVED** and are not
release claims. The remaining REQ-002..012 packaged, native, integration, and
general authority/refusal matrix stays open.

## Qualified safety boundary

Managed Maven `moveClass` apply remains available only with exact JDT binding
evidence. `LEXICAL_FALLBACK` is review-only and is rejected before workspace-lock
and WAL creation.

The target-scoped authority implementation now includes:

- a dependency-bounded reverse-observer closure;
- candidate-total `BOUND_TARGET`, `BOUND_OTHER`, and `UNRESOLVED` classification;
- exact Java target-name lookup protection;
- before/staged diagnostic and evidence identity;
- an operation-authority lease revalidated under the workspace lock;
- schema-v1 immutable `REVIEW_ONLY_GUIDANCE` for four safely enumerable Maven
  authority defects, with typed evidence groups and pre-lock/WAL CLI refusal;
- schema-v1 immutable `LEXICAL_FALLBACK_REVIEW` for broad legacy uncertainty,
  with deterministic non-capability identity, bounded edit-free residuals, and
  cross-surface refusal before plan/edit registration or managed-write entry;
- descriptor-closed traversal over selected Maven nodes;
- RefactorKit-owned evidence for a selected fixed ordinary Maven leaf whose POM
  closure is complete but JAR is absent; and
- typed structural refusal for missing, malformed, or drifted selected
  POM/parent/BOM/mediation/relocation evidence;
- result-owned structural evidence when a root-declared active reactor POM is
  missing, with deterministic workspace-relative no-follow path facts; and
- generic non-managed required-file lease evidence revalidated under lock before
  WAL, with immutable structured expected/observed drift diagnostics.

A dependency edge deterministically excluded by effective scope, transitive
optionality, or an exact inherited exclusion is pruned before child version,
repository, POM, JAR, or network lookup. If another path selects the same
coordinate, its complete effective descriptor closure becomes required again.

Offline discovery remains plugin-free, credential-free, and network-disabled by
default. No caller-attested classpath can grant authority.

## Permanent executable evidence

`features/java-maven-move-class-apply-authority.feature` currently contains 12
requirement definitions expanding to 22 executable cases. All 12 definitions and
all 22 cases are `@implemented-and-validated`; none remains `@absent`.

The permanent fixture is
`testdata/acceptance/java-maven-move-class-authority-20-modules/`. It preserves
one root aggregator and 20 active modules while exercising direct, transitive,
test-only, same-name, unrelated, generated-source, descriptor-pruning, ordinary
missing-leaf, and descriptor-failure variants.

Validated bounded rows include:

- `REQ-JAVA-MAVEN-MOVE-AUTH-001`: two deterministic fully available semantic
  previews, one transaction, committed diagnostics, and exact rollback;
- `REQ-JAVA-MAVEN-MOVE-AUTH-002`: exact binding-selected direct, transitive, and
  test-only observers with separately reported binding decoys, Java non-code,
  and non-Java residual risks;
- `REQ-JAVA-MAVEN-MOVE-AUTH-003`: four independently reproduced readable and
  safely enumerable source-inventory, classpath-fingerprint, recovered-binding,
  and generated-root-inventory defects return deterministic non-managed guidance;
  CLI apply refuses with `guidance.nonManaged` before `PatchEngine`, lock, or WAL;
- `REQ-JAVA-MAVEN-MOVE-AUTH-004`: one immutable
  `LEXICAL_FALLBACK_REVIEW` is preserved across the Java move-class outcome API,
  source-built CLI, recipe, daemon session, managed LSP session, and MCP session;
  it exposes no plan/edit/apply capability, recipes stop without an aggregate,
  and known-operation apply attempts return typed `evidence.insufficient` before
  `PatchEngine`, lock, WAL, editor application, or mutation;
- `REQ-JAVA-MAVEN-MOVE-AUTH-005`: lexical apply refusal leaves no managed-write
  residue;
- `REQ-JAVA-MAVEN-MOVE-AUTH-006`: unrelated JDT warnings outside the observer
  closure do not demote authority;
- `REQ-JAVA-MAVEN-MOVE-AUTH-007`: enumerated offline system-path leaf authority;
- `REQ-JAVA-MAVEN-MOVE-AUTH-008`: four typed immutable guidance outcomes for
  bounded preview uncertainty, one result-owned root-declared active-POM
  structural refusal, and one lock-held pre-WAL non-managed candidate-source
  evidence-drift refusal that preserves external drift without a transaction;
- `REQ-JAVA-MAVEN-MOVE-AUTH-009`: explicit transitive test scope is preserved;
- `REQ-JAVA-MAVEN-MOVE-AUTH-010`: descriptor-closed selector pruning;
- `REQ-JAVA-MAVEN-MOVE-AUTH-011`: sidecar-free selected ordinary missing-JAR
  evidence; and
- `REQ-JAVA-MAVEN-MOVE-AUTH-012`: six selected-descriptor failure and drift
  refusals.

## Reported Magrathea field row

A clean detached worktree at Magrathea commit
`a48e3c06629acf7032d0f28a0f8bdc5426442c7c` was analyzed using the current
source-built RefactorKit CLI on Linux with OpenJDK 21.

Request:

```text
move com.magrathea.trustengine.infrastructure.x509.certificate.revocation.InMemoryRevocationRepository
to com.magrathea.trustengine.infrastructure.x509.certificate.revocation.inmemory
```

Observed preview:

```text
Evidence: JDT_BINDING
Affected files: 4
Owner: trust-engine-x509-infrastructure:main
Observers: bootstrap-application:main,
           trust-engine-x509-acceptance-tests:test
```

Managed apply completed in one transaction and the normal rollback completed
successfully. The source tree, excluding `.refactorkit` transaction metadata,
had these SHA-256 identities:

```text
before apply:   c740707ca738e7acc192aafbaa3d2f7dce033cf38a3799dd490cb83e35a66bc1
after apply:    ade6660cd50b3a42536bda13b665ba659bdd04add681d7165ea50c5616649a82
after rollback: c740707ca738e7acc192aafbaa3d2f7dce033cf38a3799dd490cb83e35a66bc1
```

The detached worktree was removed after verification. No source or metadata was
left in the user's Magrathea working tree.

## Local packaged-process REQ-001 qualification

This slice reuses the existing feature, stable requirement ID, and same REQ-001
pickle through the dedicated `packagedMavenMoveClassAuthorityTest` source set/
task. It is a second validation mode, not a new requirement or case; the Maven
feature remains 12 definitions and 22 cases with unchanged classifications and
statuses. The explicit task depends on the real unpacked self-contained package
and is not attached to ordinary `test` or `check`. External protocol and product
semantics are unchanged.

One no-follow byte copy of the permanent 20-module fixture is the complete
workspace population. The real package launcher and embedded Java run in child
OS processes with isolated home/temp state and a scrubbed allowlisted
environment. `JAVA_HOME`, `JDK_JAVA_OPTIONS`, `_JAVA_OPTIONS`, `CLASSPATH`,
Maven/Gradle options, and proxy variables are absent; one controlled
`JAVA_TOOL_OPTIONS` supplies isolated home/temp properties. A poison `java` shim
is first on minimal `PATH` and remains uninvoked. Canonical child `java.home`
proves the embedded runtime.

Observed SHA-256 identities are:

- fixture and copy manifests:
  `905b354c151e8219d6f755335ecac381a0dab83df20de55d22f8a15a8695829e`;
- unchanged unpacked-package manifest:
  `681822f781379f5ddbe67389d1e33606f8931ca6fc4a75a3b3fc09f657b88430`;
- launcher:
  `fa9cd72b54f269d76053da247665cb931ff39631d1ac38c7b55aaa0c9dcadef3`;
- embedded Java:
  `bd12685bb6403175bb8803ef143738bf74d06565223be944de1dad20ef5e5625`.

The observed 16-command evidence sequence:

1. attests package/runtime identity, scan/diagnostics, the seven-source fixture
   inventory, references/definition, repeat scan, and unchanged inputs;
2. runs the fixed `move-class` preview twice and obtains equal eligible
   `JDT_BINDING` output over the exact three observer and five affected paths;
3. holds the workspace lock and observes only exact CLI text `Apply refused:` /
   `ERROR: Workspace is locked by another RefactorKit writer`, before WAL;
   `workspace.locked` is not projected and the runner makes no core-code
   correlation claim;
4. invokes the fixed request once with `--apply`; the CLI replans inside that
   child process, compares rendered preview text, and makes no cross-process
   plan-ID claim;
5. safely parses the sole schema-v8 transaction: operation `moveClass`, approval
   `EXPLICIT_APPLY`/`cli`, five top-level `FileEdit` entries consisting of four
   `Modify` plus one `Rename`, nested `TextEdit` counts `1/2/1/1` across the
   modifies, and `PREPARED -> APPLYING -> APPLIED` history;
6. observes clean post-apply diagnostics; and
7. performs normal rollback, appends `ROLLING_BACK -> ROLLED_BACK` to the same
   sole record, creates no second record, and restores every non-engine byte,
   path kind, seven-source inventory entry, and snapshot identity exactly.

The one-edit-per-modify interpretation is explicitly rejected: there are five
nested text edits total, but the acceptance-step modify contains two. The exact
observer paths are `CatalogPrice.java`, `ProductTile.java`, and
`ProductLifecycleSteps.java`; the old/new target paths complete the five-path
set. Existing source-built acceptance remains the direct lock-held/fault-
injector evidence.

Official final local execution used OpenJDK 21.0.11 on Linux `amd64` and
`:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks`.
It reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit discovered 22 cases,
executed one, tag-filtered 21, and reported zero failures/errors; the selected
Cucumber scenario passed five background plus fourteen scenario steps (19/19)
and 2/2 hooks. All 16 commands completed with zero timeouts, truncations, or
process leaks. Packaged log SHA-256:
`1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`.
Cucumber JSON SHA-256:
`9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`.

An earlier independent read-only review remains separate corroborating evidence:
packaged log SHA-256
`7a71b69b745e10e9339d8cdd911c346e422328d58163db52eb92a536e8735d9e` and
JSON SHA-256
`f3ecf22fa195dbbd4e88e5a68937f7f55dec4ad4a6d90504cdc34588444df340`.
Its source-built REQ-001 rerun also passed 22 discovered/one executed/21 tag-
filtered/zero failures or errors; log SHA-256:
`96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`.

On the same JDK, official final whole-tree source-built execution of
`./gradlew --no-daemon --rerun-tasks check goldenTest` reported
`BUILD SUCCESSFUL` in 6m43s. Standard test tasks discovered 1045, executed 817,
skipped/tag-filtered 228, and had zero failures/errors; 140 XML files and 82
tasks executed; log SHA-256:
`89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`.
The dedicated packaged task is separately reported and intentionally not
included in those standard-test totals.

Targeted custom-source-set PMD/SpotBugs tasks passed; log SHA-256:
`2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`.
Static classification is intentionally non-zero. The custom packaged glue/
harness has exactly 99 reviewed/non-blocking SpotBugs findings: 57 NP, 14 MS,
12 BC, 6 AT, 9 RCN, and 1 UrF. These are test/deployment validation-adapter
findings, mostly Kotlin `lateinit`/generated representation and conservative
single-thread harness concurrency warnings. No production source file was added
or modified by this slice. The wider static baseline remains non-blocking;
aggregate static cleanliness and numerical or child-JVM coverage are not
claimed.

| Post-commit native CI host | REQ-001 packaged-process state |
|---|---|
| Linux x86-64 | `CONFIGURED_UNOBSERVED` |
| Windows x86-64 | `CONFIGURED_UNOBSERVED` |
| macOS x86-64 | `CONFIGURED_UNOBSERVED` |
| macOS arm64 | `CONFIGURED_UNOBSERVED` |

The local observation does not promote a ledger row. No row can become observed
or qualified before native CI executes the exact committed tree and publishes a
reviewable receipt. Implemented CI wiring and always-run report uploads are not
execution evidence. Windows/macOS behavior is configured and statically reviewed
only. The local observation is bounded to its exact unpacked package, host/
architecture, fixture, request, and command sequence. It excludes REQ-002
through REQ-012; daemon, LSP, MCP, recipe, and library surfaces; Magrathea and
general Maven populations; crash/restart/concurrency; archive trust/checksum/
SBOM/attestation/signing; coverage of the child JVM; and every unexecuted host.

The earlier research recommendation and ADR/C4 reviews remain historical input.
Implementation introduced evidence plumbing only and no durable decision or
production topology/relationship change, so no ADR or Structurizr artifact was
modified.

## Local verification

The dedicated OpenJDK 21 REQ-008 run discovered all 22 authority cases, selected
and passed six cases, skipped 16 by tag filter, and reported zero failures or
errors.

Independent final OpenJDK 21.0.11 verification:

```text
./gradlew --no-daemon --rerun-tasks check goldenTest
BUILD SUCCESSFUL in 7m 33s
1000 discovered cases
780 executed cases
220 skipped or tag-filtered cases
0 failures
0 errors
77 tasks executed
```

The full-run log SHA-256 is
`243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`.
SpotBugs retained the repository's configured non-blocking baseline findings.
These counts are dated mutable evidence, not a numerical coverage claim or a
packaged, runtime, native-platform, or general-authority release gate.

## Remaining release work

This acceptance does not yet qualify:

- structural-loss and post-preview-drift shapes beyond the six bounded REQ-008
  cases;
- daemon, LSP, MCP, and recipe parity beyond the bounded REQ-004 lexical-review
  shape;
- promotion of the locally observed REQ-001 package mode to any post-commit
  native CI ledger row until its exact receipt is reviewed;
- Linux, Windows, macOS x86-64, and macOS ARM64 REQ-001 native qualification
  while all rows remain `CONFIGURED_UNOBSERVED`; or
- Java/Maven diagnostics authority outside the bounded `moveClass` contract.
