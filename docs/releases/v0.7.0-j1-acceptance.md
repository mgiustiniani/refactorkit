# J1 bounded acceptance — 2026-09-08

Status: **local packaged acceptance GREEN; multi-host gate waived for 0.7.0;
independent catalogue review remains pending**.

Update: [approved change V070-LOCAL-HOST-ACCEPTANCE-001](../requirements/v0.7.0-local-host-acceptance-approved-change-001.md)
removes the repeated multi-host matrix from the `0.7.0` blocking gates. The
existing local evidence below is retained without rerunning unchanged product
tests. This does not turn missing host reports or review into PASS.

Authority boundary: this report records execution and CI repair, not new requirements
or support promotion. The [approved Java catalogue](../requirements/java-j1-catalogue-approved-baseline-001.md)
and [packaged module-rename approved change](../requirements/req-java-maven-module-rename-packaged-001-approved-change-001.md)
remain normative. [ARC42](../arc42/appendix-requirements.adoc) remains the integrated
architecture narrative; the [support matrix](v0.7.0-support-matrix.md) is its bounded
release projection.

## Reused product scope

- Preserve the eight frozen qualified catalogue rows; no planner was widened.
- Preserve the seven characterization rows and four deterministic refusal-only rows.
  Their required independent catalogue review is still pending; fresh regression
  does not substitute for that review or manufacture tests-first RED.
- Use only `testdata/acceptance/java-maven-move-class-authority-20-modules`.
  Its fresh disposable copies are the approved real-project acceptance subject;
  no external repository or second sample is required.
- Request exactly `catalog-model -> catalog-domain`, with explicit
  `newArtifactId=catalog-domain`. Preserve the five-edit oracle, operation-owned
  diagnostics gate, sole schema-v8 transaction and exact normal rollback.

## Clean-candidate packaged result

The unchanged existing outline passed **3 cases / 138 steps**, with zero failures,
skips, undefined or ambiguous steps. Each packaged CLI/daemon/MCP case used its own
copy, the extracted self-contained distribution, embedded Java, scrubbed environment
and poison PATH. The finalizer reported `PASSED` in **LOCAL_PRELIMINARY** mode, not
native CI qualification. The Java navigation fix was included; uncommitted T5 was not.

| Bound subject | Identity |
|---|---|
| Commit | `21550da09d62a2a10b870dabcccdb6d70f5fceb3` |
| Tree / state | `bf9b2e0a94e8f2a0ac1c81a07fc2fba86ea9d92a` / `CLEAN` |
| Host / runtime | Linux x86-64 / OpenJDK 21.0.11 |
| Candidate ZIP SHA-256 | `34eba5f21dad1296eb8dd7c71682af9e3c61bbcd0bfd3afc9b3488b93cad9b05` |
| Cucumber JSON SHA-256 | `129b9f8512b0a90d2e68105132ee14d99f6b48bbf5947bf2a2025e29ef725089` |
| Host manifest SHA-256 | `71307c6f6fc4bbff25bf69a3a7d79fd2333708a4a8bff930b073ad6f9de16baa` |

The local run is retained under
`.refactorkit/runs/v070-closure-tasks-20260908/j1-r001/`; it is not a published asset.
`packaged-local-evidence/` archives the complete reports and manifests.
The candidate remains isolated at `build/v070-j1-candidate-21550da0`.

Reproduce from a clean checkout using JDK 21 (offline dependencies already present):

```sh
./gradlew --offline --no-daemon --max-workers=2 \
  -Pkotlin.compiler.execution.strategy=in-process \
  :modules:refactorkit-cli:packagedMavenModuleRenameQualificationTest \
  :modules:refactorkit-cli:finalizePackagedMavenModuleRenameQualificationEvidence \
  --continue
```

Separate regression on the same still-clean candidate passed in 19m 26s:
Java **274 passed / 5 skipped**, JVM **278 passed / 5 skipped**, and packaged
annotated-navigation **1 case / 4 steps passed**. No failures or errors occurred.
The eleven J1 characterization/refusal requirements account for **113 cases /
793 steps passed**, with unchanged refusal scope. Reports are retained in
`source-regression-evidence/` and `j1-catalogue-regression.json`; the regression log
SHA-256 is `57a3eb95a2ce3e785be85d8e9e91d2916793c5d93484230f8be997bc4cea7a3e`.
These are regression results, not independent review or a new native qualification.

## CI repair, without replacing qualified infrastructure

Inspection of `b7bf1b2a` found four previously versioned native jobs and policy checks
removed, plus exact JDK pins reverted to unavailable `21.0.11+9`. The repair restores
those blocks from `b01ab414a47277714f42f51d45667d74af58648e`, retaining the current
module-rename task, finalizer test and always-run uploads. Redundant partial move-class
steps are removed because the restored dedicated matrix owns the same artifact names.
No new job design, runner, dependency, product behavior or protocol is introduced.

The two module-rename steps use `!cancelled()` so earlier unrelated failures cannot
suppress their evidence; those failures still fail the overall job. The existing
claim checker now accounts for all six restored JDK pins and confines K1/K2 identity
checks to their owning job. Existing negative tests exposed both defects before the
checker correction; all nine claim-checker tests now pass. Static YAML checks also
verify exact restored job definitions and unique expanded artifact names.

## Remaining gates and documentation gaps

| Status | Remaining obligation |
|---|---|
| `WAIVED_BY_USER` for `0.7.0` | Repeated four-host execution and reconciliation are not release prerequisites under V070-LOCAL-HOST-ACCEPTANCE-001. The 3-case/138-step Linux local packaged result stands; missing final-candidate host reports remain not verified. No branch, PR or CI run is required solely for this waived gate. |
| `missing-agent-capability` | Obtain the independent `requirements-quality-reviewer` review required by the catalogue. No multi-agent workflow was active; self-produced receipts are not independent approval. |
| `stale` | The restored K5 finalizer's existing sealed-requirement test fails on the changed signature requirement hash. The finalizer, test and requirement are unchanged from this continuation's starting HEAD. This belongs to the existing K5 task; its checksums were not rewritten to bypass review. |

The original four-host packaged feature remains `@absent`; its full matrix was
not executed. Historical J1 native checkboxes remain unchecked but no longer
block `0.7.0` for that waived matrix. The independent catalogue review is still
recorded as pending. The local pass and waiver do not qualify general Maven,
untested platforms, T5, K5, release assets, or the complete 0.7.x band.
