# RefactorKit v0.7.x Kotlin and JVM Interoperability Release Plan

Status: active development. Product-owner execution order is J1 Java closure,
then T5 TypeScript/JavaScript closure, then remaining K5 Kotlin closure, followed
by I1 release gates.

Baseline: published `v0.6.2`; current development version `0.7.0-SNAPSHOT`;
API `0.2` remains active until an explicit compatibility migration is accepted.

The `0.7.x` band may use multiple patch releases. A language capability is
promoted only when its own semantic, transaction, native-platform and packaged
acceptance evidence is complete. By product decision, every open K5, T5, J1 and
I1 row is now release-blocking for closure of the band; the controlling scope is
[`v0.7.x-completion-contract.md`](../requirements/v0.7.x-completion-contract.md).

## Objective

Deliver the first deep Kotlin semantic adapter and prove that the language-neutral
Build Model SPI and adapter kernel can support a second JVM language without
encoding Java-only assumptions. The band must establish conservative Java/Kotlin
interoperability while continuing Java depth and completing the explicitly
deferred advanced TypeScript/JavaScript operations from the `0.6.x` plan.

Kotlin support is not considered deep or stable because parsing, outline, or an
LSP rename works. Managed mutation requires hash-bound toolchain and project
evidence, semantic identity, exact diagnostics, normalized proposals, preview,
explicit authorization, `PatchEngine` apply, WAL, recovery and rollback.

## K1 — Kotlin toolchain and backend boundary

- [x] Add `refactorkit-kotlin` behind the existing language adapter registry; core
  does not depend on Kotlin compiler or IDE classes. The initial descriptor owns
  `.kt`/`.kts` routing but advertises only typed `REFUSED`/`NONE` capabilities
  until the semantic backend is qualified.
- [x] Select and document versioned provider `kotlin-compiler-explicit-v1` with
  backend `kotlin-compiler-embeddable-k2`. The first declarative qualification row
  is JDK 21 plus Kotlin 2.0.21; no semantic authority is implied. An external
  language server may bootstrap read operations but is never sufficient stable
  mutation authority by itself.
- [x] Discover configured JDK and Kotlin compiler/backend inputs explicitly, bind
  executable/JAR/version/classpath evidence with SHA-256 and a deterministic
  projection hash, double-read critical evidence, and refuse unsupported,
  workspace-local-by-default or drifting toolchains.
- [x] Execute no Java, Kotlin compiler, Gradle tasks/plugins, Maven lifecycle,
  compiler plugins, annotation processors, KSP, kapt or project scripts during
  default discovery.
- [x] Define the first bounded external compiler lifecycle for diagnostics with
  cancellation, timeout, memory/output/process limits, clean environment,
  immutable overlay, shutdown, process-tree termination and typed crash/error
  handling. Persistent-session crash recovery remains a later capability.
- [ ] Publish qualified Kotlin/JDK/Gradle/Maven support rows and license/SBOM
  boundaries before any managed capability is advertised as supported.

## K2 — Kotlin and shared JVM project model

- [x] Project Kotlin main/test/integration/custom/generated source sets through
  `kotlin-jvm-projection-v1` over the existing Build Model SPI with exact module
  ownership, target JDK/bytecode, classpath, outputs, module edges and hash-bound
  descriptor/toolchain evidence. Java-only dependency nodes remain graph-visible
  without claiming cross-language symbols.
- [x] Extend bounded declarative Maven and Gradle providers only where Kotlin JVM
  roots, plugin identity, `jvmTarget` and JDK/release metadata are literal or
  effective-model facts. Plugin-derived or executable-only model data remains
  `PARTIAL`, `EXECUTION_REFUSED`, or unavailable; Multiplatform/Android refuse.
- [ ] Model JVM binary names, Kotlin source declarations and callable/property
  identity without exposing compiler-internal handles in API `0.2`.
  - [x] Qualify the first regular-class and nested-class row with compiler `ClassId`,
    matching generated `.class` evidence, exact compiler PSI declaration ranges,
    and opaque `kotlin-jvm-type-v1` IDs.
  - [x] Extend the same proven type-ID row to top-level and nested interfaces,
    enum classes and annotation classes.
  - [x] Add named, data, nested and companion objects under additive `OBJECT`
    kind and the same binary-derived type-ID family. Anonymous objects remain
    outside the catalogue.
  - [x] Add the first non-overloaded top-level/direct-member function row with
    file-facade/owner class-file evidence and `kotlin-jvm-callable-v1` IDs derived
    from owner, JVM method name and descriptor. Properties, constructors,
    parameters and overloaded/renamed JVM callables remain open.
- [ ] Establish cross-language Java/Kotlin declaration and reference identity for
  supported JVM-visible types and members.
- [ ] Keep Kotlin/JVM, Kotlin Multiplatform, Android, generated code, compiler
  plugins, `expect`/`actual`, and script sources as separate capability/evidence
  cases. Unsupported boundaries must refuse rather than silently degrade.
- [ ] Validate the Build Model SPI against Kotlin so any remaining Java/Maven
  assumptions are removed from shared contracts rather than copied into the new
  adapter.

## Shared workspace intelligence foundation

- [x] Add the first provider-neutral, session-owned `WorkspaceIndex` bound to the
  exact project snapshot, with a whole-workspace source inventory, bounded
  provider declaration partitions, conservative incremental reconciliation and
  explicit evidence/completeness/truncation metadata.
- [x] Expose read-only `index.status` plus typed API `0.2`
  `intelligence.query` workspace/document symbol search and CLI human/optional
  JSON output. TypeScript/JavaScript completion, hover and signature help plus
  saved-snapshot Java definition/references/hover are qualified rows; unsupported provider rows
  remain explicit refusals.
- [x] Merge the existing persistent TypeScript/JavaScript language-server
  declaration projection into the central index under saved-snapshot,
  provenance, file/symbol and aggregate-deadline bounds. See
  `v0.7.0-typescript-workspace-index-acceptance.md`.
- [x] Add the shared immutable editor-overlay foundation and migrate TypeScript
  exact diagnostics to it without changing API `0.2` wire behavior. See
  `v0.7.0-editor-overlay-foundation-acceptance.md`.
- [x] Route TypeScript interactive queries through versioned editor overlays and
  add cancellation, file watching and durable provider-partition invalidation.
- [ ] Continue from the snapshot-keyed normalized JDT cache to finer persistent
  JDT compiler state and add persistent K2 integrations. See
  `v0.7.0-intelligence-index-plan.md`.

## K3 — Semantic read operations

Status: complete. Native packaged qualification passed in GitHub Actions run
`29439584115` on Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64.

- [x] Provide bounded project symbol search, document symbols, definition and
  references for qualified Kotlin/JVM projects.
  - [x] Provide saved-snapshot workspace/document search and ID-based definition
    for compiler-proven classes, named/companion objects, interfaces, enum classes,
    annotation classes and the first bounded function row.
  - [x] Add K2 FIR-resolved call-name definition and bounded partial references
    for the existing callable-ID row under
    `docs/requirements/kotlin-jvm-function-usage-navigation.md`; worker evidence
    plus daemon, CLI and MCP position routes. Packaged/native qualification passed
    in run `29432766317`. Imports, callable references, Java callers and mutation
    authority remain excluded.
  - [x] Add K2 FIR-resolved type/constructor/qualifier definition and bounded
    partial references for the existing type-ID row under
    `docs/requirements/kotlin-jvm-type-usage-navigation.md`; internal worker plus
    daemon, CLI and MCP routes qualified in run `29439584115`. Cross-language
    callers, alias/star import directives, strings and mutation authority remain
    excluded until separately qualified.
- [x] Provide compiler-backed read-only diagnostics for the first qualified
  single-module Kotlin/JVM boundary, with explicit line-only/no-location precision,
  toolchain/build/snapshot attestation and typed refusal/error results.
- [x] Define stable opaque Kotlin symbol IDs for the qualified type/function
  catalogue from durable semantic identity rather than source offsets or backend
  object identity. Future property/parameter families require separate ID rows.
  - [x] Define `kotlin-jvm-type-v1:<sha256>` for the proven declared-type row from
    the JVM binary name and `kotlin-jvm-callable-v1:<sha256>` for bounded functions
    from JVM owner/name/descriptor; no kind, path, offset, process or compiler
    handle enters either ID.
- [x] Preserve exact UTF-16/API coordinate conversion, source hashes, document
  versions and immutable workspace overlays where an external backend is used;
  shifted-source, astral-character, stale-authority and native packaged cases pass.
- [x] Aggregate Java and Kotlin diagnostics without duplicate cascades; missing
  build/toolchain inputs produce concise typed roots. Legacy API `0.2`
  `diagnostics languageId=jvm` is deterministic, language-tagged, read-only and
  covered by packaged Kotlin acceptance; correlated provider envelopes retain
  precision authority.
- [x] Expose the diagnostics backend, version/evidence, bounded runtime,
  toolchain/build completeness and refusal reasons through library, CLI, daemon,
  LSP capability metadata and MCP. Other semantic reads remain refused.

## K4 — Managed Kotlin rename foundation

Status: complete for the explicitly bounded private-declaration foundation.
Packaged/native qualification passed in GitHub Actions run `29451134010`; public
or incomplete shapes continue to refuse and no broad stable Kotlin rename is
claimed.

- [x] Qualify the first Kotlin-only private-declaration rename row with a complete
  K2 reference barrier, exact staged compiler diagnostics and managed transaction
  acceptance.
- [x] Qualify semantic rename for an initial Kotlin/JVM catalogue covering private
  classes, interfaces, objects, enum/annotation classes, direct functions,
  field-backed properties, function parameters and function type parameters where
  backend proof is exact.
- [x] Update constructors, callable/property references, imports, aliases and file
  names only when the semantic backend proposes and RefactorKit validates the
  complete operation. Proven direct forms are updated; aliases, callable
  references, delegated/constructor properties and filename/public API changes
  refuse instead of guessing.
- [x] Cover the Java-to-Kotlin boundary with a Java source naming a public Kotlin
  type and return stable `kotlin.renameCrossLanguageIncomplete`. Cross-language
  mutation remains refused until shared JVM identity is exact.
- [x] Detect/refuse exact quoted reflection/serialization/framework candidates,
  generated source ownership, scripts/plugins/unsupported build models and unknown
  external/public consumers.
- [x] Require preview, affected files, risk/evidence, before/staged diagnostics,
  explicit authorization, `PatchEngine`, WAL, rollback, stale authority and
  lock-time toolchain/build-evidence revalidation.

## K5 — Idiomatic Kotlin depth

The first prerequisite is the bidirectional public-type identity contract in
[`java-kotlin-shared-jvm-rename.md`](../requirements/java-kotlin-shared-jvm-rename.md).
Public member interoperability follows the independently gated
[`java-kotlin-shared-jvm-member-rename.md`](../requirements/java-kotlin-shared-jvm-member-rename.md).

These capabilities may span later `0.7.x` patch releases, remain part of the
Kotlin band and are mandatory before the band closes:

- [ ] Move declaration/file/package with import and Java interoperability updates.
  The first bounded public top-level type row is governed by
  [`kotlin-jvm-move-declaration.md`](../requirements/kotlin-jvm-move-declaration.md).
  - [x] Implement local and packaged CLI/daemon preview/apply/rollback for the
    explicit-import same-source-set row.
  - [x] Complete four-platform native qualification for the bounded
    explicit-import row (run `29482085130`).
  - [x] Complete standalone packaged MCP preview/apply/rollback acceptance on
    all four supported platforms (run `29488253198`).
  - [x] Add deterministic imports for compiler-proven same-package implicit
    Kotlin and Java consumers with local packaged acceptance.
  - [x] Qualify same-package consumer insertion on all four native platforms
    (run `29490554985`).
  - [x] Add exact fully-qualified Kotlin/Java target rewrites with local packaged
    acceptance and mixed-shape refusal.
  - [x] Qualify fully-qualified rewrites on all four native platforms (run
    `29493776932`).
  - [x] Remove the fabricated cross-language-consumer requirement with separate
    Kotlin-only and Java-only local packaged acceptance.
  - [x] Qualify independent consumer sets on all four native platforms (run
    `29501301246`).
  - [x] Add zero-consumer public type moves with staged K2 declaration identity
    and local packaged CLI acceptance.
  - [x] Qualify zero-consumer moves on all four native platforms (run
    `29503704995`).
  - [x] Add K2-proven Kotlin alias uses with exact alias-token preservation and
    local packaged daemon acceptance.
  - [x] Qualify alias-preserving moves on all four native platforms (run
    `29507916564`).
  - [x] Add compiler-proven Kotlin/Java package-star consumers by preserving the
    star and inserting one exact destination import, with local packaged
    MCP/daemon acceptance.
  - [x] Qualify star-import moves on all four native platforms (run
    `29511930846`).
  - [x] Add whole-file moves with compiler-proven private top-level helper
    declarations and local packaged CLI/daemon/MCP acceptance.
  - [x] Qualify private-helper type/function file moves on all four native
    platforms (run `29516695594`).
  - [x] Add explicit local and packaged private top-level property identity
    coverage.
  - [x] Qualify private-property helper moves on all four native platforms (run
    `29522349554`).
  - [x] Add whole-file moves for additional public top-level types whose complete
    Kotlin/Java consumers use exact explicit imports, with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify public-sibling file moves on all four native platforms (run
    `29527320435`).
  - [x] Add FIR-proven Kotlin alias consumers for additional public sibling types
    with local packaged daemon acceptance.
  - [x] Qualify aliased public-sibling moves on all four native platforms (run
    `29533418906`).
  - [x] Add exact Kotlin/Java package-star consumers for additional public sibling
    types with sorted explicit destination imports and local packaged MCP/daemon
    acceptance.
  - [x] Qualify package-star public-sibling moves on all four native platforms
    (run `29538823488`).
  - [x] Add compiler-proven same-package implicit consumers for additional public
    sibling types with sorted destination imports and local packaged MCP/daemon
    acceptance.
  - [x] Qualify same-package public-sibling moves on all four native platforms
    (run `29543325594`).
  - [x] Add exact fully-qualified Kotlin/Java consumers for additional public
    sibling types with local packaged MCP/daemon acceptance.
  - [x] Qualify fully-qualified public-sibling moves on all four native platforms
    (run `29558131857`).
  - [x] Add local packaged CLI/daemon/MCP acceptance for selecting a non-filename
    public sibling while preserving the original file path.
  - [x] Qualify non-filename sibling selection on all four native platforms (run
    `29560641892`).
  - [ ] Expand to the next bounded move shape.
- [ ] Organize imports and deterministic project-style-aware formatting.
  - [x] Add and qualify the bounded explicit-type-import library planner with
    complete K2 usage evidence, CRLF preservation, staged diagnostics and
    rollback on all four native test platforms (run `29566422517`).
  - [x] Route through CLI/daemon/MCP and qualify packaged Linux
    preview/apply/rollback.
  - [x] Qualify packaged CLI/daemon/MCP execution on all four native platforms
    (run `29571167892`).
  - [x] Add FIR-proven external/source type aliases with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify FIR-proven alias imports on all four native platforms (run
    `29575465366`).
  - [x] Add exact package-star preservation/sorting with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify exact package-star imports on all four native platforms (run
    `29601040232`).
  - [ ] Add callable imports and project-style-aware formatting.
- [ ] Change signature across overrides, overloads, default/named arguments and
  Java callers.
- [ ] Add bounded extract/inline operations with control/data-flow proof.
- [ ] Model extension receivers, suspend functions, companion/data/sealed/value
  classes, delegated properties and JVM annotations.
- [ ] Add explicit `expect`/`actual`, multiplatform, Android and framework-aware
  capability matrices before claiming support for those shapes.

## T5 — Deferred TypeScript/JavaScript advanced operations

- [x] Add additive API `0.2` `diagnostics.v2` with exact UTF-16 ranges,
  request/snapshot/semantic-lease correlation, saved/immutable-overlay authority,
  compiler attestation and runtime limits, structured readiness, bounded response,
  CLI/MCP access and unchanged legacy `diagnostics` compatibility.

The following work was explicitly deferred by the `0.6.x` plan. It proceeds in
parallel without weakening Kotlin gates and is mandatory before `0.7.x` closes:

- [ ] Move file/type/module with import/export and project-reference updates.
- [ ] Organize imports and deterministic project-style-aware formatting.
- [ ] Change signature with overload/override/call-site coverage.
- [ ] Add bounded extract/inline operations where control/data-flow proof exists.
- [ ] Add TypeScript/JavaScript migration recipes without executing framework or
  package build code.

Each operation retains explicit-toolchain provenance, exact compiler diagnostics,
preview, approval, `PatchEngine`, WAL and rollback. Dynamic or mixed JavaScript
never inherits typed TypeScript certainty.

## J1 — Concurrent Java and shared-platform depth

- [ ] Continue Java change-signature, move/package/module, extract/inline and
  hierarchy catalogue expansion using JDT-backed evidence.
  - [x] Add exact JDT method-parameter declaration/use identities and a bounded
    signed-overload-aware rename-parameter planner with clean staged JDT proof.
  - [x] Qualify parameter rename through packaged CLI preview/apply/rollback on
    all four native platforms (run `29607477633`).
  - [x] Add local packaged daemon/MCP parameter-rename preview/apply/rollback.
  - [x] Replace lexical add/remove/reorder authority with exact JDT method,
    parameter and invocation evidence plus disposable staged-source validation.
  - [x] Qualify packaged CLI/daemon/MCP rename/add/remove/reorder on all four
    native platforms (run `29614333900`).
  - [x] Add bounded JDT parameter type change with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify parameter type change on all four native platforms (run
    `29618448454`).
  - [x] Add bounded override/implementer-family add-parameter with explicit
    external-consumer-risk acceptance and local packaged CLI/daemon/MCP evidence.
  - [x] Qualify hierarchy add through packaged CLI/daemon/MCP on all four native
    platforms (run `29621466959`).
  - [x] Add hierarchy remove-parameter with zero-use proof and local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify hierarchy remove through packaged CLI/daemon/MCP on all four
    native platforms (run `29624918322`).
  - [x] Add local JDT-bound hierarchy reorder using a selected-name-to-index
    permutation across declarations whose parameter names may differ.
  - [x] Add packaged CLI/daemon/MCP hierarchy reorder acceptance with exact rollback.
  - [x] Qualify hierarchy reorder on all four native platforms (run `29626304459`).
  - [x] Add local hierarchy type change with staged connectivity, call-binding,
    and body-use-count validation.
  - [x] Add packaged CLI/daemon/MCP hierarchy type-change acceptance with exact rollback.
  - [x] Qualify hierarchy type change on all four native platforms
    (run `29628955986`).
  - [x] Add local bounded private-constructor rename/add/remove/reorder/type-change
    with exact class-creation, `this(...)`, and `super(...)` invocation evidence.
  - [x] Qualify private constructors through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify the private-constructor row on all four native platforms
    (run `29634807550`).
  - [x] Add local executable rows for private generic methods, one-explicit-
    argument varargs, and annotation-preserving parameter reorder.
  - [x] Qualify bounded generic/varargs/annotation rows through packaged CLI/daemon/MCP.
  - [x] Qualify these bounded rows on all four native platforms
    (run `29640122922`).
  - [x] Add local public ordinary-constructor explicit-risk acceptance and
    fail-closed record-constructor coverage.
  - [x] Qualify public constructors through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify public constructors on all four native platforms
    (run `29642091983`).
  - [x] Add local exact-overload isolation, lambda-body caller update, functional-
    reference refusal, and multi-expanded-varargs refusal matrix.
  - [x] Qualify overload/lambda through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify the overload/lambda/refusal matrix on all four native platforms
    (run `29643185758`).
  - [x] Add a JDT-only mixed-JVM hard barrier for public constructors and
    hierarchy signature mutation.
  - [x] Qualify the mixed-JVM refusal through the packaged CLI.
  - [x] Qualify the mixed-JVM refusal on all four native platforms
    (run `29646244081`).
  - [x] Add packaged byte-identical method-reference and record-constructor refusal.
  - [x] Qualify the expanded refusal matrix on all four native platforms
    (run `29647225014`).
  - [x] Add local opt-in static-method/constructor-reference migration to staged
    JDT-bound lambdas.
  - [x] Qualify bounded functional-reference migration through packaged CLI/daemon/MCP.
  - [x] Qualify bounded functional-reference migration on all four native platforms
    (run `29651568518`).
  - [x] Add local shared Java/Kotlin `addParameter` using ECJ→K2 external callable
    evidence, JDT Java calls, staged diagnostics, WAL, and exact rollback.
  - [x] Qualify shared Java/Kotlin add-parameter through packaged CLI/daemon/MCP.
  - [x] Qualify shared Java/Kotlin add-parameter on all four native platforms
    (run `29655068022`).
  - [x] Close the bounded Java change-signature catalogue with no pending row.
- [ ] Establish authoritative release-aware Java/Maven diagnostics under
  [`java-maven-authoritative-diagnostics.md`](../requirements/java-maven-authoritative-diagnostics.md)
  before granting broader Maven ownership-migration authority.
  - [x] Add bounded explicit-JDK `ct.sym` release-signature attestation with
    double-read hashes, deterministic identity and typed refusal; this evidence
    does not yet grant JDT authority.
  - [x] Bind effective Maven `--release` to an attested, bounded `ct.sym` JEP 247
    view in the JDT source-set environment; source-only configurations refuse
    implicit platform selection.
  - [x] Add permanent library and packaged CLI/daemon/MCP Java 8/21 evidence for
    historical API rejection, complete current `java.se` visibility, transitive
    reactor sources, read-only execution, concise missing-platform roots and
    post-model module filtering on all four native platforms (run `29660037679`).
  - [ ] Qualify exact releases 8 through 25 with permanent platform inputs and
    four-platform native evidence.
    - [x] Add the local packaged CLI/daemon/MCP release matrix using one explicit
      hash-attested JDK 25 input, ECJ's exact JEP 247 historical views and a
      `jdk.zipfs`-capable reduced runtime; native qualification remains open.
  - [x] Add the first Maven scope-derivation row: hash-bound runtime-only
    external artifacts stay outside owner main compilation and flow through a
    reactor edge only to downstream test compilation (native run `29671357121`).
  - [x] Add the first source-set availability row: a missing test-only artifact
    yields one typed test root, suppresses only derivative test binding rows and
    preserves genuine main diagnostics (packaged native run `29682355909`).
  - [x] Propagate unavailable main compile environments through transitive
    reactor source-set closures and unavailable runtime exports only into
    downstream test environments (native run `29683959746`).
  - [x] Add bounded breadth-first nearest/declaration-order mediation, effective
    root dependency-management selection, optional/exclusion omission and
    non-transitive `provided` children for external Maven artifacts (native run
    `29687396137`).
  - [x] Add bounded local version-range selection and typed refusal for arbitrary
    dependency artifact types and Maven relocation metadata (native run
    `29688995827`).
  - [x] Bind managed-apply, recipe and Java mutation diagnostic deltas to exact
    provider/category/evidence/path/range/message identity (native runtime run
    `29692500361`; corrected Linux/golden run `29693799767`).
  - [x] Rehydrate committed and restored bytes through the same diagnostics gate;
    automatically roll back post-apply provider/identity failures (native run
    `29699796789`).
  - [x] Prove transitive Maven main/test impact diagnostics and pre-WAL refusal
    from an upstream-only source mutation (native run `29706289604`).
  - [ ] Complete remaining Maven classifier/variant and scope rows and permanent
    packaged rows.
- [ ] Add transactional Java ownership migration across Maven reactor modules:
  move the proven source set/type/package, update only authoritative reactor POM
  module/dependency edges when required, reject dependency cycles or unresolved
  effective-model ownership, and validate the staged reactor without executing
  project plugins or using the network.
  - [ ] Keep module-directory moves, parent `<modules>` edits, `artifactId`/`groupId`
    changes and dependency-coordinate rewrites as explicit independently
    previewable arguments; never infer them from a Java package move.
  - [ ] Preserve POM comments/unknown elements, refuse ambiguous property-managed
    coordinates, and apply Java plus POM edits through one `PatchEngine` plan with
    diagnostics, authorization, WAL, recovery and byte-exact rollback.
  - [ ] Qualify existing-module ownership moves first, then module rename/move,
    using multi-module Maven fixtures and disposable real-project acceptance.
- [ ] Reuse shared JVM identity and Build Model improvements rather than adding
  Kotlin-specific branches to Java planners.
- [ ] Expand Spring, Jakarta/JPA, Jackson, reflection/configuration and generated
  code risk evidence where cross-language JVM operations expose those boundaries.
- [ ] Retire compatibility projections only through an explicit API migration;
  API `0.2` behavior remains supported throughout this band.

## I1 — Integrations and native acceptance

- [x] Preserve strict read-only startup semantics: project open/scan/LSP
  initialization inspect WAL state without creating `.refactorkit` or
  `workspace.lock`; incomplete state refuses until explicit mutating
  `patch.recover`. Packaged semantic-read acceptance hashes hidden metadata.
- [x] Route Kotlin files, mixed snapshots, compiler diagnostics and the bounded
  declared-type symbol/definition row through library, CLI, daemon capability
  metadata and MCP while preserving editor-native Kotlin LSP ownership. Bounded
  function/type references are explicitly partial; K4 now adds bounded managed
  private-declaration mutation while broader K5 routes remain open.
- [ ] Add representative Maven, Gradle, mixed Java/Kotlin and refusal fixtures;
  no sample may require implicit network access or build-script execution.
- [ ] Add unit, property, golden, protocol, agent-simulation, hostile-workspace,
  stress, timeout and crash/restart coverage.
- [x] Qualify at least one non-trivial Kotlin or Java/Kotlin semantic mutation on
  Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64 using the packaged
  RefactorKit runtime and explicitly qualified external toolchain inputs.
- [x] Verify preview, exact staged diagnostics, authorization, apply, WAL,
  kill-during-write recovery, rollback and byte-identical restoration natively.
- [ ] Publish support matrix, limitations, migration notes, checksums, SPDX SBOMs,
  attestations and independent downloaded-asset verification.

## Explicit non-goals for the first `0.7.0` slice

- Claiming all Kotlin Multiplatform, Android, compiler-plugin, kapt/KSP or script
  projects as supported.
- Treating LSP rename alone as managed Kotlin authority.
- Executing Gradle or Maven project code to obtain a model.
- Freezing API `1.0` or retiring compatibility models without migration evidence.
- Completing K5, T5 and J1 in one oversized commit. All rows are nevertheless
  required before the band closes and shall land as independently accepted slices.

## First implementation sequence

1. Activate `0.7.0-SNAPSHOT` metadata and this plan.
2. Add the isolated `refactorkit-kotlin` module and capability descriptor with all
   unsupported semantic operations explicitly refused.
3. Implement hash-bound Kotlin toolchain discovery and bounded project-model
   projection.
4. Deliver semantic reads and exact diagnostics on a committed Kotlin/JVM sample.
   Diagnostics plus the compiler-proven class/object/interface/enum/annotation
   search/definition row and first bounded function identity are complete;
   property/constructor identities, usage resolution and references continue in
   smaller slices.
5. Deliver one managed Kotlin rename end to end.
6. Add exact Java/Kotlin interoperability evidence and refusal boundaries.
7. Expand K5/T5/J1 operations in small independently accepted slices.
8. Run full native packaged acceptance before publishing any `v0.7.x` tag.

## Release gate

The first `v0.7.x` release requires a real Kotlin compiler/Analysis API-backed
mutation—not structural or lexical fallback—to pass semantic resolution, exact
normalized preview, staged diagnostics, explicit authorization, managed apply,
WAL, recovery and rollback on every supported native platform. At least one mixed
Java/Kotlin boundary must either succeed with exact evidence or refuse with a
stable typed reason. Existing Java and TypeScript capabilities must remain green.
