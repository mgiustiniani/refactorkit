# RefactorKit v0.7.x Kotlin and JVM Interoperability Release Plan

Status: active development. Product-owner execution order is P0 worktree
stabilization, then P0 binding-clean Maven move-class managed apply, followed by
J1 Java closure, T5 TypeScript/JavaScript closure, remaining K5 Kotlin closure,
and I1 release gates.

Baseline: published `v0.6.2`; current development version `0.7.0-SNAPSHOT`;
API `0.2` remains active until an explicit compatibility migration is accepted.

The `0.7.x` band may use multiple patch releases. A language capability is
promoted only when its own semantic, transaction, native-platform and packaged
acceptance evidence is complete. By product decision, every open K5, T5, J1 and
I1 row is now release-blocking for closure of the band; the controlling scope is
[`v0.7.x-completion-contract.md`](../requirements/v0.7.x-completion-contract.md).
Qualified and pending boundaries are tracked in
[`v0.7.0-support-matrix.md`](v0.7.0-support-matrix.md); additive API `0.2`
adoption guidance is in [`v0.7.0-migration.md`](v0.7.0-migration.md).

## Objective

Deliver the first deep Kotlin semantic adapter and prove that the language-neutral
Build Model SPI and adapter kernel can support a second JVM language without
encoding Java-only assumptions. The band must establish conservative Java/Kotlin
interoperability while continuing Java depth and completing the explicitly
deferred advanced TypeScript/JavaScript operations from the `0.6.x` plan.

Kotlin support is not considered deep or stable because parsing, outline, or an
LSP rename works. Managed mutation requires hash-bound toolchain and project
evidence, semantic identity, exact diagnostics, normalized proposals, preview,
explicit authorization, `PatchEngine` apply, WAL, recovery and rollback.

Current P0 admission status (2026-08-02): the local all-module build-scaffolding
gate is resolved on the corrected working tree. The historical clean pushed
`main` revision `2b5aa7234cc385214acb6958834763288aefc527` failed OpenJDK 21
`./gradlew --no-daemon testClasses` before compilation (log SHA-256
`408d45e7e2c1bea4152c220af283455421a3bee84dca2b3f069832e9541983d5`). The
authorized slice reproduced RED with SHA-256
`72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`, changed
only `modules/refactorkit-kotlin/build.gradle.kts` to resolve
`testRuntimeClasspath` inside `tasks.test.doFirst`, and passed the GREEN rerun
with SHA-256 `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`.
The subsequent full OpenJDK 21 `check goldenTest` run passed 82/82 tasks, 1,062
tests with 228 skipped, and zero failures/errors (log SHA-256
`48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). This
is bounded source-built scaffolding evidence only. Actions run `30746237578`
remains a separate `failure` for the historical SHA (three failed jobs; macOS
arm64 succeeded); corrected-revision CI, all unqualified K5/T5/J1/I1 work, and
release qualification remain open.

## P0 — Stabilization and Maven move-class managed-apply authority

A 2026-07-29 field report records a coherent 780-file, 20-module Maven scan and
an accurate four-file `move-class` preview, followed by correct pre-WAL apply
refusal because only review-only lexical evidence was available. This preserves
the safety contract but exposes a product-critical authority and usability gap.
See [`features/java-maven-move-class-apply-authority.feature`](../../features/java-maven-move-class-apply-authority.feature),
[`v0.7.0-java-maven-move-class-acceptance.md`](v0.7.0-java-maven-move-class-acceptance.md),
ARC42 requirements `RPK-JAVA-MOVE-001..007`, and source packet
`SRC-2026-07-29-004`. A detached reproduction against the pre-move Magrathea
commit first confirmed the original lexical refusal. Descriptor-closed Maven
traversal now prunes test/provided/optional/excluded child edges before child
POM or JAR lookup, while alternate selected paths still require complete
descriptors. The same detached commit now produces `JDT_BINDING`, applies the
four-file move in one transaction, and rolls back to the exact pre-apply source
hash on Linux/JDK 21. All 13 bounded Maven move-class authority definitions and
31 expanded fixture cases are validated. Independent final OpenJDK 21.0.11
`check goldenTest` verification passed 1000 discovered cases with 780 executed,
220 skipped/tag-filtered, zero failures/errors, and 77 tasks; log SHA-256
`243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`.
General-population authority remains open. The dedicated packaged public-CLI
mode and complete production-JAR matrix cover all 13 bounded requirement IDs/31
cases. Corrected native run `31336067318` checked out exact revision
`3782b1109aa8874ab887d6031d5bfa86bf5e9ab8`; Linux x86-64, Windows x86-64,
macOS x86-64, and macOS arm64 each passed both modes and emitted a fail-closed
revision/host/JDK/report-bound receipt. Aggregate SHA-256 is
`8047b8c93e25175b2efd1af74e6be734317ac4cb734ebe7ac5f465a8c74dbfdc`.
All four operation-specific native rows are `PASSED` after independent review
SHA-256
`8569012fad31ee62da02b1cefe4792612ffdc80e68831be35c5ec86c8f8c8820`.
The broad workflow's separate failure is retained and no green whole-repository
cross-platform CI claim is made.

- [x] Restore a green all-module `build`/`check`/golden baseline before adding
  further capabilities; no selective test command may substitute for compiling
  every production and test source set. Verified locally on JDK 21 with `check`,
  `goldenTest`, `pmdAll`, and `spotbugsAll` after repairing the MCP test seam.
- [x] Reconcile roadmap and support-matrix claims with executable evidence;
  incomplete Kotlin, TypeScript, Maven-module, recipe, SBOM, and qualification
  rows remain open or explicitly refused. The fail-closed reconciliation receipt
  SHA-256 is
  `a891ca901cf363e2fb0980d26311385a5c50bd0b7e8efd31b13f19b52c641d98`;
  independent row-closure review SHA-256 is
  `db3767968229d86f2d8e8a2cb6b161e4cf1222573769b9c22fcac450f2d302c7`.
- [x] Add the permanent offline Maven fixture with one aggregator and 20 active
  modules, dependency-bounded main/test source sets, direct/transitive/test-only
  observers, same-name and unrelated decoys, generated-source evidence, and
  authority-loss variants.
  - [x] Add the plugin-free offline base fixture with 20 active JAR modules,
    three-plus dependency levels, a reproducible local external artifact,
    materialized generated source, direct/transitive/test-only Java observers,
    same-name/unrelated decoys, and an inert non-Java Gherkin candidate.
  - [x] Complete the bounded independently selectable authority-loss matrix.
    REQ-003 validates four readable/enumerable preview defects; REQ-008 validates
    four additional typed preview uncertainties, root-declared active-POM loss,
    and non-managed candidate-source drift under the workspace lock. REQ-013
    closes the remaining named unreadable, truncated, mixed-edit, classpath,
    affected-conflict, and staged-diagnostic admission shapes; only unbounded or
    general-population shapes remain outside this fixture contract.
- [x] Derive the complete reverse-observer closure from the effective reactor and
  require binding-clean JDT identity for the target and every managed Java edit.
  - [x] Prove the fully `AVAILABLE` base row with two deterministic
    `JDT_BINDING` previews, exact staged diagnostics, one transaction, and
    byte/path/inventory/snapshot-exact rollback
    (`REQ-JAVA-MAVEN-MOVE-AUTH-001`).
  - [x] Prove only exact binding-selected direct, transitive, and test observers
    are edited; distinguish binding-backed decoys from Java non-code and
    non-Java residual review risks (`REQ-JAVA-MAVEN-MOVE-AUTH-002`).
  - [x] Scope JDT cleanliness to an `AVAILABLE`, dependency-bounded closure so
    unrelated module warnings do not demote authority while owner/observer
    warnings remain blocking (`REQ-JAVA-MAVEN-MOVE-AUTH-006`).
  - [x] Add candidate-total target-scoped authority for one enumerated
    `OFFLINE_MISSING` leaf artifact with hash-bound expected identity and
    negative-presence evidence (`REQ-JAVA-MAVEN-MOVE-AUTH-007`).
  - [x] Preserve explicit effective transitive `test` scope over dependency-
    management defaults and permit a fully available target closure when global
    `OFFLINE_MISSING` evidence belongs only to excluded modules
    (`REQ-JAVA-MAVEN-MOVE-AUTH-009`).
  - [x] Make Maven graph completeness descriptor-closed over selected nodes:
    deterministically pruned scope/optional/exclusion edges require no child
    POM/JAR lookup, while an alternate selected path restores the descriptor
    requirement (`REQ-JAVA-MAVEN-MOVE-AUTH-010`).
  - [x] Requalify the reported pre-move Magrathea commit with the source-built
    CLI: `JDT_BINDING` preview, four affected files, one managed transaction,
    successful diagnostics gate, and exact source-tree restoration after
    rollback on Linux/JDK 21.
  - [x] Enumerate ordinary *selected* Maven repository missing-binary evidence
    without fixture-only identity sidecars (`REQ-JAVA-MAVEN-MOVE-AUTH-011`) and
    prove missing, malformed, or drifted selected POM/parent/BOM/mediation/
    relocation evidence refuses before semantic selection, lock, and WAL
    (`REQ-JAVA-MAVEN-MOVE-AUTH-012`).
- [x] Evaluate managed-write eligibility under the workspace lock before WAL;
  evidence drift, truncation, missing classpath/source path, conflicts, mixed
  lexical edits, or diagnostic regression must leave the workspace unchanged.
  REQ-JAVA-MAVEN-MOVE-AUTH-013 passed independent revision-bound review and its
  nine-case post-promotion source/package-matrix rerun; independent P0 row-
  closure review SHA-256
  `db3767968229d86f2d8e8a2cb6b161e4cf1222573769b9c22fcac450f2d302c7`
  authorized this status-only checkbox change.
  - [x] Reject immutable `LEXICAL_FALLBACK` evidence before workspace-lock
    acquisition, leaving no `.refactorkit`, lock, WAL, transaction, byte, path,
    inventory, or snapshot residue (`REQ-JAVA-MAVEN-MOVE-AUTH-005`).
  - [x] Revalidate the bounded target-scoped operation-authority lease and its
    required classpath presence/absence records under lock before WAL
    (`REQ-JAVA-MAVEN-MOVE-AUTH-007`).
  - [x] Bind non-managed candidate-source file evidence into the lease, capture
    the observed engine-owned snapshot under lock, and return typed
    `authorityLease.evidenceDrift` before WAL while preserving the caller's
    external change (`REQ-JAVA-MAVEN-MOVE-AUTH-008`).
  - Bind complete/non-truncated semantic evidence and the exact canonical
    normalized `WorkspaceEdit` identity; refuse all nine independently scoped
    truncation, widening, source/classpath, conflict, and diagnostic-regression
    conditions under lock before WAL (`REQ-JAVA-MAVEN-MOVE-AUTH-013`).
- [x] Return typed immutable non-managed `REVIEW_ONLY_GUIDANCE` for the bounded
  readable and safely enumerable Java/Maven CLI defects in
  `REQ-JAVA-MAVEN-MOVE-AUTH-003`. Schema/checklist v1 guidance is not a
  `PatchPlan`, has no applyable plan ID, WAL, transaction, or RefactorKit
  rollback claim, and CLI apply refuses before `PatchEngine` and workspace-lock
  acquisition.
- [x] Project broad legacy lexical uncertainty as immutable schema-v1
  `LEXICAL_FALLBACK_REVIEW` across the Java move-class outcome API, CLI, recipe,
  daemon, managed LSP, and MCP (`REQ-JAVA-MAVEN-MOVE-AUTH-004`). The envelope is
  explicitly not semantically complete, has no protocol plan/edit/apply
  capability, and known-operation apply attempts refuse before managed-write or
  editor application.
- [x] Fail closed for the six bounded phase-correct REQ-008 authority failures:
  typed non-managed guidance for candidate/lookup/closure/diagnostic uncertainty,
  typed structural refusal for a missing root-declared active POM, and lock-held
  pre-WAL refusal for non-managed candidate-source drift.
- [x] Prove semantic preview, one-transaction apply, exact post-apply diagnostics,
  byte-exact rollback, lexical pre-WAL refusal, and consistent library/CLI/
  daemon/LSP/MCP/recipe behavior on every qualified native platform. Dedicated
  run `31336067318` passed the public child-process oracle and the complete
  production-JAR 13-ID/31-case matrix on all four hosts; independent review
  SHA-256
  `8569012fad31ee62da02b1cefe4792612ffdc80e68831be35c5ec86c8f8c8820`
  authorized this status-only closure.
  - [x] Prove the fully `AVAILABLE` CLI semantic preview/apply/rollback row with
    deterministic evidence and one transaction.
  - [x] Prove the CLI lexical-refusal no-residue row with tagged Cucumber
    acceptance.
  - [x] Prove one bounded `OFFLINE_MISSING` semantic CLI preview/apply/rollback
    row with one transaction and byte-exact restoration.
  - [x] Prove the real reported pre-move reactor through the source-built CLI on
    Linux/JDK 21: semantic preview, one transaction, diagnostics-gated apply,
    and exact rollback.
  - [x] Prove bounded source-built/in-process daemon/LSP/MCP/recipe parity for
    the REQ-004 lexical-review projection and refusal shape.
  - [x] Implement and observe locally the dedicated
    `packagedMavenMoveClassAuthorityTest` source set/task as a second validation
    mode for existing REQ-001. One no-follow copy of the permanent fixture runs
    through the real unpacked launcher and embedded runtime with scrubbed subject
    environment, poison-`PATH` proof, and 16 bounded commands. Repeat
    `JDT_BINDING` preview, exact three-observer/five-path closure, the exact
    human-text lock refusal before WAL, one child-process-replanned apply with no
    cross-process plan-ID claim, a sole schema-v8 transaction, clean post-apply
    diagnostics, normal rollback, exact non-engine restoration, and zero
    timeouts/truncations/leaks passed. The journal has five top-level `FileEdit`
    entries—four `Modify` plus one `Rename`—with nested `TextEdit` counts
    `1/2/1/1`, not one text edit per modify. Ordinary `test`/`check` remain
    packaging-independent. Official final OpenJDK 21.0.11/Linux `amd64`
    execution of
    `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks`
    reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit 22
    discovered/one executed/21 tag-filtered/zero failures or errors; and one
    Cucumber scenario with five background plus fourteen scenario steps (19/19)
    and 2/2 hooks. Packaged log SHA-256
    `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`;
    JSON SHA-256
    `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`.
    The separate source-built REQ-001 review rerun passed 22/one/21/zero, log
    SHA-256
    `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`.
    Final whole-tree `./gradlew --no-daemon --rerun-tasks check goldenTest`
    reported `BUILD SUCCESSFUL` in 6m43s: standard tests 1045 discovered/817
    executed/228 skipped or tag-filtered/zero failures or errors, 140 XML files,
    82 tasks, log SHA-256
    `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`.
    The packaged task is separately reported and intentionally excluded from
    those totals. Targeted custom-source-set PMD/SpotBugs tasks passed, log
    SHA-256
    `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`.
    Static classification is intentionally non-zero: exactly 99 reviewed/non-
    blocking packaged glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN,
    1 UrF), all test/deployment validation-adapter findings, mostly Kotlin
    `lateinit`/generated representation and conservative single-thread harness
    concurrency warnings. No production source file was added or modified by
    this slice; the wider baseline remains non-blocking, and aggregate static
    cleanliness plus numerical/child-JVM coverage are not claimed. Dedicated
    run `31336067318` passed public REQ-001 and the complete 13-ID/31-case matrix
    on all four hosts; all operation-specific native rows are `PASSED` after the
    independent review cited above.
  - [x] Prove the remaining authority/refusal shapes through separately scoped
    packaged processes and the qualified native platform matrix. The complete
    matrix executes REQ-002 through REQ-013 against packaged production JARs on
    all four named hosts; independent review SHA-256
    `8569012fad31ee62da02b1cefe4792612ffdc80e68831be35c5ec86c8f8c8820`
    authorized this status-only closure. General Maven/release authority remains
    outside this bounded row.

## K1 — Kotlin toolchain and backend boundary

- [x] Add `refactorkit-kotlin` behind the existing language adapter registry; core
  does not depend on Kotlin compiler or IDE classes. The initial descriptor owns
  `.kt`/`.kts` routing but advertises only typed `REFUSED`/`NONE` capabilities
  until the semantic backend is qualified.
- [x] Select and document versioned provider `kotlin-compiler-explicit-v1` with
  backend `kotlin-compiler-embeddable-k2`. The first declarative qualification row
  is JDK 21 plus Kotlin 2.0.21; no semantic authority is implied. An external
  language server may bootstrap read operations but is never sufficient stable
  mutation authority by itself.
- [x] Discover configured JDK and Kotlin compiler/backend inputs explicitly, bind
  executable/JAR/version/classpath evidence with SHA-256 and a deterministic
  projection hash, double-read critical evidence, and refuse unsupported,
  workspace-local-by-default or drifting toolchains.
- [x] Execute no Java, Kotlin compiler, Gradle tasks/plugins, Maven lifecycle,
  compiler plugins, annotation processors, KSP, kapt or project scripts during
  default discovery.
- [x] Define the first bounded external compiler lifecycle for diagnostics with
  cancellation, timeout, memory/output/process limits, clean environment,
  immutable overlay, shutdown, process-tree termination and typed crash/error
  handling. Persistent-session crash recovery remains a later capability.
- [ ] Publish qualified Kotlin/JDK/Gradle/Maven support rows and license/SBOM
  boundaries before any managed capability is advertised as supported.

## K2 — Kotlin and shared JVM project model

- [x] Project Kotlin main/test/integration/custom/generated source sets through
  `kotlin-jvm-projection-v1` over the existing Build Model SPI with exact module
  ownership, target JDK/bytecode, classpath, outputs, module edges and hash-bound
  descriptor/toolchain evidence. Java-only dependency nodes remain graph-visible
  without claiming cross-language symbols.
- [x] Extend bounded declarative Maven and Gradle providers only where Kotlin JVM
  roots, plugin identity, `jvmTarget` and JDK/release metadata are literal or
  effective-model facts. Plugin-derived or executable-only model data remains
  `PARTIAL`, `EXECUTION_REFUSED`, or unavailable; Multiplatform/Android refuse.
- [ ] Model JVM binary names, Kotlin source declarations and callable/property
  identity without exposing compiler-internal handles in API `0.2`.
  - [x] Qualify the first regular-class and nested-class row with compiler `ClassId`,
    matching generated `.class` evidence, exact compiler PSI declaration ranges,
    and opaque `kotlin-jvm-type-v1` IDs.
  - [x] Extend the same proven type-ID row to top-level and nested interfaces,
    enum classes and annotation classes.
  - [x] Add named, data, nested and companion objects under additive `OBJECT`
    kind and the same binary-derived type-ID family. Anonymous objects remain
    outside the catalogue.
  - [x] Add the first non-overloaded top-level/direct-member function row with
    file-facade/owner class-file evidence and `kotlin-jvm-callable-v1` IDs derived
    from owner, JVM method name and descriptor. Properties, constructors,
    parameters and overloaded/renamed JVM callables remain open.
- [ ] Establish cross-language Java/Kotlin declaration and reference identity for
  supported JVM-visible types and members.
- [ ] Keep Kotlin/JVM, Kotlin Multiplatform, Android, generated code, compiler
  plugins, `expect`/`actual`, and script sources as separate capability/evidence
  cases. Unsupported boundaries must refuse rather than silently degrade.
- [ ] Validate the Build Model SPI against Kotlin so any remaining Java/Maven
  assumptions are removed from shared contracts rather than copied into the new
  adapter.

## Shared workspace intelligence foundation

- [x] Add the first provider-neutral, session-owned `WorkspaceIndex` bound to the
  exact project snapshot, with a whole-workspace source inventory, bounded
  provider declaration partitions, conservative incremental reconciliation and
  explicit evidence/completeness/truncation metadata.
- [x] Expose read-only `index.status` plus typed API `0.2`
  `intelligence.query` workspace/document symbol search and CLI human/optional
  JSON output. TypeScript/JavaScript completion, hover and signature help plus
  saved-snapshot Java definition/references/hover are qualified rows; unsupported provider rows
  remain explicit refusals.
- [x] Merge the existing persistent TypeScript/JavaScript language-server
  declaration projection into the central index under saved-snapshot,
  provenance, file/symbol and aggregate-deadline bounds. See
  `v0.7.0-typescript-workspace-index-acceptance.md`.
- [x] Add the shared immutable editor-overlay foundation and migrate TypeScript
  exact diagnostics to it without changing API `0.2` wire behavior. See
  `v0.7.0-editor-overlay-foundation-acceptance.md`.
- [x] Route TypeScript interactive queries through versioned editor overlays and
  add cancellation, file watching and durable provider-partition invalidation.
- [ ] Continue from the snapshot-keyed normalized JDT cache to finer persistent
  JDT compiler state and add persistent K2 integrations. See
  `v0.7.0-intelligence-index-plan.md`.

## Shared managed-write and runtime orchestration extraction

- [x] Replace the duplicated daemon/LSP/MCP access-order pending-plan maps with
  one language-neutral, session-owned `PendingPlanStore`. The bounded source-built
  acceptance proves exact opaque payload retention, true 128-entry access-order
  LRU, cross-session isolation, no filesystem/WAL/transaction side effects, and
  preservation of each surface's existing lifecycle and response policy under
  `REQ-PENDING-PLAN-STORE-001..002`. Independent final OpenJDK 21.0.11
  `check goldenTest` verification passed 1002 discovered cases with 782 executed,
  220 skipped/tag-filtered, zero failures/errors, and 77 tasks; log SHA-256
  `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`.
- [x] Replace duplicated CLI/daemon/LSP/MCP rollback-entry orchestration with one
  language-neutral `ManagedRollbackExecutor`. `APPLIED_ONLY` remains selected by
  CLI/LSP/MCP and `JOURNAL_RECORD` by daemon; the LSP dirty/open-document preflight,
  exact journal failures, surface response/lifecycle policy, and `PatchEngine`
  rollback authority remain unchanged. Final independent OpenJDK 21.0.11
  acceptance passed 8 scenarios and 67 expanded steps with zero failures/errors;
  log SHA-256
  `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`.
  Independent whole-working-tree `check goldenTest` verification then passed
  1010 discovered cases with 790 executed, 220 skipped/tag-filtered, zero
  failures/errors, and 77 tasks; log SHA-256
  `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`.
- [x] Replace the identical daemon/MCP managed-apply diagnostics-gate routing
  expressions with the stateless `ManagedApplyDiagnosticsGateSelector` in
  `refactorkit-jvm`. Exact branch precedence, gate IDs, case-sensitive original
  plan metadata, current adapter identity, eager external lookup/lazy provider
  execution, surface lifecycle, and `PatchEngine` under-lock authority remain
  unchanged. Final independent OpenJDK 21.0.11 acceptance passed 11 scenarios
  and 92 steps; log SHA-256
  `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`.
  Independent whole-working-tree `check goldenTest` verification then passed
  1021 discovered cases with 801 executed, 220 skipped/tag-filtered, zero
  failures/errors, and 78 tasks; log SHA-256
  `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`.
- [x] Replace the duplicate daemon/MCP mixed-workspace composition pipelines with
  the stateless, language-neutral core `WorkspaceSnapshotComposer`. Exact empty-
  contribution identity, normalized later-source precedence, deterministic file
  ordering, bounded field overlay, conditional TypeScript evidence, optional
  current Kotlin/JVM evidence, exact failure identity, and daemon/MCP-only
  adoption are covered by `REQ-WORKSPACE-SNAPSHOT-COMPOSER-001..005`. Final
  independent OpenJDK 21.0.11 source-built/in-process acceptance passed 8
  scenarios and 65 expanded steps; log SHA-256
  `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`.
  Daemon 58/58 and MCP 26/26 regressions passed; combined log SHA-256
  `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`.
  Independent whole-working-tree `check goldenTest` verification then passed
  1029 discovered cases with 809 executed, 220 skipped/tag-filtered, zero
  failures/errors, and 78 tasks; log SHA-256
  `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`.
  The production composer/adoption has no SpotBugs finding; 21 glue-only
  Kotlin/generated findings remain non-blocking (6 NP, 3 RCN, 12 BC).
  Refresh/index/semantic-session/managed-write/protocol policy remains outside
  the composer; CLI/LSP and packaged/native/concurrent qualification are open.
- [x] Replace the five duplicated ordinary Java preview-planner routes with the
  sealed `JavaRefactoringPreviewCommand` values and fieldless
  `JavaRefactoringPreviewDispatcher`. The internal per-call invoker preserves the
  existing default mappings; all five generic adapter decoders converge, and
  daemon/MCP adopt exactly those five only after surface parsing. The first
  independent review blocked source-only REQ-003; corrected split runners then
  passed all four definitions/eight cases/94 steps (focused log SHA-256
  `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`).
  Independent Java/daemon/MCP regressions passed with zero failures/errors (log
  SHA-256
  `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`),
  and the six-feature audit reconciled 27 definitions/59 cases/386 declared
  steps/838 expanded steps (SHA-256
  `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`).
  External protocol schemas/capability catalogues and `PatchEngine` authority are
  unchanged; `ManagedApplyInvoker` remains rejected. Focused qualification is
  source-built/in-process. Final dated mutable whole-working-tree source-built
  verification on 2026-08-01 used OpenJDK 21.0.11 and
  `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in
  6m 54s; 1045 tests were discovered, 817 executed, 228 skipped/tag-filtered,
  zero failures/errors, 140 XML files were produced, and 79 tasks executed; log
  SHA-256
  `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`.
  The targeted affected-module static command passed (log SHA-256
  `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Its
  classification is deliberately non-zero: the full targeted log contains no
  finding owned by `JavaRefactoringPreviewCommand`,
  `JavaRefactoringPreviewDispatcher`,
  `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP
  `toolPreviewRefactoring`, or the five changed generic decoder helpers. Exactly
  48 reviewed/non-blocking slice-local glue findings remain: Java-module command
  glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN,
  4 BC). Dispatcher-method references are generated Kotlin nullability analysis
  at glue call sites, not production-method findings. Wider configured
  SpotBugs/PMD baselines remain non-blocking; aggregate static cleanliness and
  numerical coverage are not claimed. This dated mutable source-built aggregate
  does not qualify packaged, native, cross-platform, concurrent, crash-restart,
  general-orchestration, or numerical-coverage behavior.
- [ ] Continue targeted extraction of shared workspace-refresh lifecycle, apply,
  and request-mapping services without changing protocol ownership or weakening
  surface-specific authority checks. Do not replace these seams with one stateful
  God service.

## K3 — Semantic read operations

Status: complete. Native packaged qualification passed in GitHub Actions run
`29439584115` on Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64.

- [x] Provide bounded project symbol search, document symbols, definition and
  references for qualified Kotlin/JVM projects.
  - [x] Provide saved-snapshot workspace/document search and ID-based definition
    for compiler-proven classes, named/companion objects, interfaces, enum classes,
    annotation classes and the first bounded function row.
  - [x] Add K2 FIR-resolved call-name definition and bounded partial references
    for the existing callable-ID row under
    `docs/requirements/kotlin-jvm-function-usage-navigation.md`; worker evidence
    plus daemon, CLI and MCP position routes. Packaged/native qualification passed
    in run `29432766317`. Imports, callable references, Java callers and mutation
    authority remain excluded.
  - [x] Add K2 FIR-resolved type/constructor/qualifier definition and bounded
    partial references for the existing type-ID row under
    `docs/requirements/kotlin-jvm-type-usage-navigation.md`; internal worker plus
    daemon, CLI and MCP routes qualified in run `29439584115`. Cross-language
    callers, alias/star import directives, strings and mutation authority remain
    excluded until separately qualified.
- [x] Provide compiler-backed read-only diagnostics for the first qualified
  single-module Kotlin/JVM boundary, with explicit line-only/no-location precision,
  toolchain/build/snapshot attestation and typed refusal/error results.
- [x] Define stable opaque Kotlin symbol IDs for the qualified type/function
  catalogue from durable semantic identity rather than source offsets or backend
  object identity. Future property/parameter families require separate ID rows.
  - [x] Define `kotlin-jvm-type-v1:<sha256>` for the proven declared-type row from
    the JVM binary name and `kotlin-jvm-callable-v1:<sha256>` for bounded functions
    from JVM owner/name/descriptor; no kind, path, offset, process or compiler
    handle enters either ID.
- [x] Preserve exact UTF-16/API coordinate conversion, source hashes, document
  versions and immutable workspace overlays where an external backend is used;
  shifted-source, astral-character, stale-authority and native packaged cases pass.
- [x] Aggregate Java and Kotlin diagnostics without duplicate cascades; missing
  build/toolchain inputs produce concise typed roots. Legacy API `0.2`
  `diagnostics languageId=jvm` is deterministic, language-tagged, read-only and
  covered by packaged Kotlin acceptance; correlated provider envelopes retain
  precision authority.
- [x] Expose the diagnostics backend, version/evidence, bounded runtime,
  toolchain/build completeness and refusal reasons through library, CLI, daemon,
  LSP capability metadata and MCP. Other semantic reads remain refused.

## K4 — Managed Kotlin rename foundation

Status: complete for the explicitly bounded private-declaration foundation.
Packaged/native qualification passed in GitHub Actions run `29451134010`; public
or incomplete shapes continue to refuse and no broad stable Kotlin rename is
claimed.

- [x] Qualify the first Kotlin-only private-declaration rename row with a complete
  K2 reference barrier, exact staged compiler diagnostics and managed transaction
  acceptance.
- [x] Qualify semantic rename for an initial Kotlin/JVM catalogue covering private
  classes, interfaces, objects, enum/annotation classes, direct functions,
  field-backed properties, function parameters and function type parameters where
  backend proof is exact.
- [x] Update constructors, callable/property references, imports, aliases and file
  names only when the semantic backend proposes and RefactorKit validates the
  complete operation. Proven direct forms are updated; aliases, callable
  references, delegated/constructor properties and filename/public API changes
  refuse instead of guessing.
- [x] Cover the Java-to-Kotlin boundary with a Java source naming a public Kotlin
  type and return stable `kotlin.renameCrossLanguageIncomplete`. Cross-language
  mutation remains refused until shared JVM identity is exact.
- [x] Detect/refuse exact quoted reflection/serialization/framework candidates,
  generated source ownership, scripts/plugins/unsupported build models and unknown
  external/public consumers.
- [x] Require preview, affected files, risk/evidence, before/staged diagnostics,
  explicit authorization, `PatchEngine`, WAL, rollback, stale authority and
  lock-time toolchain/build-evidence revalidation.

## K5 — Idiomatic Kotlin depth

The first prerequisite is the bidirectional public-type identity contract in
[`java-kotlin-shared-jvm-rename.md`](../requirements/java-kotlin-shared-jvm-rename.md).
Public member interoperability follows the independently gated
[`java-kotlin-shared-jvm-member-rename.md`](../requirements/java-kotlin-shared-jvm-member-rename.md).

These capabilities may span later `0.7.x` patch releases, remain part of the
Kotlin band and are mandatory before the band closes:

- [ ] Move declaration/file/package with import and Java interoperability updates.
  The first bounded public top-level type row is governed by
  [`kotlin-jvm-move-declaration.md`](../requirements/kotlin-jvm-move-declaration.md).
  - [x] Implement local and packaged CLI/daemon preview/apply/rollback for the
    explicit-import same-source-set row.
  - [x] Complete four-platform native qualification for the bounded
    explicit-import row (run `29482085130`).
  - [x] Complete standalone packaged MCP preview/apply/rollback acceptance on
    all four supported platforms (run `29488253198`).
  - [x] Add deterministic imports for compiler-proven same-package implicit
    Kotlin and Java consumers with local packaged acceptance.
  - [x] Qualify same-package consumer insertion on all four native platforms
    (run `29490554985`).
  - [x] Add exact fully-qualified Kotlin/Java target rewrites with local packaged
    acceptance and mixed-shape refusal.
  - [x] Qualify fully-qualified rewrites on all four native platforms (run
    `29493776932`).
  - [x] Remove the fabricated cross-language-consumer requirement with separate
    Kotlin-only and Java-only local packaged acceptance.
  - [x] Qualify independent consumer sets on all four native platforms (run
    `29501301246`).
  - [x] Add zero-consumer public type moves with staged K2 declaration identity
    and local packaged CLI acceptance.
  - [x] Qualify zero-consumer moves on all four native platforms (run
    `29503704995`).
  - [x] Add K2-proven Kotlin alias uses with exact alias-token preservation and
    local packaged daemon acceptance.
  - [x] Qualify alias-preserving moves on all four native platforms (run
    `29507916564`).
  - [x] Add compiler-proven Kotlin/Java package-star consumers by preserving the
    star and inserting one exact destination import, with local packaged
    MCP/daemon acceptance.
  - [x] Qualify star-import moves on all four native platforms (run
    `29511930846`).
  - [x] Add whole-file moves with compiler-proven private top-level helper
    declarations and local packaged CLI/daemon/MCP acceptance.
  - [x] Qualify private-helper type/function file moves on all four native
    platforms (run `29516695594`).
  - [x] Add explicit local and packaged private top-level property identity
    coverage.
  - [x] Qualify private-property helper moves on all four native platforms (run
    `29522349554`).
  - [x] Add whole-file moves for additional public top-level types whose complete
    Kotlin/Java consumers use exact explicit imports, with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify public-sibling file moves on all four native platforms (run
    `29527320435`).
  - [x] Add FIR-proven Kotlin alias consumers for additional public sibling types
    with local packaged daemon acceptance.
  - [x] Qualify aliased public-sibling moves on all four native platforms (run
    `29533418906`).
  - [x] Add exact Kotlin/Java package-star consumers for additional public sibling
    types with sorted explicit destination imports and local packaged MCP/daemon
    acceptance.
  - [x] Qualify package-star public-sibling moves on all four native platforms
    (run `29538823488`).
  - [x] Add compiler-proven same-package implicit consumers for additional public
    sibling types with sorted destination imports and local packaged MCP/daemon
    acceptance.
  - [x] Qualify same-package public-sibling moves on all four native platforms
    (run `29543325594`).
  - [x] Add exact fully-qualified Kotlin/Java consumers for additional public
    sibling types with local packaged MCP/daemon acceptance.
  - [x] Qualify fully-qualified public-sibling moves on all four native platforms
    (run `29558131857`).
  - [x] Add local packaged CLI/daemon/MCP acceptance for selecting a non-filename
    public sibling while preserving the original file path.
  - [x] Qualify non-filename sibling selection on all four native platforms (run
    `29560641892`).
  - [ ] Expand to the next bounded move shape.
  - [ ] Reject standalone companion-object movement with one stable typed refusal.
    A companion is nested in its enclosing type and may move only as part of that
    enclosing type/file. Companion extraction is a separate future operation and
    must not be represented as `moveDeclaration`.
- [ ] Organize imports and deterministic project-style-aware formatting.
  - [x] Add and qualify the bounded explicit-type-import library planner with
    complete K2 usage evidence, CRLF preservation, staged diagnostics and
    rollback on all four native test platforms (run `29566422517`).
  - [x] Route through CLI/daemon/MCP and qualify packaged Linux
    preview/apply/rollback.
  - [x] Qualify packaged CLI/daemon/MCP execution on all four native platforms
    (run `29571167892`).
  - [x] Add FIR-proven external/source type aliases with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify FIR-proven alias imports on all four native platforms (run
    `29575465366`).
  - [x] Add exact package-star preservation/sorting with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify exact package-star imports on all four native platforms (run
    `29601040232`).
  - [ ] Add callable imports and project-style-aware formatting.
- [ ] Change signature across overrides, overloads, default/named arguments and
  Java callers.
- [ ] Add bounded extract/inline operations with control/data-flow proof.
- [ ] Model extension receivers, suspend functions, companion/data/sealed/value
  classes, delegated properties and JVM annotations.
- [ ] Add explicit `expect`/`actual`, multiplatform, Android and framework-aware
  capability matrices before claiming support for those shapes.

## T5 — Deferred TypeScript/JavaScript advanced operations

- [x] Add additive API `0.2` `diagnostics.v2` with exact UTF-16 ranges,
  request/snapshot/semantic-lease correlation, saved/immutable-overlay authority,
  compiler attestation and runtime limits, structured readiness, bounded response,
  CLI/MCP access and unchanged legacy `diagnostics` compatibility.

The following work was explicitly deferred by the `0.6.x` plan. It proceeds in
parallel without weakening Kotlin gates and is mandatory before `0.7.x` closes.
Each checkbox is an independently qualified operation row; none may be closed by a
catch-all `moveSymbol`, `extractMethod`, `inlineMethod`, or source-presence claim:

- [ ] Source-file relocation using exact `getEditsForFileRename` authority,
  including compiler-proven import/export updates.
- [ ] Declaration move using the exact action returned by
  `getApplicableRefactors` and selected through `getEditsForRefactor`.
- [ ] Module-specifier rewrite and TypeScript project-reference migration as a
  distinct model-changing operation; npm package/module rename remains separate.
- [ ] Organize imports through explicit `All`, `SortAndCombine`, and
  `RemoveUnused` modes with exact formatting/preferences evidence.
- [ ] Extract function and extract constant through exact returned action
  identities and bounded control/data-flow evidence.
- [ ] Inline variable through its exact returned action identity.
- [ ] Change signature with separately proven declaration-family, overload,
  override, and call-site evidence; otherwise return a stable typed refusal.
- [ ] Inline function only as a separately proven row; never infer it from inline
  variable or extract evidence, and otherwise retain a stable typed refusal.
- [ ] Add TypeScript/JavaScript migration recipes without executing framework or
  package build code and without losing child operation/evidence/gate authority.

Pinned TypeScript 5.9.3/compiler-server requests must cross a typed adapter
anti-corruption layer. The current advanced planner files use invented LSP method
shapes, the current inline route delegates to extract, and the Java-only recipe
engine rejects TypeScript recipes; these are prototype/negative evidence, not
checkbox completion. Dynamic or mixed JavaScript never inherits typed TypeScript
certainty and may close an unsupported shape only through a truthful stable typed
refusal.

API `0.2` LSP module/identifier rename retains its existing client-managed
proposal contract and no-rollback metadata. Only an explicit managed apply of a
retained plan through `PatchEngine` may be qualified for WAL/recovery/rollback;
client application and managed application must not be conflated.

## J1 — Concurrent Java and shared-platform depth

- [ ] Continue Java change-signature, move/package/module, extract/inline and
  hierarchy catalogue expansion using JDT-backed evidence.
  - [x] Add exact JDT method-parameter declaration/use identities and a bounded
    signed-overload-aware rename-parameter planner with clean staged JDT proof.
  - [x] Qualify parameter rename through packaged CLI preview/apply/rollback on
    all four native platforms (run `29607477633`).
  - [x] Add local packaged daemon/MCP parameter-rename preview/apply/rollback.
  - [x] Replace lexical add/remove/reorder authority with exact JDT method,
    parameter and invocation evidence plus disposable staged-source validation.
  - [x] Qualify packaged CLI/daemon/MCP rename/add/remove/reorder on all four
    native platforms (run `29614333900`).
  - [x] Add bounded JDT parameter type change with local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify parameter type change on all four native platforms (run
    `29618448454`).
  - [x] Add bounded override/implementer-family add-parameter with explicit
    external-consumer-risk acceptance and local packaged CLI/daemon/MCP evidence.
  - [x] Qualify hierarchy add through packaged CLI/daemon/MCP on all four native
    platforms (run `29621466959`).
  - [x] Add hierarchy remove-parameter with zero-use proof and local packaged
    CLI/daemon/MCP acceptance.
  - [x] Qualify hierarchy remove through packaged CLI/daemon/MCP on all four
    native platforms (run `29624918322`).
  - [x] Add local JDT-bound hierarchy reorder using a selected-name-to-index
    permutation across declarations whose parameter names may differ.
  - [x] Add packaged CLI/daemon/MCP hierarchy reorder acceptance with exact rollback.
  - [x] Qualify hierarchy reorder on all four native platforms (run `29626304459`).
  - [x] Add local hierarchy type change with staged connectivity, call-binding,
    and body-use-count validation.
  - [x] Add packaged CLI/daemon/MCP hierarchy type-change acceptance with exact rollback.
  - [x] Qualify hierarchy type change on all four native platforms
    (run `29628955986`).
  - [x] Add local bounded private-constructor rename/add/remove/reorder/type-change
    with exact class-creation, `this(...)`, and `super(...)` invocation evidence.
  - [x] Qualify private constructors through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify the private-constructor row on all four native platforms
    (run `29634807550`).
  - [x] Add local executable rows for private generic methods, one-explicit-
    argument varargs, and annotation-preserving parameter reorder.
  - [x] Qualify bounded generic/varargs/annotation rows through packaged CLI/daemon/MCP.
  - [x] Qualify these bounded rows on all four native platforms
    (run `29640122922`).
  - [x] Add local public ordinary-constructor explicit-risk acceptance and
    fail-closed record-constructor coverage.
  - [x] Qualify public constructors through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify public constructors on all four native platforms
    (run `29642091983`).
  - [x] Add local exact-overload isolation, lambda-body caller update, functional-
    reference refusal, and multi-expanded-varargs refusal matrix.
  - [x] Qualify overload/lambda through packaged CLI/daemon/MCP with exact rollback.
  - [x] Qualify the overload/lambda/refusal matrix on all four native platforms
    (run `29643185758`).
  - [x] Add a JDT-only mixed-JVM hard barrier for public constructors and
    hierarchy signature mutation.
  - [x] Qualify the mixed-JVM refusal through the packaged CLI.
  - [x] Qualify the mixed-JVM refusal on all four native platforms
    (run `29646244081`).
  - [x] Add packaged byte-identical method-reference and record-constructor refusal.
  - [x] Qualify the expanded refusal matrix on all four native platforms
    (run `29647225014`).
  - [x] Add local opt-in static-method/constructor-reference migration to staged
    JDT-bound lambdas.
  - [x] Qualify bounded functional-reference migration through packaged CLI/daemon/MCP.
  - [x] Qualify bounded functional-reference migration on all four native platforms
    (run `29651568518`).
  - [x] Add local shared Java/Kotlin `addParameter` using ECJ→K2 external callable
    evidence, JDT Java calls, staged diagnostics, WAL, and exact rollback.
  - [x] Qualify shared Java/Kotlin add-parameter through packaged CLI/daemon/MCP.
  - [x] Qualify shared Java/Kotlin add-parameter on all four native platforms
    (run `29655068022`).
  - [x] Close the bounded Java change-signature catalogue with no pending row.
- [x] Establish authoritative release-aware Java/Maven diagnostics under
  [`java-maven-authoritative-diagnostics.md`](../requirements/java-maven-authoritative-diagnostics.md)
  before granting broader Maven ownership-migration authority.
  - [x] Add bounded explicit-JDK `ct.sym` release-signature attestation with
    double-read hashes, deterministic identity and typed refusal; this evidence
    does not yet grant JDT authority.
  - [x] Bind effective Maven `--release` to an attested, bounded `ct.sym` JEP 247
    view in the JDT source-set environment; source-only configurations refuse
    implicit platform selection.
  - [x] Add permanent library and packaged CLI/daemon/MCP Java 8/21 evidence for
    historical API rejection, complete current `java.se` visibility, transitive
    reactor sources, read-only execution, concise missing-platform roots and
    post-model module filtering on all four native platforms (run `29660037679`).
  - [x] Qualify exact releases 8 through 25 with permanent platform inputs and
    four-platform native evidence (run `29891915918`).
    - [x] Add the packaged CLI/daemon/MCP release matrix using one explicit
      hash-attested JDK 25 input, ECJ's exact JEP 247 historical views and a
      `jdk.zipfs`-capable reduced runtime.
  - [x] Add the first Maven scope-derivation row: hash-bound runtime-only
    external artifacts stay outside owner main compilation and flow through a
    reactor edge only to downstream test compilation (native run `29671357121`).
  - [x] Add the first source-set availability row: a missing test-only artifact
    yields one typed test root, suppresses only derivative test binding rows and
    preserves genuine main diagnostics (packaged native run `29682355909`).
  - [x] Propagate unavailable main compile environments through transitive
    reactor source-set closures and unavailable runtime exports only into
    downstream test environments (native run `29683959746`).
  - [x] Add bounded breadth-first nearest/declaration-order mediation, effective
    root dependency-management selection, optional/exclusion omission and
    non-transitive `provided` children for external Maven artifacts (native run
    `29687396137`).
  - [x] Add bounded local version-range selection and typed refusal for arbitrary
    dependency artifact types and Maven relocation metadata (native run
    `29688995827`).
  - [x] Bind managed-apply, recipe and Java mutation diagnostic deltas to exact
    provider/category/evidence/path/range/message identity (native runtime run
    `29692500361`; corrected Linux/golden run `29693799767`).
  - [x] Rehydrate committed and restored bytes through the same diagnostics gate;
    automatically roll back post-apply provider/identity failures (native run
    `29699796789`).
  - [x] Prove transitive Maven main/test impact diagnostics and pre-WAL refusal
    from an upstream-only source mutation (native run `29706289604`).
  - [x] Complete remaining Maven classifier/variant and scope rows and permanent
    packaged rows (run `29891915918`).
    - [x] Add exact type/classifier dependency-management identity, recursively
      derived provided/test child scopes, non-exported scoped reactor edges and
      combined packaged CLI/daemon/MCP evidence.
- [ ] Add transactional Java ownership migration across Maven reactor modules
  under [`java-maven-ownership-migration.md`](../requirements/java-maven-ownership-migration.md):
  move the proven source set/type/package, update only authoritative reactor POM
  module/dependency edges when required, reject dependency cycles or unresolved
  effective-model ownership, and validate the staged reactor without executing
  project plugins or using the network.
  - [x] Add the local immutable auxiliary-workspace-file snapshot/simulation,
    precondition, apply, post-apply rehydration and rollback prerequisite without
    broadening language source scope to XML; packaged/native qualification joins
    the first ownership mutation row.
  - [x] Implement the first local library acceptance row: complete non-generated
    root move between existing effective Maven modules, one exact literal
    consumer dependency rewrite, lexical byte preservation, isolated offline
    staged-reactor/JDT validation, cycle refusal, one WAL transaction and exact
    rollback. Basic missing-destination and remaining-source dependency refusals
    are covered. CLI/daemon/MCP preview routes and managed apply gates are
    qualified on all four native hosts in run `29903077059`; richer mediation
    remains open.
  - [ ] Keep module-directory moves, parent `<modules>` edits, `artifactId`/`groupId`
    changes and dependency-coordinate rewrites as explicit independently
    previewable arguments; never infer them from a Java package move.
  - [x] Preserve POM comments/unknown elements, refuse ambiguous property-managed
    coordinates, and apply Java plus POM edits through one `PatchEngine` plan with
    diagnostics, authorization, WAL, recovery and byte-exact rollback. A 128-case
    deterministic property row varies CRLF/LF, whitespace, namespace prefixes,
    comments, CDATA, processing instructions and quoted `>` attributes while
    proving that only the authorized artifact text changes.
  - [x] Qualify the existing-module ownership row first on Linux x86-64, Windows
    x86-64, macOS x86-64 and macOS arm64 using packaged CLI/daemon/MCP evidence
    (run `29903077059`).
  - [ ] Qualify module rename/move next using multi-module Maven fixtures and
    disposable real-project acceptance.
    - [x] Implement and independently validate the bounded language-neutral core
      authoritative snapshot-diagnostics evaluation required by the first
      model-changing descriptor row. The implemented transient value deep-detaches
      the full snapshot/module/build-model/diagnostic collection graph; core
      preserves the exact candidate shell, independently enforces module/build-root
      containment and no-follow external-fingerprint authenticity, and admits only
      modules/classpath evidence/build models for rehydration. Exact `S0`/`C1`/`S1`
      identity, post-provider under-lock workspace/lease revalidation, schema-v8
      `S1.hash`, committed `S1` plus staged diagnostics, and same-record automatic
      rollback to evaluated `S0` are validated while existing diagnostics-only
      gates/hashes remain exact. Independent OpenJDK 21.0.11 acceptance passed
      16/16 scenarios and 128/128 steps (log SHA-256
      `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`;
      JSON SHA-256
      `c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`).
    - [x] Implement and validate `REQ-JAVA-MAVEN-MODULE-RENAME-001` as the exact
      direct-library-only row. On a no-follow disposable copy of the permanent
      aggregator-plus-20-direct-child fixture, it renames `catalog-model` to
      `catalog-domain` with independently explicit `newArtifactId=catalog-domain`.
      Maintained Woodstox 7.1.1 StAX2 authority selects exactly three raw-POM
      element-text range edits (child project artifact, direct root `<module>`,
      and one effective-model-proven direct dependency artifact), followed by two
      file renames; all other bytes remain exact. DTD/external entity/external-DTD
      paths are denied, no POM is serialized, and no hand-written tokenizer is a
      fallback. Deterministic preview, complete offline staged reactor/diagnostics,
      explicit `PatchEngine` apply, one schema-v8 WAL record, committed `S1`, and
      exact normal rollback passed 1/1 scenario and 50/50 steps (log SHA-256
      `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`;
      JSON SHA-256
      `d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`).
      The final review returned `PASS_FOR_PROMOTION` (manifest SHA-256
      `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`;
      summary SHA-256
      `76a2d8d508fbec9786abfb30bc25a0953f41cf6c163a0508e25433a6530d3b02`).
      Final post-promotion whole-working-tree verification on OpenJDK 21.0.11
      used `./gradlew --no-daemon --rerun-tasks check goldenTest`:
      `BUILD SUCCESSFUL` in 7m 29s; 82 tasks executed; 144 standard-test XML files;
      1,062 tests discovered, 834 executed, 228 skipped/tag-filtered; zero
      failures/errors. Full log
      `/tmp/refactorkit-authoritative-module-rename-final-check.log` has SHA-256
      `c3a7b2a902b45267b60179da887c143d9e79f180f9e7ee4043e7631d4e66326d`;
      the standard-test XML checksum-manifest SHA-256 is
      `951f7bd281fc4900a096a2295937ee73784294f0ecb2a5c63f469d0c1dea907a`.
      SpotBugs remains configured non-blocking with `ignoreFailures=true`. The
      independent receipt's reviewed/non-blocking slice classification records
      zero EI/EI2 for the five corrected module-rename contract target classes;
      one non-real authoritative-evaluation EI on a defensive unmodifiable list;
      Kotlin compiler BC and non-correctness Bx artifacts; 47 authoritative glue
      findings (27 BC, 11 MS, 6 NP, 1 RCN, 2 UrF); and 113 module-rename glue
      findings (37 BC, 19 MS, 46 NP, 10 RCN, 1 REC). The glue reports are
      lifecycle-nullability or generated-bytecode/static-field findings, and no
      slice defect remained unclassified. PMD has no Kotlin source input for this
      slice. No aggregate numerical coverage is available. The existing dedicated
      packaged Maven task
      `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest` is
      intentionally excluded from the standard totals; this receipt does not
      qualify packaged, native, cross-platform, or general
      Maven support.
      This bounded requirement-evidence promotion does not close the parent
      checkbox or qualify nested/profile/property/inherited/ambiguous origins,
      module creation, other coordinate dimensions, JPMS, CLI/daemon/LSP/MCP,
      recipes, packaged/native/cross-platform behavior, disposable real projects,
      general Maven populations, or a support-matrix/release-wide row. All native
      CI rows remain unqualified.
    - [x] Implement and independently promote
      `REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001` for the exact source-built
      managed-apply selector route only. Public constants on
      `JavaRenameMavenModulePlanner` define raw operation
      `java.renameMavenModule` and gate
      `java-rename-maven-module-staged-reactor-v1` without granting authority.
      `ManagedApplyDiagnosticsGateSelector` routes only that byte-exact operation
      to language-neutral `DiagnosticsGate.lazyAuthoritative`; case, prefix, and
      leading/trailing-space variants use their real existing fallbacks. The lazy
      delegate is constructed once with synchronized first-use semantics, must
      expose the exact ID and an authoritative provider, and has no observer.
      One retained-plan apply evaluated S0/C1/S1 through the operation gate,
      created one schema-v8 record, and normal rollback restored S0/D0 through
      the same record. The final official JDK 21 focused rerun passed one
      scenario, all 25 scenario steps plus six Background steps (31 expanded),
      and both hooks, with no failures, skips, or undefined results; the log
      SHA-256 is
      `4e826737d94150bcebe0a38d3fee970e84339532f3fe4ef07189da2506bdedef`
      and the Cucumber JSON SHA-256 is
      `2cf337ab1f963b133222b941df0fd577b9518794e60d23dde81390ba6e760a4c`.
      Independent review returned `PASS_FOR_PROMOTION`; selector 11/92, direct
      module rename 1/50, and focused core/Java/daemon/MCP regressions passed.
      The official audit reports 37 definitions/82 cases, 33/77
      implemented-and-validated and 4/5 absent (SHA-256
      `542222f5b7656720d1706bc1d3995902396dfc4086252180bd85b407fee67f69`).
      The subsequent required `./gradlew --no-daemon --rerun-tasks check
      goldenTest` gate is authoritative for the promoted tree: `BUILD SUCCESSFUL`
      in 7m 31s with 82/82 tasks. Standard `test` XML only, excluding the
      intentionally separate stale packaged Maven task, reconciles 146 files,
      1,068 discovered, 835 executed, 233 skipped/tag-filtered, and zero
      failures/errors; the full log SHA-256 is
      `663ddcf3cb836fa2058d65160b57df19202b2f1908a0331faeb96ee6ca66209d`
      and the XML checksum-manifest SHA-256 is
      `bca4e1f718f56ce3095ef9168267b42506f467ec5b1046408e764b1948502905`.
      Static analysis remains configured non-blocking, and no aggregate numerical
      coverage is claimed. Earlier RED, implementation, focused, and scoped
      whole-tree receipts remain historical.
      This historical selector receipt did not itself close the parent checkbox.
      Subsequent independently reviewed SURFACE-002 through SURFACE-005 work now
      qualifies the bounded source-built CLI apply/refusal/rollback row, public
      daemon/MCP preview/apply/refusal/rollback row, client-managed LSP
      `documentChanges` row, and managed LSP apply/rollback row on the same exact
      fixture contract. The managed-surface feature currently has five validated
      definitions/six cases and 192 expanded steps. These later receipts still do
      not qualify recipes, packaged/native/cross-platform execution, a disposable
      real reactor, general Maven behavior, a release, or broad support; those
      missing boundaries keep this parent checkbox open.
- [x] Reuse shared JVM identity and Build Model improvements rather than adding
  Kotlin-specific branches to Java planners. Java ownership and source-root moves
  consume core `BuildModel`/`BuildSourceRootOwnership`; tests remove every legacy
  `Module` source-root projection and retain the same accepted plan.
- [x] Expand Spring, Jakarta/JPA, Jackson, reflection/configuration and generated
  code risk evidence where cross-language JVM operations expose those boundaries.
  Java ownership migration now aggregates the shared framework detector across
  every moved compilation unit, elevates exact quoted moved-type identities,
  refuses generated headers/annotations/roots and retains the existing Java/Kotlin
  dynamic-candidate refusal rows.
- [x] Retire compatibility projections only through an explicit API migration;
  API `0.2` behavior remains supported throughout this band. No compatibility
  projection is removed: Maven ownership is inventoried as an additive
  experimental operation on existing preview/apply envelopes and contract tests
  retain the beta method inventory.

## I1 — Integrations and native acceptance

- [x] Preserve strict read-only startup semantics: project open/scan/LSP
  initialization inspect WAL state without creating `.refactorkit` or
  `workspace.lock`; incomplete state refuses until explicit mutating
  `patch.recover`. Packaged semantic-read acceptance hashes hidden metadata.
- [x] Route Kotlin files, mixed snapshots, compiler diagnostics and the bounded
  declared-type symbol/definition row through library, CLI, daemon capability
  metadata and MCP while preserving editor-native Kotlin LSP ownership. Bounded
  function/type references are explicitly partial; K4 now adds bounded managed
  private-declaration mutation while broader K5 routes remain open.
- [ ] Add representative Maven, Gradle, mixed Java/Kotlin and refusal fixtures;
  no sample may require implicit network access or build-script execution.
- [ ] Add unit, property, golden, protocol, agent-simulation, hostile-workspace,
  stress, timeout and crash/restart coverage.
  - [x] Add the Maven ownership unit/property/protocol/packaged rows, including
    128 lexical byte-preservation variants, malformed/property/profile refusals,
    daemon/MCP apply/rollback and bounded Windows cleanup behavior. Wider stress
    and module-directory crash/restart rows remain open.
- [x] Qualify at least one non-trivial Kotlin or Java/Kotlin semantic mutation on
  Linux x86-64, Windows x86-64, macOS x86-64 and macOS arm64 using the packaged
  RefactorKit runtime and explicitly qualified external toolchain inputs.
- [x] Verify preview, exact staged diagnostics, authorization, apply, WAL,
  kill-during-write recovery, rollback and byte-identical restoration natively.
- [ ] Publish support matrix, limitations, migration notes, checksums, SPDX SBOMs,
  attestations and independent downloaded-asset verification.
  - [x] Add the active `0.7.x` support matrix and additive API `0.2` migration
    notes with explicit qualification provenance and unsupported rows; release checksums,
    SBOMs, attestations and downloaded-asset verification remain open.
  - [ ] Produce one versioned release evidence manifest joining repository/tag/full
    commit/clean-version identity to every platform archive and extracted-image
    hash, fixture/request/toolchain identity, operation-family report hash, SBOM
    hash, and attestation subject digest; independently download and execute the
    final published native assets rather than substituting intermediate Actions
    artifacts.

## Explicit non-goals for the first `0.7.0` slice

- Claiming all Kotlin Multiplatform, Android, compiler-plugin, kapt/KSP or script
  projects as supported.
- Treating LSP rename alone as managed Kotlin authority.
- Executing Gradle or Maven project code to obtain a model.
- Freezing API `1.0` or retiring compatibility models without migration evidence.
- Completing K5, T5 and J1 in one oversized commit. All rows are nevertheless
  required before the band closes and shall land as independently accepted slices.

## First implementation sequence

1. Activate `0.7.0-SNAPSHOT` metadata and this plan.
2. Add the isolated `refactorkit-kotlin` module and capability descriptor with all
   unsupported semantic operations explicitly refused.
3. Implement hash-bound Kotlin toolchain discovery and bounded project-model
   projection.
4. Deliver semantic reads and exact diagnostics on a committed Kotlin/JVM sample.
   Diagnostics plus the compiler-proven class/object/interface/enum/annotation
   search/definition row and first bounded function identity are complete;
   property/constructor identities, usage resolution and references continue in
   smaller slices.
5. Deliver one managed Kotlin rename end to end.
6. Add exact Java/Kotlin interoperability evidence and refusal boundaries.
7. Expand K5/T5/J1 operations in small independently accepted slices.
8. Run full native packaged acceptance before publishing any `v0.7.x` tag.

## Release gate

The first `v0.7.x` release requires a real Kotlin compiler/Analysis API-backed
mutation—not structural or lexical fallback—to pass semantic resolution, exact
normalized preview, staged diagnostics, explicit authorization, managed apply,
WAL, recovery and rollback on every supported native platform. At least one mixed
Java/Kotlin boundary must either succeed with exact evidence or refuse with a
stable typed reason. Existing Java and TypeScript capabilities must remain green. Closure additionally
requires one clean tagged revision whose source-built, operation-family, native
archive, extracted runtime, SBOM, attestation, and independently downloaded-asset
receipts reconcile to the release evidence identity defined by the completion
contract. Queued/running CI, a local package, or an intermediate Actions artifact
is not release qualification.
