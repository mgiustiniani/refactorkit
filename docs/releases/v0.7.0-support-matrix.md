# RefactorKit v0.7.x support matrix

Status: active `0.7.0-SNAPSHOT` qualification ledger. Rows marked **pending native**
are not release claims until their four-platform workflow succeeds.

## Runtime assets

| Host | Architecture | Embedded Java runtime | Native managed acceptance |
|---|---|---:|---|
| Linux | x86_64 | yes | yes |
| Windows | x86_64 | yes | yes |
| macOS | Intel x86_64 | yes | yes |
| macOS | Apple Silicon arm64 | yes | yes |

The package requires no globally installed Java. Historical Java platform analysis
uses the packaged/explicitly attested `ct.sym`; the reduced runtime includes
`jdk.zipfs`. macOS and Windows packages remain unsigned.

## Java language and platform authority

| Capability | Status | Boundary |
|---|---|---|
| Java parsing/source forms | qualified for source levels 8–25 | JDT grammar level follows the effective source model |
| Maven `--release` diagnostics | qualified for exact releases 8–25 on all four hosts | one explicit hash-attested JDK 25 input supplies JEP 247 historical views; run `29891915918` |
| Current Java SE visibility | qualified | complete `java.se` image, including `java.sql` and `java.net.http` |
| Source-only project platform | explicit input required | no running-VM substitution; missing authority is typed refusal |
| Named JPMS/module-path reactor | unsupported | typed refusal until a separate module-path row is qualified |

## Build model support

| Build system/model | Status | Boundary |
|---|---|---|
| Offline Maven effective reactor | authoritative | plugin execution and credentials disabled; network denied unless anonymous resolution is explicitly enabled |
| Maven main/test source sets | authoritative | generated materialized roots are analyzed but mutations refuse generated code |
| Maven dependency mediation | authoritative bounded row | nearest/declaration order, dependency management, optional/exclusions, ranges, exact type/classifier and compile/provided/runtime/test/system scope |
| Gradle | declarative partial | no settings/build script, task, plugin or Tooling API execution |
| Conventional Java layout | structural partial | no inferred external dependency authority |
| Mixed Java/Kotlin Maven | qualified bounded rows | shared JVM operations require both JDT and K2 evidence where documented |

## Java managed refactoring catalogue

| Operation | Contract status | Qualified boundary |
|---|---|---|
| `renameClass` / `renameMember` | beta-contract | exact JDT declaration/reference binding; framework/generated/dynamic boundaries remain conservative |
| `moveClass` / package migration | beta-contract; all 22 bounded Maven authority fixture cases qualified | exact JDT package/import/FQCN managed rewrite with preview, diagnostics, WAL and rollback; schema-v1 non-managed guidance for REQ-003 and four REQ-008 preview uncertainties; schema-v1 edit-free lexical review across bounded source-built/in-process surfaces under REQ-004; typed active-reactor-POM refusal and lock-held non-managed candidate-source drift refusal under REQ-008; packaged-process, native-platform and general parity rows remain pending |
| `moveSourceRoot` | beta-contract | complete root, unchanged source bytes/package/FQCN; no POM edits |
| `java.moveAcrossMavenModules` | experimental, qualified bounded row | complete non-generated root between existing Maven modules plus exact literal consumer dependency rewrite; one Java/POM transaction; all four hosts in run `29903077059` |
| `changeSignature.*` | experimental bounded catalogue | exact selected methods, hierarchy/constructor/functional-reference rows documented separately |
| `extractMethod` | experimental limited | conservative control/data-flow refusal |
| `organizeImports` / `formatFile` | beta-contract | generated or ambiguous inputs refuse |
| `safeDelete` | beta-contract | references refuse unless explicit force policy allows the row |

Every managed write requires preview, explicit authorization, exact staged
diagnostics, `PatchEngine`, WAL, recovery metadata and rollback. Native editor-
applied LSP edits remain client-managed and do not advertise RefactorKit rollback.

## Maven ownership migration limits

The first `java.moveAcrossMavenModules` row supports existing reactor modules and
literal jar/no-classifier module identities. It preserves POM bytes outside exact
coordinate text ranges and rejects property/profile/inherited ambiguity, duplicate
origins without explicit all-occurrence intent, cycles, missing destination edges,
remaining source dependencies, generated code and diagnostics regressions.

Module-directory moves, parent `<modules>` rewrites and group/artifact/version
migration are not yet qualified. They must remain explicit future arguments and
must not be inferred from Java package moves.

## Framework and dynamic-risk evidence

Spring, Jakarta/JPA and Jackson annotations are detected without loading framework
dependencies. Relevant rename/move/delete/ownership operations elevate risk and
report deterministic annotation locations. Exact quoted moved FQCN candidates
are reported for runtime/configuration review. RefactorKit does not claim to
rewrite reflection, XML configuration, persisted names, JPQL, serialized payloads
or generated metamodel/processor output.

## Integration compatibility

API version remains `0.2`. Existing CLI commands, daemon methods, MCP tools and
beta operation envelopes remain supported. New diagnostics/index fields are
additive. `java.moveAcrossMavenModules` is inventoried as an additive experimental
operation carried by the existing preview/apply methods and tools.

Daemon, managed LSP, and MCP now share one language-neutral 128-entry access-
order pending-plan store while retaining distinct lifecycle policies and unchanged
wire responses. `REQ-PENDING-PLAN-STORE-001..002` qualifies only source-built,
in-process retention mechanics and policy preservation; packaged and concurrent-
session qualification remain open.

CLI, daemon, managed LSP, and MCP also share the language-neutral rollback-entry
sequence while preserving surface visibility (`APPLIED_ONLY` except daemon
`JOURNAL_RECORD`), LSP preflight, response/lifecycle policy, and engine authority.
`REQ-MANAGED-ROLLBACK-EXECUTOR-001..002` passed eight source-built/in-process
cases and 67 expanded steps. It does not qualify packaged, native-platform,
concurrent, or crash/restart rollback behavior.

Daemon and MCP now share only their exact managed-apply diagnostics-gate routing
decision. Gate IDs, provider laziness, current adapters, external resolver
behavior, surface lifecycle, and `PatchEngine` authority remain unchanged.
`REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001..002` passed 11 source-built/in-process
cases and 92 steps. CLI, managed LSP, recipes, testkit, and direct library apply
remain explicitly excluded; packaged/native/cross-platform qualification is open.

Daemon and MCP also share the stateless, language-neutral mixed-workspace
composition pipeline in core. `REQ-WORKSPACE-SNAPSHOT-COMPOSER-001..005` passed
8 source-built/in-process cases and 65 expanded steps while preserving exact
empty-contribution identity, bounded source-inventory overlay, TypeScript-before-
optional-Kotlin evidence order, current collaborators, and unchanged failures.
Refresh/index/semantic-session/managed-write/protocol policy remains surface-
owned; CLI/LSP and packaged/native/cross-platform/concurrent qualification remain
open.

For exactly `renameClass`, `renameMember`, `moveSourceRoot`, single-file Java
`organizeImports`, and `safeDelete`, the generic Java adapter plus daemon/MCP now
share the sealed `JavaRefactoringPreviewCommand` and fieldless dispatcher while
retaining distinct decoders and surface policy. All four requirements/eight cases/
94 steps passed source-built/in-process acceptance, including real public daemon/
MCP session dispatch after the first source-only REQ-003 review was blocked
(focused log SHA-256
`10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`).
Java/daemon/MCP regressions passed with zero failures/errors (log SHA-256
`9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`).
Operation contract statuses, API `0.2`, protocol schemas, capability catalogues,
and managed-write authority are unchanged. `moveClass`, other languages, CLI/LSP,
multi-file imports, managed apply, and `ManagedApplyInvoker` remain outside.

The preceding bounded Maven move-class, pending-plan, managed-rollback,
managed-apply gate-selector, and workspace-composer receipts remain historical
and separately scoped. Final dated mutable whole-working-tree source-built
verification for the current Java preview-command working tree on 2026-08-01
used OpenJDK 21.0.11 and
`./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in
6m 54s; 1045 tests were discovered, 817 executed, 228 skipped/tag-filtered,
zero failures/errors, 140 XML files were produced, and 79 tasks executed; log
SHA-256
`fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`.
The targeted affected-module static command passed (log SHA-256
`8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Its
classification is deliberately non-zero: the full targeted log contains no
finding owned by `JavaRefactoringPreviewCommand`,
`JavaRefactoringPreviewDispatcher`,
`ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP
`toolPreviewRefactoring`, or the five changed generic decoder helpers. Exactly
48 reviewed/non-blocking slice-local glue findings remain: Java-module command
glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN,
4 BC). Dispatcher-method references are generated Kotlin nullability analysis at
glue call sites, not production-method findings. Wider configured SpotBugs/PMD
baselines remain non-blocking; aggregate static cleanliness and numerical
coverage are not claimed. This aggregate receipt complements the focused
source-built/in-process evidence and remains dated mutable source-built evidence,
not packaged, native, cross-platform, concurrent, crash-restart,
general-orchestration, or numerical-coverage qualification.

See also:

- [`v0.7.0-plan.md`](v0.7.0-plan.md)
- [`v0.7.0-migration.md`](v0.7.0-migration.md)
- [`../requirements/java-maven-authoritative-diagnostics.md`](../requirements/java-maven-authoritative-diagnostics.md)
- [`../requirements/java-maven-ownership-migration.md`](../requirements/java-maven-ownership-migration.md)
