# RefactorKit v0.7.x support matrix

Status: active `0.7.0-SNAPSHOT` qualification ledger. Rows marked **pending native**
are not release claims until their four-platform workflow succeeds. The normative
release-blocking scope is the
[`v0.7.x` completion contract](../requirements/v0.7.x-completion-contract.md).
Existing qualified rows remain bounded evidence; this matrix is only a projection
and does not close K5, T5, J1, I1, or the release as a whole.

## Roadmap/support claim reconciliation

This projection is fail-closed by
`scripts/verify-v070-roadmap-support-claims.py`; a roadmap row moving from open
to complete requires its support boundary to change in the same reviewed slice.

| Ledger area | Executable evidence retained | Explicit non-claim while rows remain open |
|---|---|---|
| Kotlin K1/K2/K5 | qualified bounded K1/K2 toolchain/build/capability/identity/state/orchestration foundations plus separately qualified operation rows | General Kotlin managed support and every unchecked K5 operation remain unqualified. |
| TypeScript/JavaScript T5 | bounded workspace intelligence and earlier separately qualified operations | All nine unchecked T5 advanced-operation rows remain unqualified. |
| Maven module J1 | one exact direct-library fixture plus bounded source-built managed surfaces | Packaged/native/cross-platform module rename or move and general Maven-module authority remain unqualified. |
| Recipes | separately named operation-specific source-built rows | Recipe evidence is operation-specific; no generic or advanced migration-recipe authority is claimed. |
| SPDX SBOM / I1 | workflow generation and attestation wiring only | SBOM workflow wiring is configured, but final SPDX assets, attestations, publication, and downloaded-asset verification remain unqualified. |
| Maven move-class native P0 | exact revision-bound four-host run `31336067318` passed the dedicated operation jobs and independent review SHA-256 `8569012fad31ee62da02b1cefe4792612ffdc80e68831be35c5ec86c8f8c8820` | Native evidence state is explicit and uniform; no row was qualified before independent review. |

Historical generic native acceptance does not qualify an operation-specific row.

Current admission status (2026-08-09): the local P0 all-module
build-scaffolding gate is resolved on the corrected working tree. The authorized
slice reproduced the OpenJDK 21 `testClasses` RED (SHA-256
`72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`), changed
only `modules/refactorkit-kotlin/build.gradle.kts` by deferring
`testRuntimeClasspath` resolution into `tasks.test.doFirst`, and passed the GREEN
rerun (SHA-256
`1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`). The
full OpenJDK 21 `check goldenTest` run then passed 82/82 tasks and 1,062 tests
with 228 skipped and zero failures/errors (log SHA-256
`48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). This
source-built result closes no support-matrix row. Actions run `30746237578`
remains a separate `failure` for historical revision
`2b5aa7234cc385214acb6958834763288aefc527` (three failed jobs; macOS arm64
succeeded). Corrected-revision CI and all unqualified K5, T5, J1, I1, native,
package, and release rows remain open; final promotion still requires one
immutable release evidence identity and independent verification of each final
published native asset.

## K1/K2 shared JVM foundation (qualified bounded rows)

The immutable candidate projection
[`v0.7.0-kotlin-support.json`](v0.7.0-kotlin-support.json) remains the exact
SHA-256-bound input qualified by four-host run `31355580092`. Independent
artifact audit SHA-256 is
`5f035ba7513779b8ed5315fd04fb67fcf197e01ab62615123ce51ac56867d018`;
post-native review SHA-256 is
`46997d03d2f9ea4ca97ecf737c2dc6aa99bdb7e78d7a55bd64b04450971a0d96`.
Every host executed the same 14 required suites (184 executed, zero skipped,
failures or errors), packaged smoke and subject-JAR attestation at exact source
revision `26e84d2e17b21c3172c2042a828c89a1f8b1540b`.

| Foundation | Qualified boundary | State |
|---|---|---|
| Kotlin toolchain | exact hash-attested Kotlin 2.0.21 compiler classpath and JDK 21.0.11; Maven plugin-free/offline bounded row; Gradle declarative partial row | qualified bounded inputs; no executable Gradle/Maven version-range claim |
| Kotlin declaration identity | descriptor-exact overloads, literal `@JvmName`, source constructors, direct/constructor properties, parameters and type parameters; no compiler handles | qualified bounded catalogue |
| Shared Java/Kotlin identity | exact type/method/constructor/field tuple joined from non-recovered JDT bindings and K2/class-file evidence | qualified read identity; no mutation authority |
| Build Model SPI | typed language/platform/compiler/version/plugin/evidence facets shared by Java, Kotlin, Gradle and TypeScript projections | qualified language-neutral contract |
| Persistent semantic state | bounded session-owned normalized JDT/K2 results with exact snapshot attestations, LRU limits and explicit clear | qualified session boundary; no cross-process compiler-object serialization |
| Refresh orchestration | stateless core scan/compare/prepare ordering adopted by daemon, LSP and MCP while all lifecycle/write policy remains surface-owned | qualified narrow orchestration boundary |
| Dependency license boundary | ten explicit Kotlin adapter/toolchain component rows, including caller-supplied versus packaged distribution | published boundary; not the final release inventory or SBOM |
| SPDX SBOM | `SPDX-JSON` workflow state | `configured-unpublished`; final assets/attestation/signing/publication remain I1 |

Android, Multiplatform, `.kts`, compiler-plugin/kapt/KSP semantics, generated-code
mutation and `expect`/`actual` remain typed refusals. These operation-specific
foundation rows are not a general Kotlin managed-support or green whole-repository
CI claim and do not close K5, T5, J1 or I1.

## Runtime assets

| Host | Architecture | Embedded Java runtime | Historical generic native managed acceptance |
|---|---|---:|---|
| Linux | x86_64 | yes | yes |
| Windows | x86_64 | yes | yes |
| macOS | Intel x86_64 | yes | yes |
| macOS | Apple Silicon arm64 | yes | yes |

The package requires no globally installed Java. Historical Java platform analysis
uses the packaged/explicitly attested `ct.sym`; the reduced runtime includes
`jdk.zipfs`. macOS and Windows packages remain unsigned.

## Java language and platform authority

| Capability | Status | Boundary |
|---|---|---|
| Java parsing/source forms | qualified for source levels 8–25 | JDT grammar level follows the effective source model |
| Maven `--release` diagnostics | qualified for exact releases 8–25 on all four hosts | one explicit hash-attested JDK 25 input supplies JEP 247 historical views; run `29891915918` |
| Current Java SE visibility | qualified | complete `java.se` image, including `java.sql` and `java.net.http` |
| Source-only project platform | explicit input required | no running-VM substitution; missing authority is typed refusal |
| Named JPMS/module-path reactor | unsupported | typed refusal until a separate module-path row is qualified |

## Build model support

| Build system/model | Status | Boundary |
|---|---|---|
| Offline Maven effective reactor | authoritative | plugin execution and credentials disabled; network denied unless anonymous resolution is explicitly enabled |
| Maven main/test source sets | authoritative | generated materialized roots are analyzed but mutations refuse generated code |
| Maven dependency mediation | authoritative bounded row | nearest/declaration order, dependency management, optional/exclusions, ranges, exact type/classifier and compile/provided/runtime/test/system scope |
| Gradle | declarative partial | no settings/build script, task, plugin or Tooling API execution |
| Conventional Java layout | structural partial | no inferred external dependency authority |
| Mixed Java/Kotlin Maven | qualified bounded rows | shared JVM operations require both JDT and K2 evidence where documented |

## Java managed refactoring catalogue

| Operation | Contract status | Qualified boundary |
|---|---|---|
| `renameClass` / `renameMember` | beta-contract | exact JDT declaration/reference binding; framework/generated/dynamic boundaries remain conservative |
| `moveClass` / package migration | beta-contract; all 13 definitions/31 bounded cases qualified locally; all 13 IDs/31 cases pass a packaged-production matrix | exact JDT package/import/FQCN managed rewrite with preview, diagnostics, WAL and rollback; schema-v1 non-managed guidance and lexical review; typed structural/freshness refusal; complete/edit-exact lease admission under lock before WAL; public-CLI REQ-001 and the complete embedded-runtime package-JAR matrix pass locally, while all four post-commit native-CI rows and general Maven authority remain pending |
| `moveSourceRoot` | beta-contract | complete root, unchanged source bytes/package/FQCN; no POM edits |
| `java.moveAcrossMavenModules` | experimental, qualified bounded row | complete non-generated root between existing Maven modules plus exact literal consumer dependency rewrite; one Java/POM transaction; all four hosts in run `29903077059` |
| `java.renameMavenModule` | experimental; bounded direct-library and source-built managed-surface rows; **not a broad support promotion** | REQ-JAVA-MAVEN-MODULE-RENAME-001 qualifies one exact 20-direct-child fixture plan/apply/rollback. SURFACE-001 through SURFACE-005 additionally qualify the byte-exact authoritative selector, bounded source-built CLI apply/refusal/rollback, public daemon/MCP preview/apply/refusal/rollback, client-managed LSP `documentChanges`, and managed LSP apply/rollback contracts. Recipes, packaged/native/cross-platform execution, disposable real-reactor evidence, general Maven/release behavior, and broad support remain open. |
| `changeSignature.*` | experimental bounded catalogue | exact selected methods, hierarchy/constructor/functional-reference rows documented separately |
| `extractMethod` | experimental limited | conservative control/data-flow refusal |
| `organizeImports` / `formatFile` | beta-contract | generated or ambiguous inputs refuse |
| `safeDelete` | beta-contract | references refuse unless explicit force policy allows the row |

### Packaged Maven `moveClass` qualification ledger

The public-CLI REQ-001 mode and a complete local embedded-runtime package-JAR
matrix for all 13 IDs/31 cases exist, but the ledger below tracks exact
post-commit native CI rows. REQ-013 is implemented-and-validated after
independent `PASS` and post-promotion rerun. Native CI wiring and `if: always()`
report uploads are implemented. Initial run `31334149989` failed during
setup-java resolution before subject execution because configured identity
`21.0.11+9` was unavailable; the corrected catalogue identity is Temurin
`21.0.11+10.0.LTS`. `PASSED` means that both the revision-bound dedicated job/
fail-closed receipt and independent promotion review succeeded; local execution,
failed precursor runs, or static review alone cannot promote it. Run `31334391495` proved the broad build could
fail before these tasks, so the operation now owns a dedicated four-host job and
fail-closed revision-bound receipt rather than inheriting unrelated build-job
success. Dedicated run `31335192160` supplied behavior RED: three hosts passed
both modes before the finalizer's stale 22-case expectation failed, while Windows
refused a harness read of the deliberately locked file. The finalizer now expects
31/one/30 and the harness compares non-engine state without reading lock bytes.
Corrected run `31336067318` checked out exact revision
`3782b1109aa8874ab887d6031d5bfa86bf5e9ab8`; all four dedicated jobs passed both
modes and emitted `PASSED` host receipts bound to Temurin `21.0.11+10.0.LTS`,
the REQ-013 baseline, feature SHA-256, report hashes, and 13-ID/31-case totals.
Independent aggregate receipt SHA-256 is
`8047b8c93e25175b2efd1af74e6be734317ac4cb734ebe7ac5f465a8c74dbfdc`.
The overall workflow remained failed only because the separate broad build/runtime
jobs retain pre-existing platform/installation-bounded catalogue/result failures;
that conclusion is explicit in the aggregate and is not presented as a green
repository-wide CI claim.

| Host | Architecture | Validation mode | State |
|---|---|---|---|
| Linux | x86_64 | public-CLI REQ-001 plus packaged-production 13-ID/31-case matrix | `PASSED` |
| Windows | x86_64 | public-CLI REQ-001 plus packaged-production 13-ID/31-case matrix | `PASSED` |
| macOS | Intel x86_64 | public-CLI REQ-001 plus packaged-production 13-ID/31-case matrix | `PASSED` |
| macOS | Apple Silicon arm64 | public-CLI REQ-001 plus packaged-production 13-ID/31-case matrix | `PASSED` |

Each row must run both isolated modes. The first executes the real self-contained
CLI as child processes over one no-follow fixture copy and retains the exact
REQ-001 preview/lock/apply/journal/diagnostics/rollback oracle. The second runs
each requirement tag group in a separate worker under package `runtime/bin/java`,
loads Core/Java/CLI/daemon/LSP/MCP production classes only from regular package
JARs, executes all 13 IDs/31 cases, and emits non-overwriting JSON/JUnit receipts
plus a fail-closed manifest. Ordinary `test`/`check` remains packaging-independent.

Official final OpenJDK 21.0.11/Linux `amd64` execution of
`:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks`
reported `BUILD SUCCESSFUL` in 22s with 35 tasks, JUnit 22 discovered/one
executed/21 tag-filtered/zero failures or errors, and one Cucumber scenario with
five background plus fourteen scenario steps (19/19) and 2/2 hooks. Packaged log
SHA-256 is
`1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`;
JSON SHA-256 is
`9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`.
The post-promotion complete local package matrix also passed all 31 cases, 436
scenario-step results, 62 hooks, and the production-classpath/runtime
attestation. Its fail-closed manifest SHA-256 is
`b3d5853b85c225a29cc548e66f2e51981cb8a55b80e886d38b871012cc663bac`;
REQ-013 JSON SHA-256 is
`316d255d711cfbfc779c9f81e74b1a23f421d64f3c6d758cd1903b6035139193`.

The final source-built whole-tree gate on the same JDK reported
`BUILD SUCCESSFUL` in 6m43s with standard tests 1045 discovered/817 executed/
228 skipped or tag-filtered/zero failures or errors, 140 XML files, and 82 tasks;
log SHA-256
`89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`.
The packaged task is separately reported and intentionally absent from those
standard-test totals. Targeted custom-source-set PMD/SpotBugs tasks passed; log
SHA-256
`2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`.
Static classification is intentionally non-zero: 99 reviewed/non-blocking
packaged glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), all
test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated
representation and conservative single-thread harness concurrency warnings. No
production source file was added or modified by this slice; the wider baseline
remains non-blocking, and aggregate static cleanliness plus numerical/child-JVM
coverage are not claimed.

The historical local observation alone did not alter the table. Runs
`31334149989` and `31334391495` reached no subject execution on any host. The latter's topology
summary/metadata SHA-256 are
`df354924dfef5c338ba995986b1ba3bbfc857f829cf4e1092f3b9d8d6f8dff7a` /
`12002180afde3beef0bb02a7fbf576c4c66bce089912e6ca55f879e12b48ff6d`;
all four dedicated jobs then passed in run `31336067318`; independent review
SHA-256 `8569012fad31ee62da02b1cefe4792612ffdc80e68831be35c5ec86c8f8c8820`
authorized the states shown in the table.

Neither the local nor dedicated matrix qualifies a general Maven population,
crash/concurrency shape, archive trust/checksum/SBOM/signing, numerical coverage,
or a green whole-repository CI claim. The dedicated host rows passed only their
bounded operation-specific contract. See
[`v0.7.0-java-maven-move-class-acceptance.md`](v0.7.0-java-maven-move-class-acceptance.md).

Every managed write requires preview, explicit authorization, exact staged
diagnostics, `PatchEngine`, WAL, recovery metadata and rollback. Native editor-
applied LSP edits remain client-managed and do not advertise RefactorKit rollback.

## Maven ownership migration limits

The first `java.moveAcrossMavenModules` row supports existing reactor modules and
literal jar/no-classifier module identities. It preserves POM bytes outside exact
coordinate text ranges and rejects property/profile/inherited ambiguity, duplicate
origins without explicit all-occurrence intent, cycles, missing destination edges,
remaining source dependencies, generated code and diagnostics regressions.

Beyond the separately bounded direct-child fixture and exact source-built
selector/apply/rollback route above, module-directory moves, parent `<modules>`
rewrites and group/artifact/version migration are not qualified. They must remain
explicit arguments and must not be inferred from Java package moves. Neither
bounded row establishes packaged/native/general-project or release support.

## Framework and dynamic-risk evidence

Spring, Jakarta/JPA and Jackson annotations are detected without loading framework
dependencies. Relevant rename/move/delete/ownership operations elevate risk and
report deterministic annotation locations. Exact quoted moved FQCN candidates
are reported for runtime/configuration review. RefactorKit does not claim to
rewrite reflection, XML configuration, persisted names, JPQL, serialized payloads
or generated metamodel/processor output.

## Integration compatibility

API version remains `0.2`. Existing CLI commands, daemon methods, MCP tools and
beta operation envelopes remain supported. New diagnostics/index fields are
additive. `java.moveAcrossMavenModules` is inventoried as an additive experimental
operation carried by the existing preview/apply methods and tools.

Daemon, managed LSP, and MCP now share one language-neutral 128-entry access-
order pending-plan store while retaining distinct lifecycle policies and unchanged
wire responses. `REQ-PENDING-PLAN-STORE-001..002` qualifies only source-built,
in-process retention mechanics and policy preservation; packaged and concurrent-
session qualification remain open.

CLI, daemon, managed LSP, and MCP also share the language-neutral rollback-entry
sequence while preserving surface visibility (`APPLIED_ONLY` except daemon
`JOURNAL_RECORD`), LSP preflight, response/lifecycle policy, and engine authority.
`REQ-MANAGED-ROLLBACK-EXECUTOR-001..002` passed eight source-built/in-process
cases and 67 expanded steps. It does not qualify packaged, native-platform,
concurrent, or crash/restart rollback behavior.

Daemon and MCP share only their managed-apply diagnostics-gate selection
component. `REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001..002` had 11 promoted
source-built/in-process cases and 92 steps. The requirement-bound K5 move-next
candidate makes exact Kotlin `moveDeclaration` operation diagnostics independent
of Java-affected files while preserving the non-move K2 fallback; its updated
local oracle passes 12 cases and 100 expanded steps. This routing delta remains
candidate-only until the dedicated K5 native/review gate passes.
REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001
adds one exact operation-owned branch: only raw `java.renameMavenModule` returns
lazy authoritative gate `java-rename-maven-module-staged-reactor-v1`; synchronized
first use constructs one exact authoritative delegate, no observer participates,
and the final official JDK 21 focused retained-plan apply/normal rollback passed
one scenario, all 25 scenario steps plus six Background steps (31 expanded), and
two hooks, with no failures, skips, or undefined results (log/Cucumber-JSON
SHA-256
`4e826737d94150bcebe0a38d3fee970e84339532f3fe4ef07189da2506bdedef` /
`2cf337ab1f963b133222b941df0fd577b9518794e60d23dde81390ba6e760a4c`).
The subsequent required whole-tree gate is authoritative for the promoted tree:
`BUILD SUCCESSFUL` in 7m 31s with 82/82 tasks; standard `test` XML only,
excluding the intentionally separate stale packaged Maven task, reconciles 146
files, 1,068 discovered, 835 executed, 233 skipped/tag-filtered, and zero
failures/errors (log/XML-manifest SHA-256
`663ddcf3cb836fa2058d65160b57df19202b2f1908a0331faeb96ee6ca66209d` /
`bca4e1f718f56ce3095ef9168267b42506f467ec5b1046408e764b1948502905`).
Static analysis remains configured non-blocking, and no aggregate numerical
coverage is claimed; earlier receipts remain historical. This is
selector/apply/rollback evidence, not public daemon/MCP route adoption.
CLI/LSP/MCP previews, recipes, public daemon/MCP behavior beyond selection,
packaged/native/cross-platform/general-Maven/release behavior, and broad support
remain open; REQ-002 through `005` remain absent.

Daemon and MCP also share the stateless, language-neutral mixed-workspace
composition pipeline in core. `REQ-WORKSPACE-SNAPSHOT-COMPOSER-001..005` passed
8 source-built/in-process cases and 65 expanded steps while preserving exact
empty-contribution identity, bounded source-inventory overlay, TypeScript-before-
optional-Kotlin evidence order, current collaborators, and unchanged failures.
Refresh/index/semantic-session/managed-write/protocol policy remains surface-
owned; CLI/LSP and packaged/native/cross-platform/concurrent qualification remain
open.

For exactly `renameClass`, `renameMember`, `moveSourceRoot`, single-file Java
`organizeImports`, and `safeDelete`, the generic Java adapter plus daemon/MCP now
share the sealed `JavaRefactoringPreviewCommand` and fieldless dispatcher while
retaining distinct decoders and surface policy. All four requirements/eight cases/
94 steps passed source-built/in-process acceptance, including real public daemon/
MCP session dispatch after the first source-only REQ-003 review was blocked
(focused log SHA-256
`10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`).
Java/daemon/MCP regressions passed with zero failures/errors (log SHA-256
`9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`).
Operation contract statuses, API `0.2`, protocol schemas, capability catalogues,
and managed-write authority are unchanged. `moveClass`, other languages, CLI/LSP,
multi-file imports, managed apply, and `ManagedApplyInvoker` remain outside.

The preceding bounded Maven move-class, pending-plan, managed-rollback,
managed-apply gate-selector, and workspace-composer receipts remain historical
and separately scoped. Final dated mutable whole-working-tree source-built
verification for the current Java preview-command working tree on 2026-08-01
used OpenJDK 21.0.11 and
`./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in
6m 54s; 1045 tests were discovered, 817 executed, 228 skipped/tag-filtered,
zero failures/errors, 140 XML files were produced, and 79 tasks executed; log
SHA-256
`fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`.
The targeted affected-module static command passed (log SHA-256
`8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Its
classification is deliberately non-zero: the full targeted log contains no
finding owned by `JavaRefactoringPreviewCommand`,
`JavaRefactoringPreviewDispatcher`,
`ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP
`toolPreviewRefactoring`, or the five changed generic decoder helpers. Exactly
48 reviewed/non-blocking slice-local glue findings remain: Java-module command
glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN,
4 BC). Dispatcher-method references are generated Kotlin nullability analysis at
glue call sites, not production-method findings. Wider configured SpotBugs/PMD
baselines remain non-blocking; aggregate static cleanliness and numerical
coverage are not claimed. This aggregate receipt complements the focused
source-built/in-process evidence and remains dated mutable source-built evidence,
not packaged, native, cross-platform, concurrent, crash-restart,
general-orchestration, or numerical-coverage qualification.

See also:

- [`v0.7.0-plan.md`](v0.7.0-plan.md)
- [`v0.7.0-migration.md`](v0.7.0-migration.md)
- [`../requirements/java-maven-authoritative-diagnostics.md`](../requirements/java-maven-authoritative-diagnostics.md)
- [`../requirements/java-maven-ownership-migration.md`](../requirements/java-maven-ownership-migration.md)
