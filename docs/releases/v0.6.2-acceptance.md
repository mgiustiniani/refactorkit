# RefactorKit v0.6.2 Acceptance Record

Status: release-candidate acceptance pending immutable tag publication.

Date: 2026-07-14.

## Purpose

`v0.6.2` is the publication patch for the completed `v0.6.0` feature scope. The
immutable `v0.6.0` and `v0.6.1` tags were not published because their Windows tag
jobs failed before packaging; no partial release was created and neither tag was
moved. Their pre-tag branch CI runs had reported all four native targets green,
but cached test tasks had not exposed the clean-build defect.

The failed `v0.6.1` tag exposed host-dependent validation that passed glob syntax
to `java.nio.file.Path`: Windows correctly rejected `src/**/*.ts`. `v0.6.2`
validates include/exclude and path-alias patterns lexically, preserving workspace
escape refusal without asking the host path parser to interpret wildcards. The
release workflow retains `clean build --no-daemon`, followed by a separate
package/native-smoke command. API `0.2`, qualified toolchain and safety gates are
unchanged.

## Pre-tag evidence

| Gate | Evidence | Result |
|---|---|---|
| Version | source and packaged runtime report implementation `0.6.2`; API `0.2` | PASS locally; native CI pending |
| Feature acceptance | [`v0.6.0-acceptance.md`](v0.6.0-acceptance.md) | PASS |
| Build | JDK 21 `./gradlew clean build --no-daemon` | PASS locally; clean native CI pending |
| Golden | `goldenTest` | PASS, 23/23 |
| JVM suites | Kotlin/JVM unit and integration tests | PASS, 548 tests |
| Toolchain | Node 22.18.0, TLS 5.1.3, TypeScript 5.9.3 | PASS |
| Managed rename | exact diagnostics, path alias/re-export apply, WAL and rollback | PASS |
| Crash/recovery | semantic kill/restart and daemon kill at durable `APPLYING` | PASS |
| Runtime archive | native structure/architecture/modules/permissions/checksum/launcher | PASS locally; native CI pending |
| Diff hygiene | `git diff --check` | PASS |

The final patch commit must pass branch CI on all four targets before tagging.

## Publication evidence

To be completed after immutable tag `v0.6.2` succeeds:

- [ ] release commit and tag;
- [ ] branch release-candidate CI;
- [ ] four-platform tag workflow and publish job;
- [ ] checksums and SPDX SBOMs;
- [ ] build/SBOM attestations;
- [ ] independent downloaded Linux archive verification and SHA-256;
- [ ] unsigned macOS/Windows disclosure.

Published tags are immutable. A later defect receives a new patch version.
