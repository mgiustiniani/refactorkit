# v0.7.0 Kotlin/JVM move-declaration acceptance

Status: public-sibling consumer rows and non-filename sibling selection complete
across local, packaged CLI/daemon/MCP and four native platforms.

## Qualified first row

`moveDeclaration` accepts one compiler-proven public top-level Kotlin/JVM type
that is the only top-level declaration in its file. The destination remains in
one authoritative non-generated source-root path. A supported Kotlin or Java
consumer either has one explicit non-aliased, non-star import, belongs to the
exact old package and resolves the type implicitly, or uses the exact old fully
qualified identity at every compiler-proven target token.

K2 proves the declaration and Kotlin usages. Ephemeral hash-attested K2 classes
let JDT prove Java uses of the same binary identity. Preview changes only the
package token, explicit import tokens, deterministic new imports for proven
same-package consumers, exact fully-qualified target identities and one
source-root-relative file path.

The staged snapshot must compile with K2, publish the destination binary identity
and resolve Java consumers through JDT. Preview never writes source, class, JAR
or transaction metadata into the workspace.

## Transaction evidence

Local library acceptance previews, applies and rolls back this mixed move:

```text
fixture.api.PublicGreeting
  -> fixture.api.v2.PublicGreeting

Kotlin import: fixture.api.PublicGreeting -> fixture.api.v2.PublicGreeting
Java import:   fixture.api.PublicGreeting -> fixture.api.v2.PublicGreeting
```

The declaration file, Kotlin import and Java import apply in one `PatchEngine`
transaction. Rollback restores the original file path and bytes.

Packaged acceptance exercises a read-only CLI preview plus daemon and standalone
MCP preview/apply/rollback for `PortableGreeting`. Semantic lease and snapshot
authority are required by both transports; daemon additionally binds the
workspace-index generation.

## Refusal evidence

Executable tests cover absent external-consumer approval, explicit imports,
same-package Kotlin/Java import insertion, exact fully-qualified Kotlin/Java rewrites, zero-consumer public types, independent Kotlin-only and Java-only consumer sets,
K2-proven alias uses with exact alias preservation, deterministic explicit
imports after preserved Kotlin/Java package-star imports, whole-file movement of
private top-level helper types/functions/properties with separately executable
property identity coverage, selection of any public sibling while preserving the
original filename, additional public sibling types with exact explicit/FIR-aliased Kotlin/Java
imports, exact package-star imports, or same-package implicit uses expanded into
sorted destination imports, or exact fully-qualified identities joined to every
compiler-proven final type token, and refusal of mixed imported and qualified
uses. The implementation also refuses ambiguous ownership, generated roots,
destination conflicts, multiple top-level declarations, default packages,
dynamic/framework evidence and incomplete K2/JDT evidence.

## Local evidence

```text
677 JVM tests
0 failures
23 golden cases
Packaged Kotlin move CLI preview: passed, read-only
Packaged Kotlin move daemon preview/apply/rollback: passed
Packaged zero-consumer CLI preview, Java-only star-import MCP apply/rollback and
mixed Kotlin/Java fully-qualified daemon apply/rollback led by a non-filename
public sibling while preserving the original filename: passed
Base native run 29482085130: Linux x86-64, Windows x86-64, macOS x86-64,
  and macOS arm64 passed
Packaged MCP native run 29488253198: all four supported platforms passed
Same-package consumer native run 29490554985: all four supported platforms passed
Fully-qualified use native run 29493776932: all four supported platforms passed
Single-language consumer run 29496007448: Linux and both macOS runners passed;
Windows lost hosted-runner communication during the preceding TypeScript smoke,
so that run does not qualify the Windows Kotlin row
Retry 29499821972: Linux and both macOS runners passed; the Windows build failed
with broad transient K2/ECJ process failures before packaged qualification, so it
did not qualify the Windows row
Successful retry 29501301246: all four supported platforms passed, including the
packaged Kotlin-only CLI and Java-only MCP rows
Zero-consumer native run 29503704995: all four supported platforms passed
Alias-preserving native run 29507916564: all four supported platforms passed
Package-star native run 29511930846: all four supported platforms passed
Private-helper run 29515071850: Linux and both macOS runners passed; Windows had
a transient packaged K2 references worker failure before move qualification, so
that run does not qualify the Windows row
Successful private-helper retry 29516695594: all four supported platforms passed
Private-property native run 29522349554: all four supported platforms passed
Public-sibling native run 29527320435: all four supported platforms passed
Aliased-sibling run 29531840408: Linux and both macOS runners passed; Windows
failed in the preceding TypeScript CLI diagnostics smoke before Kotlin packaged
qualification, so that run does not qualify the Windows row
Successful aliased-sibling retry 29533418906: all four supported platforms passed
Package-star sibling run 29538823488: all four supported platforms passed
Same-package sibling run 29543325594: all four supported platforms passed
Fully-qualified sibling run 29558131857: all four supported platforms passed
Non-filename sibling run 29560641892: all four supported platforms passed
```

This does not qualify files containing additional public functions/properties or unsupported top-level
declarations, ambiguous same-package, partially-qualified, mixed qualified/import or malformed
star/alias consumers
of public sibling types, destination filename changes, general Kotlin
package/module movement,
partially-qualified or mixed qualified/imported rewrites, malformed/multiple
aliases, multiple/static/malformed star imports,
multiplatform, Android, compiler plugins or build-file edits.
