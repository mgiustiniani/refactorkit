# 0.7.0 tagged build recovery — r009

Status: **authorized tag update executed; second tagged run failed; corrected
runtime and remaining local gates passed; another exact-tag authorization pending**.

[ARC42](../arc42/appendix-requirements.adoc) owns integrated status. This evidence
satellite does not expand the bounded language, host or transaction contracts.

## Authorized action and actual GitHub execution

The user explicitly authorized moving the unreleased public `v0.7.0` tag from
`14220182bccaafac8361bfcb4bc44cfdf9fd4602` to
`89426f70aec6c1fd40c4105d9ef00b6d8fadd316`. The remote update used an exact
force-with-lease; the local update used old-value compare-and-swap. No branch
was pushed. The original failed commit/run34442715747 and r008 seal remain.

[Run34468219354](https://github.com/mgiustiniani/refactorkit/actions/runs/34468219354)
builds that exact89426f70 source on Linuxx86-64 only:

| Gate | Actual result |
|---|---|
|Clean `build goldenTest`|PASS;1617 reported =1242 passed+375 skipped/filtered; zero failures/errors|
|Isolated CLI installation-boundary fixture|PASS; original guards/oracles remain active|
|Package and native smoke|PASS|
|Packaged Java authoritative diagnostics|PASS|
|Real TypeScript and Kotlin packaged smokes|PASS|
|Packaged kill-during-write recovery|PASS|
|Actual J1 packaged CLI/daemon/MCP qualification and finalizer|3 cases PASS,138 steps; `nativeQualification=true`, clean exact GitHub source|
|K1/K2 finalizer|FAIL: required `refactorkit-lsp-0.7.0.jar` missing|
|Later K5 and T5/I1 qualification|NOT REACHED|
|SPDX, attestations, draft/release assets, publication|NOT REACHED / none created|

The complete248-report collection includes the existing annotated-navigation case
from its separate task. That one additional collected passing case explains
1616→1617 relative to r007's report collection: a module/classname/case/status
multiset comparison removes no earlier identity or status. An initial comparison
by nonunique suite display names was unsuitable and is retained separately, not
used as qualification or a product failure.

J1's actual native runtime ZIP is75828566 bytes with SHA-256
`4988485e472aec3c4326e38219bd44de45f2edbacc6ab9122b4eced2757c422b`.
Its identity comes from the native manifest; the failed workflow did not upload
that ZIP as a release asset. It is not an independently downloaded accepted asset.

## Exact packaging regression and restoration

The previously qualified K1/K2 revision26e84d2e included the internal
`runtimeOnly(project(":modules:refactorkit-lsp"))` edge. Commitb7bf1b2a removed it
while retaining `testImplementation`. Thus source LSP tests could pass without the
required packaged subject JAR. The existing K1/K2 gate correctly exposed this
assembly regression; it was not another installation fixture problem.

Commit **`b85fde2407299cc0cfaac863c541e54087f61d0c`** changes exactly:

- `modules/refactorkit-cli/build.gradle.kts`: restore that single internal runtime edge;
- `scripts/test-finalize-native-k1-k2-shared-foundations.py`: add a fast structural
  guard alongside the unchanged authoritative package/JAR checks.

No new external dependency coordinate/version, compiler copy, production algorithm,
runner, glue, Gherkin, LSP launcher or LSP managed-write capability is introduced.
The old unrelated packaged-matrix removals are not restored by this bounded fix.
The historical required-JAR set and refusal are not weakened.

Actual offline assembly yields75266798 bytes, SHA-256
**`0c481706c04fa967c4e60857b9b9f37efa2575accc86581b6d672b13a231bdd0`**.
Compared with the local versioned c53ecc07 archive, all214 existing file members
are byte-identical, none removed; only `refactorkit-lsp-0.7.0.jar` is added. Its SHA
`06a2670b9986178529d5d4c1bf50c9955dd86dad1a4e46a7cb220cb629f85ba3` matches the
existing LSP module's built JAR. This does not assert equality to the Temurin CI ZIP.

The build's complete input hashes join unchanged to clean commitb85fde24. Original
native refusal, local missing-member evidence and structural RED remain retained.
Independent review returns `PASS_FOR_LSP_RUNTIME_RESTORATION`.

## Actual remaining local gates on the corrected runtime

All following executions bind b85fde24 and0c481706, with source/archive/extracted
member-image audits before and after every bounded command:

| Existing machinery | Result |
|---|---|
|Runtime archive verifier and real MCP initialize/tools/EOF|PASS|
|Packaged Kotlin compatibility smoke|PASS|
|K1/K2 source suites and actual finalizer|200 tests /14 suites, zero failures/errors/skips; seven exact subject JARs; PASS|
|K5 completion source, smoke and actual finalizer|24 tests /five suites; PASS|
|K5 move-next source, smoke and actual finalizer|25 tests /three suites; PASS|
|Packaged T5|26 cases /95 steps; PASS|
|Packaged bounded I1 crash/conflict|8 cases /31 steps; PASS|
|T5/I1 transport closure|34 transports; all children exited|
|J1 CLI/daemon/MCP and actual local finalizer|3 cases /138 steps; PASS, correctly `nativeQualification=false`|

These are new local executions, not reruns or promotion of the failed GitHub job.
Independent supplied-evidence review returns **PASS_FOR_CORRECTED_TAGGED_BUILD**
for exact b85fde24. The reviewer did not execute tests or recompute filesystem hashes.

## Remaining boundary

The public tag still points to89426f70. A further update to reviewed **b85fde24**
requires explicit confirmation; the earlier authorization has already been used.
Later documentation commits do not change this proposed code/packaging subject.

The exact new tagged CI build, final asset/SPDX/checksum/provenance/attestation joins,
independent post-native review and downloaded-candidate/release verification remain
open. No release or draft exists. No global installation is replaced. Other hosts
remain **WAIVED_BY_USER / not verified**, not PASS. Static findings remain configured
nonblocking and numerical coverage unmeasured. Earlier live-race/filesystem/power-loss
and client-managed LSP limitations remain unchanged.

Exact evidence: [r009](../../.refactorkit/runs/v070-tagged-release-r009/summary.md).
