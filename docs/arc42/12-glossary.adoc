= 12. Glossary
:toc:
:sectnums:

Status: mixed, with confirmed target-scoped Maven move-class authority terms integrated from reviewed domain research and bounded implementation evidence. Closed catalogue version selection, exact legacy-v1 preservation, default v2, `preview-only`, atomic protocol truthfulness, and RESULT-001 projection vocabulary are implementation-informed for one atomic Kotlin CLI revision. CATALOG-001, CATALOG-002, and RESULT-001 are `@implemented-and-validated` within separately bounded source-built evidence; RESULT-002 through `006` remain absent. Confirmed `v0.7.x` completion vocabulary remains planned/open unless separately qualified. Repository Gherkin contains 40 repository-unique definitions and 85 expanded cases across eleven features (697 declared scenario steps, two Backgrounds/10 Background steps, and 1,291 expanded case steps), all `@implemented-and-validated` within their owning evidence boundaries. A current official `io.cucumber:gherkin:28.0.0` parser check reports zero parse/tag/status/duplicate-ID/reconciliation errors; the prior all-implemented audit SHA-256 `a8a104d1977fd481049d29c7b844869266410d8ffa66a0f9cfc9fd825a2eac62` remains historical. Every promoted definition retains separately bounded execution evidence and does not imply release-wide or general support. Workspace Snapshot Composition has a stateless language-neutral core implementation and final focused OpenJDK 21.0.11 source-built/in-process evidence for 8/8 scenarios and 65/65 steps (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`). Daemon/MCP adoption is source-inspected only and their 58/58 plus 26/26 regressions passed (combined log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`); CLI/LSP and packaged/native/cross-platform/concurrent/numerical-coverage/general-orchestration qualification remain excluded. The preceding four-feature audit remains historical evidence for its then-current 18 implemented definitions/43 cases/679 expanded steps (log SHA-256 `fb7c81a1d3223e6959d6838ac38c5c737f1bf8804570489405ed59bc1a279bc7`). REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 passed final independent OpenJDK 21.0.11 source-built/in-process acceptance with 11/11 scenarios and 92/92 steps and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`); daemon/MCP regressions passed 58/58 and 26/26. Final whole-working-tree source-built verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. Explicit non-null final-segment handling eliminated the selector's initial production NP finding while preserving root-path `NullPointerException` and exact `.java`/`.JAVA` behavior; the final full log contains no selector/provider-function production finding. Twenty-three glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. This selector vocabulary is qualified only for source-built/in-process behavior, daemon/MCP regression, dated source-built aggregate verification, and bounded SURFACE-005 LSP adoption; CLI, recipes, testkit/direct library, and LSP behavior beyond SURFACE-005 remain excluded and `PatchEngine` authority is unchanged. It does not denote packaged, native, cross-platform, concurrent, general apply-authority, or numerical-coverage support. The appendix preserves the earlier planned/`@absent` audit as historical pre-promotion evidence. For managed rollback, a focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`), and detached unmodifiable ordered snapshotting corrected it. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`); CLI/daemon/LSP/MCP adoption is source-inspected only. The historical managed-rollback-scoped whole-working-tree verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). Earlier REQ-008- and PendingPlanStore-specific receipts remain historical evidence for their explicitly stated scopes. REQ-JAVA-MAVEN-MOVE-AUTH-008 adds four immutable preview-guidance blocker vocabularies, result-owned active-reactor-POM structural refusal, generic required-file authority evidence, immutable structured diagnostic details, and an under-lock evidence-drift vocabulary that distinguishes expected semantic inventory from expected/observed file evidence. The exact Magrathea request at `a48e3c06` remains CLI/Linux/JDK-21-specific; no packaged/runtime/native/general support is inferred. Pending-plan evidence likewise does not qualify packaged processes, concurrency, or general service extraction. Managed-rollback evidence is dated mutable source-built evidence and does not qualify packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority.

For Workspace Snapshot Composition, final official whole-working-tree verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 7s; 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors; 136 XML files; 78 tasks executed; log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`. The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption. Static findings are not zero: 21 slice-local glue-only Kotlin/generated SpotBugs findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. This vocabulary remains qualified only by dated mutable source-built/in-process and aggregate evidence, not by packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage evidence. The preceding selector whole-tree receipt remains historical for its scope.

Java Refactoring Preview Command vocabulary below is implementation-informed for the current mutable working tree based on clean base `59b2030b0d44998d0109ff523861d6253df61d62`. All four definitions/eight cases/94 steps passed bounded source-built/in-process acceptance (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`), and Java/daemon/MCP module regressions passed with zero failures/errors (log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`). Final dated mutable whole-working-tree source-built verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 6m 54s; 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors; 140 XML files; 79 tasks executed; log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`. The targeted affected-module static command passed (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Its deliberately non-zero classification has no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers; exactly 48 reviewed/non-blocking slice-local glue findings remain—Java-module command glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN, 4 BC). Dispatcher-method references are generated Kotlin nullability analysis at glue call sites, not production-method findings. Wider configured SpotBugs/PMD baselines remain non-blocking, and neither aggregate static cleanliness nor numerical coverage is claimed. The vocabulary does not denote a new protocol capability or packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification; the final aggregate is dated mutable source-built evidence complementing, not replacing, the focused source-built/in-process receipts.

Packaged-process qualification vocabulary is implementation-informed for one official final local OpenJDK 21.0.11/Linux `amd64` observation of existing REQ-001 on the mutable, uncommitted tree. `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks` reported `BUILD SUCCESSFUL` in 22s with 35 tasks, JUnit 22 discovered/one executed/21 tag-filtered/zero failures or errors, and one Cucumber scenario with five background plus fourteen scenario steps (19/19) and 2/2 hooks; packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`, JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The final source-built whole-tree gate separately passed in 6m43s with standard tests 1045 discovered/817 executed/228 skipped or tag-filtered/zero failures or errors, 140 XML files, and 82 tasks (log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`); those totals intentionally exclude the packaged task. Targeted custom-source-set PMD/SpotBugs tasks passed (log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`) with an intentionally non-zero 99-finding packaged glue/harness classification: 57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, and 1 UrF. These are reviewed/non-blocking test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated representation and conservative single-thread harness concurrency warnings; no production source was added or modified by this slice. The wider baseline remains non-blocking, and neither aggregate static cleanliness nor numerical/child-JVM coverage is claimed. The same feature remains twelve definitions/22 cases with unchanged statuses. Native CI wiring and `if: always()` report uploads are implemented but unexecuted; every post-commit native CI ledger row remains `CONFIGURED_UNOBSERVED`.

REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001 through `005` add separately bounded implementation-informed vocabulary for exact selector/apply/normal rollback, CLI, daemon/MCP, client-managed LSP proposal, and managed LSP apply/rollback behavior. All five definitions/six cases are `@implemented-and-validated`. For SURFACE-002, the feature-only correction restored the exact existing child-process/socket observation step locally after two RED logs proved `authorityObservationRequested=false`; independent requirements review returned `PASS`, and the status-only promotion preserved that scenario-local step. Current feature SHA-256 is `1c29ad27146f06072c9815a1e056e2702cd25b8975ea2f0307385091e9859ad3`. Final post-promotion focused log SHA-256 `aee52f1114259413d6d6c77d384c6b30ed5afd82ab6b8c3d92921eb4bea367a2` and Cucumber JSON SHA-256 `4db1de3fca326ea9067a0d6653570c5fcf090a266e142cb9d27c854afd191b97` record one case with 29 scenario and five Background steps passed; final whole-tree log SHA-256 `34b45bc447938de5bc5ce2c6351bdb7feda4e4403642e6add9d3e31cd7a044b0` records `BUILD SUCCESSFUL` in 12m 47s with 87/87 tasks and zero standard-test failures/errors. Earlier RED and pre-promotion GREEN receipts remain historical; static findings remain non-blocking, and no clean-static or numerical-coverage term is introduced. SURFACE-005 remains bounded by baseline `REQ005-BDD-EVIDENCE-001` SHA-256 `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; independent review returned `PASS` with no findings for AC-005-01 through AC-005-11, and post-promotion source-built/in-process public-`LspSession.dispatch` acceptance passed one scenario/47 results (JSON SHA-256 `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54`). The durable RED log remains bounded to the observed pre-success runtime failure; separate source/diff/domain inspection, not that log, identified the former generic `java-jdt` route. External editor/transport, recipes, packaged/native/cross-platform execution, concurrency/crash behavior, general Maven projects, release/broad support, and numerical coverage remain outside this vocabulary.

REQ-JAVA-MAVEN-MODULE-RENAME-001 contributes the three implementation-informed terms below. Its feature adds one functional definition/case and 50 steps to source totals with effective `@implemented-and-validated` status. The term scope remains direct-library/fixture bounded and adds no broad support claim.

[cols="1,3,2,2",options="header"]
|===
|Term |Definition |Scope |Evidence

|RefactorKit
|Deterministic refactoring and code-intelligence engine for safe, previewable, rollbackable code transformations.
|System
|README, AGENTS

|PatchPlan
|Structured preview/apply or refusal plan for a code modification, including operation, snapshot hash, affected files, workspace edit, diagnostics, warnings, risk, confidence, and evidence. In the bounded Java `moveClass` dispatcher, `Plan(JavaMoveClassPreview)` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence; `Guidance(JavaMoveClassReviewOnlyGuidance)` carries REQ-003 plus four bounded REQ-008 preview uncertainties; and `LexicalReview(JavaMoveClassLexicalFallbackReviewEnvelope)` carries REQ-004 alone. A legacy lexical fallback may remain an internal review-only compatibility `PatchPlan`, but its ID/edit is not admitted as a protocol plan.
|Core
|`Models.kt`; ADR 0002; `JavaMoveClassOperationDispatcher`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/004/008

|PlanId
|Opaque identifier generated for a `PatchPlan`. At a protocol boundary it is a revocable session-scoped lookup handle only when the exact surface-admitted payload is retained in that session's pending managed-plan store. It is not approval, authorization, a transaction ID, or possession-based write authority: explicit apply invocation and all `PatchEngine` gates remain mandatory. An internal lexical review plan's random UUID-based ID is not a protocol handle and must never be exposed or aliased to an `operationId`; unknown `PlanId` input remains `INVALID_PARAMS`.
|Core / integrations
|`Models.kt`; `RefactoringApplyIdentity`; ADRs 0002 and 0004; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004; reviewed `SRC-2026-07-31-002`

|Pending Plan
|Session-owned, in-memory registration of one exact `PatchPlan` plus any surface-owned apply-critical metadata under the plan identity. Membership makes the plan pending for that surface; it is not another `PatchStatus`, regenerated preview, durable transaction, WAL record, or rollback record. Missing, evicted, discarded, and cleared identities use the surface's existing indistinguishable not-found behavior.
|Daemon / LSP / MCP orchestration
|Implemented-and-validated REQ-PENDING-PLAN-STORE-001/002; `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`; exact daemon/LSP/MCP session production paths

|Bounded Pending-plan Store
|Implemented language-neutral core component generic over the exact retained payload. Every integration session owns a separate instance. It provides identity-keyed insertion, access-order lookup, side-effect-free remove/clear, and silent least-recently-accessed eviction above exactly 128 entries. Admission, snapshot/lifecycle policy, protocol projection, approval, apply/rollback, filesystem access, WAL, transaction, and diagnostics remain outside it. Current evidence is source-built/in-process and does not qualify concurrency or a general service-extraction pattern.
|Core primitive with daemon / LSP / MCP surface policy
|`modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`; `DaemonSession.kt`; `LspSession.kt`; `McpSession.kt`; implemented-and-validated REQ-PENDING-PLAN-STORE-001/002

|WorkspaceEdit
|Collection of file edits to modify, create, delete, or rename files in a workspace.
|Core
|`Models.kt`

|TextEdit
|A replacement over a source range.
|Core
|`Models.kt`, `TextEdits.kt`

|PatchEngine
|Core service that locks, preflights, journals, stages, durably commits per-path images, recovers interrupted managed writes, and performs conflict-safe or explicitly forced rollback. Multi-path batches are recoverable rather than one filesystem-wide ACID commit.
|Core
|`PatchEngine.kt`, ADR 0002, transactionality audit

|Transaction
|Domain identity and compensation view embedded in the durable journal record. Journal pre/post images and lifecycle—not the `Transaction` object alone—provide recovery and conflict-safe rollback evidence.
|Core
|`Models.kt`, `docs/transactionality-audit.md`

|Compensatable Batch
|Legacy classification for previewed/preflighted sequential writes with post-success compensation only. Current managed paths have advanced to locked, write-ahead-journaled, durably staged recoverable batches.
|Core / integrations
|ADRs 0002 and 0004, transactionality audit

|Transaction Log
|Versioned write-ahead recovery metadata stored under `.refactorkit/transactions`, durably created before mutation and atomically advanced through apply/rollback/recovery states. Schema-v2 SHA-256 integrity, v1-on-update migration, and blocking owner-only corruption quarantine are implemented; full metadata and complete fault evidence remain planned.
|Core / workspace
|`TransactionLog`, transactionality audit

|Supplied Transaction ID
|Raw string received by a rollback surface. In the implemented managed rollback boundary it is passed unchanged to `TransactionId.parseOrNull`; it is not trimmed, case-folded, canonicalized, or itself treated as authority.
|Core rollback orchestration
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `features/managed-rollback-executor-extraction.feature`; `ManagedRollbackExecutor.kt`; final focused log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`

|Rollback Lookup Visibility
|Caller-selected initial journal visibility with values `APPLIED_ONLY` and `JOURNAL_RECORD`. CLI/LSP/MCP select `APPLIED_ONLY`, under which every non-`APPLIED` record is observationally absent before guard/engine work. Daemon selects `JOURNAL_RECORD`, under which any decodable ID-matching record reaches authoritative engine handling. Visibility is applied to one full `loadRecord` result; it is not a second lookup or rollback mode.
|Core/surface rollback boundary
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; ADR 0004; inspected CLI/daemon/LSP/MCP adoption

|Resolved Journal Record
|The exact complete `TransactionJournalRecord` returned by the executor's single outer pre-lock lookup and retained unchanged in every post-resolution outcome. It is lookup evidence, not current or post-rollback state: `PatchEngine` deliberately reloads under lock and may advance the journal after resolution.
|Core rollback orchestration / daemon projection
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; preserved `TransactionLog`/`PatchEngine` planning inspection at `27bc8a34`; focused in-process evidence

|Rollback Preflight Guard
|Generic surface-supplied read-only allow/reject decision invoked with the resolved journal record after visibility and before `PatchEngine` acquires its lock. LSP preserves dirty-document-first and then affected-open-document rejection; CLI, daemon, and MCP allow. It carries opaque surface rejection data and is neither authorization nor authoritative under-lock validation.
|Core/surface rollback boundary
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; ADR 0004; inspected surface adoption

|Transaction-journal Failure
|Exact `TransactionLogException` identity, code, message, and cause produced by journal access. Implemented outcomes distinguish failure during the single outer lookup, where no record was resolved, from failure escaping the one rollback call, where the resolved pre-lock record is available. It is neither visible-record absence nor an ordinary engine refusal.
|Core rollback orchestration / journal safety
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-002; `TransactionLog`; `ManagedRollbackExecutor.kt`; focused in-process evidence

|Managed Rollback Executor
|Implemented language-neutral core application service that parses one supplied ID, performs one outer full-record lookup, applies caller-selected visibility, runs an optional surface-owned preflight guard, invokes public `PatchEngine.rollback` exactly once, and returns typed outcomes retaining the exact resolved pre-lock record. It owns no rendering, mode policy, refresh/diagnostics/lifecycle, authorization, WAL/recovery replacement, direct write, or rollback algorithm.
|Core Patch and Model Engine C3 component
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; inspected CLI/daemon/LSP/MCP adoption; Chapters 4-10; ADRs 0002/0004; LLM Wiki concept `managed-rollback-executor`

|Managed Rollback Outcome
|Implemented typed result algebra comprising invalid ID, visible-record absence, lookup journal failure, preflight rejection, rollback-call journal failure, exact engine-applied success, and engine refusal. Every variant after successful resolution carries the same full resolved pre-lock record. Journal failures preserve exact exception identity; refusal diagnostics preserve construction-time order and values through a detached unmodifiable snapshot, so later source-list mutation cannot change the outcome.
|Core/surface rollback handoff
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; focused in-process evidence; inspected surface projection

|Saga
|Multi-step workflow that commits steps separately and attempts later compensation. RefactorKit recipes no longer use this boundary; they stage one recipe-wide transaction.
|Recipes
|ADR 0004, transactionality audit `TX-008`

|ProjectSnapshot
|Immutable snapshot of workspace modules, source files, classpath/build evidence, and auxiliary evidence files with a content hash. In bounded move guidance, strict expected-source and generated-root manifests are captured as auxiliary files so their exact text affects snapshot identity.
|Core
|`Models.kt`; `JavaProjectScanner`; REQ-JAVA-MAVEN-MOVE-AUTH-003

|Workspace Snapshot Composition
|Implemented stateless application/infrastructure policy that invokes one authoritative base scanner, overlays a bounded later source-inventory contribution, performs ordered conditional evidence attachments, and returns one canonical immutable `ProjectSnapshot`. It has no identity or retained lifecycle and is not an aggregate, entity, repository, persistence mechanism, refresh service, index, transaction, or write authority. REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` define the implemented-and-validated bounded contract. Production evidence is the language-neutral core `WorkspaceSnapshotComposer`; adoption is source-inspected only in private daemon/MCP `scanWorkspace`.
|Core Patch and Model Engine policy; daemon/MCP concrete wiring
|`WorkspaceSnapshotComposer.kt`; `features/workspace-snapshot-composer-extraction.feature`; `DaemonSession.scanWorkspace`; `McpSession.scanWorkspace`; focused acceptance log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; final dated whole-working-tree source-built aggregate log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`, with no production finding attributable to the composer/adoption and 21 non-blocking glue-only findings (6 NP, 3 RCN, 12 BC); Chapters 4-10

|Snapshot Refresh
|Surface-owned lifecycle action that decides when to invoke composition and then stores, reconciles, indexes, invalidates, clears, or reports resulting state. Refresh is observably distinct from the read-only composition policy and remains daemon/MCP policy after the implemented extraction.
|Daemon / MCP application lifecycle
|`DaemonSession`; `McpSession`; transactionality audit `TX-018`; REQ-WORKSPACE-SNAPSHOT-COMPOSER-005; LLM Wiki concept `workspace-snapshot-composition`

|Authoritative Base Snapshot
|The complete `ProjectSnapshot` returned by the invocation's primary scanner. It supplies workspace, modules, classpath evidence, existing build models, auxiliary evidence, source inventory, extensions, and ignore policy. A secondary inventory may overlay only three source fields; ordered attachers alone may subsequently replace their own provider model.
|Workspace Snapshot Composition
|`WorkspaceSnapshotComposer.kt`; source-inspected daemon/MCP wiring; implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-001/002

|Source-inventory Contribution
|Secondary scanner result from which composition accepts only source `files`, `sourceExtensions`, and `ignoredDirectories`. Its workspace, modules, classpath evidence, build models, and auxiliary evidence have no authority. The current bounded contribution uses exactly `ts`/`tsx` as `typescript` and `js`/`jsx` as `javascript`.
|Workspace Snapshot Composition / structural adapter wiring
|`GenericProjectScanner`; `WorkspaceSnapshotComposer.kt`; source-inspected daemon/MCP wiring; implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-002

|Empty-contribution Identity
|Implemented invariant that a secondary contribution with no files passes the exact base snapshot to the optional Kotlin stage without sorting, extension/ignore union, or TypeScript attachment. With no Kotlin attachment, the same base instance and hash are returned.
|Workspace Snapshot Composition
|`WorkspaceSnapshotComposer.kt`; focused source-built/in-process acceptance; REQ-WORKSPACE-SNAPSHOT-COMPOSER-001

|Later-source Precedence
|For a non-empty contribution, base files followed by later script files are keyed by `path.normalize()`; the later script `SourceFile` wins a collision, and surviving values are ordered by `path.toString()`. This policy does not authorize a full snapshot merge.
|Workspace Snapshot Composition
|`WorkspaceSnapshotComposer.kt`; focused source-built/in-process acceptance; REQ-WORKSPACE-SNAPSHOT-COMPOSER-002

|Current Collaborator
|Scanner or attacher supplied for one composition invocation rather than retained at composer construction or startup. Per-invocation supply is required so daemon/MCP use current scanner configuration and optional Kotlin toolchain state.
|Workspace Snapshot Composition / integration wiring
|`WorkspaceSnapshotComposer.kt`; source-inspected current daemon/MCP session wiring; REQ-WORKSPACE-SNAPSHOT-COMPOSER-003/005

|RefactoringRequest
|Existing immutable but stringly generic request accepted by `LanguageAdapter.applyRefactoring`. It is a core adapter-port DTO, not the typed Java command and not a daemon, MCP, CLI, or LSP protocol request. For the five implemented routes, Java decoding preserves its current refusal/default behavior and converges on the canonical typed downstream planner path rather than a parallel catalogue.
|Core / language-adapter port
|`Models.kt`; five converged decoder routes in `JavaLanguageAdapter.kt`; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-002

|LanguageAdapter
|Contract implemented by language-specific adapters for parse, symbols, references, diagnostics, and refactoring planning. Its current method name `applyRefactoring(RefactoringRequest)` returns a `PatchPlan`; it does not apply a plan to the workspace and must not be conflated with `PatchEngine.apply`.
|Core / adapters
|`Models.kt`; ADRs 0002 and 0004

|Java Refactoring Preview Command
|Implemented immutable, complete, Java-owned sealed value hierarchy whose exactly five variants select one existing planner against one supplied `ProjectSnapshot` and the invocation's current `JavaLanguageAdapter`, then return the exact `PatchPlan`. The variants are `RenameClass`, `RenameMember`, `MoveSourceRoot`, single-file `OrganizeImports`, and `SafeDelete`. A command has no stable identity, retained lifecycle, apply authority, pending-plan meaning, or persistence.
|Java Language Adapter / Java Refactoring Planning
|`JavaRefactoringPreviewCommand.kt`; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001/004; focused source-built/in-process evidence

|Java Refactoring Preview Dispatcher
|Implemented fieldless Java-owned class that receives the exact supplied snapshot, current Java adapter, and one complete preview command. Its public call creates an `ExistingJavaRefactoringPreviewPlannerInvoker`; an internal overload accepts the per-call `JavaRefactoringPreviewPlannerInvoker` seam, maps one sealed variant to one unchanged default planner, returns its exact plan, and propagates an escaping exception unchanged. It owns no alias/default conversion, validation, state, diagnostics, filesystem mutation, persistence, apply, lifecycle, rendering, or protocol behavior.
|Internal Java Language Adapter C3/code-scale responsibility
|`JavaRefactoringPreviewCommand.kt`; REQ-JAVA-PREVIEW-COMMAND-001/004; delegated LATE ADR/C4 reviews found no policy or topology change

|Surface ACL
|Surface-owned anti-corruption translation from CLI flags/positions, daemon JSON-RPC fields, MCP tool arguments, or LSP commands/positions into a complete internal command and from internal results/failures back to that surface. Aliases, defaults, missing/unknown wording, coordinate units, stability, edit ownership, rendering, and admission remain surface policy. Daemon and MCP adopt the Java command boundary for exactly five routes; CLI/LSP adoption remains excluded.
|Integration boundary
|ADR 0004; `DaemonSession.kt`; `McpSession.kt`; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-003 through real public session dispatch

|Canonical Java Preview Planner Path
|Implemented single downstream route from a complete Java preview command through one fieldless dispatcher and internal per-call invoker seam to one existing planner. Daemon, MCP, and `JavaLanguageAdapter.applyRefactoring(RefactoringRequest)` retain distinct upstream decoders, but the five admitted operations no longer invoke planners through divergent switches. Canonicality changes no planner output, exception, protocol, admission, diagnostics, or apply authority.
|Java refactoring planning
|Chapters 4-6 and 8; QS-CORR-011; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001 through `004`

|Java Language Adapter
|Java-specific implementation providing source scanning, symbols, references, diagnostics, and refactoring planners. It owns the implemented Java preview command hierarchy/dispatcher; core does not import those Java types. Its existing generic adapter method remains an upstream compatibility decoder and converges on the same canonical planner path for the five admitted routes.
|Java module
|`JavaLanguageAdapter.kt`; `JavaRefactoringPreviewCommand.kt`; focused and module-regression evidence

|Symbol
|Named code element such as class, interface, enum, record, annotation type, method, field, constructor, package, or unknown.
|Core / Java
|`Models.kt`

|Annotation Type
|Java `@interface` declaration represented as an `ANNOTATION` symbol. RefactorKit indexes its declaration, nesting owner, binding identity, and usages for type-level lookup and existing class-style operations.
|Core / Java
|`Models.kt`, `JavaLanguageAdapter.kt`, `JdtJavaSemanticAnalyzer.kt`, ADR 0008

|Annotation Element
|Zero-parameter member declared inside a Java annotation type, represented by JDT analysis as a signed `METHOD` symbol such as `com.acme.Route#path()`. Read-only queries and clean exact signed rename cover its declaration and binding-matched named usages; implicit values have no element name to edit. Change-signature and default-value editing are outside this support.
|Java
|`JdtJavaSemanticAnalyzer.kt`, `JavaRenameMemberPlannerTest.kt`, ADR 0008

|Reference
|Source location that references a symbol.
|Core / Java
|`Models.kt`

|Diagnostic
|Structured message with severity and optional location/code.
|Core
|`Models.kt`

|Managed-apply Diagnostics-gate Selector
|Implemented stateless `refactorkit-jvm` function that maps exact language ID and original plan metadata plus current adapters and a surface-owned external resolver to one gate. It invokes no provider. Exact raw Java operation `java.renameMavenModule` has first Java precedence and returns its operation-owned lazy authoritative gate; every non-match retains the existing Maven-ownership/Java/Kotlin/external fallback. The returned module gate may retain the immutable plan/lease, but the selector retains no adapter, lifecycle, protocol, filesystem, or apply state.
|Shared JVM composition / managed apply orchestration
|`ManagedApplyDiagnosticsGateSelector.kt`; implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 and REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001; existing selector 11/92 evidence plus added source-built 1/31 selector/apply/rollback evidence

|Selection Metadata
|The exact caller-supplied language ID (`PendingPlan.languageId` at daemon/MCP production call sites) plus original `PatchPlan.operation`, `PatchPlan.evidence`, and `PatchPlan.affectedFiles` consumed by the selector. It excludes normalized workspace-edit affected files, snapshot-language inference, source-content inspection, and case normalization.
|Daemon/MCP managed-apply gate routing
|Chapter 8; production selector and adopting call sites; implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002

|Java-affected Kotlin Plan
|Only a Kotlin-labelled plan whose original affected-file set satisfies `plan.affectedFiles.any { Objects.requireNonNull(it.fileName).toString().endsWith(".java") }`. The explicit non-null final-segment requirement preserves the earlier root-path `NullPointerException`; the check remains case-sensitive before `PatchEngine` normalization, so `.java` matches and `.JAVA` does not.
|Mixed JVM diagnostics-gate routing
|Implemented selector vocabulary validated by REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001, including the lower-case `.java` and upper-case `.JAVA` cases

|Surface-owned External Gate Resolver
|Callback `(String) -> DiagnosticsGate` supplied by daemon or MCP. It performs that surface's existing active semantic-adapter lookup during selection, preserves its validation, exception type/message, and gate identity, and returns the external gate unchanged. The lookup occurs now; the returned provider runs later under `PatchEngine` authority.
|Daemon/MCP boundary to external semantic adapters
|Implemented selector and source-inspected `requireSemanticAdapter(...).diagnosticsGate()` adopting callbacks; REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 validate exact gate/exception identity and lookup-now/provider-later behavior in process

|Lazy Diagnostics Gate
|A `DiagnosticsGate` whose provider has not run. Selection may construct or resolve the gate before apply, but baseline, staged, post-image, and possible restored-baseline provider invocations remain deferred to `PatchEngine` while it holds the managed-write lock and authority sequence.
|Core diagnostics / managed apply
|Existing `DiagnosticsGate` and `PatchEngine` behavior plus implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002

|Lazy Authoritative Diagnostics Gate
|Language-neutral `DiagnosticsGate.lazyAuthoritative(id, gateFactory)`. Gate creation and ID inspection do not call the factory. First evaluation constructs one delegate under `LazyThreadSafetyMode.SYNCHRONIZED`; the returned gate must have the expected ID and an authoritative provider, and that provider is reused. The API has no evaluation observer. In the production module-rename route the selector supplies the factory and retained plan/lease; no caller/test factory or mechanics observer participates in apply.
|Core diagnostics / exact module-rename managed apply
|`DiagnosticsGate.kt`; `JavaRenameMavenModulePlanner.OPERATION` / `DIAGNOSTICS_GATE_ID`; implemented-and-validated REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001; focused 1-scenario/31-step source-built evidence

|Diagnostics Authority
|The right to use a diagnostic result as semantic evidence for a managed operation. Global Java/Maven authority binds a complete available source-set environment, exact workspace/overlay, Build Model/classpath, Java platform signatures, and provider. Bounded `001` validates one complete-`AVAILABLE` row with zero authoritative before, staged, after, and rollback diagnostics. Bounded `002` independently keeps each selected observer in its exact dependency-bounded environment and excludes unrelated source roots while preserving clean before/staged/restored diagnostics. The qualified target-scoped Maven `moveClass` mode still requires complete structural, candidate-total, staged-delta, and freshness proof; `007`, `009`, `010`, `011`, and `012` cover independently bounded missing-binary, scope, pruning, ordinary-absence, and selected-descriptor shapes. Maven build success is supplementary only.
|Core / Java / safety
|ADR 0012; `RPK-JAVA-DIAG-005`; `RPK-JAVA-MOVE-003..004`; REQ-JAVA-MAVEN-MOVE-AUTH-001, `002`, `007`, `009`, `010`, `011`, and `012`

|Binding-clean Semantic Authority
|Operation- and snapshot-scoped proof that one exact non-recovered JDT target binding and every in-scope Java edit site refer to the same symbol, that the complete authority scope is inventoried without concealment by parse/binding failure or truncation, and that staged re-analysis establishes the new identity. It authorizes semantic plan construction, not mutation by itself. Bounded `002` demonstrates that this selects only the target package/import/FQN edits while exact other-symbol and residual lexical evidence remains non-managed.
|Java semantic refactoring
|ADRs 0008 and 0012; `RPK-JAVA-MOVE-003`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001/002

|Target-scoped Java Semantic Authority
|Fail-closed proof for one Java semantic operation. Full-reactor active-POM authority, descriptor closure over every selected external node, hash-bound selected/pruned edge classifications, ownership, source inventory, platform/provider, and candidate audit/exclusion remain global. Per-missing-binary negative-presence and retained-diagnostic evidence is required only for the before/staged operation authority closure. The exact target is known, every Java candidate that could denote it is classified, retained closure diagnostics are exactly unchanged and positively non-concealing, and staged evidence has no new or changed error or drift. It is narrower than globally complete source-set diagnostics and does not hide `OFFLINE_MISSING`.
|Java semantic refactoring / Maven move class
|Accepted ADR 0012; `RPK-JAVA-MOVE-001..004`; concepts reviewed 2026-07-29 and 2026-07-30; bounded implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001, `002`, `007`, `009`, `010`, `011`, and `012`; dated Magrathea CLI field evidence after `010`

|Candidate-total Java Inventory
|Independent, bounded, hash-bound lexical inventory of every old-FQN token and matching simple-name token in a type-capable Java position. Each exact range is classified once by JDT as `BOUND_TARGET`, `BOUND_OTHER`, or `UNRESOLVED`; target-relevant or relevance-uncertain unresolved candidates veto authority. Exact bound totals exclude Java grammar-noncode strings and non-Java text, which are separate residual classes. The inventory finds evidence that binding analysis might omit, but never selects an edit. Bounded `002` reports exactly 12 `BOUND_TARGET`, one exact-binding `BOUND_OTHER`, and zero `UNRESOLVED` JDT records.
|Java move-class completeness
|ADR 0012; `RPK-JAVA-MOVE-003`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-002, `007`-`011`; REQ-008 validates only its named uncertainty/freshness cases

|Java Non-code Residual
|An old-FQN lexical occurrence inside a Java source file but outside a type-capable Java grammar position, such as a string literal. It is not a JDT `BOUND_OTHER` candidate and never selects an edit; reporting marks it `JAVA_NON_CODE_RESIDUAL` and `managedEdit=false` for review. Bounded `002` records exactly one such occurrence in `reporting-unrelated/.../ProductReport.java`, outside the observer closure, and preserves the file byte-for-byte.
|Java move-class residual risk
|`JavaMoveClassCandidateReporter`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-002

|Non-Java Residual
|A path containing old-FQN lexical text in a non-Java file. It has no JDT binding classification and never selects an edit; reporting marks the path `NON_JAVA_RESIDUAL` and `managedEdit=false` for review. Bounded `002` reports `NON_JAVA_RESIDUAL=2` for `catalog-acceptance/src/test/resources/features/product-lifecycle.feature` and the fixture-root `README.md`, and preserves both files byte-for-byte.
|Java move-class residual risk
|`JavaMoveClassCandidateReporter`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-002

|Java Target-name Lookup Protection
|Bounded sidecar-free proof that an ordinary absent binary cannot change what a target spelling denotes. Apart from the selected exact source declaration, a `BOUND_TARGET` occurrence is protected only when it is a simple type use with exactly one resolved non-static single-type import of the exact target FQN, or when the occurrence itself uses that exact FQN; JDT must bind the import/use to the selected declaration in before and staged images. Unprotected simple names, relevant type/static wildcard or unresolved-static ambiguity, same-package/inherited/enclosing lookup, recovered/problem evidence, unresolved candidates, or candidate/diagnostic/edit overlap fail this proof.
|Java move-class target non-concealment
|Accepted ADR 0012; `RPK-JAVA-MOVE-003`; concept reviewed 2026-07-29; bounded `009`/`010` evidence applies to an `AVAILABLE` target closure; implemented-and-validated `011` establishes this proof for one ordinary sidecar-free selected-JAR row

|Isolated Unchanged Baseline Diagnostic
|A retained diagnostic whose provider/configuration, code, category, severity, deterministically mapped path/range, and message have exact before/staged multiset identity, plus positive proof that neither the diagnostic nor its missing input can intersect, introduce, shadow, ambiguate, or conceal target lookup. Unchanged counts or source-set co-location are insufficient.
|Diagnostics / target-scoped authority
|Accepted ADR 0012; `RPK-JAVA-MOVE-003..004`; bounded implemented-and-validated `007`, `008`, and `011` evidence

|Maven Reactor Classpath and Source-path Completeness
|Layered proof derived from the declared full effective reactor. Every active reactor POM is mandatory, while external dependency closure is descriptor-closed over selected nodes in every main/test projection. Structural completeness also requires graph/variant and reactor edges, source-set ownership, source/materialized-generated-root inventory, platform/provider facts, full-reactor candidate exclusion, and dependency-bounded reverse-observer facts. External classpath availability is separate. Missing-binary evidence covers only the operation authority closure; full graph discovery never grants unrelated siblings visibility.
|Build Model / Java
|ADRs 0010-0012; `RPK-JAVA-MOVE-002`; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001, `002`, `007`, `009`, `010`, `011`, and `012`

|Maven Descriptor-closed Dependency Graph
|Per-consumer-source-set/projection graph whose active reactor POMs all remain mandatory and whose selected external nodes each have complete effective POM, parent, imported-BOM, management, mediation, and relocation inputs. Every outgoing declaration from an active reactor or selected external effective POM is hash-bound and classified as selected or deterministically pruned with exact path/projection provenance. Closure stops below a pruned external edge, not below a selected node.
|Build Model / Maven diagnostics
|Accepted ADR 0012; descriptor-closed graph concept reviewed 2026-07-30; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-010/012

|Selected Descriptor Authority Context
|RefactorKit-captured baseline for one selected external Maven coordinate. It contains unique safe workspace-relative descriptor path/layer/SHA-256 expectations and a SHA-256 parsed descriptor identity. The parsed identity is explicit proof that the selected model, identity, and relocation state were parsed; descriptor bytes alone cannot silently imply “no relocation.” A missing identity or missing, malformed, or drifted expected descriptor fails structurally before binary classification or semantic selection. REQ-JAVA-MAVEN-MOVE-AUTH-012 records workspace, descriptor facts, evidence records, and evidence hashes around each refusal and proves exact equality after evaluation.
|Build Model / Maven move-class authority
|`MavenSelectedDescriptorAuthorityContext`; `MavenSelectedDescriptorExpectation`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-012

|Deterministically Pruned Maven Edge
|An external dependency edge omitted for one path/projection because derived transitive scope is null/outside that projection, `optional=true` applies transitively, or an exact inherited path exclusion matches. It contributes no child node or subtree, so no child version/repository/POM/JAR/network lookup is required on that path when child facts cannot change selection. Another selected path restores the requirements. Type/classifier alone never prune, and ambiguity fails closed. Bounded REQ-JAVA-MAVEN-MOVE-AUTH-010 serializes six such child declarations as 18 deterministic `PRUNED` records across `COMPILE`, `RUNTIME`, and `TEST`, binding path, projection, reason, variant, and pre-lookup boundary.
|Build Model / Maven dependency selection
|Accepted ADR 0012; descriptor-closed graph concept reviewed 2026-07-30; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-010

|Enumerated Offline Leaf Artifact
|A selected missing non-reactor binary whose operation evidence is bounded to affected closure source sets. Implemented synthetic REQ-JAVA-MAVEN-MOVE-AUTH-007 records a pre-removal expected hash and provided-type manifest plus negative presence. Implemented ordinary REQ-JAVA-MAVEN-MOVE-AUTH-011 instead uses RefactorKit-owned repository identity/path, present descriptor hashes, selected-leaf proof, and exact sidecar-free no-follow absence without a provided-type claim or invented absent-content hash. A child below a pruned edge is not an offline leaf because it is not selected on that path.
|Build Model / Java move class
|Accepted ADR 0012; `RPK-JAVA-MOVE-002`; bounded implemented-and-validated `007` and `011`

|Ordinary Maven Repository Missing-binary Evidence
|RefactorKit-owned proof for one selected, fixed-release, non-reactor binary leaf in an explicitly configured simple local-only Maven-2-layout repository. Its complete effective descriptor closure is present and hash-bound, but its safely contained binary path is exactly absent before, staged, and under lock. Evidence binds coordinate/type/classifier/version, provider/layout/root, deterministic POM/JAR paths, affected closure source sets, selected-leaf proof, and no-follow absence fact hashes; it invents no absent-content hash and supplies no caller classpath or provided-type authority. Bounded REQ-JAVA-MAVEN-MOVE-AUTH-011 validates the unclassified `jar` form only, not a `test-jar` matrix.
|Java Maven move-class target-scoped authority
|Accepted ADR 0012; ordinary missing-binary concept reviewed 2026-07-30; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-011

|Reverse-observer Closure
|The target's owning source set plus every active Maven main/test source set whose effective dependency closure can legally resolve the target in the relevant image. The full reactor is needed to prove excluded siblings are unrelated; this closure is a structural input to the broader operation authority closure. Bounded `001` reports the owner separately and proves exactly three reverse observers. Bounded `002` proves their relation and binding-selected use forms: direct `catalog-pricing:main` (import, constructor, declared type), transitive `catalog-storefront:main` (import, `instanceof`, class literal), and test-only `catalog-acceptance:test` (Cucumber import, declared type, exact FQN), with no unrelated root added to any JDT source path.
|Build Model / Java move class
|`RPK-JAVA-MOVE-002`; `RPK-JAVA-DIAG-002..003`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001/002

|Operation Authority Closure
|The union, across before and staged images, of the target owner, every reverse observer, and every source set whose JDT result contributes target-binding or retained-diagnostic evidence. Missing-binary coordinate/path and diagnostic non-concealment evidence is quantified over this closure, while structural graph and candidate-exclusion proof remains full-reactor.
|Java Maven move-class authority
|ADR 0012; `RPK-JAVA-MOVE-002..004`; bounded implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001/002/009/010/011

|Managed-write Eligibility
|Explicit operation-scoped `ELIGIBLE` or `INELIGIBLE` decision evaluated under the workspace lock before WAL creation for otherwise eligible plans. For target-scoped Java move class it combines descriptor-closed selected-graph/prune evidence, global candidate exclusion, operation-closure missing-binary evidence where present, candidate-total exact bindings, retained-diagnostic non-concealment, exact staged no-new-error proof, freshness/conflict checks, and normal PatchEngine gates. Bounded `001` validates `ELIGIBLE` for two deterministic complete-`AVAILABLE` previews and revalidation through one exact staged-image transaction. Ordinary row `011` and negative rows `010`/`012` retain their separately bounded eligibility/refusal evidence. Preview, `OFFLINE_MISSING`, confidence, approval, caller classpath attestation, or Maven success is not eligibility.
|Core / Java / safety
|Accepted ADRs 0002, 0008, and 0012; `RPK-JAVA-MOVE-004`; bounded `001`/`002`/`007`/`009`/`010`/`011`/`012`

|Java Move-class Operation Outcome
|Implemented Java-specific dispatcher result with `Plan(JavaMoveClassPreview)`, `Guidance(JavaMoveClassReviewOnlyGuidance)`, and `LexicalReview(JavaMoveClassLexicalFallbackReviewEnvelope)` arms. `Plan` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence. `Guidance` carries REQ-003 plus four bounded REQ-008 preview uncertainties; `LexicalReview` carries REQ-004 alone. The two immutable non-managed arms remain mutually distinct and cannot become plans; a restored environment requires a full rescan and new dispatcher result. This is the direct Java move-class operation API, not the generic `LanguageAdapter` `PatchPlan` API or a generic core algebra.
|Java adapter / Maven move class
|`JavaMoveClassOperationDispatcher`; `RPK-JAVA-MOVE-001/005`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/004/008

|Guided Manual Refactoring Fallback / Review-only Move Guidance
|`JavaMoveClassReviewOnlyGuidance`, an immutable schema-v1 `REVIEW_ONLY_GUIDANCE` handoff for a safely enumerable Java/Maven `moveClass` defect. It binds request, snapshot, optional staged overlay, and deterministic canonical evidence identities; carries immutable typed blockers/candidate groups/completeness/actions/checklist; and has no `PatchPlan`, `PlanId`, `WorkspaceEdit`, replacement, pending plan, WAL, transaction, rollback claim, or conversion. Source-built CLI returns `guidance.nonManaged` before `PatchEngine`, lock, and WAL. REQ-003 validates four manifest/recovered-binding defects; REQ-008 independently validates four complete non-truncated authority-layer defects, including one collected by the dispatcher's second stage. Neither is the legacy lexical-review envelope.
|Java adapter / CLI user guidance
|`JavaMoveClassGuidance.kt`; `JavaMoveClassGuidanceCollector`; `JavaMoveClassGuidanceJsonRenderer`; `RPK-JAVA-MOVE-005/007`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/008

|REQ-008 Preview Uncertainty Guidance
|Implemented four-row schema-v1 `REVIEW_ONLY_GUIDANCE`: `java.maven.moveClass.candidate.unresolved` for a complete inside-closure ambiguous/problem `UNRESOLVED` candidate; `java.maven.moveClass.targetLookup.unresolvedPrerequisite` for unresolved `STATIC_IMPORT_ON_DEMAND` owner lookup while actual candidates stay bound; `java.maven.moveClass.reverseObserverClosure.explicitOldFqnOutside` for an explicit old FQN outside the dependency closure; and `java.maven.moveClass.retainedDiagnostic.identityDrift` for same-`S0` `BEFORE_VS_STAGED` identity drift. Each binds request, `S0`, canonical evidence, exact typed layer data, complete records, and `S*` where applicable; the last uses dispatcher second-stage collection. None has plan/edit capability or returns `LexicalReview`; source-built CLI refuses pre-lock/pre-WAL/no-residue as `guidance.nonManaged`.
|Java Maven move-class fail-closed preview
|`JavaMoveClassGuidance*`; `RPK-JAVA-MOVE-002..004`, `006..007`; accepted ADR 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008

|Authority-layer Blocker Precedence
|Deterministic choice of the most specific failed proof layer when one fact satisfies several blocker predicates. In validated REQ-008, `java.maven.moveClass.reverseObserverClosure.explicitOldFqnOutside` is primary even though JDT observes the candidate as `UNRESOLVED`; `java.maven.moveClass.candidate.unresolved` is not co-primary. This keeps closure consistency independent from inside-closure candidate totality.
|Java Maven move-class audit and refusal
|`JavaMoveClassGuidanceCollector`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008

|Lexical Fallback Review Authority Envelope
|Implemented immutable schema-v1 protocol result `LEXICAL_FALLBACK_REVIEW` for a legacy Java/Maven `moveClass` `LEXICAL_FALLBACK`. It reports `semanticCompleteness=NOT_SEMANTICALLY_PROVEN`, `REVIEW_ONLY`, `LEXICAL_FALLBACK`, `INELIGIBLE`, and `evidence.insufficient`; binds normalized request, exact snapshot, hidden compatibility review artifact, and sorted evidence with four SHA-256 identities; exposes bounded path/content-hash/risk residual guidance; and contains no protocol plan/edit/range/replacement/apply/transaction capability. It is a safe operation-specific projection over an internal compatibility preview, not a semantic preview and not REQ-003 `REVIEW_ONLY_GUIDANCE`.
|Java Maven move class / protocol admission
|`JavaMoveClassLexicalFallbackReview*`; ADRs 0002, 0004, 0008, and 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Lexical Review Operation ID
|Deterministic identifier `lexical-review-<64 lowercase hex>` computed from request, snapshot, hidden review-artifact, and evidence SHA-256 identities plus fixed disposition/actions. It is a non-capability audit correlation key. A known cached ID retrieves only the same immutable envelope for typed refusal; it never selects a pending plan or enters `PatchEngine`. Unknown or evicted IDs are `INVALID_PARAMS`.
|Protocol audit / safety
|`JavaMoveClassLexicalFallbackReviewJsonProjection.operationId`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Lexical Review Artifact
|Hidden canonical identity of the internal lexical compatibility workspace edit. `reviewArtifactSha256` commits to normalized file-operation paths; for modifications, current-content hashes plus exact start/end line/character ranges and replacement-byte hashes; for creates, absent status plus exact create-content hash and overwrite flag; and for delete/rename, current-content identity plus target path where applicable. The artifact itself, its ranges, and replacement/create bytes are not exposed in protocol residual records.
|Java lexical review internals
|`JavaMoveClassLexicalFallbackReviewJsonProjection.reviewArtifactSha256`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Lexical Review Residual Guidance
|Exposed edit-free review metadata containing only normalized path, current content SHA-256, typed risk facts, and `managedEdit=false`. Schema v1 caps returned records at 200 and 262,144 canonical UTF-8 bytes; reports `recordCompleteness=COMPLETE or TRUNCATED`, total/returned counts, deterministic returned counts for all four risk categories, and truncation; and uses the envelope's fixed four next actions. It exposes no source range, token hash, Maven owner, replacement/create content, file operation, semantic-reference claim, or affected-edit list.
|Java lexical review protocol
|`JavaMoveClassLexicalReviewResidualGuidance`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Audit-only Lexical Review Cache
|Synchronized Java-owned access-order LRU map from lexical review `operationId` to the exact immutable envelope, hard-capped at 128 entries and 16,777,216 aggregate canonical bytes. It is edit-free and separate from pending managed plans; it contains no internal `PatchPlan`, source replacement, authorization, authority lease, transaction, or callback into `PatchEngine`. Daemon/LSP/MCP clear it on relevant project, snapshot, mutation, semantic-session, initialization, and close transitions. Eviction/clearing changes only whether a later ID is known, never eligibility.
|Java dispatcher and daemon / LSP / MCP session state
|`JavaMoveClassLexicalReviewAuditCache`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Move-class Expected-evidence Manifest
|A strict format-version-1 properties file for one expected readable source entry or one materialized generated-root inventory. `JavaProjectScanner` captures the two recognized module-root filenames as `refactorkit-expected-evidence` auxiliary files; guidance validates owner/source set/path, exact snapshot content, and manifest content SHA-256. These manifests detect bounded mismatch or omission but do not grant semantic authority. The separate system-path classpath `.refactorkit-evidence` manifest remains classpath evidence and contributes its content hash plus expected/observed artifact fingerprints.
|Java scanner / Maven move guidance
|`.refactorkit-expected-source-inventory.properties`; `.refactorkit-generated-root-inventory.properties`; `JavaMoveClassGuidanceEvidence`; REQ-JAVA-MAVEN-MOVE-AUTH-003

|Evidence Fingerprint Mismatch
|A deterministic inequality between an expected fingerprint already bound into authority evidence and a freshly observed fingerprint for the same readable input; it is not hash age. REQ-003 uses this during preview construction. REQ-008 separately validates same-`S0` before/staged retained-diagnostic identity drift and later external non-managed candidate-source evidence drift.
|Java/Maven authority and freshness
|`JavaMoveClassGuidanceEvidence`; ADRs 0010 and 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/008

|Structural Authority Loss
|Loss, unreadability, unsafe containment, or unbounded incompleteness of required structure. Because the population cannot be safely bounded, this is `REFUSED`, not `REVIEW_ONLY_GUIDANCE`. Bounded REQ-008 removes `catalog-shipping/pom.xml` while the readable root POM declares it active; result-owned `JavaMoveClassStructuralRefusal` carries `java.maven.reactorDescriptor.missing`, `STRUCTURAL_CLOSURE`, `ACTIVE_REACTOR_POM`, module, root-POM hash/range, expected path, `MISSING`, and deterministic component-wise workspace-relative `NOFOLLOW` identity. Missing module directory is supported; symlink/non-regular final states are not `MISSING`. Selected alternate-path/external descriptors remain REQ-010/012; broader structural loss is not inferred.
|Build Model / Java Maven move authority
|ADRs 0010 and 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008/010/012

|Post-preview Candidate-inventory Drift
|Validated REQ-008 apply-phase failure in which an eligible REQ-007 preview binds `S0`, `S*`, expected semantic candidate-inventory SHA-256, and `E0`; generic required-file evidence binds one non-managed `BOUND_OTHER` source. After lock acquisition that source changes and one engine observation becomes `S1`. Revalidation returns primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged`, with expected/observed content and required-file-evidence hashes. No observed semantic candidate inventory/hash is claimed. External `S1` is preserved; no WAL, transaction, managed target edit, or destination is created; the lock file may remain.
|Core / Java Maven move-class lease
|`OperationAuthorityFileEvidence`; `DiagnosticDetails`; accepted ADR 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008; REQ-011 selected artifact/POM/repository drift excluded

|Operation Authority File Evidence
|Generic immutable core value for a caller-defined evidence kind, normalized workspace-relative path, expected content SHA-256, and sorted immutable attributes. It binds a non-managed file into an operation lease without moving language semantics into core. REQ-008 uses kind `CANDIDATE_INVENTORY` for one `BOUND_OTHER` Java source; core revalidates content under lock and computes expected/observed required-file-evidence hashes.
|Core authority lease
|`OperationAuthorityFileEvidence`; `OperationAuthorityLease`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008

|Diagnostic Details
|Generic immutable core map of sorted string fields attached to a `Diagnostic`. Defensive copying and immutable iteration preserve deterministic structured refusal data. REQ-008 uses it for evidence layer/kind/path/content/snapshot identities, expected semantic candidate-inventory hash, and expected/observed required-file-evidence hashes.
|Core diagnostics
|`DiagnosticDetails`; `PatchEngine`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008

|Java Platform Signature Catalog
|Immutable, licensed, integrity-verified, hash-attested and SBOM-covered Java SE types, members, and release-appropriate module metadata used for exact `--release` diagnostics. It is distinct from source grammar, the host JDK, and RefactorKit's reduced jlink runtime.
|Java / packaging
|ADR 0012; `RPK-JAVA-DIAG-001`

|Authoritative Snapshot Diagnostics Evaluation
|Implemented transient immutable value and language-neutral core application port that bind one authoritative `ProjectSnapshot` plus diagnostics to one exact candidate. Construction deep-detaches every outer/nested collection in `ProjectSnapshot`, `Module`, `BuildModel`, `BuildModule`, `BuildSourceSet`, diagnostics, and `DiagnosticDetails`; a fresh canonical hash over exposed snapshot state retains construction-time identity and diagnostics/details remain unchanged. Core preserves normalized workspace root, source/auxiliary partition, tracked paths/language IDs/bytes, source extensions, and ignored directories; only modules, classpath evidence, and build models may be rehydrated. Core validates `Module.root`, every module source root, and every `BuildModule.root` as workspace-contained. Explicit absolute external classpath evidence remains allowed only when core independently recomputes its current no-follow path/kind fingerprint and exactly matches supplied evidence; provider authority includes neither arbitrary roots nor fabricated classpath facts. Baseline equals S0/D0, staged evaluation yields exact S1/D1, post-provider under-lock workspace/lease revalidation precedes schema-v8 S1 journaling, committed evaluation equals S1/D1, and mismatch automatically rolls back to evaluated S0/D0. Existing diagnostics-only gates/hashes remain exact. The result is discarded with the apply attempt; it is not an aggregate, repository, durable service/store, stream, or God service and adds no core language dependency.
|Core Patch and Transaction Execution / language-adapter anti-corruption layer
|Implemented-and-validated REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`; accepted ADRs 0002/0012; `docs/build-model.md`; `docs/transactionality-audit.md`; focused 16/16-scenario, 128/128-step evidence

|Bounded Maven Module Rename
|Implemented direct-library operation that changes one proven active reactor module's workspace-relative direct-child directory and changes its Maven artifact component only when the caller independently supplies `newArtifactId`. Directory spelling never implies coordinate intent; omission preserves the artifact and authorizes no dependency-coordinate edit. The first row is exactly `catalog-model` to `catalog-domain` with explicit artifact `catalog-domain`, one direct parent module origin, and effective-model-proven literal dependency origins. Maintained Woodstox 7.1.1 StAX2 location information over hash-bound raw POMs is the sole exact element-text range authority; DTD processing, external general/parameter entities, and external DTD access are disabled. It uses byte-preserving range edits without POM serialization or hand-written-tokenizer fallback, modifies the moved POM before file renames in one `WorkspaceEdit`, requires a complete offline staged reactor/diagnostics, and delegates explicit apply/one-WAL-transaction/exact rollback to `PatchEngine`. The parser dependency is bounded to `refactorkit-java`.
|Java Language Adapter / Maven ownership migration
|Implemented-and-validated REQ-JAVA-MAVEN-MODULE-RENAME-001; `docs/requirements/java-maven-ownership-migration.md`; focused 1/1-scenario, 50/50-step evidence; no broad support qualification

|Maven Module-rename Planner
|Implemented stateless Java/Maven domain service with application-service orchestration responsibility. It consumes one immutable command, raw POM documents, authoritative effective-model projection, and exact parser-reported origin/range evidence from maintained Woodstox 7.1.1 StAX2 location information, then stages reactor/diagnostics and returns one `PatchPlan`. It rejects operation-owned hand-written XML tokenization and POM serialization. Its Woodstox 7.1.1/StAX2 parser dependency remains inside `refactorkit-java`; core stays XML-library independent. It has no aggregate/entity identity, repository, cache, lifecycle, or durable store. The existing transaction journal remains the WAL, and `PatchEngine` remains the sole managed mutation/rollback authority. The historical prototype remains negative evidence; the promoted implementation uses exactly three parser-authorized POM range edits plus two file renames and retains every stated exclusion.
|Internal Java Language Adapter C3/code-scale responsibility
|REQ-JAVA-MAVEN-MODULE-RENAME-001; `JavaRenameMavenModulePlanner`; Chapters 4-6 and 8; accepted ADRs 0002/0012; no C4 change

|Maven Source-set Semantic Environment
|Complete hash-bound compile view for one active reactor module and one main or test source set: source/materialized-generated roots, outputs, Maven-scoped reactor/external visibility, Java settings/platform, provider evidence, and source-set availability.
|Build Model / Java
|ADR 0012; `RPK-JAVA-DIAG-002..004`

|Effective Maven Dependency Scope
|Scope derived with declaration provenance preserved. An explicitly declared transitive `test` scope remains `test`; a dependency-management scope default supplies scope only when the dependency declaration omits it. For an external transitive edge, a null or projection-excluded derived scope is a deterministic prune whose classification precedes child lookup when child facts cannot alter it.
|Build Model / Maven source sets
|Accepted ADR 0012; `RPK-JAVA-DIAG-002..003`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-009/010

|Reactor-root Workspace Authority
|Rule that the caller-declared full Maven reactor root defines both the readable workspace and effective-model boundary. Module targeting filters results after model construction and never authorizes implicit upward or outward discovery.
|Build Model / security
|ADR 0012; `RPK-JAVA-DIAG-002`

|Typed Unavailability
|A concise structured result stating that a semantic input or supported shape is unavailable for an affected source set. It replaces guessed diagnostics and is not equivalent to a source compiler error. It blocks global authority. For the sole target-scoped Maven `moveClass` mode, selected-binary `OFFLINE_MISSING` may proceed only after descriptor closure, candidate exclusion, and operation-closure evidence pass. Missing POM/parent/BOM/effective-model facts on an included edge always block; absence below a deterministically pruned edge is not a missing selected input. Outside-closure absence stays visible without being mislabeled operation evidence.
|Diagnostics / safety
|Accepted ADR 0012; `RPK-JAVA-DIAG-004..005`; `RPK-JAVA-MOVE-002..004`; implemented-and-validated `010`/`011`/`012`

|RiskLevel
|Risk classification for a patch or refactoring: LOW, MEDIUM, HIGH.
|Core / safety
|`Models.kt`

|Stable Contract
|An API `1.0` behavior or capability with a versioned schema/semantics, deterministic failure modes, compatibility policy, and passing RC acceptance evidence. Stability does not imply support for every refactoring shape.
|Release / integration
|`docs/releases/v1.0.0-plan.md`, ADRs 0002 and 0004

|Evidence Classification
|Per-operation outcome classified as JDT binding-backed, separately proven structural behavior, review-only lexical evidence, or refusal. In target-scoped move authority, lexical inventory is completeness/veto evidence only; it never upgrades a range to `JDT_BINDING` or mutation evidence. Exact JDT candidates stay separate from Java non-code and non-Java residuals, all guidance occurrences report `managedEdit=false`. `LEXICAL_FALLBACK` remains a review-only internal compatibility-plan evidence kind rejected by `PatchEngine`; the implemented Java move-class outcome dispatcher projects it to `LEXICAL_FALLBACK_REVIEW` without exposing plan ID/edit/range/replacement capability. Bounded REQ-003 authority loss and four bounded REQ-008 preview uncertainties instead return distinct plan-free `REVIEW_ONLY_GUIDANCE`. Bounded `001`/`002` prove semantic/discrimination rows; `003` proves four guidance defects; `004` alone proves the lexical-review cross-surface boundary and clean-JDT preservation; `008` proves four additional guidance cases, one structural `Plan` refusal, and one library/`PatchEngine` under-lock refusal. A separate CLI/Linux/JDK-21 run at Magrathea commit `a48e3c06` remains request-specific. Existing REQ-001 also has one dedicated local Linux `amd64` packaged-process observation; neither bounded row generalizes to REQ-002..012 package behavior, a native CI matrix, or general authority.
|Java / safety
|ADRs 0002, 0004, 0008, and 0012; `RPK-JAVA-MOVE-003..005`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001/002/003/004/005/008; dated Magrathea field evidence

|Packaged-process Qualification
|Bounded evidence that an existing capability executes through the real unpacked launcher and embedded runtime as separate child OS processes, rather than through source-built classes or an in-process seam. For implemented REQ-JAVA-MAVEN-MOVE-AUTH-001 qualification, it is a second Cucumber validation mode over the same feature/ID/pickle and therefore adds no requirement or case. It binds one no-follow fixture copy; package/runtime identities; scrubbed subject environment and poison-`PATH` proof; 16 bounded commands; repeat semantic preview; exact three observer/five affected paths; exact human-text lock refusal before WAL without projected `workspace.locked` or core-code correlation; one child-process-replanned apply with no cross-process plan-ID claim; a sole schema-v8 transaction with four `Modify` plus one `Rename` and nested `TextEdit` counts `1/2/1/1`; clean post-apply diagnostics; normal rollback; exact manifests; and bounded timeout/cleanup. It is a test/deployment adapter and immutable qualification projection, not a product aggregate, event store, protocol, or new C4 container.
|Test / deployment evidence
|Implementation under `modules/refactorkit-cli/src/packagedMavenMoveClassAuthorityTest/**`; Chapters 2, 4, 6-8, 10-11 and requirements appendix; official final packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`; whole-tree log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`; targeted static log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`

|Local Packaged-process Observation
|Implementation-informed execution receipt for the exact local unpacked package, host/architecture, fixture, request, and command sequence on a mutable tree. It supports an implementation-informed statement for that bounded local mode without becoming a release/native-CI ledger receipt. The recorded REQ-001 observation used OpenJDK 21.0.11 on Linux `amd64`, passed 35/35 Gradle tasks and 19/19 Cucumber steps plus 2/2 hooks, restored fixture manifest `905b354c151e8219d6f755335ecac381a0dab83df20de55d22f8a15a8695829e`, and left package manifest `681822f781379f5ddbe67389d1e33606f8931ca6fc4a75a3b3fc09f657b88430` unchanged. It does not qualify archive trust, child-JVM coverage, other surfaces/requirements/populations, adverse crash/concurrency, or another native host.
|Local test/deployment evidence
|Chapters 6-8; `docs/testing.md`; `docs/packaging.md`; source-built REQ-001 rerun log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`

|Configured Unobserved (`CONFIGURED_UNOBSERVED`)
|Qualification-ledger state meaning that an exact post-commit platform/CI row has no reviewed passing receipt. It is not qualified, supported, or promotable for that row. Repository implementation, static review, or a local mutable-tree observation does not change this state. For REQ-001 package mode, native CI wiring and always-run report uploads are implemented but unexecuted; Linux x86-64 CI, Windows x86-64, macOS x86-64, and macOS arm64 remain independently `CONFIGURED_UNOBSERVED`; Windows/macOS logic is configured and statically reviewed only.
|Release qualification ledger
|`docs/releases/v0.7.0-support-matrix.md`; `docs/releases/v0.7.0-java-maven-move-class-acceptance.md`; exact post-commit CI receipts pending

|Release Candidate (RC)
|Immutable `v1.0.0-rc.N` tag used only after the deep language roadmap, Clojure integration, alpha/beta validation, and API freeze to validate proposed stable contracts and artifacts; a failed tagged candidate is replaced by a new commit and RC number.
|Release
|`docs/releases/v1.0.0-plan.md`

|SBOM
|Software Bill of Materials published with a stable runtime asset as supply-chain verification evidence.
|Packaging / release
|ADR 0005

|CLI Command Catalogue
|Transient immutable versioned protocol document for public CLI route discovery. V1 and v2 are separate closed contracts with the same fixed three-route inventory and fields. V1 keeps every entry `catalog-only`; v2 alone reports rename `preview-only`. Catalogue presence is not execution, approval, write authority, language capability, result data, installed-runtime attestation, support promotion, or persistence.
|CLI distribution surface
|Current atomic Kotlin production SHA-256 `db6067bf3235191957203474da95f03c4399f4de47661af9a3dcb3ef65720246` / `ac65359ed95dd09efaa6820b05a7c7d0c18a3b4495cc3e4f738f76d19149b50d`; current promoted feature SHA-256 `f02fc091db83e8b96909e4c596bda9702204c62f2874fdf1a0fd45a01e3970cf`; shared test launcher SHA-256 `081b01d28686118aaa4ab7e94b1e3f8e17b8b9c31b5d6a59b79c7ad99305bdc5`; current mode-`0444` post-promotion CATALOG-001/002 GREEN log/JSON SHA-256 `5f31ba5a9162fb4673af2cffce95e1d32bc3da8648726aa23ecde3f1766e71e7` / `1c8397c4e697558c58e997286505594016cdbc827a9df1446b2c07ef4c49ba63` and `b561b3ebf0e32d7dca84081af0e48bca21dd57d8b392e99af18b4f99dcfd7e21` / `96f1e3825fc933baae06b76981a135a0aee7d79b6498382c43a36aca6fbb7464`; implementation-informed, both requirements `@implemented-and-validated`. Earlier pairs remain immutable historical evidence

|CLI Catalogue Version Selector
|Closed parser/ACL mapping with exactly two admitted states: no selector selects v2; exact `--schema-version 1` selects v1. Malformed, duplicate, missing-value, and unsupported selectors refuse without fallback; this defines no qualified result-error envelope or exit code. The selector performs no workspace lifecycle or field-level schema conversion.
|CLI Application inbound protocol boundary
|Approved CATALOG-001 change and CATALOG-002 baseline; implementation-informed post-promotion GREEN, both declared `@implemented-and-validated`

|Legacy Catalogue v1 ACL
|Compatibility boundary that constructs and validates the exact previously promoted v1 bytes/semantics only when explicit version 1 is selected. It never labels a v2 document as v1 and retains `catalog-only` for all three entries.
|CLI catalogue compatibility boundary
|Approved CATALOG-001 change SHA-256 `9fee5e4230e9e00ef267ddf48b72ab133d3469cb8c9b2fa8cf5aa42e9af2024e`; implementation-informed post-promotion GREEN, declared `@implemented-and-validated`

|CLI JSON Support Classification
|Version-scoped catalogue value. `catalog-only` claims only discovery. `preview-only` claims only successful source-built RESULT-001 preview for rename; it claims no apply, refusal, error, limits/truncation, write authority, installed runtime, other command, or wider support.
|CLI catalogue/result truth boundary
|CATALOG-002 and RESULT-001 are `@implemented-and-validated` within bounded source-built evidence; RESULT-001 has final post-re-promotion 30/30 evidence; all deferred outcomes remain absent

|Atomic Protocol Truthfulness
|Admission invariant requiring default v2's rename `preview-only` advertisement and successful RESULT-001 preview to enter production and promotion together. CATALOG-001 compatibility, CATALOG-002, and RESULT-001 remain separately reviewable evidence subjects but share one production changeset and joint promotion verification.
|CLI protocol release boundary
|Approved CATALOG-001 change, CATALOG-002 baseline, and RESULT approved change 002; one atomic production revision with post-promotion catalogue GREEN and final post-re-promotion RESULT 30/30 GREEN. All three rows are validated within separately bounded source-built evidence; installed/package/platform and wider E2E qualification remain open

|CLI Route Identity
|Exact ordered public token path after the executable name, stored as one byte-exact space-separated string such as `java create-module`. The `refactorkit` executable prefix is excluded. A CLI alias would be another complete accepted token path; the three implemented bounded v1 Java routes have no aliases. A route is not inferred from a class, recipe, language capability, support-matrix row, or semantic operation name.
|CLI distribution surface / anti-corruption layer
|LLM Wiki concept `cli-route-identity-and-semantic-operation-identity`; historical v1 evidence plus current implementation-informed CATALOG-001/002 post-promotion GREEN

|Semantic Operation Identity
|Immutable planner/plan behavior name selected after CLI parsing, distinct from the public route and from an alias. The implemented bounded catalogue mappings are `java create-module` to `java.createMavenModule`, `java move-across-maven-modules` to `java.moveAcrossMavenModules`, and `java rename-module` to `java.renameMavenModule`. Presence in the catalogue neither invokes the operation nor proves success, stability, approval, result-JSON support, or write authority.
|CLI-to-Java-planner boundary
|Reviewed domain concept `cli-route-identity-and-semantic-operation-identity`; current implementation-informed parser/catalogue/help GREEN under `@implemented-and-validated` CATALOG-001/002

|CLI Operation Result Envelope
|Transient immutable `refactorkit.cli-result/v1` document for one completed CLI invocation. In RESULT-001 it has exactly ordered top-level fields `schemaVersion`, `command`, `requestId`, `outcome`, `plan`, `transaction`, `diagnostics`, and `truncated`; the qualified preview fixes `1`, `java.renameMavenModule`, valid correlation, `preview`, non-null plan, null transaction, empty diagnostics, and `false`. V1 has no top-level `schema` or `error`. It is neither an internal plan/transaction DTO nor a durable result, approval, apply handle, or audit record.
|CLI distribution surface / RESULT-001 preview only
|Initial/decomposition/atomic-correction SHA-256 `8f29946c5dcb0128075764fbcd2756156d61edfad4d0fffe2f49afa526c68957` / `7c8555ba5c3f393f1809136a38e86da9419234962864513bd4515001557c250c` / `40a0183ec29584724a98e2357738078e7caa8d8356c90a7c0e4cd9930ff3e73e`; current promoted feature/status-neutral-canonical SHA-256 `5956f93a681562f6922a9101daa8c153c9a3b8d1f7fdc260bd0070d5faf48ea5` / `b6f62831bf38c2fdaadbd02abc024abd16c8818a812678c76a1eb146642e4ed9`; final mode-`0444` post-re-promotion GREEN log/JSON SHA-256 `6ab958c33d541b79c08b2eef354a2bddfdb298d60040364f4d5c935915d14c3d` / `2e10af7f91c7fdf57f5501672dce98869baf26258b86129a35b5b4cec451f21a`; supporting `testClasses`/12-of-12 unit SHA-256 `5ff927b32797f4acf60693a562b900e21d958d1b6a9166d9de91b1c52aded264`; corrected pre-final-promotion pair `63d8d62f22317d09579abf6381a08a01c85f894e31db183c98aa52f222f3feb6` / `4996f117788f1fa53195f0822d25a92e0848e322a4e9da11723f6df36130e4ca` remains superseded historical evidence; implementation-informed, RESULT-001 `@implemented-and-validated`

|CLI Result Projection ACL
|Surface-owned anti-corruption translation from the exact internal `PatchPlan`, validation diagnostics, and—only in later slices—journal outcome into closed CLI result protocol values. It exposes safe normalized correlation/projection fields while excluding internal `PlanId`, raw workspace edits, source/replacement bytes, prose, authority leases, journal DTOs, absolute roots, and host metadata. Projection grants no mutation authority and creates no store.
|CLI Application internal C3/code-scale responsibility
|ADRs 0002/0004; approved RESULT atomic correction; current implementation-informed two-file Kotlin production and final post-re-promotion 30/30 GREEN; RESULT-001 is `@implemented-and-validated` within the bounded source-built contract and atomically coupled to default v2

|Canonical CLI Plan Projection
|Closed plan document with exactly ordered fields `sha256`, `snapshotSha256`, `requiresApproval`, `changeCount`, and `changes`. Each visible change has exactly `kind`, `path`, and `previousPath`; current kinds are `modify` with null previous path and `move` with source previous path/destination path. The list preserves normalized application order and intentionally hides text ranges/replacements and internal plan identity.
|CLI result protocol / RESULT-001
|Chapter 8; approved RESULT-001 receipts; implementation-informed final post-re-promotion 30/30 GREEN, declared `@implemented-and-validated` within the bounded source-built contract

|Canonical CLI Plan SHA-256
|Lowercase SHA-256 over the complete normalized edit using length-framed UTF-8 strings, explicit kind bytes, unsigned big-endian counts/ranges, and exact replacement bytes in canonical edit order. It remains stable across independent equivalent plans even when internal random plan IDs differ. It is content correlation only, not a plan ID, approval, authorization, transaction identity, apply handle, or semantic authority.
|CLI result protocol / plan correlation
|Reviewed Wiki concept `canonical-cli-plan-and-transaction-projections`; Chapter 8; implementation-informed final post-re-promotion GREEN, declared `@implemented-and-validated` within the bounded source-built contract

|CLI Request ID
|Opaque 1..128-byte ASCII correlation value matching `[A-Za-z0-9][A-Za-z0-9._:-]{0,127}` when caller supplied and preserved byte-for-byte. When absent in RESULT-001, the CLI generates one `request-<lowercase canonical UUIDv4>`. It has no plan, transaction, approval, idempotency, authentication, or semantic-authority meaning.
|CLI result protocol / invocation correlation
|Reviewed Wiki concept `cli-result-diagnostics-correlation-and-truncation`; request correlation implementation-informed, declared `@implemented-and-validated` within the bounded source-built contract

|CLI Result Diagnostic Projection
|Reserved transient immutable v1 document with exactly ordered fields `kind`, `severity`, `code`, `category`, `evidence`, `subject`, `precision`, and `location`; a location has exactly `path`, `startLine`, `startCharacter`, `endLine`, and `endCharacter`. RESULT-001's qualified preview emits an empty array. Populated validation/refusal/error mapping, redaction, limits, and truncation belong to deferred RESULT-003 through `006` and are not current implementation claims.
|CLI result protocol / deferred populated outcomes
|Reviewed Wiki diagnostic concept; empty RESULT-001 array and reserved type are implementation-informed under bounded `@implemented-and-validated`; populated RESULT-003..006 behavior remains absent

|CLI
|Command-line RefactorKit application.
|Integration
|`refactorkit-cli`

|JSON-RPC Daemon
|Long-running local stdio JSON-RPC process for project sessions and preview/apply/rollback operations.
|Integration
|`refactorkit-daemon`, integration docs

|LSP Server
|Language Server Protocol integration for editor-native operations.
|Integration
|`refactorkit-lsp`

|MCP Server
|Model Context Protocol interface exposing deterministic tools/resources/prompts to local LLM agents.
|Integration
|`refactorkit-mcp`

|External Java Class Importer
|External code assimilation module that creates preview plans for imported Java classes with provenance/license/conflict checks.
|Web importer
|`refactorkit-web-importer`, importer docs

|Recipe
|YAML-defined multi-step refactoring workflow whose eligible steps are previewed against one evolving staged snapshot and reduced to one recipe-wide managed apply/rollback transaction. Implemented REQ-004 is a terminal exception: a lexical `moveClass` returns `RecipeResult.NonManaged`, discards prior in-memory staging, runs no later step, and has no recipe-wide plan. REQ-003/008 guidance remains a distinct dispatcher outcome; no recipe-path parity is claimed for it.
|Java / CLI
|`RecipeEngine`; `docs/recipe-engine.md`; transactionality audit; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004

|Golden Test
|Test case with before/after trees, request JSON, and expected plan metadata.
|Testkit
|`docs/testing.md`, `testdata/golden`

|Agent Simulation Test
|Test that simulates local LLM agent workflow: scan, preview, inspect, apply, verify, rollback.
|Testkit
|`docs/testing.md`

|Tree-sitter Foundation
|Current structural multi-language foundation with regex-backed scanner and extension point for real Tree-sitter/native support.
|Multi-language
|`refactorkit-tree-sitter`, multi-language docs

|ExternalLspAdapter
|Adapter intended to launch external language servers and bridge future LSP-backed language support.
|Multi-language
|`docs/multi-language.md`

|Kotlin Companion Move Boundary
|Confirmed rule that a companion object is a nested part of its enclosing type, not an independently movable declaration. A standalone move request must return a stable typed refusal; a supported enclosing type/file move may carry it unchanged.
|Kotlin K5 movement
|`RPK-V07-KOT-COMPANION-MOVE-001`; completion contract; planned correction

|Enclosing-type Carry
|Movement of an admitted enclosing Kotlin type/file in which its nested companion remains part of the same declaration and moves only as a consequence of the enclosing operation. It is not companion extraction or independent companion movement.
|Kotlin K5 movement
|Confirmed program decision; `docs/requirements/kotlin-jvm-move-declaration.md` boundary refinement

|Companion Extraction
|Future separately named operation that would create or move declarations out of a companion or enclosing type. It needs its own identity, consumer updates, diagnostics, refusal contract, and acceptance and is not represented by `moveDeclaration`.
|Future Kotlin capability
|Confirmed exclusion; unimplemented

|Typed TypeScript Refactor Authority
|Toolchain-, lease-, snapshot-, UTF-16-selection-, preference-, and returned-action-bound authority to request one exact pinned TypeScript compiler/server refactoring and normalize its immutable proposal. The TypeScript adapter owns the anti-corruption translation; `PatchEngine` separately owns managed writes.
|TypeScript T5
|Reviewed 2026-08-02 concept `typed-typescript-refactor-authority` and `SRC-2026-08-02-013` against commit `2b5aa7234cc385214acb6958834763288aefc527`; planned

|Source-file Relocation
|Move of one TypeScript source file using `getEditsForFileRename`, including compiler-proven import/export module-specifier edits. It does not mean moving selected declarations.
|TypeScript T5
|`RPK-V07-TS-FILE-RELOCATE-001`; planned

|Declaration Move
|Move of selected TypeScript declarations between files/modules using one exact action returned by `getApplicableRefactors` and selected through `getEditsForRefactor`. It is distinct from file relocation.
|TypeScript T5
|`RPK-V07-TS-DECLARATION-MOVE-001`; planned

|Module-specifier / Project-reference Migration
|Model-changing TypeScript operation that updates exact import/export specifiers and `tsconfig` project-reference relationships. It is distinct from file relocation, declaration move, and npm package/module rename.
|TypeScript T5 / Build Model
|`RPK-V07-TS-MODULE-MIGRATION-001`; planned

|Organize-import Mode
|Explicit TypeScript organize-import intent: `All`, `SortAndCombine`, or `RemoveUnused`, bound to exact formatting preferences and snapshot. One mode's evidence does not qualify another.
|TypeScript T5
|`RPK-V07-TS-ORGANIZE-IMPORTS-001`; planned

|Extract Function / Constant
|Two exact upstream applicable-refactor action families for extracting a selected expression/statements into a function or constant. Their returned action identity and control/data-flow constraints must be retained.
|TypeScript T5
|`RPK-V07-TS-EXTRACT-001`; planned

|Inline Variable
|Exact TypeScript action that replaces a variable use/declaration under compiler evidence. It is a separate row and supplies no inline-function authority.
|TypeScript T5
|`RPK-V07-TS-INLINE-VARIABLE-001`; planned

|Inline Function
|Conditional TypeScript capability requiring its own compiler-proven operation/action and acceptance. It must never be inferred from inline-variable or extraction; absent authority yields stable typed refusal.
|TypeScript T5
|`RPK-V07-TS-INLINE-FUNCTION-001`; planned conditional row

|Authority-preserving Recipe Composition
|Recipe reduction that retains every authority-owning child's exact operation identity, plan, evidence, lease, auxiliary inputs, and diagnostics/evaluation gate through final-candidate apply, or uses a separately proven recipe-wide evaluator. Net-byte composition or relabeling to `recipe:<id>` is insufficient.
|Recipe orchestration / managed apply
|`RPK-V07-RECIPE-AUTHORITY-001`; reviewed concept `authority-preserving-recipe-composition`; planned correction

|Client-managed Module-rename Proposal
|API `0.2` LSP module/identifier rename `WorkspaceEdit` that is version-checked, applied by the editor, marked `client-managed`, and has no RefactorKit transaction or rollback. It must not be reported as managed apply.
|LSP compatibility
|`RPK-V07-LSP-MODULE-RENAME-001`; ADR 0004; confirmed

|Managed Module-rename Apply
|Explicit application of an exact retained module-rename plan through `PatchEngine`, after managed LSP dirty/affected-open-document guards. Only this route may claim RefactorKit authorization, diagnostics-gated WAL, recovery, and rollback.
|LSP / Core
|`RPK-V07-LSP-MODULE-RENAME-001`; ADRs 0002/0004; planned operation-specific qualification

|Release Evidence Identity
|Immutable join across repository, tag, full commit, clean/version state, native archive and extracted-image hashes, fixture/request/scenario/toolchain identities, operation-family report hashes, SBOM hash, and attestation subject digest. The support matrix is a projection, not proof.
|Release qualification / supply chain
|`RPK-V07-RELEASE-EVIDENCE-001`; reviewed concept `release-evidence-identity`; planned I1

|P0 Build-scaffolding Gate
|Prerequisite that clean OpenJDK 21 all-module `testClasses` compilation passes before production K5/T5/J1/I1 implementation. The historical commit `2b5aa7234cc385214acb6958834763288aefc527` failed before compilation; the authorized correction changed only the Kotlin module build scaffold and deferred `testRuntimeClasspath` resolution into test execution.
|Build / completion process
|`RPK-V07-SCAFFOLD-001`; resolved for local test-compilation admission; historical baseline log SHA-256 `408d45e7e2c1bea4152c220af283455421a3bee84dca2b3f069832e9541983d5`, slice RED `72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`, GREEN `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`, and full OpenJDK 21 log `48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`. Actions runs `30746237578` and `30749086970` remain failed historical observations; their shared Linux archive-verification cause is separately corrected locally by wiring root `distCliRuntimeZip` to `:modules:refactorkit-cli:refactorkitRuntimeZip`, with archive SHA-256 `afcd65b0a800ed21f4c46d3b47e0fb569f81fb9c913c5d2965a5189edd3a0b85`. Final JDK 21 source-built verification with `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 7m 28s with 82/82 tasks; log `/tmp/refactorkit-runtime-zip-task-final-check.log`, SHA-256 `009dd3940c1042cc6d1d97e18709795760d6eb4025b685f1be4c71e7e1fccae4`. This receipt remains source-built evidence and does not qualify CI, native, or release behavior. Post-commit CI and release qualification remain unobserved/open

|Framework-aware Risk Detection
|Lexical detection of Spring, JPA/Jakarta Persistence, and Jackson annotations to raise warnings/risk during selected Java refactorings.
|Java
|`docs/framework-awareness.md`
|===
