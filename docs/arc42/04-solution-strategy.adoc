= 4. Solution Strategy
:toc:
:sectnums:

Status: implementation-informed with planned items explicitly marked.

== Strategy Summary

RefactorKit is structured as a local, deterministic refactoring engine around a language-independent patch core and pluggable language/integration adapters. The primary strategy is to make changes previewable and diagnosable, with managed compensation after completely successful apply, for humans, CI, editors, and local AI agents. The audited current flows are preflighted compensatable batches, not ACID or crash-safe transactions; stronger rollback, isolation, durability, and recovery properties remain planned.

The release strategy published the hardened Java/transaction foundation as `v0.4.0`, the cross-platform runtime and multi-language kernel as `v0.5.0`, and the managed TypeScript/JavaScript semantic foundation as `v0.6.0`. Later `0.x` bands build deep adapters. API `1.0` freezes only after Clojure completes the planned language sequence and global alpha/beta/RC acceptance passes; active API remains `0.2` until an explicit migration. Stability is evidence-gated per language and operation; unsupported shapes remain conservative refusals.

The packaged-process qualification strategy for existing `REQ-JAVA-MAVEN-MOVE-AUTH-001` is now implemented as a deployment/test validation adapter, not product semantics: the authoritative feature/pickle is executed by a separate packaged Cucumber source set/task against the real unpacked self-contained CLI. Ordinary `test`/`check` remains source-built and packaging-independent. Official final OpenJDK 21.0.11/Linux `amd64` execution of `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks` reported `BUILD SUCCESSFUL` in 22s with 35 tasks, JUnit 22 discovered/1 executed/21 tag-filtered/0 failures or errors, and one Cucumber scenario with five background plus fourteen scenario steps (19/19 passed) and 2/2 hooks; packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`, JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The separately reported final source-built whole-tree gate passed 1045 discovered/817 executed/228 skipped or tag-filtered standard tests with zero failures/errors, 140 XML files, and 82 executed tasks (log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`); it intentionally excludes the dedicated packaged task. Targeted custom-source-set PMD/SpotBugs tasks also passed (log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`) with an intentionally non-zero, reviewed/non-blocking classification of 99 packaged glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), no production source addition/modification by this slice, and no aggregate-cleanliness or numerical/child-JVM-coverage claim. This remains implementation-informed local evidence for the mutable tree, not a native-CI promotion. Native CI wiring and `if: always()` report uploads are implemented but unexecuted; Linux x86-64 CI, Windows x86-64, macOS x86-64, and macOS arm64 therefore remain `CONFIGURED_UNOBSERVED` pending exact post-commit receipts. The implementation does not broaden REQ-002..012, other surfaces, field/general populations, crash/concurrency, archive trust/SBOM/signing, or child-JVM coverage.

== Confirmed `v0.7.x` Completion Strategy

Status: confirmed program architecture, with implementation planned or otherwise open unless a separately cited bounded row is already qualified. The normative scope is link:../requirements/v0.7.x-completion-contract.md[`docs/requirements/v0.7.x-completion-contract.md`], and the requirements appendix owns the decomposed completion catalogue. Existing accepted decisions and historical execution evidence remain unchanged.

The completion strategy is *authority before aggregation*:

* Kotlin movement follows declaration ownership. A companion object cannot be selected as an independent move target; moving the enclosing type/file may carry it, while extraction is a different future operation. Standalone selection must fail with a stable typed refusal.
* TypeScript T5 is decomposed into source-file relocation, declaration move, module-specifier/project-reference migration, explicit organize-import modes, extract-function/constant, inline-variable, change-signature, and a separately proven inline-function row. The TypeScript adapter owns a typed anti-corruption layer over the pinned compiler/server protocol and returns immutable proposals or stable refusals. Dynamic/mixed JavaScript receives no inherited TypeScript authority.
* API `0.2` LSP module/identifier rename remains a client-managed proposal with no RefactorKit rollback. The separate managed retained-plan route is the only LSP route that may enter `PatchEngine` and claim WAL/recovery/rollback after its existing document guards.
* Recipes preserve the exact authority of every admitted child. A recipe cannot turn an operation-owned plan into a generic `recipe:<id>` plan with weaker evidence or diagnostics. Until a recipe-wide authoritative evaluator is proven, composition is limited to one authority-owning mutation plus non-mutating projection/summary steps.
* Release qualification joins operation evidence and native artifacts through one immutable release evidence identity and independently verifies the final published assets. A local run, source-built test, queued/running CI job, or intermediate artifact is not a release claim.

The immediate local sequencing gate was build scaffolding and is now resolved for production admission on the corrected working tree. The authorized slice reproduced the OpenJDK 21 `testClasses` RED (SHA-256 `72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`), changed only `modules/refactorkit-kotlin/build.gradle.kts` by deferring `testRuntimeClasspath` resolution into `tasks.test.doFirst`, and passed the GREEN rerun (SHA-256 `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`). Full OpenJDK 21 `check goldenTest` then passed 82/82 tasks and 1,062 tests with 228 skipped and zero failures/errors (log SHA-256 `48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). This is bounded source-built scaffold evidence, not product qualification. Actions run `30746237578` remains a separate failed historical-SHA observation; corrected-revision CI and all unqualified K5/T5/J1/I1 work remain open.

These rules refine capability admission, evidence, and release qualification inside existing building blocks. They add no actor, bounded context, deployable container, durable store, protocol version, or runtime relationship. They implement accepted ADR 0002/0004/0005/0008/0009 directions rather than selecting a new architecture. Consequently this scaffolding-only LATE reconciliation requires neither an ADR update nor a C4 change.

== Fundamental Decisions

[cols="1,3,2,1",options="header"]
|===
|Topic |Strategy |Evidence |Status

|Patch-first architecture
|RefactorKit-managed operations produce `PatchPlan` / `WorkspaceEdit` previews and are applied by the core patch engine. Managed writes use WAL-backed staged per-path atomic replacement; admitted native LSP edits remain client-applied outside this boundary, while recipes stage all eligible steps into one managed transaction. A review/audit result is not a planned change and must remain outside both managed-plan and client-edit admission.
|`modules/refactorkit-core`, `docs/patch-engine.md`, ADRs 0002/0004, `docs/transactionality-audit.md`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|implementation-informed for admitted plans and the bounded lexical-review admission path

|Language adapter boundary
|The core owns generic models and safety mechanics; language-specific intelligence lives behind `LanguageAdapter` implementations.
|`LanguageAdapter`, `JavaLanguageAdapter`, tree-sitter foundation.
|implementation-informed

|Implemented Java Refactoring Preview Command boundary
|Inside the Java Language Adapter, the sealed five-variant `JavaRefactoringPreviewCommand` hierarchy and fieldless `JavaRefactoringPreviewDispatcher` establish one canonical downstream path for exactly `renameClass`, `renameMember`, `moveSourceRoot`, single-file Java `organizeImports`, and `safeDelete`. An internal per-call invoker seam maps to the unchanged default planners. Daemon and MCP construct only those five commands after their existing parsing and required-field validation; the five generic `JavaLanguageAdapter.applyRefactoring(RefactoringRequest)` decoders preserve existing refusal/default behavior and converge on the same dispatcher. Every call supplies the exact current snapshot/adapter and returns the exact plan or escaping exception without state or side effects. `ManagedApplyInvoker` remains rejected because forwarding adds no invariant while absorbing policy would create a God service.
|`JavaRefactoringPreviewCommand.kt`, `JavaLanguageAdapter.kt`, `DaemonSession.kt`, and `McpSession.kt`; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001 through `004`; focused 8-case/94-step log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`; module-regression log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`; six-feature audit SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`; final 2026-08-01 whole-working-tree OpenJDK 21.0.11 `./gradlew --no-daemon --rerun-tasks check goldenTest` receipt: `BUILD SUCCESSFUL` in 6m 54s, 1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, 79 tasks, log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`; targeted affected-module static command passed, log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`; LATE ADR/C4 reviews required no change.
|implementation-informed for the exact bounded source-built/in-process implementation, public daemon/MCP session dispatch, and dated mutable source-built aggregate. Static classification is non-zero and reviewed/non-blocking: the full targeted log contains no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers; exactly 48 slice-local glue findings remain—Java-module 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface 24 (13 NP, 7 RCN, 4 BC). Dispatcher-method references are generated Kotlin nullability analysis at glue call sites, not production-method findings. Wider SpotBugs/PMD baselines remain non-blocking; aggregate static cleanliness and numerical coverage are not claimed. Packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification remains open

|Implemented behavior-preserving Workspace Snapshot Composition
|The duplicate daemon/MCP mixed-snapshot pipeline is extracted into one stateless, language-neutral core composition service. Per-invocation collaborators preserve the surface's current Java base scanner and optional Kotlin toolchain while daemon/MCP retain concrete Tree-sitter and TypeScript wiring. The service preserves exact empty-script base identity, normalized later-script collision precedence, path-string ordering, the three-field source overlay, one conditional TypeScript attachment, optional Kotlin attachment last, unchanged failures, determinism, and read-only behavior. It centralizes only composition policy: every refresh/watch/index/pending/audit/lease/semantic, mutation, protocol, and response decision stays with the surfaces or existing authorities. No new module is introduced, and CLI/LSP do not adopt this slice.
|`WorkspaceSnapshotComposer.kt`; source-inspected adoption in the private daemon/MCP `scanWorkspace` methods; implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`; missing-composer compile RED SHA-256 `d2876ad413703a3895999a9377f8b7c6a713578eb8156b652bb079c70f1a6d95`; final focused OpenJDK 21.0.11 acceptance 8/8 scenarios and 65/65 steps (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`, Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`); daemon/MCP 58/58 and 26/26 regression log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`; final 2026-08-01 official whole-working-tree OpenJDK 21.0.11 `check goldenTest` receipt: `BUILD SUCCESSFUL` in 7m 7s, 1029 discovered/809 executed/220 skipped or tag-filtered, zero failures/errors, 136 XML files, 78 tasks, log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`; delegated LATE ADR/C4 reviews.
|implementation-informed for the bounded source-built/in-process contract, source-inspected daemon/MCP adoption, module regressions, and dated mutable source-built aggregate. The aggregate log has no production finding attributable to the composer or daemon/MCP adoption; 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. No packaged, native, cross-platform, concurrent, crash-restart, numerical-coverage, or general-orchestration qualification

|Java-first MVP
|Java is the first semantic language target, with scanner, symbols, references, diagnostics, and Java refactoring planners.
|`refactorkit-java`, Java docs.
|implementation-informed

|Multiple local integration modes
|Expose the same deterministic capabilities through CLI, JSON-RPC daemon, LSP, MCP, and library use.
|Module layout, README, integration docs.
|implementation-informed

|Behavior-preserving bounded pending-plan retention
|Replace the duplicated daemon, LSP, and MCP access-order maps with one language-neutral core store generic over the exact retained surface payload. Each integration session owns its own instance and keeps admission, explicit discard, apply/refusal consumption, rollback, snapshot-refresh, initialization/project, and close policy outside the store. The shared primitive preserves the exact 128-entry access-order LRU and side-effect-free insertion/lookup/remove/clear mechanics only; it does not unify lifecycle behavior, protocol results, approval, diagnostics, apply, WAL, transactions, or rollback.
|`modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt` plus the adopting `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt` production paths; implemented-and-validated REQ-PENDING-PLAN-STORE-001/002; focused OpenJDK 21.0.11 log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`; subsequent 2026-07-31 whole-working-tree source-built verification reported 1002 discovered, 782 executed, 220 skipped/tag-filtered, and zero failures/errors (77 tasks; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`), while the non-blocking SpotBugs baseline had no `PendingPlanStore` findings; accepted ADRs 0002 and 0004.
|implementation-informed for bounded source-built/in-process behavior; packaged behavior, concurrency, general extraction, and numerical coverage qualification remain open

|Implemented behavior-preserving managed rollback orchestration
|One language-neutral `ManagedRollbackExecutor` inside core shares only raw-ID parsing, one pre-lock full-record lookup, caller-selected initial visibility, a generic surface-owned preflight guard, one public `PatchEngine.rollback` invocation, and typed outcome transport. `APPLIED_ONLY` preserves CLI/LSP/MCP initial absence semantics for non-`APPLIED` records; `JOURNAL_RECORD` preserves daemon access to any decodable ID-matching record so `PatchEngine` retains state/recovery authority. Every outcome after resolution carries the exact complete pre-lock record; lookup and rollback-call journal exceptions remain separate and preserve exact exception identity. `Refused` preserves diagnostic order and values through a detached unmodifiable snapshot, not mutable list identity. Mode choice, protocol projection, refresh/diagnostics, session and pending/audit lifecycle, authorization, WAL/recovery, and the rollback algorithm remain outside the component.
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 in `features/managed-rollback-executor-extraction.feature`; `ManagedRollbackExecutor.kt`; inspected `RefactorKitCli.kt`, `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt` adoption. A focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`), and detached unmodifiable ordered snapshotting corrected it. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical rollback-scoped whole-working-tree verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings remain non-zero but reviewed/non-blocking: seven core medium findings (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings) and 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC); the former three `Refused`-diagnostics EI/EI2 findings are eliminated. This dated mutable source-built evidence does not qualify packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority. ADRs 0002/0004, the earlier commit-`27bc8a34b14caf2fce9ae06e4ad5536e49a62c01` path inspection, and LLM Wiki concept `managed-rollback-executor` remain preserved decision/planning evidence.
|implementation-informed for the focused bounded source-built/in-process executor, historical rollback-scoped dated mutable whole-working-tree source-built aggregate, and inspected surface adoption only; no packaged, cross-platform, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or changed `PatchEngine` authority qualification

|Implemented behavior-preserving managed-apply diagnostics-gate selection
|The formerly identical diagnostics-gate routing expression from daemon `refactor.apply` and MCP `apply_refactoring` is implemented as one stateless `refactorkit-jvm` selector. Each apply passes the current Java/Kotlin adapters and a surface-owned external resolver. The selector returns one lazy `DiagnosticsGate` without running a provider, preserves original-plan metadata predicates, branch precedence, IDs, provider ordering, and external lookup-now/provider-later behavior, and leaves every under-lock diagnostics and mutation decision in `PatchEngine`. Its explicit non-null final-segment check preserves the earlier root-path `NullPointerException` and exact `.java`-matches/`.JAVA`-does-not-match behavior. CLI, LSP, recipes, testkit/direct library, post-success response diagnostics, preview/query paths, lifecycle, rendering, WAL, rollback, and recovery remain separate.
|`ManagedApplyDiagnosticsGateSelector.kt`; adopting `DaemonSession.refactorApply` and `McpSession.toolApplyRefactoring` call sites; `ManagedApplyDiagnosticsGateSelectorCucumberTest.kt` and steps; implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002. Final independent OpenJDK 21.0.11 source-built/in-process acceptance passed 11/11 scenarios and 92/92 steps with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`); daemon regression passed 58/58 and MCP regression passed 26/26. Final whole-working-tree source-built verification on the same OpenJDK used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. Static cleanup eliminated the initial one selector production NP finding; the final full log contains no `ManagedApplyDiagnosticsGateSelector.kt` or provider-function production finding. Twenty-three glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. The EARLY commit-`642c187` inspection and LLM Wiki concept remain historical planning evidence; accepted ADRs 0002/0004 are unchanged.
|implementation-informed for source-built/in-process selector behavior, source-inspected daemon/MCP adoption, daemon/MCP regression, and the dated whole-working-tree source-built aggregate; no packaged, native, cross-platform, concurrent, apply-authority, or numerical-coverage qualification

|AI-safe workflow
|Local agents should use symbol/context/refactoring tools and review previews rather than directly editing source files.
|`docs/ai-workflow.md`, `refactorkit-mcp`.
|implementation-informed

|Incremental compiler-backed Java analysis with classified evidence
|Proven Java slices use exact Eclipse JDT binding evidence; prototype operations currently retain explicit lexical behavior or conservative refusal when semantic evidence is unavailable or not clean. Stable promotion requires that every fallback be separately narrow and proven safe; otherwise the stable outcome is refusal. No lexical path may claim compiler-backed semantic authority. Class rename and safe delete use exact type-binding evidence when possible. Move class uses exact target-type binding to scope package/import/FQN edits to actual referencing files. Java annotation types are represented as first-class symbols by lexical and JDT indexing and reuse the existing type-level lookup, rename, move, safe-delete, recipe, and owner-validation paths. Annotation elements are represented as zero-parameter signed `METHOD` symbols; read-only lookup/reference queries resolve them, and clean signed member rename updates exact declarations and binding-matched named usages. An unambiguous `FIELD` selector `Owner#field` uses exact field-binding declaration/reference ranges when global JDT evidence is clean, with owner-field collision refusal taking precedence over semantic fallback; unclean or unresolved evidence retains explicit lexical member fallback. This does not imply semantic support for arbitrary unsigned methods. Organize imports uses per-file clean binding-use evidence to remove an exact import only when its imported declaration binding has no non-import use; unresolved files retain lexical cleanup behavior.
|ADR 0008; signed method and unambiguous field member-rename, class-rename, move-class, safe-delete, annotation-type, annotation-element, and organize-imports planners/analyzers and tests.
|implementation-informed

|Authoritative Java/Maven semantic environments
|For managed Java operations, RefactorKit—not Maven—is the definitive semantic diagnostics authority. The planned product-critical epic constructs the full declared reactor before module-result filtering, models independent main/test environments, consumes exact release-aware platform signatures, returns typed unavailability for incomplete affected environments, and qualifies classpath reactors before JPMS. Global source-set authority requires available affected environments; Maven builds remain supplementary acceptance only.
|ADR 0012; `docs/requirements/java-maven-authoritative-diagnostics.md`.
|confirmed architecture; planned, product-critical

|Target-scoped Maven move authority
|A qualified same-module/source-set Maven `moveClass` may use a narrower operation-specific authority mode without claiming global source-set completeness. Reactor graph authority is descriptor-closed per main/test projection: every active reactor POM is mandatory; every selected external node needs complete effective POM/parent/BOM/management/mediation/relocation closure; and each outgoing declaration has a hash-bound selected or exact scope/optional/exclusion-pruned result. A deterministically pruned external edge contributes no child node/subtree and triggers no child POM/JAR lookup on that path; another selected path restores those requirements. Selection precedes child version/repository/descriptor/binary lookup whenever downstream child facts cannot alter the result, while ambiguity and any missing descriptor on an included edge fail closed. Per-binary negative-presence and retained-diagnostic evidence is limited to the proven before/staged operation authority closure. Bounded `001`/`002` prove the complete-`AVAILABLE` and exact observer/candidate/residual strategies. Within an `OFFLINE_MISSING` closure, bounded `007`/`009`-`012` prove independently scoped missing-binary, scope, selector, ordinary-absence, and selected-descriptor behavior. Implemented-and-validated `008` closes six isolated evidence-phase failures: four complete preview uncertainties return immutable typed guidance; a root-declared missing active POM returns typed structural refusal; and an eligible `007` lease binds a non-managed `BOUND_OTHER` source as generic required-file evidence so one lock-held engine observation can return primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` before WAL. The semantic candidate-inventory hash is expected-only; expected and observed hashes describe required-file evidence, not an observed semantic candidate inventory. Caller-attested classpaths, Maven/plugin execution, settings credentials, implicit network, and lexical promotion remain forbidden.
|Accepted ADRs 0010 and 0012; `RPK-JAVA-MOVE-001..004` and `006..007`; `OperationAuthorityFileEvidence`, `DiagnosticDetails`, `JavaMoveClassStructuralRefusal`, `JavaMavenMoveClassAuthorityFailureCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`; dated Magrathea field evidence at `a48e3c06` remains separate.
|confirmed P0 architecture; all twelve bounded definitions and 22 expanded fixture cases implemented and validated; the exact Magrathea CLI request remains qualified only on Linux/JDK 21; one local Linux `amd64` unpacked-package REQ-001 process row is implementation-informed, while REQ-002..012 packaged execution, generalization, broader integration shapes, runtime-distribution claims, and every native CI ledger row remain open

|Implemented bounded Maven module-rename slice
|REQ-JAVA-MAVEN-MODULE-RENAME-001 narrows the first validated J1 row to direct library planning for one exact root child of one authoritative offline reactor. The first request changes directory `catalog-model` to `catalog-domain` and independently supplies `newArtifactId=catalog-domain`; omission of that argument preserves the old artifact and authorizes no coordinate rewrite. Effective-model evidence must map the child project's direct artifact literal, one direct root `<module>` literal, and every exact literal dependency artifact origin before lossless range edits are selected. Exact raw-POM element-text ranges are supplied only by maintained Woodstox 7.1.1 StAX2 location information with DTD processing, external general/parameter entities, and external DTD access disabled. No POM serialization or hand-written XML-tokenizer fallback is allowed; the dependency remains inside `refactorkit-java`. One ordered `WorkspaceEdit` modifies the moved POM before child-file renames, stages a complete effective reactor and diagnostics without build/plugin/network/credential activity, and returns a deterministic normalized preview. Explicit `PatchEngine` apply owns one WAL transaction and exact rollback. The planner is a stateless Java/Maven domain/application service over immutable POM/model evidence, not a new aggregate or store.
|`features/java-maven-module-rename.feature`; `docs/requirements/java-maven-ownership-migration.md`; accepted ADRs 0002/0010/0012; final OpenJDK 21.0.11 review manifest SHA-256 `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`; focused log/JSON SHA-256 `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`/`d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`; C4 topology unchanged.
|implementation-informed only for the exact direct-library fixture row; surfaces, recipes, packaged/native/cross-platform behavior, general projects, native CI, support-matrix promotion, and broad module rename/move remain excluded

|Implemented bounded authoritative snapshot diagnostics evaluation
|For selected model-changing edits, core staging produces exact file candidate `C1`, then one language-neutral application port returns one transient immutable authoritative `ProjectSnapshot` plus diagnostics for that same candidate. Construction deep-detaches every outer/nested collection in the snapshot/module/build-model graph, diagnostics, and `DiagnosticDetails`; fresh canonical recomputation over exposed snapshot state must retain construction-time identity. Core constrains the result shell, allowing only modules/classpath evidence/build models to be rehydrated, and independently validates `Module.root`/all module source roots and every `BuildModule.root` as workspace-contained. An explicit absolute external classpath entry remains admissible only when its current no-follow path/kind fingerprint is independently recomputed and exactly matches supplied policy-bound evidence; fabricated facts refuse before WAL. Provider authority includes neither arbitrary roots nor fabricated classpath evidence. Exact S0/C1/S1 identity, repeated post-provider under-lock workspace/lease validation, schema-v8 `S1.hash`, exact committed S1/diagnostics verification, and automatic-rollback S0/diagnostics verification preserve deterministic transaction authority without adding a durable service or language dependency. Existing diagnostics-only gates and hashes are compatibility invariants.
|Implemented-and-validated REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`; accepted ADRs 0002 and 0012; `docs/build-model.md`; `docs/transactionality-audit.md`; focused 16/16-scenario and 128/128-step evidence under OpenJDK 21.0.11.
|implementation-informed for the bounded application-port mode; required and used by the implemented direct-library module-rename row, without generalizing to other operations or support surfaces

|Java move-class plan/guidance handoff boundary
|The bounded Java adapter path uses a plan-free `JavaMoveClassRequestValidator` shared by `JavaMoveClassPlanner` and `JavaMoveClassOperationDispatcher`. The dispatcher is the direct Java move-class operation API and outcome boundary; it is not the generic `LanguageAdapter` `PatchPlan` API. Its disjoint outcomes are `Plan(JavaMoveClassPreview)`, `Guidance(JavaMoveClassReviewOnlyGuidance)`, and `LexicalReview(JavaMoveClassLexicalFallbackReviewEnvelope)`. `Plan` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence. `Guidance` carries REQ-003 plus the four bounded preview uncertainties from `008`; the `008` diagnostic-identity case is collected in the dispatcher's second stage after a non-lexical preview. The `008` structural case stays on the `Plan` arm as `PatchStatus.REFUSED` with result-owned `JavaMoveClassStructuralRefusal` and no edit, lease, approval, or apply identity. `LexicalReview` carries REQ-004 alone and projects an internal compatibility preview as `LEXICAL_FALLBACK_REVIEW`. No `008` case returns `LexicalReview`, and only a fresh rescan/new preview can produce a separate managed plan.
|ADRs 0002, 0008, 0010, and 0012; `JavaMoveClassRequestValidator`, `JavaMoveClassOperationDispatcher`, `JavaMoveClassReviewOnlyGuidance`, `JavaMoveClassStructuralRefusal`, `JavaMoveClassLexicalFallbackReviewEnvelope`, and the JSON projections; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/004/008.
|implementation-informed for bounded REQ-003 source-built library/CLI outcomes, five REQ-008 source-built library/CLI preview-time outcomes, the REQ-008 library/`PatchEngine` under-lock row, and REQ-004's bounded direct-dispatch/CLI/recipe/daemon-session/LSP-session/MCP-session paths; no generic, packaged, runtime, or native qualification

|Legacy lexical-fallback protocol admission
|Every legacy Java/Maven `moveClass` `LEXICAL_FALLBACK` now leaves `JavaMoveClassOperationDispatcher` as immutable schema-v1 `LEXICAL_FALLBACK_REVIEW` before CLI/protocol rendering, pending-plan registration, recipe aggregation, or LSP edit conversion. Canonical JSON hashing produces request, exact snapshot, hidden review-artifact, and sorted-evidence SHA-256 identities plus deterministic non-capability `lexical-review-<sha256>` `operationId`. The hidden review-artifact identity commits to internal compatibility file-operation paths, exact edit ranges, and replacement/create bytes; exposed residual guidance contains only bounded path/content-hash/risk records with `managedEdit=false`, `COMPLETE or TRUNCATED` record completeness, deterministic returned-category counts, and fixed next actions. Protocols expose no plan/edit/range/replacement/apply token. Java-owned LRU audit caches retain immutable envelopes only, enforce 128-entry/16,777,216-byte limits, and clear on relevant session/workspace lifecycle changes. The shared exactly-one `operationId`/`planId` parser makes known operations typed `evidence.insufficient` refusals and unknown/evicted identities `INVALID_PARAMS`; daemon/LSP preserve structured JSON-RPC error `data`, MCP returns structured tool error content, and recipes stop without an aggregate. The core compatibility-plan gate remains defense in depth. This envelope remains disjoint from REQ-003 `REVIEW_ONLY_GUIDANCE`; clean JDT behavior remains the existing `Plan` outcome.
|ADRs 0002, 0004, 0008, and 0012; `JavaMoveClassLexicalFallbackReview*`, `RefactoringApplyIdentity`, CLI/recipe/daemon/LSP/MCP handlers, and `JavaMavenMoveClassLexicalFallbackReviewCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|implementation-informed for the bounded source-built/in-process scenario; no packaged-process, runtime-distribution, native-platform, or general-authority qualification

|Framework risk awareness
|Spring, JPA/Jakarta Persistence, and Jackson annotations raise risk and warnings for selected Java refactorings.
|`JavaFrameworkDetector`, `docs/framework-awareness.md`.
|implementation-informed

|Multi-language path
|Provide structural, regex-backed multi-language outline/search/local rename now; leave full semantic support to native bindings or external LSP-backed adapters later.
|`docs/multi-language.md`, `refactorkit-tree-sitter`.
|implementation-informed / planned
|===

The `v0.7.x` completion refinements above fit the accepted decision set without changing an ADR body. ADR 0002 already prevents recipe or protocol translation from weakening `PatchEngine` admission; ADR 0004 already separates client-managed LSP edits from managed writes and keeps surface policy outside shared orchestration; ADR 0005 owns verifiable self-contained release assets; ADR 0008 requires evidence-classified semantic authority and conservative refusal; and ADR 0009 owns the evidence-gated multi-language roadmap. A later proposal that changes those ownership rules, creates a durable recipe/evidence service, versions the protocol, or adds a runtime participant would trigger ADR and C4 delegation.

== Bounded Contexts / Building Blocks

RefactorKit is not documented as domain-driven business bounded contexts, but its architecture has clear technical bounded contexts:

* Core Patch and Model Engine
* Java Language Adapter
* CLI Application
* JSON-RPC Daemon
* LSP Server
* MCP Server
* External Java Class Importer
* Multi-language Structural Foundation
* Test Harness

The implemented Workspace Snapshot Composition service remains an internal responsibility of the existing Core Patch and Model Engine. It creates neither a new bounded context nor a new module. The implemented Java Refactoring Preview Command boundary likewise remains an internal C3/code-scale responsibility of the existing Java Language Adapter; it creates no new bounded context, module, runtime participant, or persistence boundary. The implemented bounded Maven module-rename planner has the same placement at C3/code scale inside the Java Language Adapter and reuses the existing Core Patch/WAL boundary; it adds no bounded context, module, participant, relationship, or store.

The enclosing containers/building blocks are shown in `docs/c4/workspace.dsl`; the promoted internal slice changes no model element or relationship, so C4 remains unchanged.

== Technology Strategy

[cols="1,2,2,1",options="header"]
|===
|Area |Choice |Reason |Status

|Implementation language
|Kotlin/JVM
|Good fit for JVM-native Java tooling and immutable/sealed model types.
|implementation-informed

|Build system
|Gradle Kotlin DSL multi-module project
|Coordinates modules, tests, applications, and jlink packaging.
|implementation-informed

|Serialization
|kotlinx.serialization JSON in daemon/LSP/MCP modules
|Structured protocol handling.
|implementation-informed

|Packaging
|jlink-generated embedded runtime plus checksum, SBOM, provenance/attestation, and reproducibility evidence
|Avoid requiring users to install Java globally and make promoted release assets independently verifiable.
|implementation-informed; `v0.3.0` runtime/checksum smoke evidence recorded, stable evidence planned

|Java platform evidence
|Immutable, licensed, integrity-verified and hash-attested release-specific Java SE signature catalogue, distributed or securely resolved independently of the host JDK and reduced jlink runtime
|Make Maven `--release 8..25` API diagnostics exact and reproducible.
|confirmed architecture; planned, product-critical

|Raw POM range parsing
|Maintained Woodstox 7.1.1 StAX2 location information, with DTD processing, external general/parameter entities, and external DTD access disabled; dependency bounded to `refactorkit-java`
|Provide exact hash-bound element-text ranges without POM serialization, operation-owned hand-written XML tokenization, build execution, settings, or network, while keeping core XML-library independent.
|implementation-informed for the bounded Maven module-rename row; Woodstox JAR SHA-256 `02b9d022e9d47704ff8a7a859a0dbfd3b2882a8311eb7ff1e180f760ccda2712`; no general XML/Maven/support qualification

|Architecture model
|Structurizr DSL C4 model
|Docs-as-code architecture model suitable for system/context and container views.
|implementation-informed
|===

== Links to Decisions

Chapter 9 indexes the repository ADRs. ADRs 0002, 0004, 0005, 0006, and 0008 jointly record the stable safety, integration-boundary, packaging, least-privilege, and compiler-backed Java strategies. ADRs 0010 and 0011 define effective Build Model and non-executable/credential-free provider boundaries. ADR 0010 cross-references ADR 0012's narrow operation-specific refinement without changing offline, typed-root, plugin-free, credential-free, or network-default policy. Accepted ADR 0012 makes its full-reactor phrase precise: descriptor closure is over selected nodes and hash-bound edge classifications, stopping below a deterministic scope/optional/exclusion-pruned external edge on that path but resuming on any alternate selected path. Missing included descriptors still block. Subsequent bounded evidence does not change the ADR body: REQ-JAVA-MAVEN-MOVE-AUTH-001 validates a complete-`AVAILABLE`, binding-clean preview/apply/rollback row with two deterministic previews, exact staged diagnostics and post-image, one transaction, and exact rollback; `002` validates exact direct/transitive/test-only observer selection, JDT-only 12/1/0 target/other/unresolved candidate totals, separate non-managed one-Java-occurrence/two-non-Java-path residual reporting, unchanged exclusions, one exact apply, and exact rollback; `010` validates deterministic `PRUNED` records and lookup ordering plus alternate selected-path missing-POM refusal; `011` validates one fixed ordinary local-repository selected-`jar` leaf with sidecar-free absent-binary evidence and three under-lock drift refusals; and six-case `012` validates selected-descriptor structural refusals before semantic selection, lock, and WAL. Six-case `008` validates four non-lexical guidance outcomes, one result-owned active-POM structural refusal, and one under-lock required-file-evidence refusal with honest expected-only semantic inventory vocabulary. These implementation records update ADR 0012 evidence/mappings without changing its accepted decision. Separately, a source-built CLI qualified the exact Magrathea request at commit `a48e3c06` on Linux/JDK 21 after `010`, with `JDT_BINDING`, four affected files, one managed transaction, and exact source-tree hash restoration after rollback. That request-specific field result did not depend on `011` or `012`; none of the bounded fixture rows expands integration/native qualification or establishes general support.

The implemented lexical-review envelope and bounded REQ-008 refusal mechanics introduce no new person, external system, deployable container, durable store, or relationship. They are implementation consequences of ADR 0002's managed-plan/under-lock gate, ADR 0004's surface ownership, ADR 0008's evidence classification, and ADR 0012's fail-closed authority rules. The generic immutable required-file evidence and diagnostic-detail values remain core model data, not new runtime units. ADR 0012's evidence/mapping section is reconciled without changing its accepted decision; no C4 view change is required.

The implemented pending-plan extraction realizes ADR 0004's existing shared-core/surface-policy split without changing a protocol, authority rule, deployable unit, external relationship, write boundary, or durable store. No new ADR or C4 view is needed for this bounded implementation refactoring. Centralizing lifecycle policy, apply/rollback orchestration, authorization, or persistence would be a separate architectural decision and remains explicitly outside this slice. Current evidence is source-built and in-process only; it does not qualify packaged execution, concurrent access, or a general service-extraction pattern.

The implemented `ManagedRollbackExecutor` is likewise a bounded internal realization of ADR 0002's authoritative `PatchEngine.rollback` boundary and ADR 0004's shared-core/surface-policy split. REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 and all eight cases are now `@implemented-and-validated`; production evidence is the core executor plus inspected CLI/daemon/LSP/MCP adoption, and final focused source-built/in-process evidence is bounded by log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`, after RED log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724` proved the mutable-diagnostics alias. The extraction adds no protocol, actor, external system, deployable container, deployment node, durable store, or relationship, and it does not change mutation authority, mode policy, WAL/recovery, or the rollback algorithm. Delegated late review of ADRs 0002/0004/0006 and validation of `docs/c4/workspace.dsl` found no implementation contradiction, so no ADR body, new ADR, or C4 model change is justified. Revisit that conclusion if later work centralizes protocol/lifecycle policy, invents authorization, alters initial record visibility, replaces recovery or the journal, or changes rollback semantics. Current qualification remains bounded source-built/in-process and aggregate whole-working-tree evidence plus inspected-surface adoption only; packaged, cross-platform, concurrent rollback, crash/restart, general rollback authority, and numerical coverage claims remain open.

The implemented Workspace Snapshot Composition extraction is an internal realization of ADR 0004's shared-core/surface-policy split, ADR 0009's language-neutral multi-language direction, and ADR 0010's core Build Model boundary. ADRs 0002 and 0006 remain unaffected because the slice is read-only and changes neither mutation authority nor MCP capability/trust policy. LATE `adr-writer` review found no new decision or body update; LATE `c4model` review validated `docs/c4/workspace.dsl` unchanged because no actor, container, deployment, store, participant, topology, or relationship changed. The existing daemon/MCP-to-core in-process relationships already cover adoption. Therefore no new ADR, ADR update, C4 element, or C4 relationship is justified. Reassess only if composition gains retained state, persistence, lifecycle/concurrency authority, concrete core-to-adapter dependencies, another surface, a new module, protocol behavior, or writes.

For the implemented Java Refactoring Preview Command boundary, delegated LATE `adr-writer` review confirmed that accepted ADR 0004 already owns shared language-adapter delegation, ADR 0002 remains the unchanged plan/mutation safety authority, and ADR 0006 gains no MCP capability or privilege; no ADR body or index update is justified. Delegated LATE `c4model` review validated `docs/c4/workspace.dsl` unchanged: the sealed commands, fieldless dispatcher, internal invoker seam, generic-route convergence, and daemon/MCP adoption remain internal Java-adapter dispatch covered by existing relationships. External protocol schemas/capability catalogues are unchanged. Reassess only if later work adds state, policy, persistence, a participant, a deployable boundary, a protocol capability, or a runtime relationship.

For the implemented local packaged-process REQ-001 validation mode, the earlier delegated reviews remain applicable: ADRs 0002, 0004, 0005, 0010, and 0012 already own managed safety, surface policy, jlink packaging, Build Model, and Maven move authority, while the existing Automation/CI-to-CLI and CLI-to-core/Java-adapter relationships already cover the process. The implementation adds only dedicated test glue, process harnessing, Gradle task wiring, and CI configuration; it changes no external protocol or product semantics and introduces no shipped artifact, product store/participant, mutation authority, deployment target, or runtime relationship. Therefore no ADR body/index or C4 artifact changed in this reconciliation. Reassess only if those boundaries change.

For the newly promoted authoritative-evaluation and direct-child module-rename rows, accepted ADRs 0002 and 0012 now record the implementation consequences without changing their Accepted lifecycle. The core value/port and Java planner remain internal responsibilities inside existing containers and relationships. Independent OpenJDK 21.0.11 review returned `PASS_FOR_PROMOTION`; authoritative evaluation passed 16/16 scenarios and 128/128 steps, and module rename passed 1/1 scenario and 50/50 steps. Exactly three parser-authorized POM ranges plus two file renames are in scope. This does not qualify broad module rename/move, protocol surfaces, recipes, packaged/native/cross-platform execution, general Maven projects, native CI, or release-wide support, and it requires no C4 change.
