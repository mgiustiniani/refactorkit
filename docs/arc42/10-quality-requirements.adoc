= 10. Quality Requirements
:toc:
:sectnums:

Completion-program status: confirmed `v0.7.x` quality scenarios are planned or otherwise open. The local all-module source/test compilation admission gate is resolved by bounded scaffolding evidence; corrected-revision CI and all unqualified product/release scenarios remain open, and no production completion status is promoted.

Status: mixed: implementation-informed for the separately bounded implemented slices, including managed-apply diagnostics-gate selection, Workspace Snapshot Composition, the Java Refactoring Preview Command boundary, authoritative snapshot diagnostics evaluation, the direct-child Maven module-rename row, and its exact lazy-authoritative selector/apply/rollback route. The atomic CATALOG-001/CATALOG-002/RESULT-001 Kotlin CLI revision now has all three requirements `@implemented-and-validated` within separately bounded source-built evidence. RESULT-001's final post-re-promotion run passed one scenario/30 steps after the corrected status-neutral integrity run; broader quality targets and qualification remain explicitly open. All twelve bounded Maven move-class definitions and all 22 expanded cases are implemented and validated. REQ-JAVA-MAVEN-MOVE-AUTH-001 through `007` and `009` through `012` retain their independently bounded evidence. Six-case `008` proves four immutable schema-v1 non-managed guidance outcomes, one typed active-reactor-POM structural refusal, and one under-lock required-file-evidence drift refusal. Focused OpenJDK 21 evidence passed six selected cases, skipped sixteen by tag filter, and recorded zero failures/errors. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Generalization, broader integration shapes, REQ-002..012 packaged execution, runtime-distribution qualification, and the native CI matrix remain open. Existing REQ-001 now also has one implementation-informed local Linux `amd64` unpacked-package process observation on the mutable tree; all four native CI ledger rows remain `CONFIGURED_UNOBSERVED`. The exact Magrathea request at `a48e3c06` remains separately CLI/Linux/JDK-21-specific. Other priorities and measurable targets need review. The behavior-preserving bounded pending-plan store extraction is implementation-informed: REQ-PENDING-PLAN-STORE-001/002 passed two source-built/in-process scenarios and 19 steps on OpenJDK 21.0.11 with 29 executed Gradle tasks and zero failures/errors (log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`). The historical PendingPlanStore-scoped whole-working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`. SpotBugs retained its configured non-blocking baseline and reported no `PendingPlanStore` findings. This later scoped receipt is dated mutable source-built aggregate evidence; no packaged, concurrency, general extraction, or numerical coverage qualification is inferred. Separately, REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 and all eight expanded cases in `features/managed-rollback-executor-extraction.feature` are `@implemented-and-validated`. A focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`); `Refused` now retains construction-time diagnostic order and values through detached unmodifiable snapshotting. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical three-feature final audit reported 16 unique requirement definitions, 32 cases, and 587 expanded steps, all implemented-and-validated, with zero errors (log SHA-256 `f4f9fd559ef9220c145017a2723fc4b8e888424448a392a11d20c1dc33d379c0`). The preceding official four-feature audit records 18 unique requirement definitions, 43 cases, and 679 expanded steps, all `@implemented-and-validated`, with zero errors (log SHA-256 `fb7c81a1d3223e6959d6838ac38c5c737f1bf8804570489405ed59bc1a279bc7`). The preceding official five-feature audit recorded 23 unique definitions, 51 cases, 340 declared steps, and 744 expanded steps, all implemented-and-validated with zero parse/tag/status/duplicate-ID errors (log SHA-256 `b40dc349e7d500b8d5068c15869c4908fb89c68519041e0006c60e2fd9b58867`). REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 and all 11 selector cases passed final independent OpenJDK 21.0.11 source-built/in-process acceptance: 11/11 scenarios and 92/92 steps with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`); daemon regression passed 58/58 and MCP regression passed 26/26. Final whole-working-tree source-built verification on the same OpenJDK used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. Static cleanup eliminated the selector's initial one production NP finding through the explicit non-null final-segment requirement while preserving the root-path `NullPointerException` and exact `.java`-matches/`.JAVA`-does-not-match behavior. The final full log contains no production finding for `ManagedApplyDiagnosticsGateSelector.kt` or its provider functions. Twenty-three glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. The earlier four-feature audit that recorded the selector as `@absent` remains historical pre-promotion evidence (log SHA-256 `57a48fbdf31f91e2ab0d54443849225ba51a4349cc8be84236f62210adc90c14`). Selector qualification is limited to source-built/in-process behavior, daemon/MCP regression, and dated source-built aggregate verification: CLI/LSP/recipes/testkit/direct library remain excluded, `PatchEngine` authority is unchanged, and no packaged, native, cross-platform, concurrent, apply-authority, or numerical-coverage claim is made. Historical pre-selector whole-working-tree verification after the managed-rollback correction used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). The earlier REQ-008- and PendingPlanStore-specific whole-working-tree receipts remain historical evidence for their explicitly stated scopes. That historical pre-selector receipt is dated mutable source-built aggregate evidence and does not qualify the selector or packaged/cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority.

For the final SURFACE-002 whole-tree regression reconciliation, the immutable baseline is `REQ002-WHOLE-TREE-REGRESSION-001` SHA-256 `387520937b1fc2922b146652ed18b545b508cd33d95189806178d3b5fb91bbfe`. Repository-wide and repeated focused RED logs (SHA-256 `8076201a19fad44076b810b7555859606a5717ff03cff97037fe392fcff10914` / `13497ab251fff223dcb8c490fd96c09c8ef1a09a81aad70dfa732378e9059b04`) remain historical proof of `authorityObservationRequested=false` after the child-process/socket observation step was removed from the shared Background for SURFACE-003 and not restored locally for SURFACE-002. The feature-only correction restored the exact existing step inside SURFACE-002; independent requirements review returned `PASS` and authorized `@partial` to `@implemented-and-validated`. The status-only promotion preserved the scenario-local step and produced current feature SHA-256 `1c29ad27146f06072c9815a1e056e2702cd25b8975ea2f0307385091e9859ad3`. Final post-promotion focused evidence at `docs/requirements/evidence/req002-post-promotion-focused-green-aee52f1114259413d6d6c77d384c6b30ed5afd82ab6b8c3d92921eb4bea367a2.log` was `BUILD SUCCESSFUL`; Cucumber JSON SHA-256 `4db1de3fca326ea9067a0d6653570c5fcf090a266e142cb9d27c854afd191b97` records one case and 34/34 expanded results: 29 scenario plus five Background steps. Final whole-tree JDK 21 evidence at `docs/requirements/evidence/req002-post-promotion-whole-tree-final-34b45bc447938de5bc5ce2c6351bdb7feda4e4403642e6add9d3e31cd7a044b0.log` was `BUILD SUCCESSFUL` in 12m 47s with 87/87 tasks; 155 standard-test XML files reconcile 1,096 discovered, 844 executed, 252 skipped/tag-filtered, and zero failures/errors. Static analysis remains configured non-blocking and emitted findings; neither clean static analysis nor numerical coverage is claimed. Earlier RED and pre-promotion GREEN receipts remain historical for their exact revisions.

For Workspace Snapshot Composition, final official whole-working-tree verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 7s; 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors; 136 XML files; 78 tasks executed; log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`. The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption. Static findings are not zero: 21 slice-local glue-only Kotlin/generated SpotBugs findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. Together with the independently retained focused acceptance, daemon/MCP regressions, and official Gherkin audit, this is dated mutable source-built/in-process and aggregate evidence only; it does not qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior. The preceding selector-scoped whole-tree receipt remains historical evidence for its scope.

The Java Refactoring Preview Command boundary is implementation-informed for a bounded source-built/in-process slice. A pre-implementation baseline passed Java 245/245, daemon 58/58, and MCP 26/26 (log SHA-256 `46a52ccf5fb116f5f52c7b116947ade4ff8faceea0d2fa999c330cc5cc4f1560`), and a meaningful absent-API compile RED has SHA-256 `3221bc7bc6aaef1a21117032ce6f22ea7c28ea8efb05a2797e2fec1beb3b5af8`. The first independent review correctly blocked promotion because REQ-003 had only source inspection; the corrected split runners then passed seven Java scenarios/85 steps plus one real `DaemonSession`/`McpSession` surface scenario/9 steps, all four definitions/eight cases/94 steps (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`; JSON SHA-256 `2059c28d57fde8f72b2e3f0de9fe6d95062c8de6cad40aeaeee3e4175a378ef6` and `999ee1a13d8577113113236e7b2241a5fac881e97327705eb1e4d1858ed83baa`). Independent Java/daemon/MCP regressions reported 253 discovered/252 executed/one tag-filtered, 58/58, and 26/26 with zero failures/errors (log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`). The official six-feature audit reconciled 27 definitions/59 cases/386 declared steps/838 expanded steps with zero errors (SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`). Final dated mutable whole-working-tree source-built verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 6m 54s; 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors; 140 XML files; 79 tasks executed; log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`. The targeted affected-module static command passed (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Static classification is honestly non-zero: the full targeted log contains no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers; exactly 48 reviewed/non-blocking slice-local glue findings remain—Java-module command glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN, 4 BC). Dispatcher-method references are generated Kotlin nullability analysis at glue call sites, not production-method findings. Wider configured SpotBugs/PMD baselines remain non-blocking; aggregate static cleanliness and numerical coverage are not claimed. This final receipt is dated mutable source-built aggregate evidence complementing the focused source-built/in-process evidence; it does not qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior.

REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` are
`@implemented-and-validated`, contributing four functional definitions/16 cases.
Independent OpenJDK 21.0.11 acceptance passed 16/16 scenarios and 128/128 steps
(log SHA-256
`bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`;
JSON SHA-256
`c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`).
REQ-JAVA-MAVEN-MODULE-RENAME-001 is also `@implemented-and-validated` for its one
functional 50-step direct-library case (log/JSON SHA-256
`b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`/
`d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`).
Final review returned `PASS_FOR_PROMOTION`; its manifest and summary SHA-256 are
`6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1` and
`76a2d8d508fbec9786abfb30bc25a0953f41cf6c163a0508e25433a6530d3b02`.
Corroborating final evidence passed the first core run 117/117, selected Maven
editor/scanner/planner regressions 52/52, and the managed selector 11 scenarios/
92 steps. Target `StageFacts`, `GateExpectation`, `ReactorView`, `ModuleView`, and
`SourceSetView` have zero raw/real EI/EI2 findings; the one evaluation EI warning
is non-real over a defensively copied unmodifiable list and deep mutation probes
passed. PMD was `NO-SOURCE` for Kotlin rather than clean, and the wider configured
SpotBugs baseline remains non-blocking. Woodstox 7.1.1 is scoped only to
`refactorkit-java`; exact-offset and DTD/external-DTD/entity denial passed, and
the reviewed JAR SHA-256 is
`02b9d022e9d47704ff8a7a859a0dbfd3b2882a8311eb7ff1e180f760ccda2712`.
These are bounded requirement-evidence promotions, not release-wide, protocol,
packaged/native/cross-platform, general-Maven, native-CI, or support-matrix
qualification.

REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001 is separately
`@implemented-and-validated` for exact source-built selector/apply/normal-rollback
behavior. Only exact raw `java.renameMavenModule` selects the operation-owned
`java-rename-maven-module-staged-reactor-v1` gate. Language-neutral
`DiagnosticsGate.lazyAuthoritative` defers construction, constructs the delegate
once with synchronized laziness, validates exact ID and authoritative-provider
presence, and exposes no observer. The final official JDK 21 focused rerun passed
one scenario, all 25 scenario steps plus six Background steps (31 expanded), and
two hooks, with no failures, skips, or undefined results; the log SHA-256 is
`4e826737d94150bcebe0a38d3fee970e84339532f3fe4ef07189da2506bdedef` and the
Cucumber JSON SHA-256 is
`2cf337ab1f963b133222b941df0fd577b9518794e60d23dde81390ba6e760a4c`.
Independent review returned `PASS_FOR_PROMOTION`; existing selector 11/92,
direct module rename 1/50, and focused core/Java/daemon/MCP regressions passed.
The subsequent required `./gradlew --no-daemon --rerun-tasks check goldenTest`
gate is authoritative for the promoted tree: `BUILD SUCCESSFUL` in 7m 31s with
82/82 tasks; standard `test` XML only, excluding the intentionally separate stale
packaged Maven task, reconciles 146 files, 1,068 discovered, 835 executed, 233
skipped/tag-filtered, and zero failures/errors (log SHA-256
`663ddcf3cb836fa2058d65160b57df19202b2f1908a0331faeb96ee6ca66209d`; XML
manifest SHA-256
`bca4e1f718f56ce3095ef9168267b42506f467ec5b1046408e764b1948502905`).
Static analysis remains configured non-blocking, and no aggregate numerical
coverage is claimed. Earlier RED, implementation, and focused receipts remain
historical. No public preview route, recipe,
packaged/native/cross-platform/general-Maven, release, or broad support status
follows.

== Quality Tree

* Safety
** Preview before apply
** Reject stale or unsafe edits
** Compensating rollback after a completely successful managed apply
** Implemented managed-write recovery/isolation/durability and versioned conflict-safe rollback; destructive acceptance evidence remains
** Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002: behavior-preserving managed rollback orchestration with one outer full-record lookup, explicit per-surface visibility, one engine call, exact pre-lock record retention, and distinct journal failures
** Warnings for high-risk Java/framework/import cases
* Correctness
** Symbol-aware Java operations where supported
** Conservative refusal instead of guessing
** Implemented-and-validated exact five-route Java preview-command parity across the generic Java adapter and bounded daemon/MCP public-session dispatch, with unchanged planner output/exceptions and no command-boundary side effects; qualification remains source-built/in-process
** Diagnostics before/after preview where supported
** Implemented-and-validated behavior-preserving daemon/MCP managed-apply gate selection with exact metadata precedence, lazy providers, stable pass-through ordering, and unchanged `PatchEngine` authority; qualification is source-built/in-process selector behavior, daemon/MCP regression, and dated whole-working-tree source-built aggregate evidence only
* Java/Maven diagnostics authority
** RefactorKit semantic evidence is definitive; Maven builds are supplementary
** Exact release-aware Java platforms and full-reactor source-set environments
** Typed unavailability and bounded cascade suppression instead of fabricated errors
** Default no-introduced-errors and optional strict-zero staged policy
* P0 Maven move-class authority
** Every active reactor POM remains required; each main/test projection is descriptor-closed over selected external nodes, and every outgoing declaration has a hash-bound selected or exact scope/optional/exclusion-pruned result
** A deterministic pruned edge contributes no child/subtree and causes no child POM/JAR lookup on that path; an alternate selected path restores descriptor/binary requirements, while ambiguity or a missing included descriptor blocks
** Outside-closure `OFFLINE_MISSING` remains visible without provided-type or per-binary operation inventory only after complete graph and candidate exclusion are proved
** Candidate-total `BOUND_TARGET` / `BOUND_OTHER` / `UNRESOLVED` JDT classification; target-relevant uncertainty blocks and only exact target bindings select edits
** Implemented bounded RefactorKit-owned ordinary evidence covers one fixed-release simple local Maven-2-layout selected `jar` graph leaf with complete present POM closure and an absent binary, sidecar-free no-follow absence, no invented absent-content hash or provided-type inventory, exact source binding, and target-name lookup protection
** Exact unchanged mapped identity and positive non-concealment proof for every retained closure diagnostic
** No new or changed staged error and no structural, artifact, candidate, provider, or diagnostic evidence drift
** Review-only lexical evidence remains veto-only and pre-lock/pre-WAL ineligible
** Implemented immutable Java-specific `JavaMoveClassReviewOnlyGuidance` is disjoint from the existing plan arm and has no plan conversion; this is not a generic cross-surface result algebra
** Implemented `008` is phase-correct: four complete preview uncertainties yield schema-v1 typed guidance, one globally declared active-reactor-POM loss yields typed structural preview refusal, and one non-managed candidate-source required-file-evidence drift yields under-lock pre-WAL refusal; only the first five require pre-lock/no-residue behavior
** Implemented legacy lexical fallback crosses the bounded direct-dispatch/CLI/recipe/daemon-session/managed-LSP-session/MCP-session paths only as deterministic schema-v1 `LEXICAL_FALLBACK_REVIEW`, with `NOT_SEMANTICALLY_PROVEN` / `REVIEW_ONLY` / `INELIGIBLE` / `evidence.insufficient`, bounded path/hash/risk residual review, and no protocol plan ID, edit/range/replacement/apply token, pending plan, LSP edit, recipe aggregate, or transaction
** A known lexical-review `operationId` is an audit correlation identity, not an apply capability; unknown `PlanId` stays invalid input
** Permanent 20-active-module fixture created; all twelve requirement definitions and 22 expanded cases, including corrected two-phase six-case `008`, are implemented and validated; generalization, broader integration shapes, REQ-002..012 packaged execution, runtime-distribution qualification, and the native CI matrix remain open, while the exact Magrathea CLI field request remains separately bounded
** Implemented local packaged-process validation mode for existing REQ-001: dedicated task, real unpacked launcher and embedded runtime, isolated fixture copy, scrubbed subject environment and poison-`PATH` proof, 16 bounded commands, repeat semantic preview, exact human-text lock refusal before WAL, one schema-v8 transaction with four `Modify` plus one `Rename` and nested `TextEdit` counts `1/2/1/1`, post-apply diagnostics, exact normal rollback, bounded cleanup, and no packaging dependency from ordinary `test`/`check`; one Linux `amd64` mutable-tree run is observed, while all native CI rows remain `CONFIGURED_UNOBSERVED`
* Implemented bounded Maven module rename
** Direct-library-only exact root-child directory rename with independently explicit artifact intent; effective-model-proven literal POM origins; exactly three raw element-text range edits from maintained Woodstox 7.1.1 StAX2 location information followed by two file renames; DTD/external-entity/external-DTD denial; no POM serialization or hand-written tokenizer fallback; complete offline staged reactor/diagnostics; deterministic preview; one explicitly authorized `PatchEngine`/schema-v8 WAL transaction; and exact rollback. The parser dependency is bounded to `refactorkit-java`
** Implemented language-neutral authoritative snapshot-diagnostics evaluation: deep-detach every outer/nested snapshot, module, build-model, diagnostics, and `DiagnosticDetails` collection; preserve construction-time identity under fresh canonical recomputation; validate module/build roots in core; independently recompute and match current no-follow path/kind fingerprints for explicitly allowed external classpath evidence; refuse arbitrary roots or fabricated facts before WAL; preserve exact S0/C1/S1 identity; and use same-record automatic rollback to evaluated S0/D0 while current diagnostics-only gates/hashes remain exact
** The validated direct row uses only a no-follow disposable copy of the permanent 20-direct-child fixture and leaves nested/profile/property/inherited/ambiguous origins, other coordinate fields, JPMS, and broader project qualification open
** Implemented-and-validated exact managed-apply selection: only byte-exact `java.renameMavenModule` returns the operation-owned lazy authoritative gate; construction is synchronized and one-time, gate identity/provider are checked, no observer participates, real Java fallbacks remain exact for every non-match, and one retained-plan `PatchEngine` apply/normal rollback preserves S0/C1/S1 and one schema-v8 record
** Implemented-and-validated SURFACE-005 reuses that exact selector through the direct LSP-to-Shared-JVM dependency after dirty/affected-open preflight; generic `java-jdt` is forbidden for exact module-rename managed apply, while plan removal and refresh remain success-driven
** The bounded source-built/in-process evidence qualifies only public in-process `LspSession.dispatch`; it does not qualify any preview route, external transport/editor, recipe, packaged/native/platform/concurrent/crash/general-Maven behavior, release status, numerical coverage, or broad support
* Extensibility
** Language-independent core
** One generic core pending-plan retention primitive without moving surface lifecycle or language policy into core
** Language adapter contract
** Structural multi-language foundation and optional future LSP-backed adapters
* Usability
** CLI, daemon, LSP, MCP, and library integration modes
** Focused MCP resources and prompts for agents
* Versioned source-built CLI catalogue truthfulness
** Preserve exact closed v1 bytes/semantics under `commands --json --schema-version 1`; make exact closed v2 the unversioned default
** Admit only selector absence or exact version `1`; reject malformed/unsupported values without fallback; help shows `[--schema-version 1]`
** Keep create/move `catalog-only`; allow rename `preview-only` only in v2 and only for successful RESULT-001 preview
** Retain strict stdout/stderr bytes, deterministic output, capability separation, and zero workspace/plan/lock/WAL/transaction/edit/install effects
* Atomically coupled source-built Java CLI preview result
** Closed `refactorkit.cli-result/v1` successful preview for only `java rename-module ... --json`
** Canonical complete-edit plan digest/projection without internal identity or authority leakage
** Co-release with default catalogue v2 only after CATALOG-001 compatibility, CATALOG-002, and RESULT-001 each have meaningful RED and separate independent review
** RESULT-002 applied, `003` refusal, `004` usage/operational, `005` internal, and `006` closure/limits/redaction/truncation remain absent
* Portability and supply-chain integrity
** Java 8-compatible RefactorKit bytecode, separately verified from Java source support
** Self-contained CLI package with embedded runtime
** Checksum, SBOM, provenance/attestation, and reproducibility evidence
* Performance and resource control
** Measurable reference-workspace budgets
** Bounded requests, results, caches, pending plans, transaction metadata, memory, and execution time
** A fixed maximum of 128 pending plans per integration session with true access-order LRU and deterministic silent eviction
* `v0.7.x` completion truthfulness
** All-module source/test compilation is green before production completion work
** Kotlin standalone companion movement refuses; enclosing-type movement may carry it
** TypeScript T5 operations retain distinct typed compiler/server authority; dynamic/mixed JavaScript refuses when incomplete
** API `0.2` LSP module rename keeps client-managed/no-rollback ownership unless the explicit managed path is selected
** Recipes preserve exact child operation/evidence/gate authority
** Release reports, SBOMs, attestations, and final native assets reconcile to one immutable release evidence identity
* Testability
** Unit tests, golden tests, sample projects, agent simulation tests, CI

== Quality Scenarios

[cols="1,1,3,2,1",options="header"]
|===
|ID |Quality |Scenario |Evidence / validation |Status

|QS-SAFE-001
|Safety
|When a user applies a preview after the workspace has changed, RefactorKit refuses apply and asks for plan regeneration.
|`PatchEngine.validate` checks snapshot hash mismatch.
|implementation-informed

|QS-SAFE-002
|Safety
|When a patch contains overlapping text edits, RefactorKit refuses apply.
|`PatchEngine.validateNoOverlaps`; core tests.
|implementation-informed

|QS-SAFE-003
|Safety
|When a patch attempts to edit outside the workspace root, RefactorKit refuses it.
|`PatchEngine.validateInsideWorkspace`.
|implementation-informed

|QS-SAFE-004
|Safety
|When an external Java class import would overwrite an existing class/file, the importer refuses by default.
|`docs/external-class-importer.md`, importer tests.
|implementation-informed

|QS-SAFE-006
|Safety
|When any edit path contains a symbolic-link component below the workspace root, RefactorKit refuses it with `path.symbolicLink` before writing, even when the link resolves inside the workspace; ordinary path escapes continue to use `path.outsideWorkspace`.
|`PatchEngineTest.refusesSymbolicLinkTraversalWithoutWritingOutsideWorkspace`; `PatchEngine.validateNoSymbolicLinkTraversal`.
|implementation-informed

|QS-SAFE-007
|Safety
|Before the first write, RefactorKit simulates ordered create, modify, rename, and delete existence/type transitions. A later predictable missing-file or existing-file refusal leaves earlier edits unapplied, while a valid modify-then-rename sequence succeeds.
|`PatchEngineTest.preflightRefusalLeavesEarlierEditsUnapplied`, `PatchEngineTest.preflightSupportsSequentialModifyThenRename`; `PatchEngine.validateRuntimeState`.
|implementation-informed; interrupted I/O and TOCTOU races remain open

|QS-SAFE-008
|Safety and recoverability
|When a managed apply completes without interruption or concurrency, RefactorKit returns reverse compensation and the integration saves it afterward. The contract describes this as a preflighted compensatable batch and does not promise ACID, crash-safe, durable, or all-or-nothing execution.
|`docs/transactionality-audit.md`, ADR 0002.
|implementation-informed limitation

|QS-SAFE-009
|Data integrity
|If an affected file differs from its expected post-apply state, normal rollback refuses rather than overwriting the later edit and exposes conflict/manual-recovery information.
|Transactionality audit `TX-004`; future fault/concurrency acceptance suite.
|planned release blocker

|QS-SAFE-010
|Security and recovery
|Malformed transaction IDs, traversal, transaction-directory symlinks, truncated/corrupt journal records, or integrity failures produce structured refusal without reading, deleting, or overwriting data outside the protected journal boundary.
|Transactionality audit `TX-003`, `TX-011`, `TX-012`; future security/corruption suite.
|planned release blocker

|QS-SAFE-011
|Isolation and durability
|Under concurrent writers, I/O failure, process kill, power loss, disk full, permission changes, or filesystems without atomic move, managed apply either preserves the prior state or records a durable, deterministic recovery state and declared degraded guarantee.
|Transactionality audit `TX-001`, `TX-002`, `TX-005`, `TX-006`; future fault-injection/restart/filesystem suite.
|planned release blocker

|QS-SAFE-012
|Behavior preservation, safety, and traceability
|For any supplied rollback ID, the implemented core executor parses the raw value exactly. Invalid input performs no journal/guard/lock/engine work. A valid ID causes exactly one pre-lock `loadRecord`; CLI/LSP/MCP apply `APPLIED_ONLY`, daemon applies `JOURNAL_RECORD`, and no visible record short-circuits. A visible record reaches the surface guard before the engine lock and then at most one public `PatchEngine.rollback` call. Every post-resolution outcome carries that exact complete pre-lock record; lookup-time and call-time `TransactionLogException` outcomes remain distinct with unchanged exception identity/code/message/cause. Engine-applied identity is retained, while a refusal copies diagnostics into a detached unmodifiable snapshot preserving construction-time order and values even if the source list is later mutated. LSP preserves dirty-document-first then affected-open-document rejection; the other surfaces allow. Rendering, mode choice, refresh/index/diagnostics, pending/audit lifecycle, authorization, WAL/recovery, and rollback semantics remain outside the executor.
|Implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; `ManagedRollbackExecutorCucumberTest.kt`; `ManagedRollbackExecutorSteps.kt`; inspected CLI/daemon/LSP/MCP adoption; mutable-diagnostics RED log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`; final focused log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`; final official Gherkin audit log SHA-256 `f4f9fd559ef9220c145017a2723fc4b8e888424448a392a11d20c1dc33d379c0`; historical rollback-scoped whole-working-tree source-built log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96` (OpenJDK 21.0.11, `check goldenTest`, 1010 discovered/790 executed/220 skipped or tag-filtered, zero failures/errors, 77 tasks). Slice-local SpotBugs classification is reviewed/non-blocking and non-zero: seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings); 25 glue-only Kotlin/lifecycle/generated warnings remain (13 NP, 1 RCN, 5 MS, 6 BC); the former three `Refused`-diagnostics EI/EI2 findings are eliminated by detached unmodifiable ordered snapshotting. The EARLY architecture review of commit `27bc8a34b14caf2fce9ae06e4ad5536e49a62c01`, ADRs 0002/0004, and LLM Wiki concept `managed-rollback-executor` remain preserved planning evidence.
|implementation-informed for eight focused source-built/in-process scenarios, the historical rollback-scoped dated mutable whole-working-tree source-built aggregate, and inspected surface adoption; no packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority qualification

|QS-CORR-001
|Correctness
|When clean JDT analysis resolves the safe-delete target binding, RefactorKit refuses deletion for binding-matched references without conflating unrelated same-simple-name types in other packages. If JDT parse/classpath warnings or the exact target binding prevent semantic evidence, it uses the lexical scan and warns explicitly.
|`JavaSafeDeletePlannerTest`: imported target refusal with JDT evidence, unrelated same-simple-name type allowed, and lexical fallback warning.
|implementation-informed

|QS-CORR-002
|Correctness
|When extract-method selection has control-flow or variable-dependency ambiguity, planner refuses rather than guessing parameters/returns.
|`docs/extract-method.md`, tests.
|implementation-informed

|QS-CORR-003
|Correctness
|When change-signature would affect overloaded, generated, method-reference, public/protected hierarchy, or unsafe cases, planner refuses or escalates risk.
|`docs/change-signature.md`, tests.
|implementation-informed

|QS-CORR-004
|Correctness
|When clean JDT analysis resolves the exact class-rename target binding, RefactorKit renames the type declaration, constructors, binding-matched imports/type references/constructor calls, and source file without changing same-simple-name types in other packages. It refuses unchanged names and existing target symbol/file conflicts; unavailable clean binding evidence produces an explicit lexical-fallback warning.
|`JavaRenameClassPlannerTest`: constructor rename/apply, imports, same-simple-name disambiguation, target conflict refusal, and fallback warning.
|implementation-informed

|QS-CORR-005
|Correctness
|In the recorded implementation, clean JDT analysis resolves the move-class target binding and scopes package/import/FQN substitutions to binding-matched files while retaining package-declaration and file-move operations. REQ-JAVA-MAVEN-MOVE-AUTH-001 validates two identical complete-`AVAILABLE`/`JDT_BINDING`/`ELIGIBLE` previews over the exact `catalog-pricing:main`, `catalog-storefront:main`, and `catalog-acceptance:test` reverse-observer closure. Each has five top-level `FileEdit` entries—four `Modify` plus one `Rename`—across the old/new target paths and three observer files. The four modifies contain nested `TextEdit` counts `1/2/1/1`, five text edits total; no one-edit-per-modify invariant is claimed. Authoritative before and staged diagnostics are zero. Prohibited build-code, lifecycle/plugin/annotation-processor, settings/credential/helper, and network activity is denied. Apply commits the exact staged hash/files/auxiliary inputs/seven-source inventory in one lock-held managed transaction; after diagnostics remain zero; normal rollback restores all non-engine bytes, paths, seven source entries, and the original snapshot hash, and rollback diagnostics exactly equal the zero-entry baseline. REQ-JAVA-MAVEN-MOVE-AUTH-002 independently validates exact direct/transitive/test-only binding observer selection and keeps JDT candidate totals distinct from Java string-literal and non-Java residual review risks; five exact binding-derived text edits plus the declaration rename produce the exact staged image, and one managed transaction rolls back to the full pre-apply non-engine workspace state. REQ-JAVA-MAVEN-MOVE-AUTH-003 independently validates four safely enumerable non-managed guidance outcomes, typed JSON rendering, and the CLI refusal branch before `PatchEngine`; it does not broaden the semantic edit proof. REQ-JAVA-MAVEN-MOVE-AUTH-004 validates the distinct operation-specific lexical-review projection/refusal boundary and explicitly checks that a fresh clean full-reactor scan still returns the existing `SEMANTIC_PREVIEW`/`JDT_BINDING`/`ELIGIBLE` `Plan` outcome. Requirements `006`/`007`/`009`/`010`/`011`/`012` retain their independently bounded warning, missing-binary, scope, pruning, ordinary-absence, and selected-descriptor evidence. REQ-JAVA-MAVEN-MOVE-AUTH-008 independently validates four non-lexical guidance cases, one typed root-declared active-POM structural refusal, and one under-lock required-file-evidence refusal with primary `authorityLease.evidenceDrift`, secondary `snapshot.scopeChanged`, preserved external `S1`, and no WAL. `PatchEngine.apply` still rejects immutable lexical compatibility evidence before workspace-lock acquisition. Separately, the source-built CLI qualified only the exact Magrathea `a48e3c06` move on Linux/JDK 21 after `010`. Existing REQ-001 also has one local Linux `amd64` packaged-process observation through the real launcher and embedded runtime; neither result promotes a post-commit native CI ledger row or expands REQ-002..012/general authority.
|Tagged REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` Cucumber evidence, including `JavaMavenMoveClassAuthorityFailureCucumberTest`; focused REQ-008 OpenJDK-21 evidence discovered 22, passed six selected cases, skipped sixteen, and had zero failures/errors; independent log SHA-256 `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`. Historical whole-tree evidence remains bounded to its recorded tree. Official final OpenJDK 21.0.11/Linux `amd64` execution of `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks` reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit 22 discovered/one executed/21 tag-filtered/zero failures or errors; and one Cucumber scenario with five background plus fourteen scenario steps, 19/19 passed, and 2/2 hooks; packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The separately retained source-built REQ-001 review rerun passed 22/one/21/zero with log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`. On the same JDK, final `./gradlew --no-daemon --rerun-tasks check goldenTest` passed in 6m43s: standard tests 1045 discovered/817 executed/228 skipped or tag-filtered/zero failures or errors, 140 XML files, 82 tasks executed, log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`; the separately reported packaged task is intentionally not in those totals. Targeted custom-source-set PMD/SpotBugs tasks passed, log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`. Their static classification is intentionally non-zero: 99 reviewed/non-blocking packaged glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), all test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated representation and conservative single-thread harness concurrency warnings. No production source file was added or modified by this slice; the wider baseline remains non-blocking, and aggregate static cleanliness plus numerical/child-JVM coverage are not claimed. Dated Magrathea field observation at `a48e3c06` remains separate.
|Twelve validated definitions/22 bounded cases, one exact CLI field request, and one local Linux `amd64` REQ-001 packaged-process mode implementation-informed; REQ-002..012 packaged execution, general authority, broader integration shapes, and every native CI ledger row remain open

|QS-CORR-006
|Correctness
|For a file with clean JDT evidence, organize imports removes an exact import only when its declaration binding has no non-import use, including normalized generic type, method, and variable uses. It preserves wildcard imports. For an unresolved file, it retains lexical sorting, deduplication, and same-package removal without claiming semantic unused-import cleanup.
|`JavaOrganizeImportsPlannerTest`: used `List<Map<...>>` and generic static `emptyList` imports retained; unused `Set` and `singletonList` imports removed; wildcard preservation; unresolved-file lexical behavior; existing preview/apply/rollback coverage.
|implementation-informed

|QS-CORR-007
|Correctness
|When two annotation types share the simple name `Audited`, an exact rename of `com.acme.Audited` to `Tracked` changes its declaration, source file, and binding-matched usages while preserving `com.other.Audited` and its usage. Annotation types also remain available to read-only lookup and existing type-level move, safe-delete, recipe, and owner-validation paths.
|`JavaRenameClassPlannerTest`: exact annotation-type declaration/file/usage rename with same-simple-name package disambiguation; lexical and JDT annotation indexing/analyzer evidence.
|implementation-informed

|QS-CORR-008
|Correctness
|Given the signed annotation-element selector `com.acme.Route#path()`, read-only lookup/reference queries resolve the zero-parameter `METHOD` symbol and its binding-matched named usages. With clean semantic evidence, rename to `value` updates the exact declaration and `@Route(path=...)` name while preserving an unrelated `com.other.Route#path()` declaration and usage. Implicit single-element values contain no name to edit.
|`JavaRenameMemberPlannerTest`: annotation-element lookup/reference and applied signed rename with package-disambiguation evidence.
|implementation-informed

|QS-CORR-009
|Correctness
|Given an unambiguous `FIELD` selector `com.acme.Stats#count` and clean global JDT evidence, rename uses exact field-binding ranges so the declaration, `this.count`, and `stats.count` change while a shadowing local `count` and `com.other.Stats#count` remain unchanged. If the owner already declares the target field, the planner refuses before semantic fallback; unclean or unresolved evidence otherwise retains explicit lexical member fallback. This scenario does not cover arbitrary unsigned methods.
|`JavaRenameMemberPlannerTest`: exact field declaration/qualified-reference apply with local-shadow and other-owner preservation, owner-field target-collision refusal, and fallback behavior.
|implementation-informed

|QS-CORR-011
|Behavior preservation and correctness
|For each of `renameClass`, `renameMember`, `moveSourceRoot`, single-file Java `organizeImports`, and `safeDelete`, a complete Java preview command receives the caller's exact current `ProjectSnapshot` and `JavaLanguageAdapter`, invokes exactly one existing planner, returns the exact `PatchPlan`, and propagates an escaping exception unchanged without command-boundary state or side effects. Daemon/MCP construct the command only after existing surface parsing and required-field validation; generic `JavaLanguageAdapter.applyRefactoring(RefactoringRequest)` preserves its decoder/refusal behavior and converges on the same downstream planner path.
|Implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001 through `004`; `JavaRefactoringPreviewCommand.kt`; sentinel and real default-planner Java runner; real public `DaemonSession`/`McpSession` surface runner; focused 94/94-step log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`; module-regression log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`.
|implementation-informed for the exact bounded source-built/in-process implementation, final dated mutable source-built aggregate, and reviewed non-zero static classification; packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification remains open

|QS-AI-001
|AI safety
|A local LLM can get focused context through MCP without arbitrary filesystem access and request preview/refactoring tools.
|`docs/ai-workflow.md`, `docs/integration-contracts.md`.
|implementation-informed

|QS-API-001
|Interoperability
|Before opening a project, a daemon client can discover implementation/API identity, JSON-RPC stdio transport, method stability and project/write requirements, and advertised patch-safety properties without causing workspace access or mutation.
|`DaemonSessionTest.serverCapabilitiesDescribesMethodsAndSafetyWithoutOpenProject`; daemon contract documentation.
|implementation-informed

|QS-CLI-CATALOG-001
|Backward compatibility and closed negotiation
|After the atomic cutover, exact `commands --json --schema-version 1` must return the previously promoted v1 bytes and `catalog-only` semantics. Selector absence returns v2. Malformed, duplicate, missing-value, or unsupported selectors refuse without fallback but without a qualified result-error envelope or exit-code claim, and help advertises exactly `refactorkit commands --json [--schema-version 1]`.
|CATALOG-001 baseline/change SHA-256 `56485baf0ab096ad994e18f2fa29650bbf4bf2c2098d027ccd426c8a360d0009` / `9fee5e4230e9e00ef267ddf48b72ab133d3469cb8c9b2fa8cf5aa42e9af2024e`; current promoted feature SHA-256 `f02fc091db83e8b96909e4c596bda9702204c62f2874fdf1a0fd45a01e3970cf`; current mode-`0444` post-promotion log/JSON SHA-256 `5f31ba5a9162fb4673af2cffce95e1d32bc3da8648726aa23ecde3f1766e71e7` / `1c8397c4e697558c58e997286505594016cdbc827a9df1446b2c07ef4c49ba63` passed one scenario/22 steps from a standard JDK 21 parent with both caller Java option variables explicitly unset. Earlier pairs remain immutable historical evidence.
|implementation-informed and `@implemented-and-validated` for explicit-v1 compatibility within the bounded source-built qualification

|QS-CLI-CATALOG-002
|Correctness, determinism, and non-mutation
|Default `commands --json` emits exact closed v2. Only rename reports `preview-only`; create and move remain `catalog-only`. Repeated v1 and v2 invocations are byte-identical per version, emit one compact object plus LF with empty stderr, and create no workspace scan, plan, lock, WAL, transaction, edit, or installation interaction. `capabilities` remains byte- and schema-separate.
|CATALOG-002 baseline SHA-256 `df91e509e82c1a516f226887030702a78d0c41288ccdfe14b241942ef6fd2fea`; current promoted feature SHA-256 `f02fc091db83e8b96909e4c596bda9702204c62f2874fdf1a0fd45a01e3970cf`; current mode-`0444` post-promotion log/JSON SHA-256 `b561b3ebf0e32d7dca84081af0e48bca21dd57d8b392e99af18b4f99dcfd7e21` / `96f1e3825fc933baae06b76981a135a0aee7d79b6498382c43a36aca6fbb7464` passed one scenario/31 steps from a standard JDK 21 parent with `JAVA_TOOL_OPTIONS` and `JDK_JAVA_OPTIONS` explicitly unset. Shared launcher SHA-256 `081b01d28686118aaa4ab7e94b1e3f8e17b8b9c31b5d6a59b79c7ad99305bdc5` supplies explicit `-Djava.security.manager=allow` only to the bounded child and removes both inherited variables; scenario process-harness/steps SHA-256 remain `1ffe05b9e5c6418b3a206d4e113ea4e9ab5f72ae37f63d4046d5cac95406679e` / `d719adb927698dec637df43c7a626a60cdcdd22149686d5f3c4dc0e68af4e19d`. `testClasses` and the 12/12 CLI unit regression pass with caller variables unset; capabilities bytes remain SHA-256 `dec4087fa8b7ba038a5b9a82728ec724054ee6fd28456eb9ec637fd7d0d0d1c3`. Prior pairs remain immutable historical evidence.
|implementation-informed and `@implemented-and-validated` for default-v2, explicit-v1, rejection, determinism, non-mutation, and harness environment independence

|QS-CLI-CATALOG-003
|Atomic truthfulness
|The default v2 advertisement of rename `preview-only` and successful RESULT-001 preview enter production in one changeset. CATALOG-001 compatibility, CATALOG-002, and RESULT-001 each have meaningful RED and separate revision-bound independent review before any promotion; all promote together by status only and receive joint post-promotion verification.
|Approved CATALOG-001 change SHA-256 `9fee5e4230e9e00ef267ddf48b72ab133d3469cb8c9b2fa8cf5aa42e9af2024e`; CATALOG-002 baseline SHA-256 `df91e509e82c1a516f226887030702a78d0c41288ccdfe14b241942ef6fd2fea`; RESULT atomic correction SHA-256 `40a0183ec29584724a98e2357738078e7caa8d8356c90a7c0e4cd9930ff3e73e`; one atomic production revision is `RefactorKitCli.kt` SHA-256 `db6067bf3235191957203474da95f03c4399f4de47661af9a3dcb3ef65720246` plus `CliProtocolProjection.kt` SHA-256 `ac65359ed95dd09efaa6820b05a7c7d0c18a3b4495cc3e4f738f76d19149b50d`.
|implementation-informed: the atomic production gate is satisfied; both catalogue requirements retain post-promotion GREEN, and RESULT-001 has final 30/30 post-re-promotion source-built GREEN at `@implemented-and-validated`

|QS-CLI-RESULT-001
|Correctness, interoperability, and determinism
|Given the qualified disposable Maven module-rename fixture and a fixed valid caller request ID, source-built `java rename-module ... --json` preview emits the exact closed `refactorkit.cli-result/v1` field/value/nullability contract in producer order. The plan digest commits to complete hidden edit detail without exposing internal identity or authority. Output is exit 0, one compact object plus LF, empty stderr, empty diagnostics, null transaction, `truncated=false`, and fixed-correlation deterministic bytes.
|RESULT initial/decomposition/atomic-correction SHA-256 `8f29946c5dcb0128075764fbcd2756156d61edfad4d0fffe2f49afa526c68957` / `7c8555ba5c3f393f1809136a38e86da9419234962864513bd4515001557c250c` / `40a0183ec29584724a98e2357738078e7caa8d8356c90a7c0e4cd9930ff3e73e`; current promoted feature/status-neutral-canonical SHA-256 `5956f93a681562f6922a9101daa8c153c9a3b8d1f7fdc260bd0070d5faf48ea5` / `b6f62831bf38c2fdaadbd02abc024abd16c8818a812678c76a1eb146642e4ed9`; meaningful RED log/JSON SHA-256 `896524674ae4745bce1dcd869dcef19de0f79b5fa46e5d06bfda88f802791a78` / `94c6e89a3879634ed2679a307ccd4f6cab942f90c428c792efd097d8edc7e5b9`. Immutable post-promotion failure log/JSON SHA-256 `fcd32c7eb2736ba5c2d9faa65109b18f1f900886d04bf382af2c74b28eeeaa49` / `95770ab372b1c97052da80dea6d72b043da3276255d8eb6cbf63ad255105e554` record the stale full-feature pin failing before preview execution. The corrected pre-final-promotion status-neutral mode-`0444` GREEN log/JSON SHA-256 `63d8d62f22317d09579abf6381a08a01c85f894e31db183c98aa52f222f3feb6` / `4996f117788f1fa53195f0822d25a92e0848e322a4e9da11723f6df36130e4ca` passed one scenario/30 steps and remains immutable superseded evidence. Final mode-`0444` post-re-promotion log/JSON SHA-256 `6ab958c33d541b79c08b2eef354a2bddfdb298d60040364f4d5c935915d14c3d` / `2e10af7f91c7fdf57f5501672dce98869baf26258b86129a35b5b4cec451f21a` passed one scenario/30 steps; supporting fresh-`testClasses`/12-of-12 unit evidence SHA-256 is `5ff927b32797f4acf60693a562b900e21d958d1b6a9166d9de91b1c52aded264`. Earlier pairs remain immutable historical evidence.
|implementation-informed GREEN and declared `@implemented-and-validated` within the bounded source-built contract; installed/package/platform and wider E2E qualification remain open

|QS-CLI-RESULT-002
|Safety, compatibility, and bounded qualification
|The preview-result ACL constructs transient immutable documents only. Qualified JSON preview creates no managed write, lock, WAL, transaction, rollback claim, or installed-runtime interaction; human preview remains unchanged. JSON apply plus invalid or misplaced request-ID usage reject before scan or mutation, without claiming a RESULT-002 or RESULT-004 envelope. RESULT-002 through `006`, other commands/languages, installed/package/native/other-host execution, fresh SURFACE-002 authority, release, and support promotion remain absent.
|The same immutable receipts; human stdout/stderr/bundle SHA-256 `b353a04f51595e749cbb7a0c7a4e5a63c355bd8182f555e6c01acdb8971a1c4c` / `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `2c38357f125f5d20b1ea9b840d2a8eff12e64337a5fe8655afa10cb5777fb31b`; regression-only SURFACE-002 log SHA-256 `a345ade4a1053204c98490699773a5c8fc26344d2b433d5e1829a709f9dd7ec9`; ADRs 0002/0004.
|implementation-informed for bounded preview safety and compatibility; RESULT-002 through `006`, fresh SURFACE-002 qualification, installation/package/platform/release, and promotion remain absent

|QS-PORT-001
|Portability
|A packaged CLI can run with its embedded runtime and scan a sample project without relying on `PATH` Java.
|CI smoke test and packaging docs.
|implementation-informed

|QS-TEST-001
|Testability
|CI runs Gradle build, golden tests, self-contained package build, and packaged CLI smoke test.
|`.github/workflows/ci.yml`.
|implementation-informed

|QS-SAFE-005
|Safety
|A stable apply path rejects unsafe symlinks/path traversal, exposes interrupted or partial apply deterministically, tolerates or reports transaction-log corruption, and restores the pre-apply hash after rollback for every managed write.
|Stable safety acceptance matrix in `docs/releases/v1.0.0-plan.md`; ADR 0002.
|confirmed target; planned evidence

|QS-CORR-010
|Correctness
|Every advertised stable Java operation labels JDT-backed, narrowly proven lexical, or refusal evidence; unresolved or incomplete semantic evidence never results in a lexical edit presented as semantic authority.
|Stable semantic acceptance matrix; ADR 0008.
|confirmed target; planned evidence

|QS-COMPAT-001
|Compatibility
|Representative Java 8, 11, 17, 21, and 25 projects parse, bind, diagnose, and execute only advertised stable operation shapes using explicit per-project compliance.
|Stable Java compatibility matrix; ADRs 0001 and 0008.
|confirmed target; planned evidence

|QS-DIAG-001
|Validation
|Stable compiler-backed diagnostics have deterministic severity, source range, evidence, and category, and configured before/simulation/after/rollback gates refuse unapproved regressions.
|Stable diagnostics acceptance matrix; ADRs 0002 and 0008.
|confirmed target; planned evidence

|QS-DIAG-002
|Semantic correctness
|For a supported Java/Maven request, RefactorKit either returns diagnostics from a complete attested source-set environment or one concise typed unavailability root. A successful Maven compile/test may be reported separately but never suppresses or excuses a RefactorKit finding.
|`RPK-JAVA-DIAG-003..006`; ADR 0012; permanent reactor fixture.
|planned, product-critical release blocker

|QS-DIAG-003
|Java compatibility and portability
|With `JAVA_HOME` unset, the packaged launcher resolves `java.sql.Connection` and `java.net.http.HttpClient` for a release-21 fixture. A release-8 fixture rejects `HttpClient` and `String.isBlank`, while release 11+ accepts them, using immutable hash-attested platform signatures rather than the running jlink image.
|`RPK-JAVA-DIAG-001` and `007`; four-platform packaged acceptance.
|planned, product-critical release blocker

|QS-DIAG-004
|Project-model correctness and security
|A module-targeted request opened at the declared reactor root resolves only its effective Maven main/test dependency closure after full-reactor construction; unrelated siblings remain hidden. External traversal is descriptor-closed over selected nodes per projection: every active reactor POM remains mandatory, every selected child has complete effective descriptor closure, and each outgoing declaration has a hash-bound selected/pruned result. Effective scope derivation preserves an explicitly declared transitive `test` scope and uses a dependency-management scope default only when the declaration omits scope. A deterministic derived-scope/optional/exclusion prune is decided before child lookup when child facts cannot affect it; alternate selected paths restore normal requirements, and an included missing descriptor blocks. Opening a child directory neither discovers parents/siblings nor reads outside the declared workspace.
|`RPK-JAVA-DIAG-002..003`; accepted ADR 0012; full-reactor fixture and workspace-boundary tests; bounded implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-009/010/012.
|bounded dependency-scope, descriptor-pruning/alternate-selected-POM, and six selected-descriptor refusal cases implementation-informed; full diagnostics-authority acceptance planned, product-critical

|QS-DIAG-005
|Change safety
|Before and exact staged diagnostics bind snapshot, overlay mode, source-set/impact closure, model/classpath evidence, platform, and provider. Default mode rejects introduced or changed errors, strict mode requires zero staged errors, and global source-set authority blocks on every unavailable affected environment even when unavailable at baseline. The qualified target-scoped Maven `moveClass` mode is narrower: full-reactor structural/candidate authority stays global, while admitted external `OFFLINE_MISSING` coordinate/path and retained-diagnostic evidence is operation-closure scoped under QS-MOVE-004 through `006`. File-move mapping preserves an exact unchanged error while the same message at a new location is introduced.
|`RPK-JAVA-DIAG-004..005`; ADRs 0002 and 0012; exact delta acceptance; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001 zero-to-zero `AVAILABLE`, `007` closure-missing, `009` closure-isolation, and `010` descriptor-pruning/attestation evidence.
|bounded target-scoped rows implementation-informed; global/general authority planned, product-critical release blocker

|QS-DIAG-006
|Reproducibility and least privilege
|Diagnostics include only safely declared, already materialized generated sources and cause no Maven/wrapper/lifecycle/plugin/annotation-processor/credential/network execution or workspace write. Named JPMS/module-path reactors return typed unsupported until their dedicated complete slice is accepted.
|`RPK-JAVA-DIAG-006..007`; ADRs 0010-0012; hostile-workspace and read-only acceptance.
|planned, product-critical release blocker

|QS-DIAG-007
|Behavior preservation and correctness
|For every exact selection-metadata combination, daemon and MCP choose the same gate as the reviewed duplicate branch table: exact Java Maven operation before ordinary Java; exact Kotlin Java-affected split using original `plan.affectedFiles.any { Objects.requireNonNull(it.fileName).toString().endsWith(".java") }`; `moveDeclaration` before `JDT_BINDING` before broad fallback; Kotlin-only K2 otherwise; and unchanged external resolution for every other exact language ID. The explicit non-null final-segment requirement preserves the earlier root-path `NullPointerException`. The four built-in IDs remain exactly `java-maven-ownership`, `java-jdt`, `kotlin-k2-java-jdt`, and `kotlin-k2`; external IDs are returned unchanged. `.java` matches, while `.JAVA`, case-changed language IDs, and deliberately unusual operation/evidence combinations must not be normalized or narrowed. The exact supplied Java/Kotlin adapter identities are retained.
|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001 in `features/managed-apply-diagnostics-gate-selector.feature`; production `ManagedApplyDiagnosticsGateSelector.kt`; source-inspected daemon/MCP adoption; Cucumber runner/glue. Final independent OpenJDK 21.0.11 source-built/in-process acceptance passed all ten outline cases/80 expanded steps within the 11/11-scenario, 92/92-step run, with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`). Daemon regression passed 58/58 and MCP regression passed 26/26. Static cleanup eliminated the initial selector production NP finding without changing the root-path or case-sensitive behavior. Commit `642c187` remains the historical duplicate-branch baseline.
|implementation-informed for exact selector routing and source-inspected daemon/MCP adoption; no packaged/native/cross-platform/concurrent/apply-authority/numerical-coverage qualification

|QS-DIAG-008
|Safety, ordering, and authority
|Selecting a gate invokes zero diagnostics providers. The external resolver is called exactly once only for external IDs during selection, propagates its exception unchanged, and returns its gate unchanged; lookup is immediate while provider execution remains deferred. `PatchEngine` alone invokes the provider under its workspace lock for baseline, staged, post-image, and possible restored-baseline checks. Maven rebuild/scan work remains provider-lazy; Kotlin-only returns compiler result diagnostics verbatim; each mixed route retains compiler/K2 diagnostics before converted JDT diagnostics without selector sorting/deduplication. Current adapters remain available through post-apply or automatic rollback checks. CLI/LSP/recipes/testkit, preview/query routes, daemon post-success diagnostics, lifecycle, rendering, WAL, explicit rollback, and recovery remain unaffected.
|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002 in `features/managed-apply-diagnostics-gate-selector.feature`; production selector; focused Cucumber runner/glue; accepted ADRs 0002/0004. Its one 12-step case passed within the final independent 11/11-scenario, 92/92-step OpenJDK 21.0.11 source-built/in-process run (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`). The acceptance fixture verifies zero provider calls during selection, per-call adapter identity, exact external lookup/gate/exception behavior, non-mutation, and explicit exclusions without invoking `PatchEngine`; daemon/MCP adoption is source-inspected and their regressions passed 58/58 and 26/26. Final whole-working-tree source-built verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. The standalone `:modules:refactorkit-jvm:compileTestKotlin --rerun-tasks` command initially failed because `testRuntimeClasspath` was resolved eagerly during configuration (RED log SHA-256 `798419c8a27a9a0c9ca74dd3d41ab2851acfb84d3d4f492002aaed71bfafa98a`); it passed after the compiler-classpath system property was deferred to test execution (GREEN log SHA-256 `c0ee8f6dfc932015dbb8b2067a3f3637c2208b6888a72bc672369bbb3d5df7e7`). This RED/GREEN pair is build-scaffold correctness evidence only, not product or package qualification. Its final full log contains no selector/provider-function production finding; 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking.
|implementation-informed for the bounded selector, daemon/MCP regression, and dated source-built aggregate; CLI/LSP/recipes/testkit/direct library excluded and `PatchEngine` authority unchanged

|QS-SNAPSHOT-001
|Behavior preservation, correctness, and determinism
|For identical filesystem and collaborator evidence, daemon and MCP composition returns equal canonical `ProjectSnapshot` content/hash through one ordered service. An empty script contribution invokes no TypeScript attacher and passes the exact base snapshot unchanged to the optional Kotlin stage; without Kotlin it returns the same instance/hash and does not reorder files or widen extension/ignore scope. A non-empty contribution uses the exact four script language mappings, normalized-path later-script collision precedence, `path.toString()` output ordering, and only the `files`/`sourceExtensions`/`ignoredDirectories` overlay before one TypeScript attachment and optional current Kotlin attachment last. Same-provider build models remain replaced rather than duplicated.
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `003` in `features/workspace-snapshot-composer-extraction.feature` specify the exact identity, bounded overlay, determinism, per-call ordering, and provider pass-through contract. Production evidence is `WorkspaceSnapshotComposer.kt`; focused acceptance glue is `WorkspaceSnapshotComposerCucumberTest.kt` and `WorkspaceSnapshotComposerSteps.kt`. A missing-composer compile RED is retained at SHA-256 `d2876ad413703a3895999a9377f8b7c6a713578eb8156b652bb079c70f1a6d95`. Final independent OpenJDK 21.0.11 source-built/in-process acceptance passed all 8 scenarios and 65 expanded steps with zero failures, errors, or skips (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`).
|implementation-informed for the bounded source-built/in-process correctness and determinism contract; no broader platform, concurrency, numerical-coverage, or general-orchestration qualification

|QS-SNAPSHOT-002
|Maintainability, failure fidelity, and least privilege
|The composer is stateless and accepts current scanner/attacher collaborators per invocation; it retains no scanner, adapter, toolchain, process, root, snapshot, lease, or lifecycle. Core imports no concrete adapter/surface type. Scanner/attacher exceptions escape with unchanged identity, typed model unavailability remains evidence, and composition performs no write, build execution, lock, WAL, transaction, apply, recovery, or rollback. Daemon/MCP retain refresh/watch/index/pending/audit/lease/semantic/protocol behavior; CLI/LSP and a new module remain excluded.
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-003 through `005`; fieldless language-neutral core implementation; focused acceptance; source-inspected private daemon/MCP `scanWorkspace` adoption; Chapters 2, 5, 6, and 8; LATE delegated ADR/C4 reviews. Daemon/MCP regressions passed 58/58 and 26/26 (combined log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`). Final 2026-08-01 official whole-working-tree OpenJDK 21.0.11 `check goldenTest` verification passed in 7m 7s with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). It has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption; static findings remain non-zero and non-blocking: 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` (6 NP, 3 RCN, 12 BC), with the broader configured baseline also non-blocking. REQ-005 adoption is qualified only by source inspection plus the regressions; CLI/LSP, lifecycle/index/`PatchEngine`/protocol authority, and every managed-write concern remain outside.
|implementation-informed for the bounded source-built/in-process failure, state-isolation, and ownership contract plus dated mutable source-built aggregate evidence; no packaged, native, cross-platform, concurrent, crash-restart, numerical-coverage, or general-orchestration qualification

|QS-MOVE-001
|Semantic correctness, completeness, and recoverability
|For the permanent reactor with one aggregator and 20 active non-aggregator modules, a supported same-module/source-set class move derives the complete direct, transitive, and test-only reverse-observer closure, including Java Cucumber step-definition sources, while keeping each JDT environment dependency-bounded. Every bounded lexical Java candidate is classified, every Java edit is selected by an exact target binding, staged re-analysis preserves other-symbol classifications, leaves no old bound target, and introduces no error, apply creates one transaction, and rollback restores the original hash and inventory byte-for-byte. Unrelated siblings, a same-simple-name decoy, and non-Java strings remain unchanged.
|`RPK-JAVA-MOVE-001..004` and `006`; the permanent offline fixture is created. REQ-JAVA-MAVEN-MOVE-AUTH-001 validates the supported complete-`AVAILABLE` shape with two deterministic source-built `JDT_BINDING` previews, the exact owner/three-observer boundary, five top-level `FileEdit` entries (four `Modify`, one `Rename`), nested `TextEdit` counts `1/2/1/1` across the modifies, zero-to-zero staged diagnostics, one exact staged-image transaction, and exact byte/path/seven-source/snapshot rollback with diagnostics. REQ-JAVA-MAVEN-MOVE-AUTH-002 independently validates the exact observers and use forms: direct `catalog-pricing:main` (import, constructor, declared type), transitive `catalog-storefront:main` (import, `instanceof`, class literal), and test-only `catalog-acceptance:test` (Cucumber import, declared type, exact FQN). The JDT-only candidate inventory is exactly 12 `BOUND_TARGET`, one exact-binding `BOUND_OTHER` decoy, and zero `UNRESOLVED`; residual categories remain separately non-managed. Requirements `006`, `007`, and `009`-`012` retain their narrower evidence. A separate Linux/JDK 21 CLI field run at Magrathea `a48e3c06` remains request-specific. The implemented second validation mode exercises the same REQ-001 pickle through the real unpacked launcher and embedded runtime over one no-follow fixture copy. Sixteen bounded commands prove scrubbed environment/poison-`PATH`, repeat-equal previews, exact three observer paths and five affected paths, the exact two-line human lock refusal before WAL with no projected `workspace.locked` or core-code correlation claim, one child-process-replanned apply with no cross-process plan-ID claim, one schema-v8 transaction containing four `Modify` plus one `Rename` top-level `FileEdit` entries and `1/2/1/1` nested `TextEdit` counts, clean post-apply diagnostics, normal rollback, exact baseline restoration, unchanged fixture/package, and zero timeouts/truncations/leaks. Official final OpenJDK 21.0.11/Linux `amd64` packaged execution passed in 22s with 35 tasks, JUnit 22/one/21/zero, and Cucumber five background plus fourteen scenario steps (19/19) and 2/2 hooks; log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`, JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The separate final source-built whole-tree gate passed 1045/817/228/zero standard-test accounting, 140 XML files, and 82 tasks (log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`), intentionally excluding the packaged task. Targeted custom-source-set static tasks passed (log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`) with 99 reviewed/non-blocking packaged validation-adapter findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), no production source addition/modification by this slice, and no aggregate-cleanliness or numerical/child-JVM-coverage claim. This is additional validation evidence for QS-MOVE-001, not a new quality scenario, requirement, or case.
|fixture complete-`AVAILABLE` positive flow, exact observer/candidate/residual discrimination, independently bounded target-scoped rows, six descriptor-refusal cases, one exact CLI field request, and one local Linux `amd64` packaged REQ-001 mode implemented/validated within their stated evidence; all four native CI rows remain `CONFIGURED_UNOBSERVED`, and generalization remains P0 product-critical

|QS-MOVE-002
|Fail-closed safety and consistency
|Every active reactor POM and every descriptor input for a selected external node must be present, current, and hash-bound; every outgoing declaration must be classified. A deterministically derived-scope/optional/exclusion-pruned external edge needs no child descriptor/binary on that path, but ambiguity, another selected path, or loss of an included POM/parent/BOM/management/mediation/relocation input makes target-scoped eligibility `INELIGIBLE`. A selected external binary may remain `OFFLINE_MISSING` only after descriptor closure passes; negative-presence proof is mandatory for closure binaries, while outside-closure absence remains visible without operation inventory only after exclusion is proved. Any target-relevant `UNRESOLVED` candidate, lookup ambiguity, diagnostic identity/non-concealment failure, new/changed staged error, truncation, conflict, provider failure, or evidence drift refuses before WAL. Caller-attested classpaths, Maven/plugin execution, settings credentials, implicit network, approval, or lexical evidence cannot promote authority.
|`RPK-JAVA-MOVE-002..004`, `006..007`; ADRs 0002, 0008, and 0010-0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`.
|Twelve validated definitions/22 bounded cases implementation-informed, including six phase-correct REQ-008 failures; general authority and packaged/runtime/native acceptance remain open P0

|QS-MOVE-003
|Usability, determinism, and truthfulness
|For each safely enumerable REQ-JAVA-MAVEN-MOVE-AUTH-003 defect, repeat preview from the same exact request, snapshot SHA-256, expected/observed fingerprints, and candidate evidence returns equal immutable `JavaMoveClassReviewOnlyGuidance` with the same canonical evidence SHA-256 and no UUID or wall-clock input. Schema version 1 carries one stable typed blocker with separate Maven module/source-set fields; exact `BOUND_TARGET`, exact `BOUND_OTHER`, unresolved/recovered Java, Java non-code, and non-Java groups; explicit `COMPLETE` or `COMPLETE_WITH_TYPED_OMISSIONS`; the bounded `SOURCE_INVENTORY_ENTRY` omission; ordered typed restoration actions; and checklist version 1's seven fixed human/VCS steps. Strict expected-source and generated-root manifests are snapshot-bound auxiliary files; blocker data binds their manifest hashes, while classpath mismatch data binds its strict evidence-manifest hash and expected/observed artifact fingerprints. The guidance type has no `PatchPlan`, `PlanId`, `WorkspaceEdit`, replacement text, pending-plan handle, transaction, RefactorKit rollback field, or plan conversion. CLI `--apply` returns `guidance.nonManaged` before `PatchEngine`, lock, and WAL. The four variants are a readable source omitted by the real default scanner after an effective primary Maven `testSourceDirectory` replacement, a readable system-path artifact fingerprint mismatch, a recovered target-use binding without syntax failure, and a materialized-generated-root inventory mismatch owned by `catalog-generated-support:main`. Primary replacement is modeled separately from additive build-helper/Kotlin roots: additive declarations retain default Java roots, and no plugin is executed. Broad syntax failure still follows REQ-005 lexical behavior.
|`RPK-JAVA-MOVE-002`, `004`, `005`, `006`, and `007`; `JavaMoveClassReviewOnlyGuidance`, `JavaMoveClassGuidanceEvidence`, `JavaMoveClassOperationDispatcher`, `JavaMoveClassGuidanceJsonRenderer`; `JavaMavenMoveClassGuidanceAuthorityCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003. Independent final 2026-07-30 OpenJDK 21 `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 13s, 956 discovered, 773 executed, 183 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; log SHA-256 `8d9b19576a27deaae2c12c767f929664c228db85d1dcc8644de3e41ecbe6ca9d`. This is dated mutable working-tree evidence; SpotBugs retained the configured non-blocking baseline findings. Cross-surface lexical review is separately owned and validated by `004`.
|implemented and validated for four bounded REQ-003 default-scanner defects through the Java dispatcher/source-built CLI; REQ-008 is independently closed for its six non-overlapping cases only

|QS-MOVE-004
|Target-scoped semantic correctness and recoverability
|With `reactorStructureStatus=COMPLETE` and `externalClasspathStatus=OFFLINE_MISSING` for exactly one synthetic enumerated leaf artifact with hash-bound expected identity/provided types and negative-presence evidence, the permanent fixture exposes one exact non-recovered declaration and 13 candidate records classified exactly once as 12 `BOUND_TARGET`, one exact same-name `BOUND_OTHER`, and zero `UNRESOLVED`. Its two retained external-type diagnostics map to the same full before/staged identities and carry machine-checkable non-concealment proof. The result remains globally classpath-incomplete but is a deterministic `SEMANTIC_PREVIEW` with `JDT_BINDING` and `ELIGIBLE`; no lexical range selects an edit, staged analysis introduces no new or changed error, apply creates exactly one transaction, after diagnostics attest the commit, and rollback restores every non-engine byte, path, source-inventory entry, and snapshot hash exactly.
|`RPK-JAVA-MOVE-002..004`, `006..007`; ADR 0012; REQ-JAVA-MAVEN-MOVE-AUTH-007.
|implemented and validated for one bounded synthetic enumerated-leaf row; general qualification remains open, while the exact Magrathea CLI field request is separately qualified after `010`

|QS-MOVE-005
|Phase-correct fail-closed target uncertainty, structure, and freshness
|Implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008 accepts six isolated variants without overlap. Four complete preview variants return immutable schema-v1 `REVIEW_ONLY_GUIDANCE`: inside-closure ambiguous `UNRESOLVED` candidate (`CANDIDATE_TOTALITY`); unresolved `STATIC_IMPORT_ON_DEMAND` owner while actual candidates stay bound (`TARGET_NAME_LOOKUP`); explicit old FQN outside the closure with closure-blocker precedence (`CLOSURE_CONSISTENCY`); and same-`S0` `BEFORE_VS_STAGED` problem-ID/message drift collected by the dispatcher second stage (`DIAGNOSTIC_IDENTITY`). Each binds request/`S0`/canonical evidence and exact typed layer data, contains no plan/edit capability, never returns `LexicalReview`, and makes source-built CLI apply refuse `guidance.nonManaged` before `PatchEngine`, lock, WAL, or residue. Independently, missing root-declared `catalog-shipping/pom.xml` returns structural `PatchStatus.REFUSED` with result-owned `JavaMoveClassStructuralRefusal`, `STRUCTURAL_CLOSURE`, `ACTIVE_REACTOR_POM`, `MISSING`, root-POM hash/range, expected path, and component-wise workspace-relative `NOFOLLOW` identity; missing directory is supported, while symlink/non-regular states are not `MISSING`; source-built CLI refuses pre-lock/no-residue. Finally, an eligible REQ-007 preview binds a non-managed `BOUND_OTHER` source through immutable `OperationAuthorityFileEvidence`. One under-lock engine observation of external `ProductDecoy` state `S1` yields primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` in immutable sorted `DiagnosticDetails` before WAL, preserving `S1` and possibly the lock file but creating no transaction/target edit/destination. It reports the expected semantic candidate-inventory hash and expected/observed required-file-evidence hashes; it does not claim an observed semantic candidate inventory/hash.
|`RPK-JAVA-MOVE-002..004`, `006..007`; accepted ADR 0012; corrected feature; `JavaMavenMoveClassAuthorityFailureCucumberTest`; focused OpenJDK-21 evidence: six passed, sixteen tag-filtered, zero failures/errors, independent log SHA-256 `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Exclusions remain REQ-003/004/010/011/012.
|implemented and validated for five bounded source-built library/CLI preview-time cases and one bounded library/`PatchEngine` under-lock case; no recipe/daemon/LSP/MCP parity is claimed for REQ-008; packaged/runtime/native and general qualification remain open

|QS-MOVE-006
|Target-scoped ordinary missing-binary correctness and non-concealment
|For implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-011, one included non-reactor selected graph leaf is the fixed non-snapshot, unclassified `com.acme.fixture.missing:ordinary-missing-leaf:1.0.0:jar` from one explicitly configured simple local-only Maven-2-layout repository. Its effective POM, parents/BOMs, management/mediation inputs, and selected-leaf proof are present and hash-bound while the JAR path is absent. RefactorKit—not the caller—enumerates repository identity, deterministic paths, selecting projections, affected closure source sets, descriptor/leaf hashes, and exact sidecar-free no-follow before/staged/under-lock negative presence; it records no absent-content hash or provided-type claim. Thirteen candidate records have exact JDT and target-name lookup protection with zero unresolved/unattributed diagnostics. Repeat previews are eligible with `JDT_BINDING`; apply commits one transaction; rollback restores bytes, paths, inventory, and snapshot hash exactly. Three independent under-lock drifts—JAR/path appearance or replacement, present descriptor replacement, and repository/path-identity change—refuse before WAL without RefactorKit mutation.
|`RPK-JAVA-MOVE-002..004`, `006..007`; accepted ADR 0012; ordinary missing-binary concept reviewed 2026-07-30; `JavaMavenMoveClassOrdinaryMissingLeafAuthorityCucumberTest`; REQ-JAVA-MAVEN-MOVE-AUTH-011.
|implemented and validated for the bounded ordinary selected-`jar` fixture row; no general or `test-jar` qualification is inferred

|QS-MOVE-007
|Maven declaration-scope correctness and closure isolation
|Given a complete, hash-bound fixture reactor, effective traversal preserves six explicitly declared transitive `test` scopes instead of overwriting them from matching omitted/default-`compile` or explicit-`compile` dependency-management entries. Those children enter neither the consumer main/test analysis classpaths nor target-closure missing reports. Every target-closure main/test environment remains `AVAILABLE`; the model and lease retain global `OFFLINE_MISSING` solely for a structurally excluded outside-closure module. Candidate-total lookup-protected JDT evidence produces repeat eligible previews; apply commits one transaction; after and rollback diagnostics attest the exact post/restored images; rollback restores bytes, paths, source inventory, and snapshot hash.
|`RPK-JAVA-MOVE-002..004`, `006..007`; ADR 0012; REQ-JAVA-MAVEN-MOVE-AUTH-009.
|implemented and validated for the bounded fixture; does not by itself qualify independently validated `010`/`011`/`012`; the exact Magrathea CLI field request is separately qualified after `010`

|QS-MOVE-008
|Maven selector-pruning correctness
|For implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-010, a selected parent POM and all management inputs remain present and hash-bound while six external children pruned by derived transitive `test`/`provided` scope, transitive optionality, or exact inherited path exclusion have no node/subtree. Traversal emits 18 deterministic `PRUNED` records across `COMPILE`, `RUNTIME`, and `TEST`, each binding declaring POM, consumer/source set, projection, path, variant, exact reason, and a boundary before child version/range, repository/path, POM/effective-model, JAR, or network lookup. Repeat candidate-total `JDT_BINDING` previews remain eligible; apply commits exactly one transaction; after diagnostics attest the post-image; and rollback restores bytes, paths, inventory, and snapshot hash exactly. An alternate compile/runtime path restores descriptor requirements, and its absent selected child POM returns stable structural `REFUSED`/`INELIGIBLE` before lock/WAL with no plan, edit, residue, transaction, or mutation. Type/classifier remain identity rather than prune reasons. This row does not exercise an included ordinary missing binary.
|`RPK-JAVA-MOVE-002`, `004`, `006`, and `007`; accepted ADR 0012; `JavaMavenMoveClassDescriptorPruningAuthorityCucumberTest`; REQ-JAVA-MAVEN-MOVE-AUTH-010.
|implemented and validated for the bounded fixture; `011` independently qualifies the ordinary missing-binary fixture, `012` independently qualifies six selected-descriptor refusals, and the exact Magrathea CLI field request was separately qualified after `010` without requiring `011` or `012`

|QS-MOVE-009
|Ordinary selected-leaf absence provenance
|The fixed-release Maven-2-layout row in QS-MOVE-006 publishes RefactorKit-owned repository identity, deterministic paths, present descriptor hashes, selected-leaf proof, exact `ABSENT` observations, and an evidence-fact hash without an absent-JAR content hash. Appearance, path remapping, descriptor drift, or disappearance of any present structural input invalidates the lease before WAL. No caller-attested classpath, sidecar, settings credential, implicit network resolution, or lexical fallback supplies authority.
|`RPK-JAVA-MOVE-002..004`, `006..007`; accepted ADR 0012; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-011.
|implemented and validated for the bounded ordinary selected-`jar` fixture row, including three under-lock pre-WAL drift cases

|QS-MOVE-010
|Selected-descriptor loss/drift refusal
|For implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-012, six fresh isolated fixture cases independently produce missing selected leaf POM, malformed selected-POM raw/effective model, missing required parent, drifted imported BOM, drifted dependency-management/mediation declaration, or missing parsed model identity that explicitly proves relocation interpretation. Each starts with every other selected descriptor present/current/hash-bound and an engine-captured context of unique safe path/layer/content SHA-256 expectations plus parsed identity. Each produces repeat-stable structural `REFUSED`/`INELIGIBLE` guidance with the coordinate, direct dependency path, `COMPILE`/`RUNTIME`/`TEST`, descriptor layer, and `MISSING`/`MALFORMED`/`DRIFTED` condition. Refusal occurs before semantic selection, applyable plan ID, lock, and WAL; no authority lease, missing-binary/graph-leaf downgrade, `.refactorkit` residue, transaction, or mutation exists, and recorded workspace/descriptor/evidence facts and hashes remain equal. This negative row is separate from a child descriptor that is not required because its edge was already deterministically pruned.
|`RPK-JAVA-MOVE-002`, `004`, `006`, and `007`; accepted ADR 0012; `JavaMavenMoveClassSelectedDescriptorAuthorityCucumberTest`; REQ-JAVA-MAVEN-MOVE-AUTH-012.
|implemented and validated for six bounded selected-descriptor cases; general and cross-surface authority are not inferred

|QS-MOVE-011
|Cross-surface safety, determinism, and interoperability
|For the same normalized Maven `moveClass` request and snapshot with broad observer syntax failure, the direct Java move-class operation dispatcher, source-built CLI, recipe, daemon session, managed LSP session, and MCP session preserve one immutable schema-v1 `LEXICAL_FALLBACK_REVIEW`. Canonical JSON hashing produces equal `requestSha256`, exact `snapshotSha256`, hidden `reviewArtifactSha256`, sorted-fact `evidenceSha256`, and deterministic non-capability `lexical-review-<sha256>` `operationId`; internal random `PatchPlan.id` and approval/acknowledgement/confidence/force metadata cannot alter them. Every result reports `semanticCompleteness=NOT_SEMANTICALLY_PROVEN`, `REVIEW_ONLY`, `LEXICAL_FALLBACK`, `INELIGIBLE`, and `evidence.insufficient`. The hidden review-artifact hash commits to normalized internal compatibility file-operation paths, exact edit ranges, and replacement/create bytes. Exposed residual records contain only normalized path, current content SHA-256, typed risk facts, and `managedEdit=false`; they are capped at 200 records/262,144 canonical bytes, report `recordCompleteness=COMPLETE or TRUNCATED`, deterministic returned-category counts, and four fixed next actions. No protocol emits a plan/edit/range/replacement/apply token, pending plan, LSP `changes`/`documentChanges`, transaction, or rollback capability. The Java-owned LRU audit cache stores envelopes only, is capped at 128 entries/16,777,216 canonical bytes, and is lifecycle-cleared. Recipe evaluation stops with null aggregate plan. Shared parsing requires exactly one `operationId` or `planId`: a still-known operation returns the same envelope and typed `evidence.insufficient` before `PatchEngine`, lock, WAL, editor application, or mutation; no/both/blank, unknown/evicted operation, and arbitrary/unknown plan identities are `INVALID_PARAMS`. Daemon/LSP preserve structured JSON-RPC blocker/envelope `data`; MCP returns equivalent structured tool error content. REQ-003 `REVIEW_ONLY_GUIDANCE` remains distinct, and a fresh binding-clean scan retains existing eligible JDT `Plan` behavior.
|ADRs 0002, 0004, 0008, and 0012; `JavaMoveClassLexicalFallbackReview*`, `JavaMoveClassOperationDispatcher`, `RefactoringApplyIdentity`, CLI/recipe/daemon/LSP/MCP handlers, and `JavaMavenMoveClassLexicalFallbackReviewCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004. The focused OpenJDK-21 report discovered 22 cases, executed/passed the one 38-step REQ-004 case, skipped 21 by tag filter, and recorded zero failures/errors; reviewed log SHA-256 `5fb89c290bd098f249c2ec734c8c7bbffae162e5357ab6b1683042fc68e698ce`. Subsequent independent full working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 3s; 978 discovered, 774 executed, 204 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; reviewed log SHA-256 `890ec9e263d98c3f1ad3dcf4bf0f1f75f6d2e31fe6c2b658b0a1b7dcaf9b293a`. This is dated mutable working-tree evidence; the SpotBugs baseline remained non-blocking.
|implemented and validated for the bounded source-built/in-process scenario; no packaged-process, runtime-distribution, stdio/editor-deployment, native-platform, or general-authority qualification

|QS-MODULE-RENAME-001
|Correctness, determinism, least privilege, and recoverability
|Given a no-follow disposable copy of the permanent offline reactor with one aggregator and 20 exact direct children, direct library planning renames directory `catalog-model` to `catalog-domain` and changes the artifact component only because the caller independently supplies `newArtifactId=catalog-domain`. Omission preserves `catalog-model` and authorizes no dependency-coordinate change. The planner proves the child project's direct artifact literal, one direct root module literal, and every effective-model-proven literal dependency artifact origin; all other POM bytes remain exact. Maintained Woodstox 7.1.1 StAX2 location information over hash-bound raw POMs is the sole exact element-text range authority; DTD processing, external general/parameter entities, and external DTD access are disabled; no POM is serialized and no hand-written tokenizer is a fallback. The parser dependency is limited to `refactorkit-java`. One normalized `WorkspaceEdit` modifies the moved POM before deterministic child-file renames and rebuilds a complete staged reactor without Maven/project-plugin execution or settings/credential/network access. During explicit apply, transient language-neutral evaluation must deep-detach every outer/nested collection in the snapshot/module/build-model graph, diagnostics, and `DiagnosticDetails`, preserve construction-time identity under fresh canonical recomputation of exposed snapshot state, and reproduce baseline `S0`/`D0`. Core must validate `Module.root`, every module source root, and every `BuildModule.root` as workspace-contained; explicit absolute external classpath evidence is admissible only when core independently recomputes and exactly matches its current no-follow path/kind fingerprint. Arbitrary roots or fabricated fingerprints refuse before WAL. Evaluation then returns staged authoritative `S1`/`D1` while preserving the C1 tracked shell, repeats workspace/lease revalidation after provider completion, persists schema-v8 `postSnapshotHash=S1.hash`, and requires committed `S1` plus exact staged diagnostics. Mismatch or unavailability automatically rolls back and requires restored `S0`/`D0`. Repeated equal immutable inputs produce equal normalized preview evidence. A later normal rollback advances the same transaction and restores exact bytes/path kinds, after which fresh authority attests inventories, reactor/snapshot identities, and diagnostics.
|Implemented-and-validated REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` and REQ-JAVA-MAVEN-MODULE-RENAME-001; accepted ADRs 0002/0010/0012; permanent fixture; final OpenJDK 21.0.11 `PASS_FOR_PROMOTION` manifest SHA-256 `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`; authoritative 16/16-scenario, 128/128-step log/JSON SHA-256 `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`/`c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`; module-rename 1/1-scenario, 50/50-step log/JSON SHA-256 `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`/`d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`. Final post-promotion whole-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 29s; 82 tasks executed; 144 standard-test XML files; 1,062 tests discovered, 834 executed, 228 skipped/tag-filtered; zero failures/errors. The full log is `/tmp/refactorkit-authoritative-module-rename-final-check.log` with SHA-256 `c3a7b2a902b45267b60179da887c143d9e79f180f9e7ee4043e7631d4e66326d`; the standard-test XML checksum-manifest SHA-256 is `951f7bd281fc4900a096a2295937ee73784294f0ecb2a5c63f469d0c1dea907a`. SpotBugs remains configured non-blocking with `ignoreFailures=true`. The independent receipt classified the slice as reviewed/non-blocking: the five corrected module-rename contract target classes have zero EI/EI2; the authoritative-evaluation EI is non-real on a defensive unmodifiable diagnostics list; relevant BC/Bx reports are Kotlin compiler collection-cast/non-correctness-boxing artifacts; and the 47 authoritative glue findings (27 BC, 11 MS, 6 NP, 1 RCN, 2 UrF) plus 113 module-rename glue findings (37 BC, 19 MS, 46 NP, 10 RCN, 1 REC) are test/glue lifecycle-nullability or generated-bytecode/static-field reports. No slice defect remained unclassified. PMD has no Kotlin source input for this slice and supplies no Kotlin finding claim. No aggregate numerical coverage is available; these totals are not coverage. The existing dedicated packaged Maven task `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest` is intentionally excluded from the standard totals. This source-built receipt does not qualify packaged, native, cross-platform, or general Maven support.
|implemented and validated for the exact core evaluator and direct-library fixture row; CLI/daemon/LSP/MCP, recipes, packaged/native/cross-platform, general Maven projects, native CI, support-matrix/release-wide qualification, and the broad roadmap item remain open

|QS-MODULE-RENAME-002
|Exact authoritative route identity, laziness, and authority retention
|Given the canonical retained module-rename plan/lease from QS-MODULE-RENAME-001, the normal production selector with exact language `java` returns gate `java-rename-maven-module-staged-reactor-v1` only for the byte-exact raw operation `java.renameMavenModule`. Case changes, omitted prefix, another Maven operation, ordinary Java operations, and one leading/trailing U+0020 use their real existing fallbacks without normalization. Selection and gate-ID inspection perform no provider work. `DiagnosticsGate.lazyAuthoritative` constructs the authoritative delegate at first evaluation at most once under `LazyThreadSafetyMode.SYNCHRONIZED`, verifies exact ID and authoritative-provider presence, and has no observer. One approved apply evaluates exact S0/C1/S1 through the retained operation gate, invokes no generic Java/Maven fallback, writes one schema-v8 `APPLIED` record, and normal rollback advances that record to `ROLLED_BACK` while restoring S0/D0.
|Implemented-and-validated REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001; `features/java-maven-module-rename-managed-surfaces.feature`; `C-TECH-030`; `C-BOUND-007`; `R-043`; final focused JDK 21 1-scenario/25-scenario-step-plus-6-Background-step (31-expanded-step)/2-hook log/Cucumber-JSON SHA-256 `4e826737d94150bcebe0a38d3fee970e84339532f3fe4ef07189da2506bdedef` / `2cf337ab1f963b133222b941df0fd577b9518794e60d23dde81390ba6e760a4c`, with no failures, skips, or undefined results; independent `PASS_FOR_PROMOTION`; selector 11/92 and direct rename 1/50 regressions plus focused core/Java/daemon/MCP regressions passed. The authoritative promoted-tree `check goldenTest` gate passed in 7m 31s with 82/82 tasks and standard-test totals 146 XML files/1,068 discovered/835 executed/233 skipped-tag-filtered/zero failures or errors, excluding the intentionally separate stale packaged Maven task (log/XML-manifest SHA-256 `663ddcf3cb836fa2058d65160b57df19202b2f1908a0331faeb96ee6ca66209d` / `bca4e1f718f56ce3095ef9168267b42506f467ec5b1046408e764b1948502905`). Static analysis is configured non-blocking; no aggregate numerical coverage is claimed.
|implemented and validated only for source-built selector/apply/normal-rollback behavior. CLI/LSP/MCP preview, recipes, public daemon/MCP adoption beyond selector selection (REQ-003), packaged/native/cross-platform/general-Maven/release behavior, and broad support remain open

|QS-MODULE-RENAME-003
|Managed LSP safety, authority retention, lifecycle correctness, and recoverability
|Given three independent API `0.2` LSP sessions retain the canonical module-rename plan over separate exact S0 copies, the dirty-buffer probe must reject before the affected-open-document condition, and both probes must return `DOCUMENT_VERSION_MISMATCH` before fresh scan, gate construction, `PatchEngine`, lock, WAL, target edit, pending-plan removal, or refresh while retaining their plans. A clean/closed `refactorkit.applyPlan` supplies only approval surface `lsp-managed-command`, fresh exact S0 with every auxiliary POM byte, and the exact retained plan/language/current adapters to the existing five-argument `ManagedApplyDiagnosticsGateSelector.select`. Exact `java.renameMavenModule` must select/evaluate only `java-rename-maven-module-staged-reactor-v1`, never generic `java-jdt`; one schema-v8 record reaches `APPLIED` for the canonical five-entry edit before response. Only `ApplyResult.Applied` removes the plan and refreshes S1. Normal rollback advances the same record to `ROLLED_BACK`, creates no second transaction, restores exact non-engine S0/D0, and only success refreshes restored state. The client-managed SURFACE-004 proposal remains separate and non-rollbackable by RefactorKit.
|REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-005; immutable baseline `REQ005-BDD-EVIDENCE-001` at `docs/requirements/req005-surface005-baseline.md` SHA-256 `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; promoted feature SHA-256 `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`; `C-TECH-030`; `C-BOUND-002`/`007`; `R-041`/`043`; accepted ADRs 0002/0004/0012. The durable pre-production focused Cucumber RED log at `docs/requirements/evidence/req005-pre-production-focused-cucumber-red.log` (SHA-256 `b7ae8c9bfbd7122404403f13b10107a46f359030187bd9459e7b26d42f400cc8`) proves only that runtime managed apply failed before the success assertions: the run failed at `JavaMavenModuleRenameSurface005Steps.kt:673` with `BUILD FAILED`, one failed test, and five skipped. Separate source/diff/domain inspection identified the former generic `java-jdt` route; that route and the producer-reported moved-type diagnostic detail are not hash-bound claims from this log. After selector adoption, production adds the direct `refactorkit-lsp` -> `refactorkit-jvm` dependency and calls the public five-argument selector with the exact retained plan, language `java`, current adapters, and fail-closed external resolver. Independent requirements review returned `PASS` with no findings for AC-005-01 through AC-005-11. Post-promotion JDK 21 public-`LspSession.dispatch` evidence passed one scenario/47 results (JSON SHA-256 `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54`); earlier SURFACE-004, `LspSessionTest`, and selector regressions passed 17/17, 22/22, and 30/30 (SHA-256 `d9e68222f88122043c16c49374d2ada9a6dbcb2b9317890987e619b84af9d685` / `4a6876354dc83d364a7001a495f5ff0aad0a2c75e42437c726abe797e7f11c6d` / `4a11d1150dfe2bca5c072b8f6ea1d1f8b4896375dde39f22a0d6a3f533f0f46c`).
|implemented-and-validated for the bounded source-built/in-process public-dispatch row; no external transport/editor, packaged/native/platform, concurrent/crash, general-Maven, release, numerical-coverage, or broader-support qualification is claimed

|QS-V07-BUILD-001
|Build correctness and change safety
|Before any production K5, T5, J1, or I1 implementation change, a clean OpenJDK 21 checkout passes `./gradlew --no-daemon testClasses` for every module. Configuration must not resolve a classpath and then mutate its dependency parent. A scaffolding failure is fixed in an isolated prerequisite change and cannot be hidden by selective compilation or tests.
|`docs/requirements/v0.7.x-completion-contract.md`; historical clean `HEAD=origin/main=2b5aa7234cc385214acb6958834763288aefc527` failure log SHA-256 `408d45e7e2c1bea4152c220af283455421a3bee84dca2b3f069832e9541983d5`. The authorized scaffolding-only working-tree slice reproduced RED (SHA-256 `72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`), changed only `modules/refactorkit-kotlin/build.gradle.kts` to defer `testRuntimeClasspath` resolution into `tasks.test.doFirst`, and passed GREEN (SHA-256 `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`). Full OpenJDK 21 `check goldenTest` passed 82/82 tasks, 1,062 tests with 228 skipped, and zero failures/errors (log SHA-256 `48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). Actions runs `30746237578` and `30749086970` remain separate historical failures. Their shared Linux archive-verification cause is corrected locally by changing only root `build.gradle.kts` so `distCliRuntimeZip` depends on `:modules:refactorkit-cli:refactorkitRuntimeZip`; the resulting 72,030,777-byte archive (SHA-256 `afcd65b0a800ed21f4c46d3b47e0fb569f81fb9c913c5d2965a5189edd3a0b85`) passed checksum and Linux x86-64 archive/launcher verification. Final JDK 21 source-built verification with `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 7m 28s with 82/82 tasks; log `/tmp/refactorkit-runtime-zip-task-final-check.log`, SHA-256 `009dd3940c1042cc6d1d97e18709795760d6eb4025b685f1be4c71e7e1fccae4`. This remains package-scaffold/source-built evidence only and does not qualify CI, native, or release behavior; post-commit CI remains open.
|confirmed P0 release gate, resolved for local test-compilation admission; Linux archive-task wiring is corrected separately and locally; product, package/native/cross-platform, post-commit CI, and release qualification remain open

|QS-V07-KOTLIN-001
|Semantic correctness and truthful refusal
|When a caller selects a Kotlin companion object as a standalone move target, RefactorKit returns a stable typed refusal with no edit/apply capability because the companion is nested in the enclosing type. Moving a separately supported enclosing type/file may carry the companion unchanged. Extraction is not disguised as movement and requires a separate operation.
|Confirmed program decision; `RPK-V07-KOT-COMPANION-MOVE-001` in the requirements appendix; compiler/JVM ownership evidence. Current companion-target planner branch is negative evidence only.
|confirmed target; executable requirement/code correction/qualification planned

|QS-V07-TYPESCRIPT-001
|Semantic correctness, safety, and evidence fidelity
|For each T5 operation row, the TypeScript adapter binds the pinned toolchain, lease, snapshot/overlay, UTF-16 selection, preferences, exact upstream operation or returned refactor/action identity, normalized edits, staged compiler diagnostics, and operation limits. Source-file relocation, declaration move, module-specifier/project-reference migration, each organize-import mode, extract-function/constant, inline-variable, change-signature, and inline-function remain separate. Missing or incomplete authority refuses; dynamic/mixed JavaScript never inherits a TypeScript success.
|`RPK-V07-TS-AUTHORITY-001` and the eight operation rows in the requirements appendix; reviewed 2026-08-02 source packet `SRC-2026-08-02-013` and concept `typed-typescript-refactor-authority` against commit `2b5aa7234cc385214acb6958834763288aefc527`.
|confirmed target; current advanced planner source unqualified, acceptance planned

|QS-V07-LSP-001
|API compatibility and ownership truthfulness
|For REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-004, an API `0.2` LSP proposal must project the upstream immutable canonical plan into immutable versioned `documentChanges`, report exact `PREVIEW`/`client-managed`/rollback-unavailable/document-versions-checked ownership facts, preserve the ordered three POM modifications then two file renames, use JSON `null` for closed documents, and fabricate no open-document version. Its session plan ID is transient correlation only, not approval or transaction identity. Proposal creation and editor-owned application perform no operation apply gate, `PatchEngine`, lock, WAL, RefactorKit transaction, state refresh, recovery, or rollback; client application is non-transactional and non-rollbackable by RefactorKit. A legacy client receives `DOCUMENT_VERSION_MISMATCH` before edit return or plan retention with unchanged bytes/journal. Only the separate SURFACE-005 explicit managed application of the exact retained plan may enter `PatchEngine` after dirty/affected-open-document preflight and claim authorization, diagnostics-gated WAL, recovery, and rollback. Evidence from one route never qualifies the other.
|REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-004 baseline `REQ004-BDD-EVIDENCE-001` SHA-256 `61079d0aba9a9bf156faa80eadb937cca20155eeab47006df746cbd677123c1c`; independent `PASS` with no findings against reviewed HEAD `db25c9af2d62c8e9e90f93792a6c6593e6e13698` plus scoped boundary manifest SHA-256 `e797994c103d960546c32547de01ad6182b080dafdba52599854eaa74656c8ee`; promoted SURFACE-004 feature SHA-256 `20d3df5193586bfe0b3db202bf48295c1c8cfd9b41bfcaa3f71ce76846be0e81`; historical post-SURFACE-005-promotion feature SHA-256 `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`; historical pre-promotion feature SHA-256 `7df1e5ab23804def6fa7d633df66fd6a42423b1c913ec12170b8fcca1715bf36` after the separate feature-only SURFACE-002 reconciliation; current status-only promoted feature SHA-256 `1c29ad27146f06072c9815a1e056e2702cd25b8975ea2f0307385091e9859ad3`, with SURFACE-002 `@implemented-and-validated` after independent `PASS`; post-promotion JDK 21 focused Cucumber JSON SHA-256 `79234761252fdcb07845a34eda0581c7f4b53beecbe21e1eb294d00c552731ff` (one scenario, 17/17 passed results); reviewed supporting `LspSessionTest` regression XML SHA-256 `d84069c7f4318d41cf4f2f64276df7af2e01e00082a56a24c638e2571d0de69d` (22 tests, zero failures/errors/skips); `RPK-V07-LSP-MODULE-RENAME-001`; ADRs 0002/0004; `docs/integration-contracts.md`; transactionality audit `TX-007`.
|confirmed compatibility; implemented-and-validated for the bounded, implementation-informed source-built/in-process SURFACE-004 evidence through public `LspSession.dispatch` plus harness-side client application. The exact five-entry/null-version oracle, client simulation outside LSP, absent engine/journal state, and fail-closed test-only legacy/retention observations are bounded evidence only. SURFACE-005 is separately implemented-and-validated under baseline SHA-256 `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; no external transport, packaged/native/editor integration, cross-platform, general-Maven, release, broad-support, or numerical-coverage claim

|QS-RECIPE-002
|Authority preservation and recoverability
|A recipe containing an authority-owning mutation retains that child's exact operation identity, plan, evidence, lease, auxiliary inputs, and diagnostics/authoritative-evaluation gate through final-candidate apply, or uses a separately proven recipe-wide evaluator. Until the latter exists, a second authority-owning mutation or any stale/lost/relabelled/proposal-only authority refuses before lock/WAL. Relabeling as `recipe:<id>` cannot weaken the gate.
|`RPK-V07-RECIPE-AUTHORITY-001`; ADRs 0002/0004; reviewed concept `authority-preserving-recipe-composition` at commit `2b5aa7234cc385214acb6958834763288aefc527`.
|confirmed target; current model-changing composition path is negative evidence, corrective acceptance planned

|QS-V07-RELEASE-001
|Supply-chain integrity and reproducibility
|Every promoted native `v0.7.x` row joins repository/tag/full commit/clean-version identity, platform/architecture, archive and extracted-image hashes, fixture/request/scenario/toolchain identities, operation-family report hashes, SPDX SBOM hash, and attestation subject digest. A verifier independently downloads and validates the final published asset and executes no-global-Java representative apply/rollback. Local/source-built runs, queued/running CI, and intermediate artifacts do not substitute.
|`RPK-V07-RELEASE-EVIDENCE-001`; ADR 0005; reviewed 2026-08-02 concept `release-evidence-identity` against commit `2b5aa7234cc385214acb6958834763288aefc527`.
|confirmed target; I1 evidence chain planned

|QS-API-002
|Interoperability
|API `1.0` CLI and daemon clients receive versioned capability metadata and deterministic outcomes for unknown methods/fields, invalid parameters, stale plans, rollback failures, and supported `0.2` transition behavior or explicit version refusal.
|Stable protocol contract suite; ADR 0004.
|confirmed target; planned evidence

|QS-PROTO-001
|Interoperability and security
|Every exposed LSP capability/command and MCP tool/resource/prompt is classified stable or experimental; stable paths pass initialization, malformed-input, refusal, cancellation, and workspace-boundary tests without arbitrary MCP filesystem access.
|Stable LSP/MCP contract suites; ADRs 0004 and 0006.
|confirmed target; planned evidence

|QS-PROTO-002
|Consistency and interoperability
|An admitted native LSP edit is identified as client-managed/no-rollback, plans from full-sync open-buffer overlays, carries exact open-document versions, and refuses incapable clients; managed LSP writes reject dirty or affected-open buffers. An ineligible lexical move review must instead return no `changes`/`documentChanges` at all.
|Transactionality audit `TX-007`; ADR 0004; `LspSession`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|ordinary ownership and bounded lexical no-edit command path implementation-informed; packaged/native editor qualification remains open

|QS-PROTO-003
|Behavioral compatibility, resource control, and maintainability
|With daemon, LSP, and MCP using one generic core pending-plan store, each session still retains at most 128 exact payloads in true access order: successful lookup refreshes recency, a 129th insertion silently evicts the untouched least-recently accessed entry, and touching the oldest makes the previously second-oldest entry the eviction victim. Cross-surface characterization preserves each current admission/removal/clear matrix—including daemon-only discard, selected-versus-all apply consumption, refusal retry differences, rollback differences, document/project resets, and unconditional MCP importer registration—while every existing protocol response remains unchanged. Evict, remove, discard, and clear create no workspace write, lock, WAL, transaction, or rollback evidence.
|`PendingPlanStore.kt`, `DaemonSession.kt`, `LspSession.kt`, `McpSession.kt`, and `PendingPlanStoreCucumberTest`/steps; implemented-and-validated REQ-PENDING-PLAN-STORE-001/002; focused OpenJDK 21.0.11 log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`; subsequent 2026-07-31 whole-working-tree source-built verification reported 1002 discovered, 782 executed, 220 skipped/tag-filtered, and zero failures/errors (77 tasks; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`), with no `PendingPlanStore` SpotBugs findings; ADRs 0002 and 0004.
|implementation-informed for two source-built/in-process scenarios and 19 steps; packaged, concurrency, and general service-extraction qualification remains open

|QS-RECIPE-001
|Recoverability
|Recipe execution stages eligible steps against one evolving snapshot and commits one validated recipe-wide plan; later planning failure and no-op execution write no transaction. A lexical `moveClass` review must terminate before aggregate-plan construction and discard all staged images.
|Transactionality audit `TX-008`; ADR 0004; `RecipeEngine`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|ordinary recipe boundary and bounded lexical `RecipeResult.NonManaged` early-stop implementation-informed

|QS-EDIT-001
|Correctness
|All modifications to one file use one original coordinate space, are merged and checked together for overlap and complete line/character bounds, and every final file is rendered before the first workspace mutation.
|Transactionality audit `TX-009`, `TX-010`; future core tests.
|planned release blocker

|QS-PERF-001
|Performance
|Reference workspaces remain within documented scan, index, preview, apply, and memory budgets, while daemon requests, results, caches, pending plans, and transaction metadata fail with deterministic limit/timeout/cancellation behavior rather than unbounded growth.
|Stable benchmark and stress jobs in `docs/releases/v1.0.0-plan.md`.
|confirmed target; planned evidence

|QS-SUPPLY-001
|Supply-chain integrity
|A downloaded Linux x86_64 stable archive runs without global Java and independently verifies checksum, SBOM, provenance/attestation, layout, permissions, licenses, version metadata, and documented reproducibility evidence or limits.
|Stable packaging acceptance matrix; ADR 0005.
|confirmed target; planned evidence
|===

== Validation Commands

Current repository validation commands include:

[source,bash]
----
./gradlew --no-daemon testClasses
./gradlew build
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.mavenmoveauthority.JavaMavenMoveClassAvailableAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.mavenmoveobserverauthority.JavaMavenMoveClassObserverAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.acceptance.JavaMavenMoveClassApplyAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.acceptance.JavaMavenMoveClassGuidanceAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.req004lexicalreview.JavaMavenMoveClassLexicalFallbackReviewCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.req008authority.JavaMavenMoveClassAuthorityFailureCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.acceptance.JavaMavenMoveClassDescriptorPruningAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.acceptance.JavaMavenMoveClassOrdinaryMissingLeafAuthorityCucumberTest
./gradlew :modules:refactorkit-cli:test --tests org.refactorkit.cli.acceptance.JavaMavenMoveClassSelectedDescriptorAuthorityCucumberTest
./refactorkit test-golden
./gradlew packageCliRuntime
modules/refactorkit-cli/build/package/refactorkit/bin/refactorkit --help
modules/refactorkit-cli/build/package/refactorkit/bin/refactorkit scan samples/java-maven-simple
./gradlew :modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest
----

The packaged REQ-001 command is explicit, depends on the unpacked package task, and remains outside ordinary `test` and `check`. Its local Linux `amd64` receipt is bounded to the mutable tree and is reported separately from the now-passing final whole-tree source-built and targeted custom-source-set static gates; its JUnit/Cucumber results are intentionally absent from the standard-test totals. It is not a post-commit native CI receipt. Native CI wiring and `if: always()` report uploads are implemented but unexecuted.

Dated verification used OpenJDK 21. Earlier full-suite and focused REQ-004 evidence remains valid only for its recorded working-tree states. For the final REQ-008 reconciliation, the corrected feature contains twelve requirement definitions and 22 expanded cases, all `@implemented-and-validated`. The focused source-built runner discovered 22, selected/executed/passed six REQ-008 cases, skipped sixteen by tag filter, and had zero failures/errors. Independent review log SHA-256 is `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`; the `gherkin-writer` execution log is `6f7e2d8a9c767f265a117853267a37ab31ec31d0878637870b37202ec8ba2c71`. Subsequent independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Separately, the exact Magrathea `a48e3c06` source-built CLI/Linux/JDK-21 result remains request-specific and does not establish packaged/runtime/native/general authority.

== Quality Gaps and Resolved Admission Evidence

[cols="1,3,1",options="header"]
|===
|Gap |Impact |Status

|Historical P0 all-module test-compilation regression
|The clean pushed baseline could not establish a trustworthy production-work baseline because Gradle failed before compilation when `refactorkit-kotlin:testImplementation` was mutated after `testRuntimeClasspath` resolution.
|resolved for local admission by the isolated RED/GREEN scaffold correction and full OpenJDK 21 aggregate run recorded in `QS-V07-BUILD-001`; preserve the gate green, while corrected-revision CI and release qualification remain open

|No confirmed quality-goal priorities
|Chapter 1 quality goals may not reflect business priorities.
|needs-review

|Compiler-backed Java analysis covers selected slices only
|Signed method rename (including exact annotation-element declarations and named usages), unambiguous `Owner#field` field rename, type-level rename (including annotation types), move-class file scoping, safe-delete type references, annotation-type/element indexing and reference analysis, and exact-import use checks use selected JDT binding evidence, but lexical/structural behavior and false positives/negatives remain elsewhere and on explicit fallback paths. Field evidence does not imply semantic support for arbitrary unsigned methods. Annotation-element change-signature and default-value editing are not supported. Organize imports preserves wildcard imports and unresolved files do not receive semantic unused-import removal.
|known risk

|Performance targets not yet accepted
|The stable plan now requires measurable reference-workspace and resource budgets, but numeric budgets and passing evidence remain pending.
|planned release blocker

|No full threat model
|Security properties for MCP/importer/external language-server boundaries need formal review; stable MCP least-privilege and workspace-boundary tests reduce but do not eliminate this gap.
|needs-review

|Transactionality guarantees exceed current evidence
|Managed writes now have one-writer locking, affected-file revalidation, durable write-ahead lifecycle, compatible startup compensation, explicit recovery-required blocking, and supported-filesystem staged/forced/atomic replacement. Declared source scope is hash-bound and independently rescanned under lock; Java classpath entries, prospective outputs, local JAR directories, and declaration files are content-fingerprinted and revalidated before journaling. Same-file coordinates, cross-entry overlaps, complete preflight rendering, and line/character bounds are now enforced. Managed Java flows use central staged JDT diagnostics gates, and direct apply requires an explicit gate. Auditable approval, error mapping, and daemon refresh are implemented. Native LSP writes are version-checked and explicitly client-managed; recipes use one staged managed transaction.
|release blocker; remaining and partially evidenced `TX` findings require closure plus destructive fault acceptance

|Authoritative Java/Maven diagnostics are not yet claimed complete
|The current syntax/compliance, Build Model, diagnostic-gate, and platform-attestation precursors do not by themselves prove full-reactor, source-set, release-specific API authority. False JDK, external, or sibling-reactor diagnostics would invalidate managed semantic authority even when Maven later succeeds.
|planned, product-critical release blocker (`RPK-JAVA-DIAG-001..007`)

|General and field Maven move-class authority remains unaccepted
|The permanent 20-active-module fixture has twelve implemented-and-validated definitions and 22 expanded cases, including six-case REQ-008. That closes the bounded feature catalogue only. The second packaged Cucumber mode is locally observed solely for the same existing REQ-001 pickle on Linux `amd64`; it adds no case and does not cover REQ-002..012. The final source-built whole-tree and targeted custom-source-set static gates passed, with the packaged task separately reported and the static classification intentionally non-zero at 99 reviewed/non-blocking packaged glue/harness findings; no production source was added or modified by this slice, and aggregate cleanliness plus numerical/child-JVM coverage are not claimed. All four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`; wiring/report uploads are implemented but unexecuted, and Windows/macOS logic is configured/static-reviewed only. The exact Magrathea `a48e3c06` request remains CLI/Linux/JDK-21-specific, and no broader integration shape, native matrix, or general field population is qualified.
|planned P0 product-critical generalization blocker (`RPK-JAVA-MOVE-001..007`); twelve bounded definitions/22 cases, one exact CLI field request, and one local Linux `amd64` packaged REQ-001 mode closed only for their evidence; broader packaged/runtime/native/general qualification remains open

|Managed rollback qualification remains bounded
|The shared core executor is implemented, its CLI/daemon/LSP/MCP adoption is source-inspected, and REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 pass all eight focused in-process cases. RED evidence proved the mutable-diagnostics alias, and detached unmodifiable ordered snapshotting eliminated its three `Refused` EI/EI2 findings. The historical rollback-scoped whole-working-tree source-built gate also passed, but it is aggregate evidence and does not execute packaged surfaces or qualify cross-platform behavior, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority. Static findings are not zero: seven intentionally reviewed/non-blocking core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), as do 25 non-blocking glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). Double lookup, visibility normalization, guard reordering, record reconstruction, journal-failure collapse, mutable diagnostic aliasing, surface-policy migration, or any transfer of `PatchEngine` authority would still break the behavior-preserving boundary.
|mitigated for the bounded source-built/in-process slice; reviewed static findings remain intentionally non-blocking; needs-review before any packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or broader surface-execution claim

|Managed-apply gate-selector qualification remains bounded
|The shared selector is implemented in `refactorkit-jvm`, adopted at the daemon/MCP managed-apply call sites and bounded `LspSession.applyPlan`, and independently passed all 11 focused scenarios/92 steps; daemon and MCP regressions passed 58/58 and 26/26. The final whole-working-tree source-built gate also passed. Explicit final-segment non-null handling eliminated the initial selector production NP finding while preserving root-path `NullPointerException` and exact `.java`/`.JAVA` behavior; the final full log has no selector/provider-function production finding. Twenty-three glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. This establishes source-built/in-process selector behavior, source-inspected daemon/MCP adoption, and implemented-and-validated bounded source-built/in-process SURFACE-005 LSP adoption. CLI, recipes, testkit/direct library, all other LSP behavior, daemon post-success diagnostics, and surface lifecycle/rendering remain outside the selector's responsibility. External transport/editor integration, packaged/native/platform execution, concurrency/crash behavior, general Maven projects, release, broad apply authority, and numerical coverage are not established; `PatchEngine` remains authoritative.
|mitigated for the bounded selector and implemented-and-validated source-built/in-process LSP adoption; reviewed glue-only findings remain non-blocking; every broader execution/authority claim remains open

|Workspace Snapshot Composition qualification remains bounded
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` and all eight cases are implemented-and-validated for the exact shared-service, collaborator-call/identity, ordering, failure-fidelity, and negative-ownership contract. Final focused source-built/in-process acceptance passed 8/8 scenarios and 65/65 steps; source inspection confirms only private daemon/MCP adoption, and their regressions passed 58/58 plus 26/26. The final dated mutable whole-working-tree source-built aggregate passed with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). It reports no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption, while 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC) and the broader configured baseline remains non-blocking. REQ-005 is source-inspected integration evidence rather than packaged surface execution. CLI/LSP, lifecycle/index/`PatchEngine`/protocol authority, packaged/native/cross-platform execution, concurrency, crash/restart, numerical coverage, and general orchestration remain unqualified.
|mitigated for the bounded composer contract, daemon/MCP module regressions, dated source-built aggregate, and reviewed non-blocking static findings; broader execution, concurrency, crash-restart, coverage, and orchestration qualification remains open

|Stable acceptance evidence incomplete
|API `1.0`, Java 8–25, diagnostics, protocol classification, packaging, supply-chain, reproducibility, and narrowed-or-evidenced transactionality claims cannot be marked stable until the immutable RC acceptance matrix passes.
|planned release blocker
|===
