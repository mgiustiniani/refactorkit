= 11. Risks and Technical Debt
:toc:
:sectnums:

Completion-program status: the local P0 all-module `testClasses` scaffold admission gate is resolved by an isolated build-only correction and bounded OpenJDK 21 evidence. New K5/T5/recipe/release rows, corrected-revision CI, and all other unqualified work remain open; existing bounded evidence is preserved.

Status: implementation-informed. Tagged acceptance closes all twelve bounded Maven move-class definitions and all 22 expanded cases. Existing evidence for `001`-`007` and `009`-`012` remains independently bounded. REQ-JAVA-MAVEN-MOVE-AUTH-008 now closes its four immutable preview-guidance cases, one root-declared active-POM structural refusal, and one under-lock required-file-evidence drift refusal. Focused OpenJDK 21 evidence passed six selected cases and skipped sixteen with zero failures/errors. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. The exact Magrathea `a48e3c06` request remains CLI/Linux/JDK-21-specific. Existing REQ-001 now also has one implementation-informed local Linux `amd64` packaged-process observation on the mutable tree; REQ-002..012 packaged execution, runtime-distribution claims, broader integration/native matrices, general authority, and non-Java/string handling beyond bounded evidence remain open. All four post-commit native CI ledger rows remain `CONFIGURED_UNOBSERVED`. The historical PendingPlanStore-scoped whole-working-tree verification used OpenJDK 21.0.11 on 2026-07-31 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`. SpotBugs retained its configured non-blocking baseline and reported no `PendingPlanStore` findings. That historical scoped receipt is dated mutable source-built aggregate evidence, not packaged/concurrency/general-extraction or numerical coverage qualification. For managed rollback, a focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`), and detached unmodifiable ordered snapshotting corrected it. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical managed-rollback-scoped whole-working-tree verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). The earlier REQ-008- and PendingPlanStore-specific receipts remain historical evidence for their explicitly stated scopes. Managed-rollback evidence is dated mutable source-built aggregate evidence and does not qualify packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority. For the historical post-selector tree, the selector-scoped OpenJDK 21.0.11 `./gradlew --no-daemon --rerun-tasks check goldenTest` verification reported `BUILD SUCCESSFUL` in 7m 11s, 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors, and 78 tasks (log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`). The selector's initial production NP finding is eliminated; its production file/provider functions have no final-log finding, while 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC) and the broader configured SpotBugs baseline remains non-blocking. Only the dedicated REQ-008 contract risk is closed below; broader authority and qualification risks remain.

For Workspace Snapshot Composition, final official whole-working-tree verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 7s; 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors; 136 XML files; 78 tasks executed; log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`. The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption. Static findings are not zero: 21 slice-local glue-only Kotlin/generated SpotBugs findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. This is dated mutable source-built aggregate evidence alongside the separately preserved focused source-built/in-process acceptance, daemon/MCP regressions, and Gherkin audit; it does not qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior. The preceding selector whole-tree receipt remains historical for its scope.

For the Java Refactoring Preview Command boundary, the first independent review correctly blocked promotion while REQ-JAVA-PREVIEW-COMMAND-003 was source-only. The corrected split-runner evidence now exercises the production command/dispatcher/default-planner path and real public daemon/MCP session dispatch: all four definitions/eight cases/94 steps passed (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`), and independent Java/daemon/MCP regressions passed with zero failures/errors (log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`). The official six-feature audit passed with SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`. Final dated mutable whole-working-tree source-built verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 6m 54s; 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors; 140 XML files; 79 tasks executed; log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`. The targeted affected-module static command passed (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). The static classification is deliberately non-zero: the full targeted log contains no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers; exactly 48 reviewed/non-blocking slice-local glue findings remain—Java-module command glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN, 4 BC). Dispatcher-method references arise from generated Kotlin nullability analysis at glue call sites, not production methods. Wider configured SpotBugs/PMD baselines remain non-blocking; aggregate static cleanliness and numerical coverage are not claimed. This is dated mutable source-built aggregate evidence complementing focused source-built/in-process acceptance, not packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage qualification.

REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001 mitigates only the exact authority-loss gap between a retained module-rename plan and the shared managed-apply selector. Its final official JDK 21 focused receipt passed one scenario, all 25 scenario steps plus six Background steps (31 expanded), and two hooks, with no failures, skips, or undefined results (log SHA-256 `4e826737d94150bcebe0a38d3fee970e84339532f3fe4ef07189da2506bdedef`; Cucumber JSON SHA-256 `2cf337ab1f963b133222b941df0fd577b9518794e60d23dde81390ba6e760a4c`), and independent review returned `PASS_FOR_PROMOTION`. The subsequent required whole-tree gate was authoritative for that promoted tree and remains historical: `BUILD SUCCESSFUL` in 7m 31s with 82/82 tasks; standard `test` XML only, excluding the intentionally separate stale packaged Maven task, reconciles 146 files, 1,068 discovered, 835 executed, 233 skipped/tag-filtered, and zero failures/errors (log SHA-256 `663ddcf3cb836fa2058d65160b57df19202b2f1908a0331faeb96ee6ca66209d`; XML manifest SHA-256 `bca4e1f718f56ce3095ef9168267b42506f467ec5b1046408e764b1948502905`). Static analysis remains configured non-blocking, and no aggregate numerical coverage is claimed. Earlier RED, implementation, and focused receipts remain historical. Those SURFACE-001 and prior all-implemented receipts remain historical for their exact revisions. Under immutable baseline `REQ002-WHOLE-TREE-REGRESSION-001` SHA-256 `387520937b1fc2922b146652ed18b545b508cd33d95189806178d3b5fb91bbfe`, repository-wide and repeated focused RED logs (SHA-256 `8076201a19fad44076b810b7555859606a5717ff03cff97037fe392fcff10914` / `13497ab251fff223dcb8c490fd96c09c8ef1a09a81aad70dfa732378e9059b04`) exposed a feature/glue reconciliation regression: SURFACE-003's Background correction removed the child-process/socket observation step, and SURFACE-002 lacked a local copy, leaving `authorityObservationRequested=false`. The feature-only correction restored that exact step inside SURFACE-002 without production/glue/build changes; independent requirements review returned `PASS` and authorized `@partial` to `@implemented-and-validated`. The status-only promotion preserved the scenario-local step and produced current feature SHA-256 `1c29ad27146f06072c9815a1e056e2702cd25b8975ea2f0307385091e9859ad3`. Final post-promotion focused evidence at `docs/requirements/evidence/req002-post-promotion-focused-green-aee52f1114259413d6d6c77d384c6b30ed5afd82ab6b8c3d92921eb4bea367a2.log` was `BUILD SUCCESSFUL`; Cucumber JSON SHA-256 `4db1de3fca326ea9067a0d6653570c5fcf090a266e142cb9d27c854afd191b97` records one case and 34/34 expanded results: 29 scenario plus five Background steps. Final whole-tree JDK 21 evidence at `docs/requirements/evidence/req002-post-promotion-whole-tree-final-34b45bc447938de5bc5ce2c6351bdb7feda4e4403642e6add9d3e31cd7a044b0.log` was `BUILD SUCCESSFUL` in 12m 47s with 87/87 tasks; 155 standard-test XML files reconcile 1,096 discovered, 844 executed, 252 skipped/tag-filtered, and zero failures/errors. Static analysis emitted findings under its configured non-blocking policy, so no clean-static or numerical-coverage claim is allowed. Current official Gherkin reconciliation reports 37 definitions/82 cases and 614 declared/1,208 expanded steps across two Backgrounds/10 steps; all definitions and cases are implemented-and-validated. Earlier RED and pre-promotion GREEN receipts remain historical. Every explicit external-editor/transport, package/platform, concurrency/crash, general-Maven, numerical-coverage, and release/broad-support exclusion remains risk, not implied progress.

[cols="1,3,2,2,1",options="header"]
|===
|ID |Risk / debt |Impact |Mitigation / next action |Status

|R-001
|Compiler-backed Java analysis is implemented only for selected JDT-backed slices; other operations and explicit fallback paths still rely on lexical or structural evidence.
|Refactorings outside the supported signed-method and unambiguous-field `renameMember` slices (including exact annotation-element rename), type-level rename (including annotation types), move-class file-scoping, safe-delete type-reference, annotation-type/element indexing and reference, and exact-import use slices may miss reflection, generated code, annotation processors, external callers, and some framework/string references. Arbitrary unsigned methods retain lexical behavior. Type-level rename, move class, and safe delete can produce review candidates with lexical false positives when JDT analysis is not clean or the exact target binding is unavailable; `LEXICAL_FALLBACK` is review-only and the validated immutable-evidence refusal before workspace-lock acquisition must not regress. Unresolved organize-imports files retain only their separately classified structural cleanup.
|Follow ADR 0008; classify every stable path as JDT-backed, narrowly proven non-semantic behavior, or refusal. Do not promote prototype lexical fallbacks that can perform an incorrect semantic edit; preserve the independent pre-lock `PatchEngine` evidence gate, external-family and collision refusals, and operation-specific acceptance. Retain REQ-JAVA-MAVEN-MOVE-AUTH-005 for the CLI core-gate path and REQ-JAVA-MAVEN-MOVE-AUTH-004 for the distinct bounded cross-surface lexical-review projection/refusal path.
|known risk

|R-001A
|Offline Maven effective-model analysis cannot resolve a selected node's parent, BOM, artifact POM, management/mediation/relocation input, or binary when it is absent from both the reactor and configured local repository.
|A missing descriptor input on an included edge makes graph, closure inclusion/exclusion, and source-set authority unavailable. This differs from a child below an already deterministic scope/optional/exclusion-pruned edge, which is not a graph node on that path. A missing selected binary with complete descriptor closure keeps global classpath authority unavailable and may conceal a target candidate unless the narrow operation proof passes.
|Keep source-set-scoped typed roots and suppress only causally derivative binding noise. Require every active reactor POM, descriptor closure for every selected external node, and hash-bound selected/pruned edge classifications. Stop below only deterministic pruned external edges; alternate selected paths restore requirements. Retain implemented ordinary selected-leaf acceptance `011`: RefactorKit-owned fixed-release Maven-2-layout no-follow negative presence, complete present POM closure, candidate/JDT/lookup/diagnostic/lease gates, and no invented absent-content hash or provided-type inventory. Retain implemented six-case `012`: engine-captured descriptor context and parsed relocation/model identity, stable typed refusal before semantic selection/lock/WAL, and no missing-binary downgrade or residue. Network remains explicit opt-in, anonymous, and credential-free.
|qualified limitation; bounded `007`/`009`/`010`/`011`/`012` rows implemented and the exact Magrathea CLI request field-qualified; general, integration-surface, and native-matrix authority remain open

|R-001B
|A partial child/module model, module-wide availability handling, or an unqualified Java platform can produce false missing JDK, sibling-reactor, or external types—or silently accept APIs newer than Maven `--release`.
|RefactorKit diagnostics cease to be trustworthy semantic product evidence; managed refactoring may be incorrectly blocked or, worse, granted authority from false-negative bindings. Maven success cannot repair this defect.
|Deliver `RPK-JAVA-DIAG-001..007`: declared full-reactor modeling before module filtering, independent main/test environments, exact release signatures, typed affected-environment unavailability, exact delta authority, and permanent packaged acceptance.
|planned, product-critical release blocker

|R-001C
|Effective dependency mediation could regress to overwriting an explicitly declared transitive `test` scope with a dependency-management default.
|The model could leak test-only visibility into main, omit a legal test observer, or derive the wrong reverse-observer/impact closure and then make an invalid authority decision.
|Retain declaration provenance: preserve an explicit transitive `test` scope and use dependency-management scope only when the dependency declaration omits it. Keep REQ-JAVA-MAVEN-MOVE-AUTH-009 as the bounded source-set, closure-isolation, preview/apply/rollback regression.
|bounded implementation and acceptance closed by REQ-JAVA-MAVEN-MOVE-AUTH-009; broader authoritative-diagnostics acceptance remains planned

|R-001D
|Maven traversal can over-constrain graph completeness by looking up a child version, repository path, POM, or JAR after effective scope, transitive optionality, or an exact path exclusion has already deterministically pruned that edge. The opposite error—pruning on ambiguous facts or type/classifier alone—can hide an included subtree.
|Over-enumeration creates false offline blockers and may trigger unintended lookup; under-enumeration can conceal an observer or required descriptor and grant false authority.
|Retain the independent REQ-JAVA-MAVEN-MOVE-AUTH-010 regression: six omitted-scope/optional/exclusion children produce 18 deterministic `PRUNED` projection records before child POM/JAR/network lookup; an alternate selected path restores descriptor requirements and an absent POM refuses before lock/WAL. Retain independent `012` regressions for selected leaf POM/model, parent, imported BOM, management/mediation, and parsed relocation/model identity loss or drift, including stable hashes and no binary-absence downgrade. Extend selector acceptance beyond the bounded fixture without weakening path/projection provenance, type/classifier identity, ambiguity refusal, or plugin/settings-credential/implicit-network and lexical-promotion prohibitions.
|bounded P0 risk closed by implemented-and-validated `010`/`012`; generalization remains open

|R-002
|`organize-imports` removes unused exact imports only when per-file JDT evidence is clean; wildcard imports are preserved and unresolved files retain lexical sorting, deduplication, and same-package removal.
|Users may expect IDE-equivalent wildcard analysis and unused-import cleanup even with incomplete classpaths or unresolved source.
|Keep wildcard and unresolved-file behavior explicit and conservative; broaden classpath robustness and wildcard-use analysis before claiming complete IDE-equivalent cleanup.
|known limitation

|R-003
|`extract-method` MVP only supports no-argument `private void` extraction and refuses many cases.
|Limited usefulness for real-world extraction scenarios.
|Keep refusals explicit; expand only with robust variable/control-flow analysis.
|known limitation

|R-004
|Change-signature is conservative and refuses hierarchy/public/protected/generated/method-reference cases.
|Limited support for public API evolution.
|Add compiler-backed hierarchy/call-site analysis or retain the documented limited shape and conservative refusal at stable.
|known limitation

|R-005
|Safe delete uses exact JDT bindings only for clean source analysis and does not inspect build files, generated code, reflection, external config, or all framework references.
|Possible unsafe delete if non-source references exist; lexical fallback may also conflate same-simple-name types when semantic evidence is unavailable.
|Maintain framework and forced-delete warnings; for stable classification, prove any narrow lexical behavior safe or refuse when exact evidence is unavailable. Add build/config/framework analysis before lowering risk.
|known risk

|R-006
|External importer license detection is heuristic.
|Unknown or incompatible code could be imported if user ignores warnings.
|Keep preview-only unknown-license behavior; consider policy enforcement and SPDX scanning.
|known risk

|R-007
|External LSP-backed support is implemented as a bounded proposal provider; `v0.6.0` qualifies TypeScript semantic reads and managed identifier rename, while other languages and advanced TypeScript operations remain future work.
|Users may overgeneralize the qualified TypeScript slice into stable arbitrary multi-language mutation authority.
|Keep capability evidence and mutation authority operation-specific; preserve exact diagnostics, refusal boundaries and the published support matrix.
|known limitation

|R-008
|ADR status and stable capability classification can drift from contracts and implementation evidence.
|Reviewers or clients may mistake implemented prototype behavior for an accepted stable guarantee.
|Keep chapter 9 synchronized with ADR bodies and require contract snapshots plus RC acceptance before stable classification.
|known risk

|R-009
|The stable plan freezes core patch, CLI, and daemon API `1.0` contracts, but the in-process library export boundary is not yet inventoried.
|Embedding applications may depend on internals not covered by the stable integration contract.
|Publish an explicit stable library surface or classify in-process APIs as non-stable before RC acceptance.
|planned release review

|R-010
|Self-contained assets are natively qualified for Linux x86_64, Windows x86_64, macOS x86_64 and macOS arm64; other architectures and artifact repositories remain undefined.
|Consumers may assume portability beyond the four verified archives or mistake unsigned assets for platform-signed publishers.
|Publish the exact matrix and verification commands; classify other targets as unsupported and retain `TD-DIST-001` signing disclosure.
|controlled limitation

|R-011
|Security threat model is incomplete.
|Trust boundaries around MCP clients, external code sources, external language servers, and filesystem edits need review.
|Create threat model and security ADR/quality scenarios.
|needs-review

|R-012
|Runtime/dynamic and C3 diagrams are missing.
|Architecture documentation may be less useful for maintainers working on internal interactions.
|Add scenario-driven dynamic views and C3 component views when needed.
|actionable

|R-013
|Class rename uses exact JDT bindings only when source analysis is clean and the exact target binding is available; reflection, generated code, framework strings/configuration, and unknown external callers remain outside binding-matched source edits.
|A rename may require manual follow-up outside analyzed source, while lexical fallback may conflate same-simple-name types.
|Retain reflection/framework/generated-code warnings, conflict refusals, and same-simple-name binding tests; stable classification must prove any narrow fallback safe or refuse without clean binding evidence.
|known risk

|R-014
|The 2026-07-29 field work first exposed a complex Maven shape whose detached Magrathea preview remained `LEXICAL_FALLBACK`. After descriptor-closed pruning under REQ-JAVA-MAVEN-MOVE-AUTH-010, one clean Magrathea commit/request was qualified through the source-built CLI on Linux/JDK 21. Permanent-fixture `001`/`002` retain their semantic and observer-discrimination evidence; `003` adds four isolated safely enumerable non-managed guidance variants through the Java dispatcher/source-built CLI; `004` adds one cross-surface lexical-review projection/refusal scenario through source-built/in-process handlers; and `011`/`012` retain ordinary-missing and selected-descriptor acceptance.
|The exact Magrathea request remains commit-, request-, CLI-, Linux-, and JDK-21-specific. Fixture rows are likewise bounded 20-module shapes. The dedicated REQ-001 runner now qualifies one local Linux `amd64` unpacked-package process sequence only; REQ-004 and REQ-008 still qualify neither packaged executables nor stdio/editor deployments. No evidence qualifies a native CI matrix, REQ-002..012 package paths, other field projects, or general target-scoped authority. Reflection, configuration, broader structural loss, generated output beyond bounded inventory, non-Java strings beyond reported residuals, and external consumers remain residual risks.
|Retain all twelve tagged regressions, including the 38-step `004`, six inherited `008`, and six inherited `012` cases. Preserve REQ-008 closure-blocker precedence, expected-only semantic candidate-inventory vocabulary, expected/observed required-file-evidence hashes, external `S1`, pre-WAL refusal, and lock-file-residue allowance. Retain the dedicated REQ-001 package-process regression and expand qualification only through separately scoped package/runtime rows and exact post-commit native CI receipts.
|P0 generalization release blocker remains; REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` are implemented and validated only for bounded rows/cases, the exact Magrathea `a48e3c06` CLI/Linux/JDK-21 request remains separately field-qualified, and one local Linux `amd64` REQ-001 package-process sequence is implementation-informed without native-ledger promotion

|R-014A
|A non-managed outcome could be modeled as an empty-edit `PatchPlan`, nullable wrapper, or generic protocol preview and thereby acquire a `PlanId`, pending-store entry, client edit, or apply path.
|REQ-003's bounded Java/CLI guidance type prevents that representation for four safely enumerable authority defects. REQ-004 now projects broad legacy lexical fallback from an internal compatibility preview to a distinct operation-specific `LexicalReview` outcome before generic handling. The residual risk is regression or overgeneralizing this bounded Java move-class boundary to unrelated adapters/shapes.
|Keep all three `JavaMoveClassOperationOutcome` arms disjoint. Preserve the schema-v1 envelope's `NOT_SEMANTICALLY_PROVEN` disposition, hidden compatibility-artifact hash, bounded path/hash/risk records, no protocol plan/edit/range/replacement/apply token, bounded lifecycle-cleared audit cache, exactly-one identity parser, and core compatibility gate. Add equivalent operation-specific admission before any future surface exposes another non-managed result.
|closed for bounded REQ-003/004 Java move-class outcomes; ongoing regression/generalization risk

|R-014B
|The EARLY REQ-JAVA-MAVEN-MOVE-AUTH-003 source used undefined hash-age wording, the wrong generated-root owner, and an ambiguous boundary with structural loss/post-preview drift.
|Those defects could have made acceptance bless guidance where fail-closed refusal was required.
|The feature uses expected/observed fingerprints, identifies `catalog-generated-support:main`, requires readable/safely contained/bounded inputs, and keeps REQ-003 independent from REQ-008. Four default-scanner cases validate only `003`; six separate cases now validate the corrected `008` matrix without broadening `003`.
|closed for bounded REQ-JAVA-MAVEN-MOVE-AUTH-003; REQ-008 remains independently bounded rather than counter-evidence

|R-014C
|The prior direct-planner admission defect allowed daemon/MCP pending lexical plans, recipe aggregation, and an LSP client-managed lexical `WorkspaceEdit`. Current handlers route Java `moveClass` through `JavaMoveClassOperationDispatcher` before generic handling.
|The bounded defect is closed: lexical preview returns the immutable envelope, daemon/LSP/MCP create no pending plan, LSP emits neither `changes` nor `documentChanges`, recipe returns `NonManaged` with no aggregate, and the internal random plan ID/edit stays hidden. A remaining risk is regression or assuming this in-process acceptance qualifies packaged transports/native editors.
|Retain `JavaMavenMoveClassLexicalFallbackReviewCucumberTest`: equal cross-surface projection, promotion-metadata neutrality, no protocol plan/edit capability, LRU bounds/lifecycle clearing, known-operation structured refusal, exactly-one identity parsing, unknown/evicted identity invalidation, no residue, and clean-JDT preservation. Add packaged stdio/editor/native matrices separately.
|closed for bounded REQ-JAVA-MAVEN-MOVE-AUTH-004; packaged/runtime/native integration qualification remains open

|R-014D
|The earlier REQ-JAVA-MAVEN-MOVE-AUTH-008 outline incorrectly used one pre-lock/no-residue contract for six different evidence phases and overlapped REQ-003/004/010/011/012.
|The corrected feature and implementation now keep four complete preview guidance cases, one root-declared active-POM structural preview refusal, and one under-lock pre-WAL non-managed candidate-source evidence drift refusal distinct. Closure consistency is primary over generic unresolved classification; no case is `LEXICAL_FALLBACK_REVIEW`; external `S1` is preserved; the final case may leave the lock file. Generic immutable `OperationAuthorityFileEvidence` and `DiagnosticDetails` report expected-only semantic candidate-inventory identity plus expected/observed required-file-evidence identities, never an observed semantic inventory.
|Retain the focused six-case runner and exact exclusions. Retain the independent final mutable-working-tree receipt from OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. It does not widen packaged-process/runtime/native-platform/general authority or provide numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Treat any future broader structural/freshness shape as new evidence rather than inference.
|closed for the bounded corrected REQ-JAVA-MAVEN-MOVE-AUTH-008 contract; broader authority, coverage, packaged/runtime, and native risks remain open

|R-015
|Daemon capability metadata is maintained alongside the request dispatcher and can drift from implemented method behavior.
|Clients could make incorrect compatibility, project-lifecycle, or workspace-write assumptions from stale descriptors or safety flags.
|Keep the capability contract test aligned with every documented daemon method; prefer deriving dispatch and discovery from one method registry if the surface grows.
|known risk

|R-016
|Annotation-element support is limited to JDT-backed read-only lookup/references and clean exact signed rename of the zero-parameter element declaration and named usages. Implicit single-element values have no name to edit; annotation-element change-signature and default-value editing are not implemented.
|Users could infer broader annotation-member or annotation-value editing from successful signed element rename.
|Keep the exact supported boundary explicit; retain binding-based same-name disambiguation tests and add dedicated semantic evidence before advertising change-signature or default-value editing.
|known limitation

|R-017
|Exact field rename depends on an unambiguous `Owner#field` selector and clean global JDT evidence; unclean or unresolved prototype analysis retains lexical member fallback, and non-source references remain outside binding ranges.
|Fallback may rename shadowing or unrelated same-named identifiers, and callers could overgeneralize field evidence to arbitrary unsigned methods.
|For stable classification, prove a narrow lexical contract cannot perform an incorrect semantic edit or replace it with refusal; preserve collision and binding-disambiguation tests.
|known risk

|R-018
|JDT grammar/compliance and compiler release flags do not by themselves provide the exact Java SE API/module surface for Maven `--release 8..25`; the reduced packaged jlink runtime can also omit standard modules needed by an analyzed project.
|Diagnostics may falsely reject valid APIs such as `java.sql`/`java.net.http` or silently accept APIs introduced after the configured release, invalidating semantic authority.
|Provide immutable licensed/SBOM-covered release signatures, bind their identity into every authoritative result, refuse absent evidence, and pass release-8/11/21 plus full 8-25 packaged qualification under `RPK-JAVA-DIAG-001/007`.
|planned, product-critical release blocker

|R-018A
|Named JPMS/module-path Maven reactors are not in the first authoritative diagnostics slice.
|Flattening them onto the classpath could hide module readability, exports, automatic-module, or split-package failures; refusing them limits supported projects.
|Return typed unsupported/unavailable without mutation authority until a dedicated complete JPMS model and acceptance slice is approved.
|accepted scope boundary; planned dedicated slice

|R-019
|Managed Java and TypeScript flows enforce before/staged/apply diagnostics gates, but diagnostic DTO fields and all-language API `1.0` semantics are not frozen.
|Pre-1.0 clients could interpret future category/schema evolution as a compatibility break.
|Retain operation-specific regression gates and freeze diagnostic fields/categories only with the explicit API `1.0` migration.
|known API debt; not a `v0.6.0` safety blocker

|R-020
|Numeric protocol/process/document/result/journal budgets and reference stress tests are implemented, but OS-level RSS isolation is not provided.
|V8 old-space limits do not bound total process RSS, and future larger workspaces may require budget revision.
|Retain deterministic limit errors, stress/cancellation/timeout acceptance and document the lack of an OS RSS sandbox; add platform isolation only under a separate qualified provider policy.
|known resource limitation

|R-021
|The published `v0.3.0` asset has checksum and downloaded-artifact smoke evidence, but SBOM, provenance/attestation, clean-build reproducibility, and promoted stable-RC evidence are not yet recorded.
|The `v0.3.0` package is independently checksum-verifiable and locally runnable, but a later stable package could still be non-reproducible or inconsistent with release metadata.
|Verify two clean builds and the downloaded immutable-RC asset, documenting unavoidable reproducibility limits before stable publication.
|planned stable-release blocker; `v0.3.0` checksum/smoke path implementation-informed

|R-022
|LSP and MCP surfaces are implemented but not yet completely classified as stable or experimental.
|Clients may depend on untested protocol behavior, and MCP privilege or cancellation boundaries could be overstated.
|Inventory every capability, command, tool, resource, and prompt and pass stable protocol boundary suites.
|planned release blocker

|R-023
|PatchEngine preflight rejects selected structural/path/overlap errors, symbolic-link traversal, and predictable invalid ordered file-state transitions. Managed writes hold a workspace lock, revalidate affected paths, durably journal pre/post intent, stage all content, require atomic replacement, and flush files/directories (`TX-001`, `TX-002`, `TX-005`, supported-filesystem `TX-006`, and declared-source/classpath `TX-014` closed).
|Unsupported root capability is refused and per-target atomic-move failure is recoverable. Deterministic faults, real process kills at workspace/WAL boundaries, raw torn-byte quarantine, and conditional distinct-store WAL lifecycle evidence are implemented. Packaged Linux/Windows/macOS acceptance also kills a daemon during a 256-file TypeScript transaction after the durable `APPLYING` record and first committed image, then verifies exact explicit restart compensation to `ROLLED_BACK`; slow but advancing staged-file preparation extends only a bounded inactivity window under a separate absolute deadline. Actual power loss, hostile post-validation artifact mutation, and editor-managed LSP writes remain outside complete evidence.
|Retain engine-owned scan scope, versioned checksums, blocking quarantine, fault/kill/restart, and mounted-filesystem acceptance evidence; add power-loss infrastructure where the CI environment permits.
|planned release blocker

|R-024
|Transaction-log identifier/path hardening, atomic durable persistence, schema-v2 through v8 checksums/migration, corruption quarantine, lifecycle metadata, capability reporting, raw truncation, real process-kill, and cross-store evidence are implemented (`TX-011`, `TX-012`).
|Validated UUIDv4 IDs, containment, symbolic-link/non-regular-file refusal, owner-only permissions, and blocking quarantine prevent the audited traversal/corruption paths; physical devices that falsely acknowledge successful force operations remain outside the qualified software contract.
|Preserve checksum/migration/quarantine and durable lifecycle evidence; retain explicit filesystem qualification and add hardware power-loss evidence when dedicated infrastructure permits.
|remaining journal integrity and release blocker

|R-025
|Versioned rollback verifies post-images and restores content, modes, timestamps, owner/group, user attributes, and transaction-created parents; strict UTF-8/BOM behavior is explicit (`TX-004`, `TX-013`). Java classpath evidence is fingerprinted before apply.
|Legacy image-less records require manual recovery; platform ACLs are restored where exposed and fail closed otherwise; explicit directory operations remain unsupported; a hostile writer can still mutate external classpath state after preflight validation outside the cooperative lock boundary.
|Keep forced/manual recovery, fail-closed metadata capability behavior, and unsupported directory operations explicit.
|critical data-loss risk; planned release blocker

|R-026
|Admitted native LSP edits are explicitly client-managed/no-rollback; full-sync buffer overlays, strictly increasing versions, versioned `documentChanges`, capability refusal, and managed-write open-buffer guards close `TX-007`.
|Client-owned application still depends on the editor honoring LSP version preconditions; it intentionally has no RefactorKit transaction record. Lexical-fallback `moveClass` has no client-edit authority and now exits through `LexicalReview` before the WorkspaceEdit renderer.
|Retain ownership/version contract tests and the REQ-004 admission regression proving `LEXICAL_FALLBACK_REVIEW` contains neither `changes` nor `documentChanges`, enters no pending-plan map, and returns known-operation refusal before editor application. Qualify packaged editor/native behavior separately.
|controlled for admitted edits and bounded lexical no-edit command path; packaged/native editor integration remains open

|R-027
|Recipe steps are staged against one evolving immutable snapshot and reduced to one recipe-wide managed commit (`TX-008`, `TX-009`, `TX-010` closed).
|Later planning failure now leaves workspace and journal untouched; successful apply has one WAL record and conflict-safe rollback boundary.
|Retain tests for dependent steps, multi-class package moves, no-op execution, and later-step refusal as transaction-boundary evidence.
|controlled

|R-028
|Core apply requires an explicit staged diagnostics gate; managed Java surfaces use JDT (`TX-015` closed). `TX-016` through `TX-018` are closed.
|A caller can deliberately pass a disabled gate, but this choice is explicit and identifiable rather than an implicit production default.
|Document disabled-gate use as unsupported for stable managed Java flows and retain staged-regression tests.
|planned release blocker

|R-029
|The shared pending-plan store now replaces the three duplicate maps, but the daemon/LSP/MCP admission and removal lifecycles remain intentionally asymmetric. Regression risk remains if later work changes access order, shares state between sessions, drops surface-owned payload metadata, normalizes snapshot/apply/refusal/rollback behavior, merges the lexical-review audit cache, treats plan identity as authorization, or moves apply/WAL work into the store. The store is intentionally unsynchronized under serialized session ownership, and current acceptance does not qualify concurrent callers.
|Observable protocol compatibility and retry behavior could change; the wrong plan could survive or be evicted; authority metadata could be lost; or a supposedly in-memory removal could acquire write/transaction side effects. Source-built/in-process evidence cannot establish packaged transport behavior, concurrency safety, or a reusable general service-extraction pattern.
|Retain the generic insertion/lookup/remove/clear boundary in `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt` and the separate lifecycle calls in the exact `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt` production paths. Implemented-and-validated REQ-PENDING-PLAN-STORE-001/002 characterize the 128-entry true access-order contract, exact payload/session isolation, current MCP importer exception, asymmetric lifecycle matrix, existing responses, and no direct write/lock/WAL/transaction/rollback effects. Treat concurrency support, lifecycle normalization, protocol/authority changes, or apply/rollback centralization as separately approved and separately qualified work.
|mitigated for the bounded source-built/in-process slice: two scenarios/19 steps passed on OpenJDK 21.0.11, log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`; subsequent 2026-07-31 whole-working-tree source-built verification reported 1002 discovered, 782 executed, 220 skipped/tag-filtered, and zero failures/errors (77 tasks; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`) and had no `PendingPlanStore` SpotBugs findings; packaged, concurrency, general extraction, and numerical coverage qualification needs review

|R-030
|The implemented shared rollback executor could regress by normalizing intentionally different surfaces: use `load` or multiple lookups instead of one full-record lookup plus visibility, expose non-`APPLIED` records to CLI/LSP/MCP, hide them from daemon, invoke the LSP guard before visible resolution or after lock acquisition, reconstruct/reload the record used for outcomes, call `PatchEngine.rollback` more than once, collapse journal exceptions into absence/refusal, reintroduce mutable aliasing of refusal diagnostics, or absorb mode, rendering, refresh/diagnostics, pending/audit lifecycle, authorization, WAL/recovery, or rollback policy. Consumers could also misread the retained pre-lock record as current post-call journal state. The five exact `TransactionLogException` identity warnings and two private injected-collaborator retention warnings must not be “fixed” by breaking REQ-MANAGED-ROLLBACK-EXECUTOR-002 identity fidelity or executor ownership.
|Observable not-found/error behavior, document-safety ordering, daemon inverse changed-file reporting, retry/recovery behavior, and mutation safety could change. Double lookup creates a race; lost exception identity/code/message/cause obscures corruption/quarantine/durability failures; duplicated rollback calls can cause destructive or misleading lifecycle effects; treating the outer record as current can produce false audit claims; and mutable diagnostics could change an already returned outcome. Focused in-process and aggregate whole-working-tree source-built evidence cannot establish packaged or cross-platform surface behavior, concurrent rollback, crash/restart recovery, general rollback authority, numerical coverage, or altered `PatchEngine` authority.
|Keep the Chapter 8 boundary and implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 exact: one `loadRecord`, explicit `APPLIED_ONLY` versus `JOURNAL_RECORD`, guard after visibility and before lock, one public engine call, distinct `JournalLookupFailed`/`RollbackCallJournalFailed`, the same complete *resolved pre-lock* record in every post-resolution outcome, exact journal-exception identity, and detached unmodifiable ordered refusal diagnostics. Preserve the exclusions and focused regression suite. The focused RED proved source-list aliasing (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`); final focused acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures/errors/skips/undefined/ambiguous/pending (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical managed-rollback-scoped OpenJDK 21.0.11 whole-working-tree run passed `check goldenTest` in 7m 5s with 1010 discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors, and 77 tasks (log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`). The former three `Refused` EI/EI2 findings are eliminated. Keep the remaining seven reviewed/non-blocking core medium findings exact—five exception-identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private collaborator-retention warnings—and retain the 25 non-blocking glue-only Kotlin/lifecycle/generated warnings in the record (13 NP, 1 RCN, 5 MS, 6 BC). Revisit ADR/C4 only if the boundary changes policy, persistence, participants, authority, or relationships.
|mitigated for the bounded source-built/in-process executor, historical rollback-scoped dated mutable whole-working-tree source-built aggregate, reviewed non-blocking static findings, and inspected surface adoption; no packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority qualification

|R-031
|The implemented managed-apply diagnostics-gate selector could regress by invoking diagnostics during selection; capturing stale adapters; instantiating replacements; recomputing/normalizing affected files or language IDs; changing the explicit non-null final-segment or case behavior; reordering Maven/Kotlin/fallback branches; changing gate IDs or provider ordering; remapping external resolver failures; or absorbing surface preflight, lifecycle, authorization, rendering, WAL, rollback, or recovery. Implemented-and-validated SURFACE-005 adds the continuing regression risk that LSP could again bypass the selector through generic `java-jdt`, duplicate an LSP-local operation branch, weaken its fail-closed external resolver, or move preflight/lifecycle into the shared boundary.
|Selection-time provider work would escape the one-writer lock; stale adapter capture and metadata/precedence drift can select the wrong authority; gate-ID/order drift changes observable refusals; and scope creep can transfer authority away from `PatchEngine`. For exact Maven module rename, a generic LSP gate discards the retained plan/lease and can attest the wrong semantic model. Fresh bounded source-built/in-process LSP evidence establishes only the public `LspSession.dispatch` row; it does not establish external transport/editor integration, packaged/native/platform execution, concurrency, crash/restart, general Maven projects, release-wide apply authority, or numerical coverage.
|Keep the stateless per-apply `refactorkit-jvm` selector exact: preserve implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 and the SURFACE-001 first exact module-rename branch, exact IDs/metadata behavior, passed-adapter identity, zero provider calls during selection, external resolver identity/timing, and pass-through provider ordering. For SURFACE-005, preserve the implemented direct `refactorkit-lsp` dependency on `refactorkit-jvm` and the existing public five-argument `select` call with the exact retained plan, language `java`, session-current adapters, and fail-closed external resolver after LSP preflight; do not move preflight, pending-plan, refresh, rollback, protocol, or lifecycle policy into the selector. Baseline SHA-256 is `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`. Existing focused selector evidence remains 11/11 scenarios and 92/92 steps (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`), with separately bounded daemon/MCP regressions and whole-tree evidence.
|mitigated for the bounded implemented selector and implemented-and-validated source-built/in-process LSP adoption after independent `PASS`; broader runtime, concurrency/crash, authority, release, and coverage qualification remains open

|R-032
|The implemented daemon/MCP Workspace Snapshot Composition extraction could regress by rebuilding an empty contribution instead of preserving exact base identity; unioning script extensions or ignore policy on the empty path; selecting the base rather than later script file on a normalized collision; sorting by a different key; copying secondary workspace/module/classpath/build/auxiliary authority; attaching TypeScript zero or multiple times; attaching Kotlin before TypeScript or retaining a stale toolchain; wrapping exceptions; introducing nondeterministic identity; or absorbing refresh/watch/index/pending/audit/lease/semantic, protocol, or mutation lifecycle. Moving it into `refactorkit-jvm`, adding concrete core dependencies, or adding a new module would also distort the accepted boundary.
|Snapshot hashes, source routing, build-model projections, error identity, post-apply images, and daemon/MCP compatibility could drift. Scope creep could transfer lifecycle or write authority into a read-side helper and create false aggregate/persistence semantics. Focused source-built/in-process acceptance, daemon/MCP regressions, and dated mutable whole-working-tree source-built aggregate verification mitigate the exact declared behaviors, but REQ-005 adoption remains source-inspected and the evidence does not establish packaged, native, cross-platform, concurrent, crash-restart, numerical-coverage, or general orchestration behavior.
|Keep the stateless language-neutral core service with per-invocation collaborators and one fixed public pipeline. Preserve implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`: exact empty pass-through, all four language mappings, normalized later-source precedence, path-string ordering, three-field overlay, TypeScript-once/Kotlin-last replacement order, current collaborator identity, unchanged exceptions/model status, deterministic hash, cross-surface parity, and no writes. Keep daemon/MCP lifecycle call sites in place; explicitly exclude CLI/LSP, a new module, concrete core adapter imports, a generalized scanner registry, `PatchEngine`, refresh/index/semantic policy, protocols, persistence, and every managed-write concern. Final focused acceptance passed 8/8 scenarios and 65/65 steps (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`); daemon/MCP regressions passed 58/58 and 26/26 (combined log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`). Final 2026-08-01 official whole-tree `check goldenTest` verification passed in 7m 7s with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). Its static classification has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption and is not zero: retain the 21 non-blocking slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` (6 NP, 3 RCN, 12 BC) plus the broader configured non-blocking baseline. Reassess ADR/C4 only if policy, authority, persistence, participants, topology, or relationships change.
|mitigated for the bounded source-built/in-process composer, module regressions, dated mutable source-built aggregate, reviewed non-blocking static findings, and source-inspected daemon/MCP adoption; broader packaged/native/cross-platform execution, concurrency, crash-restart, coverage, and orchestration qualification needs review

|R-033
|The implemented Java Refactoring Preview Command boundary could regress into a second planner catalogue beside `JavaLanguageAdapter.applyRefactoring(RefactoringRequest)`; move surface parsing/defaulting into Java planning; convert missing/unknown input into different refused plans or exceptions; capture a stale snapshot or adapter; copy, normalize, reorder, wrap, or regenerate planner output; collapse daemon/MCP single-file imports with CLI multi-file behavior; absorb `moveClass` non-plan outcomes or other-language/experimental/Maven routes; or grow into pending-plan, diagnostics, lifecycle, rendering, protocol, or managed-write authority. A superficially related `ManagedApplyInvoker` could still become either a useless forwarding layer or a God service.
|Daemon, MCP, generic adapter, or later callers could receive different plan IDs, operation/status/evidence/warnings/diagnostics/edit order, refusal codes, exception identity, validation errors, or admission behavior for the same existing planner request. Stale collaborator capture could plan against the wrong session state. Scope growth could expose non-managed `moveClass` evidence as a plan, silently narrow CLI behavior, couple core to Java, or transfer lock/WAL/recovery/rollback authority away from `PatchEngine`. Focused source-built/in-process acceptance plus dated aggregate/static evidence mitigates the exact bounded routes but cannot establish packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior.
|Keep the sealed Java-owned five-variant command hierarchy, fieldless dispatcher, and internal per-call invoker seam exact. Preserve daemon/MCP translation after their existing parsing/required-field validation; exact current snapshot/adapter supply; one unchanged default planner invocation; exact plan/exception fidelity; and the five converged generic decoder routes. Retain valid, missing, unknown, refused, exceptional, no-write/residue, and negative-ownership acceptance. Keep single-file imports and all named exclusions closed. Reject `ManagedApplyInvoker`; revisit ADR/C4 only for a real policy, authority, persistence, participant, topology, or relationship change. Retain the final whole-tree receipt (1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, 79 tasks; SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`) and targeted static receipt (SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`) with the exact reviewed 48-glue-finding classification above; do not turn them into aggregate cleanliness or numerical-coverage claims.
|mitigated only for the exact bounded source-built/in-process command slice, module regressions, dated mutable source-built aggregate, and reviewed non-zero static classification; R-033 is not closed globally, and packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification remains open

|R-034
|The implemented packaged-process Cucumber mode for existing REQ-JAVA-MAVEN-MOVE-AUTH-001 could regress or be misrepresented as a new requirement/case, a post-commit native CI receipt, or general packaged support; become attached to ordinary `test`/`check`; stop using the real launcher/embedded runtime and scrubbed environment; reintroduce an incorrect one-edit-per-modify journal oracle; project or infer `workspace.locked`/core-code correlation that the runner did not observe; claim a cross-process preview `PlanId`; leak/hang child processes; mutate the permanent fixture/package; or infer archive, other-surface, other-requirement, field/general, crash/concurrency, child-JVM coverage, or unexecuted-host qualification.
|Requirement totals and release claims could become false, developer tests could require expensive packaging, the wrong runtime or journal shape could be tested, lock/apply identity and authority could be misstated, CI could hang or leak processes, fixture evidence could become non-reproducible, and an operation/host could be promoted without evidence.
|Retain one dedicated source set/task selecting only the existing REQ-001 pickle and mode-specific reports; leave ordinary `test`/`check` package-independent. Preserve one no-follow fixture copy, the real unpacked launcher and embedded Java, scrubbed subject environment, poison `java` proof, 16 bounded commands, repeat-equal `JDT_BINDING` preview, exact three observer paths/five affected paths, and the exact lock text `Apply refused:` / `ERROR: Workspace is locked by another RefactorKit writer` before WAL. Preserve no projected `workspace.locked` and no core-code correlation claim; one child-process-replanned apply with no cross-process plan-ID claim; one schema-v8 transaction whose five top-level `FileEdit` entries are four `Modify` plus one `Rename` and whose nested `TextEdit` counts are `1/2/1/1`; fresh clean diagnostics; normal rollback; exact non-engine restoration; output/time/process/cleanup bounds; and final immutable-input checks. Promote a native row only from a reviewed exact post-commit CI receipt.
|mitigated for the official final local OpenJDK 21.0.11/Linux `amd64` mutable-tree observation: `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks` reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit 22/one/21/zero; and one Cucumber scenario with five background plus fourteen scenario steps (19/19) and 2/2 hooks; packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. Fixture/copy, package, launcher, and embedded-Java hashes are recorded in Chapters 6-8. The separate source-built REQ-001 review rerun passed 22/one/21/zero, log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`. Final whole-tree `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 6m43s: standard tests 1045 discovered/817 executed/228 skipped or tag-filtered/zero failures or errors, 140 XML files, 82 tasks, log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`; the packaged task is separately reported and intentionally excluded from those totals. Targeted custom-source-set PMD/SpotBugs tasks passed, log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`. Static classification is intentionally non-zero: exactly 99 reviewed/non-blocking packaged glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), all test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated representation and conservative single-thread harness concurrency warnings. No production source file was added or modified by this slice; the wider baseline remains non-blocking, and aggregate static cleanliness plus numerical/child-JVM coverage are not claimed. Native CI wiring and `if: always()` report uploads are implemented but unexecuted. Linux x86-64 CI, Windows x86-64, macOS x86-64, and macOS arm64 remain `CONFIGURED_UNOBSERVED`; Windows/macOS are configured/static-reviewed only

|R-035
|The implemented direct-child module-rename row can be overgeneralized or regress toward its historical commit-`6d4fb130` prototype: inferred artifact intent, parent-artifact selection, missed root-module origin, unproven global artifact matches, rename-before-modify ordering, empty diagnostics, hand-written XML tokenization, or POM serialization. It can also be misreported as broad module rename/move, protocol, recipe, packaged/native/cross-platform, general-Maven, native-CI, release, or support-matrix qualification.
|Such regression can publish a false-safe operation, mutate unrelated XML, weaken XML external-access controls, conceal incomplete refusal/transaction paths, or spread one fixture result to nested/profile/property/inherited/ambiguous origins, create-module, other coordinate fields, JPMS, surfaces, platforms, or general projects. Closing the broad J1 checkbox or support row from this case would be false.
|Retain the exact no-follow fixture copy, independent artifact intent and omission semantics, immutable effective/raw origin evidence, maintained Woodstox 7.1.1 StAX2 range authority, DTD/external-DTD/entity denial, and no serialization/tokenizer fallback. Keep exactly three POM range edits (`9:14-9:27`, `74:12-74:25`, `13:18-13:31`) followed by two file renames; complete offline staged reactor/diagnostics; deterministic preview; exact S0/C1/S1 apply; one schema-v8 record; and exact rollback. Preserve all exclusions and leave the broad roadmap/support rows open. Final review evidence is manifest SHA-256 `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`; the 1/1-scenario, 50/50-step log SHA-256 is `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`.
|mitigated for the exact implemented-and-validated direct-library fixture row; residual overgeneralization/regression risk remains high for every excluded surface, platform, project shape, and support claim

|R-036
|The implemented authoritative-evaluation boundary could regress by retaining a caller-owned nested collection, trusting provider-controlled roots or fingerprints, weakening the exact tracked shell, journaling a synthetic/stale post hash, skipping post-provider under-lock revalidation, comparing incomplete diagnostics, creating a second rollback transaction, or changing legacy diagnostics-only gate/hash semantics. Consumers could also infer a generic evaluator service or qualification for arbitrary model-changing operations.
|A retained alias can diverge exposed state from a cached hash; arbitrary module/build roots or fabricated external fingerprints can manufacture semantic authority; diagnostics and schema-v8 identity can describe different models; committed S1 or restored S0 attestation can become false; and broader operations/support could be promoted without evidence.
|Retain transitive deep detachment and fresh canonical hash probes; exact candidate root/source-auxiliary/path/language/byte/extension/ignore invariants; core-owned normalized containment; independently recomputed no-follow external fingerprint authenticity; exact S0/C1/S1 and D0/D1 identity; post-provider under-lock workspace/lease revalidation; same-record automatic rollback; and unchanged diagnostics-only behavior. The final 16/16-scenario, 128/128-step log/JSON SHA-256 are `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`/`c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`. Keep evaluation transient and schema-v8 as the sole WAL/idempotency/exact-result store. Target evidence types have zero raw/real EI/EI2 findings; the one evaluation EI warning is non-real over a defensively copied unmodifiable list, PMD is `NO-SOURCE` for Kotlin, and the wider SpotBugs baseline remains non-blocking.
|mitigated for REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`; residual regression and overgeneralization risk remains for unselected model-changing operations, protocols, packages/platforms, release scope, and support claims

|R-037
|The historical clean pushed v0.7.x production baseline could not compile all test sources because Gradle resolved `refactorkit-kotlin:testRuntimeClasspath` before later `testImplementation` mutation.
|Without correction, subsequent capability tests could be selective or stale, production changes would lack a trustworthy green baseline, and CI could be misreported as evidence.
|The authorized scaffolding slice preserved the historical clean-baseline failure log SHA-256 `408d45e7e2c1bea4152c220af283455421a3bee84dca2b3f069832e9541983d5`, reproduced RED with SHA-256 `72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`, and changed only `modules/refactorkit-kotlin/build.gradle.kts` to defer resolution into `tasks.test.doFirst`. The GREEN `testClasses` rerun passed with SHA-256 `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`; full OpenJDK 21 `check goldenTest` passed 82/82 tasks and 1,062 tests with 228 skipped and zero failures/errors (log SHA-256 `48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). Separately, the shared Linux distribution-verification cause was root `distCliRuntimeZip` depending on directory-only `refactorkitRuntimeDist`; the bounded root `build.gradle.kts` correction selects `refactorkitRuntimeZip` and locally produced a checksum- and launcher-verified 72,030,777-byte archive with SHA-256 `afcd65b0a800ed21f4c46d3b47e0fb569f81fb9c913c5d2965a5189edd3a0b85`. Final JDK 21 source-built verification with `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 7m 28s with 82/82 tasks; log `/tmp/refactorkit-runtime-zip-task-final-check.log`, SHA-256 `009dd3940c1042cc6d1d97e18709795760d6eb4025b685f1be4c71e7e1fccae4`. This receipt remains source-built evidence and does not qualify CI, native, or release behavior. Preserve both local gates and obtain post-commit CI.
|mitigated/resolved for local P0 admission; separately, the Linux packaging-scaffold cause in Actions runs `30746237578` and `30749086970` is corrected locally; both runs remain historical `failure` observations, and post-commit CI/release qualification remain open

|R-038
|The current Kotlin planner treats a companion object as an independently movable `OBJECT`, despite companion ownership by its enclosing type.
|A standalone move can fabricate source/package ownership, separate the companion from its enclosing declaration semantics, and falsely close K5 from existing source.
|Add an executable stable-refusal requirement and correct the planner. Permit the companion to travel only with a separately supported enclosing type/file move. Treat extraction as a future operation and retain current source as negative evidence.
|confirmed semantic defect/risk; production correction planned and unqualified, no longer scaffold-blocked

|R-039
|Current TypeScript advanced planners conflate distinct T5 operations, send invented/unqualified LSP request shapes, give `inlineMethod` extract behavior, lack direct behavioral tests, and cannot execute TypeScript recipes through the Java-only recipe engine.
|Source presence or generic labels can be mistaken for typed compiler authority, causing incorrect edits, missing project-reference/specifier changes, and false T5 closure. Dynamic/mixed JavaScript could be overpromoted from typed TypeScript evidence.
|Implement the eight distinct T5 rows through the pinned TypeScript 5.9.3 protocol anti-corruption layer, exact returned action identities, staged compiler diagnostics, and operation-specific tests. When evidence is incomplete, return stable typed refusal; never inherit TypeScript certainty into dynamic/mixed JavaScript.
|high release-blocking risk; all T5 rows remain planned/unqualified

|R-040
|Recipe reduction can relabel an operation-owned plan as `recipe:<id>`, drop its authoritative evaluator/auxiliary evidence, and apply through a weaker generic diagnostics gate.
|The final bytes may look composable while the semantic model, operation lease, POM/project evidence, or refusal authority no longer matches. A recipe could launder an unqualified mutation into `PatchEngine`.
|Preserve the exact child plan/evidence/lease/auxiliary inputs/gate for one authority-owning mutation, or separately prove a recipe-wide final-candidate evaluator. Refuse second/stale/mismatched/review-only authority before WAL. Retain current generic composition as negative evidence for model-changing children.
|high release-blocking safety risk; corrective recipe row planned

|R-041
|Client-managed API `0.2` LSP module rename and managed `applyPlan` can be conflated; the managed route could regress from its implementation-informed operation-owned selector back to generic `java-jdt`.
|A transient proposal plan ID could be mistaken for approval or transaction identity; an editor-applied proposal could be described as transactional/rollbackable despite bypassing RefactorKit's operation gate, lock-held revalidation, journal, recovery, and rollback authority; or managed qualification could be inferred from proposal/client-application evidence while bypassing dirty/open-buffer guards. On the managed route, any regression to generic diagnostics would discard the Maven plan/lease authority and could make WAL evidence semantically false.
|Keep the downstream LSP ACL and immutable protocol documents distinct from the upstream immutable Java plan. Preserve SURFACE-004 `PREVIEW`, client-managed ownership, rollback unavailability, version checks, proposal-only plan-ID correlation, and legacy pre-edit refusal. For SURFACE-005, preserve dirty-first/affected-open-second preflight before scan/selection; pass the exact retained plan/language/current adapters to the existing selector; require operation-owned authority and explicit `lsp-managed-command`; remove/refresh only after `Applied`; and refresh restored state only after successful same-record normal rollback. Keep SURFACE-004 and SURFACE-005 evidence independent.
|SURFACE-004 remains bounded implemented-and-validated evidence under its existing receipt. SURFACE-005 is separately implemented-and-validated against baseline `REQ005-BDD-EVIDENCE-001` SHA-256 `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; independent review returned `PASS` with no findings for AC-005-01 through AC-005-11. The durable pre-production focused Cucumber RED log at `docs/requirements/evidence/req005-pre-production-focused-cucumber-red.log` (SHA-256 `b7ae8c9bfbd7122404403f13b10107a46f359030187bd9459e7b26d42f400cc8`) proves only that runtime managed apply failed before the success assertions at `JavaMavenModuleRenameSurface005Steps.kt:673`, with `BUILD FAILED`, one failed test, and five skipped; separate source/diff/domain inspection identified the former generic `java-jdt` route, which is not a hash-bound claim from this log. After selector adoption and promotion, focused JSON SHA-256 `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54` records the bounded one-scenario/47-result success evidence. External transport/editor integration, packaged/native/platform execution, concurrency/crash, general Maven, release, numerical coverage, and broad qualification remain open

|R-042
|Release reports, SBOMs, attestations, native archives, and downloaded-asset smoke may not share one immutable release evidence identity; intermediate Actions artifacts can be mistaken for final published assets.
|A support matrix or release can join evidence from different commits, packages, hosts, fixtures, or toolchains and become unverifiable despite individually passing jobs.
|Create a versioned canonical release manifest joining the complete identity tuple, retain operation-family reports, verify attestation subjects, use a draft release if necessary, and independently download/verify/execute each final native asset before promotion. Treat queued/running CI and local/source-built evidence as distinct non-substitutable subjects.
|planned I1 release blocker

|R-043
|The exact Maven module-rename selector route can regress by normalizing operation identity, selecting generic `java-jdt`, eagerly or repeatedly constructing the operation gate, accepting a wrong/non-authoritative delegate, adding an observer/hook, rebuilding authority from a rendered plan, or letting a public surface bypass the selector.
|The retained canonical plan and authority lease could be discarded between preview and apply; fallback diagnostics could attest the wrong semantic model; selection-time work could escape `PatchEngine`'s authority sequence; and unexecuted surface/package/platform/general/release behavior could be promoted.
|Keep public operation/gate constants authority-free and preserve the first exact Java branch plus synchronized one-time `DiagnosticsGate.lazyAuthoritative`, exact returned ID/provider checks, no observer, same retained plan/lease, and real fallbacks for non-matches. Retain independently bounded SURFACE-001/002/003/004/005 evidence. For SURFACE-005, retain the implemented replacement of only LSP's former generic gate with the existing five-argument selector after preflight, the direct LSP-to-JVM dependency, exact `lsp-managed-command` authorization, fail-closed external resolver, and success-only lifecycle; retain bounded S0/C1/S1 plus same-record rollback evidence. Baseline SHA-256 is `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; promoted feature SHA-256 is `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`. Keep static analysis non-blocking and make no numerical-coverage claim.
|mitigated for separately qualified exact source-built rows and implemented-and-validated SURFACE-005 source-built/in-process evidence; external transport/editor integration, packaged/native/platform/general-Maven/release behavior, concurrency/crash qualification, numerical coverage, and broad support remain open

|TD-DIST-001
|The macOS and Windows runtime archives are checksum-verifiable and attested but remain unsigned: there is no Apple Developer ID/notarization or Windows Authenticode signature.
|Gatekeeper, SmartScreen, or enterprise endpoint policy may display publisher/security warnings even when archive checksum, SBOM, and provenance verification succeed. This affects warning-free installation and publisher identity, not RefactorKit patch correctness or transaction safety.
|Track signing as distribution technical debt. Add Developer ID signing plus notarization and Authenticode only when protected CI certificates, credentials, timestamp services, rotation, and revocation procedures are available. Never describe checksums, SBOMs, or attestations as substitutes for platform code signing.
|accepted technical debt; non-blocking for current unsigned experimental assets, required before claiming warning-free trusted desktop installation
|===

== Evidence Note for R-014

Status: mixed but implementation-informed evidence; bounded fixture acceptance, one exact field qualification, and one local Linux `amd64` packaged-process observation for the same REQ-001 pickle are recorded. The Maven move-class feature still reconciles to twelve definitions and 22 expanded cases, all implemented-and-validated; the second validation mode changes no Gherkin count or status. Focused REQ-008 OpenJDK-21 evidence discovered 22, selected/executed/passed six, skipped sixteen by tag filter, and had zero failures/errors. Independent review log SHA-256 is `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`; the `gherkin-writer` execution log is `6f7e2d8a9c767f265a117853267a37ab31ec31d0878637870b37202ec8ba2c71`. Subsequent independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Earlier dated full-suite and focused evidence remains scoped to its recorded working tree. The separate Magrathea `a48e3c06` source-built CLI/Linux/JDK-21 result remains consumer-specific. The dedicated REQ-001 local package run is separately bounded and does not establish a native CI ledger row, runtime-distribution support, REQ-002..012 package behavior, or general qualification.

== Technical Debt from Release Plan

The active stable plan defines the remaining release-hardening workstreams. The release-blocking `v0.7.x` scope is centralized in `docs/requirements/v0.7.x-completion-contract.md`; the decomposed non-Gherkin requirements remain planned/open except for the locally resolved scaffold gate and do not change existing executable Gherkin totals:

* `v0.4.0` and `v0.5.0` are published; `v0.6.0` adds the accepted managed TypeScript/JavaScript semantic foundation while retaining API `0.2`;
* keep API `0.2` active until normative patch/apply/rollback and API `1.0` CLI/daemon contracts are frozen and accepted;
* prove Eclipse JDT source compliance and semantic boundaries for Java 8 through Java 25;
* preserve the TX-001 through TX-018 qualified managed-file and diagnostics-only transactionality evidence plus R-036's implemented bounded authoritative snapshot-diagnostics evidence during API freeze, while keeping every unselected model-changing operation and broader support claim explicitly unqualified;
* retain precise qualification for WAL-backed managed transactions, the staged recipe-wide boundary, client-managed native LSP edits, and unsupported/destructive filesystem failure models;
* complete the planned product-critical `RPK-JAVA-DIAG-001..007` authority epic before treating Java/Maven diagnostics as definitive, including full-reactor/source-set environments, exact release signatures, non-executable discovery, exact deltas, typed JPMS refusal, and native packaged fixtures;
* preserve implemented REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`, including six-case `008` with four pre-lock guidance variants, one pre-lock active-reactor-POM structural refusal, and one under-lock pre-WAL required-file-evidence drift refusal with expected-only semantic candidate-inventory identity, preserved `S1`, no WAL/transaction/target edit, and possible lock-file residue; retain the independent final mutable-working-tree OpenJDK 21.0.11 `./gradlew --no-daemon --rerun-tasks check goldenTest` receipt (`BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`) as execution evidence only, not packaged-process/runtime/native-platform/general authority or numerical coverage qualification, with the SpotBugs baseline configured non-blocking; retain the Magrathea `a48e3c06` CLI/Linux/JDK-21 result as bounded field evidence and require explicit packaged/runtime integration and native matrices before broader qualification;
* retain the implemented local packaged-process validation mode for existing REQ-JAVA-MAVEN-MOVE-AUTH-001 without adding a requirement/case or coupling ordinary `test`/`check` to packaging; retain its official final packaged, separately accounted source-built whole-tree, and targeted static receipts, including the intentionally non-zero 99-finding packaged test/deployment glue/harness classification, no production-source change by this slice, and no aggregate-cleanliness or numerical/child-JVM-coverage claim; obtain exact post-commit native CI receipts before promoting any of the four `CONFIGURED_UNOBSERVED` ledger rows, whose wiring/report uploads are implemented but unexecuted; preserve child-process CLI replanning with no cross-process plan-ID claim and source-built direct lock-held/fault-injector evidence; and retain the explicit REQ-002..012/surface/population/crash/concurrency/archive/child-JVM-coverage/unobserved-host exclusions;
* preserve the locally resolved R-037 all-module test-compilation gate and obtain corrected-revision CI without treating either as product or release qualification;
* enforce Kotlin companion ownership/refusal, the eight distinct typed T5 operation rows, API `0.2` LSP client-managed versus managed ownership, and authority-preserving recipe composition;
* complete diagnostic regression gates and representative project acceptance;
* classify and test stable LSP/MCP boundaries;
* establish bounded performance and resource behavior;
* verify self-contained assets with checksum, SBOM, provenance/attestation, and reproducibility evidence;
* retain `TD-DIST-001` as explicit platform-signing debt until protected Apple Developer ID/notarization and Windows Authenticode credentials exist; unsigned assets must continue to disclose possible OS publisher warnings;
* promote only an RC with no unresolved release blocker.

Status: confirmed long-range target from `docs/releases/v1.0.0-plan.md` and ADR 0009; `v0.3.0` publication and downloaded-runtime smoke evidence are implementation-informed, `v0.4.0` acceptance is complete, and multi-language/stable acceptance remains pending.
