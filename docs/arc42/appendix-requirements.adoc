= Appendix: Requirements
:toc:
:sectnums:

Status: implementation-informed for separately bounded implemented slices and needs-review for broader coverage and qualification. The configured `features/` root contains eight feature documents, 32 repository-unique requirement definitions, 76 expanded executable cases, 479 declared scenario steps, one background with five background steps, and 1,016 expanded case steps. All 32 definitions and all 76 cases are effectively `@implemented-and-validated`; every other effective status is zero. The post-promotion official `io.cucumber:gherkin:28.0.0` repository-wide audit on OpenJDK 21.0.11 reports those totals with zero parse/tag/status/duplicate-ID/reconciliation errors (log SHA-256 `ed57576520bdfa92810ebbe93e0e4c238632feae2a74a10a99b35f8e16101925`). The preceding seven-feature audit remains historical evidence for its exact then-current 28 definitions/60 cases/433 declared steps/885 expanded steps, including the one absent Maven module-rename definition/case (log SHA-256 `271ec890cad8065f0536ab27ca885d81e7d070382ca96fdf9d8cf26afa7e137a`). The preceding six-feature post-promotion audit remains historical evidence for its exact then-current 27 definitions/59 cases/386 declared steps/838 expanded steps (log SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`). The authoritative diagnostics-evaluation feature contributes four functional definitions/16 cases/43 declared steps/128 expanded steps, all `@implemented-and-validated`; independent acceptance passed 16/16 scenarios and 128/128 steps (log/JSON SHA-256 `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`/`c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`). The module-rename feature contributes one functional definition/case and 50 declared/expanded steps, also `@implemented-and-validated`; its independent log/JSON SHA-256 are `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`/`d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`. Final review returned `PASS_FOR_PROMOTION` (manifest SHA-256 `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`; summary SHA-256 `76a2d8d508fbec9786abfb30bc25a0953f41cf6c163a0508e25433a6530d3b02`). These promotions do not qualify broad module rename/move, protocols, recipes, packaged/native/cross-platform execution, general Maven projects, native CI, release-wide support, or the broad roadmap row. The Java preview-command feature contributes four non-functional definitions/eight cases/46 declared steps/94 expanded steps. Its corrected split runners passed seven Java-module scenarios/85 steps plus one real public daemon/MCP session scenario/9 steps. Final dated mutable whole-working-tree source-built verification passed on OpenJDK 21.0.11 with 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors, 140 XML files, and 79 executed tasks (log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`); the targeted affected-module static command passed with an intentionally non-zero classification of exactly 48 reviewed/non-blocking slice-local glue findings (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Workspace Snapshot Composition contributes five non-functional definitions/eight cases/41 declared steps/65 expanded steps. Production evidence is the stateless language-neutral core `WorkspaceSnapshotComposer`; a meaningful compile RED failed because that composer was missing (log SHA-256 `d2876ad413703a3895999a9377f8b7c6a713578eb8156b652bb079c70f1a6d95`). Final independent OpenJDK 21.0.11 focused source-built/in-process acceptance passed 8/8 scenarios and 65/65 steps with zero failures, errors, or skips (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`). Adoption is source-inspected only in private daemon/MCP `scanWorkspace`; independent daemon 58/58 and MCP 26/26 regressions passed (combined log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`). CLI/LSP and packaged/native/cross-platform/concurrent/numerical-coverage/general-orchestration qualification remain excluded. The preceding official OpenJDK 21.0.11 `io.cucumber:gherkin:28.0.0` four-feature audit remains historical evidence for its then-current 18 implemented definitions/43 cases/299 declared steps/679 expanded steps (log SHA-256 `fb7c81a1d3223e6959d6838ac38c5c737f1bf8804570489405ed59bc1a279bc7`). For REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002, final independent OpenJDK 21.0.11 source-built/in-process acceptance passed 11/11 scenarios and 92/92 steps with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`); daemon regression passed 58/58 and MCP regression passed 26/26. Final whole-working-tree source-built verification on the same OpenJDK used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. Explicit non-null final-segment handling eliminated the selector's initial production NP finding while preserving root-path `NullPointerException` and exact `.java`/`.JAVA` behavior. The final full log contains no production finding for `ManagedApplyDiagnosticsGateSelector.kt` or its provider functions; 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. Production evidence is the stateless selector plus source-inspected adoption only at daemon/MCP managed apply. CLI/LSP/recipes/testkit/direct library remain excluded, `PatchEngine` authority is unchanged, and no packaged, native, cross-platform, concurrent, apply-authority, or numerical-coverage claim is made. The preceding four-feature audit that recorded the selector definitions/cases as `@absent` (log SHA-256 `57a48fbdf31f91e2ab0d54443849225ba51a4349cc8be84236f62210adc90c14`) remains historical pre-promotion evidence. The preceding final three-feature audit (log SHA-256 `f4f9fd559ef9220c145017a2723fc4b8e888424448a392a11d20c1dc33d379c0`), the earlier three-feature source-reconciliation receipt that recorded managed rollback as then-`@absent` (log SHA-256 `af9cfc7e69ec589bd5512b963e9eebdce936e63b240e3b1860078f0989b20b7e`), and the still-earlier two-feature parser receipt (log SHA-256 `13989eb5bad0498de6661715236b46401b15d3401d450137640f2af89cb76ba9`) remain historical evidence for their then-current sources and statuses, not the current aggregate. The managed-rollback feature contributes two non-functional definitions, eight cases, 19 declared steps, and 67 expanded steps. A focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`); detached unmodifiable ordered snapshotting corrected it. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). Production evidence is the core executor; CLI/daemon/LSP/MCP adoption is source-inspected only. REQ-PENDING-PLAN-STORE-001/002 contribute two non-functional scenarios and 19 steps. Their focused source-built/in-process run passed both scenarios with 29 executed Gradle tasks and zero failures/errors (log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`). The historical PendingPlanStore-scoped whole-working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`. SpotBugs retained its configured non-blocking baseline and reported no `PendingPlanStore` findings. This later scoped receipt is dated mutable source-built aggregate evidence; it does not qualify packaged processes, concurrent access, a general extraction pattern, or numerical coverage. Surface lifecycle policy remains separate, and no protocol capability or authority rule changed. Historical pre-selector whole-working-tree verification after the managed-rollback correction used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). This is the historical pre-selector whole-working-tree source-built receipt for the managed-rollback correction; the earlier REQ-008- and PendingPlanStore-specific whole-working-tree receipts remain historical evidence for their explicitly stated scopes. It is dated mutable aggregate evidence and does not qualify the selector, packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority.

Final official whole-working-tree verification for the Workspace Snapshot Composition working tree on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 7s; 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors; 136 XML files; 78 tasks executed; log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`. The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption. Static findings are not zero: 21 slice-local glue-only Kotlin/generated SpotBugs findings in `WorkspaceSnapshotComposerSteps.kt` remain non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. This is dated mutable source-built aggregate evidence alongside the independently retained focused source-built/in-process acceptance, daemon/MCP regressions, and Gherkin audit. It does not add Gherkin definitions/cases or qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior. The preceding selector whole-tree receipt remains historical evidence for its scope.

The twelve-definition/22-case Maven move-class subset retains its independently bounded evidence. Existing `001`-`007` and `009`-`012` meanings are unchanged. REQ-JAVA-MAVEN-MOVE-AUTH-008 validates four immutable schema-v1 preview-guidance outcomes, one typed root-declared active-reactor-POM structural refusal, and one under-lock generic required-file-evidence drift refusal. Focused OpenJDK 21 evidence selected/executed/passed its six cases, skipped sixteen by tag filter, and recorded zero failures/errors. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. These historical execution totals do not change the Maven subset and do not themselves imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. The exact Magrathea request at `a48e3c06` remains CLI/Linux/JDK-21-specific. Separately, existing REQ-001 now has one implementation-informed local Linux `amd64` packaged-process observation on the mutable tree; it adds no definition/case/status and promotes no native CI ledger row.

== Requirement Sources

[cols="1,3,1",options="header"]
|===
|Source |Use |Status

|`AGENTS.md`
|Authoritative mission, scope, module responsibilities, safety workflow, roadmap, and definition of done.
|implementation-informed

|`README.md`
|Project overview, usage modes, build and CLI examples.
|implementation-informed

|`docs/*.md`
|Subsystem behavior and limitations. `docs/transactionality-audit.md` is the evidence baseline for current apply, compensation, journal, LSP, recipe, and recovery limitations `TX-001` through `TX-018`.
|implementation-informed

|`docs/requirements/java-maven-authoritative-diagnostics.md`
|Normative repository contract for exact release platforms, full-reactor source-set visibility, typed availability, change-specific authority, secure discovery, and permanent qualification.
|planned, product-critical, release-blocking

|LLM Wiki requirement `refactorkit-java-maven-move-class-apply-authority` and concepts `target-scoped-java-semantic-authority`, `java-target-name-lookup-protection`, `maven-descriptor-closed-dependency-graph`, `ordinary-maven-repository-missing-binary-evidence`, `review-only-move-guidance-contract`, and `legacy-lexical-fallback-authority-envelope` (`RPK-JAVA-MOVE-001..007`)
|Reviewed domain contract for full-reactor active-POM authority, descriptor closure over selected external nodes, hash-bound outgoing-edge classification, closure-scoped missing-binary enumeration, candidate-total JDT binding, target-name lookup protection, exact retained-diagnostic identity/non-concealment, staged/lease gates, managed eligibility, permanent acceptance, and immutable non-managed boundaries. The initial contract was reviewed 2026-07-29; descriptor-closed and ordinary-absence updates plus primary Maven/Resolver sources `SRC-2026-07-30-001..007` were reviewed 2026-07-30 against base commit `c1d70fc51ef84fa25baaab5678dece69f8ce739a`. The REQ-003 guidance and distinct REQ-004 lexical-envelope concepts remain architecture/research inputs; bounded implementation claims rely separately on current source, tests, and tagged acceptance rather than on research.
|confirmed P0 architecture priority; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` implementation-informed; broader qualification remains open

|LLM Wiki concept `packaged-process-qualification` and analysis `refactorkit-packaged-maven-move-class-qualification-at-58d1d4a`
|Research/recommendation for a second packaged Cucumber validation mode over the existing REQ-JAVA-MAVEN-MOVE-AUTH-001 feature/ID/pickle. It defines the bounded real-launcher/embedded-runtime, isolated-fixture, scrubbed-environment, repeat-preview, refusal/apply/diagnostics/rollback, exact-restoration, timeout/cleanup, status, and exclusion contract. It is not a new requirement, case, ADR, topology, implementation claim, or execution receipt.
|reviewed 2026-08-01 against clean repository commit `58d1d4ae0c170f76149ea95897bcb833dbde2bf6`; preserved as research/recommendation input. Implementation is now separately observed locally on Linux `amd64`; all four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`

|LLM Wiki source packet `SRC-2026-07-29-004` and dated observation `Descriptor-closed Maven traversal qualifies reported Magrathea move`
|Field evidence chronology: the initial report was reviewed 2026-07-29. A 2026-07-30 follow-up used the current source-built CLI on Linux/JDK 21 against clean Magrathea consumer commit `a48e3c06629acf7032d0f28a0f8bdc5426442c7c` after `010`; the four-file `InMemoryRevocationRepository` move preview had `JDT_BINDING`, applied in one transaction, and rollback restored the exact source-tree hash. This is commit/request/surface/platform-bounded field evidence, not a requirement, permanent fixture, integration/native matrix, or general implementation claim; it did not require `011` or `012`.
|implementation-informed field evidence for one CLI/Linux/JDK-21 request; broader qualification needs review

|`modules/*`
|Implementation and test evidence. Pending-plan production evidence is exactly `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`, `modules/refactorkit-daemon/src/main/kotlin/org/refactorkit/daemon/DaemonSession.kt`, `modules/refactorkit-lsp/src/main/kotlin/org/refactorkit/lsp/LspSession.kt`, and `modules/refactorkit-mcp/src/main/kotlin/org/refactorkit/mcp/McpSession.kt`. Managed-rollback production evidence is `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/ManagedRollbackExecutor.kt` plus inspected adoption in `RefactorKitCli.kt`, `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt`; focused acceptance glue is `ManagedRollbackExecutorCucumberTest.kt` and `ManagedRollbackExecutorSteps.kt`. Managed-apply selector evidence is exactly `modules/refactorkit-jvm/src/main/kotlin/org/refactorkit/jvm/ManagedApplyDiagnosticsGateSelector.kt`, the adopting daemon/MCP call sites, the Cucumber/JUnit scaffold in `modules/refactorkit-jvm/build.gradle.kts`, and `ManagedApplyDiagnosticsGateSelectorCucumberTest.kt` plus `ManagedApplyDiagnosticsGateSelectorSteps.kt`. Workspace Snapshot Composition production evidence is exactly `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/WorkspaceSnapshotComposer.kt` plus source-inspected private daemon/MCP `scanWorkspace` adoption; focused acceptance glue is `modules/refactorkit-cli/src/test/kotlin/org/refactorkit/cli/workspacesnapshotcomposer/WorkspaceSnapshotComposerCucumberTest.kt` and `WorkspaceSnapshotComposerSteps.kt`. The test location does not imply CLI production adoption. Java preview-command production evidence is `modules/refactorkit-java/src/main/kotlin/org/refactorkit/java/JavaRefactoringPreviewCommand.kt`, the five converged routes in `JavaLanguageAdapter.kt`, and the five adopting routes in `DaemonSession.kt`/`McpSession.kt`; Java-module glue covers REQ-001/002/004 and CLI-module glue exercises REQ-003 through public sessions.
|implementation-informed within each separately stated evidence boundary

|`samples/*`
|Sample Maven, Gradle, Spring, JPA, and multi-module projects.
|implementation-informed

|`recipes/java/rename-package.yml`
|Recipe workflow example.
|implementation-informed

|`testdata/golden/*`
|Golden use-case validation examples.
|implementation-informed

|`.github/workflows/ci.yml`
|Build/test/package/smoke validation pipeline.
|implementation-informed

|`features/authoritative-diagnostics-evaluation.feature`
|Executable functional Story BDD backlog for language-neutral authoritative snapshot and diagnostic evaluation of model-changing candidates through the existing public `PatchEngine.apply` boundary: exact S0/C1/S1 identity, provider-shell constraints, post-provider revalidation, schema-v8 journaling, same-record automatic rollback, and additive diagnostics-only compatibility.
|Four repository-unique requirement-bearing definitions (two ordinary scenarios and two outlines), 16 expanded cases, 43 declared scenario steps, and 128 expanded case steps. REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` inherit `@functional-requirement` and `@implemented-and-validated` from the feature. Independent OpenJDK 21.0.11 execution passed all 16 scenarios/128 steps and validates deep detachment, pre-WAL authority, exact S0/C1/S1 identity, same-record automatic rollback, and diagnostics-only compatibility within the bounded mode.

|`features/java-maven-module-rename.feature`
|Executable functional Story BDD backlog for one bounded direct-library rename of an exact root-aggregator direct-child directory and its independently caller-authorized artifact component, including origin-proven POM edits, deterministic preview, one managed apply, and exact normal rollback.
|One ordinary requirement-bearing scenario/case with 50 declared and expanded steps. REQ-JAVA-MAVEN-MODULE-RENAME-001 has explicit scenario-level `@functional-requirement` and `@implemented-and-validated`; independent OpenJDK 21.0.11 execution passed 1/1 scenario and 50/50 steps. The status is bounded to the direct-library fixture contract and makes no broader support claim.

|`features/java-maven-move-class-apply-authority.feature`
|Executable Story BDD backlog for Maven move-class semantic preview, managed apply and rollback, observer discrimination, closure-scoped JDT warning authority, authority-loss guidance, cross-surface lexical-fallback refusal, zero-residue CLI refusal, candidate-total `OFFLINE_MISSING` authority, fail-closed uncertainty/drift handling, explicit transitive-test-scope preservation, descriptor-closed selector pruning before child lookup, alternate-path selected-POM refusal, RefactorKit-owned ordinary selected-JAR negative-presence authority with under-lock drift refusal, and six selected-descriptor loss/drift cases requiring structural pre-lock/pre-WAL refusal without a binary-absence downgrade.
|12 requirement-bearing definitions and 22 example-expanded executable cases; all 12 IDs and all 22 cases effectively `@implemented-and-validated`; `008` and `012` each expand to six validated cases

|`features/managed-apply-diagnostics-gate-selector.feature`
|Executable non-functional Story BDD contract for the implemented stateless daemon/MCP managed-apply diagnostics-gate selector: exact seven-row precedence, case-sensitive selection metadata, lazy built-in providers, lookup-now/provider-later external resolution, per-call adapter identity, unchanged gate IDs/order, `PatchEngine` authority, and explicit surface exclusions.
|Two requirement-bearing definitions expanding to 11 cases, with 20 declared scenario steps and 92 expanded case steps. REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 inherit `@non-functional-requirement` and `@implemented-and-validated` from the feature; all 11 cases passed the final focused source-built/in-process runner. Daemon/MCP regressions passed 58/58 and 26/26, and the dated whole-working-tree source-built aggregate passed; packaged/native/cross-platform/concurrent/apply-authority/numerical-coverage qualification remains open.

|`features/managed-rollback-executor-extraction.feature`
|Executable non-functional Story BDD contract for one implemented language-neutral managed rollback resolution sequence, caller-selected initial journal visibility, exact resolved pre-lock record retention, surface-owned preflight, one real engine call, distinct typed outcomes, and exclusion of response/lifecycle/diagnostics policy.
|Two requirement-bearing definitions expanding to eight cases and 67 case steps; REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 inherit `@non-functional-requirement` and `@implemented-and-validated` from the feature. All eight cases passed the final focused source-built/in-process runner after a RED proved and detached unmodifiable ordered snapshotting corrected mutable diagnostic aliasing; the historical rollback-scoped whole-working-tree source-built gate also passed, while surface adoption remains source-inspected. Neither receipt qualifies packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority.

|`features/pending-plan-store-extraction.feature`
|Executable non-functional Story BDD contract for exact generic session payload retention, true 128-entry access-order LRU, session isolation, side-effect-free removal, shared-store adoption, and preservation of the distinct daemon/LSP/MCP admission, removal, rollback, reset, close, and response policies without adding protocol capability or write authority.
|Two ordinary requirement-bearing scenarios and 19 steps; REQ-PENDING-PLAN-STORE-001/002 inherit `@non-functional-requirement` and `@implemented-and-validated` from the feature; both passed the focused source-built/in-process runner

|`features/java-refactoring-preview-command-boundary.feature`
|Executable non-functional Story BDD contract for the implemented sealed five-variant Java preview command hierarchy, fieldless dispatcher, internal per-call planner-invoker seam, exact default-planner mapping, generic-adapter decoder convergence, daemon/MCP post-parse adoption, exact plan/exception fidelity, stateless no-write behavior, and closed exclusions.
|Four repository-unique requirement-bearing definitions (three ordinary scenarios and one five-row outline), eight expanded cases, 46 declared scenario steps, and 94 expanded case steps. REQ-JAVA-PREVIEW-COMMAND-001 through `004` inherit `@non-functional-requirement` and `@implemented-and-validated` from the feature. Corrected split runners passed all eight cases: seven Java-module cases/85 steps and one real public daemon/MCP session case/9 steps. Final dated source-built aggregate verification passed with 1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, and 79 tasks (SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`); targeted static verification passed with 48 reviewed/non-blocking slice-local glue findings (SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Focused qualification remains source-built/in-process only; broader execution and numerical coverage are not inferred.

|`features/workspace-snapshot-composer-extraction.feature`
|Executable non-functional Story BDD contract for one implemented stateless language-neutral core composer: exact authoritative-base identity on an empty contribution, bounded normalized source overlay, per-call conditional/final evidence ordering, unchanged collaborator failure identity with no side effects, and adoption only at the existing daemon/MCP composition points while every listed lifecycle, index, authority, mutation, protocol, CLI, and LSP policy remains outside.
|Five repository-unique requirement-bearing definitions (four ordinary scenarios and one four-row outline), eight expanded cases, 41 declared scenario steps, and 65 expanded case steps. REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` inherit `@non-functional-requirement` and `@implemented-and-validated` from the feature. All eight cases passed final focused source-built/in-process acceptance; REQ-005 adoption is source-inspected plus daemon/MCP module regression evidence, not packaged surface execution.
|===

The external research and target-scoped concepts above were reviewed against RefactorKit base commit `c1d70fc51ef84fa25baaab5678dece69f8ce739a`; bounded implementation claims rely separately on dated mutable working-tree evidence. The 2026-07-30 clarification defines full-reactor graph completeness as descriptor-closed over selected nodes: every active reactor POM remains mandatory, every selected external node needs complete effective descriptor closure, and every outgoing declaration/classification is hash-bound. Bounded `010` validates the prune/alternate-selected-path boundary; bounded `011` independently validates the ordinary sidecar-free selected-`jar` row and its three under-lock drift gates; and six-case dual-classified outline `012` validates fail-closed `MISSING`, `MALFORMED`, and `DRIFTED` selected-descriptor outcomes with parsed relocation/model identity proof and stable recorded evidence. The follow-up Magrathea field observation is separately implementation-informed for consumer commit `a48e3c06` through the source-built CLI on Linux/JDK 21 after `010`; it is not architecture research promoted into a requirement, did not require `011` or `012`, and does not establish integration/native-matrix or general qualification.

The LATE REQ-JAVA-MAVEN-MOVE-AUTH-003 review reconciles the corrected feature with actual implementation. Four isolated copies use the ordinary default scanner to exercise a readable source omitted after an effective primary Maven `testSourceDirectory` replacement, a readable system-path JAR whose strict evidence-manifest fingerprint differs, a recovered target use without syntax failure, and a materialized generated-root inventory mismatch owned by `catalog-generated-support:main`. The effective model keeps primary main/test directories separate from additive build-helper/Kotlin roots: replacing a primary directory removes only the corresponding default Java root, while additive declarations retain defaults, and no plugin is executed. The two strict format-version-1 expected inventory manifests are snapshot-bound auxiliary files; blocker evidence carries manifest content hashes, and the classpath row carries expected/observed artifact fingerprints. Repeat guidance values and JSON are deterministic and schema/checklist version 1; CLI `--apply` refuses as `guidance.nonManaged` before `PatchEngine`, lock, or WAL. On 2026-07-30, the independent final OpenJDK 21 `./gradlew --no-daemon --rerun-tasks check goldenTest` run reported `BUILD SUCCESSFUL` in 7m 13s: 956 discovered, 773 executed, 183 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; reviewed log SHA-256: `8d9b19576a27deaae2c12c767f929664c228db85d1dcc8644de3e41ecbe6ca9d`. This is dated mutable working-tree evidence; SpotBugs retained the configured non-blocking baseline findings. The REQ-003 implementation claim remains bounded to the Java dispatcher/source-built CLI; REQ-004 separately owns lexical review, and implemented REQ-008 separately owns its four uncertainty plus two refusal-phase cases.

The LATE REQ-JAVA-MAVEN-MOVE-AUTH-004 review reconciles the promoted feature with current implementation. `JavaMoveClassOperationDispatcher` is the direct Java move-class operation API: `Plan` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence; `Guidance` carries REQ-003 plus four bounded REQ-008 preview uncertainties; and `LexicalReview` carries REQ-004 alone. This is not generic `LanguageAdapter` direct-apply evidence, and the cross-surface evidence in this paragraph belongs only to REQ-004. Schema-v1 reports `NOT_SEMANTICALLY_PROVEN`/`REVIEW_ONLY`/`LEXICAL_FALLBACK`/`INELIGIBLE`/`evidence.insufficient`, deterministic request/exact-snapshot/hidden-review-artifact/evidence SHA-256 identities, and `lexical-review-<sha256>` non-capability identity. The hidden artifact hash commits to internal compatibility file-operation paths, exact edit ranges, and replacement/create bytes; exposed records are only bounded normalized path/current-content-hash/typed-risk identities with `managedEdit=false`, `COMPLETE or TRUNCATED` record completeness, deterministic returned-category counts, and fixed next actions. CLI/protocol projections contain no plan/edit/range/replacement/apply token; recipe stops without an aggregate; daemon/LSP/MCP use lifecycle-cleared envelope-only LRU correlation; and shared exactly-one apply-identity parsing distinguishes known-operation structured `evidence.insufficient` from unknown/evicted `INVALID_PARAMS`. Daemon/LSP preserve JSON-RPC error `data`; MCP returns structured error content. The dedicated OpenJDK-21 report discovered 22 cases, executed/passed the one 38-step REQ-004 case, skipped 21, and had zero failures/errors; reviewed log SHA-256 `5fb89c290bd098f249c2ec734c8c7bbffae162e5357ab6b1683042fc68e698ce`. The test also preserves clean JDT managed-plan behavior and exact non-mutation. This is source-built/in-process evidence, not packaged/runtime/native qualification.

The LATE REQ-JAVA-MAVEN-MOVE-AUTH-008 review reconciles the corrected feature with current production/test code. Four complete preview uncertainties return immutable schema-v1 `JavaMoveClassReviewOnlyGuidance` with typed layers/codes for inside-closure ambiguity, unresolved static wildcard lookup, outside-closure explicit old FQN with closure precedence, and same-`S0` before/staged diagnostic identity drift; the last is collected by the dispatcher's second stage. None of those four returns `LexicalReview`, plan, or edit, and source-built CLI apply refuses `guidance.nonManaged` pre-`PatchEngine`/lock/WAL/residue. Missing root-declared `catalog-shipping/pom.xml` returns result-owned typed `JavaMoveClassStructuralRefusal`; deterministic component-wise workspace-relative `NOFOLLOW` identity supports a missing module directory and distinguishes it from a missing final POM, while symlink/non-regular states are not `MISSING`; source-built CLI refuses pre-lock/no-residue. The final case starts from eligible REQ-007 and binds one non-managed `BOUND_OTHER` source through immutable `OperationAuthorityFileEvidence`. `PatchEngine` captures one under-lock observed engine snapshot `S1`; immutable `DiagnosticDetails` reports primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` before WAL, preserving external `S1` and possibly the lock file. The semantic candidate-inventory hash is expected-only; expected/observed hashes describe required-file evidence, and no observed semantic inventory is claimed. All six remain independent of REQ-003/004/010/011/012. Surface evidence is limited to the source-built library and CLI for the five preview-time rows, and to library/`PatchEngine` acceptance for the under-lock row; no REQ-008 recipe/daemon/LSP/MCP parity is claimed.

== Use-case Overview Diagram

[source,plantuml]
----
@startuml
left to right direction
actor Developer
actor "Automation / CI" as CI
actor "Editor / IDE User" as IDEUser
actor "Local LLM Agent" as Agent
actor "Embedding JVM App" as App
rectangle RefactorKit {
  usecase "Scan Workspace" as UC_SCAN
  usecase "Build Symbol Index" as UC_SYMBOLS
  usecase "Run Diagnostics" as UC_DIAG
  usecase "Evaluate Authoritative Snapshot Diagnostics" as UC_AUTH_DIAG
  usecase "Preview Refactoring" as UC_PREVIEW
  usecase "Apply Patch Plan" as UC_APPLY
  usecase "Run Managed Compensation" as UC_ROLLBACK
  usecase "Run Java Refactoring" as UC_JAVA
  usecase "Rename Direct-child Maven Module" as UC_MODULE_RENAME
  usecase "Run Recipe" as UC_RECIPE
  usecase "Import External Java Class" as UC_IMPORT
  usecase "Serve JSON-RPC Daemon" as UC_DAEMON
  usecase "Serve LSP" as UC_LSP
  usecase "Serve MCP Tools" as UC_MCP
  usecase "Generate Agent Context Bundle" as UC_CONTEXT
  usecase "Package Self-contained CLI" as UC_PACKAGE
  usecase "Run Golden Tests" as UC_GOLDEN
  usecase "Run Structural Multi-language Tools" as UC_STRUCT
}
Developer --> UC_SCAN
Developer --> UC_PREVIEW
Developer --> UC_APPLY
Developer --> UC_ROLLBACK
Developer --> UC_JAVA
Developer --> UC_RECIPE
Developer --> UC_IMPORT
Developer --> UC_STRUCT
CI --> UC_SCAN
CI --> UC_DIAG
CI --> UC_PACKAGE
CI --> UC_GOLDEN
IDEUser --> UC_LSP
Agent --> UC_MCP
Agent --> UC_CONTEXT
Agent --> UC_IMPORT
App --> UC_PREVIEW
App --> UC_APPLY
App --> UC_MODULE_RENAME
UC_APPLY ..> UC_AUTH_DIAG : includes for model-changing edits
UC_AUTH_DIAG ..> UC_DIAG : includes
UC_AUTH_DIAG ..> UC_ROLLBACK : on post-apply mismatch
UC_JAVA ..> UC_PREVIEW : includes
UC_MODULE_RENAME ..> UC_PREVIEW : includes
UC_MODULE_RENAME ..> UC_APPLY : after approval
UC_RECIPE ..> UC_PREVIEW : includes
UC_IMPORT ..> UC_PREVIEW : includes
UC_APPLY ..> UC_ROLLBACK : after complete success
@enduml
----

== Use-case Catalogue

[cols="1,2,2,3,2,1",options="header"]
|===
|ID |Name |Actor |Description |Validation mode |Observed status

|UC-SCAN
|Scan workspace
|Developer, CI, Agent
|Discover workspace files, Java source roots, Maven/Gradle structure, and project snapshot data.
|CLI examples, Java scanner tests, CI smoke scan.
|implementation-informed

|UC-SYMBOLS
|Build/search symbols
|Developer, Agent, Editor
|Expose Java symbols, definitions, and references for focused code intelligence.
|CLI docs, adapter tests, MCP/LSP docs.
|implementation-informed

|UC-DIAGNOSTICS
|Run diagnostics
|Developer, CI, Agent, Editor
|Report structural findings and, for the planned authoritative Java/Maven slice, either source-accurate semantic diagnostics from a complete attested source-set environment or concise typed unavailability. Maven compile/test remains supplementary evidence.
|Current Java adapter tests plus planned `RPK-JAVA-DIAG-001..007` permanent/native packaged acceptance.
|generic command implementation-informed; authoritative epic planned, product-critical

|UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION
|Evaluate authoritative snapshot diagnostics
|Embedding JVM application / managed-apply caller
|For a model-changing edit explicitly admitted to the bounded authoritative mode through public `PatchEngine.apply`, bind baseline, staged, committed, and restored diagnostics to authoritative snapshots of the same exact candidates; constrain provider authority over the tracked shell; journal exact `S1.hash`; and automatically roll back a post-apply mismatch through the same record. Existing diagnostics-only gates remain additive compatibility behavior.
|`features/authoritative-diagnostics-evaluation.feature`; `C-TECH-034`; `R-036`; accepted ADRs 0002/0012; independent 16/16-scenario, 128/128-step acceptance.
|implementation-informed and `@implemented-and-validated` for four functional definitions/16 cases; no generic operation, protocol, package/platform, release, or support qualification

|UC-PREVIEW-REFACTOR
|Preview refactoring
|Developer, Agent, Editor/App
|Generate `PatchPlan` previews before applying mutating operations. For exactly five ordinary Java routes, the implemented sealed command hierarchy and fieldless dispatcher provide one canonical downstream planner path while daemon/MCP and the generic adapter retain their distinct upstream decoding policies.
|Core/Java tests, golden tests, AGENTS safety rules; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001 through `004`.
|implementation-informed; five-route command-boundary qualification is source-built/in-process only

|UC-APPLY-PATCH
|Apply patch plan
|Developer, Agent/App
|Acquire the workspace writer lock, check a file-derived current snapshot hash, revalidate initially affected paths, and preflight selected path, symbolic-link, overlap, and ordered file-state rules before sequential managed writes. Current behavior is a compensatable batch, not an ACID or crash-safe transaction.
|`PatchEngine`, tests, transactionality audit.
|implementation-informed limitation

|UC-ROLLBACK
|Run managed compensation
|Developer, Agent/App
|Persist compensation and pre/post recovery images before mutation, retain lifecycle history, and replay rollback under the workspace lock. Current normal rollback is not conflict-safe against post-apply changes and does not restore complete filesystem metadata. Implemented REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 share only pre-lock resolution, caller-selected visibility, surface-owned guard invocation, one public engine call, and typed outcome transport while leaving rollback and surface policy in their existing owners.
|Current core tests, testkit agent simulation, and transactionality audit `TX-002`, `TX-004`, `TX-013`; `ManagedRollbackExecutor.kt`; inspected CLI/daemon/LSP/MCP adoption; `features/managed-rollback-executor-extraction.feature`; mutable-diagnostics RED log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`; final focused log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`.
|implementation-informed limitation for current rollback and implementation-informed bounded shared executor; packaged, cross-platform, concurrent rollback, crash/restart, numerical coverage, and altered rollback authority remain unqualified

|UC-JAVA-RENAME-CLASS
|Rename Java class
|Developer, Agent, Editor
|Rename class declaration, file, imports, references, and constructors where supported.
|Golden tests, planner tests, CLI docs.
|implementation-informed

|UC-JAVA-RENAME-MEMBER
|Rename Java method or field
|Developer, Agent, Editor
|Rename scoped Java member occurrences while avoiding unrelated same-name members where possible.
|Planner tests, integration docs.
|implementation-informed

|UC-JAVA-MOVE-CLASS
|Move Java class
|Developer, Agent
|For the first accepted same-module/source-set Maven `moveClass` shape, keep active-POM/selected-descriptor/candidate/platform/provider authority global while limiting selected-missing-binary and retained-diagnostic evidence to the operation closure. Implemented-and-validated `001`-`007` and `009`-`012` retain their bounded positive, guidance, lexical-review, scope, selector, ordinary-absence, and selected-descriptor evidence. Implemented-and-validated `008` adds four non-lexical preview-guidance blockers, one result-owned active-reactor-POM structural refusal, and one under-lock required-file-evidence freshness refusal. Its expected semantic candidate-inventory identity is distinct from expected/observed required-file-evidence identities. Caller classpath attestation, plugin execution, settings credentials, implicit network, and lexical promotion remain forbidden.
|The Maven move-class feature has twelve validated IDs across 22 validated cases. Focused REQ-008 evidence passed six selected cases, skipped sixteen, and had zero failures/errors. Accepted ADR 0012 and the 20-module fixture govern only bounded evidence; the exact Magrathea `a48e3c06` CLI/Linux/JDK-21 result remains separate.
|confirmed P0 target-scoped architecture; twelve bounded definitions/22 cases and one exact CLI field request implementation-informed; general, packaged/runtime integration, and native-matrix acceptance remain open

|UC-JAVA-RENAME-MAVEN-MODULE
|Rename one direct-child Maven module
|Maven workspace maintainer through an embedding JVM application
|For the first bounded row, invoke the public Java scanner/planner directly against one declared authoritative offline reactor, rename exact direct child `catalog-model` to `catalog-domain`, and change the artifact component only because the caller independently supplies `newArtifactId=catalog-domain`. Origin-proven POM modifications precede two child-file renames; explicit direct-library apply and normal rollback remain owned by `PatchEngine` and the existing journal.
|`features/java-maven-module-rename.feature`; independent 1/1-scenario, 50/50-step direct-library acceptance; accepted ADRs 0002/0010/0012.
|implementation-informed and `@implemented-and-validated` for the exact direct-library fixture row; no protocol, recipe, package/platform, general-Maven, native-CI, release, or broader support qualification

|UC-JAVA-ORGANIZE-IMPORTS
|Organize Java imports
|Developer, Agent
|Sort/deduplicate imports and remove same-package imports; unused removal remains limited.
|Golden test, docs/testing limitation.
|implementation-informed

|UC-JAVA-SAFE-DELETE
|Safe delete Java symbol
|Developer, Agent
|Refuse deletion when references are found unless explicitly overridden by supported workflow.
|Golden refused case, planner tests.
|implementation-informed

|UC-JAVA-EXTRACT-METHOD
|Extract Java method MVP
|Developer, Agent/Editor
|Extract complete selected lines into no-argument `private void` method for conservative safe cases.
|Extract-method docs and tests.
|implementation-informed

|UC-JAVA-CHANGE-SIGNATURE
|Change Java method signature MVP+
|Developer, Agent/Editor
|Rename/add/reorder/remove parameters for conservative non-overloaded cases.
|Change-signature docs and tests.
|implementation-informed

|UC-RECIPE-RUN
|Run recipe
|Developer, CI, Agent
|Stage eligible YAML-defined Java refactoring steps against one evolving immutable snapshot, reduce the initial-to-final delta to one recipe-wide `PatchPlan`, and apply through one WAL transaction. Planning failure/no-op writes nothing. Implemented REQ-004 adds a terminal `RecipeResult.NonManaged` branch for lexical `moveClass`: discard prior staging, run no later step, and return no aggregate plan. REQ-003/008 guidance remains a distinct dispatcher result; this use-case row claims no recipe-path parity for it.
|`RecipeEngine`, recipe tests, transactionality audit `TX-008`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|implementation-informed for eligible recipe transactions and the bounded lexical early-stop

|UC-IMPORT-EXTERNAL-JAVA
|Import external Java class
|Developer, Agent
|Assimilate Java source from clipboard/file/URL/snippet/LLM response with provenance/license/conflict checks and preview-only plan.
|Importer docs and tests.
|implementation-informed

|UC-DAEMON
|Serve JSON-RPC daemon
|Developer/App/Tool
|Provide long-running local JSON-RPC operations over stdio. Java lexical `moveClass` returns an edit-free envelope and never a pending plan; bounded audit correlation uses `operationId` only. Ordinary admitted payloads are retained in the session-owned shared core store while daemon-specific admission, discard, apply/refusal, rollback, project-open, close, response, and authority policy stays in `DaemonSession`.
|`DaemonSession`, `DaemonRequestScheduler`, shared `RefactoringApplyIdentity`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004 and REQ-PENDING-PLAN-STORE-001/002.
|implementation-informed for ordinary plans, the bounded in-process lexical contract, and source-built/in-process pending-plan behavior; packaged stdio/runtime and concurrency qualification remains open

|UC-LSP
|Serve LSP
|Editor / IDE
|Expose editor-native definition, references, rename, code actions, symbols, semantic tokens, and diagnostics. Admitted native WorkspaceEdits are explicitly client-managed/no-rollback and carry tracked open-document versions; custom `applyPlan` is managed and refuses dirty/affected-open buffers. Java lexical `moveClass` returns command data before the WorkspaceEdit renderer, with neither `changes` nor `documentChanges` nor a pending plan. Ordinary admitted plans use the session-owned shared core store while LSP-specific admission, selected removal, refusal/rollback retention, document lifecycle, response, and authority policy stays in `LspSession`.
|`LspSession`, `RefactorKitLsp`, transactionality audit `TX-007`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004 and REQ-PENDING-PLAN-STORE-001/002.
|implementation-informed for admitted edits, the bounded in-process lexical path, and source-built/in-process pending-plan behavior; packaged editor/native and concurrency qualification remains open

|UC-MCP
|Serve MCP tools/resources/prompts
|Local LLM Agent
|Expose deterministic project, symbol, diagnostics, preview/apply/rollback/import/context-bundle tools. Java lexical preview returns canonical `structuredContent` without Plan ID/apply instructions; exactly-one operation/plan identity is advertised, and a known `operationId` produces only `isError=true` structured refusal. Ordinary retained payloads use the session-owned shared core store while MCP-specific preview/import admission, selected/stale-Kotlin removal, general refusal/rollback retention, project-scan/close, response, and authority policy stays in `McpSession`.
|`McpSession`, `RefactorKitMcp`, shared `RefactoringApplyIdentity`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004 and REQ-PENDING-PLAN-STORE-001/002.
|implementation-informed for ordinary plans, the bounded in-process lexical contract, and source-built/in-process pending-plan behavior; packaged transport/runtime and concurrency qualification remains open

|UC-CONTEXT-BUNDLE
|Generate AI context bundle
|Local LLM Agent
|Return focused project/symbol/snippet/diagnostic context for safe AI workflows.
|MCP docs.
|implementation-informed

|UC-STRUCTURAL-TOOLS
|Run structural multi-language tools
|Developer, Agent
|Outline/search/local-rename supported languages using structural/lexical mechanisms.
|Multi-language docs and tests.
|implementation-informed

|UC-PACKAGE-CLI
|Package self-contained CLI
|Maintainer, CI
|Build runtime distribution with embedded Java runtime and smoke-test it.
|Packaging docs, Gradle tasks, CI.
|implementation-informed

|UC-GOLDEN-TESTS
|Run golden tests
|Maintainer, CI
|Validate before/after/request/expected-plan cases for refactoring behavior.
|Testkit docs, CI.
|implementation-informed
|===

== Use-case-to-Evidence Mapping

[cols="1,2,3",options="header"]
|===
|Use case |Requirement / source ID |Evidence

|UC-SCAN
|REQ-SCAN-001
|README CLI example; `JavaProjectScanner`; CI packaged CLI smoke scan.

|UC-DIAGNOSTICS
|RPK-JAVA-DIAG-001 through RPK-JAVA-DIAG-007; antecedents RPK-PKG-007/009, REQ-TX-007, RPK-JVM-DIAG-002/003/005/006
|`docs/requirements/java-maven-authoritative-diagnostics.md`; ADR 0012; chapters 6, 8, and 10. Epic status is active/product-critical; linked executable acceptance, not this mapping, is implementation evidence.

|UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION, UC-DIAGNOSTICS, UC-APPLY-PATCH, UC-ROLLBACK
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-004
|`features/authoritative-diagnostics-evaluation.feature`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012; Chapters 5, 6, and 8-12. All four functional definitions and all 16 expanded cases are `@implemented-and-validated`. The mapping is bounded to the additive language-neutral evaluator and does not imply generic operation, protocol, packaged/native/cross-platform, release, or support qualification.

|UC-JAVA-MOVE-CLASS
|RPK-JAVA-MOVE-001 through RPK-JAVA-MOVE-007; executable Gherkin REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`
|`features/java-maven-move-class-apply-authority.feature`; current source/tests and focused evidence; ADRs 0002, 0008, and 0010-0012; chapters 2, 4-6, 8, and 10-12. All twelve definitions in that feature and all 22 expanded cases map to this use case and carry bounded tagged evidence. REQ-008 maps its four guidance, one structural, and one freshness case without broadening REQ-003/004/010/011/012. The dated Magrathea `a48e3c06` CLI/Linux/JDK-21 result remains separate field evidence, not another Gherkin case or broader qualification. The implemented packaged-process runner is a second validation mode for the same REQ-001 mapping and same pickle; it adds no use case, requirement, case, or status. One local Linux `amd64` run is observed, while all four native CI rows remain `CONFIGURED_UNOBSERVED`.

|UC-JAVA-RENAME-MAVEN-MODULE, UC-PREVIEW-REFACTOR, UC-APPLY-PATCH, UC-ROLLBACK
|REQ-JAVA-MAVEN-MODULE-RENAME-001
|`features/java-maven-module-rename.feature`; `docs/requirements/java-maven-ownership-migration.md`; `C-TECH-033`; `C-BOUND-010`; `QS-MODULE-RENAME-001`; `R-035`; accepted ADRs 0002, 0010, and 0012. The one functional definition/case is `@implemented-and-validated`. The mapping remains direct-library/fixture bounded and promotes no integration surface or broader support row.

|UC-PREVIEW-REFACTOR
|REQ-SAFE-001
|`PatchPlan`, Java planners, MCP/LSP/daemon preview contracts.

|UC-PREVIEW-REFACTOR, UC-DAEMON, UC-MCP
|REQ-JAVA-PREVIEW-COMMAND-001 through REQ-JAVA-PREVIEW-COMMAND-004
|`features/java-refactoring-preview-command-boundary.feature`; `JavaRefactoringPreviewCommand.kt`; five generic Java adapter decoder routes; five daemon/MCP adopting routes; Java-module and real public-session split runners; `C-TECH-032`; `C-BOUND-009`; `QS-CORR-011`; `R-033`; Chapters 2, 4-6, and 8-12. All four definitions/eight cases are implemented-and-validated within the bounded source-built/in-process behavior; the mapping adds no external protocol capability or managed-write authority.

|UC-APPLY-PATCH
|REQ-SAFE-002, REQ-TX-001 through REQ-TX-008, REQ-TX-011, REQ-TX-012
|`PatchEngine.apply`, snapshot hash validation, overlap/path validation, transactionality audit.

|UC-APPLY-PATCH, UC-DAEMON, UC-MCP
|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001 and REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002
|`features/managed-apply-diagnostics-gate-selector.feature`; `ManagedApplyDiagnosticsGateSelector.kt`; source-inspected daemon/MCP adoption; final focused acceptance log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`; final whole-working-tree source-built log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`; `C-TECH-030`; `C-BOUND-007`; `QS-DIAG-007`/`008`; `R-031`; Chapters 2, 4-6, and 8-12. Both definitions and all 11 cases are implemented-and-validated within the bounded selector behavior; the mapping adds no protocol capability or apply authority.

|UC-ROLLBACK
|REQ-SAFE-003, REQ-TX-004 through REQ-TX-006; REQ-MANAGED-ROLLBACK-EXECUTOR-001 and REQ-MANAGED-ROLLBACK-EXECUTOR-002
|Current evidence: `Transaction`, `TransactionLog`, agent simulation tests, and transactionality audit. Implemented shared-orchestration mapping: `ManagedRollbackExecutor.kt`, inspected CLI/daemon/LSP/MCP adoption, `features/managed-rollback-executor-extraction.feature`, mutable-diagnostics RED log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`, final focused acceptance log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`, `C-TECH-029`, `C-BOUND-006`, `QS-SAFE-012`, `R-030`, and ADRs 0002/0004. Both managed-executor requirements are implementation-informed within the source-built/in-process boundary.

|UC-RECIPE-RUN
|REQ-TX-010
|Recipe engine tests and transactionality audit.

|UC-LSP
|REQ-TX-009
|LSP module/tests and transactionality audit.

|UC-IMPORT-EXTERNAL-JAVA
|REQ-IMPORT-001
|`ExternalJavaClassImporter`, license/provenance/conflict docs.

|UC-MCP
|REQ-AI-001
|`docs/ai-workflow.md`, `docs/integration-contracts.md`, MCP module.

|UC-DAEMON, UC-LSP, UC-MCP
|REQ-PENDING-PLAN-STORE-001 and REQ-PENDING-PLAN-STORE-002
|`features/pending-plan-store-extraction.feature`; exact `PendingPlanStore.kt`, `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt` production paths; `PendingPlanStoreCucumberTest`/steps; chapters 2, 4-6, 8, and 10-12. The mapping is supporting retention behavior inside existing integration use cases, not a new protocol capability or authority boundary.

|UC-SCAN, UC-DAEMON, UC-MCP
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through REQ-WORKSPACE-SNAPSHOT-COMPOSER-005
|`features/workspace-snapshot-composer-extraction.feature`; `WorkspaceSnapshotComposer.kt`; source-inspected private daemon/MCP adoption; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-001`/`002`; `R-032`; Chapters 2, 4-6, and 8-12. All five definitions/eight cases are `@implemented-and-validated`. This is supporting implemented composition within existing scan and integration use cases, not a new user-visible capability, protocol, persistence boundary, or mutation authority.

|UC-PACKAGE-CLI
|REQ-PACKAGE-001
|Gradle jlink tasks, packaging docs, CI.
|===

== Java/Maven Diagnostics Authority Epic

The detailed normative requirements remain in `docs/requirements/java-maven-authoritative-diagnostics.md`; this appendix provides the integrated catalogue and does not duplicate a competing requirement body. The IDs do not conflict with existing repository conventions.

[cols="1,3,2,1",options="header"]
|===
|ID |Slice |Acceptance owner/evidence |Status

|RPK-JAVA-DIAG-001
|Exact immutable Java 8-25 release-platform signatures; explicit qualified JDK platform or typed unavailability for no-release/source-only configurations.
|Java adapter and packaging acceptance; ADR 0012.
|planned, product-critical

|RPK-JAVA-DIAG-002
|Declared full-reactor authority, independent source sets, and module filtering after model construction without implicit outward discovery.
|Build Model and Java adapter acceptance.
|planned, product-critical

|RPK-JAVA-DIAG-003
|Exact Maven main/test reactor and external-artifact visibility.
|Build Model/JDT permanent reactor fixture.
|planned, product-critical

|RPK-JAVA-DIAG-004
|Source-set typed availability roots and bounded causal cascade suppression.
|Diagnostics provider and protocol acceptance.
|planned, product-critical

|RPK-JAVA-DIAG-005
|Snapshot/evidence-bound before, staged, after, and rollback diagnostics; default no-introduced-errors, strict zero, and affected-unavailability blocker.
|Core diagnostics gate plus integration acceptance.
|planned, product-critical

|RPK-JAVA-DIAG-006
|Bounded, offline-default, plugin/lifecycle/processor/credential-free reproducibility and drift refusal.
|Build Model/security/native acceptance.
|planned, product-critical

|RPK-JAVA-DIAG-007
|Permanent minimal reactor and four-platform packaged qualification; classpath reactors first and typed JPMS refusal.
|Testkit, packaging, CLI/daemon/MCP release gates.
|planned, product-critical
|===

No row in this epic is marked implemented by this architecture phase. Existing precursor code, tests, or Maven successes must be evaluated separately and cannot satisfy the epic without the listed end-to-end authority evidence.

== Java/Maven Move-Class Apply Authority Epic

This architecture integration summarizes the reviewed requirement without replacing specialist evidence. Accepted ADR 0012 distinguishes global source-set authority from narrower target-scoped Maven `moveClass` authority. The Maven move-class feature contains exactly twelve definitions and 22 cases, all implemented-and-validated. Bounded `001`-`007` and `009`-`012` retain their existing meanings; bounded `008` establishes four immutable preview-guidance outcomes, one active-reactor-POM structural refusal, and one under-lock required-file-evidence refusal. No general or native-matrix authority is inferred. The exact Magrathea `a48e3c06` request remains separately CLI/Linux/JDK-21-qualified. The dedicated packaged-process Cucumber mode is now implemented locally only for existing REQ-001; because it reuses the same feature/ID/pickle, it changes validation mode rather than this catalogue or its counts. The local Linux `amd64` observation does not promote any of the four `CONFIGURED_UNOBSERVED` post-commit native CI rows.

[cols="1,3,2,1",options="header"]
|===
|ID |Slice |Acceptance owner/evidence |Status

|RPK-JAVA-MOVE-001
|First supported result boundary: one uniquely bound top-level class in a writable materialized Maven main/test source set moves within the same module/source set. The direct Java move-class API `JavaMoveClassOperationDispatcher` returns disjoint `Plan(JavaMoveClassPreview)`, `Guidance(JavaMoveClassReviewOnlyGuidance)`, or `LexicalReview(JavaMoveClassLexicalFallbackReviewEnvelope)`. `Plan` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence; `Guidance` carries REQ-003 plus four bounded REQ-008 preview uncertainties; `LexicalReview` carries REQ-004 alone; and neither non-managed arm can become a core `PatchPlan`. This operation-specific Java boundary is not the generic `LanguageAdapter` `PatchPlan` API. No result silently edits non-Java/string contracts.
|Java adapter contract and operation-shape acceptance; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001, `003`, `004`, and `008`: five preview-time library/CLI rows plus one under-lock library/`PatchEngine` row.
|bounded semantic plan; REQ-003 and five REQ-008 preview-time library/CLI outcomes; one REQ-008 library/`PatchEngine` under-lock outcome; REQ-004 direct/CLI/recipe/daemon/LSP/MCP parity; and one local Linux `amd64` packaged-process REQ-001 mode implementation-informed; REQ-002..012 packaged/runtime, general, and every native CI row remain planned P0

|RPK-JAVA-MOVE-002
|Every active reactor POM and, per main/test projection, every selected external node's effective POM/parent/BOM/management/mediation/relocation closure must be complete, current, unambiguous, and hash-bound. Every outgoing declaration has a selected or exact derived-scope/optional/exclusion-pruned record with path/projection provenance. A pruned edge contributes no child/subtree and needs no child POM/JAR on that path; alternate selected paths restore requirements. Selection precedes child lookup when downstream facts cannot affect it; type/classifier are identity, not prune reasons. Ownership, source/materialized-generated-root inventory, Java platform/provider, full-reactor candidate audit/exclusion, and reverse-observer facts remain global. Selected-missing-binary evidence is operation-closure scoped; missing included descriptors block.
|Build Model and JDT reactor acceptance. Bounded `001`/`002` own complete-`AVAILABLE` and observer rows; `006`/`007`/`009`-`012` retain their existing outcomes; `008` adds the root-declared active-POM and closure/candidate/lookup/diagnostic uncertainty cases without changing selected-descriptor boundaries.
|all twelve bounded definitions/22 cases implementation-informed; general, packaged/runtime integration, and native-matrix acceptance remain absent

|RPK-JAVA-MOVE-003
|One exact non-recovered target declaration plus an independent bounded lexical Java inventory of every old-FQN and type-capable matching simple-name range. JDT classifies each candidate exactly once as `BOUND_TARGET`, `BOUND_OTHER`, or `UNRESOLVED`; every target-relevant or relevance-uncertain unresolved candidate blocks. Only `BOUND_TARGET` selects edits. In the ordinary missing-binary row, every target simple-name use is protected by an exact resolved non-static single-type target import, or the use is the exact target FQN; relevant wildcard/static ambiguity, unprotected lookup, recovered/problem evidence, and candidate/diagnostic/edit overlap block. Every retained diagnostic needs exact unchanged mapped before/staged identity and positive non-concealment proof; staged analysis preserves other-symbol classifications, resolves the moved target identity, and introduces no new/changed error.
|JDT analyzer/planner and diagnostic-isolation acceptance. Implemented-and-validated `001` proves one exact non-recovered target binding and every managed edit site binding for the complete-`AVAILABLE` fixture. Implemented-and-validated `002` proves 12 exact `BOUND_TARGET`, one exact `BOUND_OTHER`, and zero `UNRESOLVED` JDT records while keeping one Java non-code residual occurrence and two non-Java residual paths outside managed edits. Existing `006`/`007`/`009` evidence remains scoped; implemented `008` owns only its four uncertainty cases with closure precedence; and `011` owns ordinary selected-leaf lookup protection.
|bounded binding-clean, candidate-total, six-case negative, and ordinary sidecar-free evidence implemented; broader/general acceptance planned P0

|RPK-JAVA-MOVE-004
|Explicit managed-write eligibility revalidates under lock, before WAL, the full descriptor/edge-prune proof, global candidate exclusion, before/staged authority closure, selected-missing-entry repository identity/paths and negative presence, source/candidate inventory, exact bindings/lookup protection, platform/provider, staged post-image, retained diagnostics/non-concealment, delta, conflicts, and freshness. Any selector ambiguity, missing included descriptor, target uncertainty, new/changed error, artifact appearance/disappearance, or drift refuses without mutation. Caller classpath attestation, Maven/plugin execution, settings credentials, implicit network, and lexical evidence supply no authority.
|Core `PatchEngine` and integration acceptance. Implemented-and-validated `001` proves one lock-held exact staged-image commit and exact rollback after revalidation. Implemented-and-validated `002` independently proves one exact staged-image transaction and exact full non-engine workspace rollback for its observer-discrimination request. Implemented-and-validated `004` proves lexical compatibility-plan core refusal before lock/WAL plus no managed/client application through its bounded surface paths. Existing `005`/`006`/`007`/`009`-`012` evidence remains scoped. Implemented `008` adds one generic immutable `OperationAuthorityFileEvidence` record for the non-managed `BOUND_OTHER` source; one under-lock engine observation returns primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` before WAL, preserving `S1`. It reports expected semantic candidate-inventory identity and expected/observed required-file-evidence identities only.
|twelve validated definitions expanding to 22 bounded cases implementation-informed; general, packaged/runtime, and native acceptance absent

|RPK-JAVA-MOVE-005
|Two non-managed contracts remain distinct. Implemented `JavaMoveClassReviewOnlyGuidance` is plan-free schema/checklist-v1 `REVIEW_ONLY_GUIDANCE` for the four safely enumerable REQ-003 Maven defects and four bounded REQ-008 preview uncertainties and uses `guidance.nonManaged`. Implemented `JavaMoveClassLexicalFallbackReviewEnvelope` is the protocol-safe schema-v1 projection over an internal lexical compatibility preview: deterministic non-capability `operationId` plus request/exact-snapshot/hidden-review-artifact/evidence SHA-256, `NOT_SEMANTICALLY_PROVEN`/`REVIEW_ONLY`/`LEXICAL_FALLBACK`/`INELIGIBLE`/`evidence.insufficient`, and bounded exposed path/hash/risk guidance. The hidden artifact hash alone commits to exact internal compatibility edit ranges and replacement/create bytes. No protocol exposes the internal random ID, edit/range/replacement/apply token, pending plan, LSP client edit, transaction, or rollback capability. For the REQ-004 lexical envelope, recipes terminate without an aggregate; no recipe parity is claimed for REQ-003/008 guidance. A known `operationId` retrieves only the same envelope from a bounded envelope-only audit cache for typed refusal; unknown/evicted operations and unknown plans remain invalid. Neither non-managed type converts to the other or to a semantic plan; only a full rescan/new preview can yield a new result.
|Implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003 owns four bounded guidance variants; `004` owns the lexical envelope and bounded surface parity; `008` independently owns four complete preview-time guidance extensions, one result-owned active-reactor-POM structural refusal, and one required-file-evidence drift refusal under lock before WAL. No `008` case returns `LexicalReview`.
|implementation-informed for bounded `003`, `004`, and `008`; generalization, packaged/runtime execution, and native qualification remain P0

|RPK-JAVA-MOVE-006
|Permanent offline fixture with one aggregator and 20 active non-aggregator modules, dependency-depth/main/test observers, Java Cucumber references, unrelated/same-name decoys, local external dependencies, materialized generated source, and independent authority-loss variants. Bounded `010` proves deterministic selector pruning before child lookup and alternate selected-path restoration. Implemented-and-validated `011` proves one fixed-release unclassified `jar` selected graph leaf in a simple local Maven-2-layout repository with complete present descriptor closure but absent JAR, RefactorKit-owned sidecar-free negative presence, graph-leaf proof, no invented content hash or provided-type inventory, candidate-total lookup/diagnostic gates, one transaction, exact rollback, and three under-lock pre-WAL drift refusals. Implemented-and-validated `012` adds six independent fixture variants for selected leaf POM absence/malformed XML, required parent loss, imported-BOM drift, management/mediation drift, and parsed relocation/model identity evidence loss.
|The fixture and twelve current Gherkin definitions exist; bounded `001` proves its complete-`AVAILABLE` positive shape; bounded `002` proves exact direct/transitive/test-only binding observers, candidate/residual separation, unchanged exclusions, and exact apply/rollback; `007`/`009`/`010`/`011` and the six `012` cases remain fixture-specific. Separately, one Magrathea CLI/Linux/JDK-21 request at `a48e3c06` remains field-qualified after `010`.
|fixture and twelve validated definitions expanding to 22 bounded cases implemented; general, packaged/runtime integration-surface, and native-matrix qualification remain absent/blocked

|RPK-JAVA-MOVE-007
|Stable operation identity plus separate reactor-structure/external-classpath states, exact operation authority closure, active-POM/selected-descriptor identities, selected/pruned edge records with path/projection/reason, closure-enumerated selected-missing entries including explicit absent-content-hash none, outside-closure globally visible missing state, target/observer identities, environment/candidate hashes, binding/lookup records, isolated diagnostics/non-concealment, delta, eligibility, blockers, and residual guidance. `REVIEW_ONLY_GUIDANCE` audit binds exact request/snapshot/canonical evidence hash, expected/observed fingerprints, structured module/source-set blocker identity, typed candidate groups and omissions, restoration-action order, checklist version, and explicit non-managed status. Implemented `LEXICAL_FALLBACK_REVIEW` instead binds deterministic `operationId`, request/exact-snapshot/hidden-review-artifact/evidence hashes, fixed `NOT_SEMANTICALLY_PROVEN` review-only/ineligible disposition, and bounded path/hash/risk residual metadata without any plan/apply/edit/range/replacement token. Audit distinguishes managed `PlanId` from non-capability `operationId`, pending plan from bounded envelope-only audit cache, and REQ-003 typed guidance from broad legacy lexical review. Implemented REQ-008 reporting distinguishes authority layers `CANDIDATE_TOTALITY`, `TARGET_NAME_LOOKUP`, `CLOSURE_CONSISTENCY`, `DIAGNOSTIC_IDENTITY`, `STRUCTURAL_CLOSURE`, and `EVIDENCE_FRESHNESS`; records `S0`, `S*`, `E0`, and `S1` only where applicable; gives outside-closure precedence; and distinguishes expected semantic candidate inventory from expected/observed required-file evidence.
|Versioned protocol/audit acceptance. Implemented-and-validated `001` adds deterministic snapshot/closure/edit/diagnostic/transaction/rollback reporting evidence; `002` adds exact JDT-only target/other/unresolved totals and distinct residual reporting; `003` adds schema/checklist version 1 guidance; `004` adds the canonical cross-surface lexical envelope, hidden artifact commitment, bounded residual counts/completeness, known-operation structured refusal, exactly-one identity parser, no LSP edit, no recipe aggregate, cache bounds/lifecycle, and clean-JDT preservation; existing `006`/`007`/`009`-`011` remains bounded; `012` adds descriptor blockers/hashes; `008` adds immutable layer data, structural refusal data, generic file evidence, and structured diagnostics.
|bounded reporting evidence implemented for twelve definitions expanding to 22 cases; general/packaged/runtime/native contracts remain P0
|===

Twelve bounded Gherkin definitions expanding to 22 cases are implementation-informed. REQ-008 contributes four pre-lock/no-residue guidance cases, one pre-lock/no-residue active-reactor-POM structural refusal, and one lock-held/pre-WAL required-file-evidence refusal that preserves external `S1` and may leave the lock file. All evidence remains fixture/request/surface bounded. The exact Magrathea `a48e3c06` request remains separately CLI/Linux/JDK-21-qualified; packaged/runtime/native/general authority remains open.

=== Bounded Target-Authority Acceptance

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status

|REQ-JAVA-MAVEN-MOVE-AUTH-001
|Complete-`AVAILABLE` binding-clean apply/rollback: two previews from identical snapshot/evidence hashes are deterministic `SEMANTIC_PREVIEW` results with `JDT_BINDING` and `ELIGIBLE`. They select exactly three reverse observers and five affected paths. The journal oracle is five top-level `FileEdit` entries—four `Modify` plus one `Rename`; nested `TextEdit` counts are `1/2/1/1` across the four modifies, five total, with no one-edit-per-modify invariant. Authoritative diagnostics remain zero before and staged. Prohibited build-code, Maven lifecycle/plugin/annotation-processor, settings/credential/helper, and network activity is denied. One approved preview commits its exact staged hash/files/auxiliary inputs/seven-source inventory in one lock-held managed transaction; authoritative after diagnostics remain zero. Normal rollback retains the single transaction as rolled back and restores every non-engine byte, path, all seven source entries, the original snapshot hash, and the zero-entry authoritative diagnostic baseline.
|Functional and non-functional
|`RPK-JAVA-MOVE-001`, `002`, `003`, `004`, `006`, and `007`; accepted ADRs 0002, 0008, and 0012
|Explicit scenario-level `@implemented-and-validated`; one bounded isolated permanent-fixture case. Source-built/in-process evidence is observed. A second packaged-process validation mode over this same case is now observed locally on OpenJDK 21.0.11/Linux `amd64`; it creates no additional case or status change. All four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`. This row remains independent of separately validated observer-discrimination scenario `002` and does not validate other integration surfaces, a native matrix, Magrathea, or general authority.

|REQ-JAVA-MAVEN-MOVE-AUTH-002
|Only binding-proven Java observers are updated: direct `catalog-pricing:main` contributes import, constructor, and declared-type uses; transitive `catalog-storefront:main` contributes import, `instanceof`, and class-literal uses; test-only `catalog-acceptance:test` contributes Cucumber import, declared-type, and exact-FQN uses. Every use resolves to the moved target without adding unrelated JDT roots. JDT-only totals are exactly 12 `BOUND_TARGET`, one exact-binding `BOUND_OTHER` same-name decoy, and zero `UNRESOLVED`. The `ProductReport.java` string literal is separately `JAVA_NON_CODE_RESIDUAL=1`; the feature resource and fixture-root `README.md` are the two paths counted by `NON_JAVA_RESIDUAL=2`; every residual reports `managedEdit=false`. The decoy and all three residual paths remain byte-identical. Five binding-derived text edits plus one declaration rename produce the exact staged image; one lock-held transaction applies it and normal rollback restores every non-engine byte/path, full source inventory, and snapshot hash exactly.
|Functional
|`RPK-JAVA-MOVE-002`, `003`, `006`, and `007`; accepted ADRs 0008 and 0012
|Explicit scenario-level `@implemented-and-validated`; one bounded isolated permanent-fixture case. This validates the exercised planner, source-built CLI preview reporting, and core managed apply/rollback path only; it does not establish cross-surface, native-matrix, Magrathea, or general authority.

|REQ-JAVA-MAVEN-MOVE-AUTH-003
|Safely enumerable preview defects produce immutable non-managed guidance. Four independent isolated copies exercise the ordinary default scanner: a readable required step-definition omitted after Maven test-source-root change; a readable system-path JAR whose expected strict manifest fingerprint differs from its freshly observed fingerprint; one recovered target use without syntax failure; and a materialized generated-root inventory mismatch for `catalog-generated-support:main`. Repeat outcomes are equal immutable `JavaMoveClassReviewOnlyGuidance` with schema/checklist version 1, exact request/snapshot/evidence SHA-256 identities, one typed blocker, separate typed candidate/residual groups, explicit completeness and bounded source-inventory omission, ordered actions, and the fixed seven-step checklist. Expected-source/generated-root manifests are snapshot-bound auxiliary files; relevant manifest content hashes and classpath fingerprints enter evidence. JSON CLI preview contains no plan/apply/transaction/rollback capability. The same CLI command with `--apply` returns `guidance.nonManaged` before `PatchEngine`, lock, or WAL and preserves all bytes, paths, inventories, and snapshot hash. Broad syntax failure remains REQ-005 lexical behavior. Structural loss/unreadability and post-preview drift are outside REQ-003. Implemented REQ-008 independently validates four additional complete preview-guidance uncertainties plus its named root-POM and candidate-source drift cases; implemented REQ-004 separately validates lexical review. Neither broadens the four-case REQ-003 evidence boundary.
|Functional and non-functional. The typed read-only handoff is observable behavior; deterministic evidence, honest completeness, structural non-applicability, and pre-lock/pre-WAL non-mutation are correctness and safety constraints.
|`RPK-JAVA-MOVE-002`, `004`, `005`, `006`, and `007`; ADRs 0002, 0008, 0010, and 0012; `JavaMoveClassReviewOnlyGuidance`, `JavaMoveClassOperationDispatcher`, `JavaMoveClassGuidanceJsonRenderer`; `JavaMavenMoveClassGuidanceAuthorityCucumberTest`.
|Explicit scenario-level `@implemented-and-validated`; four bounded defects inside one production scenario. Independent final 2026-07-30 OpenJDK 21 `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 13s, 956 discovered, 773 executed, 183 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; reviewed log SHA-256 `8d9b19576a27deaae2c12c767f929664c228db85d1dcc8644de3e41ecbe6ca9d`. This is dated mutable working-tree evidence; SpotBugs retained the configured non-blocking baseline findings. No force/recipe/daemon/LSP/MCP, structural-loss/post-preview-drift, native-matrix, or general result-algebra claim is inferred.

|REQ-JAVA-MAVEN-MOVE-AUTH-004
|No force flag, recipe, or integration surface promotes lexical fallback. The direct Java move-class outcome dispatcher, source-built CLI, recipe engine, daemon session, managed LSP session, and MCP session project the same immutable schema-v1 `LEXICAL_FALLBACK_REVIEW`. It reports `NOT_SEMANTICALLY_PROVEN`/`REVIEW_ONLY`/`LEXICAL_FALLBACK`/`INELIGIBLE`/`evidence.insufficient`; deterministic request/exact-snapshot/hidden-review-artifact/evidence SHA-256 and non-capability `operationId`; bounded exposed path/hash/risk records with explicit record completeness/category counts/fixed actions; and no protocol plan/edit/range/replacement/apply token. Only the hidden artifact hash commits to internal compatibility edit ranges and replacement/create bytes. Recipe stops without an aggregate. Envelope-only LRU correlation is bounded/lifecycle-cleared. Shared exactly-one identity parsing returns known-operation structured refusal and unknown/evicted operation or unknown-plan `INVALID_PARAMS`; daemon/LSP preserve JSON-RPC error `data`, MCP returns structured tool error content, and LSP emits no edit. REQ-003 guidance remains distinct; a fresh clean JDT scan retains existing eligible plan behavior.
|Non-functional. The source tag correctly classifies safety, cross-surface consistency, evidence identity, protocol capability separation, edit-free LSP behavior, bounded resource/lifecycle behavior, exact identity parsing, and pre-WAL non-mutation.
|`RPK-JAVA-MOVE-004`, `005`, and `007`; ADRs 0002, 0004, 0008, and 0012; `JavaMoveClassLexicalFallbackReview*`, `JavaMoveClassOperationDispatcher`, `RefactoringApplyIdentity`, and `JavaMavenMoveClassLexicalFallbackReviewCucumberTest`.
|Explicit scenario-level `@implemented-and-validated`. The focused OpenJDK-21 report discovered 22 cases, executed/passed this one 38-step case, skipped 21 by tag filter, and recorded zero failures/errors; reviewed log SHA-256 `5fb89c290bd098f249c2ec734c8c7bbffae162e5357ab6b1683042fc68e698ce`. Subsequent independent full working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 3s; 978 discovered, 774 executed, 204 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; reviewed log SHA-256 `890ec9e263d98c3f1ad3dcf4bf0f1f75f6d2e31fe6c2b658b0a1b7dcaf9b293a`. This is dated mutable working-tree evidence; the SpotBugs baseline remained non-blocking. Evidence is bounded to source-built classes and in-process session dispatch; no packaged/runtime/native/general qualification is inferred.

|REQ-JAVA-MAVEN-MOVE-AUTH-007
|Bounded candidate-total offline-missing apply: `reactorStructureStatus=COMPLETE`, `externalClasspathStatus=OFFLINE_MISSING`, exactly one synthetic enumerated leaf artifact with hash-bound expected identity/provided types and negative-presence evidence, one exact non-recovered declaration, 13 Java candidate records classified exactly once as 12 `BOUND_TARGET`, one `BOUND_OTHER`, and zero `UNRESOLVED`, two exactly unchanged/non-concealing retained diagnostics, no lexical-selected edit, no new or changed error, one transaction, after attestation, deterministic repeat preview, and byte/path/source-inventory/snapshot-hash-exact rollback while global classpath incompleteness remains visible.
|Functional and non-functional
|`RPK-JAVA-MOVE-002`, `003`, `004`, `006`, and `007`; accepted ADR 0012 target-scoped Maven `moveClass` authority
|Explicit scenario-level `@implemented-and-validated`; bounded synthetic fixture evidence only, independent of complete-`AVAILABLE` `001`, ordinary-artifact, and Magrathea qualification

|REQ-JAVA-MAVEN-MOVE-AUTH-008
|Target-relevant uncertainty and drift fail closed through six phase-partitioned cases. Four complete preview variants return immutable schema-v1 `REVIEW_ONLY_GUIDANCE` for inside-closure ambiguity, unresolved static wildcard lookup, outside-closure explicit old FQN with closure precedence, and same-`S0` before/staged diagnostic identity drift collected in dispatcher stage two; all four have typed layer data, no plan/edit/LexicalReview, and source-built CLI `guidance.nonManaged` refusal pre-`PatchEngine`/lock/WAL/residue. Missing root-declared active `catalog-shipping/pom.xml` returns result-owned typed `JavaMoveClassStructuralRefusal` with deterministic component-wise workspace-relative `NOFOLLOW` identity; missing-directory and missing-final-POM states are distinct, symlink/non-regular states are not `MISSING`, and CLI apply refuses pre-lock/no-residue. The final eligible REQ-007 preview binds a non-managed `BOUND_OTHER` source through immutable `OperationAuthorityFileEvidence`; one under-lock engine observation of external `S1` returns primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` in immutable `DiagnosticDetails` before WAL, preserves `S1`, creates no transaction/managed target edit/destination, and may retain the lock. It reports expected semantic candidate-inventory hash and expected/observed required-file-evidence hashes only.
|Non-functional. Safety, integrity, evidence freshness, honest identity vocabulary, phase-correct refusal, and exact non-overlap are quality constraints; no functional tag is declared.
|`RPK-JAVA-MOVE-002`, `003`, `004`, `006`, and `007`; accepted ADR 0012; `JavaMavenMoveClassAuthorityFailureCucumberTest`; exclusions REQ-003/004/010/011/012. Surface evidence is source-built library/CLI for five preview-time rows and library/`PatchEngine` only for the under-lock row; REQ-004 alone owns bounded recipe/daemon/LSP/MCP parity.
|Explicit outline-level `@implemented-and-validated`; six inherited cases. Focused OpenJDK-21 evidence discovered 22, selected/executed/passed six, skipped sixteen by tag filter, and had zero failures/errors; independent log SHA-256 `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`; `gherkin-writer` log SHA-256 `6f7e2d8a9c767f265a117853267a37ab31ec31d0878637870b37202ec8ba2c71`. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. The full-run accounting does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking.

|REQ-JAVA-MAVEN-MOVE-AUTH-009
|Explicit transitive test scope preserves target-closure move authority: every POM/parent/BOM, mediation input, and transitive edge in the fixture's full effective reactor graph is complete, current, and hash-bound. A compile-visible fixture dependency explicitly declares six fixture-owned transitive children with `test` scope while matching dependency-management entries repeat the same `groupId`, `artifactId`, type, and classifier with omitted/default-`compile` or explicit `compile` scope. Effective traversal retains the declaration's `test` scope, so none of those children enters the downstream consumer's compile or test analysis classpath or missing-binary report. The before/staged target closure remains `AVAILABLE`; global `OFFLINE_MISSING` remains visible solely for structurally excluded outside-closure modules and creates no outside-closure per-binary, provided-type, or retained-diagnostic inventory obligation after structural and candidate exclusion is proved. One exact target declaration, candidate-total `BOUND_TARGET`/`BOUND_OTHER` classification with zero `UNRESOLVED`, exact-import-or-FQN lookup protection, no relevant wildcard/static ambiguity, `JDT_BINDING` eligibility, no new or changed staged error, one managed transaction, after attestation, and exact rollback are all required; `LEXICAL_FALLBACK` remains review-only.
|Functional and non-functional. Deriving the correct consumer classpaths and producing eligible preview/apply/rollback are functional outcomes; Maven scope correctness, complete target-closure evidence, lookup protection, non-promotion of outside-closure absence, staged diagnostic integrity, and exact rollback are quality and safety constraints.
|`RPK-JAVA-MOVE-002`, `003`, `004`, `006`, and `007`; accepted ADR 0012 declaration-provenance Maven scope semantics and target-scoped closure authority
|Explicit scenario-level `@implemented-and-validated`; one bounded executable fixture case. Tagged evidence preserves all six explicit transitive `test` scopes, keeps target-closure environments `AVAILABLE` while global `OFFLINE_MISSING` is exclusively outside the closure, produces eligible lookup-protected previews, commits one transaction, attests the post-image, and restores bytes, paths, source inventory, and snapshot hash exactly. It does not by itself qualify independently validated `010`/`011`/`012`; the exact Magrathea field result is separately qualified after `010`.

|REQ-JAVA-MAVEN-MOVE-AUTH-010
|Descriptor-pruned children need no artifacts until another path selects them: an inactive fixture profile preserves the root aggregator and 20 active non-aggregator modules while a compile-visible selected external parent has complete, current, hash-bound descriptor and management inputs. Six fixed-release child declarations are deterministically pruned by transitive `test`/`provided` scope, transitive optionality, or exact inherited path exclusion before child version/range, repository/path, POM/effective-model, JAR, or network lookup; their deliberately absent POMs/JARs therefore create no selected node, subtree, missing input, or graph failure on those paths. Type/classifier remain identity rather than prune reasons. The scenario also requires deterministic candidate-total `JDT_BINDING` preview, managed apply, attestation, and exact rollback. If any child is also selected through an authority-relevant effective `compile` or `runtime` path without a prune, complete effective-POM/parent/BOM/management/mediation/relocation closure becomes mandatory; the absent selected child POM yields stable typed `REFUSED`/`INELIGIBLE` before lock and WAL with no plan, edit, residue, transaction, or mutation.
|Functional and non-functional. Correct classpath derivation and the positive preview/apply/rollback result are functional; descriptor-closed pruning order, hash-bound path/projection/reason evidence, alternate-path restoration, stable pre-mutation refusal, reproducibility, and exact rollback are correctness and safety constraints.
|`RPK-JAVA-MOVE-002`, `004`, `006`, and `007`; accepted ADR 0012 descriptor-closed enumeration
|Explicit scenario-level `@implemented-and-validated`; one bounded executable fixture case. Tagged evidence emits 18 deterministic `PRUNED` records for six children across `COMPILE`, `RUNTIME`, and `TEST`, binds each path/projection/reason and pre-lookup boundary, performs no child POM/JAR/network lookup on those paths, produces repeat eligible candidate-total `JDT_BINDING` previews, commits exactly one transaction, attests the post-image, and restores bytes, paths, source inventory, and snapshot hash exactly. The alternate selected path yields stable structural `REFUSED`/`INELIGIBLE` for the absent child POM before lock/WAL with no plan, edit, residue, transaction, or mutation. It does not itself implement independently validated ordinary row `011` or six-case selected-descriptor outline `012`. The exact Magrathea CLI field result was separately qualified after this `010` traversal behavior and did not require `011` or `012`.

|REQ-JAVA-MAVEN-MOVE-AUTH-011
|RefactorKit-owned ordinary JAR absence preserves target-scoped move authority: one fixed non-snapshot `com.acme.fixture.missing:ordinary-missing-leaf:1.0.0` selected graph leaf uses an unclassified `jar` variant, has no relocation, and comes from one explicit local-only Maven-2-layout repository. Its POM/effective parent/BOM/management/mediation closure is present, complete, current, and hash-bound; outgoing-edge records prove it is a selected leaf. Its deterministic JAR path is absent before and staged under no-follow checks, with no evidence sidecar, expected JAR hash, or provided-type inventory. One RefactorKit-enumerated selected-missing record binds repository identity, POM/JAR paths, selecting paths/projections, affected operation-closure source sets, descriptor hashes, leaf proof, and negative-presence fact hashes. Candidate-total exact JDT classifications, exact target-name lookup protection, no relevant overlap, and zero unresolved diagnostics attributable to unknown provided types gate repeat eligible `JDT_BINDING` previews while global status stays `OFFLINE_MISSING`. Apply revalidates evidence under lock before WAL, commits exactly one transaction, attests after diagnostics, and rolls back bytes, paths, source inventory, and snapshot hash exactly. Independent JAR/path-component appearance or replacement, selected-POM/effective-input replacement, or repository/path identity drift refuses under lock before WAL without mutation.
|Functional and non-functional. Eligible preview/apply/rollback for the bounded ordinary absence is functional; repository and descriptor provenance, sidecar-free non-following negative presence, no invented binary facts, graph/candidate/lookup/diagnostic totality, deterministic evidence, one transaction, exact rollback, and fail-closed drift handling are correctness and safety constraints.
|`RPK-JAVA-MOVE-002`, `003`, `004`, `006`, and `007`; accepted ADR 0012 ordinary missing-binary path
|Explicit scenario-level `@implemented-and-validated`; one bounded executable fixture case. Tagged evidence fixes the ordinary local-repository selected `jar` leaf, verifies complete present descriptor/leaf proof and sidecar-free absent-binary evidence without an invented hash/provided types, classifies 13 candidates with target-name lookup protection and zero unresolved/unattributed diagnostics, produces repeat eligible `JDT_BINDING` previews, commits one transaction, attests apply, restores bytes/paths/inventory/snapshot hash exactly, and refuses three under-lock evidence drifts before WAL without RefactorKit mutation. It is independent of the earlier Magrathea field qualification.

|REQ-JAVA-MAVEN-MOVE-AUTH-012
|Missing or drifted selected descriptors refuse ordinary missing-leaf authority: starting independently from every eligible `011` precondition, six fixture variants remove the selected leaf POM, make its XML malformed, remove its required parent, drift an imported BOM, drift the dependency-management/mediation declaration that supplies the selected version, or remove parsed-model proof that the leaf has no relocation. Every other selected descriptor input remains present, well-formed, current, and hash-bound. Descriptor-closed traversal must return structural `REFUSED` guidance with `INELIGIBLE` managed-write status and a stable blocker naming the selected coordinate, direct dependency path, affected source-set projections, descriptor layer, and condition. No selected descriptor failure may be reclassified as an admissible missing binary or proven graph leaf. Refusal precedes semantic edit selection, applyable plan ID, workspace lock, and WAL and leaves no `.refactorkit` residue or mutation.
|Functional and non-functional. Typed structural refusal and layer-specific blocker guidance are functional outcomes; complete selected-descriptor evidence, hash freshness, no binary-absence downgrade, pre-lock/pre-WAL non-mutation, reproducibility, and offline/plugin-free behavior are correctness and safety constraints.
|`RPK-JAVA-MOVE-002`, `004`, `006`, and `007`; accepted ADR 0012 selected-descriptor closure and fail-closed blocker.
|Explicit outline-level `@implemented-and-validated`; six inherited example cases. Tagged evidence proves stable `MISSING`/`MALFORMED`/`DRIFTED` structural refusal, parsed relocation/model identity requirements, context/hash preservation, and no semantic selection, binary-absence downgrade, lock/WAL, residue, or mutation.
|===

=== Implemented Local Packaged-process Validation of Existing REQ-001

Status: implementation-informed for one official final OpenJDK 21.0.11 local execution on Linux `amd64` against the mutable, uncommitted tree, with independent review receipts retained separately. Native CI wiring and `if: always()` report uploads are implemented but unexecuted. Linux x86-64 CI, Windows x86-64, macOS x86-64, and macOS arm64 remain `CONFIGURED_UNOBSERVED` until exact post-commit CI receipts are reviewed; Windows/macOS behavior is configured and statically reviewed only.

This is not a thirteenth Maven requirement or a twenty-third case. The dedicated `packagedMavenMoveClassAuthorityTest` source set/task reuses `features/java-maven-move-class-apply-authority.feature`, selects the repository-unique `@REQ-JAVA-MAVEN-MOVE-AUTH-001` pickle, uses packaged-only glue, and emits dedicated Cucumber JSON and JUnit XML. It depends on `refactorkitRuntimeDist`, uses one fork, and is explicitly invoked after packaging. Normal `test` and `check` remain packaging-independent. No external protocol or product semantic changed.

The bounded evidence scope is one no-follow byte copy of the permanent 20-module fixture and the real self-contained unpacked CLI. Subject processes use isolated home/temp directories and a scrubbed allowlisted environment; `JAVA_HOME`, `JDK_JAVA_OPTIONS`, `_JAVA_OPTIONS`, `CLASSPATH`, Maven/Gradle options, and proxy variables are absent, while one controlled `JAVA_TOOL_OPTIONS` supplies isolated home/temp properties. A poison `java` shim is first on minimal `PATH` and remains uninvoked. Launcher bytes and canonical child `java.home` attest the embedded runtime. Observed SHA-256 identities are fixture/copy manifest `905b354c151e8219d6f755335ecac381a0dab83df20de55d22f8a15a8695829e`, unchanged package manifest `681822f781379f5ddbe67389d1e33606f8931ca6fc4a75a3b3fc09f657b88430`, launcher `fa9cd72b54f269d76053da247665cb931ff39631d1ac38c7b55aaa0c9dcadef3`, and embedded Java `bd12685bb6403175bb8803ef143738bf74d06565223be944de1dad20ef5e5625`.

The 16-command sequence observes clean 20-module/seven-source baseline scan/diagnostics, references, definition, repeat scan, two equal `SEMANTIC_PREVIEW` / `JDT_BINDING` / `ELIGIBLE` previews, a contended-lock apply probe, one successful apply, post-apply scan/diagnostics, normal rollback, and restored scan/diagnostics. The exact observer source sets and paths are:

* `catalog-pricing:main` — `catalog-pricing/src/main/java/com/acme/catalog/pricing/CatalogPrice.java`;
* `catalog-storefront:main` — `catalog-storefront/src/main/java/com/acme/catalog/storefront/ProductTile.java`;
* `catalog-acceptance:test` — `catalog-acceptance/src/test/java/com/acme/catalog/acceptance/ProductLifecycleSteps.java`.

The exact five affected paths are those three plus `catalog-model/src/main/java/com/acme/catalog/legacy/Product.java` and `catalog-model/src/main/java/com/acme/catalog/api/Product.java`. The sole schema-v8 transaction contains five top-level `FileEdit` entries: four `Modify` plus one `Rename`. The modifies are the old target path, acceptance steps, pricing source, and storefront source; their nested `TextEdit` counts are exactly `1/2/1/1`, five total. There is no one-edit-per-modify invariant.

The lock probe observes only exact CLI human text `Apply refused:` followed by `ERROR: Workspace is locked by another RefactorKit writer` and refuses before WAL. `workspace.locked` is not projected, and this runner makes no core-code correlation claim. The successful CLI apply replans inside its own child process, compares the rendered preview portion, and makes no cross-process plan-ID claim. The sole transaction reaches `APPLIED`, fresh post-apply diagnostics are clean, normal rollback advances that same record to `ROLLED_BACK`, and exact baseline bytes, path kinds, seven-source inventory, and snapshot identity are restored. The permanent fixture and unpacked package remain unchanged. All 16 commands complete with zero timeouts, truncations, or process leaks.

Official final local packaged evidence used `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks`: `BUILD SUCCESSFUL` in 22s with 35 tasks. JUnit discovered 22 cases, executed one, tag-filtered 21, and reported zero failures/errors; the selected Cucumber scenario passed five background plus fourteen scenario steps (19/19) and 2/2 hooks. Packaged log SHA-256 is `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; Cucumber JSON SHA-256 is `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. An earlier independent read-only review remains separate corroborating evidence: packaged log SHA-256 `7a71b69b745e10e9339d8cdd911c346e422328d58163db52eb92a536e8735d9e`, JSON SHA-256 `f3ecf22fa195dbbd4e88e5a68937f7f55dec4ad4a6d90504cdc34588444df340`; its separate source-built REQ-001 rerun passed 22 discovered/one executed/21 tag-filtered/zero failures or errors, log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`.

On the same JDK, official final whole-tree source-built execution of `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 6m43s. Standard test tasks discovered 1045, executed 817, skipped/tag-filtered 228, and had zero failures/errors; 140 XML files and 82 tasks executed; log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`. The dedicated packaged task is separately reported and intentionally not included in those standard-test totals. Targeted custom-source-set PMD/SpotBugs tasks passed, log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`. Static classification is intentionally non-zero: the custom packaged glue/harness has exactly 99 reviewed/non-blocking SpotBugs findings—57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, and 1 UrF. They are test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated representation and conservative single-thread harness concurrency warnings. No production source file was added or modified by this slice. The wider static baseline remains non-blocking; aggregate static cleanliness and numerical or child-JVM coverage are not claimed.

[cols="2,2",options="header"]
|===
|Post-commit native CI row |State

|Linux x86-64
|`CONFIGURED_UNOBSERVED`

|Windows x86-64
|`CONFIGURED_UNOBSERVED`

|macOS x86-64
|`CONFIGURED_UNOBSERVED`

|macOS arm64
|`CONFIGURED_UNOBSERVED`
|===

No row may be promoted before native CI executes the exact committed tree and publishes a reviewed receipt. CI wiring and always-run report uploads being implemented does not constitute execution. The local observation is bounded only to the exact unpacked package/host/architecture/fixture/command sequence. It excludes REQ-JAVA-MAVEN-MOVE-AUTH-002 through `012`; daemon, LSP, MCP, recipe, and library surfaces; Magrathea and general Maven populations; crash/restart/concurrency; archive trust/checksum/SBOM/attestation/signing; coverage of the child JVM; and every unexecuted host.

The reviewed LLM Wiki research remains recommendation history. Earlier delegated ADR/C4 reviews found existing decisions and topology sufficient; implementation added evidence plumbing only, so no ADR or Structurizr artifact changed.

=== LATE REQ-004 Implementation Evidence

Status: implementation-informed and reconciled with scenario-level `@implemented-and-validated`.

The corrected feature now uses the returned non-capability `operationId` for known daemon/LSP/MCP refusal and keeps arbitrary `planId=plan-lexical` as unknown input. Current source implements exactly-one non-blank `operationId`/`planId` parsing in core. A cached operation returns typed `evidence.insufficient`; no/both/blank, unknown/evicted operation, and arbitrary plan identities return `INVALID_PARAMS`. Daemon and LSP preserve blocker/envelope in JSON-RPC error `data`; MCP returns equivalent `isError=true` structured content.

The schema-v1 envelope is immutable and reports `NOT_SEMANTICALLY_PROVEN`, `REVIEW_ONLY`, `LEXICAL_FALLBACK`, `INELIGIBLE`, and `evidence.insufficient`. It binds normalized request, exact snapshot, hidden review artifact, and sorted evidence with four SHA-256 identities and deterministic `lexical-review-<sha256>` correlation. The exposed residual records are path/content-hash/risk identities with `managedEdit=false`, not source ranges or token hashes. Only `reviewArtifactSha256` commits to exact internal compatibility edit ranges and replacement/create bytes. Record bounds are 200 and 262,144 canonical UTF-8 bytes, with `COMPLETE or TRUNCATED`, total/returned counts, deterministic returned-risk-category counts, and four fixed next actions.

The direct API under test is `JavaMoveClassOperationDispatcher.preview` and its `JavaMoveClassOperationOutcome.LexicalReview`, not generic `LanguageAdapter.applyRefactoring`. CLI, recipe, daemon session, managed LSP session, and MCP session route Java `moveClass` through that boundary. No envelope exposes a plan/edit/range/replacement/apply token; no pending plan or LSP edit is created; recipe returns no aggregate; the bounded envelope-only LRU cache is lifecycle-cleared; compatibility-plan core apply refuses before lock/WAL; and a fresh clean JDT scan retains existing `SEMANTIC_PREVIEW`/`JDT_BINDING`/`ELIGIBLE` plan behavior.

The dedicated OpenJDK-21 Cucumber report discovered 22 cases, selected/executed/passed the single REQ-004 production case with 38 expanded steps, skipped 21 cases by tag filter, and recorded zero failures/errors. The reviewed focused log SHA-256 is `5fb89c290bd098f249c2ec734c8c7bbffae162e5357ab6b1683042fc68e698ce`; the final dry-run also discovered 22, selected one, skipped 21, and had zero failures/errors, with reviewed log SHA-256 `40814182128d54c599522e84887c2fd8b51c422988daade149dd2b49b83a0a39`. This proves the source-built/in-process scenario only and does not qualify packaged executables, stdio/editor deployment, runtime distributions, native platforms, or general move authority.

REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` occur in the feature with repository-unique IDs. Ten ordinary scenarios and both six-row outlines (`008`, `012`) carry explicit `@implemented-and-validated`; their example rows inherit one ID, classification, and status. The feature reconciles to twelve definitions and 22 validated cases.

== Authoritative Snapshot Diagnostics Evaluation

Status: implementation-informed and `@implemented-and-validated`. `features/authoritative-diagnostics-evaluation.feature` contains two ordinary scenarios and two `Scenario Outline` declarations, no `Background`, four repository-unique functional requirement IDs, 16 expanded cases, 43 declared scenario steps, and 128 expanded case steps. Classification and status are inherited from feature-level `@functional-requirement @implemented-and-validated`. Independent OpenJDK 21.0.11 execution passed 16/16 scenarios and 128/128 steps (log/JSON SHA-256 `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`/`c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`).

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001
|An authoritative post-image is journaled and committed with one exact identity. Baseline evaluation is exact `S0`/`D0`; simulation yields tracked candidate `C1`; staged evaluation returns exact `S1`/`D1` while preserving the candidate root, source/auxiliary partition, tracked paths/language IDs/bytes, extension scope, and ignore policy. After provider completion, `PatchEngine` repeats live-workspace and operation-lease validation under the same lock, proves rendered-image equality, writes schema-v8 `PREPARED` with `postSnapshotHash=S1.hash`, and returns success only after committed evaluation reproduces `S1.hash` and the complete `D1` multiset.
|Functional only. The source declares functional behavior for the managed apply result and exact transaction identity; it declares no non-functional tag, even though safety and authority invariants are acceptance details.
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, and `UC-APPLY-PATCH`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012; Chapters 5, 6, 8, and 10-12.
|Feature-level inherited `@implemented-and-validated`; one ordinary 17-step case passed in the independent runner.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|Provider authority violation `"<case>"` is refused before WAL. Eight independent rows cover workspace root, source/auxiliary split, tracked path, language ID, tracked content, source extensions, ignored directories, and provider-induced live workspace mutation. Each violation is reported before a `PREPARED` record or managed edit; the read-only transaction listing stays empty, and provider output or diagnostics never authorize a write.
|Functional only. The outline specifies eight observable authority-violation refusal outcomes and no non-functional classification tag.
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, and `UC-APPLY-PATCH`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012; Chapters 5, 6, 8, and 10-12.
|Feature-level inherited `@implemented-and-validated`; twelve example-expanded cases from seven declared outline steps, totaling 84 expanded case steps, passed in the independent runner.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003
|Post-apply `"<mismatch>"` advances the same record through automatic rollback. Two independent rows cover semantic snapshot mismatch and diagnostic multiset mismatch after the sole record reached `APPLIED`; that schema-v8 record advances through `PREPARED, APPLYING, APPLIED, ROLLING_BACK, ROLLED_BACK`, exact baseline bytes/directories are restored, restored evaluation reproduces `S0.hash`/`D0`, and no second transaction is created.
|Functional only. The outline specifies the automatic-rollback result and retained transaction lifecycle; it declares no non-functional classification tag.
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012; Chapters 5, 6, 8, and 10-12.
|Feature-level inherited `@implemented-and-validated`; two example-expanded cases from eight declared outline steps, totaling 16 expanded case steps, passed in the independent runner.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-004
|Existing diagnostics-only gates retain additive compatibility. One case fixes enabled-provider invocation at baseline, staged, and committed inputs, disabled-provider invocation at zero, current simulator-derived journal hashes, lazy construction, diagnostic regression/unavailability/post-apply/rollback semantics, schema-v8 record meanings, and the absence of any schema bump, historical rewrite, new persistence, serialized evaluator state, scanner registry, language-specific core type, or provider write authority.
|Functional only. The scenario specifies observable compatibility behavior for existing enabled/disabled apply paths and declares no non-functional classification tag.
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-034`; accepted ADRs 0002 and 0012; Chapters 5, 6, 8, and 10-12.
|Feature-level inherited `@implemented-and-validated`; one ordinary 11-step compatibility case passed in the independent runner.
|===

The case accounting is exact: REQ-001 contributes one case, REQ-002 contributes twelve, REQ-003 contributes two, and REQ-004 contributes one, for 16 cases. Validated behavior includes deep-detached construction-time identity, core-owned pre-WAL semantic containment and no-follow external-fingerprint authenticity, exact S0/C1/S1 schema-v8 identity, same-record automatic rollback, and unchanged legacy diagnostics-only behavior. Static evidence is deliberately bounded and not an aggregate-cleanliness claim:
`StageFacts`, `GateExpectation`, `ReactorView`, `ModuleView`, and `SourceSetView`
have zero raw and zero real EI/EI2 findings; the one
`AuthoritativeDiagnosticsEvaluation` EI warning is non-real over a defensively
copied unmodifiable list and deep mutation probes passed. PMD was `NO-SOURCE`
for Kotlin, and the wider configured SpotBugs baseline remains non-blocking.
Broader operation/surface/support qualification is not inferred.

== Bounded Maven Module Rename

Status: implementation-informed and `@implemented-and-validated`. `features/java-maven-module-rename.feature` contains one ordinary functional scenario, no `Background`, no `Scenario Outline`, one repository-unique stable requirement ID, 50 declared scenario steps, and one 50-step executable case. Independent OpenJDK 21.0.11 execution passed 1/1 scenario and 50/50 steps (log/JSON SHA-256 `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479`/`d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`).

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-JAVA-MAVEN-MODULE-RENAME-001
|An explicit direct-child directory and artifact rename commits once and rolls back exactly. On no-follow disposable copies of `testdata/acceptance/java-maven-move-class-authority-20-modules`, a direct JVM-library caller must prove one sole aggregator, exact direct child `catalog-model`, absent destination `catalog-domain`, the child project's literal artifact origin, one root direct-module origin, and one effective-model-proven literal dependency origin. Caller-explicit `newArtifactId=catalog-domain` authorizes the artifact change; omission preserves `catalog-model` and authorizes no coordinate edit. The normalized edit is exactly three POM modifications followed by two file renames, stages one complete 20-child reactor with unchanged authoritative diagnostics, applies once through `PatchEngine` with authorization surface `direct-library`, journals one schema-v8 transaction, and restores the exact non-engine image through normal rollback. The scenario also requires bounded refusals for inferred coordinate intent and property-managed or ambiguous origins.
|Functional only. The source declares `@functional-requirement` and no `@non-functional-requirement`; the exact classification is preserved even though determinism, least authority, diagnostics, and rollback are acceptance details.
|`UC-JAVA-RENAME-MAVEN-MODULE`, `UC-PREVIEW-REFACTOR`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-033`; `C-BOUND-010`; `QS-MODULE-RENAME-001`; `R-035`; accepted ADRs 0002, 0010, and 0012.
|Explicit scenario-level `@implemented-and-validated`; no inherited status. The independent direct-library runner passed. Historical prototype defects remain negative evidence rather than a description of the promoted planner.
|===

The first row is bounded to direct library use, one exact root-aggregator direct child, the named permanent fixture shape, and independently explicit directory/artifact intent. It performs exactly three POM range edits (`catalog-model/pom.xml` `9:14-9:27`, root `pom.xml` `74:12-74:25`, and `catalog-pricing/pom.xml` `13:18-13:31`) plus two file renames. Woodstox 7.1.1 is scoped to `refactorkit-java` only; exact offsets and DTD/external-DTD/entity denial passed, and its reviewed JAR SHA-256 is `02b9d022e9d47704ff8a7a859a0dbfd3b2882a8311eb7ff1e180f760ccda2712`. The row excludes module creation; nested or non-direct modules; positive profile/property-managed/inherited/duplicate/ambiguous origin editing; other coordinate components; JPMS; CLI, daemon, LSP, MCP, and other protocol surfaces; recipes; packaged/native/cross-platform qualification; disposable real-project acceptance; general Maven support; native CI; release-wide support; and completion of the broad Java module rename/move roadmap item.

== Pending-plan Store Extraction

Status: implementation-informed and reconciled with feature-level `@implemented-and-validated`. The extraction is a supporting quality/safety contract for existing daemon/LSP/MCP use cases, not a new externally invocable use case.

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-PENDING-PLAN-STORE-001
|Exact opaque payloads are retained by reference in separate session-owned generic stores. The fixed capacity is 128; successful lookup refreshes access recency; inserting entry 129 after touching entry 1 evicts entry 2; missing or evicted IDs preserve each surface's existing not-found response and require a new preview. Eviction, remove, and clear affect in-memory references only and create no workspace edit, filesystem write, workspace lock, WAL record, managed transaction, or rollback evidence.
|Non-functional. The scenario constrains bounded resources, isolation, deterministic eviction, exact retention, safety, and absence of write side effects.
|`C-TECH-028`; `QS-PROTO-003`; UC-DAEMON, UC-LSP, and UC-MCP; ADRs 0002 and 0004.
|Feature-level inherited `@implemented-and-validated`; one focused source-built/in-process scenario with 13 steps passed on OpenJDK 21.0.11.

|REQ-PENDING-PLAN-STORE-002
|The shared store exposes only insert, successful access-order lookup, remove, and clear. `DaemonSession`, `LspSession`, and `McpSession` each own a separate instance while retaining their exact asymmetric preview/import admission, discard availability, selected-versus-all apply consumption, refusal consumption/retry, rollback, document/project reset, close, and success/error response policies at surface call sites. The extraction invents no uniform lifecycle, protocol response, approval, or authority rule.
|Non-functional. The scenario constrains compatibility, separation of concerns, protocol stability, and authority/lifecycle ownership.
|`C-TECH-028`; `QS-PROTO-003`; UC-DAEMON, UC-LSP, and UC-MCP; ADRs 0002 and 0004.
|Feature-level inherited `@implemented-and-validated`; one focused source-built/in-process scenario with 6 steps passed on OpenJDK 21.0.11.
|===

Production evidence is exact and bounded to `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`, `modules/refactorkit-daemon/src/main/kotlin/org/refactorkit/daemon/DaemonSession.kt`, `modules/refactorkit-lsp/src/main/kotlin/org/refactorkit/lsp/LspSession.kt`, and `modules/refactorkit-mcp/src/main/kotlin/org/refactorkit/mcp/McpSession.kt`. The CLI-hosted `PendingPlanStoreCucumberTest` and steps call the core primitive and real session classes in process. The OpenJDK 21.0.11 focused run passed two scenarios/19 steps, executed 29 Gradle tasks, and reported zero failures/errors; log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`. Subsequent independent whole-working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`. SpotBugs retained its configured non-blocking baseline and reported no `PendingPlanStore` findings. This is dated mutable source-built aggregate evidence, not packaged/concurrency/general-extraction or numerical coverage qualification.

This evidence does not qualify packaged executables/transports, concurrent access, long-running service races, or a general service-extraction pattern. It does not change protocol success/error shapes, `PlanId` authority, explicit-apply approval, `PatchEngine` gates, or the separate surface lifecycle matrix.

== Managed Rollback Executor Extraction

Status: implementation-informed. REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 inherit `@non-functional-requirement` and `@implemented-and-validated` from `features/managed-rollback-executor-extraction.feature`. One ordinary scenario and one seven-row outline produce two definitions, eight cases, 19 declared steps, and 67 expanded case steps. Production evidence is `ManagedRollbackExecutor.kt`; inspected adoption is in CLI/daemon/LSP/MCP; runner/glue evidence is `ManagedRollbackExecutorCucumberTest.kt` and `ManagedRollbackExecutorSteps.kt`. A focused RED proved mutable diagnostic aliasing by mutating the source list after `execute` (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`); `Refused` now copies diagnostics into a detached unmodifiable ordered snapshot. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical three-feature official Gherkin audit found 16 definitions, 32 cases, 587 expanded steps, and zero errors (log SHA-256 `f4f9fd559ef9220c145017a2723fc4b8e888424448a392a11d20c1dc33d379c0`). The preceding four-feature audit records the two managed-apply selector definitions and 11 cases as implemented-and-validated; the official post-promotion five-feature audit adds five implemented-and-validated Workspace Snapshot Composer definitions/eight cases and leaves this managed-rollback evidence boundary separate. The earlier four-feature absent-status audit remains historical pre-promotion evidence. The historical rollback-scoped whole-working-tree verification used the same OpenJDK and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). This is dated mutable source-built aggregate evidence. It does not qualify packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority.

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-MANAGED-ROLLBACK-EXECUTOR-001
|One visible applied record is durably rolled back through the real engine. The implemented executor parses the exact raw ID, performs one pre-lock full-record lookup, applies `APPLIED_ONLY`, invokes one allow guard before the lock, calls public `PatchEngine.rollback` exactly once with the resolved transaction and supplied `NORMAL` mode, and returns the exact applied transaction plus the same complete resolved pre-lock record. `PatchEngine` alone owns its under-lock reload and rollback rules; the executor owns no caller response, lifecycle, refresh, diagnostics, or mode policy.
|Non-functional. The scenario constrains ordering, responsibility boundaries, exact evidence retention, durability handoff, non-duplication, and behavior preservation rather than adding a new user-visible capability.
|`UC-ROLLBACK`; `C-TECH-029`; `C-BOUND-006`; `QS-SAFE-012`; `R-030`; ADRs 0002 and 0004; Chapters 4-6 and 8-12.
|Feature-level inherited `@implemented-and-validated`; the ordinary 11-step definition/case passed in the focused source-built/in-process run.

|REQ-MANAGED-ROLLBACK-EXECUTOR-002
|Short circuits and surface visibility retain distinct outcomes. Seven independent example rows preserve invalid raw input, visible absence, `APPLIED_ONLY` hiding of a non-`APPLIED` record, daemon `JOURNAL_RECORD` engine refusal, LSP dirty-document-first preflight rejection, outer lookup journal failure, and rollback-call journal failure. Each row fixes parse/lookup/guard/engine/lock counts and preserves the exact typed outcome and available failure/resolved-record evidence without moving response, session, refresh, or diagnostics policy into the executor. Exact journal-exception identity is retained; refusal diagnostics retain construction-time order and values in a detached unmodifiable snapshot rather than aliasing a mutable source list.
|Non-functional. The outline constrains cross-surface compatibility, fail-fast ordering, document safety, outcome discrimination, exception fidelity, evidence identity, and separation of concerns.
|`UC-ROLLBACK`; `C-TECH-029`; `C-BOUND-006`; `QS-SAFE-012`; `R-030`; ADRs 0002 and 0004; Chapters 4-6 and 8-12.
|Feature-level inherited `@implemented-and-validated`; the outline's seven cases/eight declared steps/56 expanded case steps passed in the focused source-built/in-process run.
|===

The feature, production executor, acceptance glue, final focused run, and historical rollback-scoped whole-working-tree source-built run provide bounded implementation evidence for `ManagedRollbackExecutor`, `RollbackLookupVisibility`, and the typed outcome algebra. The mutable-diagnostics RED and corrected acceptance additionally prove detached unmodifiable ordered snapshotting for `Refused`; exact `TransactionLogException` identity and the exact resolved record remain intentionally retained. The retained record is explicitly pre-lock lookup evidence rather than current or post-rollback state. CLI/daemon/LSP/MCP adoption is confirmed by source inspection only, not packaged or cross-platform execution. Existing rollback evidence and `PatchEngine` authority remain bounded and unchanged.

== Managed-apply Diagnostics-gate Selector

Status: implementation-informed within a bounded source-built/in-process selector and daemon/MCP regression boundary, with dated whole-working-tree aggregate evidence. `features/managed-apply-diagnostics-gate-selector.feature` contains one ten-row `Scenario Outline` and one ordinary `Scenario`: two repository-unique non-functional requirement definitions, 11 expanded cases, 20 declared scenario steps, and 92 expanded case steps. REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 and every expanded case inherit `@implemented-and-validated` from the feature. Production evidence is `ManagedApplyDiagnosticsGateSelector.kt`; source inspection confirms adoption only at `DaemonSession.refactorApply` and `McpSession.toolApplyRefactoring`; runner/glue evidence is `ManagedApplyDiagnosticsGateSelectorCucumberTest.kt` and `ManagedApplyDiagnosticsGateSelectorSteps.kt`, supported by the JVM module build scaffold. Final independent OpenJDK 21.0.11 acceptance passed 11/11 scenarios and 92/92 steps with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`). Daemon regression passed 58/58 and MCP regression passed 26/26. Final whole-working-tree source-built verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. The standalone `:modules:refactorkit-jvm:compileTestKotlin --rerun-tasks` command initially failed because `testRuntimeClasspath` was resolved eagerly during configuration (RED log SHA-256 `798419c8a27a9a0c9ca74dd3d41ab2851acfb84d3d4f492002aaed71bfafa98a`); it passed after the compiler-classpath system property was deferred to test execution (GREEN log SHA-256 `c0ee8f6dfc932015dbb8b2067a3f3637c2208b6888a72bc672369bbb3d5df7e7`). This RED/GREEN pair is build-scaffold correctness evidence only, not product or package qualification. The focused selector runner does not invoke `PatchEngine`.

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001
|Selection case `"<case>"` follows the first matching ordered route. Ten examples specify the exact seven-row precedence, original case-sensitive final-segment predicate, exact/case-sensitive language IDs, Maven-before-Java and mixed-Kotlin operation/evidence precedence, stable built-in gate IDs, unchanged external gate identity, and prohibition of metadata normalization or inference. The final production form is `Objects.requireNonNull(it.fileName).toString().endsWith(".java")`; source inspection confirms that making non-nullness explicit preserves the earlier root-path `NullPointerException`.
|Non-functional. The outline constrains deterministic compatibility, routing correctness, exact identity, and preservation of an existing safety boundary rather than adding a user-visible capability.
|`UC-APPLY-PATCH`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-030`; `C-BOUND-007`; `QS-DIAG-007`; `R-031`; ADRs 0002 and 0004; Chapters 4-6 and 8-12.
|Feature-level inherited `@implemented-and-validated`; all ten cases/80 expanded steps passed in the final focused source-built/in-process run.

|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002
|Selection remains lazy, per-call, and bounded to the duplicate daemon and MCP decision. The scenario specifies current-adapter arguments, zero built-in provider work during selection, exactly one synchronous external lookup only for external IDs, unchanged gate/exception identity, provider deferral, unchanged `PatchEngine` authority, and exclusion of CLI/LSP/recipes/testkit/direct library, lifecycle, rendering, diagnostics-query/post-success work, WAL, rollback, and recovery.
|Non-functional. The scenario constrains laziness, authority, failure identity, state isolation, ordering, responsibility boundaries, and cross-surface compatibility.
|`UC-APPLY-PATCH`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-030`; `C-BOUND-007`; `QS-DIAG-008`; `R-031`; ADRs 0002 and 0004; Chapters 4-6 and 8-12.
|Feature-level inherited `@implemented-and-validated`; its one case/12 expanded steps passed in the final focused source-built/in-process run.
|===

The feature, production selector, build/test scaffold, acceptance glue, final focused run, source-inspected daemon/MCP adoption, 58/58 and 26/26 regressions, and final whole-working-tree source-built run provide bounded implementation evidence. Explicit non-null final-segment handling eliminated the initial selector production NP finding while preserving root-path and `.java`/`.JAVA` behavior. The final full log contains no `ManagedApplyDiagnosticsGateSelector.kt` or provider-function production finding; 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), and the broader configured SpotBugs baseline remains non-blocking. The historical commit-`642c187` duplicate branch and pre-promotion audit SHA-256 `57a48fbdf31f91e2ab0d54443849225ba51a4349cc8be84236f62210adc90c14` remain planning/status history. CLI, managed LSP, recipes, testkit/direct library, daemon post-success diagnostics, lifecycle, rendering, WAL, rollback, and recovery remain excluded. `PatchEngine` authority is unchanged. No packaged, native, cross-platform, concurrent, apply-authority, or numerical-coverage claim is made.

== Workspace Snapshot Composer Extraction

Status: implementation-informed within the exact source-built/in-process boundary plus dated mutable whole-working-tree source-built aggregate evidence. `features/workspace-snapshot-composer-extraction.feature` has no `Background`; four ordinary scenarios and one four-row `Scenario Outline` produce five repository-unique non-functional requirement definitions, eight expanded cases, 41 declared scenario steps, and 65 expanded case steps. REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` inherit effective `@implemented-and-validated` status from the feature. Production evidence is `WorkspaceSnapshotComposer.kt`; focused runner/glue evidence is `WorkspaceSnapshotComposerCucumberTest.kt` plus `WorkspaceSnapshotComposerSteps.kt`. The meaningful compile RED failed on the missing composer (log SHA-256 `d2876ad413703a3895999a9377f8b7c6a713578eb8156b652bb079c70f1a6d95`). Final independent OpenJDK 21.0.11 focused acceptance passed 8/8 scenarios and 65/65 steps with zero failures, errors, or skips (log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422`; Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`). Daemon/MCP adoption is source-inspected only and their regressions passed 58/58 and 26/26 (combined log SHA-256 `9acaed65925c596ef0c161d363c1d8383acd216747e7bc4c93d50ee19c92c9ae`). Final 2026-08-01 official OpenJDK 21.0.11 whole-tree `check goldenTest` verification passed in 7m 7s with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). It has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption. Static findings remain non-zero and non-blocking: 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` (6 NP, 3 RCN, 12 BC), plus the broader configured non-blocking baseline. CLI/LSP, lifecycle/index/`PatchEngine`/protocol authority, and packaged/native/cross-platform/concurrent/crash-restart/numerical-coverage/general-orchestration qualification remain excluded.

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001
|An empty source contribution preserves the exact authoritative base. Two calls prove that the TypeScript evidence port is never invoked; without a final port the same base instance/hash, file order, extensions, and ignored directories return unchanged; with a current final port that port receives the same base instance and its exact output is returned unchanged.
|Non-functional. Exact identity, compatibility, no-op behavior, and absence of unnecessary collaborator work are correctness and determinism constraints.
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `QS-SNAPSHOT-001`; `R-032`; Chapters 5, 6, 8, 10, and 12.
|Feature-level inherited `@implemented-and-validated`; the production composer passed this one seven-step case in the final focused source-built/in-process run.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-002
|A non-empty contribution overlays only `files`, `sourceExtensions`, and `ignoredDirectories`. Normalized-path collision keeps the later source, surviving values are ordered by `path.toString()`, extension/ignore sets are exact unions, all other authoritative base fields remain exact, and repeat composition from identical evidence has equal canonical content/hash without clock, UUID, or composer identity.
|Non-functional. Bounded authority, collision correctness, deterministic ordering, immutability, and reproducibility are quality constraints.
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-001`; `R-032`; Chapters 5, 6, 8, 10, and 12.
|Feature-level inherited `@implemented-and-validated`; the production composer passed this one ten-step case in the final focused source-built/in-process run.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-003
|Every non-empty call receives its own primary scanner, source-inventory scanner, conditional TypeScript-configured attacher, and optional current Kotlin-configured final attacher. The exact call trace is primary, source, conditional, then optional final; every attacher sees the exact prior output once; no collaborator is retained across calls; provider replacement or typed unavailability passes through without reinterpretation.
|Non-functional. Ordering, per-call state isolation, exact identity, compatibility, and typed evidence fidelity are quality constraints.
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-001`/`002`; `R-032`; Chapters 5, 6, 8, 10, and 12.
|Feature-level inherited `@implemented-and-validated`; the production composer passed this one eight-step case in the final focused source-built/in-process run.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-004
|A sentinel failure from the authoritative scanner, secondary source scanner, conditional attacher, or optional final attacher escapes as the same object with type/code/message/cause unchanged. No later stage runs, and no workspace/caller/process/lock/`.refactorkit` state or write/build/process/lifecycle/lock/WAL/transaction/PatchEngine/recovery/rollback side effect changes.
|Non-functional. Fail-fast ordering, failure identity, least privilege, non-mutation, and authority separation are safety and compatibility constraints.
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-002`; `R-032`; Chapters 5, 6, 8, 10, and 12.
|Feature-level inherited `@implemented-and-validated`; the production composer passed all four outline cases/32 expanded steps in the final focused source-built/in-process run.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-005
|Only daemon and MCP adopt the stateless core composer. Daemon invokes it at project open, saved-workspace refresh, apply baseline, apply post-image, and rollback refresh; MCP invokes it at project scan, apply baseline, apply post-image, and rollback refresh. Exact extension mapping stays in surface/adapter wiring. Refresh/saved state, workspace index, pending/authority state, semantic lifecycle, `PatchEngine` authority, and protocol policy remain surface-owned; CLI/LSP, concrete adapter imports in core, a generalized registry/new module, stored state, index, protocol, persistence, and mutation authority remain excluded.
|Non-functional. Separation of concerns, compatibility, dependency direction, least authority, and bounded adoption are architecture quality constraints.
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-002`; `R-032`; ADRs 0004/0009/0010 decision fit; Chapters 2, 4-10, and 12.
|Feature-level inherited `@implemented-and-validated`; this one eight-step case passed through source inspection of the exact core/daemon/MCP wiring and exclusions, supported by daemon/MCP module regressions. It is not packaged surface execution evidence.
|===

REQ-WORKSPACE-SNAPSHOT-COMPOSER-005 preserves these exact surface-owned exclusions:

[cols="2,5",options="header"]
|===
|Policy boundary |Surface-owned decisions

|Refresh and saved state
|Invocation timing, stored root and snapshot, watcher dirty or overflow state, refresh count, and response selection.

|Workspace index
|Open, reconcile, contribution, generation, and symbol discovery.

|Pending and authority state
|Pending plans, lexical-review audit, admission, removal, clearing, and semantic leases.

|Semantic lifecycle
|Toolchain or adapter discovery, start, stop, restart, reset, close, and post-apply scan-before-cleanup ordering.

|`PatchEngine` authority
|Preview routing, diagnostics gates, authorization, lock, WAL, apply, validation, recovery, and automatic or explicit rollback.

|Protocol policy
|Capabilities, JSON or text rendering, error mapping, changed-file projection, and success or refusal responses.
|===

CLI and LSP retain their existing scan and lifecycle behavior. Core imports no concrete Java, Tree-sitter, TypeScript, Kotlin, daemon, or MCP type and gains no generalized scanner registry or new orchestration module. The composer returns only an immutable snapshot and owns no stored state, index, protocol, persistence, or mutation authority.

The composer production class and focused acceptance directly validate REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `004` and the source-inspected ownership assertions of `005`. Independent daemon/MCP regressions validate compatibility at the two adopting modules without turning source inspection into packaged execution evidence. The final dated whole-working-tree source-built aggregate passed with no production finding attributable to the composer or daemon/MCP adoption. Its static result is not zero: 21 slice-local glue-only Kotlin/generated findings remain reviewed/non-blocking and are not production defects. LATE ADR reassessment found no new decision or body update; LATE C4 reassessment validated `docs/c4/workspace.dsl` unchanged because the implementation remains internal C3-scale behavior inside the existing Core Patch and Model Engine and existing in-process relationships.

== Java Refactoring Preview Command Boundary

Status: implementation-informed and reconciled with feature-level `@implemented-and-validated`. `features/java-refactoring-preview-command-boundary.feature` has no `Background`; one five-row `Scenario Outline` and three ordinary scenarios produce four repository-unique non-functional definitions, eight cases, 46 declared scenario steps, and 94 expanded steps. Production evidence is `JavaRefactoringPreviewCommand.kt`: a sealed hierarchy containing only `RenameClass`, `RenameMember`, `MoveSourceRoot`, single-file `OrganizeImports`, and `SafeDelete`; a fieldless `JavaRefactoringPreviewDispatcher`; an internal per-call `JavaRefactoringPreviewPlannerInvoker` seam; and `ExistingJavaRefactoringPreviewPlannerInvoker`, which maps to the unchanged default planners. Five generic `JavaLanguageAdapter` decoders converge on the dispatcher. Daemon and MCP construct only these five commands after their existing parsing/required-field checks; their refused-plan, pending-plan, and rendering policies remain outside.

The pre-implementation baseline passed Java 245/245, daemon 58/58, and MCP 26/26 on OpenJDK 21.0.11 (log SHA-256 `46a52ccf5fb116f5f52c7b116947ade4ff8faceea0d2fa999c330cc5cc4f1560`). The absent API produced a meaningful compile RED (SHA-256 `3221bc7bc6aaef1a21117032ce6f22ea7c28ea8efb05a2797e2fec1beb3b5af8`). The first independent review correctly blocked promotion because REQ-003 was source-only. The correction reused the same feature through split runners: the Java-module runner executes REQ-001/002/004 with sentinel plus real default planners; the CLI-module runner executes REQ-003 through public `DaemonSession`/`McpSession` dispatch, covering all five operations plus missing, unknown, and refused paths and no preview writes/residue. Final focused evidence passed seven Java scenarios/85 steps plus one real-surface scenario/9 steps—four definitions, eight cases, 94/94 steps—with zero failures/errors (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`; JSON SHA-256 `2059c28d57fde8f72b2e3f0de9fe6d95062c8de6cad40aeaeee3e4175a378ef6` and `999ee1a13d8577113113236e7b2241a5fac881e97327705eb1e4d1858ed83baa`). Independent regressions reported Java 253 discovered/252 executed/one tag-filtered, daemon 58/58, and MCP 26/26 with zero failures/errors (log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`).

[cols="2,3,2,2,2",options="header"]
|===
|Requirement ID |Title / acceptance boundary |Classification |Architecture mapping |Declared status and observed evidence

|REQ-JAVA-PREVIEW-COMMAND-001
|Five complete typed command values preserve the exact supplied snapshot/adapter/value identities, select exactly one unchanged default planner, invoke no other planner, and return the same `PatchPlan` instance with every listed field/order unchanged.
|Non-functional. Exact compatibility, identity, determinism, and one-path planner fidelity are quality constraints.
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `QS-CORR-011`; Chapters 4-6, 8, 10, and 12.
|Feature-level inherited `@implemented-and-validated`; all five outline cases/60 expanded steps passed through the Java-module runner using the internal seam and real default mappings.

|REQ-JAVA-PREVIEW-COMMAND-002
|The five generic `RefactoringRequest` decoders preserve existing string/path/boolean/default and missing/unknown refusal behavior, construct only complete commands, and converge on the same dispatcher without a parallel direct-planner path.
|Non-functional. Compatibility, conservative refusal, canonical routing, and no duplicate catalogue are architecture/correctness constraints.
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `C-BOUND-009`; `QS-CORR-011`; `R-033`.
|Feature-level inherited `@implemented-and-validated`; its one 11-step case passed through the Java-module runner with sentinel and real default-planner evidence.

|REQ-JAVA-PREVIEW-COMMAND-003
|Daemon JSON-RPC and MCP retain distinct parsing, aliases/defaults, required-field/unknown-operation failures, refused-plan handling, admission, and rendering while supplying each call's current snapshot/adapter to the same typed dispatcher for exactly five Java operations.
|Non-functional. Protocol compatibility, current-collaborator identity, policy ownership, and no pre-validation planner work are architecture quality constraints.
|`UC-PREVIEW-REFACTOR`, `UC-DAEMON`, and `UC-MCP`; `C-BOUND-009`; `QS-CORR-011`; ADRs 0004/0006 decision fit.
|Feature-level inherited `@implemented-and-validated`; after the source-only evidence was rejected, the corrected CLI-module runner passed this one nine-step case through real public `DaemonSession`/`McpSession` dispatch, including all five operations plus missing/unknown/refused and no-write/residue checks.

|REQ-JAVA-PREVIEW-COMMAND-004
|The boundary is fieldless, propagates the exact returned plan or escaping exception, retains no state, contains exactly five variants, and owns none of the listed other-language, `moveClass`, CLI/LSP, multi-file import, protocol, pending, diagnostics, filesystem, `PatchEngine`, apply/rollback, lifecycle, process, catalogue, or persistence behavior.
|Non-functional. Statelessness, exception fidelity, least authority, negative ownership, and plan-only separation are safety/architecture constraints.
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `C-BOUND-009`; `QS-CORR-011`; `R-033`; ADRs 0002/0004/0006 decision fit.
|Feature-level inherited `@implemented-and-validated`; its one 14-step case passed through the Java-module runner. `ManagedApplyInvoker` remains rejected and absent.
|===

The official post-promotion six-feature Gherkin audit reports 27 definitions/unique IDs, 59 cases, 386 declared steps, and 838 expanded steps, all implemented-and-validated with zero parse/tag/status/duplicate/reconciliation errors (SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`). LATE ADR review required no ADR change; LATE C4 validation required no DSL change. External protocol schemas and capability catalogues did not change. `moveClass`, other languages, mixed/experimental/Maven routes, CLI/LSP adoption, multi-file imports, admission/lifecycle, diagnostics, managed writes, and `ManagedApplyInvoker` remain excluded. Focused evidence remains source-built/in-process. Final dated mutable whole-working-tree source-built verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 6m 54s; 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors; 140 XML files; 79 tasks executed; log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`. The targeted affected-module static command passed (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Static classification is deliberately non-zero: the full targeted log contains no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers. Exactly 48 slice-local glue findings remain reviewed/non-blocking: Java-module command glue 24 = 15 NP, 6 RCN, 1 Dm, and 2 BC; CLI real-surface glue 24 = 13 NP, 7 RCN, and 4 BC. Finding text that references dispatcher methods belongs to generated Kotlin nullability analysis at glue call sites, not production methods. The wider configured SpotBugs/PMD baselines remain non-blocking; aggregate static cleanliness and numerical coverage are not claimed. This dated mutable aggregate does not add Gherkin definitions/cases or qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior.

== Transactionality Requirement Clarifications

These requirements capture the audit's required contract decisions and acceptance gaps. They are not implementation claims.

[cols="1,4,1",options="header"]
|===
|ID |Requirement |Status

|REQ-TX-001
|Define atomicity, consistency validation, isolation, durability, compensation, rollback, and crash recovery separately; do not describe current preflighted compensatable batches as ACID or crash-safe transactions.
|needs-review; release-blocking terminology

|REQ-TX-002
|Define the transaction boundary for one plan, recipe, native LSP edit, custom managed LSP apply, diagnostics, and direct library use.
|needs-review

|REQ-TX-003
|Cover process kill, power loss, disk full, permission change, non-atomic filesystems, log corruption, concurrent writers, and editor-buffer divergence in the failure model.
|planned acceptance

|REQ-TX-004
|Normal rollback must detect post-apply conflicts and refuse rather than silently overwrite later changes; forced and manual recovery require explicit contracts.
|planned release blocker

|REQ-TX-005
|Define a durable versioned journal lifecycle, integrity and permissions, retention, startup recovery, and observable incomplete-recovery states before managed writes are called crash-safe.
|planned release blocker

|REQ-TX-006
|Define snapshot scope, engine-verified affected-file preconditions, per-path expected hashes, metadata scope, case sensitivity, generated files, and symbolic-link behavior.
|planned release blocker

|REQ-TX-007
|Define authoritative diagnostics and regression gates before, during simulation, after apply, and after rollback, including deterministic recovery when validation fails.
|planned release blocker

|REQ-TX-008
|Define one-writer workspace locking, timeout, stale-lock recovery, cancellation, and multi-process behavior.
|planned release blocker

|REQ-TX-009
|Classify native LSP edits as client-managed/no-rollback, overlay full-sync buffers, require monotonic versions, emit exact open-document versions, refuse incapable clients, and guard managed disk writes from open-buffer divergence.
|implemented and tested (`TX-007` closed)

|REQ-TX-010
|Stage every recipe step against one evolving immutable snapshot, expose one recipe-wide plan, and perform at most one managed WAL transaction; planning failure and no-op execution must not write journal records.
|implemented and tested (`TX-008` closed)

|REQ-TX-011
|Use one coordinate space per file/plan, merge all same-file edits, validate complete line/character bounds and cross-edit overlap, and render every final file before mutation.
|planned release blocker

|REQ-TX-012
|Treat explicit managed apply as auditable approval, refuse missing authorization, persist approval identity, map managed refusal categories deterministically, and refresh integration state after mutation.
|implemented and tested (`TX-016`, `TX-017`, `TX-018` closed)
|===

== Gherkin Scenario Listing

The configured project feature root `features/` contains eight discovered feature sources:

* `features/authoritative-diagnostics-evaluation.feature`
* `features/java-maven-module-rename.feature`
* `features/java-maven-move-class-apply-authority.feature`
* `features/java-refactoring-preview-command-boundary.feature`
* `features/managed-apply-diagnostics-gate-selector.feature`
* `features/managed-rollback-executor-extraction.feature`
* `features/pending-plan-store-extraction.feature`
* `features/workspace-snapshot-composer-extraction.feature`

The authoritative diagnostics-evaluation `Feature` maps to `UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION` with supporting `UC-DIAGNOSTICS`, `UC-APPLY-PATCH`, and `UC-ROLLBACK` behavior. It has no `Background`. Its two ordinary scenarios and two outlines carry four repository-unique REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` IDs and inherit exactly `@functional-requirement` plus `@implemented-and-validated` from the feature. REQ-001 contributes one 17-step case, REQ-002 contributes twelve seven-step cases, REQ-003 contributes two eight-step cases, and REQ-004 contributes one 11-step case: four definitions, 16 cases, 43 declared steps, and 128 expanded steps. All passed the independent OpenJDK 21.0.11 runner; source reconciliation separately establishes parseability, classification, status, uniqueness, paths, and counts.

The Maven module-rename feature's `Business Need` addresses `UC-JAVA-RENAME-MAVEN-MODULE` for a Maven workspace maintainer through the direct JVM library. It has no `Background`, outline, or examples. Its one ordinary 50-step scenario/case carries repository-unique REQ-JAVA-MAVEN-MODULE-RENAME-001, explicit `@functional-requirement`, and explicit scenario-level `@implemented-and-validated`. The source and passing runner bound planning to the public Java scanner/planner over one declared offline direct-child reactor, followed by separately authorized `PatchEngine` apply and normal rollback; every named protocol, recipe, package/platform, real-project, general-Maven, native-CI, support, and broad-roadmap scope remains excluded.

Final post-promotion whole-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 29s; 82 tasks executed; 144 standard-test XML files; 1,062 tests discovered, 834 executed, 228 skipped/tag-filtered; zero failures/errors. The full log is `/tmp/refactorkit-authoritative-module-rename-final-check.log` with SHA-256 `c3a7b2a902b45267b60179da887c143d9e79f180f9e7ee4043e7631d4e66326d`; the standard-test XML checksum-manifest SHA-256 is `951f7bd281fc4900a096a2295937ee73784294f0ecb2a5c63f469d0c1dea907a`. SpotBugs remains configured non-blocking with `ignoreFailures=true`. The independent receipt classified the slice as reviewed/non-blocking: the five corrected module-rename contract target classes have zero EI/EI2; the authoritative-evaluation EI is non-real on a defensive unmodifiable diagnostics list; relevant BC/Bx reports are Kotlin compiler collection-cast/non-correctness-boxing artifacts; and the 47 authoritative glue findings (27 BC, 11 MS, 6 NP, 1 RCN, 2 UrF) plus 113 module-rename glue findings (37 BC, 19 MS, 46 NP, 10 RCN, 1 REC) are test/glue lifecycle-nullability or generated-bytecode/static-field reports. No slice defect remained unclassified. PMD has no Kotlin source input for this slice and supplies no Kotlin finding claim. No aggregate numerical coverage is available; these totals are not coverage. The existing dedicated packaged Maven task `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest` is intentionally excluded from the standard totals. This source-built receipt does not qualify packaged, native, cross-platform, or general Maven support.

The Maven move-class feature's `Business Need` addresses `UC-JAVA-MOVE-CLASS` for a multi-module Maven workspace maintainer. Its `Background` is not a production scenario. Each of twelve requirement-bearing definitions (ten `Scenario`, two `Scenario Outline`) has one repository-unique stable `@REQ-*` ID, at least one classification, and exactly one effective status. All twelve are `@implemented-and-validated`; the six rows under each outline inherit its ID/classification/status, producing 22 validated executable cases. Focused REQ-008 evidence discovered 22, executed/passed six, skipped sixteen, and had zero failures/errors; independent log SHA-256 `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc`, with `gherkin-writer` execution log `6f7e2d8a9c767f265a117853267a37ab31ec31d0878637870b37202ec8ba2c71`. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. Those execution totals do not alter the Maven subset and do not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. Magrathea field evidence remains separate and bounded. The implemented dedicated packaged Cucumber mode selects the same REQ-001 pickle as a second validation mode; it does not change the twelve definitions, 22 cases, classifications, or effective statuses. One official final OpenJDK 21.0.11/Linux `amd64` run is observed: the dedicated task reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit 22 discovered/one executed/21 tag-filtered/zero failures or errors; and Cucumber five background plus fourteen scenario steps (19/19) with 2/2 hooks; packaged log SHA-256 `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`, JSON SHA-256 `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The separate final whole-tree source-built gate passed 1045 discovered/817 executed/228 skipped or tag-filtered standard tests with zero failures/errors, 140 XML files, and 82 tasks (log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`); it intentionally excludes the packaged task. Targeted custom-source-set PMD/SpotBugs tasks passed (log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`) with an intentionally non-zero classification of 99 reviewed/non-blocking packaged test/deployment glue/harness findings (57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, 1 UrF), no production source addition/modification by this slice, and no aggregate-static-cleanliness or numerical/child-JVM-coverage claim. Its four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`; wiring/report uploads are implemented but unexecuted. The separate source-built REQ-001 review rerun passed 22/one/21/zero with log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`.

The managed-apply diagnostics-gate selector feature has no `Background`. Its ten-row `Scenario Outline` and one ordinary scenario each carry one repository-unique `@REQ-*` ID and inherit `@non-functional-requirement` plus `@implemented-and-validated` from the feature. REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001 contributes ten eight-step cases; REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002 contributes one 12-step case. The resulting two definitions, 11 cases, 20 declared steps, and 92 expanded steps passed the final independent focused OpenJDK 21.0.11 source-built/in-process run with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`). Daemon/MCP adoption is source-inspected and their regressions passed 58/58 and 26/26. Final whole-working-tree source-built verification passed in 7m 11s with 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors, and 78 tasks (log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`); selector production findings are zero after the explicit non-null cleanup, while 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC) and the broader configured SpotBugs baseline remains non-blocking. Broader execution and authority are not inferred.

The managed-rollback feature has no `Background`. Its one ordinary scenario and one seven-row `Scenario Outline` each carry one repository-unique `@REQ-*` ID and inherit `@non-functional-requirement` plus `@implemented-and-validated` from the feature. REQ-MANAGED-ROLLBACK-EXECUTOR-001 contributes one 11-step case; REQ-MANAGED-ROLLBACK-EXECUTOR-002 contributes seven eight-step cases. A focused RED proved mutable diagnostic aliasing after `execute` (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`). After detached unmodifiable ordered snapshotting, the resulting two definitions, eight cases, 19 declared steps, and 67 expanded steps passed the final focused OpenJDK 21.0.11 source-built/in-process runner with 29 tasks and zero failures/errors/skips/undefined/ambiguous/pending; log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`. Production and acceptance paths are listed above. The historical rollback-scoped whole-working-tree source-built run passed with 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors, and 77 tasks (log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`). Slice-local SpotBugs findings are non-zero but reviewed/non-blocking: seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), and 25 glue-only Kotlin/lifecycle/generated warnings remain (13 NP, 1 RCN, 5 MS, 6 BC); the former three `Refused`-diagnostics EI/EI2 findings are eliminated. This aggregate run is not additional Gherkin case evidence, and surface adoption remains inspected rather than executed evidence.

The pending-plan feature has no `Background` or examples. Its two ordinary scenarios each carry one repository-unique `@REQ-*` ID and inherit `@non-functional-requirement` plus `@implemented-and-validated` from the feature, producing two cases and 19 steps. The focused OpenJDK 21.0.11 source-built/in-process runner passed both scenarios, executed 29 Gradle tasks, and reported zero failures/errors; log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`. The subsequent independent 2026-07-31 whole-working-tree source-built run reported 1002 discovered, 782 executed, 220 skipped/tag-filtered, and zero failures/errors across 77 tasks; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`, with no `PendingPlanStore` findings in the configured non-blocking SpotBugs baseline. This evidence remains bounded to mutable source-built behavior; it does not qualify packaged or concurrent behavior, a general extraction pattern, or numerical coverage.

The Workspace Snapshot Composer feature has no `Background`. Its four ordinary scenarios and one four-row `Scenario Outline` each carry one repository-unique `@REQ-*` ID and inherit `@non-functional-requirement` plus `@implemented-and-validated` from the feature. REQ-WORKSPACE-SNAPSHOT-COMPOSER-001/002/003/005 contribute one case with 7/10/8/8 steps respectively; REQ-WORKSPACE-SNAPSHOT-COMPOSER-004 contributes four eight-step failure cases. The result is five definitions, eight cases, 41 declared scenario steps, and 65 expanded case steps, all implemented-and-validated. Final focused source-built/in-process acceptance passed 8/8 scenarios and 65/65 steps with zero failures, errors, or skips. The later whole-tree gate passed with 1029 discovered/809 executed/220 skipped or tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`); this aggregate execution is not additional Gherkin case evidence. Production and adoption evidence remain bounded as stated above.

The Java Refactoring Preview Command feature has no `Background`. Its one five-row `Scenario Outline` and three ordinary scenarios each carry one repository-unique `@REQ-*` ID and inherit `@non-functional-requirement` plus `@implemented-and-validated` from the feature. REQ-JAVA-PREVIEW-COMMAND-001 contributes five 12-step cases; REQ-002/003/004 contribute one case with 11/9/14 steps respectively. The result is four definitions, eight cases, 46 declared scenario steps, and 94 expanded steps. The first independent review blocked promotion while REQ-003 was source-only. The corrected split runners passed seven Java-module cases/85 steps plus one real public daemon/MCP session case/9 steps, all eight cases with zero failures/errors (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`). Final dated mutable source-built aggregate verification passed with 1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, and 79 tasks (log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`); targeted static verification passed with the reviewed non-zero 48-glue-finding classification (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Focused evidence remains bounded source-built/in-process behavior only; this does not qualify packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage behavior.

The current repository-wide audit using official `io.cucumber:gherkin:28.0.0` across all eight paths found eight feature documents, 32 definitions, 76 expanded cases, 32 unique IDs, 479 declared scenario steps, one separate five-step background, and 1,016 expanded case steps. Every definition/case has at least one justified classification and exactly one effective status: all 32 definitions/76 cases are `@implemented-and-validated`, and every other effective status has zero definitions/cases. Parse/tag/status/duplicate-ID/reconciliation errors are zero (OpenJDK 21.0.11 log SHA-256 `ed57576520bdfa92810ebbe93e0e4c238632feae2a74a10a99b35f8e16101925`). The preceding official seven-feature audit remains historical evidence for 28 definitions/60 cases/433 declared steps/885 expanded steps, including the one absent Maven module-rename definition/case (log SHA-256 `271ec890cad8065f0536ab27ca885d81e7d070382ca96fdf9d8cf26afa7e137a`). The preceding official six-feature post-promotion audit remains historical evidence for 27 definitions/59 cases/386 declared steps/838 expanded steps, all then `@implemented-and-validated` (log SHA-256 `f20f39f2421edaf4c5208363a3479a0f7696635631a27d95aeb2a99d04d9be0d`). The preceding official five-feature audit remains historical evidence for its then-current source set (log SHA-256 `b40dc349e7d500b8d5068c15869c4908fb89c68519041e0006c60e2fd9b58867`); the preceding official four-feature audit is likewise historical (log SHA-256 `fb7c81a1d3223e6959d6838ac38c5c737f1bf8804570489405ed59bc1a279bc7`). The pre-promotion four-feature audit hash `57a48fbdf31f91e2ab0d54443849225ba51a4349cc8be84236f62210adc90c14` and earlier audit hashes `f4f9fd559ef9220c145017a2723fc4b8e888424448a392a11d20c1dc33d379c0`, `af9cfc7e69ec589bd5512b963e9eebdce936e63b240e3b1860078f0989b20b7e`, and `13989eb5bad0498de6661715236b46401b15d3401d450137640f2af89cb76ba9` remain preserved receipts for their then-current source sets and statuses.

[cols="2,4,3,4,4,4,3,2,4",options="header"]
|===
|Requirement ID |Scenario |Actor / capability |Classification and rationale |Source tags |Architecture mapping |Feature path |Effective declared status |Validation mode / observed implementation

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001
|An authoritative post-image is journaled and committed with one exact identity
|Embedding JVM application and Core Patch/Transaction Execution; authoritative model-changing managed apply.
|Functional only. The exact accepted apply result, authoritative S1 journal identity, and committed S1/D1 reproduction are observable behavior. The source declares no non-functional tag.
|`@REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 @functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, and `UC-APPLY-PATCH`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012.
|`features/authoritative-diagnostics-evaluation.feature`
|`@implemented-and-validated` (feature-level inherited status)
|Independent OpenJDK 21.0.11 execution passed the ordinary 17-step case as part of the 16/16-scenario, 128/128-step authoritative runner; exact evaluation/journal/apply identity and deep-detachment probes passed.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|Provider authority violation `"<case>"` is refused before WAL
|Embedding JVM application and Core Patch/Transaction Execution; candidate-shell authority enforcement and pre-WAL refusal.
|Functional only. Twelve authority-violation refusal results are observable behavior. The source declares no non-functional tag.
|`@REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002 @functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, and `UC-APPLY-PATCH`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012.
|`features/authoritative-diagnostics-evaluation.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by twelve example rows)
|Seven declared outline steps expand to twelve cases/84 case steps covering tracked-shell identity, module/build-root containment, no-follow external-fingerprint authenticity, and post-provider live-workspace revalidation. All refusal paths passed before WAL.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003
|Post-apply `"<mismatch>"` advances the same record through automatic rollback
|Embedding JVM application and Core Patch/Transaction Execution; post-apply verification and automatic managed compensation.
|Functional only. The mismatch result, one-record lifecycle, exact restoration, and returned refusal are observable behavior. The source declares no non-functional tag.
|`@REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003 @functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-034`; `R-036`; accepted ADRs 0002 and 0012.
|`features/authoritative-diagnostics-evaluation.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by two example rows)
|Eight declared outline steps expand to two cases/16 case steps for semantic snapshot mismatch and diagnostic multiset mismatch. Both passed same-record automatic rollback, exact restoration, restored S0/D0 evaluation, and one-record lifecycle validation.

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-004
|Existing diagnostics-only gates retain additive compatibility
|Existing `DiagnosticsGate.enabled`/`disabled` callers and Core Patch/Transaction Execution; additive API compatibility.
|Functional only. Enabled/disabled invocation, hash, record, apply, and rollback results are observable compatibility behavior. The source declares no non-functional tag.
|`@REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-004 @functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, `UC-DIAGNOSTICS`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-034`; accepted ADRs 0002 and 0012.
|`features/authoritative-diagnostics-evaluation.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The ordinary 11-step case passed exact enabled/disabled call order, lazy/no-call behavior, legacy hashes, diagnostics identity, rollback semantics, schema-v8 compatibility, and negative ownership.

|REQ-JAVA-MAVEN-MODULE-RENAME-001
|An explicit direct-child directory and artifact rename commits once and rolls back exactly
|Maven workspace maintainer through a direct JVM-library caller; bounded direct-child module-directory and independently authorized artifact rename.
|Functional only. The source declares exactly `@functional-requirement`; it does not declare `@non-functional-requirement`. Deterministic preview, least-authority origin proof, diagnostics, one transaction, and exact rollback remain acceptance details within this exact declared classification.
|`@REQ-JAVA-MAVEN-MODULE-RENAME-001 @functional-requirement @implemented-and-validated`
|`UC-JAVA-RENAME-MAVEN-MODULE`, `UC-PREVIEW-REFACTOR`, `UC-APPLY-PATCH`, and `UC-ROLLBACK`; `C-TECH-033`; `C-BOUND-010`; `QS-MODULE-RENAME-001`; `R-035`; accepted ADRs 0002, 0010, and 0012.
|`features/java-maven-module-rename.feature`
|`@implemented-and-validated` (explicit scenario-level status; no inherited status)
|Independent OpenJDK 21.0.11 execution passed the one 50-step scenario/case. The row is bounded to direct public Java scanner/planner use over a no-follow disposable copy of the exact 20-direct-child fixture, caller-explicit `catalog-model` to `catalog-domain` directory and artifact intent, exactly three parser-authorized POM modifications before two file renames, separately authorized `PatchEngine` apply on surface `direct-library`, one exact-S1 schema-v8 record, and exact normal rollback. Broader origin/module/coordinate/JPMS/protocol/recipe/package/platform/real-project/general-Maven/native-CI/support/roadmap scopes remain excluded.

|REQ-JAVA-MAVEN-MOVE-AUTH-001
|A binding-clean move is committed once and rolled back byte for byte
|Maven workspace maintainer; managed semantic move, apply, and rollback.
|Functional and non-functional. The move/apply/rollback outcome is functional; deterministic offline analysis, one managed transaction, authoritative diagnostics, and byte-exact rollback are quality and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-001 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-001`, `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassAvailableAuthorityCucumberTest` passed one isolated copy of the permanent fixture. Two source-built previews from identical snapshot/evidence hashes are deterministic `SEMANTIC_PREVIEW`/`JDT_BINDING`/`ELIGIBLE` results with an `AVAILABLE` model, exactly three reverse observers (`catalog-pricing:main`, `catalog-storefront:main`, `catalog-acceptance:test`), five affected paths, five top-level `FileEdit` entries (four `Modify`, one `Rename`), nested `TextEdit` counts `1/2/1/1` across the four modifies, and zero authoritative before/staged diagnostics. Build-code execution, Maven lifecycle/plugins/annotation processors, settings/credentials/helpers, and network access are denied and no prohibited activity is attempted. One approved preview commits the exact staged hash, source files, auxiliary POM inputs, and seven-source inventory in one lock-held managed transaction; authoritative after diagnostics remain zero. Normal rollback leaves that sole transaction `ROLLED_BACK`, restores every non-engine byte/path, all seven source entries, and the original snapshot hash, and produces authoritative diagnostics exactly equal to the zero-entry baseline. This evidence is fixture/request bounded and remains independent of separately validated scenarios `002`, `003`, and `004`; it does not itself validate other integration shapes, packaged/native matrices, Magrathea, or general authority. The second packaged-process Cucumber mode for this same pickle is now locally observed through one isolated fixture copy and the real self-contained child CLI with scrubbed subject environment, poison-`PATH` proof, 16 bounded commands, repeat `JDT_BINDING` preview, exact human-text lock refusal before WAL, one child-process-replanned apply/sole schema-v8 transaction, post-apply diagnostics, normal rollback, exact non-engine restoration, and bounded cleanup. It makes no cross-process plan-ID, projected `workspace.locked`, or core-code correlation claim. Official final packaged log SHA-256 is `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; JSON SHA-256 is `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. Existing source-built evidence remains the direct lock-held/fault-injector row. The final whole-tree and targeted custom-source-set static gates passed separately; the packaged task is intentionally excluded from standard-test totals, static classification remains non-zero/reviewed/non-blocking at exactly 99 packaged validation-adapter findings, and no aggregate-cleanliness or numerical/child-JVM-coverage claim is made. Native CI wiring/report uploads are implemented but unexecuted; all post-commit native CI rows are `CONFIGURED_UNOBSERVED`.

|REQ-JAVA-MAVEN-MOVE-AUTH-002
|Only binding-proven Java observers are updated
|Maven workspace maintainer; binding-proven observer and reference discrimination.
|Functional. The scenario specifies which legal Java observers are changed and which exact-binding decoy, Java grammar-noncode string, and non-Java candidates remain unchanged.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-002 @functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The dedicated tagged `JavaMavenMoveClassObserverAuthorityCucumberTest` passed one isolated copy of the permanent fixture. Exact target bindings select only direct `catalog-pricing:main` (import, constructor, declared type), transitive `catalog-storefront:main` (import, `instanceof`, class literal), and test-only `catalog-acceptance:test` (Cucumber step-definition import, declared type, exact FQN). Their effective direct dependencies, Build Model/source-root evidence, and dependency-bounded JDT source paths remain unchanged, and no decoy/unrelated root is added. The JDT-only candidate report is exactly `BOUND_TARGET=12`, `BOUND_OTHER=1`, `UNRESOLVED=0`; the sole `BOUND_OTHER` is `catalog-decoy/.../Product.java`. The old-FQN string literal in `reporting-unrelated/.../ProductReport.java` is separately `JAVA_NON_CODE_RESIDUAL=1`, outside the observer closure, and `managedEdit=false`, not `BOUND_OTHER`; the feature resource and fixture-root `README.md` are the two paths counted by `NON_JAVA_RESIDUAL=2`, each with `managedEdit=false`. The decoy and all three residual paths remain byte-identical. Five binding-derived text edits across the target and three observer files plus one declaration-file rename produce five affected paths; every listed Java use resolves to `com.acme.catalog.api.Product`, with no resolvable old target in the authority scope. One lock-held apply commits the exact staged snapshot in one `APPLIED` transaction. Normal rollback leaves that same sole transaction `ROLLED_BACK` and restores every non-engine byte/path, full scanned source inventory, snapshot hash, old declaration path, and absent destination exactly. The source-built CLI preview is asserted to expose the scenario-listed decoy, Java non-code residual, and feature-resource residual paths; the exact candidate-total report also includes the fixture-root README residual. Managed apply is exercised through the planner/core patch path; no other integration surface, native matrix, Magrathea request, or general authority is qualified.

|REQ-JAVA-MAVEN-MOVE-AUTH-003
|Safely enumerable preview defects produce immutable non-managed guidance
|Maven workspace maintainer; typed authority-loss guidance and managed-apply refusal.
|Functional and non-functional. Typed guidance/refusal is an observable result; deterministic evidence, honest completeness, structural non-applicability, pre-WAL refusal, and unchanged workspace state are quality and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-003 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-005`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The dedicated `JavaMavenMoveClassGuidanceAuthorityCucumberTest` passed the one production scenario against four real isolated default-scanner variants. An effective primary Maven `testSourceDirectory` replacement keeps `ProductLifecycleSteps.java` readable but absent from `snapshot.files`; primary replacement is separate from additive build-helper/Kotlin roots, which retain the default Java roots without plugin execution. A still-readable system-path JAR differs from its strict evidence manifest; one target use resolves only to a recovered binding without syntax failure; and a generated source changes the expected inventory of the root owned by `catalog-generated-support:main`. Repeat dispatcher previews produce equal immutable `JavaMoveClassReviewOnlyGuidance`; schema/checklist version 1, request/snapshot/evidence SHA-256 identities, each stable blocker/module/source-set, separate typed candidate/residual groups, explicit completeness and the bounded source-inventory omission, ordered restoration actions, and the seven checklist items are asserted. Strict expected-source/generated-root manifests are snapshot-bound auxiliary files; relevant manifest hashes and expected/observed classpath fingerprints are asserted. CLI JSON preserves typed data with `managed=false`/`applyable=false` and no plan conversion. CLI `--apply` returns `guidance.nonManaged` before `PatchEngine`, workspace lock, WAL, pending plan, or transaction; every byte, path, inventory, and snapshot hash remains equal. The same acceptance preserves REQ-005 broad syntax-failure `LEXICAL_FALLBACK`. Independent final 2026-07-30 OpenJDK 21 `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 13s, 956 discovered, 773 executed, 183 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; reviewed log SHA-256 `8d9b19576a27deaae2c12c767f929664c228db85d1dcc8644de3e41ecbe6ca9d`. This is dated mutable working-tree evidence; SpotBugs retained the configured non-blocking baseline findings. This validates only the Java dispatcher/source-built CLI slice, not force/recipe/daemon/LSP/MCP parity, structural-loss/post-preview-drift refusal, a generic result algebra, native matrices, or general authority.

|REQ-JAVA-MAVEN-MOVE-AUTH-004
|No force flag, recipe, or integration surface promotes lexical fallback
|Maven workspace maintainer; cross-surface lexical-fallback safety invariant.
|Non-functional. The scenario constrains deterministic audit identity, safety, cross-surface consistency, protocol capability separation, evidence visibility, recipe termination, LSP edit suppression, and pre-WAL non-mutation across every entry path.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-004 @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-005`, and `RPK-JAVA-MOVE-007`; ADRs 0002, 0004, 0008, and 0012.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The dedicated `JavaMavenMoveClassLexicalFallbackReviewCucumberTest` passed one isolated fixture case with 38 expanded steps on OpenJDK 21. Broad observer syntax failure produces legacy internal `LEXICAL_FALLBACK`; `JavaMoveClassOperationDispatcher` returns equal immutable `LexicalReview` envelopes on repeat. All direct-dispatch/CLI/daemon-session/LSP-session/MCP-session projections are equal; recipe returns the same envelope and no aggregate. Schema-v1 constants, `NOT_SEMANTICALLY_PROVEN`, normalized request, exact snapshot, four SHA-256 identities, `lexical-review-<sha256>` ID, fixed disposition/actions, sorted typed evidence, path/content-hash/risk-only residual records, record/byte bounds, `COMPLETE or TRUNCATED`, deterministic returned-category counts, and immutable collections are asserted. Replacement/create-byte probes prove that only hidden `reviewArtifactSha256` commits to exact internal ranges/content. No protocol plan/edit/range/replacement/apply token, pending plan, LSP edit, transaction, rollback, or residue exists. Promotion metadata is identity-neutral; compatibility core apply refuses before lock/WAL; recipes discard earlier staged images; lifecycle clearing/eviction makes operations unknown; shared exactly-one parsing distinguishes known structured `evidence.insufficient` from `INVALID_PARAMS`; JSON-RPC `data` and MCP structured error content are asserted; and a clean full-reactor scan remains eligible JDT `Plan`. Focused report: 22 discovered, 1 passed, 21 skipped, zero failures/errors; reviewed log SHA-256 `5fb89c290bd098f249c2ec734c8c7bbffae162e5357ab6b1683042fc68e698ce`. This is source-built/in-process evidence, not packaged/runtime/native/general qualification.

|REQ-JAVA-MAVEN-MOVE-AUTH-005
|Refused lexical-fallback CLI apply leaves no managed-write residue
|Maven workspace maintainer; CLI refusal and no-managed-write-residue safety.
|Non-functional. The scenario constrains read-only scan/preview behavior, pre-WAL refusal, metadata non-creation, transaction absence, and preservation of workspace bytes, paths, inventory, and snapshot hash.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-005 @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-004` and `RPK-JAVA-MOVE-007`.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|`JavaMavenMoveClassApplyAuthorityCucumberTest`, filtered to `@REQ-JAVA-MAVEN-MOVE-AUTH-005 or @REQ-JAVA-MAVEN-MOVE-AUTH-006`, passed both selected scenarios. The `005` path uses a disclosed isolated copy of the permanent fixture with one relevant parse failure, verifies CLI preview evidence `LEXICAL_FALLBACK` and `INELIGIBLE`, and verifies `evidence.insufficient` refusal through the immutable `PatchEngine` evidence gate before workspace-lock acquisition. No `.refactorkit`, `workspace.lock`, WAL, managed transaction, source-byte/path/inventory, or snapshot mutation remains. This evidence is CLI-specific and does not accept positive semantic apply or scenario 004's other entry surfaces.

|REQ-JAVA-MAVEN-MOVE-AUTH-006
|Unrelated JDT warnings do not demote a clean move authority closure
|Maven workspace maintainer; dependency-bounded observer selection and closure-scoped JDT warning authority.
|Functional and non-functional. Retaining a `SEMANTIC_PREVIEW` with exactly the binding-proven observers is observable behavior; limiting the current `AVAILABLE`-fixture warning gate to the proven closure is a correctness and safety constraint. This scenario does not define the separate retained-baseline diagnostic rule for target-scoped `OFFLINE_MISSING` authority.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-006 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassApplyAuthorityCucumberTest` passed this scenario. With one unambiguous target owner, an `AVAILABLE` effective Maven model, and complete `AVAILABLE` source-set ownership, the planner derived the dependency-bounded reverse-observer closure containing `catalog-pricing:main`, `catalog-storefront:main`, and `catalog-acceptance:test`. A type-resolution warning in excluded `reporting-unrelated:main` left the result `SEMANTIC_PREVIEW` with `JDT_BINDING`; a warning introduced in selected observer `catalog-pricing:main` demoted that plan to `LEXICAL_FALLBACK`, and the managed-write gate reported `evidence.insufficient`/`INELIGIBLE`. This validates the `AVAILABLE` warning-scoping slice only; candidate-total `OFFLINE_MISSING` belongs to `007`, while `008` independently validates its four named uncertainty cases and two refusal phases.

|REQ-JAVA-MAVEN-MOVE-AUTH-007
|Candidate-total authority applies and reverses a move with an offline leaf artifact
|Maven workspace maintainer; candidate-total target-scoped semantic preview, managed apply, and rollback.
|Functional and non-functional. Eligible target-scoped move/apply/rollback is observable behavior; complete layered authority, deterministic evidence, exact diagnostic isolation, one transaction, after/rollback attestation, and byte-exact restoration are correctness and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-007 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012 target-scoped Maven `moveClass` authority.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassApplyAuthorityCucumberTest` passed the bounded isolated-fixture row. It hash-checks then removes exactly one synthetic leaf JAR while retaining expected coordinate/path/SHA-256, a provided-type manifest, and `missing` negative-presence evidence. The lease reports `COMPLETE` structure/closure and global `OFFLINE_MISSING`; 13 Java candidate records are classified exactly once as 12 `BOUND_TARGET`, one `BOUND_OTHER`, and zero `UNRESOLVED`; two retained `PriceAuthority` diagnostics have exact before/staged identity and positive non-concealment proof. Repeat previews are identical and eligible with `JDT_BINDING`; apply creates exactly one `APPLIED` transaction; after attestation succeeds; rollback restores all non-engine bytes, paths, source inventory, and snapshot hash exactly and passes restored diagnostics attestation. This remains independent of complete-`AVAILABLE` `001`, implemented six-case `008`, `010`-`012`, and the bounded Magrathea field result.

|REQ-JAVA-MAVEN-MOVE-AUTH-008
|Each isolated "<case>" authority failure is refused at its evidence phase
|Maven workspace maintainer; fail-closed target-authority and evidence-freshness guardrails.
|Non-functional. Safety, evidence integrity/freshness, honest expected-versus-observed identity vocabulary, pre-lock versus lock-held timing, and exact non-overlap are quality constraints. No functional tag is declared.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-008 @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (Scenario Outline level; inherited by six example rows)
|`JavaMavenMoveClassAuthorityFailureCucumberTest` passed all six selected cases under OpenJDK 21: five preview-time cases through the source-built library/CLI and one post-preview under-lock case through library/`PatchEngine` acceptance; sixteen other feature cases were tag-filter skipped and zero failed/errored. No recipe/daemon/LSP/MCP parity is claimed for REQ-008. The observed contract is detailed in the validated matrix above and remains source-built/fixture-bounded.

|REQ-JAVA-MAVEN-MOVE-AUTH-009
|Explicit transitive test scope preserves target-closure move authority
|Maven workspace maintainer; effective Maven dependency-scope derivation and target-closure semantic move authority.
|Functional and non-functional. Correct consumer main/test classpaths plus eligible preview/apply/rollback are observable behavior; preserving explicit declaration scope, proving a complete `AVAILABLE` target closure, keeping outside-closure absence visible but non-promoting, candidate-total lookup protection, staged diagnostic integrity, and exact rollback are correctness and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-009 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012 declaration-provenance Maven scope semantics and target-scoped closure authority.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassExplicitTransitiveScopeAuthorityCucumberTest` passed the bounded isolated-fixture row. Six fixture-owned transitive children explicitly declared `test` retain that scope despite matching omitted/default-`compile` or explicit-`compile` dependency-management entries; they enter neither `catalog-pricing` main/test analysis classpaths nor target-closure missing reports. Every target-closure environment is `AVAILABLE`; the model and lease preserve global `OFFLINE_MISSING` solely for `reporting-unrelated`, structurally outside the closure; the operation lease therefore has no outside-closure per-binary or retained-diagnostic inventory. Repeat candidate-total lookup-protected previews are eligible with `JDT_BINDING`; apply creates exactly one `APPLIED` transaction; after attestation succeeds; rollback restores all non-engine bytes, paths, source inventory, and snapshot hash exactly and passes restored diagnostics attestation. This fixture row does not by itself qualify independently validated `010`/`011`/`012`; the exact Magrathea field result is separately bounded after `010`.

|REQ-JAVA-MAVEN-MOVE-AUTH-010
|Descriptor-pruned children need no artifacts until another path selects them
|Maven workspace maintainer; descriptor-closed Maven selector pruning and alternate-path structural refusal.
|Functional and non-functional. Correctly deriving selected main/test analysis classpaths and preserving eligible preview/apply/rollback on pruned paths are functional outcomes; pruning before child lookup, hash-bound path/projection/reason evidence, alternate-path descriptor restoration, pre-lock/pre-WAL refusal, and exact non-mutation are correctness, reproducibility, and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-010 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012 descriptor-closed selected-graph enumeration.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassDescriptorPruningAuthorityCucumberTest` passed the bounded isolated-fixture row. Six child declarations pruned by exact derived scope, optionality, or inherited exclusion emit 18 deterministic `PRUNED` records across `COMPILE`, `RUNTIME`, and `TEST`, with hash-bound declaring POM, consumer/source set, path, projection, variant, exact reason, and a boundary before child version/range, repository/path, POM/effective-model, JAR, or network lookup. No pruned child becomes a node, subtree, classpath entry, missing input, or graph failure; target-closure environments remain `AVAILABLE`. Repeat candidate-total lookup-protected previews are eligible with `JDT_BINDING`; apply creates exactly one `APPLIED` transaction; after attestation succeeds; rollback restores all non-engine bytes, paths, source inventory, and snapshot hash exactly. An alternate selected compile/runtime path makes the absent child POM mandatory and returns stable structural `REFUSED`/`INELIGIBLE` before lock/WAL with no plan, edit, residue, transaction, or mutation. This does not itself implement independently validated ordinary row `011` or selected-descriptor outline `012`. A separate source-built CLI field run qualified the exact Magrathea `a48e3c06` request after this `010` behavior; it did not require `011` or `012`.

|REQ-JAVA-MAVEN-MOVE-AUTH-011
|RefactorKit-owned ordinary JAR absence preserves target-scoped move authority
|Maven workspace maintainer; ordinary selected-missing-binary semantic preview, managed apply/rollback, and drift refusal.
|Functional and non-functional. Eligible target-scoped preview/apply/rollback is functional; deterministic local-repository provenance, sidecar-free no-follow negative presence, complete selected-POM graph evidence, candidate and target-lookup totality, diagnostics, one transaction, exact rollback, and under-lock pre-WAL drift refusal are correctness and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-011 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-003`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012 ordinary missing-binary path.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (scenario-level; no inherited status)
|The tagged `JavaMavenMoveClassOrdinaryMissingLeafAuthorityCucumberTest` passed the bounded isolated-fixture row. One fixed non-snapshot unclassified `com.acme.fixture.missing:ordinary-missing-leaf:1.0.0` `jar` is selected across `COMPILE`, `RUNTIME`, and `TEST` from one explicit local-only Maven-2-layout repository. Its POM/effective-input descriptor closure and graph-leaf proof are present and hash-bound while its JAR is absent under sidecar-free no-follow observations; no expected JAR hash or provided-type inventory is invented. Thirteen before/staged candidate records have exact JDT classifications and target-name lookup protection with zero `UNRESOLVED` candidates and zero unresolved/unattributed diagnostics. Repeat previews are eligible with `JDT_BINDING`; apply creates exactly one `APPLIED` transaction; after attestation succeeds; rollback restores all non-engine bytes, paths, source inventory, and snapshot hash exactly. Three fresh previews independently introduce JAR/path appearance or replacement, selected-POM/effective-input replacement, or repository/deterministic-path identity drift while the workspace lock is held; each returns `authorityLease.evidenceDrift` before WAL with no managed mutation. This fixture evidence is independent of the earlier Magrathea CLI field qualification.

|REQ-JAVA-MAVEN-MOVE-AUTH-012
|Missing or drifted selected descriptors refuse ordinary missing-leaf authority
|Maven workspace maintainer; fail-closed selected-descriptor authority and structural refusal guidance.
|Functional and non-functional. Typed structural refusal and a descriptor-specific blocker are observable behavior; complete selected-descriptor closure, hash freshness, offline determinism, pre-lock/pre-WAL non-mutation, and prohibition of binary-absence downgrade are correctness and safety constraints.
|`@REQ-JAVA-MAVEN-MOVE-AUTH-012 @functional-requirement @non-functional-requirement @implemented-and-validated`
|`UC-JAVA-MOVE-CLASS`; `RPK-JAVA-MOVE-002`, `RPK-JAVA-MOVE-004`, `RPK-JAVA-MOVE-006`, and `RPK-JAVA-MOVE-007`; accepted ADR 0012 selected-descriptor closure and fail-closed blocker.
|`features/java-maven-move-class-apply-authority.feature`
|`@implemented-and-validated` (Scenario Outline level; inherited by six example rows; no feature-level status)
|The tagged `JavaMavenMoveClassSelectedDescriptorAuthorityCucumberTest` passed all six isolated fixture cases. Each case starts from an eligible ordinary `011` baseline and an engine-captured `MavenSelectedDescriptorAuthorityContext` with safe descriptor path/layer/SHA-256 expectations plus a SHA-256 parsed descriptor identity proving model/relocation interpretation. The independent mutations yield `MISSING` for selected POM, parent, or parsed identity; `MALFORMED` for selected-POM XML/model; and `DRIFTED` for imported BOM or dependency-management/mediation declaration. Each result is structural `REFUSED`/`INELIGIBLE` with `java.maven.selectedDescriptor.missing`, `.malformed`, or `.drifted`, no authority lease, and a repeat-stable blocker naming coordinate, direct path from `catalog-pricing:main`, `COMPILE`/`RUNTIME`/`TEST`, descriptor layer, and condition. Refusal precedes semantic edit selection, applyable plan ID, lock, and WAL; the failed descriptor is never reported as an admissible missing binary or proven leaf, `.refactorkit` is never created, and recorded bytes, paths, inventories, snapshot hash, descriptor facts, evidence records, and evidence hashes remain equal after evaluation.

|REQ-PENDING-PLAN-STORE-001
|Exact session payloads follow a true 128-entry access-order bound
|Daemon/LSP/MCP session support; exact bounded pending-plan retention and isolation.
|Non-functional. Exact payload identity, session isolation, resource bounds, true access-order eviction, missing-plan compatibility, and absence of write/transaction side effects are quality and safety constraints.
|`@REQ-PENDING-PLAN-STORE-001 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|UC-DAEMON, UC-LSP, and UC-MCP; `C-TECH-028`; `QS-PROTO-003`; ADRs 0002 and 0004.
|`features/pending-plan-store-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|`PendingPlanStoreCucumberTest`/steps used separate generic stores and exact object identity; filled 128 entries, touched entry 1, inserted entry 129, and observed entry 2 evicted; verified session isolation, missing-plan responses through real daemon/LSP/MCP sessions, and no direct sentinel-workspace, `.refactorkit`, lock, WAL, transaction, or rollback effect from eviction/remove/clear. The 13-step scenario passed in the focused OpenJDK 21.0.11 source-built/in-process run. It does not qualify packaged or concurrent behavior.

|REQ-PENDING-PLAN-STORE-002
|Integration surfaces preserve their existing lifecycle policies
|Daemon/LSP/MCP session support; behavior-preserving store adoption with surface-owned policy.
|Non-functional. Separation of concerns, compatibility, lifecycle asymmetry, response stability, and non-expansion of protocol/authority behavior are quality and safety constraints.
|`@REQ-PENDING-PLAN-STORE-002 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|UC-DAEMON, UC-LSP, and UC-MCP; `C-TECH-028`; `QS-PROTO-003`; ADRs 0002 and 0004.
|`features/pending-plan-store-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The six-step scenario verified that the shared store's public boundary is only insert/lookup/remove/clear, verified distinct `PendingPlanStore` instances in real `DaemonSession`, `LspSession`, and `McpSession` objects, and exercised their existing in-process admission, discard availability, selected-versus-all apply consumption, general/stale-Kotlin refusal behavior, rollback retention/clear differences, project/document/close clearing, MCP importer exception, and success/error responses. Lifecycle policy remains at the surface call sites; no uniform lifecycle, protocol capability, or authority rule was introduced. Exact production paths are `PendingPlanStore.kt`, `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt`. Packaged, concurrency, and general service-extraction qualification remains open.

|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001
|Selection case `"<case>"` follows the first matching ordered route
|Daemon/MCP managed-apply maintainers; implemented deterministic gate-routing compatibility.
|Non-functional. Exact precedence, case sensitivity, identity, routing determinism, and non-normalization are correctness and compatibility constraints.
|`@REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-APPLY-PATCH`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-030`; `C-BOUND-007`; `QS-DIAG-007`; `R-031`; ADRs 0002 and 0004.
|`features/managed-apply-diagnostics-gate-selector.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by ten example rows)
|The outline declares eight steps and expands to ten cases/80 case steps. All ten passed through the production selector in the independent OpenJDK 21.0.11 source-built/in-process run, validating exact precedence, gate IDs, route/provider identity, original case-sensitive metadata, and no normalization. Source inspection confirms daemon/MCP adoption; their module regressions passed. No packaged/native/cross-platform/concurrent/apply-authority/numerical-coverage claim is inferred.

|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002
|Selection remains lazy, per-call, and bounded to the duplicate daemon and MCP decision
|Daemon/MCP managed-apply maintainers; implemented laziness, adapter identity, external-resolution, and authority boundary.
|Non-functional. Provider deferral, exact external lookup/exception identity, per-call state, scope exclusion, and unchanged `PatchEngine` authority are safety and compatibility constraints.
|`@REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-002 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-APPLY-PATCH`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-030`; `C-BOUND-007`; `QS-DIAG-008`; `R-031`; ADRs 0002 and 0004.
|`features/managed-apply-diagnostics-gate-selector.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The ordinary scenario's 12 steps passed through the production selector in process. Acceptance verifies zero provider calls during selection, exact per-call adapters, one synchronous lookup only for external IDs, unchanged external gate/exception identity, no workspace mutation, and explicit exclusions. It does not invoke `PatchEngine`; apply authority remains unchanged.

|REQ-MANAGED-ROLLBACK-EXECUTOR-001
|One visible applied record is durably rolled back through the real engine
|CLI/daemon/LSP/MCP rollback callers; implemented shared resolution and authoritative engine handoff.
|Non-functional. Exact ordering, full-record identity, responsibility separation, one-call behavior, durability handoff, and absence of caller-policy migration are correctness, safety, traceability, and compatibility constraints.
|`@REQ-MANAGED-ROLLBACK-EXECUTOR-001 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-ROLLBACK`; `C-TECH-029`; `C-BOUND-006`; `QS-SAFE-012`; `R-030`; ADRs 0002 and 0004.
|`features/managed-rollback-executor-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|`ManagedRollbackExecutorCucumberTest`/steps exercised the core executor with `TransactionLog` and real `PatchEngine` in process. The ordinary scenario declares and passed 11 steps as one case, including durable `ROLLED_BACK` journal state, pre-image path/byte/metadata restoration, exact complete resolved-record identity, one outer lookup, and one engine call. Source inspection confirms CLI/daemon/LSP/MCP adoption; no packaged or cross-platform surface execution is inferred.

|REQ-MANAGED-ROLLBACK-EXECUTOR-002
|Short circuits and surface visibility retain distinct outcomes
|CLI/daemon/LSP/MCP rollback callers; implemented cross-surface compatibility boundary and typed failure/outcome handoff.
|Non-functional. Fail-fast ordering, surface visibility, document safety, exact call counts, outcome discrimination, exception fidelity, evidence identity, and policy ownership are correctness, safety, and compatibility constraints.
|`@REQ-MANAGED-ROLLBACK-EXECUTOR-002 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-ROLLBACK`; `C-TECH-029`; `C-BOUND-006`; `QS-SAFE-012`; `R-030`; ADRs 0002 and 0004.
|`features/managed-rollback-executor-extraction.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by seven example rows)
|The final focused runner passed all seven outline cases/56 expanded case steps. Acceptance glue verifies the declared parse/lookup/guard/engine/lock counts, distinct outcomes, exact record identity, journal exception identity/code/message/cause, and LSP rejection payload ordering in process. For the daemon refusal row it mutates the retained mutable diagnostics source after `execute`, verifies the outcome kept the ordered baseline values, and verifies the public outcome list rejects mutation. Actual surface adoption is source-inspected only; packaged, cross-platform, concurrent rollback, and crash/restart behavior remain unqualified.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001
|An empty source contribution preserves the exact authoritative base
|Daemon/MCP maintainers; implemented exact no-contribution snapshot composition.
|Non-functional. Exact instance/hash preservation, compatibility, deterministic no-op behavior, and zero unnecessary TypeScript attachment are quality constraints.
|`@REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `QS-SNAPSHOT-001`; `R-032`.
|`features/workspace-snapshot-composer-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The final focused runner exercised the production composer and passed this ordinary seven-step case in process, including exact instance/hash and zero conditional-attacher calls.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-002
|A non-empty source contribution overlays only its bounded inventory
|Daemon/MCP maintainers; implemented deterministic mixed-source overlay.
|Non-functional. Authority containment, normalized collision precedence, exact ordering/unions, and repeatable hash identity are correctness and reproducibility constraints.
|`@REQ-WORKSPACE-SNAPSHOT-COMPOSER-002 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-001`; `R-032`.
|`features/workspace-snapshot-composer-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The final focused runner exercised the production composer and passed this ordinary ten-step case in process, including exact three-field overlay, later-source precedence, ordering, retained authority, and deterministic hash.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-003
|Each call attaches conditional evidence once before its optional current final evidence
|Daemon/MCP maintainers; implemented per-call collaborator ordering and isolation.
|Non-functional. Exact invocation order/identity, no retained collaborator state, and typed provider-result fidelity are quality and compatibility constraints.
|`@REQ-WORKSPACE-SNAPSHOT-COMPOSER-003 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-001`/`002`; `R-032`.
|`features/workspace-snapshot-composer-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The final focused runner exercised the same production composer across three calls and passed this ordinary eight-step case, proving per-call identity/order and no retained collaborator reuse in process.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-004
|A collaborator failure escapes unchanged without composition side effects
|Daemon/MCP maintainers; implemented fail-fast identity and no-side-effect boundary.
|Non-functional. Failure fidelity, no later work, least privilege, and exact non-mutation are safety and compatibility constraints.
|`@REQ-WORKSPACE-SNAPSHOT-COMPOSER-004 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-002`; `R-032`.
|`features/workspace-snapshot-composer-extraction.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by four example rows)
|The final focused runner passed all four outline cases/32 expanded steps against the production composer, retaining exact sentinel identity, short-circuit order, and recorded no-side-effect state in process.

|REQ-WORKSPACE-SNAPSHOT-COMPOSER-005
|Adoption centralizes composition only for daemon and MCP
|Daemon/MCP maintainers; implemented bounded adoption with exact ownership exclusions.
|Non-functional. Dependency direction, separation of concerns, protocol/lifecycle compatibility, and no authority expansion are architecture quality constraints.
|`@REQ-WORKSPACE-SNAPSHOT-COMPOSER-005 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-SCAN`, `UC-DAEMON`, and `UC-MCP`; `C-TECH-031`; `C-BOUND-008`; `QS-SNAPSHOT-002`; `R-032`; ADRs 0004/0009/0010 decision fit.
|`features/workspace-snapshot-composer-extraction.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The final focused case validates the exact implementation and ownership assertions by source inspection. Production adoption is limited to private daemon/MCP `scanWorkspace`; CLI/LSP remain excluded. Daemon/MCP 58/58 and 26/26 regressions support compatibility, but no packaged surface execution or general orchestration is inferred.

|REQ-JAVA-PREVIEW-COMMAND-001
|Complete typed `"<command>"` values preserve one existing planner invocation
|Java adapter maintainers; exact five-route plan-only dispatch.
|Non-functional. Exact input/output identity, planner selection, and unchanged evidence/order are correctness and compatibility constraints.
|`@REQ-JAVA-PREVIEW-COMMAND-001 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `QS-CORR-011`.
|`features/java-refactoring-preview-command-boundary.feature`
|`@implemented-and-validated` (Scenario Outline inherits feature status; inherited by five example rows)
|The Java-module runner passed all five cases/60 expanded steps. The internal invoker seam proves exact snapshot/adapter/value/plan identity with sentinels; source and real default-planner probes preserve the existing five mappings.

|REQ-JAVA-PREVIEW-COMMAND-002
|The generic Java adapter remains a compatibility decoder into the canonical path
|Java library callers; generic request compatibility and canonical dispatch.
|Non-functional. Decoder/refusal compatibility and elimination of a duplicate planner catalogue are correctness/architecture constraints.
|`@REQ-JAVA-PREVIEW-COMMAND-002 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `C-BOUND-009`; `QS-CORR-011`; `R-033`.
|`features/java-refactoring-preview-command-boundary.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The Java-module runner passed the one 11-step case, including all five complete decoders, safe-delete false default, missing/unknown generic refused-plan fidelity, and no parallel direct-planner routes.

|REQ-JAVA-PREVIEW-COMMAND-003
|Daemon and MCP keep distinct surface policy around the same typed dispatch
|Daemon/MCP callers and maintainers; public in-process preview compatibility.
|Non-functional. Surface ACL ownership, current collaborator identity, error/admission/rendering compatibility, and pre-validation refusal are architecture quality constraints.
|`@REQ-JAVA-PREVIEW-COMMAND-003 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-PREVIEW-REFACTOR`, `UC-DAEMON`, and `UC-MCP`; `C-BOUND-009`; `QS-CORR-011`.
|`features/java-refactoring-preview-command-boundary.feature`
|`@implemented-and-validated` (feature-level inherited status)
|After the first independent review rejected source-only evidence, the corrected CLI-module runner passed the one nine-step case through public `DaemonSession` and `McpSession` dispatch. It exercises all five operations plus missing, unknown, and refused paths, current per-call state, retained surface policy, and no preview writes or `.refactorkit` residue.

|REQ-JAVA-PREVIEW-COMMAND-004
|The boundary remains stateless, plan-only, and closed to the five-command slice
|Architecture maintainers; least-authority and negative-ownership boundary.
|Non-functional. Statelessness, exact exception identity, no side effects, and closed scope are safety/architecture constraints.
|`@REQ-JAVA-PREVIEW-COMMAND-004 @non-functional-requirement @implemented-and-validated` (classification/status inherited from Feature)
|`UC-PREVIEW-REFACTOR`; `C-TECH-032`; `C-BOUND-009`; `QS-CORR-011`; `R-033`.
|`features/java-refactoring-preview-command-boundary.feature`
|`@implemented-and-validated` (feature-level inherited status)
|The Java-module runner passed the one 14-step case, proving exact plan/exception pass-through, no retained fields, exactly five variants, named exclusions, no independent diagnostics/write/lock/WAL/transaction/apply/rollback/lifecycle/persistence behavior, and no `ManagedApplyInvoker`.
|===

=== REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION Validated Expanded Case Matrix

All 16 rows inherit `@functional-requirement` and
`@implemented-and-validated` from
`features/authoritative-diagnostics-evaluation.feature`. Every row passed the
independent OpenJDK 21.0.11 runner (16/16 scenarios, 128/128 steps; log SHA-256
`bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2`).

[cols="2,2,3,3,2",options="header"]
|===
|Requirement |Expanded case |Exact source condition |Required result |Effective status / evidence

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001
|1 — exact authoritative post-image
|Exact baseline `S0`/`D0`, exact staged file candidate `C1`, and deep-detached provider result `S1`/`D1` for that candidate.
|Core verifies construction-time identity, pre-WAL authority, exact schema-v8 `S1.hash`, and committed `S1`/`D1` before `Applied` returns.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|2 — workspace root
|Provider returns a snapshot rooted at a different bounded workspace.
|Refuse normalized-workspace-root authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|3 — source and auxiliary split
|Provider repartitions one tracked file between source and auxiliary inventories.
|Refuse source/auxiliary-partition authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|4 — tracked path
|Provider returns one tracked file under a different normalized path.
|Refuse tracked-path-inventory authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|5 — language ID
|Provider changes one tracked file's language ID.
|Refuse tracked-language-identity authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|6 — tracked content
|Provider changes one tracked file's content.
|Refuse tracked-content authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|7 — source extensions
|Provider returns a different `sourceExtensions` set.
|Refuse source-extension-scope authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|8 — ignored directories
|Provider returns a different `ignoredDirectories` set.
|Refuse ignored-directory-policy authority before WAL or managed edit.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|9 — outside Module root
|Provider places one `Module.root` outside the candidate workspace.
|Core refuses normalized module-root containment before WAL.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|10 — outside Module source root
|Provider places one module source root outside the candidate workspace.
|Core refuses normalized module-source-root containment before WAL.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|11 — outside BuildModule root
|Provider places one `BuildModule.root` outside the candidate workspace.
|Core refuses normalized build-module-root containment before WAL.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|12 — fabricated external fingerprint
|Provider claims a fingerprint different from core's current no-follow recomputation for an explicitly allowed external regular file.
|Core refuses external classpath-evidence authenticity before WAL.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002
|13 — live workspace mutation
|Provider changes a live tracked file while otherwise returning the exact candidate.
|Post-provider live-S0 revalidation refuses before WAL; provider diagnostics authorize no write.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003
|14 — semantic snapshot mismatch
|Committed evaluation returns a hash different from `S1.hash` with diagnostics `D1`.
|The same schema-v8 record advances through automatic rollback and restored `S0`/`D0`; no second transaction exists.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003
|15 — diagnostic multiset mismatch
|Committed evaluation returns exact `S1.hash` with diagnostics different from `D1`.
|The same record follows the identical automatic-rollback and restored-evaluation contract.
|`@implemented-and-validated`; passed

|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-004
|16 — diagnostics-only compatibility
|Enabled and disabled legacy gates are used without authoritative evaluation.
|Enabled retains three lazy provider calls and existing hashes/semantics; disabled invokes no provider; schema-v8 meanings stay unchanged.
|`@implemented-and-validated`; passed
|===

=== REQ-JAVA-PREVIEW-COMMAND-001 Validated Example Matrix

All five rows inherit REQ-JAVA-PREVIEW-COMMAND-001, `@non-functional-requirement`, and `@implemented-and-validated` from one repository-unique `Scenario Outline`. Every row passed through the Java-module runner; the default mapping remains exactly the production mapping below.

[cols="2,3,4",options="header"]
|===
|Command |Complete values |Existing default planner mapping

|`RenameClass`
|`SymbolId`, `newName`
|`JavaRenameClassPlanner(currentAdapter).preview(currentSnapshot, symbolId.value, newName)`

|`RenameMember`
|`SymbolId`, `newName`
|`JavaRenameMemberPlanner(currentAdapter).preview(currentSnapshot, symbolId.value, newName)`

|`MoveSourceRoot`
|`from`, `to` paths
|`JavaMoveSourceRootPlanner(currentAdapter).preview(currentSnapshot, from, to)`

|`OrganizeImports`
|One exact Java file path
|`JavaOrganizeImportsPlanner().previewSingleFile(currentSnapshot, file)`

|`SafeDelete`
|`SymbolId`, complete `force` boolean
|`JavaSafeDeletePlanner(currentAdapter).preview(currentSnapshot, symbolId.value, force)`
|===

=== REQ-WORKSPACE-SNAPSHOT-COMPOSER-004 Validated Example Matrix

All four rows inherit REQ-WORKSPACE-SNAPSHOT-COMPOSER-004, `@non-functional-requirement`, and `@implemented-and-validated` from one repository-unique `Scenario Outline`. Every row passed against the production composer in the final focused source-built/in-process run.

[cols="2,1,2,4",options="header"]
|===
|Collaborator |Stage |Sentinel failure |Validated exact boundary

|Authoritative base scanner
|1
|`E_primary`
|The same sentinel object escapes; stages 2-4 do not run; all recorded state and no-side-effect exclusions remain unchanged.

|Secondary source-inventory scanner
|2
|`E_secondary`
|The same sentinel object escapes; stages 3-4 do not run; all recorded state and no-side-effect exclusions remain unchanged.

|TypeScript-configured conditional evidence attacher
|3
|`E_conditional`
|The same sentinel object escapes; stage 4 does not run; all recorded state and no-side-effect exclusions remain unchanged.

|Current Kotlin-configured final evidence attacher
|4
|`E_final`
|The same sentinel object escapes after earlier successful read stages; all recorded state and no-side-effect exclusions remain unchanged.
|===

=== REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001 Validated Example Matrix

All ten rows inherit REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001, `@non-functional-requirement`, and `@implemented-and-validated` from one repository-unique `Scenario Outline`. Every row passed through the production selector in the focused source-built/in-process run.

[cols="2,3,1,2,4",options="header"]
|===
|Example |Exact routing distinction |Precedence row |Specified gate ID |Validated lazy provider or identity

|exact Java Maven ownership
|Exact `java` plus `java.moveAcrossMavenModules`
|1
|`java-maven-ownership`
|Current-Java-adapter Maven ownership provider

|ordinary Java
|Exact `java` after row 1 fails
|2
|`java-jdt`
|Current Java adapter diagnostics provider

|Java-affected Kotlin move beats evidence
|Exact `kotlin`, original `.java` affected file, `moveDeclaration`, and `JDT_BINDING`
|3
|`kotlin-k2-java-jdt`
|Current-Kotlin-adapter move-declaration planner provider

|Java-affected non-move JDT binding
|Exact `kotlin`, original `.java` affected file, non-move operation, and `JDT_BINDING`
|4
|`kotlin-k2-java-jdt`
|Current-Kotlin-adapter Java-to-Kotlin public-type rename provider

|Java-affected broad fallback
|Exact `kotlin`, original `.java` affected file, unusual non-move/non-JDT metadata
|5
|`kotlin-k2-java-jdt`
|Current-Kotlin-adapter Kotlin-to-Java public-type rename provider

|Kotlin-only before operation and evidence
|Exact `kotlin` with no original lower-case `.java` final segment
|6
|`kotlin-k2`
|Current Kotlin adapter compiler-diagnostics result provider

|uppercase JAVA is not Java-affected
|Exact `kotlin` with `Foo.JAVA`, proving case-sensitive final-segment matching
|6
|`kotlin-k2`
|Current Kotlin adapter compiler-diagnostics result provider

|case-changed Java ID is external
|Exact language ID `Java`, without case normalization
|7
|`external-case-sensitive-java-v1`
|Exact resolver-returned gate unchanged

|case-changed Kotlin ID is external
|Exact language ID `Kotlin`, without case normalization
|7
|`external-case-sensitive-kotlin-v1`
|Exact resolver-returned gate unchanged

|ordinary external ID is opaque
|Exact language ID `typescript`
|7
|`typescript-compiler-exact-v1`
|Exact resolver-returned gate unchanged
|===

Every row is `@implemented-and-validated`. The table records the declared metadata and the route, gate ID, provider/adapter identity, external resolver behavior, and selection-time laziness observed by the focused production-selector acceptance; it does not claim packaged surface execution or apply authority.

=== REQ-JAVA-MAVEN-MOVE-AUTH-008 Validated Example Matrix

All rows inherit REQ-JAVA-MAVEN-MOVE-AUTH-008, `@non-functional-requirement`, and `@implemented-and-validated` from one repository-unique Scenario Outline. They are six executable cases: five preview-time cases and one lock-held apply-revalidation case.

[cols="1,2,3,3,3",options="header"]
|===
|Example / phase |Validated isolated fixture defect |Outcome and primary blocker |Observed identity and typed data |Timing, residue, and non-overlap

|1 — preview
|Inside `catalog-storefront:main`, create a competing decoy `Product`, retain the target import, and add the competing import to `ProductTile.java`. The parse-valid simple-name use has ambiguous/problem JDT evidence and is `UNRESOLVED`; it is neither recovered nor truncated.
|Immutable `REVIEW_ONLY_GUIDANCE`; `java.maven.moveClass.candidate.unresolved`.
|Request, mutated `S0`, canonical guidance evidence; `authorityLayer=CANDIDATE_TOTALITY`; module/source set; path/content hash; every exact candidate range; `bindingState=AMBIGUOUS`; competing FQNs; `classification=UNRESOLVED`; `candidateCompleteness=COMPLETE`.
|`guidance.nonManaged` before `PatchEngine`, lock, WAL, or `.refactorkit`; no plan/edit. Recovered binding and preview-time fingerprint mismatch remain REQ-003; truncation/unbounded inventory is structural refusal.

|2 — preview
|Add unresolved `import static com.acme.fixture.missing.ProductProvider.*;` to `ProductTile.java` while retaining the exact target import. Every actual `Product` candidate remains exact `BOUND_TARGET` or `BOUND_OTHER`.
|Immutable `REVIEW_ONLY_GUIDANCE`; `java.maven.moveClass.targetLookup.unresolvedPrerequisite`.
|Request, `S0`, canonical guidance evidence; `authorityLayer=TARGET_NAME_LOOKUP`; `prerequisiteKind=STATIC_IMPORT_ON_DEMAND`; import path/range/content hash; unresolved owner; target simple name; affected candidate-range hash.
|Same pre-lock/no-residue non-managed refusal. This is lookup-prerequisite uncertainty, not broad REQ-004 lexical fallback.

|3 — preview
|Replace the string field in `reporting-unrelated/.../ProductReport.java` with an exact Java type use of `com.acme.catalog.legacy.Product`; `reporting-unrelated:main` retains no dependency path to `catalog-model`.
|Immutable `REVIEW_ONLY_GUIDANCE`; primary `java.maven.moveClass.reverseObserverClosure.explicitOldFqnOutside`.
|Request, `S0`, canonical guidance evidence; `authorityLayer=CLOSURE_CONSISTENCY`; exact path/range/content hash/FQN; source set; `closureMembership=OUTSIDE`; `dependencyPath=NONE`; closure-evidence hash; observed classification `UNRESOLVED`.
|Same pre-lock/no-residue refusal. Closure blocker takes precedence over generic `candidate.unresolved`, preserving independence from row 1.

|4 — preview
|In one unchanged disk snapshot, add the fixture dependency and same-package non-annotation `PriceAuthority`, annotate legacy `Product`, and make the fixture type absent. Before analysis reports the unresolved annotation; staged package move resolves the spelling as a non-annotation, changing problem ID/message.
|Immutable `REVIEW_ONLY_GUIDANCE`; `java.maven.moveClass.retainedDiagnostic.identityDrift`.
|Request, disk `S0`, staged overlay `S*`, canonical guidance evidence; `authorityLayer=DIAGNOSTIC_IDENTITY`; `phase=BEFORE_VS_STAGED`; before/staged provider hashes, problem IDs, categories, severities, mapped paths/ranges, messages, diagnostic-multiset hashes; `changedFields=[problemId,message]`.
|Same pre-lock/no-residue refusal. No disk/provider input changes between analyses; later external change belongs only to row 6.

|5 — structural preview
|Delete `catalog-shipping/pom.xml` while the readable hash-bound root POM still globally declares active module `catalog-shipping`.
|`PatchStatus.REFUSED` with `RefactoringEvidence.STRUCTURAL`; `java.maven.reactorDescriptor.missing`.
|Result-owned `JavaMoveClassStructuralRefusal`: `STRUCTURAL_CLOSURE`; `ACTIVE_REACTOR_POM`; module; declaring root-POM hash/range; expected path; `MISSING`; deterministic component-wise workspace-relative `NOFOLLOW` absence hash. Missing directory and missing final POM have distinct stable facts; symlink components/final symlink/non-regular final path are not `MISSING`. Empty edits; no lease, approval, pending plan, or apply identity.
|Preview and source-built CLI refusal before lock/WAL/residue. This is not REQ-010 alternate selected child POM or REQ-012 selected external descriptor loss.

|6 — apply revalidation
|First produce an eligible REQ-007 preview containing one non-managed `BOUND_OTHER` decoy. After `.refactorkit/workspace.lock` is acquired but before authority validation/WAL, replace `catalog-decoy/.../Product.java` with valid source declaring `ProductDecoy`, changing candidate inventory without touching a managed target path.
|Initial `SEMANTIC_PREVIEW` / `JDT_BINDING` / `ELIGIBLE`; apply returns typed `ApplyResult.Refused` with `authorityLease.evidenceDrift`.
|Immutable `OperationAuthorityFileEvidence` plus `DiagnosticDetails`: `EVIDENCE_FRESHNESS`; `CANDIDATE_INVENTORY`; path; expected/observed content hashes; preview `S0`; staged `S*`; one observed engine snapshot `S1`; expected semantic candidate-inventory SHA-256; expected/observed required-file-evidence SHA-256; lease `E0`; no observed semantic candidate inventory/hash.
|Primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged` while lock held and before WAL. Preserve external `S1`; create no WAL, transaction, managed target edit, or destination. Lock may remain. Selected JAR/POM/effective-input/repository/path drift remains REQ-011.
|===

No row returns `LEXICAL_FALLBACK_REVIEW`, promotes scanner-only evidence to `JDT_BINDING`, or overlaps REQ-003/004/010/011/012. Repeat evaluation is deterministic for the same canonical inputs.

=== REQ-JAVA-MAVEN-MOVE-AUTH-012 Example Matrix

All rows inherit REQ-JAVA-MAVEN-MOVE-AUTH-012, `@functional-requirement`, `@non-functional-requirement`, and `@implemented-and-validated` from the Scenario Outline. They are six executable cases under one repository-unique requirement ID. Tagged evidence validates structural `REFUSED`/`INELIGIBLE` guidance before semantic edit selection, applyable plan ID, lock, or WAL; no row is downgraded to ordinary missing-binary authority or leaves managed residue, and every recorded context/evidence hash remains stable.

[cols="1,2,5,2,2",options="header"]
|===
|Example |Descriptor layer |Independent mutation |Descriptor condition |Expected result

|1
|Selected leaf POM
|Remove the deterministic selected leaf POM regular file.
|`MISSING`
|`REFUSED` / `INELIGIBLE`

|2
|Selected leaf POM model
|Replace the selected leaf POM with malformed XML that cannot produce a complete raw or effective model.
|`MALFORMED`
|`REFUSED` / `INELIGIBLE`

|3
|Required parent POM
|Remove the one fixture-owned parent POM referenced by the selected leaf.
|`MISSING`
|`REFUSED` / `INELIGIBLE`

|4
|Required imported BOM
|Change the one fixture-owned imported BOM after its content hash is bound.
|`DRIFTED`
|`REFUSED` / `INELIGIBLE`

|5
|Dependency-management/mediation declaration
|Change the required declaration that supplies the mediated selected version after hash binding.
|`DRIFTED`
|`REFUSED` / `INELIGIBLE`

|6
|Relocation/model parse evidence
|Remove parsed-model proof that the selected leaf has no relocation while descriptor bytes remain present.
|`MISSING`
|`REFUSED` / `INELIGIBLE`
|===

=== REQ-MANAGED-ROLLBACK-EXECUTOR-002 Validated Example Matrix

All seven rows inherit REQ-MANAGED-ROLLBACK-EXECUTOR-002, `@non-functional-requirement`, and `@implemented-and-validated` from one repository-unique `Scenario Outline`. The focused source-built/in-process runner passed every row; the work counts and typed outcomes below are bounded acceptance evidence, while named surface adoption is source-inspected.

[cols="1,2,3,3",options="header"]
|===
|Example / surface |Initial condition and visibility |Validated work boundary |Validated typed outcome and evidence

|1 — any rollback surface
|Upper-case raw transaction ID; journal must not be accessed; caller-selected visibility; guard must not run.
|`parseOrNull=1`; outer `loadRecord=0`; guard=0; real rollback=0; workspace lock=0.
|`InvalidTransactionId` carries the exact unnormalized raw ID; no parsed ID or resolved record.

|2 — CLI
|No ID-matching record; `APPLIED_ONLY`; guard must not run.
|Parse once; one outer pre-lock lookup; no guard, rollback, or lock.
|`TransactionNotFound` carries the parsed ID and `APPLIED_ONLY`; no resolved record.

|3 — CLI, LSP, and MCP
|One complete ID-matching `ROLLED_BACK` record; `APPLIED_ONLY`; guard must not run.
|Parse and outer lookup once; visibility hides the record; no guard, rollback, or lock.
|`TransactionNotFound` carries the parsed ID and `APPLIED_ONLY`; hidden non-`APPLIED` record is not exposed.

|4 — daemon
|One complete ID-matching `ROLLED_BACK` record; `JOURNAL_RECORD`; allow guard.
|Parse, outer lookup, guard, and real rollback once; engine lock reached.
|`Refused` carries the exact resolved record and a detached unmodifiable snapshot of the ordered `transaction.notApplied` baseline; mutating the retained source list after `execute` changes the source but not the outcome, and mutation through the outcome list is rejected. The executor neither reloads nor reconstructs the record.

|5 — LSP
|One complete `APPLIED` record; `APPLIED_ONLY`; unrelated dirty document and affected open path both exist.
|Parse, lookup, visibility, and one pre-lock guard; dirty-document-first rejection; no rollback or lock.
|`PreflightRejected` carries the same resolved record and unchanged `DOCUMENT_VERSION_MISMATCH` payload.

|6 — daemon lookup
|Outer `loadRecord` throws exact `TransactionLogException E_lookup`; `JOURNAL_RECORD`; guard must not run.
|Parse and one outer pre-lock lookup; no guard, rollback, or lock.
|`JournalLookupFailed` carries parsed ID and exact code/message/cause; no resolved record.

|7 — daemon rollback call
|Outer lookup resolves one complete `APPLIED` record; under-lock engine journal access throws exact `TransactionLogException E_call`; `JOURNAL_RECORD`; allow guard.
|Parse, outer lookup, guard, real rollback, and engine lock each reached once.
|`RollbackCallJournalFailed` carries the same resolved record and exact code/message/cause.
|===

Visible absence, preflight rejection, engine refusal, lookup journal failure, and rollback-call journal failure remain distinct. Every row excludes response projection, session lifecycle, refresh, and diagnostics policy from the executor.

=== Reconciled Gherkin Totals

[cols="3,1,5",options="header"]
|===
|Metric |Total |Reconciliation

|Configured project feature roots
|1
|`features/`.

|Feature documents / discovered `.feature` paths
|8
|`features/authoritative-diagnostics-evaluation.feature`, `features/java-maven-module-rename.feature`, `features/java-maven-move-class-apply-authority.feature`, `features/java-refactoring-preview-command-boundary.feature`, `features/managed-apply-diagnostics-gate-selector.feature`, `features/managed-rollback-executor-extraction.feature`, `features/pending-plan-store-extraction.feature`, and `features/workspace-snapshot-composer-extraction.feature`.

|Requirement-bearing definitions
|32
|Twenty-four ordinary `Scenario` declarations (two authoritative diagnostics-evaluation, one Maven module-rename, ten Maven move-class, three Java preview-command, one managed-apply selector, one managed rollback, two pending-plan, and four Workspace Snapshot Composer scenarios) plus eight `Scenario Outline` declarations (authoritative diagnostics-evaluation `002`/`003`, Maven move-class `008`/`012`, Java preview-command `001`, managed-apply selector `001`, managed rollback `002`, and Workspace Snapshot Composer `004`); the one Maven move-class `Background` is excluded.

|Scenario Outline definitions
|8
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-002, REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-003, REQ-JAVA-MAVEN-MOVE-AUTH-008, REQ-JAVA-MAVEN-MOVE-AUTH-012, REQ-JAVA-PREVIEW-COMMAND-001, REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001, REQ-MANAGED-ROLLBACK-EXECUTOR-002, and REQ-WORKSPACE-SNAPSHOT-COMPOSER-004.

|Scenario Outline example rows
|52
|Twelve provider-authority rows under authoritative diagnostics `002`, two post-apply-mismatch rows under authoritative diagnostics `003`, six uncertainty/drift rows under Maven `008`, six selected-descriptor loss/drift rows under Maven `012`, five command rows under Java preview-command `001`, ten routing rows under managed-apply selector `001`, seven short-circuit/visibility rows under managed rollback `002`, and four collaborator-failure rows under Workspace Snapshot Composer `004`.

|Example-expanded executable cases
|76
|Twenty-four ordinary scenarios plus 52 rows inherited from the eight outlines.

|Background declarations
|1
|The Maven move-class feature contains the sole `Background`.

|Background steps
|5
|The sole background contributes five steps to each Maven move-class pickle but is excluded from declared scenario-step totals.

|Declared scenario steps
|479
|The four authoritative diagnostics-evaluation definitions declare 43 scenario steps, the Maven module-rename definition declares 50, the twelve Maven move-class definitions declare 241, the four Java preview-command definitions declare 46, the two managed-apply selector definitions declare 20, the two managed-rollback definitions declare 19, the two pending-plan definitions declare 19, and the five Workspace Snapshot Composer definitions declare 41; the separate five-step Maven move-class `Background` is not included in this metric.

|Expanded executable case steps
|1,016
|Gherkin pickles include inherited background steps and example expansion: 128 steps for the 16 authoritative diagnostics-evaluation cases, 50 for the one Maven module-rename case, 501 for the 22 Maven move-class cases, 94 for the eight Java preview-command cases, 92 for the 11 managed-apply selector cases, 67 for the eight managed-rollback cases, 19 for the two pending-plan cases, and 65 for the eight Workspace Snapshot Composer cases.

|Repository-unique stable requirement IDs in current Gherkin
|32
|Exactly one `@REQ-*` ID occurs on each requirement-bearing definition. Example rows intentionally inherit their outline's one ID, and no ID labels another definition.

|Functional-only requirement definitions
|6
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`, REQ-JAVA-MAVEN-MODULE-RENAME-001, and REQ-JAVA-MAVEN-MOVE-AUTH-002.

|Non-functional-only requirement definitions
|18
|REQ-JAVA-MAVEN-MOVE-AUTH-004, REQ-JAVA-MAVEN-MOVE-AUTH-005, REQ-JAVA-MAVEN-MOVE-AUTH-008, REQ-JAVA-PREVIEW-COMMAND-001 through `004`, REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002, REQ-MANAGED-ROLLBACK-EXECUTOR-001/002, REQ-PENDING-PLAN-STORE-001/002, and REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`.

|Dual-classified requirement definitions
|8
|REQ-JAVA-MAVEN-MOVE-AUTH-001, REQ-JAVA-MAVEN-MOVE-AUTH-003, REQ-JAVA-MAVEN-MOVE-AUTH-006, REQ-JAVA-MAVEN-MOVE-AUTH-007, REQ-JAVA-MAVEN-MOVE-AUTH-009, REQ-JAVA-MAVEN-MOVE-AUTH-010, REQ-JAVA-MAVEN-MOVE-AUTH-011, and REQ-JAVA-MAVEN-MOVE-AUTH-012.

|Definitions carrying `@functional-requirement`
|14
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`, REQ-JAVA-MAVEN-MODULE-RENAME-001, REQ-JAVA-MAVEN-MOVE-AUTH-001, REQ-JAVA-MAVEN-MOVE-AUTH-002, REQ-JAVA-MAVEN-MOVE-AUTH-003, REQ-JAVA-MAVEN-MOVE-AUTH-006, REQ-JAVA-MAVEN-MOVE-AUTH-007, REQ-JAVA-MAVEN-MOVE-AUTH-009, REQ-JAVA-MAVEN-MOVE-AUTH-010, REQ-JAVA-MAVEN-MOVE-AUTH-011, and REQ-JAVA-MAVEN-MOVE-AUTH-012.

|Definitions carrying `@non-functional-requirement`
|26
|All requirement IDs except REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`, REQ-JAVA-MAVEN-MODULE-RENAME-001, and REQ-JAVA-MAVEN-MOVE-AUTH-002. Classification totals overlap for the eight dual-classified definitions.

|Definitions with effective `@absent`
|0
|None.

|Example-expanded cases with effective `@absent`
|0
|None.

|Definitions with effective `@implemented-and-validated`
|32
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004`, REQ-JAVA-MAVEN-MODULE-RENAME-001, REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`, REQ-JAVA-PREVIEW-COMMAND-001 through `004`, REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002, REQ-MANAGED-ROLLBACK-EXECUTOR-001/002, REQ-PENDING-PLAN-STORE-001/002, and REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`. Every definition retains separately bounded evidence.

|Example-expanded cases with effective `@implemented-and-validated`
|76
|All twenty-four ordinary scenarios plus all 52 cases inherited from the eight outlines.

|Mappings to `UC-AUTHORITATIVE-DIAGNOSTICS-EVALUATION`, with diagnostics/apply/rollback supporting mappings
|4
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` and all 16 functional implemented-and-validated cases map to the bounded language-neutral authoritative evaluation capability plus existing diagnostics, managed-apply, and rollback use cases. The mapping adds no protocol capability, durable persistence, generic operation, or provider authority.

|Mappings to `UC-JAVA-RENAME-MAVEN-MODULE`, with preview/apply/rollback supporting mappings
|1
|REQ-JAVA-MAVEN-MODULE-RENAME-001 and its one functional implemented-and-validated case map to the bounded direct-library module-rename use case plus existing preview, managed-apply, and rollback capabilities. The mapping adds no protocol capability or broader support claim.

|Mappings to `UC-JAVA-MOVE-CLASS`
|12
|Every Maven move-class requirement-bearing definition, and therefore all 22 Maven executable cases, maps to this use case.

|Managed-rollback-executor mappings to `UC-ROLLBACK`
|2
|REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 and their eight implemented-and-validated cases map to supporting orchestration within the existing rollback use case; they add no protocol capability or mutation authority.

|Managed-apply-selector mappings to UC-APPLY-PATCH, UC-DAEMON, and UC-MCP
|2
|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 and their 11 implemented-and-validated cases map to supporting gate selection within the existing managed-apply integration paths. The mapping adds no protocol capability or apply authority.

|Mappings to UC-DAEMON, UC-LSP, and UC-MCP
|2
|REQ-PENDING-PLAN-STORE-001/002 and their two cases map to supporting retention behavior within all three existing integration use cases; they do not create a protocol capability.

|Mappings to UC-SCAN, UC-DAEMON, and UC-MCP
|5
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` and their eight implemented-and-validated cases map to supporting implemented composition inside the existing scan/daemon/MCP use cases. They add no user-visible capability, protocol, persistence, or mutation authority.

|Mappings to UC-PREVIEW-REFACTOR, with UC-DAEMON/UC-MCP for surface adoption
|4
|REQ-JAVA-PREVIEW-COMMAND-001 through `004` and their eight implemented-and-validated cases map to the existing preview use case; REQ-003 additionally maps to bounded daemon/MCP public-session dispatch. They add no external protocol capability, persistence, or mutation authority.

|Distinct `RPK-JAVA-MOVE-*` slices covered
|7
|The union of all twelve Gherkin requirement mappings is RPK-JAVA-MOVE-001 through RPK-JAVA-MOVE-007 and conforms to accepted ADR 0012's target-scoped authority, declaration-scope, descriptor-closed selected-graph, ordinary selected-missing-binary, and fail-closed selected-descriptor refinements.

|Additional validation modes outside Gherkin counts
|1
|The dedicated packaged-process Cucumber mode reuses existing REQ-JAVA-MAVEN-MOVE-AUTH-001 and its same pickle. It adds zero feature paths, definitions, IDs, cases, classifications, steps, or declared statuses. One local Linux `amd64` execution is implementation-informed; its four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`.

|Observed implemented-and-validated requirement definitions
|27
|Java Refactoring Preview Command adds four observed definitions/eight cases: corrected split runners passed seven Java-module cases/85 steps and one real public daemon/MCP session case/9 steps, all 94 steps, with zero failures/errors (log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5`); Java/daemon/MCP regressions passed with zero failures/errors (log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`). Final dated mutable source-built whole-tree verification passed with 1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, and 79 tasks (log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`); the targeted affected-module static command passed with the reviewed non-zero 48-glue-finding classification (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Workspace Snapshot Composer adds five observed definitions/eight cases: the production composer passed final focused 8/8-scenario, 65/65-step source-built/in-process acceptance, while REQ-005 adoption remains source-inspected plus daemon 58/58 and MCP 26/26 regressions. Final dated mutable whole-working-tree source-built aggregate verification passed with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption; static findings are non-zero, with 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` remaining non-blocking (6 NP, 3 RCN, 12 BC), while the broader configured baseline remains non-blocking. This does not qualify packaged/native/cross-platform/concurrent/crash-restart/numerical-coverage/general orchestration. REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` passed only bounded tagged rows. `008` and `012` each expand to six cases. Focused `008` evidence passed six selected cases, skipped sixteen others, and had zero failures/errors. The historical REQ-008-specific independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. REQ-PENDING-PLAN-STORE-001/002 passed both focused source-built/in-process scenarios and all 19 steps with 29 executed Gradle tasks and zero failures/errors; log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`. The historical PendingPlanStore-scoped whole-working-tree verification on 2026-07-31 used the same OpenJDK and Gradle command: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`; the non-blocking SpotBugs baseline had no `PendingPlanStore` findings. A focused RED first proved the REQ-MANAGED-ROLLBACK-EXECUTOR-002 mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`). After detached unmodifiable ordered snapshotting, REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 passed 8/8 source-built/in-process scenarios and 67/67 expanded steps with 29 tasks and zero failures/errors/skips/undefined/ambiguous/pending; final focused log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`. The historical pre-selector whole-working-tree source-built verification after the managed-rollback correction used OpenJDK 21.0.11 and the same Gradle command: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings remain non-zero but reviewed/non-blocking: seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings); 25 glue-only Kotlin/lifecycle/generated warnings remain (13 NP, 1 RCN, 5 MS, 6 BC); the former three `Refused`-diagnostics EI/EI2 findings are eliminated. The REQ-008- and PendingPlanStore-specific whole-working-tree receipts remain historical evidence for their explicit scopes. These are dated mutable source-built execution totals, not additional Gherkin definitions/cases or packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority qualification. Maven, pending-plan, managed-rollback, and Magrathea evidence remain separately bounded; managed-rollback surface adoption is inspected only. REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 add two observed definitions and 11 cases: final independent OpenJDK 21.0.11 source-built/in-process acceptance passed 11/11 scenarios and 92/92 steps with zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `74749cf8635d7bbbac3abb4a3eeaf669f6aab3d58770e423094ce791c7882997`), while daemon/MCP regressions passed 58/58 and 26/26. Final whole-working-tree source-built verification used the same Gradle command: `BUILD SUCCESSFUL` in 7m 11s; 1021 discovered, 801 executed, 220 skipped/tag-filtered, zero failures/errors; 78 tasks; log SHA-256 `e3e4b3e02fac87003f12cbf87801298653eb12e43c213d1f40fe5177ece75e02`. The selector's initial production NP finding is eliminated and no selector/provider-function production finding remains in the final log; 23 glue-only Kotlin/generated findings remain non-blocking (2 RCN, 13 MS, 1 SA, 7 BC), with the broader configured SpotBugs baseline still non-blocking. This is not packaged/native/cross-platform/concurrent/apply-authority/numerical-coverage evidence.
|===

== Validation Modes

[cols="1,3",options="header"]
|===
|Mode |Meaning

|Documentation evidence
|Behavior is described in repository docs or `AGENTS.md`.

|Source evidence
|Behavior appears in module source files.

|Unit test evidence
|Behavior has module tests.

|Golden test evidence
|Behavior has a `testdata/golden` case.

|CI evidence
|Behavior is exercised by `.github/workflows/ci.yml`.

|Domain research
|A reviewed requirement or concept informs planned architecture but is not repository implementation or executable evidence.

|Field report
|A dated external observation motivates priority and qualification; it does not replace a permanent fixture or prove general support.

|Gherkin source reconciliation
|Feature discovery, stable IDs, classifications, effective statuses, paths, totals, and architecture/use-case mappings agree with the `.feature` source. This validates documentation consistency only, not behavior or implementation.

|Tagged Cucumber evidence
|A runner filtered by a stable requirement tag executes the selected production scenario. Passing steps support only that scenario and exercised surface; scenarios reported skipped because of the filter remain unvalidated.

|Packaged-process Cucumber evidence
|A dedicated runner executes an existing tagged pickle through the real self-contained launcher and embedded runtime as child OS processes. It is a validation mode, not a new requirement/case. The REQ-001 mode is implementation-informed from one local Linux `amd64` mutable-tree observation; each post-commit native CI row remains independently `CONFIGURED_UNOBSERVED` until its exact reviewed receipt exists. Source-built, generic package, static-review, or local evidence cannot promote those ledger rows.
|===

== Open Requirements Gaps

[cols="1,3,1",options="header"]
|===
|Gap |Impact |Status

|Executable Story BDD coverage remains narrow
|The configured root has eight feature sources and 32 definitions/76 cases, all `@implemented-and-validated`. The newly promoted authoritative diagnostics-evaluation definitions contribute 4/16 and the module-rename definition contributes 1/1; every feature remains complete only for its separately bounded catalogue. Most other use cases still have no mapped Gherkin scenarios.
|needs-review; status reconciliation is complete, while broad use-case coverage and every stated qualification exclusion remain open

|Authoritative snapshot diagnostics evaluation qualification is bounded
|REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` provide four repository-unique functional implemented-and-validated definitions and 16 cases at `features/authoritative-diagnostics-evaluation.feature`. Independent 16/16-scenario, 128/128-step evidence validates the exact public `PatchEngine` boundary, deep detachment, core-owned pre-WAL authority, S0/C1/S1 journaling, same-record automatic rollback, and diagnostics-only compatibility. It does not qualify arbitrary model-changing operations, protocols, packages/platforms, releases, or support.
|mitigated for the exact bounded application-port mode; retain regression evidence and all broader exclusions

|Bounded Maven module rename qualification is bounded
|REQ-JAVA-MAVEN-MODULE-RENAME-001 has one repository-unique functional implemented-and-validated scenario/case and 50 steps at `features/java-maven-module-rename.feature`. Its direct-library boundary covers only the named 20-direct-child fixture shape, independently explicit directory/artifact intent, exactly three parser-authorized POM ranges plus two file renames, separately authorized `PatchEngine` apply, one exact-S1 journal record, and exact normal rollback. Protocol/recipe/package/native/cross-platform/real-project/general-Maven/native-CI/support and broad-roadmap completion remain excluded.
|mitigated for the exact direct-library fixture row; broader qualification remains open and the broad roadmap/support rows stay unpromoted

|Java Refactoring Preview Command qualification and final gate are bounded
|REQ-JAVA-PREVIEW-COMMAND-001 through `004` validate the sealed five-variant values, fieldless dispatcher, per-call invoker seam, exact default-planner mapping, generic decoder convergence, real daemon/MCP session dispatch after parsing, exact plan/exception fidelity, no writes/residue, and all named exclusions through bounded source-built/in-process runners. External protocol schemas/capability catalogues did not change. Final dated mutable whole-working-tree verification passed with 1045 discovered/817 executed/228 skipped or tag-filtered, zero failures/errors, 140 XML files, and 79 tasks (SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`); targeted static verification passed (SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`) with no finding owned by the named production boundary/adoption paths and exactly 48 reviewed/non-blocking glue findings. No packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage execution is qualified.
|mitigated only for the exact four-definition/eight-case/94-step source-built/in-process slice, module regressions, dated mutable source-built aggregate, and reviewed non-zero static classification; not closed globally, and broader execution qualification remains open

|Workspace Snapshot Composer qualification is bounded
|REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005` define the exact empty-base identity, bounded overlay, collaborator ordering/isolation, unchanged failure/no-side-effect boundary, daemon/MCP adoption points, and ownership exclusions. The production composer and final focused runner validate all five definitions/eight cases/65 expanded steps in process. Adoption remains source-inspected only in private daemon/MCP `scanWorkspace`, supported by 58/58 and 26/26 module regressions. The final dated mutable whole-working-tree source-built aggregate passed with 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, and 78 tasks (log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`). It reports no production finding attributable to the composer or daemon/MCP adoption, while 21 slice-local glue-only Kotlin/generated findings remain non-blocking (6 NP, 3 RCN, 12 BC) and the broader configured baseline remains non-blocking. CLI/LSP and lifecycle/index/`PatchEngine`/protocol ownership remain excluded. No packaged, native, cross-platform, concurrent, crash-restart, numerical-coverage, or general-orchestration behavior is qualified.
|mitigated for the bounded source-built/in-process composer contract, module regressions, dated source-built aggregate, and reviewed non-blocking static findings; broader execution, concurrency, crash-restart, coverage, and orchestration qualification remains open

|Managed-apply diagnostics-gate selector qualification is bounded
|REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002 validate the seven-row route table, explicit non-null final-segment behavior, laziness, per-call adapter identity, external lookup timing/identity, unchanged `PatchEngine` authority, and excluded surfaces through source-built/in-process selector acceptance; source inspection confirms daemon/MCP adoption, both module regression suites passed, and the dated whole-working-tree source-built aggregate passed. The initial selector production NP finding is eliminated, no selector/provider-function production finding remains, and the 23 glue-only Kotlin/generated findings remain non-blocking. The evidence does not exercise packaged/native/cross-platform transports, concurrent apply, CLI/LSP/recipes/testkit/direct-library adoption, or apply authority, and test totals are not numerical coverage.
|mitigated for source-built/in-process selector behavior, daemon/MCP regression, dated source-built aggregate, and reviewed non-blocking static findings; broader execution and authority qualification remains open

|Managed rollback executor qualification is bounded
|REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 pass one positive case and seven compatibility/failure cases through the final source-built/in-process runner, and the historical rollback-scoped whole-working-tree source-built gate also passed; source inspection confirms CLI/daemon/LSP/MCP adoption. RED evidence proved mutable diagnostic aliasing, and detached unmodifiable ordered snapshotting eliminated its former three `Refused` EI/EI2 findings. Static findings are not zero: seven intentionally reviewed/non-blocking core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 non-blocking glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). The aggregate gate does not become packaged or cross-platform surface evidence, and no concurrent rollback, crash/restart, general rollback authority, numerical coverage, or `PatchEngine` authority change is qualified.
|mitigated for the bounded source-built/in-process slice; reviewed static findings remain intentionally non-blocking; needs-review before any packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or broader surface-execution claim

|Pending-plan extraction qualification is bounded
|The focused runner calls source-built classes and real daemon/LSP/MCP sessions in process. It does not exercise packaged executables/transports, concurrent callers, long-running service races, or a reusable general service-extraction pattern; lifecycle policy remains surface-owned and no protocol/authority change is claimed.
|needs-review before any packaged, concurrency, or generalized service-extraction claim

|No explicit use-case metadata files
|Use-case IDs in this appendix are derived from documentation and module evidence.
|needs-review

|No complete confirmed stakeholder/business priority set
|Maven move-class apply authority is now explicitly P0, but the rest of the functional catalogue may not reflect complete product priorities.
|needs-review

|General target-scoped Maven move-class managed apply remains unaccepted beyond bounded rows
|The permanent fixture has twelve bounded validated definitions and 22 cases, including six-case REQ-008. A second packaged-process validation mode is implemented and observed locally only for existing REQ-001 on Linux `amd64`; it does not cover REQ-002..012 or create a requirement/case. The official final packaged task, separately accounted source-built whole-tree gate, and targeted custom-source-set static tasks passed. Static classification is intentionally non-zero at 99 reviewed/non-blocking packaged test/deployment glue/harness findings; no production source was added or modified by this slice, and aggregate cleanliness plus numerical/child-JVM coverage are not claimed. All four post-commit native CI rows remain `CONFIGURED_UNOBSERVED`; their wiring/report uploads are implemented but unexecuted, and Windows/macOS logic is configured/static-reviewed only. The exact Magrathea `a48e3c06` request remains CLI/Linux/JDK-21-specific; no packaged/runtime integration matrix, native matrix, or general field population is qualified.
|planned, P0 product-critical generalization blocker (`RPK-JAVA-MOVE-001..007`); twelve bounded definitions/22 cases, one exact CLI field request, and one local Linux `amd64` REQ-001 package mode closed only for their evidence; native ledger configured-unobserved

|Authoritative Java/Maven diagnostic evidence is incomplete
|Global Java mutation cannot claim definitive source-set authority until every affected environment has exact full-reactor/classpath/platform/provider evidence and `RPK-JAVA-DIAG-001..007` packaged acceptance. The narrower Maven `moveClass` mode has bounded REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012` evidence and one exact Magrathea CLI/Linux/JDK-21 field result, but still lacks general, packaged/runtime integration, and native-matrix authority. Maven success, caller classpath attestation, executable Maven, implicit network, or fixture-only results are not substitutes.
|planned, product-critical release blocker

|Transaction semantics exceed implementation evidence
|The transactionality audit narrows remaining gaps to metadata/journal hardening, and destructive fault evidence. Managed writes and recipes now have WAL-backed staged boundaries, but one filesystem-wide ACID guarantee is not claimed.
|release blocker; requirements need confirmation/narrowing
|===
