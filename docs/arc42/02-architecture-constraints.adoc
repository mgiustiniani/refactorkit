= 2. Architecture Constraints
:toc:
:sectnums:

Completion-program status: confirmed `v0.7.x` constraints are active. The local P0 all-module `testClasses` admission gate is resolved by a scaffolding-only working-tree correction; corrected-revision CI and every independently unqualified K5, T5, J1, and I1 row remain open. Existing evidence below remains intact and bounded.

Status: implementation-informed, with a confirmed P0 target-scoped Maven move-class authority priority; needs-review for remaining general authority and qualification. All thirteen bounded Maven move-class requirement definitions and all 31 expanded cases are implemented and validated. REQ-JAVA-MAVEN-MOVE-AUTH-001/002 retain complete-`AVAILABLE` and exact observer/candidate/residual evidence; `003`/`004` retain distinct immutable guidance and lexical-review boundaries; `005`-`007` and `009`-`012` retain their independently bounded refusal, warning, missing-binary, scope, selector, ordinary-absence, and selected-descriptor evidence. Six-case `008` validates four immutable schema-v1 `REVIEW_ONLY_GUIDANCE` results, one result-owned structural active-reactor-POM refusal, and one under-lock required-file-evidence drift refusal before WAL. Nine-case `013` validates complete/edit-exact authority under lock before WAL. The implementation uses a dispatcher second stage for retained-diagnostic identity drift, generic immutable `OperationAuthorityFileEvidence` and `DiagnosticDetails`, and one engine-owned observed snapshot under lock. Its semantic candidate-inventory hash is expected-only; expected and observed identities belong to required-file evidence. Focused OpenJDK 21 evidence selected/executed/passed six of 22 cases, skipped sixteen by tag filter, and had zero failures/errors. Independent final mutable-working-tree verification on OpenJDK 21.0.11 used command `./gradlew --no-daemon --rerun-tasks check goldenTest`; it reported `BUILD SUCCESSFUL` in 7m 33s, with 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors, and 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. This historical execution receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking. The exact Magrathea request at `a48e3c06` remains CLI/Linux/JDK-21-specific. Separately, exact revision-bound run `31336067318` passed public REQ-001 and the complete 13-ID/31-case production-JAR matrix on four hosts. Every native row remains `PASSED`; general authority and a green whole-repository CI claim remain open.

The behavior-preserving bounded pending-plan store extraction is now implementation-informed in the mutable working tree based on `f587a707d7491974e4f55e4f61371a2e30648468`. The production implementation is `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`, adopted by `modules/refactorkit-daemon/src/main/kotlin/org/refactorkit/daemon/DaemonSession.kt`, `modules/refactorkit-lsp/src/main/kotlin/org/refactorkit/lsp/LspSession.kt`, and `modules/refactorkit-mcp/src/main/kotlin/org/refactorkit/mcp/McpSession.kt`. Focused source-built/in-process OpenJDK 21.0.11 acceptance passed two scenarios and 19 steps with 29 Gradle tasks and zero failures/errors (log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`). Subsequent independent whole-working-tree verification on 2026-07-31 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 23s; 1002 discovered, 782 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`. SpotBugs retained its configured non-blocking baseline and reported no `PendingPlanStore` findings. This later PendingPlanStore-scoped receipt is dated mutable source-built aggregate evidence; it does not qualify packaged behavior, concurrency, a general extraction pattern, or numerical coverage. Surface lifecycle policy remains separate, and no protocol or authority contract changed.

The Java Refactoring Preview Command boundary is now implementation-informed in the current mutable working tree. `JavaRefactoringPreviewCommand` is a sealed five-variant value hierarchy; the fieldless `JavaRefactoringPreviewDispatcher` delegates through an internal per-call `JavaRefactoringPreviewPlannerInvoker` seam to the unchanged default planners. The five generic Java adapter decoders converge on that path, and daemon/MCP adopt only those five routes after their existing parsing. Independent OpenJDK 21.0.11 focused acceptance passed all four definitions/eight cases/94 steps, module regressions passed with zero failures/errors, and the six-feature audit reconciled 27 definitions/59 cases/386 declared steps/838 expanded steps. Final dated mutable whole-working-tree source-built verification on 2026-08-01 used OpenJDK 21.0.11 and `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 6m 54s; 1045 tests discovered, 817 executed, 228 skipped/tag-filtered, zero failures/errors; 140 XML files; 79 tasks executed; log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`. The targeted affected-module static command passed (log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`). Static classification is deliberately non-zero: the full targeted log has no finding owned by `JavaRefactoringPreviewCommand`, `JavaRefactoringPreviewDispatcher`, `ExistingJavaRefactoringPreviewPlannerInvoker`, daemon `refactorPreview`, MCP `toolPreviewRefactoring`, or the five changed generic decoder helpers; exactly 48 slice-local glue findings remain reviewed/non-blocking—Java-module command glue 24 (15 NP, 6 RCN, 1 Dm, 2 BC) and CLI real-surface glue 24 (13 NP, 7 RCN, 4 BC). Finding text that references dispatcher methods comes from generated Kotlin nullability analysis at glue call sites, not production methods. The wider configured SpotBugs/PMD baselines remain non-blocking; neither aggregate static cleanliness nor numerical coverage is claimed. This final receipt is dated mutable source-built aggregate evidence complementing the focused source-built/in-process acceptance; it does not qualify packaged, native, cross-platform, concurrent, crash-restart, general-orchestration, or numerical-coverage behavior.

== Technical Constraints

[cols="1,3,2,1",options="header"]
|===
|ID |Constraint |Evidence |Status

|C-TECH-001
|The main implementation language is Kotlin/JVM.
|Gradle Kotlin modules and source layout.
|implementation-informed

|C-TECH-002
|Java bytecode compatibility is configured for Java 8 where possible.
|Root `build.gradle.kts` configures Java compile release 8 and Kotlin JVM target 1.8.
|implementation-informed

|C-TECH-003
|The build itself uses a JDK 21 toolchain for Kotlin/JVM modules and CI.
|Module build scripts use `jvmToolchain(21)`; CI uses Temurin 21.
|implementation-informed

|C-TECH-004
|Core engine must stay language-independent and must not depend on Java-specific logic.
|`refactorkit-core` contains `LanguageAdapter`; Java-specific planners live in `refactorkit-java`.
|implementation-informed

|C-TECH-005
|Every mutating operation must produce a previewable `PatchPlan` before application.
|`AGENTS.md`; patch model and planner classes.
|implementation-informed

|C-TECH-006
|Patch application must reject stale snapshot hashes, overlapping edits, paths outside the workspace root, and any symbolic-link component below the workspace root. Before its first write, it must simulate ordered file existence/type transitions so predictable missing-file or existing-file refusals cannot leave earlier edits applied.
|`PatchEngine.validate`, `PatchEngine.validateEdits`, and `PatchEngine.validateRuntimeState`; ADR 0002; core tests.
|implementation-informed; interrupted I/O and TOCTOU safety remain open

|C-TECH-007
|Current managed apply returns compensation only after all sequential writes succeed, and integrations then store transaction logs under `.refactorkit/transactions`. This is a preflighted compensatable batch, not an ACID, crash-safe, durable, or isolated transaction.
|`TransactionLog`, ADR 0002, `docs/transactionality-audit.md`.
|implementation-informed limitation

|C-TECH-008
|External Java class imports must remain preview-based, warn about unknown license/provenance, and avoid overwriting by default.
|`docs/external-class-importer.md`, `ExternalJavaClassImporter`.
|implementation-informed

|C-TECH-009
|The CLI distribution must be able to include an embedded Java runtime created with `jlink`.
|`docs/packaging.md`, CLI Gradle packaging tasks.
|implementation-informed

|C-TECH-010
|The stdio daemon must expose read-only capability discovery before a project is opened. Discovery reports protocol and transport identity, method stability and project/write requirements, and the safety properties clients may rely on.
|`DaemonSession.serverCapabilities`, `DaemonSessionTest`, `docs/integration-contracts.md`.
|implementation-informed

|C-TECH-011
|Stable Java source support must use an Eclipse JDT version and explicit project compliance configuration proven on representative Java 8, 11, 17, 21, and 25 constructs. Java 8-compatible RefactorKit bytecode is a separate constraint and is not source-support evidence.
|`docs/releases/v1.0.0-plan.md`; ADRs 0001 and 0008.
|confirmed target; planned acceptance

|C-TECH-012
|A stable Java operation must classify its evidence as JDT binding-backed, narrowly proven lexical behavior, or refusal. Missing or unclean semantic evidence must never become hidden lexical semantic authority.
|`docs/releases/v1.0.0-plan.md`; ADRs 0003, 0007, and 0008.
|confirmed target; planned acceptance

|C-TECH-013
|Stable diagnostics and validation must provide deterministic severity, source range, evidence, and error categories and must refuse configured regressions beyond the approved plan.
|`docs/releases/v1.0.0-plan.md`; ADRs 0002 and 0008.
|confirmed target; planned acceptance

|C-TECH-014
|Any stable transactionality claim must define and evidence its mutation boundary, filesystem capability/degradation, write-ahead recovery lifecycle, one-writer isolation, affected-file preconditions, conflict-safe compensation, journal security/integrity, complete edit rendering, diagnostics gates, approval semantics, and recovery/error states. Until then, documentation must use the narrower compensatable-batch classification.
|ADRs 0002 and 0004; `docs/transactionality-audit.md` (`TX-001` through `TX-018`).
|planned release constraint; needs-review

|C-TECH-015
|RefactorKit is the definitive semantic diagnostics and refactoring authority for managed Java operations. Maven compile/test is supplementary evidence only and must never excuse a false RefactorKit diagnostic. Global source-set authority still requires every affected environment to be available and reports incomplete inputs as typed unavailability. Only the qualified target-scoped Maven `moveClass` mode in ADR 0012 may admit enumerated leaf external `OFFLINE_MISSING` inputs, without hiding the global unavailable state and only after its stronger operation-specific proof passes.
|ADR 0012; `RPK-JAVA-DIAG-001..007`; `RPK-JAVA-MOVE-001..004`; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001/007/011/012 evidence.
|confirmed architecture; bounded target-scoped rows implementation-informed, global authority planned and product-critical

|C-TECH-016
|The caller-declared full Maven reactor root remains workspace and effective-model authority. Module targeting filters results only after full-reactor model, dependency-closure, and impact-closure construction. No implicit ancestor, sibling, upward, or outward discovery may widen that workspace.
|ADR 0012; `RPK-JAVA-DIAG-002`.
|confirmed architecture; planned

|C-TECH-017
|Exact Maven `--release` diagnostics require immutable, integrity-verified, hash-attested, licensed, and SBOM-covered release-aware Java platform signatures. Source compliance, the host JDK, and the reduced running jlink image are insufficient. A no-release/source-only project must name a qualified JDK platform or receive typed unavailability.
|ADR 0012; `RPK-JAVA-DIAG-001`.
|confirmed architecture; planned, product-critical

|C-TECH-018
|Only safely declared, already materialized generated sources may enter analysis. Discovery must not implicitly execute Maven or wrappers, lifecycle goals, plugins, annotation processors, credential helpers, Maven settings credentials, or other build code. Unnamed/classpath Maven reactors are qualified first; named JPMS/module-path reactors remain typed unsupported until a complete dedicated slice is accepted.
|ADRs 0010-0012; `RPK-JAVA-DIAG-006..007`.
|confirmed architecture; planned

|C-TECH-019
|Managed same-module Maven `moveClass` may use target-scoped authority only when full-reactor structural evidence remains complete, current, unambiguous, and hash-bound. Every active reactor POM is mandatory. For every active module's main/test projection, external traversal is descriptor-closed over selected nodes: each selected external node has complete effective POM/parent/BOM/management/mediation/relocation inputs, and every outgoing declaration has a hash-bound `SELECTED` or exact scope/optional/exclusion-pruned classification. A deterministically pruned external edge contributes no child node or subtree and requires no child POM/JAR on that path; an alternate selected path restores those requirements. Missing descriptor facts on an included edge or any ambiguity that can change observer inclusion/exclusion blocks. Module/source-set ownership, source/materialized-generated-root inventory, full-reactor candidate audit/exclusion, Java platform, and provider facts also remain global. Per-missing-binary negative-presence evidence is required only for the before/staged operation authority closure; outside-closure `OFFLINE_MISSING` remains visible but needs no operation inventory after exclusion is proved.
|ADRs 0010 and 0012; `RPK-JAVA-MOVE-002`; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001/002/007/008/009/010/011/012 evidence; dated Magrathea field evidence at `a48e3c06`.
|confirmed P0 architecture; thirteen bounded definitions/31 cases including complete/edit-exact admission are implementation-informed; the exact Magrathea CLI field request remains Linux/JDK-21-specific; exact four-host public/complete package evidence passed, while general integration remains planned; every bounded native row passed independent review

|C-TECH-020
|For target-scoped Maven `moveClass`, an independent bounded lexical Java inventory is a completeness veto only. Every old-FQN or matching simple-name candidate in a type-capable Java position must be hash-bound and classified exactly once by JDT as `BOUND_TARGET`, `BOUND_OTHER`, or `UNRESOLVED`; every target-relevant or relevance-uncertain `UNRESOLVED` candidate blocks. Only exact `BOUND_TARGET` ranges may select edits. Binding totals must exclude Java grammar-noncode strings and non-Java text: those are reported separately as residual review risks with `managedEdit=false`. In implemented-and-validated ordinary missing-binary row `011`, the exact selected source declaration plus Java target-name lookup protection covers every target occurrence: either an exact non-static single-type import of the target FQN protects a simple type use, or the use is the exact target FQN. Unprotected simple names, relevant type/static wildcard or unresolved-static ambiguity, recovered/problem evidence, candidate/diagnostic/edit overlap, changed retained-diagnostic identity, new or changed errors, or evidence drift block. Retained baseline diagnostics require exact unchanged mapped before/staged identity and positive non-concealment proof; staged analysis preserves `BOUND_OTHER` and resolves the moved target and its uses to the new identity.
|ADRs 0008 and 0012; `RPK-JAVA-MOVE-003..004`; Java target-name lookup protection reviewed 2026-07-29; bounded REQ-JAVA-MAVEN-MOVE-AUTH-001/002/007/008/009/011 evidence.
|confirmed P0 architecture; bounded complete-`AVAILABLE`, observer/candidate/residual, synthetic, explicit-scope, ordinary selected-leaf, and six-case REQ-008 fail-closed rows implementation-informed; general authority remains open

|C-TECH-021
|`LEXICAL_FALLBACK` is review-only and always ineligible for managed write. Lexical discovery never supplies positive binding or mutation evidence. `PatchEngine` rejects a direct compatibility plan with lexical evidence before workspace-lock or write-ahead-log creation regardless of approval, force flags, recipes, or protocol translation. The operation-specific `JavaMoveClassOperationDispatcher` now keeps three outcomes disjoint: `Plan(JavaMoveClassPreview)` may carry managed, refused, or result-owned typed structural-refusal results and rejects lexical evidence; `Guidance(JavaMoveClassReviewOnlyGuidance)` carries REQ-003 plus the four bounded REQ-008 preview uncertainties; and `LexicalReview(JavaMoveClassLexicalFallbackReviewEnvelope)` carries REQ-004 alone. Neither non-managed outcome can become a managed plan; restoring authority requires a full reactor rescan and a new dispatcher preview.
|ADRs 0002, 0008, and 0012; `RPK-JAVA-MOVE-004..005` and `007`; `JavaMoveClassOperationDispatcher`, `JavaMoveClassReviewOnlyGuidance`, `JavaMoveClassStructuralRefusal`, `JavaMoveClassLexicalFallbackReviewEnvelope`, `RefactorKitCli.cmdMoveClass`, `PatchEngine`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003/004/005/008.
|confirmed no-lexical-promotion architecture; REQ-004's bounded direct Java outcome/CLI/recipe/daemon-session/managed-LSP-session/MCP-session parity implementation-informed; REQ-003 and the five REQ-008 preview-time rows are bounded to source-built library/CLI evidence, while REQ-008's under-lock drift row is bounded to library/`PatchEngine` acceptance; packaged/runtime/native qualification remains open

|C-TECH-022
|Effective Maven dependency-scope derivation must preserve declaration provenance. An explicitly declared transitive `test` scope is retained; a dependency-management scope default may supply scope only when that dependency declaration omits it and must not overwrite the explicit value.
|ADR 0012; `RPK-JAVA-DIAG-002..003`; Maven source-set authority.
|confirmed architecture; implemented and validated for bounded REQ-JAVA-MAVEN-MOVE-AUTH-009, while general diagnostics-authority acceptance remains planned

|C-TECH-023
|When derived transitive scope, transitive optionality, or an exact inherited path exclusion can decide an external edge without child facts, RefactorKit must classify and hash-bind that decision before child version/range, repository, POM/effective-model, or JAR lookup. Type/classifier are normalized management and variant identity, never standalone prune reasons. Ambiguous selection fails closed, and a selected edge with a missing POM, parent, BOM, management, mediation, or relocation input remains structurally unavailable. No caller-attested classpath, plugin execution, Maven settings credential, implicit network lookup, or lexical evidence may fill the gap.
|Accepted ADR 0012 descriptor-closed graph clarification; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-010/012.
|confirmed architecture; bounded selector-pruning/alternate-selected-path behavior and six selected-descriptor loss/drift cases implementation-informed; generalization remains open

|C-TECH-024
|`JavaMoveClassReviewOnlyGuidance` may represent only an intelligible authority defect whose relevant facts remain safely readable and bounded enough to enumerate. Schema version 1 binds the exact `moveClass` request, snapshot SHA-256, and deterministic canonical evidence SHA-256 over sorted blockers, candidate records, omissions, actions, and expected/observed fingerprints; UUIDs, wall-clock values, and “older hash” comparisons are excluded. Each result has at least one typed blocker with separate mandatory Maven module and source-set identity. The implemented REQ-003 codes are `java.maven.moveClass.sourceInventory.missingEntry`, `java.maven.moveClass.classpathFingerprint.mismatch`, `java.maven.moveClass.targetUse.recoveredBinding`, and `java.maven.moveClass.materializedGeneratedRootInventory.fingerprintMismatch`. The same guidance arm also carries the four bounded REQ-008 preview codes `java.maven.moveClass.candidate.unresolved`, `java.maven.moveClass.targetLookup.unresolvedPrerequisite`, `java.maven.moveClass.reverseObserverClosure.explicitOldFqnOutside`, and `java.maven.moveClass.retainedDiagnostic.identityDrift`. Effective primary Maven `sourceDirectory`/`testSourceDirectory` replacements are modeled separately from statically interpreted additive build-helper/Kotlin roots: only a replaced default Java root is removed, so additive declarations retain default roots and no Maven plugin is executed. Exact non-recovered `BOUND_TARGET` and `BOUND_OTHER` facts remain separate from unresolved/recovered Java lexical candidates, Java non-code residuals, and non-Java text residuals; every occurrence is snapshot/path/range/content-hash bound and `managedEdit=false`. Completeness is explicit; the omitted readable source is represented by the typed `SOURCE_INVENTORY_ENTRY` omission, while the other three cases are complete. Ordered typed restoration actions end with full-reactor rescan and new preview; checklist version 1 fixes seven human/VCS-owned steps. Strict format-version-1 expected-source and generated-root manifests are captured in `ProjectSnapshot.auxiliaryFiles`; their manifest content hashes and the classpath evidence manifest hash/fingerprints enter guidance evidence. Lost, unreadable, unsafe, or otherwise unbounded required structural input remains outside this guidance type. REQ-JAVA-MAVEN-MOVE-AUTH-008 independently validates those four complete preview-guidance uncertainties, one root-declared missing-active-POM structural case, and one non-managed candidate-source post-preview drift case; none broadens REQ-003. The materialized generated main root is owned by `catalog-generated-support:main`.
|`JavaMoveClassGuidance.kt`, `JavaMoveClassGuidanceEvidence.kt`, `JavaProjectScanner`, the two checked-in expected-inventory manifests, `JavaMavenMoveClassGuidanceAuthorityCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-003; independent final 2026-07-30 OpenJDK 21 `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 13s, 956 discovered, 773 executed, 183 skipped/tag-filtered, zero failures/errors, and 77 tasks executed; log SHA-256 `8d9b19576a27deaae2c12c767f929664c228db85d1dcc8644de3e41ecbe6ca9d`. This is dated mutable working-tree evidence; SpotBugs retained the configured non-blocking baseline findings.
|implementation-informed for the four bounded REQ-003 source-built library/CLI guidance defects; the shared guidance arm also carries four independently validated REQ-008 source-built library/CLI preview uncertainties, while REQ-008's structural and under-lock rows remain separate `Plan`/`PatchEngine` outcomes

|C-TECH-025
|A legacy Java/Maven `moveClass` compatibility preview with `LEXICAL_FALLBACK` is projected by the Java move-class operation outcome dispatcher as one immutable schema-v1 `LEXICAL_FALLBACK_REVIEW`. The envelope reports `semanticCompleteness=NOT_SEMANTICALLY_PROVEN`, `authorityStatus=REVIEW_ONLY`, `evidenceKind=LEXICAL_FALLBACK`, `managedWriteEligibility=INELIGIBLE`, and `blockerCode=evidence.insufficient`. Canonical JSON hashing supplies `requestSha256`, exact `snapshotSha256`, hidden `reviewArtifactSha256`, sorted-fact `evidenceSha256`, and deterministic non-capability `operationId=lexical-review-<sha256>`. The hidden review-artifact hash commits to normalized internal compatibility file operations, exact edit ranges, and replacement/create byte hashes; none of those ranges or bytes is exposed. Exposed residual records contain only normalized path, current content SHA-256, typed risk facts, and `managedEdit=false`. They are bounded to 200 records and 262,144 canonical UTF-8 record bytes, report `recordCompleteness=COMPLETE or TRUNCATED`, deterministic returned-category counts, and fixed next actions. No envelope contains a protocol plan/edit/range/replacement/apply token, pending managed plan, LSP `changes`/`documentChanges`, transaction, or rollback capability. The Java-owned LRU audit cache holds envelopes only, is capped at 128 entries/16,777,216 canonical bytes, and is cleared on relevant project, snapshot, mutation, semantic-session, initialization, and close lifecycle changes. Daemon, LSP, and MCP share `RefactoringApplyIdentity.parse`, which requires exactly one non-blank `operationId` or `planId`: a known operation returns typed `evidence.insufficient`; unknown or evicted operations and arbitrary plans return `INVALID_PARAMS`. Daemon/LSP JSON-RPC preserve structured error `data`; MCP returns equivalent structured error content. Recipes return the lexical envelope immediately without an aggregate, and a clean full-reactor JDT preview still returns the existing eligible managed `Plan` outcome.
|ADRs 0002, 0004, 0008, and 0012; `JavaMoveClassLexicalFallbackReview.kt`, `JavaMoveClassLexicalFallbackReviewJsonProjection.kt`, `JavaMoveClassOperationDispatcher`, `RefactoringApplyIdentity`, CLI/recipe/daemon/LSP/MCP handlers, and `JavaMavenMoveClassLexicalFallbackReviewCucumberTest`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004.
|implementation-informed for the bounded source-built/in-process cross-surface scenario; no packaged-process, runtime-distribution, native-platform, or general-authority qualification

|C-TECH-026
|Implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-008 is phase-partitioned. Four safely enumerable preview-time uncertainty variants return immutable schema-v1 `JavaMoveClassReviewOnlyGuidance` with `resultType=REVIEW_ONLY_GUIDANCE`, exact request identity, mutated snapshot `S0`, deterministic canonical evidence identity, complete non-truncated records, no managed edit or plan capability, and source-built CLI `guidance.nonManaged` refusal before `PatchEngine`, workspace lock, WAL, or `.refactorkit` residue. They cover an inside-closure ambiguous/problem `UNRESOLVED` candidate, an unresolved static-import-on-demand lookup prerequisite while actual candidates stay bound, an explicit old FQN outside the reverse-observer closure, and same-`S0` before-versus-staged retained-diagnostic identity drift; the last is collected by the dispatcher's second stage. None returns `LexicalReview`. A root-declared active module whose POM is missing returns `PatchStatus.REFUSED`/`RefactoringEvidence.STRUCTURAL` with result-owned typed `JavaMoveClassStructuralRefusal`, empty edits/affected files, and no lease or apply identity. Its component-wise workspace-relative `NOFOLLOW` identity distinguishes a missing final POM from a missing module directory; symlink components, a final symlink, and a non-regular final path are not classified `MISSING`. Source-built CLI `--apply` also refuses this case before lock/WAL/residue. The post-preview case starts from an eligible REQ-007 preview, changes a non-managed `BOUND_OTHER` source after lock acquisition, and refuses before WAL while preserving external `S1`; it creates no WAL, transaction, managed target edit, or destination and may retain `.refactorkit/workspace.lock`.
|`JavaMoveClassGuidance*`, `JavaMoveClassOperationDispatcher`, `JavaMoveClassStructuralRefusal`, `MavenEffectiveReactorBuilder`, `JavaProjectScanner`, `RefactorKitCli`, `PatchEngine`, `JavaMavenMoveClassAuthorityFailureCucumberTest`; accepted ADR 0012; `RPK-JAVA-MOVE-002..004`, `006..007`; corrected `@implemented-and-validated` REQ-JAVA-MAVEN-MOVE-AUTH-008.
|implementation-informed for exactly five bounded REQ-008 source-built library/CLI preview-time cases and one bounded library/`PatchEngine` under-lock acceptance case; no recipe/daemon/LSP/MCP parity or packaged/runtime/native/general qualification

|C-TECH-027
|REQ-008 blocker data is implemented as immutable layer-specific values. `java.maven.moveClass.candidate.unresolved` carries `CANDIDATE_TOTALITY`, module/source set, path/content hash, exact candidate ranges, `AMBIGUOUS`, competing FQNs, `UNRESOLVED`, and complete/non-recovered/non-truncated facts. `java.maven.moveClass.targetLookup.unresolvedPrerequisite` carries `TARGET_NAME_LOOKUP`, `STATIC_IMPORT_ON_DEMAND`, import path/range/content hash, unresolved owner, target simple name, and affected-range hash while actual candidates remain bound. `java.maven.moveClass.reverseObserverClosure.explicitOldFqnOutside` carries `CLOSURE_CONSISTENCY`, path/range/content hash/FQN/source set, `OUTSIDE`, `NONE`, closure hash, and observed `UNRESOLVED`; it takes primary precedence over generic candidate uncertainty. `java.maven.moveClass.retainedDiagnostic.identityDrift` carries `DIAGNOSTIC_IDENTITY`, `BEFORE_VS_STAGED`, before/staged provider/problem/category/severity/path/range/message identities, multiset hashes, `[problemId,message]`, `S*`, and `diskDrift=false`. `java.maven.reactorDescriptor.missing` carries `STRUCTURAL_CLOSURE`, `ACTIVE_REACTOR_POM`, module, declaring root-POM hash/range, expected path, `MISSING`, and no-follow absence identity. For freshness, generic immutable `OperationAuthorityFileEvidence` binds the non-managed `BOUND_OTHER` source and attributes; generic immutable sorted `DiagnosticDetails` reports `EVIDENCE_FRESHNESS`, `CANDIDATE_INVENTORY`, path, expected/observed content hashes, preview `S0`, observed `S1`, the expected semantic candidate-inventory hash, and expected/observed required-file-evidence hashes. `PatchEngine` uses one under-lock engine snapshot, returns primary `authorityLease.evidenceDrift` plus secondary `snapshot.scopeChanged`, and never reports an observed semantic candidate inventory/hash. Exclusions remain REQ-003/004/010/011/012 exactly as declared in Gherkin.
|`JavaMoveClassGuidance.kt`, `JavaMoveClassGuidanceCollector`, `JavaMoveClassTargetAuthority`, `Models.kt`, `PatchEngine`, corrected feature, and six-case focused OpenJDK-21 evidence: six passed, sixteen tag-filtered, zero failures/errors; independent log SHA-256 `ba7dda54ba85904ddde200982fbc41613b4a48f95bd8481bdccfad3427de6edc` (`gherkin-writer` log `6f7e2d8a9c767f265a117853267a37ab31ec31d0878637870b37202ec8ba2c71`). Independent final mutable-working-tree verification on OpenJDK 21.0.11 used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 33s; 1000 discovered, 780 executed, 220 skipped/tag-filtered, 0 failures, 0 errors; 77 tasks executed; full log SHA-256 `243b66895d262a03945e7f0c32431ccc0dd02055613ec555a2b6f359ac92491b`. The receipt does not imply packaged-process/runtime/native-platform/general authority or numerical coverage qualification; the SpotBugs baseline remains configured non-blocking.
|implementation-informed for the bounded six-case contract; final full mutable-working-tree execution evidence recorded; broader qualification remains open

|C-TECH-028
|Daemon, LSP, and MCP must each own one session-local instance of one shared, language-neutral core pending-plan store, generic over the exact surface payload and keyed by the retained `PatchPlan` identity. The store is limited to exact-payload insertion, lookup, removal, and clear. It must preserve `ProtocolLimits.MAX_PENDING_PLANS = 128`, true access-order LRU semantics in which a successful lookup refreshes recency, and silent eviction of the least-recently accessed entry when an insertion would exceed the bound. It must not choose admission eligibility, diagnostics, language or semantic-lease policy; interpret a plan ID as approval or authorization; call `PatchEngine`; centralize apply or rollback; access the filesystem; create `.refactorkit`, a workspace lock, WAL, transaction, or rollback evidence; or alter protocol responses. Daemon/LSP/MCP continue to own their exact admission, discard, apply-success, apply-refusal, rollback, snapshot-refresh, initialization/project, and close lifecycle calls. In particular, snapshot-changing lifecycle differences remain surface policy outside the store, and the current unconditional MCP importer registration remains unchanged until a separately approved behavior change.
|`modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/PendingPlanStore.kt`; `modules/refactorkit-daemon/src/main/kotlin/org/refactorkit/daemon/DaemonSession.kt`; `modules/refactorkit-lsp/src/main/kotlin/org/refactorkit/lsp/LspSession.kt`; `modules/refactorkit-mcp/src/main/kotlin/org/refactorkit/mcp/McpSession.kt`; implemented-and-validated REQ-PENDING-PLAN-STORE-001/002; focused log SHA-256 `724e1e1378219cb9f3211ade826c037edb1e31c08c688ea7142a6dc00cca4a6c`; 2026-07-31 whole-working-tree source-built verification (1002 discovered, 782 executed, 220 skipped/tag-filtered, zero failures/errors, 77 tasks) log SHA-256 `03848917d90be1c4d3b4852038bd4364d3d9dc9329c3e749f67e44a0828dcdd4`; ADRs 0002 and 0004.
|implementation-informed for the source-built/in-process bounded extraction; packaged, concurrency, and general service-extraction qualification needs review

|C-TECH-029
|The implemented language-neutral core `ManagedRollbackExecutor` must parse the supplied raw transaction ID with `TransactionId.parseOrNull` without trimming, case-folding, or other normalization; perform exactly one pre-lock `TransactionLog.loadRecord`; apply caller-selected `RollbackLookupVisibility` (`APPLIED_ONLY` for CLI/LSP/MCP and `JOURNAL_RECORD` for daemon); invoke a generic surface-owned read-only preflight guard only after a record is visible and before the engine lock; and invoke public `PatchEngine.rollback` exactly once. Every post-resolution outcome must carry the same complete resolved pre-lock `TransactionJournalRecord`, not a reload or reconstruction. A `Refused` outcome must retain diagnostic order and values in a detached, unmodifiable snapshot rather than aliasing a caller-owned mutable list. Lookup-time and rollback-call `TransactionLogException` failures must remain distinct and retain the exact exception identity, code, message, and cause. `PatchEngine` remains responsible for its under-lock reload, recovery/state/image/conflict/force checks, journal transitions, and rollback algorithm.
|ADRs 0002 and 0004; implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002 in `features/managed-rollback-executor-extraction.feature`; `modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/ManagedRollbackExecutor.kt`; inspected adoption in `RefactorKitCli.kt`, `DaemonSession.kt`, `LspSession.kt`, and `McpSession.kt`. A focused RED proved the mutable diagnostic alias defect (log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`); production now uses detached unmodifiable ordered snapshotting for `Refused` diagnostics. Final independent focused OpenJDK 21.0.11 source-built/in-process acceptance passed 8/8 scenarios and 67/67 expanded steps with 29 tasks and zero failures, errors, skips, undefined, ambiguous, or pending results (log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`). The historical rollback-scoped whole-working-tree verification used `./gradlew --no-daemon --rerun-tasks check goldenTest`: `BUILD SUCCESSFUL` in 7m 5s; 1010 tests discovered, 790 executed, 220 skipped/tag-filtered, zero failures/errors; 77 tasks; log SHA-256 `2e2b6ef136f9e4eaeedab0c7bb4de7705fbc9b116adc09603631c38d26010c96`. Slice-local SpotBugs findings are non-zero and reviewed/non-blocking: the former three `Refused`-diagnostics EI/EI2 findings are eliminated; seven core medium findings remain (five exact `TransactionLogException` identity warnings required by REQ-MANAGED-ROLLBACK-EXECUTOR-002 and two private dependency-injection collaborator-retention warnings), plus 25 glue-only Kotlin/lifecycle/generated warnings (13 NP, 1 RCN, 5 MS, 6 BC). This is dated mutable source-built evidence; it does not qualify packaged or cross-platform execution, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority. The earlier inspection at commit `27bc8a34b14caf2fce9ae06e4ad5536e49a62c01` and LLM Wiki concept `managed-rollback-executor` remain preserved as planning evidence.
|implementation-informed for the focused bounded source-built/in-process executor, historical rollback-scoped dated mutable whole-working-tree source-built aggregate, and inspected surface adoption; no packaged, cross-platform, concurrent rollback, crash/restart, general rollback authority, numerical coverage, or altered `PatchEngine` authority qualification

|C-TECH-030
|The implemented managed-apply diagnostics-gate extraction is one stateless selector in `refactorkit-jvm`, invoked with the exact pending-plan language ID, original `PatchPlan`, and current Java/Kotlin adapters. Selection invokes no diagnostics provider. Exact Java operation `JavaRenameMavenModulePlanner.OPERATION` (`java.renameMavenModule`) has first Java precedence and returns `DiagnosticsGate.lazyAuthoritative` with exact public gate identity `JavaRenameMavenModulePlanner.DIAGNOSTICS_GATE_ID` (`java-rename-maven-module-staged-reactor-v1`) bound to the retained plan and lease. No trimming, case-folding, prefix/alias matching, or relabelling is allowed; all non-matches continue through the real Maven-ownership or generic Java fallbacks. The language-neutral lazy-authoritative factory is not invoked by gate creation or ID inspection; first evaluation constructs one delegate with `LazyThreadSafetyMode.SYNCHRONIZED`, requires the returned gate to have the same ID and an authoritative provider, reuses that provider, and exposes no evaluation-observer hook. The remaining ordered branch table and exact IDs stay as documented in Chapter 8, including exact language IDs, explicit non-null Java-affected predicate, Kotlin precedence, and unchanged external gates. For SURFACE-005, `refactorkit-lsp` now depends on `refactorkit-jvm`, and `LspSession.applyPlan` calls the public five-argument selector with the exact retained plan, language `java`, the session-current Java/Kotlin adapters, and a fail-closed external resolver; no LSP-local operation branch or production observer seam was introduced. `PatchEngine` retains exclusive baseline/staged/post-image provider invocation, regression, WAL, rollback, and recovery authority.
|`DiagnosticsGate.kt`; `JavaRenameMavenModulePlanner.kt`; `ManagedApplyDiagnosticsGateSelector.kt`; implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002, REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001, and bounded SURFACE-005. SURFACE-005 baseline SHA-256 is `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; promoted feature SHA-256 is `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`. The durable pre-production focused Cucumber RED log at `docs/requirements/evidence/req005-pre-production-focused-cucumber-red.log` (SHA-256 `b7ae8c9bfbd7122404403f13b10107a46f359030187bd9459e7b26d42f400cc8`) proves only that runtime managed apply failed before the success assertions: the run failed at `JavaMavenModuleRenameSurface005Steps.kt:673` with `BUILD FAILED`, one failed test, and five skipped. Separate source/diff/domain inspection identified the former generic `java-jdt` route; that route and the producer-reported moved-type diagnostic detail are not hash-bound claims from this log. After selector adoption and independent review, the reviewer returned `PASS` with no findings for AC-005-01 through AC-005-11. The post-promotion JDK 21 source-built/in-process run through public `LspSession.dispatch` was `BUILD SUCCESSFUL` for one SURFACE-005 scenario and 47/47 passed results (Cucumber JSON SHA-256 `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54`). Existing selector acceptance remains 11/11 scenarios and 92/92 steps. Historical SURFACE-001 and promoted-tree receipts remain bounded to their stated revisions. Static analysis remains configured non-blocking, and no aggregate numerical coverage is claimed.
|implemented-and-validated for the exact source-built selector route, its direct `PatchEngine` apply/normal-rollback sequence, and the bounded source-built/in-process SURFACE-005 LSP adoption through public `LspSession.dispatch`. Public behavior beyond separately qualified rows, CLI/LSP/MCP previews, recipes, external transport/editor integration, packaged/native/cross-platform/general-Maven/release behavior, concurrency/crash qualification, numerical coverage, and broad support remain open

|C-TECH-031
|The implemented daemon/MCP workspace-snapshot extraction uses one stateless, language-neutral composition service in `refactorkit-core`, with the current primary scanner, secondary source-inventory scanner, TypeScript attacher, and optional current Kotlin attacher supplied for each invocation through language-neutral collaborators. The ordered behavior is base scan; exact `ts -> typescript`, `tsx -> typescript`, `js -> javascript`, and `jsx -> javascript` contribution; conditional source overlay; exactly one TypeScript attachment for a non-empty contribution; then optional Kotlin/JVM attachment last. An empty script contribution passes the exact base snapshot to the optional Kotlin stage without file reordering, extension union, or TypeScript attachment; with no Kotlin attacher it returns the same base instance and hash. A non-empty contribution keys files by `path.normalize()`, gives the later script entry collision precedence, sorts surviving values by `path.toString()`, and overlays only `files`, `sourceExtensions`, and `ignoredDirectories`. Scanner/attacher exceptions propagate unchanged, and composition introduces no UUID, clock input, or workspace write.
|`modules/refactorkit-core/src/main/kotlin/org/refactorkit/core/WorkspaceSnapshotComposer.kt`; source-inspected private `DaemonSession.scanWorkspace` and `McpSession.scanWorkspace` adoption; `ProjectSnapshot`; `TypeScriptBuildModelIntegration`; `KotlinJvmBuildModelIntegration`; implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`; missing-composer compile RED log SHA-256 `d2876ad413703a3895999a9377f8b7c6a713578eb8156b652bb079c70f1a6d95`; final focused acceptance log SHA-256 `08e8be47c53e6131f75637cddcd3a7092c91da56e9c0b2a8627f9a48f7a7e422` and Cucumber JSON SHA-256 `9d530ea27bbc8b6af6420b785bb6ad85d1a589a7f17ac5ef622bade27a71fd61`; final 2026-08-01 official whole-working-tree OpenJDK 21.0.11 `check goldenTest` receipt: 1029 discovered, 809 executed, 220 skipped/tag-filtered, zero failures/errors, 136 XML files, 78 tasks, log SHA-256 `0ca52ed3fd881743b361bcf14b2c7b9f90d3be7890253980389ab21f3145022f`.
|implementation-informed for the bounded source-built/in-process composer, source-inspected daemon/MCP adoption, module regressions, and dated mutable source-built aggregate. The aggregate log has no production finding attributable to `WorkspaceSnapshotComposer.kt` or the daemon/MCP adoption; static findings are non-zero, with 21 slice-local glue-only Kotlin/generated findings in `WorkspaceSnapshotComposerSteps.kt` remaining non-blocking (6 NP, 3 RCN, 12 BC), and the broader configured baseline remains non-blocking. CLI/LSP remain excluded; packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification remains open

|C-TECH-032
|The implemented Java Refactoring Preview Command boundary is Java-owned and stateless. After caller-owned decoding and required-field validation, one complete immutable command for exactly `renameClass`, `renameMember`, `moveSourceRoot`, single-file Java `organizeImports`, or `safeDelete` is dispatched with the exact supplied `ProjectSnapshot` and current `JavaLanguageAdapter`. `JavaRefactoringPreviewDispatcher` is fieldless and delegates each call through the internal `JavaRefactoringPreviewPlannerInvoker` seam to one unchanged default planner, returning its exact `PatchPlan` and preserving any escaping exception. The existing generic `JavaLanguageAdapter.applyRefactoring(RefactoringRequest)` route preserves its generic decode/refusal contract while converging on this same canonical downstream path; a second planner catalogue remains forbidden.
|`JavaRefactoringPreviewCommand.kt`; five converged routes in `JavaLanguageAdapter.kt`; implemented-and-validated REQ-JAVA-PREVIEW-COMMAND-001 through `004`; missing-API compile RED SHA-256 `3221bc7bc6aaef1a21117032ce6f22ea7c28ea8efb05a2797e2fec1beb3b5af8`; focused 8-case/94-step log SHA-256 `10c805b67306026cb4ab4a79de9b7258c712e28aaf0bb0070279edbbc44914f5` and JSON SHA-256 receipts `2059c28d57fde8f72b2e3f0de9fe6d95062c8de6cad40aeaeee3e4175a378ef6`/`999ee1a13d8577113113236e7b2241a5fac881e97327705eb1e4d1858ed83baa`; module-regression log SHA-256 `9a1cb587e7e377682a09353f470140a76e4dae1e5cb42a727f8ed346e2a0e9b1`; final whole-working-tree log SHA-256 `fddde07b954c36898252e510ad07758408f2c9b4e35b9102a5e71d7dd518d535`; targeted static log SHA-256 `8f29ec9da3ff6a96d0044423e8ac1f415b2651ce0870fab9b2b86d6f5f007c33`.
|implementation-informed for the exact bounded source-built/in-process command, focused and module evidence, dated source-built aggregate, and reviewed non-zero static classification; packaged/native/cross-platform/concurrent/crash-restart/general-orchestration/numerical-coverage qualification remains open

|C-TECH-033
|Implemented-and-validated REQ-JAVA-MAVEN-MODULE-RENAME-001 accepts one caller-declared authoritative offline Maven reactor and independently explicit old/new direct-child directories plus optional artifact intent. Renaming a directory never implies a Maven coordinate change: omitted `newArtifactId` preserves the old artifact and authorizes no dependency-coordinate edit. The first row explicitly changes `catalog-model` to `catalog-domain`. Only the child POM's direct project artifact literal, one exact direct parent `<module>` literal, and effective-model-proven literal dependency artifact origins may receive byte-preserving range edits. Exact raw-POM element-text ranges come only from maintained Woodstox 7.1.1 StAX2 location information over the same hash-bound bytes. DTD processing, external general and parameter entities, and external DTD access are disabled; inability to enforce those controls or obtain exact ranges refuses planning. POM serialization and operation-owned hand-written XML tokenization are forbidden. The parser dependency is bounded to `refactorkit-java`. The exact edit has three parser-authorized POM element-text replacements—`catalog-model/pom.xml` `9:14-9:27`, root `pom.xml` `74:12-74:25`, and `catalog-pricing/pom.xml` `13:18-13:31`—followed by two file renames. The moved POM modification precedes every child-file rename in one ordered `WorkspaceEdit`; a complete staged effective reactor and authoritative diagnostics are mandatory before preview publication. Maven/wrapper lifecycle and plugins must not execute, and settings, credentials, helpers, and network access must not be consulted. Explicit `PatchEngine` apply, one WAL transaction, and exact rollback remain the only mutation path.
|`features/java-maven-module-rename.feature`; `docs/requirements/java-maven-ownership-migration.md`; implemented production/glue evidence; independent OpenJDK 21.0.11 `PASS_FOR_PROMOTION` manifest SHA-256 `6633a8de15a320024f76311575d99f7c6d3bec877bb07ba6f05177efcc5d58d1`; focused 1/1-scenario, 50/50-step log SHA-256 `b882c9393147fb52e6106329e15b521b3ab19274eb4866a25f8737e5b27fc479` and JSON SHA-256 `d2ff5c1c87b533ad4c760274b6fd191b68f0113e71cf2b268af7831c48e65958`; Woodstox 7.1.1 JAR SHA-256 `02b9d022e9d47704ff8a7a859a0dbfd3b2882a8311eb7ff1e180f760ccda2712`.
|implementation-informed for one exact direct-library/fixture case; broad module rename/move, protocols, recipes, packaged/native/cross-platform execution, general Maven projects, native CI, release-wide support, and the broad roadmap row remain unqualified

|C-TECH-034
|Any managed edit that can change hash-bound semantic metadata within the selected bounded mode must use the implemented language-neutral authoritative snapshot diagnostics evaluation rather than treating a simulator copy with retained `S0` metadata as the post-image. One transient immutable result binds one authoritative `ProjectSnapshot` and diagnostics to the exact candidate. At construction it deep-detaches every outer and nested collection reachable through `ProjectSnapshot`, each `Module`, `BuildModel`, `BuildModule`, and `BuildSourceSet`, the diagnostics list, and every `DiagnosticDetails` map. No caller alias or exposed view may change it; its complete exposed state retains construction-time identity, including a fresh canonical hash over the exposed snapshot graph equal to the construction-time hash and unchanged complete diagnostics/details. Core preserves normalized workspace root, source/auxiliary partition, tracked paths/language IDs/bytes, `sourceExtensions`, and `ignoredDirectories`; only modules, classpath evidence, and build models may be rehydrated. On every provider result core, not the provider, normalizes and validates each `Module.root`, every module source root, each `BuildModule.root`, and build source-set root as workspace-contained. Explicit absolute external classpath evidence remains permitted only under the existing policy when core independently recomputes its current no-follow path/kind fingerprint and it exactly matches the supplied evidence; missing/unreadable evidence, path/kind drift, symlink substitution, or a fabricated fingerprint refuses before WAL. Provider authority includes neither arbitrary roots nor fabricated classpath facts. Baseline evaluation must equal `S0`/`D0`; staged evaluation must yield exact `S1`/`D1`; provider completion is followed by repeated under-lock workspace and operation-lease revalidation before WAL; rendered images equal `C1`/`S1`; schema-v8 `PREPARED` uses `S1.hash`; committed evaluation equals `S1.hash` plus `D1`; mismatch/unavailability automatically rolls back and restored evaluation equals `S0.hash` plus `D0`. Existing `DiagnosticsGate.enabled`/`disabled` behavior and existing hashes remain exact for diagnostics-only operations. The value is invocation-scoped; schema-v8 remains the sole WAL/idempotency/exact-result store, with no schema bump, new store, God service, or core language dependency.
|Implemented-and-validated REQ-AUTHORITATIVE-DIAGNOSTICS-EVALUATION-001 through `004` in `features/authoritative-diagnostics-evaluation.feature`; accepted ADRs 0002 and 0012; `docs/transactionality-audit.md`; `docs/build-model.md`; independent OpenJDK 21.0.11 `PASS_FOR_PROMOTION`; focused 16/16-scenario, 128/128-step log SHA-256 `bba1414c3f21f05a7487364af135631574a4eb0dca1668a2285f7436e205fcd2` and JSON SHA-256 `c56c28cec512a57c01e1e9aced0ea6f903b88339e661ffdc80f106044b3eb2a6`.
|implementation-informed for the four bounded authoritative-evaluation definitions/16 cases and the exact module-rename consumer; no general model-changing-operation, protocol, package/platform, release, or support qualification

|C-TECH-035
|A Kotlin companion object is nested in its enclosing type and is not an independently movable declaration. A whole enclosing-type/file move may carry it unchanged. Companion extraction is a distinct future operation. Selecting a companion as the standalone `moveDeclaration` target must return one stable typed refusal fixed by an executable requirement before implementation.
|Confirmed `v0.7.x` program decision; `docs/requirements/v0.7.x-completion-contract.md`; current `KotlinJvmMoveDeclarationPlanner` companion-target branch is negative/prototype evidence at commit `2b5aa7234cc385214acb6958834763288aefc527`.
|confirmed boundary; corrective requirement and implementation planned, current standalone path unqualified

|C-TECH-036
|T5 capability and evidence must be operation-specific. Source-file relocation, declaration move, module-specifier/project-reference migration, the three organize-import modes, extract-function/constant, inline-variable, change-signature, and any inline-function row have distinct authority. A typed TypeScript request must bind the pinned toolchain, semantic lease, exact snapshot/overlay, UTF-16 selection, preferences, returned refactor/action identity, normalized edits, and staged compiler diagnostics. Invented LSP methods or generic `moveSymbol`/`inlineMethod` labels grant no authority; dynamic or mixed JavaScript may close only through separately proven evidence or truthful stable refusal.
|Confirmed `v0.7.x` program decision; reviewed 2026-08-02 LLM Wiki concept `typed-typescript-refactor-authority` and source packet `SRC-2026-08-02-013` against commit `2b5aa7234cc385214acb6958834763288aefc527`; pinned TypeScript 5.9.3 / typescript-language-server 5.1.3.
|confirmed architecture; implementation and executable qualification planned

|C-TECH-037
|API `0.2` LSP module/identifier rename remains a version-checked client-managed proposal with no RefactorKit transaction or rollback. Only the explicit managed retained-plan apply route through `PatchEngine` may claim authorization, diagnostics-gated WAL, recovery, or rollback; the managed route retains dirty/affected-open-document refusal.
|ADRs 0002 and 0004; `docs/integration-contracts.md`; transactionality audit `TX-007`; confirmed completion-program compatibility decision.
|confirmed compatibility boundary; operation-specific acceptance planned

|C-TECH-038
|Recipe composition must preserve operation authority, not only final bytes. An authority-owning child may be applied only with its exact operation identity, plan, evidence, authority lease, auxiliary inputs, and diagnostics/evaluation gate, or through a separately proven recipe-wide authoritative evaluator over the complete final candidate. Relabeling a child as `recipe:<id>` and substituting a weaker generic gate is forbidden. Until recipe-wide evaluation is proven, at most one authority-owning mutation may be combined with non-mutating projection/summary steps.
|Confirmed `v0.7.x` program decision; reviewed 2026-08-02 LLM Wiki concept `authority-preserving-recipe-composition` against commit `2b5aa7234cc385214acb6958834763288aefc527`; ADRs 0002 and 0004.
|confirmed architecture; current composition path is negative evidence for model-changing children and corrective work is planned

|C-TECH-039
|The source-built CLI command catalogue is a versioned family of separate closed contracts. At base commit `8b361e3`, unversioned `refactorkit commands --json` truthfully returns the promoted v1 document. The approved atomic cutover changes the unversioned default to exact `refactorkit.cli-command-catalog/v2`, while exact `commands --json --schema-version 1` preserves the previously promoted v1 bytes and semantics. Selector absence means v2; the only explicit legacy selector is integer token `1`; malformed or unsupported values must be rejected without fallback, without defining a RESULT-004 error envelope or exact exit-code claim. Top-level help must therefore advertise exactly `refactorkit commands --json [--schema-version 1]`. V1 remains unchanged with all three entries `jsonSupport=catalog-only`. V2 differs only in schema identity/version and `java.renameMavenModule` reporting `jsonSupport=preview-only`; create-module and move-across remain `catalog-only`. Both documents retain the exact fixed inventory, field sets, types, producer and command ordering, route-to-operation mappings, aliases, modes, mutation authority, stability, and required arguments. Discovery/version selection performs no workspace open/scan, plan, lock, WAL, transaction, edit, or installation change. Existing `refactorkit capabilities` and `LanguageCapabilityProtocol` remain unchanged and separate.
|Initial CATALOG-001 baseline SHA-256 `56485baf0ab096ad994e18f2fa29650bbf4bf2c2098d027ccd426c8a360d0009`; approved compatibility change SHA-256 `9fee5e4230e9e00ef267ddf48b72ab133d3469cb8c9b2fa8cf5aa42e9af2024e`; CATALOG-002 baseline SHA-256 `df91e509e82c1a516f226887030702a78d0c41288ccdfe14b241942ef6fd2fea`; current promoted catalogue feature SHA-256 `f02fc091db83e8b96909e4c596bda9702204c62f2874fdf1a0fd45a01e3970cf`. The atomic Kotlin CLI implementation remains `RefactorKitCli.kt` SHA-256 `db6067bf3235191957203474da95f03c4399f4de47661af9a3dcb3ef65720246` plus `CliProtocolProjection.kt` SHA-256 `ac65359ed95dd09efaa6820b05a7c7d0c18a3b4495cc3e4f738f76d19149b50d`; no production, Java, build, or installation file changed in the post-promotion RESULT integrity correction. Shared test-only `GuardedChildJvm.kt` SHA-256 `081b01d28686118aaa4ab7e94b1e3f8e17b8b9c31b5d6a59b79c7ad99305bdc5` remains the common launcher: the standard JDK 21 parent explicitly unsets `JAVA_TOOL_OPTIONS` and `JDK_JAVA_OPTIONS`, while each bounded child self-supplies `-Djava.security.manager=allow` and inherits neither variable. CATALOG-001 process-harness/steps SHA-256 remain `693b79305620851ec0d35a66b76f48951e328567a741ecd29ca6753d16991fbe` / `eb4026a2448fbe1f83cc9b3959b7f7c1f911cfc7eaeaed46a10f2504125ace4b`; CATALOG-002 process-harness/steps remain `1ffe05b9e5c6418b3a206d4e113ea4e9ab5f72ae37f63d4046d5cac95406679e` / `d719adb927698dec637df43c7a626a60cdcdd22149686d5f3c4dc0e68af4e19d`; RESULT-001 process-harness/steps are `4d38ab55b7e006da0bd1d49aaea79796a5724acccb6c9149d07a644cb6b9b398` / `3407b8edf7658d3d5db9917e2b351b4a98afcb4ea2fa8aefe5804ec91d3bc0a4`. Current mode-`0444` post-promotion CATALOG-001 log/JSON SHA-256 `5f31ba5a9162fb4673af2cffce95e1d32bc3da8648726aa23ecde3f1766e71e7` / `1c8397c4e697558c58e997286505594016cdbc827a9df1446b2c07ef4c49ba63` pass 22/22 steps, and CATALOG-002 `b561b3ebf0e32d7dca84081af0e48bca21dd57d8b392e99af18b4f99dcfd7e21` / `96f1e3825fc933baae06b76981a135a0aee7d79b6498382c43a36aca6fbb7464` pass 31/31. Final mode-`0444` post-re-promotion RESULT-001 log/JSON SHA-256 `6ab958c33d541b79c08b2eef354a2bddfdb298d60040364f4d5c935915d14c3d` / `2e10af7f91c7fdf57f5501672dce98869baf26258b86129a35b5b4cec451f21a` pass one scenario/30 steps after the integrity check was made status-neutral; supporting fresh-`testClasses`/12-of-12 CLI unit evidence SHA-256 is `5ff927b32797f4acf60693a562b900e21d958d1b6a9166d9de91b1c52aded264`. The corrected pre-final-promotion pair `63d8d62f22317d09579abf6381a08a01c85f894e31db183c98aa52f222f3feb6` / `4996f117788f1fa53195f0822d25a92e0848e322a4e9da11723f6df36130e4ca` and earlier environment-dependent pairs remain immutable superseded historical receipts. Existing capabilities bytes remain SHA-256 `dec4087fa8b7ba038a5b9a82728ec724054ee6fd28456eb9ec637fd7d0d0d1c3`; ADRs 0002 and 0004.
|confirmed constraint and implementation-informed for the current atomic source-built revision. CATALOG-001, CATALOG-002, and RESULT-001 are `@implemented-and-validated` within separately bounded evidence. Installed/package/platform/release and wider-support qualification remain open

|C-TECH-040
|The approved `REQ-JAVA-CLI-RESULT-001` slice is limited to source-built local Linux/JDK-21/current-architecture successful preview output for public `java rename-module ... --json`. The closed `refactorkit.cli-result/v1` object has exactly, and is emitted in, this top-level order: integer `schemaVersion=1`, `command=java.renameMavenModule`, bounded `requestId`, `outcome=preview`, non-null `plan`, null `transaction`, empty `diagnostics`, and `truncated=false`; v1 has no top-level `schema` or `error`. A caller request ID must match `[A-Za-z0-9][A-Za-z0-9._:-]{0,127}` and is preserved exactly; when absent, the CLI generates one `request-`-prefixed lowercase UUIDv4 correlation value. The canonical plan projection and complete-edit digest remain correlation only and expose no internal plan ID, apply handle, approval, transaction identity, or semantic authority. Result values are transient immutable CLI protocol documents translated by a surface anti-corruption layer rather than serialized internal objects. The qualified preview exits 0, emits exactly one compact UTF-8 object plus one LF with empty stderr, creates no managed write/lock/WAL/transaction/rollback/install interaction, and leaves human preview behavior unchanged. Its successful preview and CATALOG-002's default v2 `preview-only` claim form one atomic truthfulness boundary: neither may enter production or promotion alone. CATALOG-001 compatibility, CATALOG-002, and RESULT-001 each require meaningful RED, separate revision-bound independent review, then one atomic production changeset, status-only promotion only after all pass, and joint post-promotion verification. RESULT-002 applied, RESULT-003 refusal, RESULT-004 usage/operational, RESULT-005 internal-error, and RESULT-006 closure/limits/redaction/truncation remain separate and absent.
|Initial RESULT baseline SHA-256 `8f29946c5dcb0128075764fbcd2756156d61edfad4d0fffe2f49afa526c68957`; approved decomposition/atomic-correction SHA-256 `7c8555ba5c3f393f1809136a38e86da9419234962864513bd4515001557c250c` / `40a0183ec29584724a98e2357738078e7caa8d8356c90a7c0e4cd9930ff3e73e`; current promoted feature SHA-256 `5956f93a681562f6922a9101daa8c153c9a3b8d1f7fdc260bd0070d5faf48ea5`; status-neutral canonical feature SHA-256 `b6f62831bf38c2fdaadbd02abc024abd16c8818a812678c76a1eb146642e4ed9`; meaningful RED log/JSON SHA-256 `896524674ae4745bce1dcd869dcef19de0f79b5fa46e5d06bfda88f802791a78` / `94c6e89a3879634ed2679a307ccd4f6cab942f90c428c792efd097d8edc7e5b9`. Current atomic production hashes remain `RefactorKitCli.kt` `db6067bf3235191957203474da95f03c4399f4de47661af9a3dcb3ef65720246` and `CliProtocolProjection.kt` `ac65359ed95dd09efaa6820b05a7c7d0c18a3b4495cc3e4f738f76d19149b50d`. The first post-promotion log/JSON SHA-256 `fcd32c7eb2736ba5c2d9faa65109b18f1f900886d04bf382af2c74b28eeeaa49` / `95770ab372b1c97052da80dea6d72b043da3276255d8eb6cbf63ad255105e554` remain immutable evidence that the old full-file pin failed before preview execution when the status token changed. The RESULT steps correction changed only the integrity assertions: it requires exactly one REQ tag line and one allowed status token, canonicalizes only that token to `@absent`, requires canonical SHA-256 `b6f62831bf38c2fdaadbd02abc024abd16c8818a812678c76a1eb146642e4ed9`, and rejects every other drift. The 30 Gherkin steps and all tripwires are unchanged, while assertion calls increased; steps SHA-256 is now `3407b8edf7658d3d5db9917e2b351b4a98afcb4ea2fa8aefe5804ec91d3bc0a4`. The corrected pre-final-promotion GREEN log/JSON SHA-256 `63d8d62f22317d09579abf6381a08a01c85f894e31db183c98aa52f222f3feb6` / `4996f117788f1fa53195f0822d25a92e0848e322a4e9da11723f6df36130e4ca` remain immutable superseded evidence. Final mode-`0444` post-re-promotion log/JSON SHA-256 `6ab958c33d541b79c08b2eef354a2bddfdb298d60040364f4d5c935915d14c3d` / `2e10af7f91c7fdf57f5501672dce98869baf26258b86129a35b5b4cec451f21a` passed one scenario and 30/30 steps from the explicitly scrubbed standard JDK 21 parent; supporting `testClasses` and 12/12 CLI unit evidence SHA-256 is `5ff927b32797f4acf60693a562b900e21d958d1b6a9166d9de91b1c52aded264`. The human stdout/stderr/bundle pins remain SHA-256 `b353a04f51595e749cbb7a0c7a4e5a63c355bd8182f555e6c01acdb8971a1c4c` / `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `2c38357f125f5d20b1ea9b840d2a8eff12e64337a5fe8655afa10cb5777fb31b`. Existing joint post-promotion SURFACE-002 regression log SHA-256 `41fa14a94a3d32e3434d87d91856bf4bb906de8603a47c4b24687bbe33ebbe5b` passed but remains regression-only, not fresh qualification; prior regression receipt `a345ade4a1053204c98490699773a5c8fc26344d2b433d5e1829a709f9dd7ec9` remains historical. ADRs 0002 and 0004.
|confirmed constraint and implementation-informed with `@implemented-and-validated` for the bounded source-built GREEN. RESULT-002 through `006`, installed/package/platform, fresh SURFACE-002 authority, release, and broader-support claims remain absent
|===

== Architectural Boundary Constraints

[cols="1,3,2,1",options="header"]
|===
|ID |Constraint |Evidence |Status

|C-BOUND-001
|Frontend/UI technologies are out of scope for this repository.
|`AGENTS.md` scope and non-goals.
|implementation-informed

|C-BOUND-002
|LSP is for editor-native features; advanced AI workflows use JSON-RPC or MCP instead of overloading LSP. Native LSP `WorkspaceEdit`s are version-checked, client-managed, and have no RefactorKit transaction/rollback; custom `refactorkit.applyPlan` is a distinct managed path that refuses dirty/affected-open buffers. For REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-004, the Java planner remains the upstream semantic supplier of immutable `PatchPlan`/`WorkspaceEdit` values and the LSP adapter remains the downstream protocol anti-corruption layer that projects immutable proposal/`documentChanges` documents. A returned session plan ID is transient correlation only, never approval, authorization, or transaction identity. Proposal creation and editor-owned application must not enter an operation apply gate, `PatchEngine`, workspace lock, WAL, transaction, state refresh, recovery, or RefactorKit rollback; client application is non-transactional and non-rollbackable by RefactorKit. The legacy capability refusal occurs before edit return or plan retention. SURFACE-005 `applyPlan` remains a separate managed requirement. Its LSP-owned preflight must reject any dirty buffer before checking an affected open document, and both refusals must precede fresh apply scan, gate selection, `PatchEngine`, lock, WAL, target edit, plan removal, and refresh. Only a clean/closed invocation supplies approval surface `lsp-managed-command`, passes the exact retained plan with language `java` and the session's current adapters through the five-argument managed selector, and may remove the plan or refresh after `ApplyResult.Applied`; successful rollback alone refreshes restored S0/D0. Client-managed ownership is available only after operation-specific admission: a `LEXICAL_FALLBACK_REVIEW` must expose neither `changes` nor `documentChanges` nor a managed `PlanId`.
|`AGENTS.md`, `docs/integration-contracts.md`, ADRs 0002/0004, transactionality audit `TX-007`; `LspSession`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-004; REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-004 baseline `REQ004-BDD-EVIDENCE-001` (SHA-256 `61079d0aba9a9bf156faa80eadb937cca20155eeab47006df746cbd677123c1c`) and its independent `PASS`; SURFACE-005 baseline `REQ005-BDD-EVIDENCE-001` at `docs/requirements/req005-surface005-baseline.md` SHA-256 `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; independent SURFACE-005 `PASS` with no findings for AC-005-01 through AC-005-11; promoted feature SHA-256 `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`; post-promotion SURFACE-005 Cucumber JSON SHA-256 `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54` with one scenario and 47/47 passed results.
|implementation-informed and implemented-and-validated for ordinary LSP ownership, the bounded lexical no-edit command path, the separately bounded SURFACE-004 proposal plus harness-side client application, and bounded source-built/in-process SURFACE-005 managed apply/rollback through public `LspSession.dispatch`; external transport plus packaged/native/editor integration remain unqualified

|C-BOUND-003
|MCP must expose deterministic tools and focused resources, not arbitrary filesystem access.
|`docs/integration-contracts.md`, `docs/ai-workflow.md`.
|implementation-informed

|C-BOUND-004
|C2 containers in the C4 model represent runtime entry points or intentionally separable building blocks; current library modules are not necessarily standalone deployables.
|`docs/c4/workspace.dsl`.
|implementation-informed

|C-BOUND-005
|At `v1.0.0`, core patch, documented CLI, and daemon contracts use API version `1.0`; every LSP capability/command and MCP tool/resource/prompt must be classified independently as stable or experimental.
|`docs/releases/v1.0.0-plan.md`; ADRs 0002, 0004, and 0006.
|confirmed target; planned acceptance

|C-BOUND-006
|The implemented `ManagedRollbackExecutor` is bounded transaction-resolution orchestration, not a new policy or mutation authority. Protocol/error rendering, exit or response selection, `RollbackMode` selection, snapshot or symbol-index refresh, diagnostics execution/publication, semantic-session closure, pending-plan or lexical-audit clearing, authorization/actor invention, WAL or recovery replacement, journal-schema redesign, direct workspace writes, and rollback-algorithm changes must remain outside it and surface- or `PatchEngine`-owned as they are today.
|ADRs 0002 and 0004; implemented-and-validated REQ-MANAGED-ROLLBACK-EXECUTOR-001/002; `ManagedRollbackExecutor.kt`; inspected CLI/daemon/LSP/MCP adoption; final bounded focused log SHA-256 `7e74ca9cf7080c7b8b2c32295cd17bdec193140346c48a08a4534549e85226ea`; mutable-diagnostics RED log SHA-256 `952026debab60a44de9090b524fba39e90a03de37f72e1b086a1065176fe9724`; managed rollback boundary in Chapters 4, 6, and 8.
|implementation-informed for source-built/in-process behavior and inspected surface adoption; `PatchEngine` authority is unchanged, and packaged, cross-platform, concurrent-rollback, and crash/restart behavior remains unqualified

|C-BOUND-007
|The implemented managed-apply gate selector remains a stateless gate-selection boundary in `refactorkit-jvm`. The exact module-rename branch may retain the immutable operation plan/lease solely to construct its operation-owned authoritative gate lazily; it must not reconstruct authority from JSON, text, diff, or operation-name presence. Daemon/MCP are qualified only within their bounded evidence; SURFACE-005 now adds the direct LSP call to the existing five-argument `select`, with the exact retained plan, language `java`, current Java/Kotlin adapters, and a fail-closed external resolver. `refactorkit-lsp` now depends on `refactorkit-jvm`, but the selector must not absorb LSP document preflight, pending-plan removal, refresh, rollback completion, protocol rendering, authorization, WAL, recovery, adapter/toolchain lifecycle, or any `PatchEngine` decision. It also must not acquire daemon, MCP, LSP, CLI, recipe, testkit, TypeScript, filesystem, transaction-log, or lifecycle dependencies of its own.
|Chapters 5, 6, and 8; accepted ADRs 0002 and 0004; `ManagedApplyDiagnosticsGateSelector.kt`; existing production references in `DaemonSession.refactorApply`, `McpSession.toolApplyRefactoring`, and bounded `LspSession.applyPlan`; implemented-and-validated REQ-MANAGED-APPLY-DIAGNOSTICS-SELECTOR-001/002, REQ-JAVA-MAVEN-MODULE-RENAME-SURFACE-001, and SURFACE-005. SURFACE-005 baseline SHA-256 is `0be6995539bab1088406c042b577edf5723cf27d4eb60092c39a621122c66ea0`; promoted feature SHA-256 is `793e02aa42e0f4d90f4a85382910b1e684bb80e3f8547d9c02080c4d8b6a8a17`; post-promotion focused JSON SHA-256 is `4ade22bb82466b27e7f265f29e7070a6d8f0aa6c5004a35cccee1d30a4204e54`; RED log SHA-256 is `b7ae8c9bfbd7122404403f13b10107a46f359030187bd9459e7b26d42f400cc8`, with the evidence boundary stated above.
|implemented-and-validated for the bounded selector, exact source-built module-rename selector/apply/rollback row, and direct bounded LSP adoption. Public behavior beyond separately qualified rows, CLI/LSP/MCP preview routes, recipes, external transport/editor integration, packaged/native/cross-platform behavior, concurrency/crash behavior, numerical coverage, and general support remain excluded; `PatchEngine` authority is unchanged

|C-BOUND-008
|Workspace Snapshot Composition is an application/infrastructure composition policy inside the existing Core Patch and Model Engine, not an aggregate, entity, repository, durable projection, or persistence mechanism. Core must not import Java, Tree-sitter, TypeScript, Kotlin, daemon, or MCP types; concrete wiring remains in daemon/MCP, and neither `refactorkit-jvm` nor a new orchestration module owns this slice. Surface-owned exclusions are exact: refresh/saved-state invocation timing, stored root/snapshot, watcher dirty/overflow state, refresh count, and response selection; `WorkspaceIndex` open/reconcile/contribution/generation/symbol discovery; pending plans, lexical-review audit, admission/removal/clearing, and semantic leases; toolchain/adapter discovery, start/stop/restart/reset/close, and post-apply scan-before-cleanup ordering; `PatchEngine` preview routing, diagnostics gates, authorization, lock, WAL, apply, validation, recovery, and automatic/explicit rollback; protocol capabilities, JSON/text rendering, error mapping, changed-file projection, and success/refusal responses. CLI/LSP scan and lifecycle behavior, a generalized scanner registry, stored state, index, protocol, persistence, and mutation authority remain outside the boundary.
|Chapters 4-9; `WorkspaceSnapshotComposer.kt`; source-inspected private daemon/MCP adoption; implemented-and-validated REQ-WORKSPACE-SNAPSHOT-COMPOSER-001 through `005`; delegated LATE ADR review of 0002/0004/0006/0009/0010; delegated LATE C4 validation of unchanged `docs/c4/workspace.dsl`.
|implementation-informed for the bounded source-built/in-process extraction; exact surface exclusions remain, `PatchEngine` authority is unchanged, and broader execution, concurrency, coverage, and orchestration qualification remains open

|C-BOUND-009
|The implemented Java Refactoring Preview Command boundary remains inside the existing Java Language Adapter. Daemon and MCP adopt it only after preserving their own parsing, aliases/defaults, required-field and unknown-operation validation, and protocol-specific failures. It must not absorb `moveClass` and its `Plan`/`Guidance`/`LexicalReview` algebra; Kotlin, TypeScript/JavaScript, or other external semantic routing; mixed Java/Kotlin add-parameter; experimental or Maven-special operations; CLI or LSP adoption; pending-plan admission or lifecycle; snapshot composition or refresh; diagnostics; `PatchEngine`; authorization, locking, WAL, recovery, apply, automatic or explicit rollback; adapter/session lifecycle; rendering; or protocol errors. Core must not import the Java command types. `ManagedApplyInvoker` remains rejected: preserving behavior would make it a useless four-argument `PatchEngine.apply` pass-through, while absorbing any excluded policy would create a cross-surface God service.
|`DaemonSession.kt` and `McpSession.kt` five-route adoption; the real public-session REQ-JAVA-PREVIEW-COMMAND-003 runner covers all five operations plus missing, unknown, and refused paths with no preview writes/residue. LATE delegated ADR review found no decision change; LATE delegated C4 validation found no topology/relationship change and left `docs/c4/workspace.dsl` unchanged. External protocol schemas and capability catalogues are unchanged.
|implementation-informed for the bounded source-built/in-process daemon/MCP adoption; CLI/LSP and packaged/native/cross-platform/concurrent/general-orchestration qualification remain excluded

|C-BOUND-010
|The implemented bounded Maven module-rename planner remains a stateless Java/Maven domain/application service inside the existing Java Language Adapter. Its request, raw POM documents, effective-model projection, parser-reported origin/range evidence, staged reactor, diagnostics, and returned `PatchPlan` are immutable values. Maintained Woodstox 7.1.1/StAX2 parsing is a `refactorkit-java` implementation dependency only; operation-owned hand-written XML tokenization is rejected, and core remains independent of Java, Maven, and XML-parser libraries. The planner creates no aggregate, entity, repository, cache, durable service, or new store; the existing transaction journal remains the WAL, and `PatchEngine` remains the sole mutation/rollback authority. The first row is direct-library only. Nested/profile/property-backed/inherited/ambiguous edited origins, module creation, group/version/packaging/type/classifier changes, JPMS, CLI/daemon/LSP/MCP, recipes, packaged/native/cross-platform qualification, disposable real projects, general Maven populations, native CI, and release/support claims remain outside.
|Implemented-and-validated REQ-JAVA-MAVEN-MODULE-RENAME-001 in `features/java-maven-module-rename.feature`; `docs/requirements/java-maven-ownership-migration.md`; accepted ADRs 0002, 0010, and 0012; independent 1/1-scenario, 50/50-step execution. Existing Java-adapter/core/WAL relationships cover the implemented internal responsibilities; no C4 topology change is justified.
|implementation-informed for the exact direct-library fixture boundary; every broader operation, surface, package/platform, project-population, CI, release, and support claim remains unqualified
|===

== Process and Quality Constraints

[cols="1,3,2,1",options="header"]
|===
|ID |Constraint |Evidence |Status

|C-PROC-001
|CI must run Gradle build, golden tests, self-contained CLI packaging, and a packaged CLI smoke test.
|`.github/workflows/ci.yml`.
|implementation-informed

|C-PROC-002
|Features are done only with tests, preview support, rollback when writing files, diagnostics, and documentation.
|`AGENTS.md` Definition of Done.
|planned, needs-review for enforcement coverage

|C-PROC-003
|Semantic Java refactoring must not be implemented as global search-and-replace.
|`AGENTS.md` hard rules.
|implementation-informed as design constraint; needs ongoing review

|C-PROC-004
|ADRs must be stored separately and chapter 9 must remain an index rather than rewriting ADR bodies.
|Installed arc42 agent rule; `docs/adr/`.
|implementation-informed

|C-PROC-005
|Release order is published immutable `v0.3.0`, `v0.4.0` hardening, `v0.5.0` native kernel, `v0.6.0` managed TypeScript foundation, incremental deep multi-language `0.x` bands, Clojure-complete `1.0` development, then immutable alpha/beta/RC tags and `v1.0.0`; tags must not be moved or recreated. API `0.2` remains active until an explicit migration passes.
|`docs/releases/v1.0.0-plan.md`; ADRs 0004, 0005, and 0008.
|confirmed; `v0.3.0` step implementation-informed

|C-PROC-006
|Stable acceptance must define and pass bounded scan, index, preview, apply, memory, request, result, cache, pending-plan, and transaction-metadata budgets with deterministic timeout/cancellation/limit behavior.
|`docs/releases/v1.0.0-plan.md`; ADRs 0004, 0006, and 0008.
|confirmed target; planned acceptance

|C-PROC-007
|Stable self-contained release assets require checksum, SBOM, provenance/attestation, clean-build reproducibility comparison, archive-layout verification, and `JAVA_HOME`-unset smoke evidence.
|`docs/releases/v1.0.0-plan.md`; ADR 0005.
|confirmed target; planned acceptance

|C-PROC-008
|The `RPK-JAVA-DIAG-001..007` epic is a product-critical release prerequisite. Acceptance must prove exact release platforms, full-reactor main/test visibility, source-set availability roots, exact before/staged/after/rollback evidence, non-executable discovery, and the permanent native packaged fixture before architecture or product docs claim definitive Java/Maven diagnostics implemented.
|`docs/requirements/java-maven-authoritative-diagnostics.md`; ADR 0012.
|confirmed architecture; planned release blocker

|C-PROC-009
|The P0 `RPK-JAVA-MOVE-001..007` acceptance must include a permanent offline, plugin-free reactor with one aggregator and 20 active non-aggregator modules, at least three dependency levels, direct/transitive main observers, a test-only Java Cucumber step-definition observer, unrelated and same-name decoys, a local external dependency, and a materialized generated root. Its positive `OFFLINE_MISSING` variants must prove full-reactor active-POM completeness and descriptor closure over selected external nodes, hash-bound selected/pruned outgoing-edge classifications, candidate-exclusion completeness, closure-scoped missing-binary evidence, candidate-total `BOUND_TARGET`/`BOUND_OTHER`/`UNRESOLVED` classification, exact unchanged non-concealing baseline diagnostics, zero introduced errors, one managed transaction, and byte-exact rollback. Independent rows must prove that deterministically scope/optional/exclusion-pruned child descriptors are not looked up on that path, that an alternate selected path restores descriptor/binary requirements, that a fixed-release Maven-2-layout selected graph leaf with a present complete POM closure and absent JAR can use RefactorKit-owned negative presence without an invented content hash, and that loss of an included POM/parent/BOM still refuses. The bounded negative slice separately accepts four complete preview-time guidance outcomes, one globally declared active-reactor-POM structural preview refusal, and one non-managed candidate-source required-file-evidence drift refusal after lock acquisition but before WAL. That last row preserves external drift and does not require lock-file absence; every other REQ-008 row is pre-lock and residue-free. The private field project is qualification input, not a permanent test dependency.
|`RPK-JAVA-MOVE-006`; accepted ADR 0012; field report `SRC-2026-07-29-004` plus dated Magrathea qualification at `a48e3c06`; implemented-and-validated REQ-JAVA-MAVEN-MOVE-AUTH-001 through `012`, including focused six-case REQ-008 evidence. REQ-004 remains the separate lexical-review surface contract.
|confirmed P0 requirement; all twelve bounded definitions/22 expanded fixture cases implementation-informed; the exact Magrathea CLI request remains field-qualified only on Linux/JDK 21, while generalization, broader integration shapes, packaged/runtime execution, and native-matrix qualification remain release-blocking

|C-PROC-010
|As a qualification-process constraint, not a new product requirement or case, packaged-process validation of existing `REQ-JAVA-MAVEN-MOVE-AUTH-001` reuses the same feature, stable ID, and pickle through the dedicated `packagedMavenMoveClassAuthorityTest` source set/task. The task is explicit and package-dependent; ordinary `test` and `check` remain packaging-independent. One no-follow byte copy of the permanent 20-module fixture is the consistency scope. Subject CLI processes use isolated home/temp state, a scrubbed allowlisted environment, and poison `java` first on minimal `PATH`; the real launcher and canonical `java.home` must attest the embedded runtime. The bounded oracle is repeat-equal `JDT_BINDING` preview; exact three observers and five affected paths; five top-level journal `FileEdit` entries consisting of four `Modify` plus one `Rename`; nested `TextEdit` counts `1/2/1/1` across the four modifies; a separate lock probe that observes only `Apply refused:` and `ERROR: Workspace is locked by another RefactorKit writer`, with no projected `workspace.locked` code and no runner claim of core-code correlation; one apply that replans within its own child process without any cross-process plan-ID claim; one schema-v8 transaction; clean post-apply diagnostics; normal rollback; and exact non-engine byte/path/seven-source/snapshot restoration. Output, timeout, descendant, cleanup, permanent-fixture, and package-input bounds remain mandatory.
|Implementation paths are `modules/refactorkit-cli/src/packagedMavenMoveClassAuthorityTest/**` and the dedicated Gradle task. Official final OpenJDK 21.0.11/Linux `amd64` execution of `:modules:refactorkit-cli:packagedMavenMoveClassAuthorityTest --rerun-tasks` reported `BUILD SUCCESSFUL` in 22s with 35 tasks; JUnit 22 discovered/1 executed/21 tag-filtered/0 failures or errors; and one Cucumber scenario with five background plus fourteen scenario steps, 19/19 passed, and 2/2 hooks. Packaged log SHA-256 is `1fa8cf358882196659abd76e9793214d906383cf780124580ed9f64fb3d643c2`; JSON SHA-256 is `9ea18b8638846c08a63c78c6201ef44c7205bb7a62a21da7fd184688e5ada955`. The separate source-built REQ-001 review rerun passed 22/1/21/0 with log SHA-256 `96ad297a087aa9790f00353c8647351c28d563420867f1cd66c64d91ffd3a01e`. On the same JDK, the final source-built whole-tree gate `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 6m43s: standard test tasks discovered 1045, executed 817, skipped/tag-filtered 228, and had zero failures/errors across 140 XML files; 82 tasks executed; log SHA-256 `89438b03d5228f8ff8173b82a5dc587a36b0907d476dd2adffceff54bca874ef`. The dedicated packaged task is reported separately and is intentionally absent from those standard-test totals. Targeted custom-source-set PMD/SpotBugs tasks passed (log SHA-256 `2cf542051224e2e81f81b5f7ec8132a59ef04258ff2902346076ab39b25c0ff2`). Static classification is intentionally non-zero: exactly 99 reviewed/non-blocking packaged glue/harness SpotBugs findings—57 NP, 14 MS, 12 BC, 6 AT, 9 RCN, and 1 UrF—are test/deployment validation-adapter findings, mostly Kotlin `lateinit`/generated-representation and conservative single-thread-harness concurrency warnings. No production source file was added or modified by this slice; the wider static baseline remains non-blocking. Aggregate static cleanliness and numerical or child-JVM coverage are not claimed.
|implementation-informed locally and by exact run `31336067318`: Linux x86-64, Windows x86-64, macOS x86-64, and macOS arm64 passed public REQ-001 plus the complete production-JAR matrix and emitted fail-closed receipts; all rows are `PASSED`. Magrathea/general populations, crash/concurrency, archive trust/SBOM/signing, numerical child-JVM coverage, unexercised deployment shapes, and a green whole-repository CI claim remain excluded

|C-PROC-011
|Production implementation for remaining K5, T5, J1, or I1 work may proceed only while the all-module source/test compilation gate is green. A narrowly scoped scaffolding correction must make OpenJDK 21 `./gradlew --no-daemon testClasses` pass without hiding the RED through selective compilation or tests.
|The historical clean pushed `HEAD=origin/main=2b5aa7234cc385214acb6958834763288aefc527` failed before compilation because `refactorkit-kotlin:testRuntimeClasspath` was resolved before later `testImplementation` mutation (log SHA-256 `408d45e7e2c1bea4152c220af283455421a3bee84dca2b3f069832e9541983d5`). The authorized slice reproduced RED (SHA-256 `72d7920f6e7609f3d7c3855c8c946f8fb7498cd5c1a4f625a29b149181145917`), changed only `modules/refactorkit-kotlin/build.gradle.kts` to defer that resolution into `tasks.test.doFirst`, and passed the GREEN rerun (SHA-256 `1c9819e09ec44341b708fd747f5ee1b2656cae296a5edcb3d2d3b8ef66c3b745`). Full OpenJDK 21 `check goldenTest` then passed 82/82 tasks and 1,062 tests with 228 skipped and zero failures/errors (log SHA-256 `48ac4ede4257552295b41c824721f845c609af6a58b3bb1bcd3e0a7924a02286`). Actions runs `30746237578` and `30749086970` remain failed historical observations. Their shared Linux `build`-job **Verify CLI runtime distribution** cause is separate from this test-compilation gate: root `distCliRuntimeZip` depended on the directory-only `:modules:refactorkit-cli:refactorkitRuntimeDist` task and did not create the expected archive. The implementation slice changes only root `build.gradle.kts` to depend on `:modules:refactorkit-cli:refactorkitRuntimeZip`; Chapter 7 records the bounded local archive receipt. Final JDK 21 source-built verification with `./gradlew --no-daemon --rerun-tasks check goldenTest` reported `BUILD SUCCESSFUL` in 7m 28s with 82/82 tasks; log `/tmp/refactorkit-runtime-zip-task-final-check.log`, SHA-256 `009dd3940c1042cc6d1d97e18709795760d6eb4025b685f1be4c71e7e1fccae4`. This receipt remains source-built evidence and does not qualify CI, native, or release behavior. Post-commit CI for this correction and all unqualified completion work remain open.
|confirmed P0 gate, resolved for local test-compilation admission; the separate Linux packaging-scaffold cause is also corrected locally; post-commit CI and release qualification open

|C-PROC-012
|Every `v0.7.x` release promotion must reconcile one immutable release evidence identity across repository, tag, full commit SHA, clean/version state, platform/architecture, archive name/hash, extracted runtime/tree identity, fixture/request/scenario and toolchain identities, operation-family report hashes, SPDX SBOM hash, and provenance/SBOM attestation subject digest. Final native assets must be independently downloaded, verified, launched without global Java, and exercised through representative managed apply/rollback. Local/source-built runs, queued/running CI, and intermediate Actions artifacts are separate subjects and cannot substitute.
|`docs/requirements/v0.7.x-completion-contract.md`; reviewed 2026-08-02 LLM Wiki concept `release-evidence-identity` against commit `2b5aa7234cc385214acb6958834763288aefc527`; ADR 0005.
|confirmed release constraint; I1 implementation/qualification planned

|C-PROC-013
|All still-open K5, T5, J1, and I1 rows in the `v0.7.x` completion contract remain release-blocking and must be closed independently by exact acceptance evidence or by an explicitly permitted stable refusal. Source presence, a parent checkbox, or another operation's platform evidence cannot promote a row.
|`docs/requirements/v0.7.x-completion-contract.md`; `docs/releases/v0.7.0-plan.md`; requirements appendix completion catalogue.
|confirmed release scope; completion blocked
|===
