Verified detached, clean `HEAD`:

`ce65175a4aed6c12920792dd283a6a4480272c86`

Merge-base: `bbaf93df170935c3af9ae4d45afa4f2af88dc213`.

## Blocking findings

### HIGH — Callable-reference consumers can silently rebind during move

`KotlinCompilerUsageExtractor.java:371-372` discards source-backed callable references. `KotlinJvmMoveDeclarationPlanner.kt:95-134` checks callable references only inside moved declarations, while `:278-286` derives consumers solely from projected usages. The zero-consumer checks at `:367-386` then pass.

Counterexample:

```kotlin
package javax.swing.SwingUtilities
fun isEventDispatchThread(): Boolean = true
```

```kotlin
package consumer
import javax.swing.SwingUtilities.isEventDispatchThread
val ref: () -> Boolean = ::isEventDispatchThread
fun main() { print(ref()) }
```

Moving the function to `moved` applied successfully with no diagnostics. The unchanged import rebound to the JDK static method:

```text
before: true
after:  false
```

This directly violates `kotlin-jvm-move-top-level-function-and-companion-refusal.md:14`.

Probe: `/tmp/probe-k5-move-callref-consumer.py`
SHA-256: `e22627fc83fe9d37b9d0d3e5bc68b9ec8799993dbab09d193304be9dbbb6020a`

### HIGH — Organize-imports still omits enum fields and aliased properties

`KotlinCompilerUsageExtractor.java:311-368` handles fields, constructors, functions and properties but has no external enum-entry path; source enum entries are explicitly discarded at `:408`. Alias projection at `:578-603` models aliases through type identities, while `:428-433` has a mismatch fallback only for functions, not properties. Nevertheless, `KotlinCompilerBridgeMain.java:178` reports `usagesComplete=true`, which `KotlinOrganizeImportsPlanner.kt:52-115` trusts.

Two applied, diagnostics-clean changes were reproduced:

1. `TimeUnit.SECONDS` plus `ChronoUnit.*`:

```text
before: java.util.concurrent.TimeUnit
after:  java.time.temporal.ChronoUnit
```

2. A source property imported as `SECONDS` plus `ChronoUnit.*`:

```text
before: kotlin.String
after:  java.time.temporal.ChronoUnit
```

In both cases RefactorKit removed the used explicit import as “compiler-proven unused.” This violates the complete property/alias usage contract at `kotlin-jvm-organize-imports-callables-formatting.md:7-14`.

Probes:

- `/tmp/probe-k5-enum-rebound.py` — `4cee58ffb423157eaaca28ecfeb531342f82d2a599ad4937b0664a30f2b07de9`
- `/tmp/probe-k5-organize-alias-property-enum.py` — `bcd83f9abc979141657f4095fb7d17b24980351a831cd1fcb426c7fd73de81d0`

### HIGH — Custom Maven generated roots receive mutation authority

Build-helper roots are accepted as ordinary additional roots at `MavenEffectiveReactorBuilder.kt:537-594` and `JavaProjectScanner.kt:78`. Generated classification is restricted to the fixed path list at `JavaProjectScanner.kt:618-635`, propagated at `:386-389`.

Consequently, checks in:

- `KotlinExtractMethodPlanner.kt:39-43`
- `KotlinInlineMethodPlanner.kt:52-56`
- `KotlinChangeSignaturePlanner.kt:159`

trust a false `generated=false`.

With a valid build-helper `add-source` execution targeting `target/custom-generated`:

```text
extract-method --apply    -> applied
inline-method             -> PREVIEW
change-signature --apply  -> applied, diagnostics []
```

The parameter rename physically rewrote the generated file. This violates the generated-source boundaries in the change-signature, extract/inline, and platform-matrix requirements.

Probe: `/tmp/probe-k5-custom-generated-param.py`
SHA-256: `ce9b281e84e3e9e7d3fabf3ffd52a38cef347f3b35778e43b55d2f41eed76351`

### HIGH — Maven compiler plugins are not detected and mutations remain enabled

`MavenEffectiveReactorBuilder.kt:500-530` records only Kotlin-plugin presence, JVM target and JDK; it never projects `<compilerPlugins>` or kapt goals. `JavaProjectScanner.kt:230` consequently labels such Maven projects as JVM, while `JvmBuildModelProviders.kt:311-324` sees no plugin IDs. The refusal at `KotlinJvmBuildModel.kt:127-136` therefore never activates.

A valid Kotlin Maven all-open configuration, including `kotlin-maven-allopen`, custom annotation and plugin option, produced:

```text
diagnostics: ready, []
extract-method: PREVIEW
```

This contradicts `kotlin-advanced-shapes-and-platform-matrices.md:29-34`, which requires compiler-plugin semantics to remain a distinct refusal case.

Probe: `/tmp/probe-k5-maven-allopen.py`
SHA-256: `34807117871815749ef9d9e06165c83341614b9453d95e85560eec4e1d7f4735`

## Qualification defects

- The move allowlist at `scripts/finalize-native-k5-move-next.py:18-48` omits the callable-reference consumer rebound above.
- The completion allowlist at `scripts/finalize-native-k5-completion.py:34-64` omits enum/alias rebound, custom generated roots, and Maven compiler-plugin detection.
- `scripts/finalize-native-k5-completion.py:73-125` also omits the two exact pinned CLI compatibility resources named and hash-bound at `java-cli-command-catalog-k5-capability-projection-change.md:9-10`; the dedicated native command at `.github/workflows/ci.yml:630-631` does not run their Cucumber oracles.
- `v0.7.0-support-matrix.md:300` still says the move gate awaits 22 cases; the sealed gate requires 23.

## Prior-defect verification

The focused 23-case move and 22-case completion suites passed. All three packaged smokes passed, including legacy operation-less add-parameter compatibility. Both finalizer suites passed: 15 move tests and 9 completion tests. The previous static-field, parameter-capture, imported-helper, conventional generated-root, private-helper callable-reference, and implicit-convention regressions now refuse as intended.

These passes demonstrate that the current native oracles do not cover the new counterexamples.

Repository remained detached and clean; no tracked or untracked repository files were modified.

AUDIT: DEFECTS_FOUND
