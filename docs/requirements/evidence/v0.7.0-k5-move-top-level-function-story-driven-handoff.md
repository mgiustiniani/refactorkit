# Story BDD handoff — REQ-KOTLIN-MOVE-FUNCTION-001 (slice completion)

Returned to `bdd-java-planner`. All four independent-reviewer findings for the Story BDD
slice are closed with executable evidence. Story BDD work only: no dataset files were
written, no raw event-stream trajectory exists, and nothing was delegated to `dataset-writer`.

## Reviewer findings closed

| Finding | Status | Evidence |
|---|---|---|
| RQ-KMF-RED-002 (BLOCKER) | CLOSED | genuine executable tests-only RED captured BEFORE production change; minimal `packageEdit` fix; RED test now GREEN |
| RQ-KMF-CUC-001 (HIGH) | CLOSED | implicit-PUBLIC positive fixture genuinely exercised (glue), GREEN |
| RQ-KMF-CUC-002 (HIGH) | CLOSED | positive private-helper co-location fixture, helper bytes + line endings preserved exactly, GREEN |
| RQ-KMF-CUC-003 (HIGH) | CLOSED | unresolved/recovered/truncated consumer fixtures now DISTINCT semantic states, each refuses `kotlin.symbolCompilationFailed` |

## RQ-KMF-RED-002 (executable tests-only RED)

Prior RED log was an absence-only check. Replaced it with a genuine executable failing result:

- Fixture: a compiler-proven public top-level function whose source file carries a trailing
  line comment on its package declaration (`package fixture.pricing // note`). Such a file
  compiles cleanly and the AC-FUNCTION-001 contract requires the preview to edit only the
  package token while preserving every other source byte.
- Before any production change, `KotlinJvmMoveDeclarationPlanner` refused with
  `kotlin.movePackageDeclarationInvalid` because `packageEdit` required an exact package line
  with no trailing comment.
- Failing command / observed result (recorded in the RED log):
  `JAVA_HOME=/usr/lib/jvm/java-21-openjdk ./gradlew --no-daemon :modules:refactorkit-jvm:test --tests "org.refactorkit.jvm.movetoplevelfunction.KotlinJvmMoveTestsOnlyRedTest"`
  -> `BUILD FAILED`, `AssertionFailedError: ... got REFUSED refusal=kotlin.movePackageDeclarationInvalid`.
- Minimal, non-weakening production fix: `packageEdit` kotlin terminator now allows a trailing
  line comment (`"(?:[ \\t]*;|[ \\t]*//.*$|[ \\t]*$)"`); only the package token is replaced, so
  the comment is preserved byte for byte. Java terminator unchanged.
- Post-fix: RED test passes (GREEN); Cucumber 29 scenarios / 109 steps all passed; full
  `:modules:refactorkit-jvm:test` BUILD SUCCESSFUL (no regression).

## RQ-KMF-CUC-001/002/003 (glue)

Empirical probes proved production ALREADY supports implicit-PUBLIC, lowercase-facade
(`fun computeinvoiceTotal()` -> SEMANTIC_PREVIEW), and positive private-helper co-location
(called and uncalled). The reviewer's "not yet supported" premise for those shapes was
empirically incorrect; the CUC findings were closed by genuinely exercising them:

- AC-FUNCTION-001 fixture now uses implicit PUBLIC (`fun computeInvoiceTotal()`, no explicit
  `public` modifier) plus a real compiler-proven private helper `private fun taxRate(): Double = 0.21`;
  glue asserts no explicit `public` modifier and helper presence.
- AC-FUNCTION-006 fixture also carries the private helper so the shared "line endings and every
  private helper byte remain exact" step is genuinely exercised for apply/rollback.
- AC-FUNCTION-004 unresolved/recovered/truncated consumers now write distinct semantic states:
  unresolved (no import), recovered (import resolves but call recovered/errored by a return-type
  mismatch), truncated (call expression cut off); each refuses the actual code
  `kotlin.symbolCompilationFailed`.

## Verification commands (all GREEN)

```
JAVA_HOME=/usr/lib/jvm/java-21-openjdk ./gradlew --no-daemon :modules:refactorkit-jvm:test --tests "org.refactorkit.jvm.movetoplevelfunction.KotlinJvmMoveTestsOnlyRedTest"
  -> BUILD SUCCESSFUL (1 test passed)
JAVA_HOME=/usr/lib/jvm/java-21-openjdk ./gradlew --no-daemon :modules:refactorkit-jvm:test --tests "org.refactorkit.jvm.movetoplevelfunction.KotlinJvmMoveTopLevelFunctionCucumberTest"
  -> BUILD SUCCESSFUL, scenarios=29 steps=109 passed=109 failed=0
JAVA_HOME=/usr/lib/jvm/java-21-openjdk ./gradlew --no-daemon :modules:refactorkit-jvm:test
  -> BUILD SUCCESSFUL (full module, no regression)
git diff --check  -> clean
```

## Files changed (post-edit SHA-256)

- `modules/refactorkit-jvm/src/test/kotlin/org/refactorkit/jvm/movetoplevelfunction/KotlinJvmMoveTopLevelFunctionSteps.kt`
  pre `3ec845e9c7c3c3876a675705495caef300f8db140e34f3f59e63a575a0eb2747`
  post `45d7024766dd1daf82a7b7fa0cddb1767a17b997abf60927231b3a691c8adc5e`
- `modules/refactorkit-jvm/src/main/kotlin/org/refactorkit/jvm/KotlinJvmMoveDeclarationPlanner.kt`
  pre `a21248752203721302cd2ac9b40afd6f7cbd496bc766b7ee62be6acf7a12441c`
  post `768300476234c367caea017c0462c3c43ccc8e654692617474af2b6f36ece982`
- NEW `modules/refactorkit-jvm/src/test/kotlin/org/refactorkit/jvm/movetoplevelfunction/KotlinJvmMoveTestsOnlyRedTest.kt`
  post `3b36d6c9d952fa84c701ae44ad44968cbabac35be5432bbbf6cfac34e168f10a`
- Evidence: RED log (replaced)
  `docs/requirements/evidence/v0.7.0-k5-move-top-level-function-tests-only-red-08dd3c3.log`
  post `45a2a1e4d0be90bfab9a2d342a4c2a08aa0bdf536d71c2d4da66b4cbd7617bed`
- Evidence: GREEN log (new)
  `docs/requirements/evidence/v0.7.0-k5-move-top-level-function-green-tests-only-red-resolved.log`
  post `f97e833151ef73c6c936bfd2d9b6e894a28d60fc284434cad82dc89848548842`

## RED / GREEN evidence

- RED: `docs/requirements/evidence/v0.7.0-k5-move-top-level-function-tests-only-red-08dd3c3.log`
  (executable failing result, `kotlin.movePackageDeclarationInvalid`, captured BEFORE production change).
- GREEN: `docs/requirements/evidence/v0.7.0-k5-move-top-level-function-green-tests-only-red-resolved.log`
  (RED test GREEN + Cucumber 29/109 + full module GREEN).

## Status

Declared status stays `@partial` (anti-fake). Promotion to `@implemented-and-validated` remains
blocked pending the independent `requirements-quality-reviewer` PASS and any baseline/CI gates.
The executable tests-only RED now exists; it does not by itself upgrade the declared status.

## Note on the RED fixture style

The executable RED fixture (`KotlinJvmMoveTestsOnlyRedTest.kt`) is a focused tests-only RED
capture for a NEW behavior (trailing-comment package declaration) that has no Cucumber scenario
yet. The feature file is `gherkin-writer`-owned, so a permanent Story BDD scenario for this
behavior would require a `gherkin-writer` feature change. If the workflow's
no-direct-JUnit-behavior-tests rule is enforced strictly for this fixture, it can be removed and
the behavior covered by a `gherkin-writer` scenario; the RED/GREEN evidence remains in the logs.

## Provenance

- implementationPromptVersion: bdd-java-story-driven-v8
- workflowName: bdd-java
- toolRegistryStatus: TOOL_REGISTRY_UNAVAILABLE (no tool registry fingerprint supplied in this environment)
- captureMode: UNAVAILABLE (story-driven has no persistent event-stream trajectory; no harness trace variables supplied)
- implementationTraceId: UNAVAILABLE
- rawEventStreamLocation: UNAVAILABLE
- baseCommit: 08dd3c3eeb8eac371ad7316fc073c38f90474b31
- baseTree: 87da8d8347303608fede4021f8528cfac238ae4d
- finalCommit: UNAVAILABLE (changes uncommitted in working tree; harness/planner will commit)
- finalTree: UNAVAILABLE
- datasetRecordingRequested: false (mandatory for every story-driven handoff)
- datasetWriterStatus: NOT_APPLICABLE_STORY_DRIVEN
