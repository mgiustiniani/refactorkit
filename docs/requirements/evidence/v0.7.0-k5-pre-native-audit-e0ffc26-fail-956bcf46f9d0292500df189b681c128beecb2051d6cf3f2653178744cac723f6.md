## Blocking finding

### Nested generic typealiases still silently rebind in both gates

`KotlinCompilerUsageExtractor.java:514-552` checks `isTypealiasExpansion()` only on the outer `ConeKotlinType`. For `List<Chosen>`, the outer type is `List`; nested type arguments are never recursively inspected. `typeIdentifier()` also selects only the outer `KtUserType` at `:818-830`. The `FirTypeAliasSymbol` qualifier check at `:554-570` catches forms such as `Chosen::class`, explaining why the new oracle passes, but not nested generic arguments.

The resulting incomplete catalogue is trusted by:

- move equivalence: `KotlinJvmMoveDeclarationPlanner.kt:255-264,357-367,706-729`;
- organize-imports fingerprinting: `KotlinOrganizeImportsPlanner.kt:74-115,156-213`.

The added tests cover only `Chosen::class`:

- `KotlinJavaPublicTypeRenamePlannerTest.kt:501-536`;
- `KotlinCompilerDiagnosticsTest.kt:492-517`;
- packaged equivalents at `smoke-packaged-k5-move-next.py:319-368` and `smoke-packaged-k5-completion.py:529-556`.

#### Executable user-typealias counterexample

Probe `/tmp/probe-e0ffc26-nested-alias-parameter-rebound.py`:

- SHA-256: `11f3829105fd4b22d2dce129117ab72cbe3cff85b227e23e5f40e573cac28e78`
- output SHA-256: `a9a36fe03f1024f3d946633fd88e2c62ca2625e22ce836e85f28466e231ee6b9`

Move fixture used a plain public `selected()`, one private helper parameterized as `List<Chosen>`, an exact explicit Kotlin consumer import, accepted external risk, and no generated/plugin shape. `Chosen` was a source-package alias for `TimeUnit`; the destination contained a regular `Chosen`.

Result:

- `move-declaration --apply`: exit `0`, `status=applied`;
- source renamed and consumer import updated;
- WAL transaction written;
- class-file generic signature changed from
  `List<? extends java.util.concurrent.TimeUnit>`
  to `List<target.api.Chosen>`.

The organize-imports variant removed `import source.lib.Chosen`, retained `other.shadow.*`, returned `status=applied`, wrote WAL, and changed the exact generic signature to `List<other.shadow.Chosen>`.

#### Executable stdlib-typealias counterexample

Probe `/tmp/probe-e0ffc26-stdlib-nested-alias-rebound.py`:

- SHA-256: `23ece4aa9ae54c9e5ba8738f05bf2112c0e82d0605399b976cd89fb875403883`
- output SHA-256: `e8c2b201735f00f540e5ab23f4200d8d5c70c9cc9bb852dec6a73b9c0324a0f3`

Using the stdlib `kotlin.collections.ArrayList` alias:

- move applied and changed `List<java.util.ArrayList<String>>` to `List<target.api.ArrayList<String>>`;
- organize imports applied and changed it to `List<other.shadow.ArrayList<String>>`;
- both wrote transactions.

A broader FIR matrix also returned `ready` for nested and nested-nullable forms such as `List<Chosen>` and `Map<String, Chosen?>`, rather than typed refusal.

This violates exact outbound identity at `kotlin-jvm-move-top-level-function-and-companion-refusal.md:15` and complete counterfactual identity at `kotlin-jvm-organize-imports-callables-formatting.md:9-14`. It also makes the pending claims at `v0.7.0-support-matrix.md:79-80` inaccurate.

## Other verification

- Exact candidate is clean, single-parent SHA `e0ffc264…`, with direct parent `a8822c28…`.
- Move requirement/sidecar hash: `52dd7de8…`.
- Completion aggregate/sidecar: `f49d3989…`; all four leaf and three approved-change hashes match.
- No roadmap checkbox was promoted (`v0.7.0-plan.md:609-636`); support remains pending.
- Packaged CLI reports `API 0.2`.
- Advanced-shape capabilities retain `MutationAuthority.NONE`; unsupported matrices remain refused (`KotlinLanguageAdapter.kt:248-388`).

### Exact oracles

Independently parsed XML:

- move: exactly 3 reports / 25 unique cases, zero skips, failures, or errors;
- completion: exactly 5 reports / 24 unique cases, zero skips, failures, or errors.

Finalizer contracts passed 16/16 and 9/9. Packaged move, completion, and legacy Kotlin smokes each produced one exact terminal marker. Relevant full Java/Kotlin/JVM tests passed: 422 discovered, 10 skipped, zero failures/errors.

Both local finalizer probes accepted the exact 25/24 report sets, seven subject JARs, exact Linux platform, clean non-merge SHA, and host/embedded OpenJDK `21.0.11+10`. Both subject maps include `JavaProjectScanner`, `MavenEffectiveReactorBuilder`, `KotlinJvmBuildModel`, its test, and `KotlinCompilerUsageExtractor` (`finalize-native-k5-move-next.py:105-113`; completion `:106-120`).

### Maven `-Xplugin`

The a8822 `-Xplugin` defect is fixed for tested forms. `MavenEffectiveReactorBuilder.kt:530-585` examines plugin- and execution-level effective configurations and projects only `external-xplugin`; scanner/projector refusal follows at `JavaProjectScanner.kt:232-237` and `KotlinJvmBuildModel.kt:127-135`.

An eight-variant probe covered equals, split argument, space, direct text, CDATA, property interpolation, compile execution, and test execution. Every case refused with `kotlin.compilerPluginsUnsupported`, leaked no plugin path, and wrote nothing.

No fresh four-host receipts were treated as evidence.

Hygiene note: the Git-visible checkout remained clean. One initial focused Gradle invocation accidentally refreshed ignored build/test output in the checkout; all authoritative reruns and adversarial fixtures were under `/tmp`, and no tracked file changed.

MOVE_NEXT verdict: FAIL_PRE_NATIVE
K5_COMPLETION verdict: FAIL_PRE_NATIVE
Overall verdict: FAIL_PRE_NATIVE

PASS, if later issued, is not promotion, native qualification, release, whole-repository CI, SBOM/signing, installed-runtime approval, or roadmap closure.
